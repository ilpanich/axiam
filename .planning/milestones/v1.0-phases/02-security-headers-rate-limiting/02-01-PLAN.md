---
phase: 02-security-headers-rate-limiting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-api-rest/src/middleware/security_headers.rs
  - crates/axiam-api-rest/src/middleware/mod.rs
  - crates/axiam-api-rest/src/server.rs
  - crates/axiam-api-rest/tests/security_headers_test.rs
  - docker/nginx.conf
autonomous: true
requirements: [REQ-2]

must_haves:
  truths:
    - "Every API response includes X-Content-Type-Options: nosniff"
    - "Every API response includes X-Frame-Options: DENY"
    - "Every API response includes Referrer-Policy: strict-origin-when-cross-origin"
    - "Nginx frontend config includes CSP with script-src self only"
    - "Nginx frontend config includes HSTS with max-age=31536000 and includeSubDomains"
    - "Nginx frontend config includes Permissions-Policy disabling unused browser features"
  artifacts:
    - path: "crates/axiam-api-rest/src/middleware/security_headers.rs"
      provides: "SecurityHeadersMiddleware Transform+Service pair"
      exports: ["SecurityHeadersMiddleware"]
    - path: "crates/axiam-api-rest/tests/security_headers_test.rs"
      provides: "Integration tests for security headers"
      min_lines: 30
    - path: "docker/nginx.conf"
      provides: "Updated nginx config with CSP, HSTS, Permissions-Policy"
      contains: "Content-Security-Policy"
  key_links:
    - from: "crates/axiam-api-rest/src/server.rs"
      to: "crates/axiam-api-rest/src/middleware/security_headers.rs"
      via: ".wrap(SecurityHeadersMiddleware)"
      pattern: "\\.wrap\\(SecurityHeadersMiddleware\\)"
---

<objective>
Add OWASP-recommended security headers to all backend API responses via Actix-Web middleware, and add CSP/HSTS/Permissions-Policy to the nginx frontend config. This fully satisfies REQ-2.

Purpose: Protect against clickjacking (X-Frame-Options), MIME sniffing (X-Content-Type-Options), referrer leakage, XSS (CSP), downgrade attacks (HSTS), and unnecessary browser features (Permissions-Policy).
Output: SecurityHeadersMiddleware in axiam-api-rest, updated nginx.conf, integration tests proving headers are present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-headers-rate-limiting/02-CONTEXT.md
@.planning/phases/02-security-headers-rate-limiting/02-RESEARCH.md

<interfaces>
<!-- Existing middleware pattern to follow exactly -->

From crates/axiam-api-rest/src/middleware/csrf.rs:
```rust
pub struct CsrfMiddleware;
// Implements Transform<S, ServiceRequest> for CsrfMiddleware
// with CsrfMiddlewareService<S> as the Service wrapper

pub struct CsrfMiddlewareService<S> {
    inner: S,
}
```

From crates/axiam-api-rest/src/middleware/mod.rs:
```rust
pub mod csrf;
// Add: pub mod security_headers;
```

From crates/axiam-api-rest/src/server.rs:
```rust
pub fn register_api_v1_routes<C: surrealdb::Connection>(cfg: &mut web::ServiceConfig) {
    // Middleware is registered via .wrap() on App or scope
    // SecurityHeadersMiddleware goes on the App level (outermost)
}
```

From docker/nginx.conf (current security headers — lines 21-24):
```nginx
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SecurityHeadersMiddleware and integration tests</name>
  <files>
    crates/axiam-api-rest/src/middleware/security_headers.rs
    crates/axiam-api-rest/src/middleware/mod.rs
    crates/axiam-api-rest/tests/security_headers_test.rs
  </files>
  <read_first>
    crates/axiam-api-rest/src/middleware/csrf.rs
    crates/axiam-api-rest/src/middleware/mod.rs
    crates/axiam-api-rest/src/server.rs
    crates/axiam-api-rest/tests/health_test.rs
  </read_first>
  <behavior>
    - Test 1: GET /health response includes header "x-content-type-options" with value "nosniff"
    - Test 2: GET /health response includes header "x-frame-options" with value "DENY"
    - Test 3: GET /health response includes header "referrer-policy" with value "strict-origin-when-cross-origin"
    - Test 4: All three headers present on a single response simultaneously
  </behavior>
  <action>
    Create `crates/axiam-api-rest/src/middleware/security_headers.rs` following the CsrfMiddleware pattern from `csrf.rs`:

    1. Define `pub struct SecurityHeadersMiddleware;`
    2. Implement `Transform<S, ServiceRequest>` for `SecurityHeadersMiddleware` where S: Service<ServiceRequest, Response = ServiceResponse<B>, Error = actix_web::Error> + 'static, B: 'static. The Transform type is `SecurityHeadersService<S>`, InitError is `()`, Future is `std::future::Ready<Result<Self::Transform, Self::InitError>>`.
    3. Define `pub struct SecurityHeadersService<S> { inner: S }`
    4. Implement `Service<ServiceRequest>` for `SecurityHeadersService<S>` — the `call` method awaits `self.inner.call(req)`, then inserts three headers on the response via `res.headers_mut().insert(...)`:
       - `HeaderName::from_static("x-content-type-options")` => `HeaderValue::from_static("nosniff")`
       - `HeaderName::from_static("x-frame-options")` => `HeaderValue::from_static("DENY")`
       - `HeaderName::from_static("referrer-policy")` => `HeaderValue::from_static("strict-origin-when-cross-origin")`
    5. The Future type for the Service impl is `Pin<Box<dyn Future<Output = Result<Self::Response, Self::Error>>>>` — same as CsrfMiddleware.

    IMPORTANT: Unlike CsrfMiddleware, this middleware NEVER short-circuits — it always passes through to the inner service and only modifies the response. No `EitherBody` needed. Use `ServiceResponse<B>` directly.

    Add `pub mod security_headers;` to `crates/axiam-api-rest/src/middleware/mod.rs`.

    Create test file `crates/axiam-api-rest/tests/security_headers_test.rs`:
    - Use `actix_web::{test, App, web, HttpResponse}` and the middleware.
    - Build a minimal App with `.wrap(SecurityHeadersMiddleware)` and a simple GET `/test` route returning 200.
    - Assert each header is present with exact value using `resp.headers().get("x-content-type-options")`.
  </action>
  <verify>
    <automated>cargo test -p axiam-api-rest --test security_headers_test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/axiam-api-rest/src/middleware/security_headers.rs` exists and contains `pub struct SecurityHeadersMiddleware`
    - File `crates/axiam-api-rest/src/middleware/mod.rs` contains `pub mod security_headers`
    - File `crates/axiam-api-rest/tests/security_headers_test.rs` exists with at least 3 test functions
    - `cargo test -p axiam-api-rest --test security_headers_test` passes with 0 failures
    - No `EitherBody` import in `security_headers.rs`
  </acceptance_criteria>
  <done>SecurityHeadersMiddleware adds all three OWASP headers to every response; integration tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Register middleware in server.rs and update nginx.conf</name>
  <files>
    crates/axiam-api-rest/src/server.rs
    docker/nginx.conf
  </files>
  <read_first>
    crates/axiam-api-rest/src/server.rs
    docker/nginx.conf
  </read_first>
  <action>
    **server.rs changes:**
    The SecurityHeadersMiddleware must be registered at the App level in `axiam-server/src/main.rs` where `HttpServer::new()` builds the App. However, since `server.rs` in axiam-api-rest defines route configuration functions (not the App builder), the middleware registration happens in `main.rs`.

    Check where `.wrap(TracingLogger::default())` or `.wrap(CsrfMiddleware)` is called in `crates/axiam-server/src/main.rs`. Add `.wrap(SecurityHeadersMiddleware)` as the OUTERMOST wrap (after TracingLogger in code = before TracingLogger in execution, since Actix uses LIFO ordering). Add the import: `use axiam_api_rest::middleware::security_headers::SecurityHeadersMiddleware;`

    If the App builder is in `main.rs`, modify `main.rs` instead of `server.rs`. The key is that SecurityHeadersMiddleware wraps the entire App, not individual scopes.

    **nginx.conf changes:**
    Replace the existing security headers block (lines ~21-24) with the full OWASP-compliant set. The changes are:

    1. Change `X-Frame-Options "SAMEORIGIN"` to `X-Frame-Options "DENY"` (per REQ-2)
    2. REMOVE `X-XSS-Protection "1; mode=block"` (OWASP deprecated; can enable XSS in old browsers)
    3. ADD `Strict-Transport-Security "max-age=31536000; includeSubDomains" always;`
    4. ADD `Permissions-Policy "geolocation=(), camera=(), microphone=(), payment=(), usb=(), magnetometer=(), gyroscope=(), accelerometer=()" always;`
    5. ADD `Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; form-action 'self'; base-uri 'self'" always;` (per D-05, D-06, D-07)

    CRITICAL: nginx `add_header` directives in child `location` blocks REPLACE parent-context headers. The existing nginx.conf already duplicates headers in each location block. When adding the new headers, they MUST be added to EVERY location block that has `add_header` directives — including the static assets location, the index.html location, and the API proxy location.
  </action>
  <verify>
    <automated>cargo check -p axiam-server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `crates/axiam-server/src/main.rs` contains `use axiam_api_rest::middleware::security_headers::SecurityHeadersMiddleware`
    - `crates/axiam-server/src/main.rs` contains `.wrap(SecurityHeadersMiddleware)`
    - `docker/nginx.conf` contains `Content-Security-Policy` with `script-src 'self'` and `style-src 'self' 'unsafe-inline'`
    - `docker/nginx.conf` contains `Strict-Transport-Security "max-age=31536000; includeSubDomains"`
    - `docker/nginx.conf` contains `Permissions-Policy "geolocation=()`
    - `docker/nginx.conf` does NOT contain `X-XSS-Protection`
    - `docker/nginx.conf` contains `X-Frame-Options "DENY"` (not SAMEORIGIN)
    - Every `location` block in nginx.conf that had `add_header` now includes the full set of 6 headers
    - `cargo check -p axiam-server` compiles without errors
  </acceptance_criteria>
  <done>SecurityHeadersMiddleware registered globally in App; nginx.conf has complete OWASP header set with CSP/HSTS/Permissions-Policy in all location blocks</done>
</task>

</tasks>

<verification>
- `cargo test -p axiam-api-rest --test security_headers_test` — all tests pass
- `cargo check -p axiam-server` — compiles cleanly
- `grep -c "Content-Security-Policy" docker/nginx.conf` returns count matching number of location blocks with headers
- `grep "X-XSS-Protection" docker/nginx.conf` returns no results
</verification>

<success_criteria>
REQ-2 fully satisfied:
- Backend middleware adds X-Content-Type-Options, X-Frame-Options, Referrer-Policy to all API responses
- Nginx adds CSP, HSTS, Permissions-Policy to all frontend responses
- CSP restricts scripts to self-origin (no inline, no eval) per D-05
- HSTS with max-age=31536000; includeSubDomains
- Integration tests prove headers present
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-headers-rate-limiting/02-01-SUMMARY.md`
</output>
