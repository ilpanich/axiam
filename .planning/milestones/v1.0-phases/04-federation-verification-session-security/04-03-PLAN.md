---
phase: 04-federation-verification-session-security
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - Cargo.toml
  - crates/axiam-federation/Cargo.toml
  - crates/axiam-federation/src/saml.rs
  - crates/axiam-federation/src/cert.rs
  - crates/axiam-federation/src/lib.rs
  - crates/axiam-federation/src/error.rs
  - crates/axiam-db/src/repository/saml_replay.rs
  - crates/axiam-db/src/repository/mod.rs
  - crates/axiam-core/src/repository.rs
  - crates/axiam-api-rest/src/handlers/federation.rs
  - docker/Dockerfile.server
  - crates/axiam-federation/tests/fixtures/saml/well_signed_response.xml
  - crates/axiam-federation/tests/fixtures/saml/tampered_response.xml
  - crates/axiam-federation/tests/fixtures/saml/replayed_response.xml
autonomous: true
requirements: [REQ-5]

must_haves:
  truths:
    - "SAML response missing <ds:Signature> → 401 (when federation_config has non-null idp_signing_cert_pem)"
    - "SAML response with tampered body byte → 401 (xmlsec sig verify fail)"
    - "SAML assertion whose ID was already consumed → 401 with reason 'assertion replay'"
    - "SAML assertion with NotOnOrAfter in the past (beyond 60s skew) → 401 (existing validator)"
    - "Docker image runtime stage has libxml2 + libxmlsec1 + libxmlsec1-openssl installed"
    - "Docker image build stage has libxml2-dev + libxmlsec1-dev + clang"
    - "Both Docker base images (rust:1.94-bookworm builder + debian:bookworm-slim runtime) are pinned with @sha256: digests"
    - "Pre-signed SAML fixture XML files exist under crates/axiam-federation/tests/fixtures/saml/ (well-signed, tampered, replayed)"
  artifacts:
    - path: "crates/axiam-federation/src/cert.rs"
      provides: "PEM↔DER conversion helpers + x509-parser validation"
      exports: ["pem_cert_to_der", "validate_pem_cert"]
    - path: "crates/axiam-db/src/repository/saml_replay.rs"
      provides: "SurrealAssertionReplayRepository with insert-or-conflict semantics"
      exports: ["SurrealAssertionReplayRepository"]
    - path: "crates/axiam-federation/tests/fixtures/saml/"
      provides: "Pre-signed SAML XML fixtures for sig-verify tests"
  key_links:
    - from: "crates/axiam-federation/src/saml.rs"
      to: "samael::crypto::verify_signed_xml"
      via: "ACS handler decodes PEM cert to DER then verifies all <ds:Signature> nodes"
      pattern: "verify_signed_xml"
    - from: "crates/axiam-api-rest/src/handlers/federation.rs"
      to: "crates/axiam-db/src/repository/saml_replay.rs"
      via: "ACS inserts assertion_id into saml_assertion_replay before consuming"
      pattern: "insert_assertion|saml_assertion_replay"
---

<objective>
Enable SAML XML signature verification (samael `xmlsec` feature), wire it into the ACS handler before the existing condition validator (saml.rs:382-412), and add SAML assertion replay protection backed by the `saml_assertion_replay` table created in plan 04-01. Update the Docker image so the runtime has libxml2 + libxmlsec1 and the build stage has the matching `-dev` packages + clang. Pin both base images by `@sha256:` digest (hard requirement — RESEARCH §"Risks and unknowns" #1 flags unpinned bases as the highest-severity Phase 4 risk because `libxml = "=0.3.3"` generates bindings against the installed libxml2). Commit pre-signed SAML fixture XML files. Replace `TODO(T19.7)` at `crates/axiam-federation/src/saml.rs:354`.

Wave: this plan was bumped from wave 2 to wave 3 because it touches the SAME files in `crates/axiam-federation/src/lib.rs`, `crates/axiam-federation/src/error.rs`, `crates/axiam-core/src/repository.rs`, and `crates/axiam-api-rest/src/handlers/federation.rs` as plan 04-02. Serializing after 04-02 is the cheapest fix; the alternative was a `serial_with` field not yet supported by the executor framework.

Purpose: REQ-5 SAML bullets — XML sig verify, condition validation (already correct, kept), assertion ID replay tracking, plus the operational pieces (Dockerfile w/ pinned bases, cert storage format, repo for the replay table).
Output: Verified SAML ACS flow that fails closed on missing signature, tampering, or replay. Docker image gains the two new shared libs with both stages pinned by digest. Pre-signed XML fixtures committed for sig-verify tests in this plan AND plan 04-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-federation-verification-session-security/04-CONTEXT.md
@.planning/phases/04-federation-verification-session-security/04-RESEARCH.md
@.planning/phases/04-federation-verification-session-security/04-01-PLAN.md
@.planning/phases/04-federation-verification-session-security/04-02-PLAN.md

<interfaces>
From `crates/axiam-federation/Cargo.toml:23`:
- `samael = { workspace = true }` with workspace pinning at root `Cargo.toml:104` as `samael = { version = "0.0.19", default-features = false }`. Flip to `features = ["xmlsec"]` (drop `default-features = false`, OR set both).

From `samael` crate (verified via RESEARCH §2):
- `#[cfg(feature = "xmlsec")] pub fn verify_signed_xml<Bytes: AsRef<[u8]>>(xml: Bytes, x509_cert_der: &[u8], id_attribute: Option<&str>) -> Result<(), samael::Error>`
- Pass `id_attribute = Some("ID")` (SAML uses uppercase `ID`).
- Verifies ALL `<ds:Signature>` elements within the document → covers both Response-level and Assertion-level (D-08).
- FAILS when no `<ds:Signature>` is present.

From `crates/axiam-federation/src/saml.rs`:
- `TODO(T19.7)` at `:354` — wiring target.
- Existing condition validator at `:382-412` — keep unchanged; sig verify must run BEFORE this block.
- `SamlFederationService` struct around `:30-80` (constructor).

From `crates/axiam-core/src/repository.rs`:
- Add new trait `AssertionReplayRepository` with `async fn insert_assertion(&self, tenant_id: Uuid, assertion_id: &str, expires_at: DateTime<Utc>) -> AxiamResult<()>` returning `Err(AxiamError::ReplayDetected)` on UNIQUE conflict.
- Add new error variant `ReplayDetected` to `AxiamError`.

From `crates/axiam-db/src/error.rs`:
- UNIQUE-violation detection — match on error message containing "already exists" or "unique" per MEMORY.md project pattern. RESEARCH §"Duplicate-key detection" §2 references how `federation_link` (schema.rs:585) handles this — read it for the exact substring.

From `docker/Dockerfile.server` (current state):
- Build stage `FROM rust:1.94-bookworm AS builder` at line 11 — currently UNPINNED (no digest).
- Build apt-get install at line 14 — currently `protobuf-compiler` only.
- Runtime stage `FROM debian:bookworm-slim AS runtime` at line 70 — currently UNPINNED (no digest).
- Runtime apt-get install at line 82-86 — currently `ca-certificates libssl3 curl`.

`x509-parser = "0.17"` is already in the workspace (verified in RESEARCH §"Library versions" table). `base64 = "0.22"` already used.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable samael/xmlsec; update Dockerfile build + runtime stages; hard-pin both base image SHAs</name>
  <files>
    Cargo.toml
    crates/axiam-federation/Cargo.toml
    docker/Dockerfile.server
  </files>
  <read_first>
    Cargo.toml
    crates/axiam-federation/Cargo.toml
    docker/Dockerfile.server
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — Workspace `Cargo.toml`: change the samael line from `samael = { version = "0.0.19", default-features = false }` to `samael = { version = "0.0.19", features = ["xmlsec"] }` (the upstream `default = ["xmlsec"]` is the same set; being explicit is clearer). Pin the version exactly: do NOT change `0.0.19`.

    Step 2 — `crates/axiam-federation/Cargo.toml`: confirm the dep line is `samael = { workspace = true }`. No change required there unless the crate had a per-crate features override.

    Step 3 — `docker/Dockerfile.server` BUILD stage (line 14): extend the existing `apt-get install` line to include `libxml2-dev libxmlsec1-dev libxmlsec1-openssl clang pkg-config`. The full list becomes `protobuf-compiler libxml2-dev libxmlsec1-dev libxmlsec1-openssl clang pkg-config`. Keep `--no-install-recommends` and the `rm -rf /var/lib/apt/lists/*` cleanup.

    Step 4 — `docker/Dockerfile.server` RUNTIME stage (line 82-86): extend the existing `apt-get install` line to include `libxml2 libxmlsec1 libxmlsec1-openssl`. Full list becomes `ca-certificates libssl3 libxml2 libxmlsec1 libxmlsec1-openssl curl`. Keep `--no-install-recommends` and cleanup.

    Step 5 — Hard SHA pinning (REQUIRED — RESEARCH §"Risks and unknowns" #1):

    Both base images MUST carry an `@sha256:<digest>` suffix after this task completes:
    - Line 11: `FROM rust:1.94-bookworm AS builder` → `FROM rust:1.94-bookworm@sha256:<digest-A> AS builder`
    - Line 70: `FROM debian:bookworm-slim AS runtime` → `FROM debian:bookworm-slim@sha256:<digest-B> AS runtime`

    To obtain each digest, use whichever of the two paths works in the executor environment:
    - (a) Docker daemon available: `docker pull rust:1.94-bookworm && docker inspect --format='{{index .RepoDigests 0}}' rust:1.94-bookworm | awk -F'@' '{print $2}'`. Same for `debian:bookworm-slim`.
    - (b) No docker daemon: query the registry manifest API directly:
      `curl -sSL -H 'Accept: application/vnd.docker.distribution.manifest.v2+json' -H 'Accept: application/vnd.oci.image.index.v1+json' https://registry-1.docker.io/v2/library/rust/manifests/1.94-bookworm -D - | grep -i docker-content-digest | awk '{print $2}' | tr -d '\r'`. (You will need an anonymous-auth token first: `TOKEN=$(curl -s "https://auth.docker.io/token?service=registry.docker.io&scope=repository:library/rust:pull" | jq -r .token)`, then pass `-H "Authorization: Bearer $TOKEN"`.) Same for `library/debian:bookworm-slim`.

    Replace both `FROM` lines with the resolved digests. Do NOT leave `TODO(pinning)` comments — this is a hard acceptance criterion. If the executor's environment lacks BOTH docker AND outbound HTTPS to docker.io, surface this as a blocking issue and stop the task; do not commit unpinned images.

    Step 6 — `cargo check -p axiam-federation` must compile with xmlsec enabled. This will require the C deps locally; if the executor's environment lacks `libxmlsec1-dev`, document the requirement in the task verify section and rely on CI for the actual compile.

    Step 7 — `cargo clippy -p axiam-federation -- -D warnings` (skip if local C deps missing; CI catches).

    Step 8 — Build the Docker image as a smoke test: `docker build -f docker/Dockerfile.server -t axiam-server:phase4-smoke .`. Must succeed end-to-end. If Docker is unavailable in the executor, defer to CI but flag this clearly.
  </action>
  <verify>
    <automated>grep -c 'samael = { version = "0.0.19", features = \["xmlsec"\] }' Cargo.toml && grep -c '@sha256:' docker/Dockerfile.server</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'features = \["xmlsec"\]' Cargo.toml` returns 1 on the samael line
    - `grep -c 'default-features = false' Cargo.toml` — the samael line no longer has this
    - `grep -c 'libxml2-dev' docker/Dockerfile.server` returns 1
    - `grep -c 'libxmlsec1-dev' docker/Dockerfile.server` returns 1
    - `grep -c 'libxmlsec1-openssl' docker/Dockerfile.server` returns 2 (build + runtime stages)
    - `grep -c 'clang' docker/Dockerfile.server` returns at least 1 (build stage)
    - `grep -c 'libxml2 ' docker/Dockerfile.server` returns at least 1 (runtime stage, with trailing space to distinguish from `libxml2-dev`)
    - `grep -c 'libxmlsec1 ' docker/Dockerfile.server` returns at least 1 (runtime stage)
    - `grep -c '^FROM .*@sha256:' docker/Dockerfile.server` returns 2 (builder + runtime, both pinned)
    - `grep -c 'TODO(pinning)' docker/Dockerfile.server` returns 0 (NO soft TODO left behind)
    - If local C deps available: `cargo check -p axiam-federation` exits 0
    - Docker build via `docker build -f docker/Dockerfile.server -t axiam-server:phase4-smoke .` exits 0 (or CI green if local docker unavailable)
  </acceptance_criteria>
  <done>samael/xmlsec activated; build + runtime Docker stages carry the new system deps; BOTH base images pinned by SHA digest (hard requirement); image builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: PEM↔DER cert helpers + SurrealAssertionReplayRepository</name>
  <files>
    crates/axiam-federation/src/cert.rs
    crates/axiam-federation/src/lib.rs
    crates/axiam-federation/src/error.rs
    crates/axiam-db/src/repository/saml_replay.rs
    crates/axiam-db/src/repository/mod.rs
    crates/axiam-core/src/repository.rs
  </files>
  <read_first>
    crates/axiam-federation/src/lib.rs
    crates/axiam-federation/src/error.rs
    crates/axiam-db/src/repository/mod.rs
    crates/axiam-db/src/repository/session.rs
    crates/axiam-db/src/repository/federation.rs
    crates/axiam-core/src/repository.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — Create `crates/axiam-federation/src/cert.rs`:
    - `pub fn pem_cert_to_der(pem: &str) -> Result<Vec<u8>, FederationError>` — strip `-----BEGIN CERTIFICATE-----` / `-----END CERTIFICATE-----` lines (and any whitespace), base64-decode the body via `base64::engine::general_purpose::STANDARD.decode`. Map errors to `FederationError::InvalidIdpCert(String)` (add this variant if missing).
    - `pub fn validate_pem_cert(pem: &str) -> Result<(), FederationError>` — call `pem_cert_to_der` then `x509_parser::parse_x509_certificate(&der)`; map parse errors to `InvalidIdpCert`. Use this in the admin federation-config create/update handler so garbage PEMs are rejected at upload (per RESEARCH §"Cert PEM → DER for samael").

    Unit tests in `cert.rs`:
    - `valid_pem_round_trip` — use a known test cert (project may have one in `crates/axiam-pki/tests/fixtures/` or generate via `rcgen` in-test); pass through `pem_cert_to_der` then `parse_x509_certificate`.
    - `garbage_pem_rejected` — `"not a pem"` → `InvalidIdpCert`.
    - `non_cert_pem_rejected` — valid base64 of random bytes → x509 parse fails → `InvalidIdpCert`.

    Add `pub mod cert;` to `crates/axiam-federation/src/lib.rs`. Add the `InvalidIdpCert(String)` variant to `FederationError` in `error.rs` mapping to 400 (admin-facing on upload) and 500 (server-side if a stored cert somehow becomes invalid).

    Step 2 — Create `crates/axiam-db/src/repository/saml_replay.rs` and the trait in `crates/axiam-core/src/repository.rs`:
    - Trait `AssertionReplayRepository`:
        * `async fn insert_assertion(&self, tenant_id: Uuid, assertion_id: &str, expires_at: DateTime<Utc>) -> AxiamResult<()>` — returns `Err(AxiamError::ReplayDetected)` on UNIQUE violation; other DB errors propagate as `AxiamError::Database`.
        * `async fn cleanup_expired(&self) -> AxiamResult<u64>` — `DELETE saml_assertion_replay WHERE expires_at < time::now()`; returns count.
    - Impl `SurrealAssertionReplayRepository<C: Connection> { db: Surreal<C> }` with a `pub fn new(db: Surreal<C>) -> Self` constructor.
    - SurrealQL for insert: `CREATE type::record('saml_assertion_replay', $row_id) SET tenant_id = $tenant_id, assertion_id = $assertion_id, expires_at = $expires_at`. Bind owned strings. Generate `$row_id = Uuid::new_v4().to_string()` per project pattern.
    - Map UNIQUE-violation error: inspect `e.to_string().contains("already exists") || e.to_string().contains("unique")` (the exact substring is project-confirmed — verify by reading the analogous `federation_link` UNIQUE handler at `crates/axiam-db/src/repository/federation.rs` UNIQUE handling, then mirror).
    - Add `AxiamError::ReplayDetected` variant if not already present. Map to 401 in the `ResponseError` impl with reason "assertion replay".

    Step 3 — Wire into `crates/axiam-db/src/repository/mod.rs` — add `pub mod saml_replay;` and re-export `pub use saml_replay::SurrealAssertionReplayRepository;`. Register the repo as `web::Data` in `axiam-server::main.rs` alongside the other repos (one-line addition).

    Step 4 — Integration test in `crates/axiam-db/tests/saml_replay.rs` (or `crates/axiam-server/tests/saml_replay.rs`):
    - First `insert_assertion(tenant_id, "abc", now + 5min)` → Ok.
    - Second `insert_assertion(tenant_id, "abc", now + 5min)` → `Err(AxiamError::ReplayDetected)`.
    - `insert_assertion(other_tenant_id, "abc", now + 5min)` → Ok (uniqueness is per-tenant).
    - `cleanup_expired()` after manually setting one row's `expires_at` to `now - 1h` deletes exactly 1 row.

    Step 5 — `cargo check -p axiam-federation -p axiam-db -p axiam-core`, run the new integration test, `cargo fmt`, `cargo clippy -p axiam-federation -p axiam-db -p axiam-core -- -D warnings`.
  </action>
  <verify>
    <automated>cargo test -p axiam-db --test saml_replay 2>&1 | tail -10; cargo test -p axiam-federation --lib cert 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/axiam-federation/src/cert.rs` exists with `pub fn pem_cert_to_der` and `pub fn validate_pem_cert`
    - Tests `valid_pem_round_trip`, `garbage_pem_rejected`, `non_cert_pem_rejected` pass
    - File `crates/axiam-db/src/repository/saml_replay.rs` exists with `pub struct SurrealAssertionReplayRepository`
    - `grep -c 'async fn insert_assertion' crates/axiam-core/src/repository.rs` returns 1
    - `grep -c 'async fn cleanup_expired' crates/axiam-core/src/repository.rs` returns at least 1 (may already exist for session)
    - `grep -c 'ReplayDetected' crates/axiam-core/src/error.rs` returns at least 1
    - Integration test `saml_replay` passes: duplicate within tenant → ReplayDetected; duplicate across tenants → Ok; cleanup_expired removes expired rows
    - `cargo clippy -p axiam-federation -p axiam-db -p axiam-core -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Cert helpers + replay repo in place and tested; UNIQUE conflict path returns `ReplayDetected` cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Wire xmlsec sig verification + replay check into SAML ACS handler (replace TODO at saml.rs:354); commit pre-signed SAML fixtures</name>
  <files>
    crates/axiam-federation/src/saml.rs
    crates/axiam-api-rest/src/handlers/federation.rs
    crates/axiam-federation/tests/fixtures/saml/well_signed_response.xml
    crates/axiam-federation/tests/fixtures/saml/tampered_response.xml
    crates/axiam-federation/tests/fixtures/saml/replayed_response.xml
    crates/axiam-federation/tests/fixtures/saml/README.md
  </files>
  <read_first>
    crates/axiam-federation/src/saml.rs
    crates/axiam-federation/src/cert.rs
    crates/axiam-federation/src/error.rs
    crates/axiam-api-rest/src/handlers/federation.rs
    crates/axiam-db/src/repository/saml_replay.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — Update `SamlFederationService` constructor to take `replay_repo: Arc<dyn AssertionReplayRepository + Send + Sync>` (or generic `R: AssertionReplayRepository`). Inject from `web::Data` at the handler call site.

    Step 2 — Replace `TODO(T19.7)` at `crates/axiam-federation/src/saml.rs:354` with a new private method `fn verify_signature(&self, xml_bytes: &[u8], config: &FederationConfig) -> Result<(), FederationError>`:
    - If `config.idp_signing_cert_pem.is_none()` → `Err(FederationError::ConfigIncomplete)` (per D-08 "null cert means config not finished — fail closed").
    - Convert PEM → DER via `crate::cert::pem_cert_to_der(&pem)`.
    - Call `samael::crypto::verify_signed_xml(xml_bytes, &der, Some("ID"))`. Map any error to `FederationError::SamlSignatureInvalid(String)`.
    - Add this method and call it BEFORE the existing condition validator block at `:382-412`. The current method order in `process_saml_response` (or whichever entry point) should be: parse XML → `verify_signature(...)` → `validate_conditions(...)` (existing) → replay-check → consume.

    Step 3 — In the same ACS path, AFTER signature + condition validation, AND BEFORE issuing the local session, call `replay_repo.insert_assertion(tenant_id, &assertion.id, assertion.not_on_or_after).await?`. On `Err(AxiamError::ReplayDetected)` → map back to `FederationError::AssertionReplay` (add this variant; → 401). On success → continue with provisioning + cookie issuance.

    The `not_on_or_after` value comes from the assertion's `Conditions` block (already parsed by the existing condition validator). Reuse the existing parsed `DateTime<Utc>`.

    Step 4 — Add new `FederationError` variants `SamlSignatureInvalid(String)`, `AssertionReplay`, `ConfigIncomplete` (if not already present). Map all to 401 in the `ResponseError` impl.

    Step 5 — Update `crates/axiam-api-rest/src/handlers/federation.rs` ACS handler around `:696-752`:
    - Inject `web::Data<SurrealAssertionReplayRepository<C>>` (or pass it via the service).
    - Surface the new error variants through `AxiamApiError`.

    Step 6 — SAML test fixtures (LOCKED: option (a) — pre-signed XML fixtures committed to the repo; per plan 04-06 which reuses these for `saml_rejects_tampered_response`). Generate three XML files using `rcgen` + an `openssl`-based signing helper or `xmlsec1` CLI invoked from a one-shot script that lives under `crates/axiam-federation/tests/fixtures/saml/generate.sh` (committed but not run by the build):

    - `crates/axiam-federation/tests/fixtures/saml/well_signed_response.xml` — A complete, valid SAML Response with a `<ds:Signature>` covering the `<saml:Assertion>` (assertion-level signature), signed with a test keypair whose corresponding cert PEM is committed alongside as `signing_cert.pem`. Assertion `ID = "well-signed-1"`. `NotOnOrAfter = "2099-01-01T00:00:00Z"`.
    - `crates/axiam-federation/tests/fixtures/saml/tampered_response.xml` — Copy of well_signed_response.xml with ONE byte flipped inside the `<saml:AttributeValue>` text content (NOT inside the signature element — the signature must remain syntactically valid so xmlsec exercises the digest comparison, not XML parsing). Signature verify MUST fail.
    - `crates/axiam-federation/tests/fixtures/saml/replayed_response.xml` — Identical to well_signed_response.xml but with assertion `ID = "replay-victim-1"` so the test inserts it once, then submits the same XML again to drive the UNIQUE-violation path.
    - `crates/axiam-federation/tests/fixtures/saml/signing_cert.pem` — The PEM the test loads as `idp_signing_cert_pem` on the test federation_config row.
    - `crates/axiam-federation/tests/fixtures/saml/generate.sh` — Reproducibility: a shell script that regenerates all four files from scratch (uses `rcgen` via a tiny helper Rust binary OR `openssl req` + `xmlsec1 --sign`). Documented in README.md. Do not invoke the script during normal `cargo test` — the fixtures are committed.
    - `crates/axiam-federation/tests/fixtures/saml/README.md` — One paragraph explaining the fixture purpose and how to regenerate. Cross-reference plan 04-06 Task 2 (`saml_rejects_tampered_response`).

    Step 7 — Unit tests in `saml.rs` `#[cfg(test)] mod tests`:
    - `verify_rejects_missing_signature` — strip the `<ds:Signature>` block from well_signed_response.xml in-test → `SamlSignatureInvalid`.
    - `verify_rejects_tampered_body` — load `tampered_response.xml` from the fixture dir → `SamlSignatureInvalid`.
    - `verify_accepts_well_signed_response` — load `well_signed_response.xml` → Ok.
    - `acs_rejects_replayed_assertion` — integration-style test that calls into the ACS path twice with `replayed_response.xml`; second call → `AssertionReplay`. Use the in-memory SurrealDB repo from Task 2.

    Step 8 — `cargo check -p axiam-federation -p axiam-api-rest`, `cargo test -p axiam-federation`, `cargo fmt`, `cargo clippy -p axiam-federation -p axiam-api-rest -- -D warnings`.
  </action>
  <verify>
    <automated>cargo test -p axiam-federation --lib saml 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TODO(T19.7)\|TODO(T19\.7)' crates/axiam-federation/src/saml.rs` returns 0
    - `grep -c 'verify_signed_xml' crates/axiam-federation/src/saml.rs` returns at least 1
    - `grep -c 'fn verify_signature' crates/axiam-federation/src/saml.rs` returns at least 1
    - `grep -c 'insert_assertion' crates/axiam-federation/src/saml.rs` returns at least 1
    - Signature verify call site precedes the existing condition validator block (line numbers: verify_signature called BEFORE the existing validator at `:382-412`)
    - `grep -c 'SamlSignatureInvalid\|AssertionReplay\|ConfigIncomplete' crates/axiam-federation/src/error.rs` returns at least 3
    - `ls crates/axiam-federation/tests/fixtures/saml/*.xml | wc -l` returns at least 3 (well_signed, tampered, replayed)
    - `ls crates/axiam-federation/tests/fixtures/saml/signing_cert.pem` exists
    - `ls crates/axiam-federation/tests/fixtures/saml/generate.sh` exists
    - `ls crates/axiam-federation/tests/fixtures/saml/README.md` exists
    - Unit test `verify_rejects_missing_signature` passes
    - Unit test `verify_rejects_tampered_body` passes (against the committed `tampered_response.xml`)
    - Unit test `verify_accepts_well_signed_response` passes
    - Unit test `acs_rejects_replayed_assertion` passes (second submission of `replayed_response.xml` → 401-mapped error)
    - `cargo clippy -p axiam-federation -p axiam-api-rest -- -D warnings` exits 0
  </acceptance_criteria>
  <done>SAML ACS now fails closed on missing sig, tampered XML, and replayed assertions; existing condition validator still runs after sig verify; committed pre-signed XML fixtures drive both this plan's tests and plan 04-06's `saml_rejects_tampered_response`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IdP → ACS endpoint | SAML response arrives via HTTP POST from the user's browser (IdP-initiated or SP-initiated); the only trust anchor is the cert in `federation_config.idp_signing_cert_pem` |
| admin → federation_config | Admin pastes the IdP cert PEM via a config endpoint; `validate_pem_cert` rejects garbage at upload |
| process memory → libxmlsec1 | XML bytes are passed across an FFI boundary; samael's `verify_signed_xml` is the gate |
| pinned base image → CVE response | `@sha256:` pinning means a CVE in libxml2 requires an explicit Dockerfile bump + rebuild — Phase 6 trivy gate will catch this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Spoofing | Attacker submits unsigned SAML response | mitigate | `verify_signed_xml` fails when no `<ds:Signature>` (Task 3 unit test `verify_rejects_missing_signature`); D-08 "null cert means config not finished — fail closed" enforced by `ConfigIncomplete` |
| T-04-15 | Tampering | Attacker replays a captured well-signed assertion | mitigate | `saml_assertion_replay` UNIQUE index on `(tenant_id, assertion_id)` — second INSERT returns ReplayDetected (Task 3 unit test `acs_rejects_replayed_assertion`); D-09 |
| T-04-16 | Tampering | XML signature wrapping attack (XSW) | mitigate | samael xmlsec follows `<Reference URI=#>` with `id_attribute = Some("ID")`; verifies the element actually referenced by the signature, not the wrapper |
| T-04-17 | InformationDisclosure | libxmlsec1 vulnerability in the runtime image | mitigate | Pinned base image SHA (Task 1 step 5 — HARD requirement); upgrade via image rebuild on CVE. Tracked by Phase 6 `trivy` scan. |
| T-04-18 | DoS | Adversary spams huge SAML XML payloads | accept | Existing actix-web body size limit (verify in `crates/axiam-api-rest/src/server.rs` — Phase 2 set a JSON body limit); SAML uses the same limit |
| T-04-19 | Repudiation | Replay row TTL expires before audit retention period | accept | Replay rows only need to live until the assertion's `NotOnOrAfter`; full audit of the consumed-assertion event is captured separately by the existing `AuditLogRepository::create` call already at the end of the ACS flow |
| T-04-20 | ElevationOfPrivilege | Attacker swaps `idp_signing_cert_pem` via an SQL-injected config update | accept | Federation config update endpoint requires admin permission (Phase 3 D-04); SQL injection prevented by SurrealQL `bind()` discipline (MEMORY.md project pattern) |
</threat_model>

<verification>
- `cargo test -p axiam-federation` passes the new SAML and cert tests using committed XML fixtures.
- `cargo test -p axiam-db --test saml_replay` passes.
- `docker build -f docker/Dockerfile.server` succeeds (CI or local) with both bases pinned by SHA.
- `cargo clippy -p axiam-federation -p axiam-api-rest -p axiam-db -- -D warnings` zero warnings.
</verification>

<success_criteria>
REQ-5 SAML bullets:
- XML signature verification (D-06/D-07/D-08) ✓ via samael xmlsec
- NotOnOrAfter / NotBefore / Audience conditions (existing validator, kept) ✓
- Assertion replay tracking (D-09) ✓ via `saml_assertion_replay` UNIQUE constraint
- 60s clock skew (existing condition validator) ✓
Operational:
- Dockerfile build + runtime stages updated for libxml2/libxmlsec1
- Both base images pinned by SHA digest (HARD REQUIREMENT — RESEARCH §"Risks and unknowns" #1)
- IdP cert stored as PEM, validated at upload, converted to DER on use (D-07)
- Pre-signed SAML fixtures committed under `crates/axiam-federation/tests/fixtures/saml/` for reuse by plan 04-06
</success_criteria>

<output>
After completion, create `.planning/phases/04-federation-verification-session-security/04-03-SUMMARY.md`
</output>
</content>
</invoke>