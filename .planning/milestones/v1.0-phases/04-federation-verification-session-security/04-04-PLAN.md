---
phase: 04-federation-verification-session-security
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-03"]
files_modified:
  - crates/axiam-api-rest/src/extractors/auth.rs
  - crates/axiam-core/src/repository.rs
  - crates/axiam-db/src/repository/session.rs
  - crates/axiam-db/src/repository/oauth2_refresh_token.rs
  - crates/axiam-auth/src/service.rs
  - crates/axiam-auth/src/password_reset.rs
  - crates/axiam-api-rest/src/handlers/auth.rs
  - crates/axiam-api-rest/src/server.rs
  - crates/axiam-api-rest/src/handlers/password_reset.rs
autonomous: true
requirements: [REQ-7]

must_haves:
  truths:
    - "POST /api/v1/auth/password/change with wrong current_password → 401"
    - "POST /api/v1/auth/password/change with weak new_password → 422 (password policy)"
    - "POST /api/v1/auth/password/change success → 204; caller's cookie still works on next request"
    - "Other concurrent session's cookie returns 401 on the next request after password change"
    - "Password reset confirm invalidates ALL of the user's sessions (no current session to preserve)"
    - "Password change AND password reset both revoke OAuth2 refresh tokens for the user"
    - "MFA reset still invalidates sessions (existing behaviour, test-locked)"
    - "REST extractor rejects access tokens with aud=axiam:m2m"
    - "REST extractor accepts tokens with aud=axiam:user OR (aud absent AND allow_missing_aud_as_user=true)"
  artifacts:
    - path: "crates/axiam-api-rest/src/extractors/auth.rs"
      provides: "AuthenticatedUser with session_id field; aud narrowing to axiam:user"
    - path: "crates/axiam-auth/src/service.rs"
      provides: "AuthService::change_password and revoke_all_sessions_except"
    - path: "crates/axiam-db/src/repository/session.rs"
      provides: "invalidate_user_sessions_except DB-level filter"
    - path: "crates/axiam-db/src/repository/oauth2_refresh_token.rs"
      provides: "revoke_all_for_user (new method)"
  key_links:
    - from: "crates/axiam-api-rest/src/handlers/auth.rs"
      to: "crates/axiam-auth/src/service.rs"
      via: "POST /api/v1/auth/password/change calls AuthService::change_password with session_id"
      pattern: "change_password.*session_id"
    - from: "crates/axiam-auth/src/service.rs"
      to: "crates/axiam-db/src/repository/oauth2_refresh_token.rs"
      via: "change_password and confirm_reset both call refresh_token_repo.revoke_all_for_user"
      pattern: "revoke_all_for_user"
---

<objective>
Deliver the session-security half of Phase 4 (REQ-7): new authenticated `POST /api/v1/auth/password/change` endpoint with current-password re-verify, password-policy check, and selective session invalidation (current session preserved); wire `revoke_all_sessions` into `PasswordResetService::confirm_reset` replacing `TODO(T19)` at `password_reset.rs:190`; add the OAuth2 refresh-token revoke-all path (RESEARCH §4 revealed two separate refresh-token systems — both must be hit). Plumb `session_id` into `AuthenticatedUser` (now trivial because plan 04-01 set `jti = session.id`). Narrow the REST extractor's audience check to `axiam:user` with the configurable back-compat window.

Purpose: Every REQ-7 acceptance criterion (password change invalidates sessions, password reset invalidates sessions, MFA reset invalidates sessions, service-account dedicated token type). Service-account `aud` plumbing was done in plan 04-01; this plan adds the per-route narrowing in the REST extractor and corresponding gRPC tolerance.
Output: A correctly behaving credential-revocation surface across both session-flow and OAuth2-flow refresh tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-federation-verification-session-security/04-CONTEXT.md
@.planning/phases/04-federation-verification-session-security/04-RESEARCH.md
@.planning/phases/04-federation-verification-session-security/04-01-PLAN.md
@.planning/phases/01-cookie-based-authentication/01-CONTEXT.md

<interfaces>
From `crates/axiam-api-rest/src/extractors/auth.rs:25-30`:
- Current shape: `pub struct AuthenticatedUser { pub user_id, pub tenant_id, pub org_id, pub claims: ValidatedClaims }`. No `session_id`.

From `crates/axiam-auth/src/token.rs` (after plan 04-01):
- `AccessTokenClaims.jti` now equals `session.id` (UUID string).
- `AccessTokenClaims.aud` is `Option<String>`; values are `AUD_USER = "axiam:user"` or `AUD_M2M = "axiam:m2m"`.

From `crates/axiam-auth/src/config.rs` (after plan 04-01):
- `AuthConfig.allow_missing_aud_as_user: bool` — back-compat knob.

From `crates/axiam-server/src/main.rs:333` (CONFIRMED via file read during revision):
- `AuthConfig` IS ALREADY registered as `web::Data` via `.app_data(web::Data::new(auth_config.clone()))` — see existing usage in `handlers/auth.rs::login`, `refresh`, `verify_mfa`, `device_auth`, `setup_confirm_mfa`. The extractor can fetch it via `req.app_data::<web::Data<AuthConfig>>()`. NO registration change is needed in this plan.

From `crates/axiam-auth/src/service.rs`:
- `AuthService::revoke_all_sessions(tenant_id, user_id)` at `:499` — calls `session_repo.invalidate_user_sessions(tenant_id, user_id)`.
- `AuthService::reset_mfa(...)` at `:548` — reference for the "invalidate sessions after auth-state change" pattern.

From `crates/axiam-auth/src/password_reset.rs` (CONFIRMED via file read during revision):
- `PasswordResetService<U, R, F, H>` (lines 29-40) holds FOUR repo handles ONLY: `user_repo: U`, `token_repo: R`, `federation_repo: F`, `history_repo: H`. It does NOT hold an `AuthService` reference and does NOT hold `session_repo` or `refresh_token_repo` directly.
- `confirm_reset` is at lines 122-206; the TODO target is at line 190 (immediately after `user_repo.update(...)` and BEFORE the function returns).
- The trait bounds at lines 30-40 use the four traits: `UserRepository`, `PasswordResetTokenRepository`, `FederationLinkRepository`, `PasswordHistoryRepository`.
- Task 3 below extends the generic parameter list to include `S: SessionRepository` and `T: RefreshTokenRepository` and calls those two repos directly inside `confirm_reset` — no service-to-service dep is introduced.

From `crates/axiam-db/src/repository/session.rs:190`:
- `invalidate_user_sessions(tenant_id, user_id)` — DELETEs all sessions for a user.

From `crates/axiam-oauth2/src/token.rs:546`:
- `refresh_token_repo.revoke(tenant_id, &token_hash)` — single-token revoke. There is NO existing user-wide revoke for the `oauth2_refresh_token` table.

`oauth2_refresh_token` table — confirm in `crates/axiam-db/src/repository/oauth2_refresh_token.rs` (the executor should `Read` it to confirm shape) and check the trait at `crates/axiam-core/src/repository.rs::RefreshTokenRepository` (confirmed present at line 574).

From `crates/axiam-api-rest/src/handlers/auth.rs:176-217`:
- `cookie_response_from_output` — the existing helper that builds a Set-Cookie response for login/refresh. Reused by plan 04-05 (federation cookies), but for password change the response is `HttpResponse::NoContent()`.

From `crates/axiam-api-rest/src/server.rs`:
- Route registration site. Add `POST /api/v1/auth/password/change` under the `/auth` scope. Rate limit per Phase 2 D-02 conventions — wrap the resource with `build_governor(rate_limit_cfg.password_reset_per_min)` (reuse the existing `password_reset_per_min` field). NOT in `PUBLIC_PATHS` — this endpoint requires auth.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session_id to AuthenticatedUser; narrow aud to axiam:user in REST extractor (no rate-limit on the back-compat warn — operator-side log filtering handles burst noise)</name>
  <files>
    crates/axiam-api-rest/src/extractors/auth.rs
  </files>
  <read_first>
    crates/axiam-api-rest/src/extractors/auth.rs
    crates/axiam-auth/src/token.rs
    crates/axiam-auth/src/config.rs
    crates/axiam-server/src/main.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — Add `pub session_id: Uuid` to `AuthenticatedUser`. In the extractor body, after `decode_access_token` succeeds, parse `claims.jti` as `Uuid`: `let session_id = Uuid::parse_str(&claims.jti).map_err(|_| AxiamError::AuthenticationFailed { reason: "invalid jti".into() })?`.

    Step 2 — Audience narrowing. After parsing `session_id`, inspect `claims.aud.as_deref()`:
    - `Some("axiam:user")` → accept.
    - `None` AND `auth_config.allow_missing_aud_as_user == true` → accept and emit `tracing::warn!(token_jti = %session_id, "accepted access token without aud — backward-compat window active")`. NO in-process rate-limit: emit on every accepted legacy token. Operator-side log filtering (loki/promtail dedup, or a fluent-bit `rewrite_tag` rule) handles burst noise. Document this trade-off inline as a doc comment on the warn call site so a future reader understands why there is no `AtomicI64` guard here.
    - `None` AND `allow_missing_aud_as_user == false` → reject 401 with reason "aud required".
    - `Some("axiam:m2m")` → reject 401 with reason "audience mismatch — this route requires axiam:user audience".
    - `Some(other)` → reject 401 with reason "unknown audience".

    Access `auth_config` from the extractor via `req.app_data::<web::Data<AuthConfig>>()`. The registration in `crates/axiam-server/src/main.rs:333` is already present (confirmed via file read during revision); NO registration change in `server.rs` is needed in this plan.

    Step 3 — Add a sibling extractor `pub struct AuthenticatedServiceAccount { pub client_id: String, pub tenant_id: Uuid, pub claims: ValidatedClaims }` that does the inverse narrowing:
    - `Some("axiam:m2m")` → accept.
    - Anything else → reject 401.

    Pull the shared parse/decode logic into a private `fn parse_validated_claims(req: &HttpRequest) -> Result<ValidatedClaims, AxiamError>` to avoid duplication.

    Step 4 — Update every consumer of `AuthenticatedUser` that previously assumed no `session_id` — compilation surfaces them. Pass `session_id` through where needed (mostly the new `change_password` handler in Task 3).

    Step 5 — Unit tests in `extractors/auth.rs`:
    - `accepts_axiam_user_audience` — craft token with `aud=axiam:user`, expect Ok.
    - `accepts_missing_aud_when_flag_true` — token with `aud=None`, config flag true, expect Ok + a warn-log assertion via `tracing_test::traced_test` (asserts the warn message was emitted at least once).
    - `rejects_missing_aud_when_flag_false` — same token, flag false, expect 401.
    - `rejects_axiam_m2m_audience_on_user_route` — token with `aud=axiam:m2m`, expect 401.
    - `session_id_matches_jti` — round-trip: issue token with `jti = "11111111-..."`, extract, assert `AuthenticatedUser.session_id == "11111111-..."`.
    - `service_account_extractor_accepts_m2m_rejects_user` — mirror tests for the new extractor.

    Step 6 — `cargo check -p axiam-api-rest`, run the new tests, `cargo fmt`, `cargo clippy -p axiam-api-rest -- -D warnings`.
  </action>
  <verify>
    <automated>cargo test -p axiam-api-rest --lib extractors::auth 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub session_id: Uuid' crates/axiam-api-rest/src/extractors/auth.rs` returns 1
    - `grep -c 'pub struct AuthenticatedServiceAccount' crates/axiam-api-rest/src/extractors/auth.rs` returns 1
    - `grep -c 'allow_missing_aud_as_user' crates/axiam-api-rest/src/extractors/auth.rs` returns at least 1
    - `grep -c 'axiam:user\|AUD_USER' crates/axiam-api-rest/src/extractors/auth.rs` returns at least 1
    - `grep -c 'axiam:m2m\|AUD_M2M' crates/axiam-api-rest/src/extractors/auth.rs` returns at least 1
    - `grep -c 'req.app_data::<web::Data<AuthConfig>>' crates/axiam-api-rest/src/extractors/auth.rs` returns at least 1
    - `grep -c 'AtomicI64\|atomic.*last_warn\|warn_rate' crates/axiam-api-rest/src/extractors/auth.rs` returns 0 (the in-process rate-limit was dropped per revision decision; warn is emitted unconditionally on every legacy token)
    - AuthConfig web::Data registration confirmed at `crates/axiam-server/src/main.rs:333` — `grep -c 'web::Data::new(auth_config' crates/axiam-server/src/main.rs` returns at least 1 (verification only; this plan does NOT modify main.rs)
    - All 6 unit tests listed above exist and pass
    - `cargo clippy -p axiam-api-rest -- -D warnings` exits 0
  </acceptance_criteria>
  <done>AuthenticatedUser carries session_id; audience narrowed to axiam:user with unconditional warn-log on legacy tokens (operator log-side filtering handles dedup); sibling extractor for service accounts.</done>
</task>

<task type="auto">
  <name>Task 2: Add invalidate_user_sessions_except (DB-level) + refresh_token_repo.revoke_all_for_user</name>
  <files>
    crates/axiam-core/src/repository.rs
    crates/axiam-db/src/repository/session.rs
    crates/axiam-db/src/repository/oauth2_refresh_token.rs
  </files>
  <read_first>
    crates/axiam-core/src/repository.rs
    crates/axiam-db/src/repository/session.rs
    crates/axiam-db/src/repository/oauth2_refresh_token.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — In `SessionRepository` trait (`crates/axiam-core/src/repository.rs`), add:
    `async fn invalidate_user_sessions_except(&self, tenant_id: Uuid, user_id: Uuid, current_session_id: Uuid) -> AxiamResult<u64>` returning the count of deleted rows.

    Step 2 — Implement on `SurrealSessionRepository<C>` in `crates/axiam-db/src/repository/session.rs`. SurrealQL: `DELETE session WHERE tenant_id = $tenant_id AND user_id = $user_id AND id != type::record('session', $current_session_id) RETURN BEFORE`. Bind owned strings. Count from the returned rows.

    Step 3 — In `RefreshTokenRepository` trait (`crates/axiam-core/src/repository.rs`, line 574), add:
    `async fn revoke_all_for_user(&self, tenant_id: Uuid, user_id: Uuid) -> AxiamResult<u64>` returning the count of revoked tokens.

    Step 4 — Implement on the SurrealDB repo at `crates/axiam-db/src/repository/oauth2_refresh_token.rs`. SurrealQL — depends on the schema; read the file first to confirm the column name (`revoked_at` vs `revoked: bool` vs DELETE-on-revoke). RESEARCH §4 confirmed the table is `oauth2_refresh_token` and that `revoke(tenant_id, token_hash)` is the existing single-token entry. Mirror its strategy (UPDATE `revoked_at = time::now()` OR DELETE — match whichever the existing `revoke` uses). Filter: `WHERE tenant_id = $tenant_id AND user_id = $user_id AND (revoked_at IS NONE)` (skip already-revoked rows so the count is meaningful).

    Step 5 — Integration tests in `crates/axiam-db/tests/session_invalidate_except.rs` (new file) and `crates/axiam-db/tests/oauth2_refresh_revoke_all.rs` (new file):
    - `invalidate_except_preserves_current` — insert 3 sessions (A, B, C) for user U; call `invalidate_user_sessions_except(tenant, U, B)`; assert sessions A and C are gone, B is alive. Return count = 2.
    - `invalidate_except_other_users_untouched` — insert sessions for U and for V; invalidate_except for U; V's sessions untouched.
    - `oauth2_revoke_all_for_user` — insert 2 refresh tokens for U, 1 for V; call `revoke_all_for_user(tenant, U)`; verify both U tokens revoked, V's untouched, count = 2.
    - `oauth2_revoke_all_idempotent` — second call returns 0.

    Step 6 — `cargo check -p axiam-core -p axiam-db`, run the integration tests, `cargo fmt`, `cargo clippy -p axiam-core -p axiam-db -- -D warnings`.
  </action>
  <verify>
    <automated>cargo test -p axiam-db --test session_invalidate_except --test oauth2_refresh_revoke_all 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'async fn invalidate_user_sessions_except' crates/axiam-core/src/repository.rs` returns 1
    - `grep -c 'async fn revoke_all_for_user' crates/axiam-core/src/repository.rs` returns 1
    - `grep -c 'id != type::record' crates/axiam-db/src/repository/session.rs` returns at least 1
    - `grep -c 'invalidate_user_sessions_except' crates/axiam-db/src/repository/session.rs` returns at least 1 (impl block)
    - `grep -c 'revoke_all_for_user' crates/axiam-db/src/repository/oauth2_refresh_token.rs` returns at least 1
    - All 4 integration tests pass
    - `cargo clippy -p axiam-core -p axiam-db -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Two new repo methods land (session selective-invalidate + OAuth2 user-wide revoke); both DB-level filtered (no fetch-then-filter); tested for cross-user isolation and idempotency.</done>
</task>

<task type="auto">
  <name>Task 3: AuthService::change_password + concrete password_reset wiring (extend generic params with SessionRepository + RefreshTokenRepository) + new POST /api/v1/auth/password/change handler and route</name>
  <files>
    crates/axiam-auth/src/service.rs
    crates/axiam-auth/src/password_reset.rs
    crates/axiam-api-rest/src/handlers/auth.rs
    crates/axiam-api-rest/src/server.rs
  </files>
  <read_first>
    crates/axiam-auth/src/service.rs
    crates/axiam-auth/src/password_reset.rs
    crates/axiam-api-rest/src/handlers/auth.rs
    crates/axiam-api-rest/src/server.rs
    crates/axiam-auth/src/password.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — In `AuthService` (`crates/axiam-auth/src/service.rs`), add:
    `pub async fn revoke_all_sessions_except(&self, tenant_id: Uuid, user_id: Uuid, current_session_id: Uuid) -> Result<(), AuthError>` — calls `session_repo.invalidate_user_sessions_except(tenant_id, user_id, current_session_id)` then `refresh_token_repo.revoke_all_for_user(tenant_id, user_id)` then writes one audit log entry (`event_type = "sessions_revoked_except_current"`). Tolerate `revoke_all_for_user` returning 0 (user may have no OAuth2 tokens).

    Step 2 — Extend `AuthService::revoke_all_sessions` at `:499` to ALSO call `refresh_token_repo.revoke_all_for_user(tenant_id, user_id)`. This is the single chokepoint that password_reset (after the wiring below) and reset_mfa already use, so this 1-line addition propagates the fix everywhere. Audit-log entry already exists; extend it with a field `oauth2_refresh_tokens_revoked: u64`.

    Step 3 — Add `pub async fn change_password(&self, tenant_id: Uuid, user_id: Uuid, current_session_id: Uuid, current_password: &str, new_password: &str) -> Result<(), AuthError>`:
    - Fetch user via `user_repo.get(tenant_id, user_id)`.
    - Verify current password via `password::verify_password(&user.password_hash, current_password)`. On mismatch → `Err(AuthError::InvalidCredentials)` (or the project's equivalent variant mapping to 401).
    - Run password-policy evaluation via the same path `password_reset::confirm_reset` uses around `:160` — `evaluate_password(tenant_id, &user, new_password)` against the tenant policy. On failure → `Err(AuthError::PasswordPolicyViolation(reason))` mapping to 422.
    - Hash via `password::hash_password(new_password)`.
    - `user_repo.update_password_hash(tenant_id, user_id, &new_hash)`.
    - Insert into `password_history` (same path as confirm_reset uses).
    - Call `self.revoke_all_sessions_except(tenant_id, user_id, current_session_id)`.
    - Audit-log entry `event_type = "password_changed"`, fields `{actor: user_id, target: user_id, mode: "self_service"}`.

    Step 4 — Wire revocation into `crates/axiam-auth/src/password_reset.rs` `confirm_reset`. The PasswordResetService struct (lines 29-40) currently holds FOUR repos and does NOT hold AuthService or session/refresh-token repos. The concrete fix (CONFIRMED via file read during revision):

    (a) Extend the generic parameter list:
    ```
    pub struct PasswordResetService<U, R, F, H, S, T>
    where
        U: UserRepository,
        R: PasswordResetTokenRepository,
        F: FederationLinkRepository,
        H: PasswordHistoryRepository,
        S: SessionRepository,
        T: RefreshTokenRepository,
    ```
    Add `session_repo: S` and `refresh_token_repo: T` fields. Update the `new(...)` constructor signature to accept them. Update every call site that constructs `PasswordResetService` (search via `rg 'PasswordResetService::new'`) — likely the handler at `crates/axiam-api-rest/src/handlers/password_reset.rs` and any test helpers. Inject `SurrealSessionRepository<C>` and the OAuth2 `SurrealOAuth2RefreshTokenRepository<C>` from `web::Data`.

    (b) In `confirm_reset` (lines 122-206), replace the TODO at line 190. The replacement is two repo calls back-to-back, BETWEEN the `password_history.create(...)` at line 181-187 and the existing `user_repo.update(...)` at line 191-203. Concrete inline sequence (no service-to-service dep, direct repo calls — matches the project pattern of password_reset.rs already using raw repos):
    ```
    self.session_repo
        .invalidate_user_sessions(tenant_id, user.id)
        .await?;
    self.refresh_token_repo
        .revoke_all_for_user(tenant_id, user.id)
        .await?;
    ```
    Remove the `TODO(T19)` comment at line 190. Note: `invalidate_user_sessions` (not `_except`) is correct here — caller is unauthenticated mid-reset, so per D-16 ALL sessions die.

    (c) Add a doc comment above the two new calls referencing D-16 + D-18 for traceability.

    Step 5 — Add `ChangePasswordRequest` struct in `crates/axiam-api-rest/src/handlers/auth.rs`:
    `#[derive(Deserialize)] pub struct ChangePasswordRequest { pub current_password: String, pub new_password: String }`.

    Add `pub async fn change_password<C: Connection>(user: AuthenticatedUser, svc: web::Data<AuthService<C>>, body: web::Json<ChangePasswordRequest>) -> Result<HttpResponse, AxiamApiError>`:
    - Validate `body.new_password.len() <= 1024` (DoS guard before Argon2id).
    - Call `svc.change_password(user.tenant_id, user.user_id, user.session_id, &body.current_password, &body.new_password).await?`.
    - Return `HttpResponse::NoContent().finish()`.

    Step 6 — Register the route in `crates/axiam-api-rest/src/server.rs` under the `/auth` scope, alongside the existing `/reset` resource (around line 119-127 in the current file):
    ```
    .service(
        web::resource("/password/change")
            .wrap(build_governor(rate_limit_cfg.password_reset_per_min))
            .route(web::post().to(handlers::auth::change_password::<C>)),
    )
    ```
    Reuse the existing `password_reset_per_min` rate limit field. The route is NOT in `PUBLIC_PATHS` — authenticated only.

    Step 7 — Integration test in `crates/axiam-api-rest/tests/password_change.rs`:
    - Bootstrap: tenant + user + two concurrent sessions (login twice → two cookies A and B).
    - POST `/api/v1/auth/password/change` from session A with correct current_password and a policy-compliant new_password → 204.
    - Subsequent request using cookie A → still works (session A preserved).
    - Subsequent request using cookie B → 401 (session B revoked).
    - Same flow with wrong current_password → 401 and password unchanged (verify by logging in with old password again).
    - Same flow with weak new_password (e.g. `"aaa"`) → 422.

    Step 8 — Integration test in `crates/axiam-api-rest/tests/password_reset_revokes_sessions.rs`: previously-issued cookie → password reset confirm with new password → previously-issued cookie returns 401 on next request.

    Step 9 — Integration test in `crates/axiam-api-rest/tests/mfa_reset_still_revokes.rs`: confirm the existing behaviour of `reset_mfa` still invalidates sessions (REQ-7 D-17 restated explicitly). Use `tracing` capture or a hit count on `session_repo.invalidate_user_sessions` if you can — simpler: log in, reset MFA, observe the next request 401s.

    Step 10 — `cargo check -p axiam-auth -p axiam-api-rest`, run all three new integration tests, `cargo fmt`, `cargo clippy -p axiam-auth -p axiam-api-rest -- -D warnings`.
  </action>
  <verify>
    <automated>cargo test -p axiam-api-rest --test password_change --test password_reset_revokes_sessions --test mfa_reset_still_revokes 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub async fn change_password' crates/axiam-auth/src/service.rs` returns 1
    - `grep -c 'pub async fn revoke_all_sessions_except' crates/axiam-auth/src/service.rs` returns 1
    - `grep -c 'TODO(T19)' crates/axiam-auth/src/password_reset.rs` returns 0
    - `rg -c 'invalidate_user_sessions|revoke_all_for_user' crates/axiam-auth/src/password_reset.rs` returns at least 2 (both call sites inside `confirm_reset`)
    - `grep -c 'S: SessionRepository\|T: RefreshTokenRepository' crates/axiam-auth/src/password_reset.rs` returns at least 2 (generic param bounds extended)
    - `grep -c 'session_repo:\|refresh_token_repo:' crates/axiam-auth/src/password_reset.rs` returns at least 2 (struct fields)
    - `grep -c 'revoke_all_for_user' crates/axiam-auth/src/service.rs` returns at least 1
    - `grep -c 'pub struct ChangePasswordRequest' crates/axiam-api-rest/src/handlers/auth.rs` returns 1
    - `grep -c '/password/change' crates/axiam-api-rest/src/server.rs` returns at least 1
    - Integration test `password_change` passes ALL of: wrong-current=401, weak-new=422, success=204, other-session-401, current-session-still-works
    - Integration test `password_reset_revokes_sessions` passes
    - Integration test `mfa_reset_still_revokes` passes
    - All `PasswordResetService::new(...)` call sites (handlers + tests) updated to pass 6 generic params — confirmed by `cargo check -p axiam-auth -p axiam-api-rest --tests` exits 0. Known call sites: `crates/axiam-api-rest/src/handlers/password_reset.rs:70` and `:151` (production handlers); `crates/axiam-auth/src/password_reset.rs` lines 318, 332, 385, 395, 414, 464, 487, 518, 531, 565 (internal unit tests in the same file already in files_modified)
    - `cargo clippy -p axiam-auth -p axiam-api-rest -- -D warnings` exits 0
  </acceptance_criteria>
  <done>POST /api/v1/auth/password/change live with correct semantics; password reset confirm and MFA reset both invalidate sessions; PasswordResetService extended with SessionRepository + RefreshTokenRepository generic params (calling repos directly — no AuthService dep cycle); OAuth2 refresh tokens revoked alongside session-flow refresh tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → /auth/password/change | Caller has a valid JWT cookie; current_password re-verify guards against session-hijack pivot |
| public unauthenticated → /auth/password-reset/confirm | Caller proves possession of the reset token; new password installed → all sessions die |
| REST extractor → access token | `aud` is now checked; m2m tokens cannot ride a user-facing route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Spoofing | Stolen access cookie pivot to password change | mitigate | `change_password` re-verifies `current_password` via Argon2id (Task 3) — attacker needs both cookie AND old password |
| T-04-22 | ElevationOfPrivilege | M2M token used to call a user-facing route | mitigate | Extractor `aud` narrowing to `axiam:user` (Task 1, unit test `rejects_axiam_m2m_audience_on_user_route`) |
| T-04-23 | Tampering | Race between password-change session-invalidate and a concurrent refresh | accept | DB-level DELETE+UPDATE; SurrealDB serializes. Concurrent refresh would create a new session row that the DELETE didn't see, but that new session belongs to a successful login — so it has the new password hash anyway. No exploit window. |
| T-04-24 | DoS | Repeated password-change attempts with bad current_password | mitigate | Rate limit on the route (reuses `password_reset_per_min` per Phase 2 conventions); each failure also triggers existing `auth_failure` audit + lockout counter (Phase 2 D-?) |
| T-04-25 | InformationDisclosure | Audit log records the password change actor and target | accept | Intentional — REQ-7 requires audit trail; the audit log does NOT record password values |
| T-04-26 | Repudiation | User claims they didn't change their password | mitigate | `password_changed` audit entry with actor + session_id + timestamp; Phase 7 audit-immutability check covers tampering |
| T-04-27 | Spoogfing | aud back-compat window kept open beyond 15 minutes | accept | Operator-controlled flag; warn log on every accepted "missing aud" token (Task 1, unconditional — no in-process rate limit) creates pressure to flip the flag; log-side dedup handles burst noise |
</threat_model>

<verification>
- `cargo test -p axiam-api-rest --test password_change` passes.
- `cargo test -p axiam-api-rest --test password_reset_revokes_sessions` passes.
- `cargo test -p axiam-api-rest --test mfa_reset_still_revokes` passes.
- `cargo test -p axiam-db --test session_invalidate_except --test oauth2_refresh_revoke_all` passes.
- `cargo clippy -p axiam-auth -p axiam-api-rest -p axiam-core -p axiam-db -- -D warnings` zero warnings.
</verification>

<success_criteria>
REQ-7 acceptance criteria:
- All active sessions/refresh tokens invalidated on password change (current session preserved) — D-14, D-15 ✓
- All active sessions/refresh tokens invalidated on password reset — D-16 ✓
- Session invalidation on MFA reset — D-17 (existing behaviour, test-locked) ✓
- Service-account dedicated token type with `aud: axiam:m2m` (REST narrowing) — D-19, D-21 ✓
RESEARCH-surfaced extras:
- OAuth2 refresh tokens revoked alongside session-flow refresh tokens (RESEARCH §4, the "two chokepoints" finding)
- `AuthenticatedUser.session_id` now exists via `jti = session.id` plumbing (unblocked D-15)
- PasswordResetService wires revocation via direct repo calls (no service-to-service dep cycle); generic param list extended with SessionRepository + RefreshTokenRepository
</success_criteria>

<output>
After completion, create `.planning/phases/04-federation-verification-session-security/04-04-SUMMARY.md`
</output>
</content>
</invoke>