---
phase: 05-email-delivery-gdpr-compliance
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - crates/axiam-api-rest/src/handlers/password_reset.rs
  - crates/axiam-api-rest/src/handlers/email_verification.rs
  - crates/axiam-audit/src/notification.rs
  - crates/axiam-api-rest/src/handlers/register.rs
autonomous: true
requirements: [REQ-6, REQ-8]

must_haves:
  truths:
    - "Requesting a password reset enqueues an OutboundMailMessage(PasswordReset) and returns a uniform {\"sent\": true} whether or not the account exists — account existence and delivery outcome never leak (D-15)"
    - "Requesting email verification enqueues an OutboundMailMessage(EmailVerification) with a server-generated token and returns the same uniform enumeration-safe response (D-15)"
    - "The notification dispatcher enqueues one OutboundMailMessage(Notification) per recipient instead of returning a recipient list"
    - "Registration records a consent record (terms_of_service) atomically with user creation"
    - "No reset/verify endpoint ever returns the token in the response body"
  artifacts:
    - path: "crates/axiam-api-rest/src/handlers/password_reset.rs"
      provides: "enqueue-to-mail-queue wiring replacing the T19.11 stub; uniform enumeration-safe response (D-15)"
      contains: "MailType::PasswordReset"
    - path: "crates/axiam-audit/src/notification.rs"
      provides: "dispatcher enqueues mail instead of returning recipient list (T19.13)"
      contains: "OutboundMailMessage"
  key_links:
    - from: "crates/axiam-api-rest/src/handlers/password_reset.rs"
      to: "queues::MAIL_OUTBOUND"
      via: "AMQP publish"
      pattern: "MAIL_OUTBOUND"
    - from: "crates/axiam-api-rest/src/handlers/register.rs"
      to: "consent repository"
      via: "atomic consent insert at registration"
      pattern: "consent"
---

<objective>
Wire the three stubbed email flows (T19.11/12/13) to enqueue mail through the AMQP queue, keep reset/verify responses enumeration-safe (D-15), and record consent at registration (REQ-8). This is the producer side of the queue Plan 02 declared and Plan 03 consumes.

Purpose: Closes the three TODO(T19) stubs so auth flows send real (async) email and registration captures GDPR consent.
Output: Wired password_reset, email_verification, notification dispatcher, and consent-on-registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-email-delivery-gdpr-compliance/05-CONTEXT.md
@.planning/phases/05-email-delivery-gdpr-compliance/05-PATTERNS.md
@.planning/phases/05-email-delivery-gdpr-compliance/05-01-SUMMARY.md
@.planning/phases/05-email-delivery-gdpr-compliance/05-02-SUMMARY.md

<interfaces>
From Plan 02: OutboundMailMessage { mail_type, tenant_id, org_id, user_id, to_address, template_context, attempt_count, enqueued_at }; MailType; queues::MAIL_OUTBOUND.
From Plan 01: SurrealConsentRepository (create_consent at registration).
AMQP publish (05-PATTERNS.md Shared Patterns): channel.basic_publish("", queues::MAIL_OUTBOUND, default, &serde_json::to_vec(&msg)?, BasicProperties::default().with_delivery_mode(2)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire password_reset + email_verification handlers (enqueue, enumeration-safe)</name>
  <files>crates/axiam-api-rest/src/handlers/password_reset.rs, crates/axiam-api-rest/src/handlers/email_verification.rs</files>
  <read_first>
    - crates/axiam-api-rest/src/handlers/password_reset.rs (FULL — TODO stub lines 87-91; the discarded (_raw_token, _user_id, _expires_at) tuple; uniform-200 response shape)
    - crates/axiam-api-rest/src/handlers/email_verification.rs (FULL — TODO at line 105; note line 108 composition-layer comment)
    - 05-PATTERNS.md § password_reset.rs (enqueue pattern, D-15 unconditional response) and Shared Patterns § AMQP Publish
    - 05-RESEARCH.md Pitfall 7 (the tuple is discarded — bind named vars and use them)
  </read_first>
  <action>
    password_reset.rs: replace the `(_raw_token, _user_id, _expires_at)` stub with named bindings; build `OutboundMailMessage { mail_type: MailType::PasswordReset, tenant_id, org_id (resolve from tenant), user_id, to_address: email, template_context: json!({"token": raw_token, "expiry_time": expires_at.to_rfc3339()}), attempt_count: 0, enqueued_at: now }` and publish to `queues::MAIL_OUTBOUND` via the injected publisher channel (add `web::Data<Channel>` or the existing AMQP handle to the handler signature). On publish error: log warn, do NOT propagate (D-15). Response stays `HttpResponse::Ok().json(json!({"sent": true}))` unconditionally — same for the unknown-email branch (token is never returned).
    email_verification.rs: same shape with `MailType::EmailVerification`. Server-generated token only (no client-supplied token in the message).
    Author NEW named tests that FAIL before this wiring exists (they assert the D-15 behavior, not just that the handler compiles): `password_reset::unknown_email_enqueues_and_returns_sent`, `email_verification::unknown_email_enqueues_and_returns_sent` (unknown address → `{"sent": true}`, identical to the known-address branch), and `password_reset::known_email_never_returns_token` (known address → `{"sent": true}` with NO token field in the body, and an OutboundMailMessage enqueued to the mock channel). Match the actual module path you create; if you name the test module differently, update the Task 1 `<verify>` command and acceptance_criteria to the real paths. These tests are the gate — they must be red on the pre-D-15 partial handler and green after wiring.
  </action>
  <action_note>Implements T19.11/T19.12, D-14 (enqueue not synchronous send), D-15 (uniform enumeration-safe 200), server-generated-tokens-only. Never call EmailService::send() here.</action_note>
  <verify>
    <automated>cargo test -p axiam-api-rest password_reset::unknown_email_enqueues_and_returns_sent email_verification::unknown_email_enqueues_and_returns_sent password_reset::known_email_never_returns_token 2>&1 | tee /tmp/v05-04-t1.txt; grep -Ev '^#' /tmp/v05-04-t1.txt | grep -Eq 'test result: ok' && grep -Eq '3 passed|test result: ok\. 3' /tmp/v05-04-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MailType::PasswordReset' crates/axiam-api-rest/src/handlers/password_reset.rs` and `MailType::EmailVerification` in email_verification.rs
    - No `EmailService::send` or synchronous send call in either handler (grep absent)
    - Reset/verify response body is `{"sent": true}` unconditionally; no `token` field in any response json (grep handlers)
    - The NEW D-15 tests exist and pass: `cargo test -p axiam-api-rest password_reset::unknown_email_enqueues_and_returns_sent email_verification::unknown_email_enqueues_and_returns_sent password_reset::known_email_never_returns_token` shows `test result: ok` (all three). These tests assert uniform `{"sent": true}` for unknown AND known addresses and no token in the body — they must be RED before the wiring and GREEN after (proving the gate is real, not satisfied by pre-existing partial tests)
    - `cargo check -p axiam-api-rest --tests` no errors
  </acceptance_criteria>
  <done>Both handlers enqueue mail with server-generated tokens and return enumeration-safe 200; no token ever leaks in the body.</done>
</task>

<task type="auto">
  <name>Task 2: Notification dispatcher enqueues (T19.13) + consent at registration</name>
  <files>crates/axiam-audit/src/notification.rs, crates/axiam-api-rest/src/handlers/register.rs</files>
  <read_first>
    - crates/axiam-audit/src/notification.rs (FULL — TODO at line 68; current dispatch returns Vec<(event_name, recipient_emails)>)
    - crates/axiam-api-rest/src/handlers/register.rs (the CreateUser flow — where to insert consent atomically)
    - crates/axiam-db/src/repository/consent.rs (Plan 01 — create_consent signature; atomicity note pitfall 5)
    - 05-PATTERNS.md § notification.rs, § consent.rs (atomic single .query() create)
    - 05-RESEARCH.md § Consent Tracking Model (consent_type='terms_of_service', version, ip/user_agent)
  </read_first>
  <action>
    notification.rs: change `NotificationDispatcher::dispatch` to accept an AMQP publisher (Channel or a thin publisher trait) and, for each matched `(event, recipient)`, enqueue one `OutboundMailMessage { mail_type: MailType::Notification, ... template_context with event details ... }` to `queues::MAIL_OUTBOUND` instead of returning the recipient list. Resolve org_id/tenant_id per existing dispatcher context. Keep the function fire-and-forget (log+continue on publish error). Update callers in axiam-audit / consumers that expected the returned Vec.
    register.rs: after `CreateUser` succeeds, insert a consent record via SurrealConsentRepository with `consent_type='terms_of_service'`, `version` (configurable, default "current"), and request `ip_address`/`user_agent`. Use the atomic single-query approach (pitfall 5) so a user is never created without a consent record. Add a unit/integration test asserting registration yields exactly one consent row for the new user.
  </action>
  <action_note>Implements T19.13 (dispatcher enqueues), REQ-8 consent tracking (Art. 7 proof of consent). Minimal consent model per Claude's Discretion in CONTEXT.md (terms acceptance at registration; withdrawal deferred).</action_note>
  <verify>
    <automated>cargo test -p axiam-audit notification 2>&1 | tee /tmp/v05-04-t2.txt; cargo test -p axiam-api-rest consent 2>&1 | tee -a /tmp/v05-04-t2.txt; grep -Ev '^#' /tmp/v05-04-t2.txt | grep -Eq 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'OutboundMailMessage' crates/axiam-audit/src/notification.rs` and `MailType::Notification`
    - dispatch no longer returns the recipient Vec (signature changed to fire-and-forget) — confirm callers updated, crate compiles
    - register.rs inserts a consent record on success (grep `consent`); consent_type 'terms_of_service' present
    - registration-consent test passes: `cargo test -p axiam-api-rest consent` shows `test result: ok`
    - `cargo test -p axiam-audit notification` shows `test result: ok`; `cargo check -p axiam-audit -p axiam-api-rest --tests` no errors
  </acceptance_criteria>
  <done>Dispatcher enqueues notification mail (no recipient-list return); registration atomically records a terms_of_service consent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public reset/verify endpoint → client | response must not leak account existence or token |
| registration input → consent record | ip/user_agent captured for consent proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-enum | Information Disclosure | reset/verify response | mitigate | Uniform `{"sent": true}` regardless of account existence or delivery outcome (D-15) |
| T-5-token-leak | Information Disclosure | reset/verify response body | mitigate | Server-generated token enqueued only; never serialized to response |
| T-5-consent-gap | Repudiation | registration without consent | mitigate | Atomic single-query consent insert (pitfall 5) — user never exists without consent proof |
| T-5-template | Tampering | notification mail body | mitigate | render_email HTML-escapes in the consumer (D-18, Plan 03) |
| T-5-SC | Tampering | cargo installs | accept | No new packages — serde_json/lapin/axiam-amqp workspace |
</threat_model>

<verification>
- `cargo test -p axiam-api-rest password_reset::unknown_email_enqueues_and_returns_sent email_verification::unknown_email_enqueues_and_returns_sent password_reset::known_email_never_returns_token consent` green (the named D-15 enumeration-safe tests + consent)
- `cargo test -p axiam-audit notification` green
- `cargo check -p axiam-api-rest -p axiam-audit --tests` no errors
- `cargo fmt` + `cargo clippy -p axiam-api-rest -p axiam-audit -- -D warnings` clean before commit
</verification>

<success_criteria>
Three stubs closed: reset/verify enqueue server-generated tokens with enumeration-safe responses; dispatcher enqueues notification mail; registration atomically records consent.
</success_criteria>

<output>
Create `.planning/phases/05-email-delivery-gdpr-compliance/05-04-SUMMARY.md` when done. Note how org_id is resolved from tenant in the handlers (cross-reference T19.15 if a nil-org shortcut is used).
</output>
</content>
