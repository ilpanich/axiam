---
phase: 06-ci-cd-infrastructure-hardening
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/vite.config.ts
  - frontend/package.json
  - frontend/package-lock.json
  - crates/axiam-auth/src/config.rs
  - crates/axiam-api-rest/src/middleware/csrf.rs
  - crates/axiam-api-rest/src/lib.rs
  - crates/axiam-api-rest/src/tests/route_openapi_parity_test.rs
  - docker/docker-compose.dev.yml
autonomous: true
requirements: [REQ-9, REQ-10]
user_setup: []

must_haves:
  # CONTEXT.md decisions covered by this plan: D-15, D-17, D-18
  truths:
    - "Production frontend build emits no source maps and adds SRI integrity hashes to assets"
    - "OpenAPI schema matches actual endpoint signatures (route↔openapi parity test passes)"
    - "Cookie Secure flag is config-driven (default true) and set false for HTTP dev"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "sourcemap:false + vite-plugin-sri3"
      contains: "sourcemap"
    - path: "crates/axiam-api-rest/src/tests/route_openapi_parity_test.rs"
      provides: "two-direction route↔openapi parity test"
      contains: "api_doc"
    - path: "crates/axiam-auth/src/config.rs"
      provides: "cookie_secure bool field, default true"
      contains: "cookie_secure"
  key_links:
    - from: "crates/axiam-api-rest/src/middleware/csrf.rs"
      to: "AuthConfig.cookie_secure"
      via: ".secure(cookie_secure) replacing hardcoded .secure(true)"
      pattern: "secure\\(cookie_secure\\)"
    - from: "crates/axiam-api-rest/src/tests/route_openapi_parity_test.rs"
      to: "openapi::api_doc + permissions ROUTE_PERMISSION_MAP/PUBLIC_PATHS"
      via: "HashSet two-direction comparison"
      pattern: "ROUTE_PERMISSION_MAP"
---

<objective>
Harden the frontend production build (sourcemap:false + SRI via vite-plugin-sri3), add the route↔openapi parity test that guarantees OpenAPI accuracy, and make the cookie Secure flag config-driven (default true, false in HTTP dev) (D-17/D-15/D-18).

Purpose: OpenAPI schema matches endpoints (REQ-9 success criterion #5); supply-chain integrity of served assets; dev login works over plain HTTP without weakening prod.
Output: vite.config.ts + SRI plugin, parity test file + module wiring, cookie_secure AuthConfig field + parameterized csrf.rs, docker-compose.dev.yml cookie override.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-CONTEXT.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-RESEARCH.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-PATTERNS.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-VALIDATION.md

<!-- depends_on 06-01: both edit frontend/package.json (06-01 adds license field; this plan adds the SRI devDependency) → sequential to avoid collision -->

<interfaces>
From crates/axiam-api-rest/src/openapi.rs: `pub fn api_doc() -> utoipa::openapi::OpenApi` (line ~402); paths exposed as `spec.paths.paths: BTreeMap<String, PathItem>`. SAML paths gated `#[cfg(feature = "saml")]`.
From crates/axiam-api-rest/src/permissions.rs: `pub const PUBLIC_PATHS: &[&str]` (line ~188, uses `*` suffix for prefix entries); `pub const ROUTE_PERMISSION_MAP: &[(&str, &str, &str)]` (line ~252; METHOD, path, permission).
From crates/axiam-auth/src/config.rs: `fn default_true() -> bool` (line 5); existing field `#[serde(default = "default_true")] pub allow_missing_aud_as_user: bool` (lines 44-46); Default impl lines 90-117.
From crates/axiam-api-rest/src/middleware/csrf.rs: `access_cookie` (~line 190), `refresh_cookie` (~line 203), `csrf_cookie` (~line 219) — each hardcodes `.secure(true)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cookie_secure to AuthConfig + parameterize csrf.rs cookie helpers</name>
  <read_first>
    - crates/axiam-auth/src/config.rs (default_true helper line 5; allow_missing_aud_as_user field lines 44-46; Default impl lines 90-117)
    - crates/axiam-api-rest/src/middleware/csrf.rs (three cookie helpers ~lines 190-227, each with hardcoded `.secure(true)`)
    - crates/axiam-api-rest/src/handlers/ (call-sites of access_cookie/refresh_cookie/csrf_cookie — grep to find them)
    - 06-PATTERNS.md §"crates/axiam-auth/src/config.rs — add cookie_secure field" and §"crates/axiam-api-rest/src/middleware/csrf.rs — parameterize .secure()"
    - 06-RESEARCH.md §"Research Area 9: D-18 — Cookie Secure Flag"
  </read_first>
  <behavior>
    - With cookie_secure=true (default), built cookies carry the Secure attribute.
    - With cookie_secure=false, built cookies omit the Secure attribute.
    - AuthConfig deserialized with no cookie_secure key defaults to true (serde default_true).
  </behavior>
  <action>
    Add `#[serde(default = "default_true")] pub cookie_secure: bool` to AuthConfig after `allow_missing_aud_as_user`, with a doc comment noting it is read from `AXIAM__AUTH__COOKIE_SECURE` and must only be false in local HTTP dev (D-18). Add `cookie_secure: true` to the AuthConfig `Default` impl. Add a `cookie_secure: bool` parameter to `access_cookie`, `refresh_cookie`, and `csrf_cookie` in csrf.rs, replacing each `.secure(true)` with `.secure(cookie_secure)`. Update every call-site in `crates/axiam-api-rest/src/handlers/` to pass `config.auth.cookie_secure` (mirror how they already pass `config.auth.access_token_lifetime_secs`). Add a unit test in csrf.rs (or config.rs) asserting a cookie built with `false` has no Secure attribute and with `true` has it. `cargo fmt` + `cargo clippy -D warnings` on axiam-auth and axiam-api-rest.
  </action>
  <verify>
    <automated>grep -q 'cookie_secure' crates/axiam-auth/src/config.rs && grep -q 'secure(cookie_secure)' crates/axiam-api-rest/src/middleware/csrf.rs && cargo test -p axiam-api-rest cookie && cargo clippy -D warnings -p axiam-api-rest 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cookie_secure: bool` field exists in AuthConfig with `#[serde(default = "default_true")]` and is `true` in the Default impl.
    - All three csrf.rs helpers take `cookie_secure` and use `.secure(cookie_secure)`; no `.secure(true)` literal remains in csrf.rs.
    - All handler call-sites updated; crate compiles `-Dwarnings`.
    - Unit test proves Secure attribute toggles with the flag.
  </acceptance_criteria>
  <done>cookie_secure config field wired end-to-end; csrf cookies honor it; tests + clippy green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route↔OpenAPI parity test (D-15)</name>
  <read_first>
    - crates/axiam-api-rest/src/openapi.rs (line ~402 `pub fn api_doc()`; SAML paths under `#[cfg(feature = "saml")]` line ~404)
    - crates/axiam-api-rest/src/permissions.rs (PUBLIC_PATHS line ~188 with `*` prefix entries; ROUTE_PERMISSION_MAP line ~252)
    - crates/axiam-api-rest/src/middleware/authz.rs (existing PUBLIC_PATHS tests lines ~143-177 — the parity-test precedent to mirror)
    - crates/axiam-api-rest/src/lib.rs (how test modules are declared — inline vs separate file)
    - 06-PATTERNS.md §"route_openapi_parity_test.rs — parity test (D-15)" (confirmed APIs, path-normalization caveat, SAML cfg gate)
    - 06-RESEARCH.md §"Research Area 7: D-15" (utoipa Paths structure, Actix reflection limitation, normalization)
  </read_first>
  <behavior>
    - Test A: every ROUTE_PERMISSION_MAP path appears as a key in api_doc().paths.paths.
    - Test B: every api_doc() path key is in ROUTE_PERMISSION_MAP or matches a PUBLIC_PATHS entry (after stripping `*` prefix wildcards).
    - Both tests compile and pass under default features AND `--no-default-features` (SAML paths gated with `#[cfg(feature = "saml")]`).
  </behavior>
  <action>
    Create `crates/axiam-api-rest/src/tests/route_openapi_parity_test.rs` mirroring the authz.rs two-direction HashSet parity pattern. Implement the two tests from the behavior block. Handle path normalization (PATTERNS caveat): strip `*` suffix from PUBLIC_PATHS and use `starts_with` for prefix entries; account for utoipa path templates (`{id}`) vs ROUTE_PERMISSION_MAP patterns — normalize param placeholders so equal routes compare equal. Gate SAML-path assertions with `#[cfg(feature = "saml")]`. Wire the test module into the crate (`#[cfg(test)] mod tests;` in lib.rs or the existing convention). Failures must print the offending paths with `{missing:#?}`. `cargo fmt` + `cargo clippy -D warnings -p axiam-api-rest`.
  </action>
  <verify>
    <automated>cargo test -p axiam-api-rest route_openapi_parity && cargo test -p axiam-api-rest --no-default-features route_openapi_parity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p axiam-api-rest route_openapi_parity` passes (both directions).
    - The same test passes under `--no-default-features` (SAML paths properly cfg-gated).
    - Adding a route to ROUTE_PERMISSION_MAP without a `#[utoipa::path]` makes the test fail (and vice versa) — drift is caught.
    - clippy `-Dwarnings` clean.
  </acceptance_criteria>
  <done>Parity test guards OpenAPI accuracy in both directions under both feature sets.</done>
</task>

<task type="auto">
  <name>Task 3: Vite sourcemap:false + SRI plugin + dev cookie override</name>
  <read_first>
    - frontend/vite.config.ts (current 29 lines: react plugin, alias, server.proxy)
    - frontend/package.json (license field added by 06-01; add SRI devDependency here)
    - docker/docker-compose.dev.yml (currently surrealdb + rabbitmq only — NO server service; D-18 target reconciliation)
    - docker/nginx.conf (existing CSP `script-src 'self'`, `style-src 'self' 'unsafe-inline'` — SRI is compatible, no CSP change; Pitfall 6)
    - 06-PATTERNS.md §"frontend/vite.config.ts — sourcemap:false + SRI plugin"
    - 06-RESEARCH.md §"Research Area 1: D-17" (vite-plugin-sri3@2.0.0, CSP interaction) and §"Open Questions #2" (D-18 dev compose target)
  </read_first>
  <action>
    Frontend (D-17): `npm install --save-dev vite-plugin-sri3@2.0.0` in frontend/ (legitimacy approved in RESEARCH §Package Legitimacy Audit — npm registry confirmed). In vite.config.ts add `import sri from "vite-plugin-sri3"`, add `sri()` to the plugins array, and add `build: { sourcemap: false }`. Leave server.proxy and alias unchanged. No nginx CSP change needed (Pitfall 6). Dev cookie (D-18): reconcile the CONTEXT-named `docker-compose.dev.yml` target — RESEARCH/VALIDATION note it has no `server` service today. Add a minimal `axiam-server` service to docker-compose.dev.yml (mirroring the prod server service: image, depends_on surrealdb+rabbitmq, the DB/broker env, ports) WITH `AXIAM__AUTH__COOKIE_SECURE: "false"` in its environment block and a clear comment that this is dev-only (cookies must work over http://localhost). Keep the prod default true (handled by the AuthConfig default + prod compose). Run the production build to verify outputs.
  </action>
  <verify>
    <automated>grep -q 'sourcemap' frontend/vite.config.ts && grep -q 'vite-plugin-sri3' frontend/package.json && grep -q 'AXIAM__AUTH__COOKIE_SECURE' docker/docker-compose.dev.yml && (cd frontend && npm run build && grep -q 'integrity="sha' dist/index.html && test -z "$(ls dist/**/*.map 2>/dev/null)")</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/dist/index.html` contains `integrity="sha...` after `npm run build` (SRI hashes injected).
    - No `*.map` files in `frontend/dist/` (sourcemap:false).
    - `vite-plugin-sri3@2.0.0` in frontend/package.json devDependencies.
    - docker-compose.dev.yml has an axiam-server service with `AXIAM__AUTH__COOKIE_SECURE: "false"` commented dev-only.
  </acceptance_criteria>
  <done>Frontend build hardened with SRI + no sourcemaps; dev compose runs the server with Secure cookies disabled for HTTP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN/cache → browser | served JS/CSS assets could be tampered in transit or at a cache |
| browser ↔ API (cookies) | auth cookies traverse the network; HTTP exposes them |
| OpenAPI spec → API consumers | an inaccurate spec misleads clients about real endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | served frontend assets | mitigate | SRI integrity hashes (vite-plugin-sri3) — browser rejects altered assets (D-17) |
| T-06-14 | Information Disclosure | production source maps | mitigate | `build.sourcemap: false` — no source exposed in prod bundles (D-17) |
| T-06-15 | Information Disclosure | auth cookies over HTTP | mitigate | cookie_secure default true (prod); false only in explicit HTTP dev compose (D-18) |
| T-06-16 | Spoofing / Repudiation | API surface ↔ documented spec | mitigate | route↔openapi parity test prevents undocumented/phantom endpoints (D-15) |
</threat_model>

<verification>
- `cargo test -p axiam-api-rest route_openapi_parity` passes (default + --no-default-features).
- `cargo test -p axiam-api-rest cookie` passes; clippy clean.
- `npm run build` → dist/index.html has integrity= hashes, no .map files.
- docker-compose.dev.yml server service carries COOKIE_SECURE=false.
</verification>

<success_criteria>
ROADMAP success criterion #5 (OpenAPI matches endpoints) proven by parity test; frontend build hardened; dev cookie auth works over HTTP without weakening prod.
</success_criteria>

<output>
Create `.planning/phases/06-ci-cd-infrastructure-hardening/06-04-SUMMARY.md` when done.
</output>
