---
phase: 08-build-unblock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-server/Cargo.toml
  - crates/axiam-server/src/cleanup.rs
  - crates/axiam-server/tests/cleanup_task.rs
  - crates/axiam-server/tests/req5_oidc_e2e.rs
  - crates/axiam-server/tests/req5_secret_at_rest.rs
  - crates/axiam-server/tests/req5_clock_skew.rs
  - crates/axiam-server/tests/req7_service_account_aud.rs
autonomous: true
requirements: [REQ-12]

must_haves:
  truths:
    - "cargo build -p axiam-server --no-default-features succeeds with 0 errors, 0 warnings"
    - "cleanup.rs imports sha2 directly (no rsa::sha2 path remains)"
    - "uuid, chrono, serde_json, sha2 are in [dependencies] of axiam-server"
    - "rsa remains in [dev-dependencies] (test files still use it)"
    - "cargo clippy -p axiam-server --all-targets --no-default-features -- -D warnings passes"
  artifacts:
    - path: "crates/axiam-server/Cargo.toml"
      provides: "uuid/chrono/serde_json/sha2 in [dependencies], rsa retained in [dev-dependencies]"
      contains: "sha2 = { workspace = true }"
    - path: "crates/axiam-server/src/cleanup.rs"
      provides: "Correct sha2 import paths at the former rsa::sha2 sites"
      contains: "use sha2::{Digest, Sha256}"
  key_links:
    - from: "crates/axiam-server/src/cleanup.rs"
      to: "sha2 crate"
      via: "direct use sha2::{Digest, Sha256}"
      pattern: "use sha2::\\{Digest, Sha256\\}"
    - from: "crates/axiam-server/Cargo.toml [dependencies]"
      to: "[workspace.dependencies] sha2/uuid/chrono/serde_json"
      via: "workspace = true inheritance"
      pattern: "workspace = true"
---

<objective>
Unblock the `axiam-server` build so every subsequent remediation wave (Phases 9–12) can compile and run. The crate currently fails with 13 errors because `uuid`, `chrono`, and `serde_json` are only in `[dev-dependencies]` while `src/cleanup.rs` (library source) uses them, and because two `use rsa::sha2::{...}` statements depend on the `rsa` crate's `sha2` re-export instead of a direct `sha2` dependency.

Purpose: REQ-12 (Build Integrity, Wave 0). Blocks Phases 9–12.
Output: A green `cargo build -p axiam-server` and a clean `cargo clippy --all-targets -- -D warnings`, with `rsa` correctly retained only in `[dev-dependencies]` for test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-build-unblock/08-RESEARCH.md
@.planning/phases/08-build-unblock/08-VALIDATION.md

Project build conventions (mandatory):
- Build/check ONLY `-p axiam-server`. NEVER the whole workspace.
- On Arch, always pass `--no-default-features` (SAML/libxmlsec1 unavailable locally).
- Verify cargo results from actual OUTPUT TEXT, not exit codes (rtk masks them) and not IDE diagnostics.
- Run `cargo fmt -p axiam-server` and `cargo clippy -p axiam-server --all-targets --no-default-features -- -D warnings` before commit.

<interfaces>
<!-- Confirmed present in workspace-root Cargo.toml [workspace.dependencies]: -->
<!-- serde_json = "1" (line 30); sha2 = "0.10" (line 69); -->
<!-- uuid = { version = "1", features = ["v4","v5","serde"] } (line 78); -->
<!-- chrono = { version = "0.4", features = ["serde"] } (line 81) -->
<!-- All four resolve via `{ workspace = true }`. No version pin needed in axiam-server. -->

<!-- Current axiam-server/Cargo.toml [dev-dependencies] has, on these lines: -->
<!-- uuid (52), chrono (53), serde_json (56), rsa = { version = "0.9", features = ["sha2"] } (58) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate Cargo deps and fix cleanup.rs sha2 imports</name>
  <files>crates/axiam-server/Cargo.toml, crates/axiam-server/src/cleanup.rs</files>
  <read_first>
    - crates/axiam-server/Cargo.toml (current [dependencies] and [dev-dependencies] blocks)
    - crates/axiam-server/src/cleanup.rs (lines 33, 36, 260, 399 and the serde_json::json! usage sites)
    - Cargo.toml (workspace root) — confirm sha2/uuid/chrono/serde_json have [workspace.dependencies] entries
    - .planning/phases/08-build-unblock/08-RESEARCH.md ("Current State — Exact Findings" + "Fix pattern" sections)
  </read_first>
  <action>
    In `crates/axiam-server/Cargo.toml`: add four entries to the `[dependencies]` table, each as `{ workspace = true }` — `uuid`, `chrono`, `serde_json`, and `sha2`. Then delete the now-duplicate `uuid`, `chrono`, and `serde_json` entries (currently lines 52, 53, 56) from `[dev-dependencies]`. Do NOT touch the `rsa = { version = "0.9", features = ["sha2"] }` entry (currently line 58) — it MUST remain in `[dev-dependencies]` because `tests/req5_oidc_e2e.rs` and `tests/req5_clock_skew.rs` import `rsa` types directly (per RESEARCH Pitfall 1). Do NOT remove `rand_core`.

    In `crates/axiam-server/src/cleanup.rs`: at both line 260 (inside `purge_user`) and line 399 (inside `process_export_job`), change the statement `use rsa::sha2::{Digest, Sha256};` to `use sha2::{Digest, Sha256};`. The `sha2::Sha256` type is identical to `rsa::sha2::Sha256` (same crate, re-export only) — this is a path correction with no behavioral change. Leave the `chrono::Utc` (line 33), `uuid::Uuid` (line 36), and `serde_json::*` usages unchanged; they now resolve because the crates are in `[dependencies]`.

    After edits run `cargo fmt -p axiam-server`.
  </action>
  <verify>
    <automated>RUSTFLAGS="-Dwarnings" cargo build -p axiam-server --no-default-features 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTFLAGS="-Dwarnings" cargo build -p axiam-server --no-default-features` completes with 0 errors and 0 warnings (read actual output text; SC1)
    - `grep -n 'rsa::sha2' crates/axiam-server/src/cleanup.rs` returns 0 matches (SC2)
    - `grep -c 'use sha2::{Digest, Sha256}' crates/axiam-server/src/cleanup.rs` returns 2
    - `grep -n 'rsa' crates/axiam-server/Cargo.toml` shows the `rsa` entry only under `[dev-dependencies]` (still present)
    - `grep -E '^\s*(uuid|chrono|serde_json|sha2)\s*=' crates/axiam-server/Cargo.toml | grep -c 'workspace = true'` confirms the four prod entries exist (each as `workspace = true`)
  </acceptance_criteria>
  <done>Binary build is green under `-Dwarnings`; cleanup.rs uses `sha2::` directly; uuid/chrono/serde_json/sha2 in `[dependencies]`; rsa retained in `[dev-dependencies]`.</done>
</task>

<task type="auto">
  <name>Task 2: Clear the 9 test-file warnings and run the -D warnings gate</name>
  <files>crates/axiam-server/tests/cleanup_task.rs, crates/axiam-server/tests/req5_oidc_e2e.rs, crates/axiam-server/tests/req5_secret_at_rest.rs, crates/axiam-server/tests/req5_clock_skew.rs, crates/axiam-server/tests/req7_service_account_aud.rs</files>
  <read_first>
    - .planning/phases/08-build-unblock/08-RESEARCH.md ("The 9 Test Warnings" table + "Fix pattern for test warning removals" — exact file/line/item list)
    - crates/axiam-server/tests/cleanup_task.rs (line 11)
    - crates/axiam-server/tests/req5_oidc_e2e.rs (lines 18, 22, 476)
    - crates/axiam-server/tests/req5_secret_at_rest.rs (lines 16, 17, 45)
    - crates/axiam-server/tests/req5_clock_skew.rs (line 74)
    - crates/axiam-server/tests/req7_service_account_aud.rs (line 276)
  </read_first>
  <action>
    Clear all 9 warnings by removing the genuinely-unused items (do NOT use `#[allow(...)]` — per RESEARCH "Don't Hand-Roll"). Exact per-site actions:
    - `tests/cleanup_task.rs` line 11: delete the `use std::sync::Arc;` statement.
    - `tests/req5_oidc_e2e.rs` line 18: drop `Utc` from `use chrono::{Duration as CDuration, Utc};`, leaving `use chrono::Duration as CDuration;`.
    - `tests/req5_oidc_e2e.rs` line 22: delete `use rsa::pkcs8::EncodePublicKey;`.
    - `tests/req5_oidc_e2e.rs` line 476 (inside a nested `mod` block, not the file top — per RESEARCH Pitfall 4): change `use axiam_federation::jwks_cache::{JwksCacheEntry, JwksCacheMap, STALE_WINDOW};` to `use axiam_federation::jwks_cache::JwksCacheEntry;` (drop `JwksCacheMap` and `STALE_WINDOW`, keep `JwksCacheEntry`).
    - `tests/req5_secret_at_rest.rs` line 16: delete `use base64::Engine;`.
    - `tests/req5_secret_at_rest.rs` line 17: delete `use base64::engine::general_purpose::STANDARD;`.
    - `tests/req5_secret_at_rest.rs` line 45: change `let mut result = db` to `let result = db` (drop `mut`).
    - `tests/req5_clock_skew.rs` line 74: rename the unused binding `cache: Arc<JwksCache>` to `_cache: Arc<JwksCache>`.
    - `tests/req7_service_account_aud.rs` line 276: change `let app = test_app!(...)` to `let _app = test_app!(...)`.

    After edits run `cargo fmt -p axiam-server`.
  </action>
  <verify>
    <automated>cargo clippy -p axiam-server --all-targets --no-default-features -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTFLAGS="-Dwarnings" cargo build -p axiam-server --no-default-features --tests` completes with 0 errors and 0 warnings (SC3)
    - `cargo clippy -p axiam-server --all-targets --no-default-features -- -D warnings` exits with no warnings/errors (read actual output text; SC4)
    - `grep -n 'use std::sync::Arc' crates/axiam-server/tests/cleanup_task.rs` returns 0 matches
    - `grep -n 'JwksCacheMap\|STALE_WINDOW' crates/axiam-server/tests/req5_oidc_e2e.rs` returns 0 matches
    - No `#[allow(` attribute was added to any of the five test files (`grep -rn '#\[allow(' crates/axiam-server/tests/` shows no new entries introduced by this task)
  </acceptance_criteria>
  <done>All 9 test warnings cleared without `#[allow]`; `cargo clippy --all-targets -- -D warnings` is green; binary and test builds both compile clean under `-Dwarnings`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase introduces no new trust boundary. No new data flows, endpoints, or inputs cross any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `sha2` import path change in cleanup.rs | accept | `sha2::Sha256` and `rsa::sha2::Sha256` are the identical type from the identical crate (rsa re-exports sha2). The change is a path correction only — no cryptographic behavior changes. No mitigation required. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase (all crates already in `[workspace.dependencies]`); RESEARCH "Package Legitimacy Audit" confirms no audit required. No install task exists; no legitimacy checkpoint needed. |

No new attack surface. All changes are Cargo manifest dependency placement, `use`-path corrections, and unused import/variable removal in test files. No auth, crypto-logic, parsing, or external-input code is modified. ASVS L1 controls are not applicable to this phase.
</threat_model>

<verification>
Run in order (project convention: `-p axiam-server` only, `--no-default-features` on Arch):

1. `RUSTFLAGS="-Dwarnings" cargo build -p axiam-server --no-default-features` → 0 errors, 0 warnings (SC1)
2. `grep -n 'rsa::sha2' crates/axiam-server/src/cleanup.rs` → 0 matches (SC2)
3. `RUSTFLAGS="-Dwarnings" cargo build -p axiam-server --no-default-features --tests` → 0 errors, 0 warnings (SC3 local sim)
4. `cargo clippy -p axiam-server --all-targets --no-default-features -- -D warnings` → clean (SC4)
5. `grep 'rsa' crates/axiam-server/Cargo.toml` → `rsa` present only under `[dev-dependencies]`

Manual (not automatable locally): push branch; confirm GitHub Actions `build`, `build-no-saml`, and `test` jobs go red→green (SC3 true CI).
</verification>

<success_criteria>
- SC1: `cargo build -p axiam-server` succeeds with uuid/chrono/serde_json/sha2 in `[dependencies]`.
- SC2: `cleanup.rs` lines 260,399 import `sha2::{Digest, Sha256}`; no `rsa::sha2` path remains.
- SC3: Test build compiles clean under `-Dwarnings`; CI `build`/`build-no-saml`/`test` jobs go green.
- SC4: All 9 test-file warnings cleared; `cargo clippy --all-targets -- -D warnings` passes.
- `rsa` retained in `[dev-dependencies]` (NOT removed — test files require it).
</success_criteria>

<output>
Create `.planning/phases/08-build-unblock/08-01-SUMMARY.md` when done.
</output>
