---
phase: 09-critical-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-api-grpc/src/middleware/auth.rs
  - crates/axiam-api-grpc/src/server.rs
  - crates/axiam-api-grpc/src/lib.rs
  - crates/axiam-api-grpc/tests/grpc_auth_test.rs
  - k8s/ingress.yml
autonomous: true
requirements: [REQ-13]
must_haves:
  truths:
    - "gRPC call without a bearer token returns UNAUTHENTICATED"
    - "gRPC call with a valid bearer token succeeds (passes the interceptor)"
    - "tenant_id/subject_id are derived from verified claims, not trusted from the request body"
    - "public gRPC ingress is removed from k8s/ingress.yml"
  artifacts:
    - path: "crates/axiam-api-grpc/src/middleware/auth.rs"
      provides: "Tonic AuthInterceptor validating bearer JWT via validate_access_token"
      contains: "impl Interceptor for AuthInterceptor"
    - path: "crates/axiam-api-grpc/src/server.rs"
      provides: "interceptor wired via with_interceptor"
      contains: "with_interceptor"
    - path: "crates/axiam-api-grpc/tests/grpc_auth_test.rs"
      provides: "accept/reject interceptor tests"
    - path: "k8s/ingress.yml"
      provides: "no axiam-grpc-ingress object"
  key_links:
    - from: "AuthInterceptor::call"
      to: "axiam_auth::token::validate_access_token"
      via: "synchronous validation of the Bearer metadata"
      pattern: "validate_access_token"
    - from: "server.rs"
      to: "AuthInterceptor"
      via: "AuthorizationServiceServer::with_interceptor"
      pattern: "with_interceptor"
---

<objective>
Close the unauthenticated-gRPC defect (SEC-003): the Tonic server registers no auth interceptor, so any caller reaches `check_access`/`batch_check_access` and the service trusts caller-supplied `tenant_id`/`subject_id`. Add a Tonic 0.14 `AuthInterceptor` that validates the bearer JWT via `validate_access_token`, wire it per-service with `with_interceptor`, remove the public gRPC ingress, and add accept/reject tests.

Purpose: gRPC authz checks must be callable only by identities the server can cryptographically verify.
Output: New interceptor module, wired server, new test file, ingress hardened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-critical-remediation/09-RESEARCH.md
@.planning/phases/09-critical-remediation/09-PATTERNS.md
@.planning/phases/09-critical-remediation/09-VALIDATION.md

<interfaces>
Tonic 0.14 interceptor (CITED docs.rs/tonic/0.14):

    use tonic::{Request, Status};
    use tonic::service::Interceptor;
    impl Interceptor for AuthInterceptor {
        fn call(&mut self, mut req: Request<()>) -> Result<Request<()>, Status> {
            let token = req.metadata().get("authorization")
                .and_then(|v| v.to_str().ok())
                .and_then(|s| s.strip_prefix("Bearer "))
                .ok_or_else(|| Status::unauthenticated("missing bearer token"))?;
            let claims = validate_access_token(token, &self.auth_config)
                .map_err(|_| Status::unauthenticated("invalid or expired token"))?;
            req.extensions_mut().insert(claims);
            Ok(req)
        }
    }

Wiring (per-service, NOT a Tower layer — Pitfall 1):

    let authz_svc = AuthorizationServiceServer::with_interceptor(
        AuthorizationServiceImpl::new(engine),
        AuthInterceptor::new(auth_config.clone()),
    );

- `axiam_auth::token::validate_access_token(token, &AuthConfig) -> Result<ValidatedClaims, _>` is SYNCHRONOUS (token.rs:281-285) — safe in `Interceptor::call` with no spawn.
- The validated claims carry `sub` (user UUID string) and `tenant_id`. Service handlers read them via `request.extensions().get::<ValidatedClaims>()` and use them for subject_id/tenant_id instead of trusting the request body.
- `AuthConfig` must be `Clone` to live in the interceptor (Pitfall 6 — verify, it likely already is).
- Do NOT attach `build_grpc_governor_layer` in the test server: `SmartIpKeyExtractor` panics without a real peer IP on in-process connections (grpc_authz_test.rs:181-182).
- k8s/server/service.yml is ALREADY `ClusterIP` (verified) — no service-type change needed. Only the `axiam-grpc-ingress` object in k8s/ingress.yml:36-63 is removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AuthInterceptor and wire it into the gRPC server</name>
  <files>crates/axiam-api-grpc/src/middleware/auth.rs, crates/axiam-api-grpc/src/server.rs, crates/axiam-api-grpc/src/lib.rs</files>
  <read_first>
    - crates/axiam-api-grpc/src/server.rs (modify target — builds Tonic Server with only a rate-limiting Tower layer)
    - crates/axiam-api-grpc/src/lib.rs (add `pub mod middleware;` / module wiring)
    - crates/axiam-auth/src/token.rs:281-285 (validate_access_token signature — sync, returns claims)
    - crates/axiam-api-rest/src/extractors/auth.rs (AuthenticatedUser — the REST analog of claims→identity, for field-name parity)
    - crates/axiam-api-grpc/src/services (the check_access / batch_check_access handlers — to read claims from extensions and stop trusting body tenant_id/subject_id)
  </read_first>
  <action>
    Create `middleware/auth.rs` with a `#[derive(Clone)] pub struct AuthInterceptor { auth_config: AuthConfig }`, a `new(auth_config)` constructor, and `impl Interceptor` whose `call` extracts the `authorization` metadata, strips `Bearer `, calls `validate_access_token`, returns `Status::unauthenticated` on missing/invalid token, and inserts the validated claims into `req.extensions_mut()`. Register the module in `lib.rs`.
    In `server.rs`: replace `AuthorizationServiceServer::new(impl)` with `AuthorizationServiceServer::with_interceptor(impl, AuthInterceptor::new(auth_config.clone()))`. Thread an `AuthConfig` into the server-construction function (mirror how the rate-limit config is passed). Keep the existing rate-limit Tower layer.
    In the service handler(s): read the claims from `request.extensions().get::<ValidatedClaims>()` and use them as the authoritative tenant_id/subject_id; if the request body still carries those fields, cross-validate (reject on mismatch) rather than blindly trust. If `AuthConfig` is not `Clone`, derive/add `Clone` (Pitfall 6).
    mTLS peer-cert extraction is out of scope this phase (A1) — bearer-only.
  </action>
  <verify>
    <automated>cargo check -p axiam-api-grpc --tests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `middleware/auth.rs` contains `impl Interceptor for AuthInterceptor` and a `validate_access_token` call returning `Status::unauthenticated` on failure.
    - SOURCE: `server.rs` contains `with_interceptor` and no longer constructs the authz service via bare `::new` without the interceptor.
    - SOURCE: the service handler reads claims from request extensions and does not trust body `tenant_id`/`subject_id` unconditionally.
    - BUILD: `cargo check -p axiam-api-grpc --tests` shows no `error[` lines.
  </acceptance_criteria>
  <done>Interceptor validates bearer JWTs and is wired per-service; crate compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gRPC interceptor accept/reject tests (Wave 0 gap — new file)</name>
  <files>crates/axiam-api-grpc/tests/grpc_auth_test.rs</files>
  <behavior>
    - A gRPC `check_access` call with NO authorization metadata returns `tonic::Code::Unauthenticated`.
    - A gRPC `check_access` call with a valid `Bearer <jwt>` passes the interceptor and returns `Ok` (allowed/denied by authz, not rejected at the interceptor layer).
    - A gRPC call with a malformed/expired token returns `Unauthenticated`.
  </behavior>
  <read_first>
    - crates/axiam-api-grpc/tests/grpc_authz_test.rs (EXACT analog — copy `setup()`/`make_engine()`/`start_test_server`/`connect_client` verbatim; lines 1-35 imports, 184-211 harness, 217-348 test shape)
    - crates/axiam-auth/src/token.rs (find `create_access_token` to mint a valid test token; and the AuthConfig/keypair builder)
    - crates/axiam-api-rest/tests (look for an existing `create_test_jwt`/test AuthConfig helper to mirror for `test_auth_config()` + `mint_test_token()`)
    - 09-PATTERNS.md (test_auth_config / mint_test_token guidance + with_interceptor client snippet)
  </read_first>
  <action>
    Create `tests/grpc_auth_test.rs`. Copy the DB setup and harness from `grpc_authz_test.rs` but change `start_test_server` to accept an `AuthConfig` and build the service with `AuthorizationServiceServer::with_interceptor(impl, AuthInterceptor::new(auth_config))`. Add a `test_auth_config()` helper that builds an `AuthConfig` with an ephemeral Ed25519 keypair and a `mint_test_token(tenant_id, user_id, &auth_config)` helper calling `axiam_auth::token::create_access_token`. Write the three behavior tests. For the accept case, attach the token via `AuthorizationServiceClient::with_interceptor(channel, move |mut req| { req.metadata_mut().insert("authorization", format!("Bearer {token}").parse().unwrap()); Ok(req) })`. Do NOT attach the governor layer (panics on in-process peer IP).
    Run with the `client` feature: `--features client`.
  </action>
  <verify>
    <automated>cargo test -p axiam-api-grpc --features client --test grpc_auth_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: no-token call asserts `err.code() == tonic::Code::Unauthenticated`.
    - BEHAVIOR: valid-token call asserts `resp.is_ok()`.
    - BEHAVIOR: malformed/expired-token call asserts `Unauthenticated`.
    - TEST: `cargo test -p axiam-api-grpc --features client --test grpc_auth_test` reports `test result: ok` (read OUTPUT text).
  </acceptance_criteria>
  <done>New test file proves the interceptor accepts valid and rejects missing/invalid tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Remove public gRPC ingress</name>
  <files>k8s/ingress.yml</files>
  <read_first>
    - k8s/ingress.yml (modify target — remove the `axiam-grpc-ingress` object at lines 36-63, including its GRPC backend-protocol annotation and the port-50051 backend)
    - k8s/server/service.yml (CONFIRM it is already ClusterIP — verified; no change expected)
  </read_first>
  <action>
    Delete the entire `axiam-grpc-ingress` `Ingress` resource from `k8s/ingress.yml` so gRPC (port 50051) is no longer publicly exposed; it stays reachable only in-cluster via the existing ClusterIP service. Leave the HTTP/frontend ingress objects intact. Confirm `k8s/server/service.yml` is `ClusterIP` (it is) — make no change there unless it is not.
  </action>
  <verify>
    <automated>grep -c 'axiam-grpc-ingress' k8s/ingress.yml; grep -c 'backend-protocol: "GRPC"' k8s/ingress.yml</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'axiam-grpc-ingress' k8s/ingress.yml` returns 0.
    - SOURCE: `grep -c 'backend-protocol: "GRPC"' k8s/ingress.yml` returns 0.
    - SOURCE: the frontend/HTTP ingress object remains present (non-empty file).
  </acceptance_criteria>
  <done>Public gRPC ingress removed; gRPC reachable only in-cluster.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gRPC client (service mesh) → Tonic server | Caller-supplied metadata + body fields are untrusted until the bearer JWT is validated |
| public internet → k8s ingress | gRPC port 50051 must not be publicly routable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 (SEC-003) | Elevation of Privilege | axiam-api-grpc check_access/batch_check_access | mitigate | AuthInterceptor validates bearer JWT via validate_access_token; identity derived from claims. Observable: no-token call returns UNAUTHENTICATED. |
| T-09-04 (SEC-003) | Spoofing | request-body tenant_id/subject_id | mitigate | Derive identity from verified claims; cross-validate/reject body mismatch. |
| T-09-05 (SEC-003) | Information Disclosure | public gRPC ingress | mitigate | Remove axiam-grpc-ingress; ClusterIP only. Observable: no `axiam-grpc-ingress` in k8s/ingress.yml. |
</threat_model>

<verification>
- `cargo check -p axiam-api-grpc --tests` clean.
- `cargo test -p axiam-api-grpc --features client --test grpc_auth_test` green.
- `grep -c axiam-grpc-ingress k8s/ingress.yml` == 0.
- `cargo fmt -p axiam-api-grpc` + `cargo clippy -p axiam-api-grpc --tests --features client -- -D warnings` clean before commit.
</verification>

<success_criteria>
SEC-003 closed: Tonic interceptor validates bearer JWT, identity from verified claims, public gRPC ingress removed, accept/reject tests pass (REQ-13 success criterion 2).
</success_criteria>

<output>
Create `.planning/phases/09-critical-remediation/09-02-SUMMARY.md` when done
</output>
