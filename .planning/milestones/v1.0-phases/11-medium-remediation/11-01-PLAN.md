---
phase: 11-medium-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-db/src/helpers.rs
  - crates/axiam-db/src/lib.rs
  - crates/axiam-db/src/error.rs
  - crates/axiam-db/src/repository/user.rs
  - crates/axiam-db/src/repository/role.rs
  - crates/axiam-db/src/repository/export_job.rs
  - crates/axiam-db/src/repository/account_deletion.rs
  - crates/axiam-db/src/schema.rs
  - crates/axiam-db/src/repository/email_config.rs
  - crates/axiam-api-rest/src/handlers/gdpr.rs
  - crates/axiam-api-rest/src/error.rs
  - crates/axiam-api-rest/src/handlers/certificates.rs
  - crates/axiam-api-rest/src/handlers/ca_certificates.rs
  - crates/axiam-api-rest/src/handlers/organizations.rs
  - crates/axiam-api-rest/src/handlers/permissions.rs
  - crates/axiam-api-rest/src/handlers/resources.rs
  - crates/axiam-api-rest/src/handlers/scopes.rs
  - crates/axiam-api-rest/src/handlers/pgp_keys.rs
  - crates/axiam-api-rest/src/handlers/users.rs
autonomous: true
requirements: [REQ-15]

must_haves:
  truths:
    - "Creating a resource that violates a unique index returns HTTP 409, not 500"
    - "Shared repo helpers (parse_uuid, CountRow, take_first_or_not_found) compile in axiam-db and are used by repos in place of duplicated private structs"
    - "REST create handlers lacking dedicated request DTOs gain typed CreateXxxRequest/UpdateXxxRequest structs with email/password validation where applicable"
  artifacts:
    - path: "crates/axiam-db/src/helpers.rs"
      provides: "Shared parse_uuid, CountRow, take_first_or_not_found helpers"
      contains: "pub fn parse_uuid"
    - path: "crates/axiam-db/src/error.rs"
      provides: "DbError::AlreadyExists variant + From mapping to AxiamError::AlreadyExists"
      contains: "AlreadyExists"
  key_links:
    - from: "crates/axiam-db/src/error.rs"
      to: "axiam-core AxiamError::AlreadyExists"
      via: "From<DbError> for AxiamError mapping"
      pattern: "AlreadyExists"
    - from: "crates/axiam-api-rest/src/error.rs"
      to: "HTTP 409 CONFLICT"
      via: "ResponseError impl status_code()"
      pattern: "CONFLICT"
---

<objective>
Consolidate the repository layer: extract duplicated repo boilerplate into shared helpers, add a `DbError::AlreadyExists` variant that maps unique-index/duplicate violations to HTTP 409, and give REST create/update handlers typed request DTOs with input validation. This is the foundational plan — `helpers.rs` and the `AlreadyExists` error variant are consumed by later plans.

Purpose: Closes CQ-B10 (shared helpers), CQ-B11 (wrong DbError variant for parse failures), CQ-B17 (unique edge indexes), CQ-B25 (request DTOs), CQ-B26 (user create validation), and the 409-mapping half of REQ-15 AC-1.
Output: `crates/axiam-db/src/helpers.rs`, extended `DbError`, unique edge indexes in `schema.rs`, request DTO structs across 8 handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-medium-remediation/11-RESEARCH.md
@.planning/phases/11-medium-remediation/11-PATTERNS.md
@.planning/phases/11-medium-remediation/11-VALIDATION.md

<interfaces>
<!-- From 11-PATTERNS.md — executor uses these directly, no exploration needed. -->

axiam-db helper imports (from user.rs / role.rs):
  use axiam_core::error::AxiamResult;
  use axiam_core::repository::{PaginatedResult, Pagination};
  use surrealdb_types::SurrealValue;
  use uuid::Uuid;
  use crate::error::DbError;

CountRow (currently duplicated private in user.rs:146-150, role.rs:52-55):
  #[derive(Debug, SurrealValue)] pub struct CountRow { pub total: u64 }

Current DbError (error.rs): Surreal(#[from]), Migration(String), NotFound { entity, id }
AxiamError::AlreadyExists already exists in axiam-core and is already mapped to
  StatusCode::CONFLICT in axiam-api-rest/src/error.rs:39 — verify exact field name in axiam-core.

CreateUserRequest/UpdateUserRequest DTO pattern: handlers/users.rs:20-34
  #[derive(Debug, Deserialize, utoipa::ToSchema)] pub struct CreateXxxRequest { ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared axiam-db helpers + AlreadyExists error variant</name>
  <files>crates/axiam-db/src/helpers.rs, crates/axiam-db/src/lib.rs, crates/axiam-db/src/error.rs, crates/axiam-api-rest/src/error.rs</files>
  <read_first>
    - crates/axiam-db/src/repository/user.rs (CountRow at ~146-150, parse pattern, get_by_id into_iter().next() NotFound)
    - crates/axiam-db/src/repository/role.rs (CountRow at ~52-55, parse_uuid analog at ~36-39)
    - crates/axiam-db/src/error.rs (current DbError enum + From<DbError> for AxiamError)
    - crates/axiam-api-rest/src/error.rs (ResponseError impl — confirm AxiamError::AlreadyExists already maps to CONFLICT at ~line 39)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (helpers.rs and error.rs Pattern Assignment sections)
  </read_first>
  <behavior>
    - parse_uuid("not-a-uuid", "user_id") returns Err(DbError) whose message contains "invalid user_id UUID"
    - parse_uuid(valid_uuid_str, "x") returns Ok(uuid)
    - take_first_or_not_found(vec![], "user", "abc") returns Err(DbError::NotFound { entity: "user", id: "abc" })
    - take_first_or_not_found(vec![1,2], "x", "y") returns Ok(1)
    - DbError::AlreadyExists { entity } converts via From<DbError> to AxiamError::AlreadyExists
  </behavior>
  <action>
    Create `crates/axiam-db/src/helpers.rs` exposing: `pub struct CountRow { pub total: u64 }` (derive Debug, SurrealValue) — the canonical version replacing the private duplicates in user.rs and role.rs; `pub fn parse_uuid(s: &str, field: &str) -> Result<Uuid, DbError>` returning a DbError whose message embeds the field name; `pub fn take_first_or_not_found<T>(items: Vec<T>, entity: &str, id: &str) -> Result<T, DbError>` returning DbError::NotFound on empty. Add `pub mod helpers;` and `pub use helpers::{CountRow, parse_uuid, take_first_or_not_found};` to lib.rs. In error.rs add the `AlreadyExists { entity: String }` variant to DbError (thiserror message "Record already exists: {entity}") and add the arm `DbError::AlreadyExists { entity } => AxiamError::AlreadyExists { ... }` to the existing From<DbError> for AxiamError impl — match the exact field name verified in axiam-core. Per CQ-B11, the existing parse-failure sites in repos misuse DbError::Migration; the new parse_uuid uses a non-Migration variant (Serialization if present, else keep Migration consistent with note in PATTERNS.md). NO code fences in implementation — follow the analog structures read above. Add a `#[cfg(test)]` module in helpers.rs covering the behaviors above.
  </action>
  <verify>
    <automated>cargo test -p axiam-db --lib -- helpers</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p axiam-db --lib -- helpers` passes (per 11-VALIDATION.md row: REQ-15 AC-1, Wave 0 helpers.rs)
    - Source assertion: helpers.rs contains `pub fn parse_uuid` and `pub struct CountRow`; error.rs DbError has `AlreadyExists` and the From impl maps it to AxiamError::AlreadyExists
    - `cargo check -p axiam-db -p axiam-api-rest` clean (disk-constraint: targeted -p only, no workspace build)
  </acceptance_criteria>
  <done>helpers.rs compiles with passing tests; DbError::AlreadyExists exists and propagates to AxiamError::AlreadyExists which already maps to HTTP 409 in the REST error handler.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt helpers in repos + unique edge indexes + email_config UPSERT + GDPR handler cleanup (CQ-B10/B11/B17/B41/B39)</name>
  <files>crates/axiam-db/src/repository/user.rs, crates/axiam-db/src/repository/role.rs, crates/axiam-db/src/repository/export_job.rs, crates/axiam-db/src/repository/account_deletion.rs, crates/axiam-db/src/repository/email_config.rs, crates/axiam-db/src/schema.rs, crates/axiam-api-rest/src/handlers/gdpr.rs</files>
  <read_first>
    - crates/axiam-db/src/repository/user.rs (private CountRow + inline parse_str sites)
    - crates/axiam-db/src/repository/role.rs (private CountRow + parse_uuid analog)
    - crates/axiam-db/src/repository/export_job.rs (DbError::Migration misuse for UUID parse — CQ-B11)
    - crates/axiam-db/src/repository/account_deletion.rs (same CQ-B11 misuse)
    - crates/axiam-db/src/schema.rs (~470-484 — five edge tables lacking unique (in,out) indexes — CQ-B17)
    - crates/axiam-db/src/repository/email_config.rs (insert/update split — should be UPSERT keyed on (scope, scope_id) — CQ-B41)
    - crates/axiam-api-rest/src/handlers/gdpr.rs (duplicate export requests; repeated audit-append blocks; weak cancel token — CQ-B39)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (Cluster 1 findings)
  </read_first>
  <action>
    Replace the private `CountRow` definitions in user.rs and role.rs with `use crate::helpers::CountRow`. Replace inline `Uuid::parse_str(...).map_err(|e| DbError::Migration(...))` UUID-parse sites in export_job.rs and account_deletion.rs (and the analog sites in user.rs/role.rs) with `crate::helpers::parse_uuid(s, "field_name")` (CQ-B11 — stops misusing the Migration variant for parse errors). In schema.rs (~470-484), add `DEFINE INDEX IF NOT EXISTS idx_<edge>_unique ON TABLE <edge> FIELDS in, out UNIQUE` for the five edge tables missing it (CQ-B17), so duplicate edge creation surfaces as a unique-index violation that maps to DbError::AlreadyExists → 409. CQ-B41: in email_config.rs replace the insert/update split with a single UPSERT keyed deterministically on `(scope, scope_id)`. CQ-B39: in handlers/gdpr.rs dedupe concurrent export requests (one in-flight export per user), factor the repeated audit-append blocks into a shared helper, and strengthen the cancel token to 256-bit. Keep existing query semantics otherwise unchanged. NO code fences — apply the analog patterns.
  </action>
  <verify>
    <automated>cargo check -p axiam-db</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p axiam-db` clean (per 11-VALIDATION.md row: shared repo helpers compile in place of per-repo structs)
    - Source assertion: user.rs and role.rs reference `crate::helpers::CountRow` (no private CountRow struct remains); export_job.rs / account_deletion.rs call `parse_uuid` not `DbError::Migration` for UUID parse
    - Source assertion: schema.rs contains `FIELDS in, out UNIQUE` for the five edge tables
    - Source assertion: email_config.rs uses a single UPSERT keyed on (scope, scope_id); gdpr.rs dedupes export requests, uses a shared audit-append helper, and a 256-bit cancel token
    - `cargo check -p axiam-db -p axiam-api-rest` clean
  </acceptance_criteria>
  <done>Repos use the shared helpers; the five edge tables have unique (in,out) indexes; email_config UPSERTs; GDPR handler deduped with a 256-bit cancel token; crates compile clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add request DTOs + input validation to 8 handlers (CQ-B25/B26)</name>
  <files>crates/axiam-api-rest/src/handlers/certificates.rs, crates/axiam-api-rest/src/handlers/ca_certificates.rs, crates/axiam-api-rest/src/handlers/organizations.rs, crates/axiam-api-rest/src/handlers/permissions.rs, crates/axiam-api-rest/src/handlers/resources.rs, crates/axiam-api-rest/src/handlers/scopes.rs, crates/axiam-api-rest/src/handlers/pgp_keys.rs, crates/axiam-api-rest/src/handlers/users.rs</files>
  <read_first>
    - crates/axiam-api-rest/src/handlers/users.rs (~20-34 CreateUserRequest/UpdateUserRequest DTO pattern; ~98-135 create path lacking email/password validation — CQ-B26)
    - crates/axiam-api-rest/src/handlers/roles.rs (already uses the DTO pattern per Phase 9/10 — reference, do not duplicate)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (Request DTOs (CQ-B25) section, list of 8 handlers)
  </read_first>
  <action>
    For each handler that takes a raw model or untyped body on create/update, add `#[derive(Debug, Deserialize, utoipa::ToSchema)]` `CreateXxxRequest` and `UpdateXxxRequest` (all-optional) structs with typed fields matching the domain model, per the users.rs analog (CQ-B25). For users.rs create path (CQ-B26), validate email format (parse via the email validation already available in the workspace, e.g. EmailAddress::from_str) and run the existing password policy check before insert — return a 400/validation error on failure rather than inserting invalid data. Verify roles.rs before editing (it already uses the pattern — skip if present). Do NOT change route signatures beyond swapping the body extractor type to the new DTO. NO code fences — follow the users.rs analog.
  </action>
  <verify>
    <automated>cargo check -p axiam-api-rest</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p axiam-api-rest` clean (disk-constraint targeted check)
    - Source assertion: certificates.rs, ca_certificates.rs, organizations.rs, permissions.rs, resources.rs, scopes.rs, pgp_keys.rs each define a `CreateXxxRequest` struct
    - Source assertion: users.rs create path calls an email-format validation and the password policy check before the repo insert
  </acceptance_criteria>
  <done>All 8 handlers expose typed request DTOs; user create validates email + password policy; axiam-api-rest compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST API | Untrusted JSON bodies cross into handlers; must be typed + validated |
| repository → SurrealDB | Duplicate/constraint violations must surface as typed errors, not raw 500s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-IDX | Tampering | edge tables (schema.rs) | mitigate | CQ-B17 — unique (in,out) indexes; duplicate edge → DbError::AlreadyExists → HTTP 409 (ASVS V5) |
| T-11-01-VAL | Tampering / Info Disclosure | users.rs create path | mitigate | CQ-B26 — email-format + password-policy validation before insert (ASVS V5 Input Validation) |
| T-11-01-DTO | Tampering | 8 create/update handlers | mitigate | CQ-B25 — typed CreateXxxRequest DTOs constrain accepted fields (ASVS V5) |
| T-11-01-ERR | Information Disclosure | repo parse-failure sites | mitigate | CQ-B11 — parse failures use parse_uuid (typed) not Migration; no raw error leakage |
</threat_model>

<verification>
- `cargo test -p axiam-db --lib -- helpers` green
- `cargo check -p axiam-db -p axiam-api-rest` clean
- Source: DbError::AlreadyExists → AxiamError::AlreadyExists → HTTP 409 chain intact
- DISK CONSTRAINT: no `cargo build --workspace` / `cargo test --workspace`; targeted `-p` only.
</verification>

<success_criteria>
- helpers.rs exists with passing unit tests
- DbError::AlreadyExists maps to HTTP 409
- Five edge tables have unique (in,out) indexes
- 8 handlers have typed request DTOs; user create validates email + password
</success_criteria>

<output>
Create `.planning/phases/11-medium-remediation/11-01-SUMMARY.md` when done.
</output>
