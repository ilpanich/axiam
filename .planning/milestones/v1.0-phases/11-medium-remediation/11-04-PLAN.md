---
phase: 11-medium-remediation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/server/configmap.yml
  - k8s/server/secret.yml
  - k8s/namespace.yml
  - k8s/network-policy/allow-ingress-to-surrealdb.yml
  - k8s/network-policy/allow-ingress-to-rabbitmq.yml
  - k8s/ingress.yml
  - docker/nginx.conf
  - docker/docker-compose.prod.yml
autonomous: true
requirements: [REQ-15]

must_haves:
  truths:
    - "k8s ConfigMap/Secret keys use the double-underscore AXIAM__ prefix so config-rs parses them"
    - "Namespace enforces the PodSecurity restricted profile (enforce label, not warn-only)"
    - "Receiver-side NetworkPolicies restrict ingress to SurrealDB and RabbitMQ to only axiam-server pods"
    - "nginx and the k8s ingress proxy /oauth2/* and /.well-known/* to axiam-server:8090"
    - "Prod docker-compose has no literal default credentials; DB/RabbitMQ creds come from fail-fast env vars"
  artifacts:
    - path: "k8s/network-policy/allow-ingress-to-surrealdb.yml"
      provides: "Receiver-side ingress policy for SurrealDB"
      contains: "component: surrealdb"
    - path: "k8s/network-policy/allow-ingress-to-rabbitmq.yml"
      provides: "Receiver-side ingress policy for RabbitMQ"
      contains: "component: rabbitmq"
  key_links:
    - from: "k8s/server/configmap.yml"
      to: "config-rs .with_prefix(AXIAM).separator(__)"
      via: "AXIAM__ double-underscore keys"
      pattern: "AXIAM__"
    - from: "docker/nginx.conf"
      to: "axiam-server:8090"
      via: "/oauth2 + /.well-known proxy_pass blocks"
      pattern: "location /oauth2"
---

<objective>
Harden the k8s and Docker infrastructure: fix the silently-ignored single-underscore ConfigMap keys to `AXIAM__`, add JWT/encryption-key secrets, enforce the PodSecurity restricted profile, add receiver-side NetworkPolicies for SurrealDB and RabbitMQ, add `/oauth2/*` and `/.well-known/*` proxy locations in nginx + ingress, and remove default credentials from the production compose file.

Purpose: Closes SEC-016 (proxy locations + backend port exposure), SEC-023 (prod compose default creds), SEC-052 (AXIAM__ env keys + RUST_LOG), SEC-053 (PSA enforce + receiver NetworkPolicies). Covers REQ-15 AC-4.
Output: corrected k8s manifests, two new NetworkPolicy files, nginx proxy blocks, env-var-driven prod compose.

Fully independent of the Rust crate plans (no shared files) — runs in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-medium-remediation/11-RESEARCH.md
@.planning/phases/11-medium-remediation/11-PATTERNS.md
@.planning/phases/11-medium-remediation/11-VALIDATION.md

<interfaces>
<!-- From 11-PATTERNS.md / 11-RESEARCH.md — use directly. -->

config-rs parse: main.rs:626 .with_prefix("AXIAM").separator("__")
  => env key AXIAM__DB__URL maps to config.db.url. Single underscore AXIAM_DB__URL is silently ignored.
  AXIAM__DB__URL value must be bare "surrealdb:8000" (no ws:// scheme — SurrealDB v3 WsClient).

Correct double-underscore reference: docker/docker-compose.prod.yml lines 30-37.
Fail-fast env pattern: docker-compose.prod.yml lines 41-42 -> "${VAR:?message}".

NetworkPolicy analog: k8s/network-policy/allow-ingress-to-server.yml
  podSelector matchLabels component: <target>; ingress from podSelector component: server; ports TCP.
  SurrealDB port 8000; RabbitMQ port 5672.

PSA enforce label (k8s/namespace.yml):
  pod-security.kubernetes.io/enforce: restricted
  pod-security.kubernetes.io/enforce-version: v1.29

nginx proxy analog: docker/nginx.conf lines 61-67 (location /api block) + security headers lines 22-26.
ingress: k8s/ingress.yml has /api and / only; gRPC 50051 already removed (SEC-003).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AXIAM__ ConfigMap/Secret keys + RUST_LOG + key secrets (SEC-052)</name>
  <files>k8s/server/configmap.yml, k8s/server/secret.yml</files>
  <read_first>
    - k8s/server/configmap.yml (~10-16 single-underscore AXIAM_DB__* keys; RUST_LOG)
    - k8s/server/secret.yml (~13-14 AXIAM_DB__USERNAME/PASSWORD)
    - docker/docker-compose.prod.yml (~30-37 correct AXIAM__ reference)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (k8s ConfigMap AXIAM__ section)
    - .planning/phases/11-medium-remediation/11-RESEARCH.md (SEC-052, Pitfall 1)
  </read_first>
  <action>
    Rename every ConfigMap key from single-underscore (`AXIAM_DB__URL`, `AXIAM_DB__NAMESPACE`, `AXIAM_SERVER__HOST`, etc.) to double-underscore (`AXIAM__DB__URL`, ...) so config-rs's `.with_prefix("AXIAM").separator("__")` parses them. Set `AXIAM__DB__URL` to the bare host:port `surrealdb:8000` (no `ws://` scheme — SurrealDB v3 WsClient rejects schemes). Change `RUST_LOG` from `"info,axiam=debug"` to `"info"` (no internal module exposure in production). In secret.yml, rename `AXIAM_DB__USERNAME`/`AXIAM_DB__PASSWORD` to `AXIAM__DB__USERNAME`/`AXIAM__DB__PASSWORD` and add secrets for the JWT private/public key PEMs and `AXIAM__AUTH__MFA_ENCRYPTION_KEY` + `AXIAM__PKI__ENCRYPTION_KEY` (placeholder values via secretKeyRef pattern — never literal production keys). NO code fences.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*#' k8s/server/configmap.yml | grep -c 'AXIAM__'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (11-VALIDATION.md row: REQ-15 AC-4 misconfig): all config/secret keys start with `AXIAM__` (double underscore); `grep -v '^#' | grep -c 'AXIAM__'` ≥ number of AXIAM keys and no `AXIAM_[A-Z]` single-underscore key remains
    - Source assertion: `RUST_LOG` is `"info"`; `AXIAM__DB__URL` has no `ws://` scheme; JWT + encryption-key secrets present in secret.yml
  </acceptance_criteria>
  <done>All k8s env keys use AXIAM__ double-underscore; RUST_LOG narrowed; key secrets added via secretKeyRef.</done>
</task>

<task type="auto">
  <name>Task 2: PSA enforce + receiver-side NetworkPolicies (SEC-053)</name>
  <files>k8s/namespace.yml, k8s/network-policy/allow-ingress-to-surrealdb.yml, k8s/network-policy/allow-ingress-to-rabbitmq.yml</files>
  <read_first>
    - k8s/namespace.yml (current PSA warn/audit labels)
    - k8s/network-policy/allow-ingress-to-server.yml (full file — ingress policy analog)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (k8s NetworkPolicy ingress structure, PSA enforce label)
    - .planning/phases/11-medium-remediation/11-RESEARCH.md (SEC-053)
  </read_first>
  <action>
    In namespace.yml add `pod-security.kubernetes.io/enforce: restricted` and `pod-security.kubernetes.io/enforce-version: v1.29` (keep warn/audit as-is). Create `k8s/network-policy/allow-ingress-to-surrealdb.yml` and `allow-ingress-to-rabbitmq.yml` following the allow-ingress-to-server.yml analog: podSelector matchLabels `component: surrealdb` (resp. `rabbitmq`), policyTypes Ingress, ingress from podSelector `component: server`, ports TCP 8000 (resp. 5672). This restricts SurrealDB/RabbitMQ ingress to only axiam-server pods (receiver-side default-deny complement). NO code fences — copy the analog structure.
  </action>
  <verify>
    <automated>grep -c 'pod-security.kubernetes.io/enforce: restricted' k8s/namespace.yml && grep -l 'component: surrealdb' k8s/network-policy/allow-ingress-to-surrealdb.yml && grep -l 'component: rabbitmq' k8s/network-policy/allow-ingress-to-rabbitmq.yml</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (11-VALIDATION.md row: REQ-15 AC-4 exposure): namespace.yml has `pod-security.kubernetes.io/enforce: restricted`; both new NetworkPolicy files exist with receiver-side ingress from `component: server` on the correct ports
  </acceptance_criteria>
  <done>PSA restricted enforced; SurrealDB and RabbitMQ ingress restricted to axiam-server.</done>
</task>

<task type="auto">
  <name>Task 3: nginx + ingress /oauth2 + /.well-known proxy; prod compose creds (SEC-016/023)</name>
  <files>docker/nginx.conf, k8s/ingress.yml, docker/docker-compose.prod.yml</files>
  <read_first>
    - docker/nginx.conf (~61-67 location /api block; ~22-26 security headers block)
    - k8s/ingress.yml (paths /api and / only; gRPC 50051 already removed)
    - docker/docker-compose.prod.yml (~30-37 AXIAM__ block; ~41-42 ${VAR:?} fail-fast; ~31-35 root/root DB creds; ~102 surrealdb command; ~121-122 RabbitMQ axiam/axiam)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (nginx location proxy_pass, Docker Compose env-var fail-fast Pattern Assignments)
    - .planning/phases/11-medium-remediation/11-RESEARCH.md (SEC-016, SEC-023)
  </read_first>
  <action>
    SEC-016: add `location /oauth2 { ... }` and `location /.well-known { ... }` proxy_pass blocks to nginx.conf pointing at `http://axiam-server:8090`, copying the proxy_set_header lines from the `/api` block and including the security-headers block (lines 22-26) in each new location to avoid header-inheritance gaps. Add `/oauth2` and `/.well-known` path entries to k8s/ingress.yml pointing to `axiam-server:8090`. Confirm backend ports stay ClusterIP-only (gRPC already removed); add a "local-testing only" comment on the `"8090:8090"` published port in prod compose if not present. SEC-023: replace the literal SurrealDB `root`/`root` creds (env vars at ~31-35, the `--user root --pass root` in the start command at ~102) and the RabbitMQ `axiam`/`axiam` creds (~121-122) with `${AXIAM__DB__USERNAME:?SurrealDB username required}` / `${AXIAM__DB__PASSWORD:?...}` / `${RABBITMQ_DEFAULT_USER:?...}` / `${RABBITMQ_DEFAULT_PASS:?...}` fail-fast references, matching the JWT-key pattern at lines 41-42. NO code fences.
  </action>
  <verify>
    <automated>grep -c 'location /oauth2' docker/nginx.conf && grep -c 'location /.well-known' docker/nginx.conf && ! grep -E 'pass[[:space:]]+root|DEFAULT_PASS:[[:space:]]*"?axiam"?' docker/docker-compose.prod.yml</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (11-VALIDATION.md row: REQ-15 AC-4 default creds): nginx.conf has `/oauth2` + `/.well-known` location blocks proxying to axiam-server:8090; ingress.yml has the matching paths
    - Source assertion: docker-compose.prod.yml has no literal `root`/`axiam` default creds — DB + RabbitMQ creds use `${VAR:?...}` fail-fast references
  </acceptance_criteria>
  <done>nginx + ingress proxy oauth2/.well-known; prod compose has no default creds; backend ports documented as dev-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → k8s config | Misconfigured env keys silently fall back to insecure defaults |
| pod → pod (cluster network) | Unrestricted lateral movement to data stores |
| internet → ingress | OAuth2/OIDC discovery endpoints must be reachable but controlled |
| operator → prod compose | Hardcoded default creds become production credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-CFG | Tampering / misconfig | k8s configmap/secret (SEC-052) | mitigate | AXIAM__ double-underscore keys + secretKeyRef; RUST_LOG narrowed (ASVS V14) |
| T-11-04-PSA | Elevation of Privilege | namespace PSA (SEC-053) | mitigate | enforce: restricted blocks non-conforming pods (ASVS V14) |
| T-11-04-NET | Elevation of Privilege / Info Disclosure | SurrealDB/RabbitMQ ingress (SEC-053) | mitigate | Receiver-side NetworkPolicies allow only axiam-server (ASVS V14) |
| T-11-04-PROXY | Info Disclosure / misrouting | nginx/ingress (SEC-016) | mitigate | Explicit /oauth2 + /.well-known proxy with security headers (ASVS V14) |
| T-11-04-CREDS | Spoofing | prod compose (SEC-023) | mitigate | Fail-fast env-var creds; no literal root/axiam defaults (ASVS V14) |
</threat_model>

<verification>
- ConfigMap/Secret keys all `AXIAM__`; RUST_LOG=info
- PSA enforce: restricted; two receiver NetworkPolicies exist
- nginx + ingress proxy /oauth2 + /.well-known
- prod compose has no literal default creds
- Grep gates use `grep -v '^#'` where header prose could self-invalidate.
</verification>

<success_criteria>
- All k8s/nginx/compose source assertions pass per 11-VALIDATION.md AC-4 rows.
</success_criteria>

<output>
Create `.planning/phases/11-medium-remediation/11-04-SUMMARY.md` when done.
</output>
