---
phase: 11-medium-remediation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/apiError.ts
  - frontend/src/lib/apiError.test.ts
  - frontend/src/components/Toaster.tsx
  - frontend/src/App.tsx
  - frontend/src/components/FormDialog.tsx
  - frontend/src/pages/resources/ResourcesPage.tsx
  - frontend/src/pages/federation/FederationPage.tsx
  - frontend/src/pages/users/UsersPage.tsx
  - frontend/src/pages/permissions/PermissionsPage.tsx
  - frontend/src/pages/certificates/CertificatesPage.tsx
  - frontend/src/pages/pgp/PgpKeysPage.tsx
  - frontend/src/router.tsx
  - frontend/src/components/ForbiddenPage.tsx
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/stores/auth.ts
  - frontend/src/hooks/useAuthInit.ts
  - frontend/src/lib/fetchCurrentUser.ts
  - frontend/src/components/shared.tsx
  - frontend/src/lib/utils.ts
  - frontend/src/hooks/useCrudMutations.ts
  - frontend/src/pages/roles/RoleDetailPage.tsx
  - frontend/src/pages/groups/GroupDetailPage.tsx
  - frontend/src/services/auth.ts
  - frontend/src/pages/DashboardPage.tsx
autonomous: true
requirements: [REQ-15]

must_haves:
  truths:
    - "getApiErrorMessage extracts a human-readable message from AxiosError/unknown and is unit-tested"
    - "A Toaster provider is wired into App.tsx and mutations across pages surface errors via toast"
    - "Forms validate input (no noValidate; email/url types + required) at the HTML5/zod layer"
    - "The resource parent picker excludes the resource itself and its descendants (no cycles)"
    - "Federation edit locks the type select (OIDC/SAML) when editing an existing config"
    - "List pages use placeholderData to avoid empty-state flash during pagination"
    - "Protected routes render a friendly ForbiddenPage when the user lacks the required permission"
    - "Login handles mfa_setup_required (navigate to setup) and mfa_required (verify) branches"
    - "tenantSlug/orgSlug are restored from /auth/me on reload"
    - "Dashboard query keys align with CRUD invalidations so counts refresh after mutations (CQ-F10)"
  artifacts:
    - path: "frontend/src/lib/apiError.ts"
      provides: "getApiErrorMessage(err: unknown): string"
      contains: "getApiErrorMessage"
    - path: "frontend/src/components/Toaster.tsx"
      provides: "Radix toast provider + useToast hook"
      contains: "ToastProvider"
    - path: "frontend/src/components/ForbiddenPage.tsx"
      provides: "Friendly 403 page"
      contains: "Forbidden"
  key_links:
    - from: "frontend/src/router.tsx"
      to: "ForbiddenPage"
      via: "ProtectedRoute permission check via usePermissions can()"
      pattern: "ProtectedRoute|can\\("
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "MFA setup flow"
      via: "mfa_setup_required branch navigate"
      pattern: "mfa_setup_required"
---

<objective>
Close the frontend medium UX/security items: a shared `getApiErrorMessage` helper + a wired Radix toast provider with `onError` toasts on all mutation pages, HTML5/zod form validation, a resource parent picker that excludes descendants, federation edit that locks the type select, pagination `placeholderData`, route guards rendering a friendly ForbiddenPage, login handling both MFA branches, and tenant/org slug restore from `/auth/me`.

Purpose: Closes CQ-F09 (toast + getApiErrorMessage), CQ-F11 (form validation), CQ-F12 (resource picker), CQ-F13 (federation edit type lock), CQ-F14 (pagination placeholderData), CQ-F30 (route guards + 403), CQ-F31 (login MFA setup branch), CQ-F29 (slug restore). Covers REQ-15 AC-5.
Output: new apiError.ts + apiError.test.ts + Toaster.tsx + ForbiddenPage.tsx; edits across mutation/list pages, router, login, auth store/hook.

Fully independent of the Rust crate plans (no shared files) — runs in Wave 1. No new UI design (frontend items are behavior remediation on existing components — --skip-ui).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-medium-remediation/11-RESEARCH.md
@.planning/phases/11-medium-remediation/11-PATTERNS.md
@.planning/phases/11-medium-remediation/11-VALIDATION.md

<interfaces>
<!-- From 11-PATTERNS.md / 11-RESEARCH.md — use directly. -->

@radix-ui/react-toast v1.2.15 already in package.json — provider NOT yet in component tree.
AxiosError unwrap analog: LoginPage.tsx:118-128.
useMutation onError analog: RolesPage.tsx:146-158 (onError -> toast destructive).
usePermissions can() (wildcard): hooks/usePermissions.ts:14-22.
AppLayout isAuthenticated guard: components/layout/AppLayout.tsx:8-22 (Navigate to /login).
LoginPage mfa_required branch: LoginPage.tsx:98-101; mfa_setup_required field exists in LoginResponse (line 29-33) but unhandled.
fetchCurrentUser: lib/fetchCurrentUser.ts:14-28 (returns AuthUser); auth store setTenantContext: stores/auth.ts:45.
FormDialog noValidate: components/FormDialog.tsx:99.
ResourcesPage parent picker: ResourcesPage.tsx:70,293.
FederationPage type select: FederationPage.tsx:320-338,520-540,778-803.
Frontend check: cd frontend && npx tsc -b --noEmit && npm run lint. Unit: npm test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getApiErrorMessage + Toaster provider + onError toasts (CQ-F09)</name>
  <files>frontend/src/lib/apiError.ts, frontend/src/lib/apiError.test.ts, frontend/src/components/Toaster.tsx, frontend/src/App.tsx, frontend/src/pages/users/UsersPage.tsx, frontend/src/pages/permissions/PermissionsPage.tsx, frontend/src/pages/certificates/CertificatesPage.tsx, frontend/src/pages/pgp/PgpKeysPage.tsx, frontend/src/pages/resources/ResourcesPage.tsx, frontend/src/pages/federation/FederationPage.tsx, frontend/src/pages/DashboardPage.tsx</files>
  <read_first>
    - frontend/src/pages/DashboardPage.tsx (~182-199 query keys that must align with CRUD invalidations — CQ-F10)
    - frontend/src/pages/LoginPage.tsx (~118-128 AxiosError unwrapping analog)
    - frontend/src/pages/roles/RolesPage.tsx (~146-158 useMutation onError analog)
    - frontend/package.json (confirm @radix-ui/react-toast v1.2.15)
    - frontend/src/App.tsx (root component — provider insertion point)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (apiError.ts, Toaster.tsx, Frontend useMutation+onError; Pitfall 8)
  </read_first>
  <behavior>
    - getApiErrorMessage(AxiosError with response.data.error) returns that error string
    - getApiErrorMessage(AxiosError with response.data.message) returns the message
    - getApiErrorMessage(plain Error) returns error.message
    - getApiErrorMessage(unknown/null) returns a generic fallback string (non-empty)
  </behavior>
  <action>
    Create `frontend/src/lib/apiError.ts` exporting `getApiErrorMessage(err: unknown): string` distilled from the LoginPage AxiosError unwrapping (response.data.error -> response.data.message -> error.message -> generic fallback) — never returns empty. Create `frontend/src/components/Toaster.tsx` using `@radix-ui/react-toast` standard pattern: a `ToastProvider` + viewport `<Toaster />` component and a `useToast()` hook exposing `toast({ description, variant })`. Add `<Toaster />` to App.tsx root so the provider is in the tree (Pitfall 8). On the mutation pages listed (UsersPage, PermissionsPage, CertificatesPage, PgpKeysPage, ResourcesPage, FederationPage — those currently missing onError), add `onError: (err) => toast({ description: getApiErrorMessage(err), variant: "destructive" })` to each create/update/delete/revoke/unlock mutation, following the RolesPage analog. CQ-F10: align the DashboardPage query keys (~182-199) with the query keys used by the CRUD pages' invalidations so dashboard counts refresh after create/update/delete. Create `frontend/src/lib/apiError.test.ts` (vitest) covering the behaviors above. NO code fences — follow analogs.
  </action>
  <verify>
    <automated>cd frontend && npm test -- apiError</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- apiError` passes (11-VALIDATION.md row: REQ-15 AC-5 UX/error leak, Wave 0 apiError.test.ts)
    - Source assertion: Toaster provider present in App.tsx; the six listed pages call `toast(... getApiErrorMessage(err) ...)` in mutation onError
    - Source assertion: DashboardPage query keys match the CRUD invalidation keys (CQ-F10)
    - `cd frontend && npx tsc -b --noEmit` clean
  </acceptance_criteria>
  <done>getApiErrorMessage unit-tested; Toaster wired; mutation errors surface as toasts across pages; dashboard counts refresh after mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Form validation, resource picker, federation edit lock, pagination + shared components/hooks/types + wire unassign (CQ-F11/F12/F13/F14/F15/F16/F17/F18)</name>
  <files>frontend/src/components/FormDialog.tsx, frontend/src/pages/resources/ResourcesPage.tsx, frontend/src/pages/federation/FederationPage.tsx, frontend/src/pages/users/UsersPage.tsx, frontend/src/components/shared.tsx, frontend/src/lib/utils.ts, frontend/src/hooks/useCrudMutations.ts, frontend/src/pages/roles/RoleDetailPage.tsx, frontend/src/pages/groups/GroupDetailPage.tsx, frontend/src/services/auth.ts</files>
  <read_first>
    - frontend/src/components/FormDialog.tsx (~99 `noValidate`)
    - frontend/src/pages/resources/ResourcesPage.tsx (~70,293 parent picker; get_children/list_ancestors API)
    - frontend/src/pages/federation/FederationPage.tsx (~320-338,520-540,778-803 type select)
    - frontend/src/pages/users/UsersPage.tsx (useQuery for the list)
    - frontend/src/services/roles.ts (~66,78 unassignRoleFromUser/unassignRoleFromGroup — exist but unwired — CQ-F18)
    - frontend/src/pages/roles/RoleDetailPage.tsx + frontend/src/pages/groups/GroupDetailPage.tsx (unassign UI insertion points)
    - frontend/src/stores/auth.ts (useAuthStore selector usage — CQ-F16); frontend/src/services/auth.ts (MfaMethod type — CQ-F17)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (FormDialog, ResourcesPage, FederationPage, UsersPage Pattern Assignments; CQ-F15/F16/F17/F18 RESEARCH Cluster 5)
  </read_first>
  <action>
    CQ-F11: remove `noValidate` from FormDialog.tsx:99 and add HTML5 validation attributes (`type="email"` on email fields, `required` on required fields, `pattern`/`type="url"` for URL fields) where the form schema applies. CQ-F12: in ResourcesPage parent picker, when editing a resource exclude the resource itself and all its descendants from the selectable parents (use the existing children/ancestors API to compute the descendant set) so a cycle cannot be created. CQ-F13: in FederationPage, disable the OIDC/SAML type select when `isEditMode === true` and ensure the submitted config payload matches the selected type's block. CQ-F14: add `placeholderData: (prev) => prev` to the UsersPage list useQuery to prevent empty-state flash during page navigation, and redirect to `page - 1` after deleting the last item on a page. CQ-F15: extract the duplicated `ToggleField`/`SectionCard`/`InfoRow`/`ActionBadge` into `frontend/src/components/shared.tsx`, `slugify` into `frontend/src/lib/utils.ts`, and the create/update/delete mutation+toast pattern into a `useCrudMutations` hook (`frontend/src/hooks/useCrudMutations.ts`); update at least the pages in this plan's scope to import them (broader sweep is fine). CQ-F16: replace whole-store `useAuthStore((s) => s)` subscriptions with field selectors (`(s) => s.user`, `(s) => s.isAuthenticated`). CQ-F17: consolidate the duplicated MfaMethod definitions to the single type in `frontend/src/services/auth.ts`. CQ-F18: wire `unassignRoleFromUser`/`unassignRoleFromGroup` (services/roles.ts:66,78) to the UI in RoleDetailPage/GroupDetailPage with a confirm + onError toast. NO code fences — follow analogs. These are source-assertion + manual-smoke verified per 11-VALIDATION.md.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc -b --noEmit && npm run lint` clean
    - Source assertion (11-VALIDATION.md row: REQ-15 AC-5 data integrity): FormDialog has no `noValidate`; ResourcesPage parent picker filters descendants; FederationPage type select is disabled in edit mode; UsersPage useQuery has `placeholderData`
    - Source assertion: shared.tsx/utils.ts/useCrudMutations.ts exist and are imported; auth store uses field selectors; single MfaMethod type in services/auth.ts; RoleDetailPage/GroupDetailPage call the unassign methods
  </acceptance_criteria>
  <done>Forms validate; resource picker excludes descendants; federation edit locks type; list pagination uses placeholderData; shared components/hooks/types extracted; unassign wired to the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Route guards + ForbiddenPage, login MFA branches, tenant/org slug restore (CQ-F30/F31/F29)</name>
  <files>frontend/src/router.tsx, frontend/src/components/ForbiddenPage.tsx, frontend/src/pages/LoginPage.tsx, frontend/src/stores/auth.ts, frontend/src/hooks/useAuthInit.ts, frontend/src/lib/fetchCurrentUser.ts</files>
  <read_first>
    - frontend/src/router.tsx (route definitions)
    - frontend/src/components/layout/AppLayout.tsx (~8-22 isAuthenticated guard analog)
    - frontend/src/hooks/usePermissions.ts (~14-22 can() wildcard)
    - frontend/src/pages/LoginPage.tsx (~29-33 LoginResponse mfa_setup_required field; ~98-101 mfa_required branch)
    - frontend/src/stores/auth.ts (~45 setTenantContext); frontend/src/hooks/useAuthInit.ts; frontend/src/lib/fetchCurrentUser.ts (~14-28)
    - .planning/phases/11-medium-remediation/11-PATTERNS.md (ProtectedRoute guard, LoginPage MFA branch, fetchCurrentUser+setTenantContext)
  </read_first>
  <action>
    CQ-F30: create `frontend/src/components/ForbiddenPage.tsx` (friendly 403 message, link back to dashboard) and a `ProtectedRoute` wrapper in router.tsx that checks `isAuthenticated` (Navigate to /login) then `usePermissions().can(requiredPermission)` — render `<ForbiddenPage />` instead of a blank/redirect-loop when the permission is missing; wrap protected routes with their required permission, extending the Sidebar `can` pattern to route level. CQ-F31: in LoginPage add an `mfa_setup_required` branch after the existing `mfa_required` branch (line 101) that navigates to the MFA setup flow (e.g. `/profile/mfa`) carrying the `setup_token` in navigation state. CQ-F29: extend fetchCurrentUser to return `tenantSlug`/`orgSlug` from the `/auth/me` response and call the auth store `setTenantContext` from useAuthInit after `/auth/me` resolves so slugs survive a hard reload. NO code fences — follow analogs. Route-guard 403, MFA branching, and slug restore are manual-smoke verified per 11-VALIDATION.md (require live session/backend).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc -b --noEmit && npm run lint` clean
    - Source assertion (11-VALIDATION.md rows: REQ-15 AC-5 authz UX): router.tsx ProtectedRoute checks `can(...)` and renders ForbiddenPage; LoginPage handles `mfa_setup_required`; fetchCurrentUser returns slugs and useAuthInit calls setTenantContext
    - Manual smoke (11-VALIDATION.md Manual-Only): `just dev-up`; low-priv user navigating to a gated route sees ForbiddenPage; login MFA branches route correctly; hard-reload restores tenant slug
  </acceptance_criteria>
  <done>Route guards render friendly 403; login handles both MFA branches; tenant/org slug restored on reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → React UI | Error payloads must be surfaced safely (no raw leak, no silent failure) |
| user → protected route | Client-side authz UX must not expose gated views (defense-in-depth atop backend RBAC) |
| login response → client navigation | MFA state must route the user correctly |
| /auth/me → SPA hydration | Tenant context must be restored, not fabricated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-ERR | Information Disclosure | mutation error handling (CQ-F09) | mitigate | getApiErrorMessage surfaces a safe message; no raw stack/email leak (ASVS V5) |
| T-11-05-VAL | Tampering | FormDialog (CQ-F11) | mitigate | HTML5/zod validation at the form boundary (ASVS V5) |
| T-11-05-CYC | Tampering / data integrity | resource hierarchy (CQ-F12) | mitigate | Parent picker excludes descendants — no cycle creation (ASVS V5) |
| T-11-05-AUTHZ | Elevation of Privilege (UX) | route guards (CQ-F30) | mitigate | ProtectedRoute can() check + ForbiddenPage; backend RBAC remains source of truth (ASVS V4) |
| T-11-05-MFA | Spoofing | login MFA branches (CQ-F31) | mitigate | mfa_setup_required routed to setup; no bypass of MFA enrollment (ASVS V2) |
| T-11-05-CTX | Tampering | tenant/org slug (CQ-F29) | mitigate | Slugs restored from /auth/me, not fabricated client-side (ASVS V4) |
| T-11-SC | Tampering | npm install | accept | No new deps; @radix-ui/react-toast already vendored (RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- `cd frontend && npm test -- apiError` green
- `cd frontend && npx tsc -b --noEmit && npm run lint` clean
- Manual smoke (live backend): route-guard 403, MFA branching, slug restore (per 11-VALIDATION.md Manual-Only table)
</verification>

<success_criteria>
- getApiErrorMessage tested; toasts wired; forms validate; resource picker safe; federation edit locks type; pagination stable; route guards + 403; login MFA branches; slug restore — all per 11-VALIDATION.md AC-5 rows.
</success_criteria>

<output>
Create `.planning/phases/11-medium-remediation/11-05-SUMMARY.md` when done.
</output>
