---
phase: 12-low-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-api-rest/src/extractors/client_info.rs
  - crates/axiam-api-rest/src/extractors/mod.rs
  - crates/axiam-api-rest/src/handlers/auth.rs
  - crates/axiam-api-rest/src/handlers/webauthn.rs
  - crates/axiam-api-rest/src/handlers/users.rs
  - crates/axiam-api-rest/src/handlers/gdpr.rs
  - crates/axiam-server/src/cleanup.rs
  - crates/axiam-server/src/main.rs
  - crates/axiam-oauth2/src/token.rs
  - crates/axiam-auth/src/service.rs
  - crates/axiam-auth/src/error.rs
  - crates/axiam-audit/src/middleware.rs
  - crates/axiam-db/src/seeder.rs
  - crates/axiam-db/src/schema.rs
  - crates/axiam-db/src/repository/role.rs
  - crates/axiam-db/tests/seeder_skip_test.rs
  - Cargo.toml
  - crates/axiam-auth/Cargo.toml
  - crates/axiam-db/Cargo.toml
  - crates/axiam-authz/Cargo.toml
autonomous: true
requirements: [REQ-16]

must_haves:
  truths:
    - "A single shared client_ip/user_agent extractor is used by auth, webauthn, and users handlers (no duplicate private definitions)"
    - "Targeted cleanup/gdpr/oauth2 error sites log on failure instead of silently dropping with let _ ="
    - "Sync change_password runs the HIBP breach check via a passed reqwest::Client"
    - "Audit-channel drops emit a structured, alertable error event (audit_dropped=true)"
    - "Re-running the permission seeder with an unchanged registry skips the UPSERT storm"
  artifacts:
    - path: "crates/axiam-api-rest/src/extractors/client_info.rs"
      provides: "Shared capped client_ip(req) + user_agent(req) functions"
      contains: "pub fn client_ip"
    - path: "crates/axiam-db/tests/seeder_skip_test.rs"
      provides: "Test proving seeder skips UPSERT when registry hash unchanged"
      contains: "seeder"
    - path: "crates/axiam-db/src/schema.rs"
      provides: "seeder_state table definition"
      contains: "seeder_state"
  key_links:
    - from: "crates/axiam-api-rest/src/handlers/auth.rs"
      to: "crates/axiam-api-rest/src/extractors/client_info.rs"
      via: "use crate::extractors::client_info::{client_ip, user_agent}"
      pattern: "extractors::client_info"
    - from: "crates/axiam-auth/src/service.rs"
      to: "crates/axiam-auth/src/policy.rs"
      via: "http_client passed to evaluate_password for HIBP"
      pattern: "http_client"
---

<objective>
Close the backend cleanup / dead-code / shared-helper LOW findings: CQ-B28 (shared client_ip/user_agent), CQ-B29 (NotificationDispatcher wired or removed), CQ-B31 (logged error handling), CQ-B33 (typed crypto errors), CQ-B34 (dep pruning + rand consolidation), CQ-B35 (HIBP on sync change-password), CQ-B36 (audit-drop metric), CQ-B42 (seeder version/hash skip).

Purpose: Eliminate copy-pasted helpers, silent error drops, and a per-boot UPSERT storm; restore a missing HIBP breach check on the sync password-change path.
Output: One shared extractor module, logged errors at five sites, HIBP-wired change_password, structured audit-drop event, hash-guarded seeder + new seeder_state table + skip test.

NOTE — CQ-B27/CQ-B43 (AppState / per-request service rebuild refactor) is DEFERRED to Phase 19 per research (significant scope: refactor main.rs + every handler signature; not appropriate for a LOW finding). Do NOT refactor AppState in this plan. Add a one-line `// TODO(T19): AppState refactor — CQ-B27/CQ-B43 deferred` note near the federation service construction in handlers/federation.rs if not already present, and mention the deferral in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-low-remediation/12-RESEARCH.md
@.planning/phases/12-low-remediation/12-PATTERNS.md
@/home/emanuele/git/priv/axiam/CLAUDE.md

BUILD-SCOPE (project memory — bake into every verify):
- Disk near-full: NO whole-workspace `cargo test`/`just test`/`cargo build --workspace` (ENOSPC on link). Use `cargo check -p <crate> --tests --no-default-features` and targeted `cargo test -p <crate> --test <name>`.
- Build per-crate with `-p`; on Arch use `--no-default-features`.
- Verify cargo via output text, not exit code (rtk masks exit codes); read actual output.
- The 3 SAML federation_test failures are a KNOWN baseline — do not "fix" them.

<interfaces>
From crates/axiam-api-rest/src/handlers/webauthn.rs:90-106 (canonical CAPPED version to lift into the shared module):
  const MAX_IP_LEN: usize = 45;
  const MAX_UA_LEN: usize = 512;
  fn client_ip(req: &HttpRequest) -> Option<String>   // req.connection_info().realip_remote_addr(), .chars().take(MAX_IP_LEN)
  fn user_agent(req: &HttpRequest) -> Option<String>  // headers "user-agent", .chars().take(MAX_UA_LEN)

From crates/axiam-auth/src/service.rs:660 (change_password signature to extend with `http_client: Option<&reqwest::Client>`):
  pub async fn change_password<H: PasswordHistoryRepository>(&self, tenant_id, user_id, current_session_id, current_password, new_password, policy, history_repo) -> AxiamResult<()>
  // line ~715 currently passes `None` to evaluate_password's HIBP slot

From crates/axiam-server/src/main.rs:388 — `let http_client = reqwest::Client::builder()...` ALREADY EXISTS; register it via `.app_data(web::Data::new(http_client.clone()))` so the change-password handler can extract `web::Data<reqwest::Client>`.

From crates/axiam-audit/src/middleware.rs:161-162 — `if tx.try_send(entry).is_err() { warn!(...) }` to escalate.
From crates/axiam-db/src/seeder.rs:37-66 — UPSERT loop to hash-guard; sha2 digest pattern at crates/axiam-server/src/cleanup.rs:260-264.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared client_ip/user_agent extractor + replace 3 duplicate defs</name>
  <files>crates/axiam-api-rest/src/extractors/client_info.rs, crates/axiam-api-rest/src/extractors/mod.rs, crates/axiam-api-rest/src/handlers/auth.rs, crates/axiam-api-rest/src/handlers/webauthn.rs, crates/axiam-api-rest/src/handlers/users.rs</files>
  <read_first>
    - crates/axiam-api-rest/src/handlers/webauthn.rs:90-106 (canonical capped helpers — PATTERNS analog, MAX_IP_LEN=45, MAX_UA_LEN=512)
    - crates/axiam-api-rest/src/handlers/auth.rs:165-180 (uncapped duplicate to delete)
    - crates/axiam-api-rest/src/handlers/users.rs:172 (inline user-agent read to replace)
    - crates/axiam-api-rest/src/extractors/mod.rs (module registration pattern)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (client_info section)
  </read_first>
  <behavior>
    - client_ip(req) returns the realip_remote_addr string capped to MAX_IP_LEN (45) chars
    - user_agent(req) returns the user-agent header value capped to MAX_UA_LEN (512) chars
    - auth.rs, webauthn.rs, users.rs all reference the shared functions; zero local/private definitions of client_ip or user_agent remain in those three handler files
  </behavior>
  <action>
    Create crates/axiam-api-rest/src/extractors/client_info.rs with `pub fn client_ip(req: &HttpRequest) -> Option<String>` and `pub fn user_agent(req: &HttpRequest) -> Option<String>`, lifting the CAPPED webauthn.rs implementation (constants MAX_IP_LEN=45, MAX_UA_LEN=512 as module-level `const`). Use ConnectionInfo realip_remote_addr for the IP (preferred over peer_addr). Register with `pub mod client_info;` in extractors/mod.rs. Delete the private `client_ip`/`user_agent` fns at auth.rs:165-180 and the MAX_IP_LEN/MAX_UA_LEN constants + fns at webauthn.rs:90-106; replace the inline header read at users.rs:172. Add `use crate::extractors::client_info::{client_ip, user_agent};` (users.rs needs only user_agent) to each file. Do NOT change call-site argument shapes.
  </action>
  <verify>
    <automated>cargo check -p axiam-api-rest --tests --no-default-features 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p axiam-api-rest --tests --no-default-features` exits 0 (read output text, not exit code)
    - `extractors/client_info.rs contains pub fn client_ip` AND `pub fn user_agent`
    - `grep -nE 'fn client_ip|fn user_agent' crates/axiam-api-rest/src/handlers/auth.rs crates/axiam-api-rest/src/handlers/webauthn.rs` returns ZERO local fn definitions (only `use` imports may reference the names)
    - MAX_IP_LEN and MAX_UA_LEN constants exist only in client_info.rs
  </acceptance_criteria>
  <done>Single shared, capped extractor; all three handlers import it; api-rest compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Logged error handling, NotificationDispatcher, typed crypto errors, audit-drop metric, dep pruning</name>
  <files>crates/axiam-server/src/cleanup.rs, crates/axiam-api-rest/src/handlers/gdpr.rs, crates/axiam-oauth2/src/token.rs, crates/axiam-audit/src/middleware.rs, crates/axiam-server/src/main.rs, crates/axiam-auth/src/error.rs, Cargo.toml, crates/axiam-auth/Cargo.toml, crates/axiam-db/Cargo.toml, crates/axiam-authz/Cargo.toml</files>
  <read_first>
    - crates/axiam-server/src/cleanup.rs:247,268-272,294,314 (the already-correct `if let Err` pattern at 268-272 is the analog; lines 247/294/314 still use `let _ =`)
    - crates/axiam-api-rest/src/handlers/gdpr.rs:124 (`let _ =` audit write)
    - crates/axiam-oauth2/src/token.rs:555 (`let _ =` revoke)
    - crates/axiam-audit/src/middleware.rs:161-162 (warn!-only drop)
    - crates/axiam-server/src/main.rs:611 (NotificationPublisher app_data registration)
    - crates/axiam-audit/src/notification.rs (NotificationDispatcher::dispatch signature — decides wire-vs-remove)
    - crates/axiam-auth/src/error.rs:104 (Crypto(String) variant)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (cleanup.rs / middleware.rs / Shared Patterns sections)
  </read_first>
  <action>
    CQ-B31: Replace `let _ =` at cleanup.rs:247 (federation_link delete → `tracing::warn!`), cleanup.rs:294 (account_deletion mark_completed → `tracing::warn!`), cleanup.rs:314 (gdpr pseudonymize audit append → `tracing::error!`, legally significant), gdpr.rs:124 (audit write → `tracing::error!`), token.rs:555 (revoke → `tracing::warn!`) with `if let Err(e) = ... { tracing::warn!/error!(error = %e, ...context...) }` per the analog at cleanup.rs:268-272. Do NOT touch AMQP `delivery.acker.*` ack patterns (correct fire-and-forget).
    CQ-B29 NotificationDispatcher: Inspect notification.rs `dispatch()`. If it only enqueues via the already-constructed NotificationPublisher (no new reqwest/HTTP dependency needed in the worker), WIRE it: call dispatch from the audit worker loop after the audit write. If wiring requires components not in worker scope (reqwest::Client etc.), DEFAULT TO REMOVE: delete the unused `NotificationPublisher` app_data registration at main.rs:611 and document the removal. Pick exactly one; state which in the SUMMARY.
    CQ-B36: Escalate middleware.rs:161-162 from `warn!` to `tracing::error!(audit_dropped = true, method = %method, path = %path, "Audit channel full — entry dropped...")`.
    CQ-B33: Split `AuthError::Crypto(String)` in error.rs into typed sub-variants (e.g. KeyParse, AesDecrypt, HmacInvalid) where call sites construct them; keep mapping to existing HTTP status. Low effort — only the most common construction sites.
    CQ-B34: Run `cargo machete` (install via `cargo install cargo-machete` if absent). For each flagged dep (expected: webauthn-rs-proto in axiam-auth, test-only tokio in axiam-db, rand in axiam-db/axiam-authz), remove it then immediately `cargo check -p <crate> --no-default-features`; if it fails, revert that single removal and add comment `# used via <dep> re-export`. Do NOT remove `rand_core = "0.6"` pins in axiam-pki/axiam-server (intentional pgp/rsa compat — add comment `# Required: pgp/rsa uses rand_core 0.6 CryptoRng; do not upgrade until upstream`).
  </action>
  <verify>
    <automated>cargo check -p axiam-server -p axiam-api-rest -p axiam-oauth2 -p axiam-audit -p axiam-auth --no-default-features 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'let _ = ' crates/axiam-server/src/cleanup.rs` shows none of lines 247/294/314 still use `let _ =` for the federation_link/account_deletion/audit-append calls
    - `grep -n 'let _ =' crates/axiam-api-rest/src/handlers/gdpr.rs` does not match the audit-write at the former line 124
    - `grep -n 'audit_dropped = true' crates/axiam-audit/src/middleware.rs` matches; `grep -n 'warn!("Audit channel full' crates/axiam-audit/src/middleware.rs` returns nothing
    - NotificationDispatcher: EITHER `grep -rn 'dispatch' crates/axiam-audit/src crates/axiam-server/src | grep -v test` shows a production call site, OR the `NotificationPublisher` app_data registration is removed from main.rs (SUMMARY states which)
    - rand_core 0.6 pins in axiam-pki/axiam-server Cargo.toml retain the explanatory comment
    - `cargo check` for all five crates (above) exits 0 (read output text)
  </acceptance_criteria>
  <done>No silent error drops at the five targeted sites; dispatcher resolved; audit drops alertable; deps pruned without breaking compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HIBP on sync change-password + seeder hash-skip + seeder_state table</name>
  <files>crates/axiam-auth/src/service.rs, crates/axiam-api-rest/src/handlers/auth.rs, crates/axiam-server/src/main.rs, crates/axiam-db/src/seeder.rs, crates/axiam-db/src/schema.rs, crates/axiam-db/src/repository/role.rs, crates/axiam-db/tests/seeder_skip_test.rs</files>
  <read_first>
    - crates/axiam-auth/src/service.rs:660-715 (change_password signature + the `None` HIBP slot at ~715)
    - crates/axiam-auth/src/policy.rs:173-220 (check_hibp already accepts Option<&reqwest::Client>)
    - crates/axiam-server/src/main.rs:388 (existing `let http_client = reqwest::Client::builder()...`)
    - crates/axiam-db/src/seeder.rs:37-66 (UPSERT loop + seed_default_roles list+scan)
    - crates/axiam-server/src/cleanup.rs:260-264 (sha2 Digest/Sha256 digest pattern analog)
    - crates/axiam-db/src/schema.rs (DEFINE TABLE IF NOT EXISTS pattern)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (service.rs / seeder.rs sections; SurrealDB v3 quirks)
    - MEMORY.md (SurrealDB v3: type::record('table', $id); bind owned String; .check() ownership; UPSERT idempotent)
  </read_first>
  <behavior>
    - change_password with `Some(&reqwest::Client)` runs the HIBP breach check; a known-breached new password is rejected
    - On second seeder run with an identical permission registry, the UPSERT loop is skipped (early return after hash match)
    - seeder_state stores the registry sha256 hash per tenant; a changed registry forces re-seed
  </behavior>
  <action>
    CQ-B35: Add parameter `http_client: Option<&reqwest::Client>` to `change_password` in service.rs and pass it through to the `evaluate_password`/`check_hibp` call (replacing the `None` at ~line 715). In handlers/auth.rs change-password handler, extract `web::Data<reqwest::Client>` and pass `Some(client.as_ref())`. In main.rs add `.app_data(web::Data::new(http_client.clone()))` for the existing http_client at line 388 (clone is cheap — reqwest::Client is an Arc).
    CQ-B42: In seeder.rs, before the UPSERT loop in the permission-seeding fn, compute `registry_hash` = sha256 over concatenated `format!("{action}|{desc}")` entries (sha2::{Digest, Sha256}, hex::encode). Derive a stable per-tenant record id (`Uuid::new_v5(&tenant_id, b"seeder_state").to_string()`), SELECT the existing seeder_state hash; if equal, early-return Ok(()). After a successful seed, `UPSERT type::record('seeder_state', $id) SET tenant_id=$tenant_id, hash=$hash, updated_at=time::now()` with owned String binds and `.check()`. Add `SeederStateRow { hash: String }` deriving SurrealValue. Add the `seeder_state` table + fields to schema.rs (DEFINE TABLE/FIELD IF NOT EXISTS). Replace seed_default_roles `list()`+linear-scan with a `get_by_name(tenant_id, name)` method on SurrealRoleRepository (model on existing get_by_id but query the `name` field).
    Create crates/axiam-db/tests/seeder_skip_test.rs: in-memory SurrealDB (kv-mem, no auth — use_ns/use_db) per existing axiam-db test pattern; seed once, capture state, seed again with same registry, assert the second call short-circuits (e.g. assert seeder_state hash unchanged and no duplicate permission rows created / UPSERT count guard).
  </action>
  <verify>
    <automated>cargo test -p axiam-db --test seeder_skip_test --no-default-features 2>&1 | tail -8; cargo check -p axiam-auth -p axiam-api-rest -p axiam-server --no-default-features 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p axiam-db --test seeder_skip_test --no-default-features` passes (read output: `test result: ok`)
    - `grep -n 'http_client' crates/axiam-auth/src/service.rs` shows change_password accepts and forwards the client (no remaining `None, // no HIBP client` comment)
    - `grep -n 'seeder_state' crates/axiam-db/src/schema.rs` matches; `SeederStateRow` exists in seeder.rs
    - `grep -n 'get_by_name' crates/axiam-db/src/repository/role.rs` matches
    - `cargo check -p axiam-auth -p axiam-api-rest -p axiam-server --no-default-features` exits 0 (read output text)
  </acceptance_criteria>
  <done>HIBP runs on the sync change-password path; restart with unchanged registry skips the UPSERT storm; behavior proven by a passing test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST API | Untrusted client IP / User-Agent enter via headers; capped before logging/storage |
| user → change-password | New password is untrusted; must be screened against the HIBP breach corpus |
| audit pipeline | Audit drop must be observable so security events are never silently lost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | cleanup.rs/gdpr.rs silent `let _ =` on legally-significant GDPR audit writes | mitigate | Task 2: `if let Err(e) = ... { tracing::error!(...) }` so failed GDPR audit emits are recorded |
| T-12-02 | Elevation of Privilege | service.rs change_password skips HIBP (breached password accepted) | mitigate | Task 3: pass reqwest::Client so HIBP breach check runs on the sync path |
| T-12-03 | Repudiation | audit channel drop only `warn!` — unalertable loss of audit trail | mitigate | Task 2: `tracing::error!(audit_dropped = true, ...)` structured field for Loki/Prometheus alerting |
| T-12-04 | Tampering | new `seeder_state` table / hash guard could let a stale hash skip a legitimately changed registry | accept | Hash is sha256 over the exact registry slice; any registry change alters the hash and forces re-seed; Task-3 test proves skip-only-when-unchanged |
| T-12-SC | Tampering | npm/pip/cargo installs (`cargo install cargo-machete`) | mitigate | cargo-machete is a dev-only CLI tool run locally for dep analysis; introduces no runtime/workspace dependency. No new runtime package added (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cargo check -p axiam-api-rest -p axiam-server -p axiam-oauth2 -p axiam-audit -p axiam-auth --tests --no-default-features` exits 0
- `cargo test -p axiam-db --test seeder_skip_test --no-default-features` passes
- No `let _ =` remains at the five targeted error sites
- Shared client_info extractor is the sole definition of client_ip/user_agent
</verification>

<success_criteria>
ROADMAP SC #1 (backend cleanup) satisfied: shared client_ip/user_agent helper, NotificationDispatcher wired-or-removed, logged errors, typed errors, dep pruning + rand consolidation, HIBP on sync change-password, audit-drop metric, seeder version/hash skip — all green per the acceptance criteria above. CQ-B27/CQ-B43 AppState refactor explicitly deferred to Phase 19.
</success_criteria>

<output>
Create `.planning/phases/12-low-remediation/12-01-SUMMARY.md` when done. State the NotificationDispatcher decision (wired vs removed) and confirm the AppState (CQ-B27/B43) Phase-19 deferral.
</output>
