---
phase: 12-low-remediation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/certificates/CertificatesPage.tsx
  - frontend/src/pages/oauth2/OAuth2ClientsPage.tsx
  - frontend/src/pages/service-accounts/ServiceAccountsPage.tsx
  - frontend/src/pages/webhooks/WebhooksPage.tsx
  - frontend/src/pages/pgp/PgpKeysPage.tsx
  - frontend/src/pages/auth/ResetPasswordPage.tsx
  - frontend/src/pages/auth/VerifyEmailPage.tsx
  - frontend/src/pages/auth/ForgotPasswordPage.tsx
autonomous: true
requirements: [REQ-16]

must_haves:
  truths:
    - "All five secret-reveal modals clear the revealed secret from React state when closed"
    - "Reset and verify tokens are stripped from the URL via history.replaceState after use"
    - "ForgotPasswordPage no longer logs the Axios error object (which carries the submitted email)"
  artifacts:
    - path: "frontend/src/pages/auth/ResetPasswordPage.tsx"
      provides: "history.replaceState token strip after confirm/error"
      contains: "replaceState"
    - path: "frontend/src/pages/auth/ForgotPasswordPage.tsx"
      provides: "Redacted failure log (no err arg)"
      contains: "redacted"
  key_links:
    - from: "frontend/src/pages/webhooks/WebhooksPage.tsx onClose"
      to: "revealed-secret state"
      via: "setRevealedSecret(null) on modal close"
      pattern: "setRevealed"
---

<objective>
Close the frontend security LOW findings: SEC-036 (secrets cleared from React state on modal close, 5 pages), SEC-037 (reset/verify tokens stripped from URL via history.replaceState), SEC-041 (no full Axios error/email logging on ForgotPasswordPage).

Purpose: Prevent revealed secrets (cert/key/client secrets, PGP keys) from lingering in JS memory after the reveal modal closes; keep password-reset and email-verify tokens out of browser history; stop leaking the submitted email into the browser console via a logged AxiosError.
Output: onClose secret-clear on 5 pages, replaceState token strip on 2 auth pages, redacted log on ForgotPasswordPage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-low-remediation/12-RESEARCH.md
@.planning/phases/12-low-remediation/12-PATTERNS.md
@/home/emanuele/git/priv/axiam/CLAUDE.md

BUILD-SCOPE (frontend): after each fix `cd frontend && npx tsc -b --noEmit`; whole-plan gate `cd frontend && npm run lint && npx tsc -b`.

<interfaces>
Secret-modal analog (frontend/src/pages/webhooks/WebhooksPage.tsx:287-307): a reveal modal with `setSecretOpen(false)` onClose that does NOT clear the revealed value. Fix is to also call the reveal-state setter with `null`.
Per-page state setter names (from RESEARCH SEC-036):
  - WebhooksPage: setRevealedSecret
  - CertificatesPage (~146,157): setRevealedKey
  - OAuth2ClientsPage (~283,297): setRevealedSecret
  - ServiceAccountsPage (~202-206): setRevealedSecret (+ optional title/desc reset)
  - PgpKeysPage (~287,298): setRevealedKey

Token-strip call (SEC-037): `window.history.replaceState({}, document.title, window.location.pathname);` AFTER the async verify/confirm completes (success AND catch branches), never before.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-036 — clear revealed secret on modal close (5 pages)</name>
  <files>frontend/src/pages/certificates/CertificatesPage.tsx, frontend/src/pages/oauth2/OAuth2ClientsPage.tsx, frontend/src/pages/service-accounts/ServiceAccountsPage.tsx, frontend/src/pages/webhooks/WebhooksPage.tsx, frontend/src/pages/pgp/PgpKeysPage.tsx</files>
  <read_first>
    - frontend/src/pages/webhooks/WebhooksPage.tsx:287-307 (secret modal state + onClose — canonical analog)
    - frontend/src/pages/certificates/CertificatesPage.tsx:146,157 (revealedKey state)
    - frontend/src/pages/oauth2/OAuth2ClientsPage.tsx:283,297 (revealedSecret state)
    - frontend/src/pages/service-accounts/ServiceAccountsPage.tsx:202-206 (revealedSecret + title/desc)
    - frontend/src/pages/pgp/PgpKeysPage.tsx:287,298 (revealedKey state)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (SEC-036 secret state clear section)
  </read_first>
  <action>
    In each of the 5 pages, change the secret-reveal modal `onClose` (currently `() => setSecretOpen(false)` or equivalent) to ALSO clear the revealed value: call the page's reveal-state setter with `null` (WebhooksPage/OAuth2ClientsPage/ServiceAccountsPage → `setRevealedSecret(null)`; CertificatesPage/PgpKeysPage → `setRevealedKey(null)`). If a reveal-state initial value is `""` rather than nullable, either change it to `string | null` initialized `null` or set it back to `""` on close — the requirement is that no revealed secret value remains in state after close. For ServiceAccountsPage also reset any secret title/description state if it holds derived secret context.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -6 && grep -lE 'setRevealed(Secret|Key)\(null\)|setRevealed(Secret|Key)\(""\)' src/pages/certificates/CertificatesPage.tsx src/pages/oauth2/OAuth2ClientsPage.tsx src/pages/service-accounts/ServiceAccountsPage.tsx src/pages/webhooks/WebhooksPage.tsx src/pages/pgp/PgpKeysPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 pages calls its reveal-state setter to clear the secret inside the modal `onClose` handler (grep for `setRevealedSecret(null)` / `setRevealedKey(null)` — at least one per page)
    - `cd frontend && npx tsc -b --noEmit` succeeds (read output text)
  </acceptance_criteria>
  <done>No revealed secret persists in React state after any of the 5 reveal modals closes.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-037 — strip reset/verify tokens from URL after use</name>
  <files>frontend/src/pages/auth/ResetPasswordPage.tsx, frontend/src/pages/auth/VerifyEmailPage.tsx</files>
  <read_first>
    - frontend/src/pages/auth/ResetPasswordPage.tsx:36,62-73 (token read + async action try/catch)
    - frontend/src/pages/auth/VerifyEmailPage.tsx:31,42-57 (token read + doVerify useEffect)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (SEC-037 sections + Pitfall 5 timing note)
  </read_first>
  <action>
    ResetPasswordPage: after `await authService.confirmPasswordReset(token, newPw)` succeeds AND in the catch branch, call `window.history.replaceState({}, document.title, window.location.pathname);` to remove the `?token=...` query. The token is already captured in the `token` const at line 36 before the action runs, so stripping after the awaited call does not break the in-flight request.
    VerifyEmailPage: inside `doVerify()`, after `await authService.verifyEmail(token!)` succeeds AND in the catch branch, call the same `window.history.replaceState(...)`. Place it before the `setVerifyState`/`cancelled` checks per the analog. Do NOT call replaceState before the await (Pitfall 5 — would break a re-read of searchParams).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -6 && grep -c replaceState src/pages/auth/ResetPasswordPage.tsx src/pages/auth/VerifyEmailPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'replaceState' frontend/src/pages/auth/ResetPasswordPage.tsx` >= 1 AND `>= 1` for VerifyEmailPage (present in both success and failure branches per analog)
    - replaceState calls occur AFTER the awaited verify/confirm call (read the code to confirm ordering, not before the await)
    - `cd frontend && npx tsc -b --noEmit` succeeds (read output text)
  </acceptance_criteria>
  <done>Reset and verify tokens are removed from the URL/browser history after the token is consumed.</done>
</task>

<task type="auto">
  <name>Task 3: SEC-041 — redact email from ForgotPasswordPage failure log</name>
  <files>frontend/src/pages/auth/ForgotPasswordPage.tsx</files>
  <read_first>
    - frontend/src/pages/auth/ForgotPasswordPage.tsx:35 (`console.warn("[ForgotPassword] request failed:", err)`)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (SEC-041 section)
  </read_first>
  <action>
    Replace `console.warn("[ForgotPassword] request failed:", err);` at line 35 with `console.warn("[ForgotPassword] reset request failed (details redacted for privacy)");` — remove the `err` argument entirely. The AxiosError's `config.data` carries the serialized request body (the submitted email); dropping `err` from the log prevents PII leakage to devtools and any error-tracking sink. Keep the surrounding user-facing behavior (the page already shows a generic "if an account exists..." message) unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -6 && grep -n 'console.warn' src/pages/auth/ForgotPasswordPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'request failed:", err' frontend/src/pages/auth/ForgotPasswordPage.tsx` returns nothing (no err arg logged)
    - `grep -ni 'redacted' frontend/src/pages/auth/ForgotPasswordPage.tsx` matches
    - `cd frontend && npx tsc -b --noEmit` succeeds (read output text)
  </acceptance_criteria>
  <done>The forgot-password failure log no longer carries the submitted email.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| revealed secret → JS heap/devtools | One-time-shown secrets (cert/client/PGP keys) must not linger in React state after the modal closes |
| token → browser history | Single-use reset/verify tokens in the URL persist in history/referrer unless stripped |
| error object → console/error-tracking | AxiosError carries request body (email) into logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Information Disclosure | revealed secret retained in React state after modal close, 5 pages (SEC-036) | mitigate | Task 1: clear reveal-state setter to null on onClose |
| T-12-14 | Information Disclosure | reset/verify token persists in URL/browser history (SEC-037) | mitigate | Task 2: history.replaceState strips token after use |
| T-12-15 | Information Disclosure | submitted email leaked via logged AxiosError (SEC-041) | mitigate | Task 3: remove err arg from console.warn |
</threat_model>

<verification>
- `cd frontend && npm run lint && npx tsc -b` green
- 5 pages clear revealed secret on close; 2 auth pages call replaceState after token use; ForgotPasswordPage logs no err
</verification>

<success_criteria>
ROADMAP SC #4 satisfied: secrets cleared from React state on modal close (5 pages), reset/verify tokens stripped from URL via history.replaceState (2 pages), no full Axios error/email logging on ForgotPasswordPage — all green per acceptance criteria.
</success_criteria>

<output>
Create `.planning/phases/12-low-remediation/12-04-SUMMARY.md` when done.
</output>
