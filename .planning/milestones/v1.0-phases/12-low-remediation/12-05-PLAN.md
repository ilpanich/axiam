---
phase: 12-low-remediation
plan: 05
type: execute
wave: 2
depends_on: [12-01, 12-02, 12-03, 12-04]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements: [REQ-16]

must_haves:
  truths:
    - "The whole effort builds, lints (-D warnings), and tests green in CI across the workspace"
    - "cargo audit / cargo-deny / npm audit report no unaddressed advisories"
    - "Frontend lint + tsc -b + vitest pass and Playwright e2e gates in CI"
    - "The manual multi-protocol smoke checklist passes against a live dev environment"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Playwright e2e gate present/verified in CI"
      contains: "playwright"
  key_links:
    - from: "CI workflow"
      to: "frontend e2e + workspace build"
      via: "gating jobs that must be green before merge"
      pattern: "playwright"
---

<objective>
Run the Phase-12 whole-effort final verification gate (ROADMAP SC #5): the full toolchain sweep across all Phase 8–12 remediation, plus the manual multi-protocol smoke. This plan is the LAST wave and depends on Plans 12-01..12-04.

Purpose: Prove the entire remediation tranche is green before the phase closes — no regressions introduced by the LOW fixes, and the security-critical end-to-end flows still work.
Output: A verification record (in the SUMMARY) of every gate's result; CI Playwright e2e gate confirmed/added; a completed manual smoke checklist with the human verifier's sign-off.

DISK CONSTRAINT (critical): `/home` is near-full; `target/` ~46 GiB. Do NOT run `cargo build --workspace` or `cargo test --workspace` locally — they ENOSPC on link. The `--workspace` build/clippy/test gate is a CI responsibility (this plan is `autonomous: false`). Locally, reclaim space first (`rm -rf target/debug/incremental`) and run per-crate `cargo check`/targeted tests as a smoke. SAML is behind the `saml` feature; the 3 SAML federation_test failures are a KNOWN baseline, NOT regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-low-remediation/12-RESEARCH.md
@.planning/phases/12-low-remediation/12-VALIDATION.md
@/home/emanuele/git/priv/axiam/CLAUDE.md

@.planning/phases/12-low-remediation/12-01-SUMMARY.md
@.planning/phases/12-low-remediation/12-02-SUMMARY.md
@.planning/phases/12-low-remediation/12-03-SUMMARY.md
@.planning/phases/12-low-remediation/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local per-crate + frontend verification sweep (disk-safe)</name>
  <files>(verification only — no source edits unless a regression is found)</files>
  <read_first>
    - .planning/phases/12-low-remediation/12-VALIDATION.md (Per-Task Verification Map + manual smoke checklist)
    - .planning/phases/12-low-remediation/12-RESEARCH.md (Validation Architecture: per-crate commands; disk pitfall 6)
    - CLAUDE.md (build/test rules) and MEMORY.md (disk-full guidance, --no-default-features)
  </read_first>
  <action>
    First reclaim disk if needed: `rm -rf target/debug/incremental`. Then run the disk-safe local sweep (read OUTPUT TEXT, not exit codes):
    1. `cargo check -p axiam-db -p axiam-api-rest -p axiam-auth -p axiam-authz -p axiam-amqp -p axiam-pki -p axiam-oauth2 -p axiam-audit -p axiam-server --tests --no-default-features`
    2. `cargo clippy -p axiam-db -p axiam-api-rest -p axiam-auth -p axiam-audit -p axiam-server --no-default-features -- -D warnings`
    3. Targeted tests for the crates Phase 12 touched: `cargo test -p axiam-db --test seeder_skip_test --no-default-features`, `cargo test -p axiam-db --lib --no-default-features`, `cargo test -p axiam-auth --lib --no-default-features`, `cargo test -p axiam-audit --lib --no-default-features`
    4. Frontend: `cd frontend && npm run lint && npx tsc -b && npx vitest run`
    If any check fails AND the failure is not the known 3-SAML-federation_test baseline, fix the regression in place (it will be a small follow-on to a Phase-12 edit), re-run, and record. Record every command's pass/fail in the SUMMARY.
  </action>
  <verify>
    <automated>cargo check -p axiam-db -p axiam-api-rest -p axiam-auth -p axiam-audit -p axiam-server --tests --no-default-features 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All per-crate `cargo check` commands report no errors (read output text)
    - `cargo clippy ... -- -D warnings` for the five touched crates reports zero warnings
    - `cargo test -p axiam-db --test seeder_skip_test --no-default-features` shows `test result: ok`
    - `cd frontend && npm run lint && npx tsc -b && npx vitest run` all pass
    - Any non-baseline failure encountered was fixed and re-verified (SUMMARY records the diff); the 3 SAML federation_test baseline failures are documented as expected, not regressions
  </acceptance_criteria>
  <done>Disk-safe local sweep green; results recorded; any regression fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm/add CI gates — workspace build/clippy/test, audit/deny, npm audit, Playwright e2e</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (existing build/clippy/test, cargo-audit, cargo-deny, npm steps; Playwright presence)
    - .planning/phases/12-low-remediation/12-RESEARCH.md (Validation Architecture final flow; AC-5)
    - .planning/phases/06-ci-cd-infrastructure-hardening (prior CI hardening — for the established job patterns)
  </read_first>
  <action>
    Read ci.yml and confirm it already gates: workspace `cargo build`/`cargo clippy -D warnings`/`cargo test` (CI has the disk headroom local does not), `cargo audit`, `cargo-deny`, `npm audit`, and frontend `lint`/`tsc -b`/`vitest`. Verify whether a Playwright e2e job exists and gates the merge. If a Playwright e2e gate is MISSING, add a job that runs `cd frontend && npx playwright install --with-deps && npx playwright test` against the app (reuse the existing dev-up / service composition pattern from CI). Keep the existing SHA pins from Plan 12-02 intact. Do NOT change `hadolint no-fail`/`trivy exit-code` advisory settings. If all gates already exist, record that no edit was needed and only confirm coverage.
  </action>
  <verify>
    <automated>grep -niE 'cargo (build|clippy|test)|cargo-audit|cargo audit|cargo-deny|npm audit|playwright|vitest|tsc' .github/workflows/ci.yml | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml gates workspace `cargo build`, `cargo clippy -- -D warnings`, `cargo test` (grep confirms the steps)
    - ci.yml gates `cargo audit`/`cargo-deny` and `npm audit`
    - ci.yml gates frontend `lint` + `tsc -b` + `vitest`
    - ci.yml has a Playwright e2e gate (`grep -ni playwright .github/workflows/ci.yml` matches) — added if it was missing
    - SHA pins from Plan 12-02 are unchanged (`grep -nE 'uses:' .github/workflows/ci.yml | grep -vE '@[0-9a-f]{40}'` returns no unpinned action `uses:` lines)
  </acceptance_criteria>
  <done>Every AC-5 toolchain gate is present in CI; Playwright e2e gates the merge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual multi-protocol smoke checklist (human verify)</name>
  <action>The human verifier runs the live smoke checklist below against a `just dev-up` + `just run` environment (plus the frontend dev server). This cannot be automated locally because it requires live REST + gRPC + federation IdP + email. Pause for sign-off before closing the phase; if any item fails, the failing fix returns to its source plan for repair.</action>
  <what-built>Phase 8–12 remediation complete; automated local sweep + CI gates green. This checkpoint runs the manual multi-protocol smoke that cannot be automated locally (requires live REST + gRPC + federation IdP + email).</what-built>
  <how-to-verify>
    Bring up the dev environment: `just dev-up` then `just run` (and the frontend dev server). Walk the smoke checklist (12-VALIDATION.md / 12-RESEARCH.md AC-5), confirming each:
    1. Login → MFA enroll → MFA verify → dashboard visible
    2. Password reset: email sent → link opened → password changed → login with new password (token stripped from URL after use — SEC-037)
    3. Email verify: link clicked → token stripped from URL → success page
    4. Change password: HIBP check triggers on a known-compromised password (CQ-B35)
    5. GDPR export → download JSON
    6. Federation OIDC login → restart server → OIDC login succeeds again (secret decrypt-at-use)
    7. Cross-org request: as Org A user, access Org B resource → 403
    8. gRPC call without credentials → UNAUTHENTICATED
    9. Bootstrap page after admin exists → 404 → "already initialized" message shown (NOT a confusing error)
    10. Admin user create: password policy checker visible and gates submit (CQ-F23)
    11. Reveal a secret (cert/client/PGP) then close the modal → secret no longer in React state (SEC-036, verify via React devtools)
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 smoke items pass, or list the failing item(s) with observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → merge | The merge gate is the last automated barrier before remediation ships; must enforce build/clippy/test/audit/e2e |
| live multi-protocol surface | REST + gRPC + federation + email flows must be exercised end-to-end to confirm no LOW fix regressed a security control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Tampering | a LOW fix silently regresses a higher-severity control (cross-org 403, gRPC auth, HIBP) | mitigate | Manual smoke items 4/6/7/8 re-exercise the critical controls; CI workspace test gate catches code regressions |
| T-12-17 | Repudiation | `--workspace` test skipped locally due to disk → false-green | mitigate | Plan is `autonomous: false`; `--workspace` build/clippy/test runs in CI (which has disk headroom), not as a local autonomous task |
| T-12-SC | Tampering | supply-chain (cargo audit/deny, npm audit) | mitigate | Task 2 confirms audit/deny/npm-audit gates remain in CI; Plan 12-02 SHA-pinned all Actions |
</threat_model>

<verification>
- Local disk-safe sweep green (per-crate check/clippy/targeted-test + frontend lint/tsc/vitest)
- CI gates confirmed for workspace build/clippy/test, cargo audit/deny, npm audit, frontend, Playwright e2e
- Manual smoke checklist (11 items) approved by human verifier
</verification>

<success_criteria>
ROADMAP SC #5 satisfied: whole-effort verification green — workspace cargo build/clippy -D warnings/test (CI), cargo audit/cargo-deny, npm audit, frontend lint && tsc -b && vitest, Playwright e2e gating in CI; manual smoke (login→MFA→reset/verify/change-pw→GDPR→federation-after-restart→cross-org 403→gRPC-no-creds rejected) passes.
</success_criteria>

<output>
Create `.planning/phases/12-low-remediation/12-05-SUMMARY.md` when done, recording every gate's result and the human smoke sign-off. This closes Phase 12 and the audit-remediation tranche.
</output>
