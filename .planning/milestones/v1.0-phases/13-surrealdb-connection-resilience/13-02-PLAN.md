---
phase: 13-surrealdb-connection-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/e2e-bootstrap.sh
  - justfile
autonomous: true
requirements: [REQ-17]
user_setup: []

must_haves:
  truths:
    - "e2e-bootstrap.sh seeds org+tenant into the SAME database the server reads (db=main), so the bootstrap endpoint finds the seeded org/tenant."
    - "The tenant CREATE no longer sets the removed is_active field, so statement 2 no longer fails with status ERR."
    - "A single `just bootstrap-local` command seeds org+tenant+admin against the run-local server, unblocking the deferred 12-HUMAN-UAT smoke."
  artifacts:
    - path: "scripts/e2e-bootstrap.sh"
      provides: "Seed script targeting the server's db (default main, env-overridable) without is_active"
      contains: "AXIAM__DB__DATABASE"
    - path: "justfile"
      provides: "bootstrap-local recipe seeding the local run-local server in one command"
      contains: "bootstrap-local"
  key_links:
    - from: "scripts/e2e-bootstrap.sh (SurrealDB /sql seed)"
      to: "server DbConfig database (main)"
      via: "surreal-db header driven by AXIAM__DB__DATABASE default main"
      pattern: "surreal-db: \\$"
    - from: "justfile bootstrap-local"
      to: "scripts/e2e-bootstrap.sh + /api/v1/admin/bootstrap"
      via: "env-configured invocation against the run-local server"
      pattern: "e2e-bootstrap.sh"
---

<objective>
Repair the documented first-run seed path so a working admin can be created
end-to-end against the server's actual database, unblocking the deferred
Phase-12 manual smoke (`12-HUMAN-UAT.md`).

Purpose: Two confirmed bugs (13-RESEARCH Q6) make the seed silently fail —
the script writes to db `axiam` while the server reads db `main`, and the tenant
CREATE sets an `is_active` field the SCHEMAFULL `tenant` table rejects (statement 2
ERR). Fixing both, plus a `just bootstrap-local` helper, gives a one-command
repeatable local first-run (REQ-17, ROADMAP SC #3, #4, #5).

Output: Corrected `scripts/e2e-bootstrap.sh` and a new `bootstrap-local` justfile recipe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-surrealdb-connection-resilience/13-CONTEXT.md
@.planning/phases/13-surrealdb-connection-resilience/13-RESEARCH.md

@scripts/e2e-bootstrap.sh
@justfile

<interfaces>
<!-- Contracts the executor needs. -->

DbConfig defaults (crates/axiam-db/src/connection.rs): namespace="axiam", database="main".
tenant SCHEMAFULL fields (crates/axiam-db/src/schema.rs:170-179):
  organization_id, name, slug, metadata, created_at, updated_at  — NO is_active.

e2e-bootstrap.sh current bugs (RESEARCH Q6):
  - line 73: `-H "surreal-db: axiam"`  → must target the server's db (main)
  - line 88: `is_active = true,`        → remove entirely
  /sql endpoint returns HTTP 200 even on statement error — existing status-field
  check (lines 97-101) is correct and MUST be preserved (project memory: silent false-green).

run-local recipe (justfile:53-72): generates Ed25519 keypair, sets COOKIE_SECURE=false,
  AMQP creds, runs `cargo run --bin axiam-server --no-default-features`. Local server on
  AXIAM_URL default http://localhost:8090; SurrealDB at http://localhost:8000 (just dev-up).
bootstrap endpoint: POST /api/v1/admin/bootstrap (public, 201 created / 404 already done).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix e2e-bootstrap.sh db-name mismatch + drop is_active</name>
  <files>scripts/e2e-bootstrap.sh</files>
  <action>Per RESEARCH Q6: (a) Add an env-driven DB name near the other env defaults: `AXIAM_DB="${AXIAM__DB__DATABASE:-main}"` and change the seed request header from `-H "surreal-db: axiam"` to `-H "surreal-db: ${AXIAM_DB}"` (matches the server's DbConfig default `main`; env-overridable). Keep `surreal-ns: axiam` unchanged. (b) Remove the `is_active = true,` line from the `CREATE type::record('tenant', ...)` block — the SCHEMAFULL tenant table has no such field and rejects it (statement 2 ERR). Do NOT touch the `type::record(...)` double-colon syntax or the per-statement status-field error check (lines 97-101) — both are load-bearing (project memory: /sql returns HTTP 200 on statement error). Echo the resolved DB name in the existing log line for visibility.</action>
  <verify>
    <automated>cd /home/emanuele/git/priv/axiam && bash -n scripts/e2e-bootstrap.sh && grep -c 'is_active' scripts/e2e-bootstrap.sh && grep -c 'surreal-db: ${AXIAM_DB}' scripts/e2e-bootstrap.sh</automated>
  </verify>
  <done>`bash -n` reports no syntax errors; `is_active` count is 0; `surreal-db: ${AXIAM_DB}` present (count >= 1); the status-field ERR/OK check block is still present.</done>
</task>

<task type="auto">
  <name>Task 2: Add just bootstrap-local recipe</name>
  <files>justfile</files>
  <action>Add a `bootstrap-local` recipe (shebang bash, `set -euo pipefail`, mirroring the run-local style) that seeds org+tenant+admin against the run-local server by invoking `scripts/e2e-bootstrap.sh` with local-dev env: `AXIAM_URL` default `http://localhost:8090`, `SURREAL_URL` default `http://localhost:8000`, and `AXIAM__DB__DATABASE` default `main` so the seed targets the same db run-local uses. Allow callers to override org/tenant/admin via the script's existing E2E_* env vars (document defaults in a comment). Add a one-line comment block noting it requires `just dev-up` (SurrealDB) and a running `just run-local` server, and that it unblocks the 12-HUMAN-UAT smoke. Do NOT duplicate seed logic — delegate to e2e-bootstrap.sh.</action>
  <verify>
    <automated>cd /home/emanuele/git/priv/axiam && just --list 2>&1 | grep -c 'bootstrap-local' && just --evaluate 2>&1 | head -1 || just --list 2>&1 | grep bootstrap-local</automated>
  </verify>
  <done>`just --list` shows the `bootstrap-local` recipe (grep count >= 1) and the justfile parses without error; the recipe delegates to scripts/e2e-bootstrap.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ↔ seed script ↔ SurrealDB /sql | Misconfigured target db/field silently produces an unseeded DB (false-green first-run) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering (silent misconfig) | e2e-bootstrap.sh writing to wrong db | mitigate | Header driven by AXIAM__DB__DATABASE default main, matching server config (Task 1) |
| T-13-05 | Repudiation/Availability (silent false-green) | /sql HTTP 200 on statement ERR | accept (already mitigated) | Existing per-statement status-field check preserved unchanged (project memory CR-01) |

No package installs — no legitimacy gate required.
</threat_model>

<verification>
- `bash -n scripts/e2e-bootstrap.sh` clean; `grep -c is_active scripts/e2e-bootstrap.sh` == 0.
- Seed header is env-driven (`surreal-db: ${AXIAM_DB}`) defaulting to `main`.
- `just --list` includes `bootstrap-local`.
- The /sql per-statement ERR/OK fail-closed check is intact (no silent false-green).
</verification>

<success_criteria>
- e2e-bootstrap.sh seeds into the db the server reads (main) and no longer sets is_active (ROADMAP SC #3).
- `just bootstrap-local` provides a repeatable one-command local first-run seeding org+tenant+admin against run-local (SC #4).
- The deferred 12-HUMAN-UAT smoke is unblocked — a working admin can be created end-to-end (SC #5). This phase does NOT execute the manual smoke (Deferred Idea).
</success_criteria>

<output>
Create `.planning/phases/13-surrealdb-connection-resilience/13-02-SUMMARY.md` when done.
</output>
