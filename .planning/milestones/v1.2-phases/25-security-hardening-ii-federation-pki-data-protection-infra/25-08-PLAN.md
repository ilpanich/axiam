---
phase: 25-security-hardening-ii-federation-pki-data-protection-infra
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-amqp/src/mail_consumer.rs
  - crates/axiam-amqp/tests/mail_consumer_test.rs
autonomous: true
requirements: [SECHRD-08]

must_haves:
  truths:
    - "The mail consumer waits a backoff delay (scaled by attempt count) before republishing a RetryNeeded message — no zero-delay hot-retry loop (SECHRD-08 / D-05d)"
    - "An ExportReady message carrying a real org_id renders deliverable end-to-end (SECHRD-08 / D-05d)"
  artifacts:
    - "mail_consumer.rs backoff (tokio::time::sleep scaled by attempt_count) before basic_publish on RetryNeeded"
    - "crates/axiam-amqp/tests/mail_consumer_test.rs::export_ready_resolves_real_org_id"
  key_links:
    - "backoff mirrors webhook.rs's exponential-backoff shape (in-process sleep, no broker plugin)"
    - "the deliverability test asserts a real org_id reaches the rendered template context"
---

<objective>
Fix the mail consumer's zero-delay retry and prove ExportReady mail is deliverable end-to-end (SECHRD-08 / D-05d). Today `mail_consumer.rs`'s `SendOutcome::RetryNeeded` branch (`:325-359`) immediately re-publishes with no delay — a hot-retry loop against a possibly-down SMTP relay. This plan inserts an in-process `tokio::time::sleep` scaled by `attempt_count` (mirroring the only in-repo backoff precedent, `webhook.rs:210-220`; no RabbitMQ delayed-exchange plugin per the "no new infra" constraint) and adds the end-to-end deliverability test asserting a real `org_id` reaches the rendered template context (the producer-side org_id resolution lands in plan 25-05).

Purpose: ExportReady mail retries with backoff and delivers with a real org_id (SECHRD-08 mail half).
Output: backoff before republish + `export_ready_resolves_real_org_id` deliverability test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-security-hardening-ii-federation-pki-data-protection-infra/25-RESEARCH.md
@.planning/phases/25-security-hardening-ii-federation-pki-data-protection-infra/25-PATTERNS.md
@crates/axiam-amqp/src/mail_consumer.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert exponential backoff before the RetryNeeded republish</name>
  <files>crates/axiam-amqp/src/mail_consumer.rs</files>
  <read_first>
    - crates/axiam-amqp/src/mail_consumer.rs:325-359 (the `SendOutcome::RetryNeeded` branch that calls `channel.basic_publish` with zero delay; `attempt_count` on the retry message)
    - crates/axiam-api-rest/src/webhook.rs:210-220 (the ONLY in-repo backoff precedent to mirror: `(initial_delay as f64) * multiplier.powi((attempt-1) as i32)` + `tokio::time::sleep` before each attempt after the first)
    - 25-RESEARCH.md Pitfall 6 + Assumptions Log A2 (in-process `tokio::time::sleep` is the low-risk in-scope choice; DO NOT introduce a broker delayed-exchange plugin or a TTL+DLX parking-lot; note the single-consumer throughput tradeoff already flagged)
    - 25-PATTERNS.md lines 216-217 (backoff insertion point)
  </read_first>
  <action>
    In the `SendOutcome::RetryNeeded` branch, BEFORE `channel.basic_publish`, insert a `tokio::time::sleep(Duration::from_secs_f64(delay))` where `delay` is computed from the message's `attempt_count` using the same exponential shape as `webhook.rs` (`initial_delay * multiplier.powi((attempt_count - 1) as i32)`), with a sane cap. Use existing config values if the mail path exposes an initial-delay/multiplier; otherwise define local constants matching the webhook defaults and add a brief comment. Keep the existing nack/requeue and DLQ semantics unchanged — only the delay before republish is added. Do not add signing to mail (out of scope per RESEARCH Pitfall 5).
  </action>
  <verify>
    <automated>cargo test -p axiam-amqp --lib mail 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The `RetryNeeded` branch sleeps a backoff delay scaled by `attempt_count` before `basic_publish` — no zero-delay republish remains
    - Backoff is in-process `tokio::time::sleep` (no broker plugin / delayed-exchange dependency added)
    - Existing nack/requeue/DLQ semantics preserved; `cargo clippy -p axiam-amqp --lib -- -D warnings` clean
  </acceptance_criteria>
  <done>The mail retry waits an exponential backoff before republishing.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end ExportReady deliverability test (real org_id reaches the template)</name>
  <files>crates/axiam-amqp/tests/mail_consumer_test.rs</files>
  <read_first>
    - crates/axiam-amqp/tests/mail_consumer_test.rs (existing mail-consumer test pattern + fake SMTP sink to reuse)
    - 25-VALIDATION.md rows SC 5b (`export_ready_resolves_real_org_id` — assert `org_id != Uuid::nil()` reaches the rendered template context) and "Test Doubles Needed" (fake SMTP sink; forced transient failure → backoff)
    - 25-RESEARCH.md D-05d (deliverability proven by test) — the producer-side org_id resolution itself lands in plan 25-05
  </read_first>
  <action>
    Add `export_ready_resolves_real_org_id`: construct an ExportReady mail message carrying a real (non-nil) `org_id`, run it through the mail consumer against a fake SMTP sink, and assert the rendered template context / delivered message carries that real `org_id` (not `Uuid::nil()`), proving end-to-end deliverability. Optionally add a companion assertion that a forced transient send failure triggers the Task-1 backoff path (republish with delay), keeping the test hermetic (no live SMTP). Reuse the existing fake-SMTP-sink fixture.
  </action>
  <verify>
    <automated>cargo test -p axiam-amqp --test mail_consumer_test export_ready_resolves_real_org_id 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p axiam-amqp --test mail_consumer_test export_ready_resolves_real_org_id` passes: a real `org_id` reaches the rendered template context and the message is deliverable end-to-end (SECHRD-08 negative/deliverability test, SC #5b)
    - The test is hermetic (fake SMTP sink, no live relay)
  </acceptance_criteria>
  <done>The ExportReady deliverability test passes with a real org_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal mail queue → SMTP relay | AXIAM-enqueued ExportReady/verification mail must actually deliver; a hot-retry loop against a down relay is a self-inflicted DoS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-22 | Denial of Service (self) | zero-delay retry loop | medium | mitigate | In-process exponential backoff before republish (Task 1) |
| T-25-23 | Denial of mail delivery | ExportReady with nil org_id undeliverable | medium | mitigate | Deliverability test asserts real org_id reaches the template (Task 2); producer resolution in plan 25-05 |
| T-25-24 | Tampering | signing mail messages (over-scope) | low | accept | Mail is server-internal, not a cross-tenant boundary (RESEARCH Pitfall 5) — no signing added to mail |
</threat_model>

<verification>
- `cargo test -p axiam-amqp --test mail_consumer_test export_ready_resolves_real_org_id` green
- `cargo test -p axiam-amqp --lib mail` green; clippy/fmt clean
</verification>

<success_criteria>
The mail consumer retries with backoff and ExportReady mail carrying a real org_id is deliverable end-to-end, proven by a passing deliverability test (SECHRD-08, SC #5b).
</success_criteria>

<artifacts>
## Artifacts this plan produces (new symbols — exclude from source-drift checks)
- Backoff computation in `mail_consumer.rs` RetryNeeded branch
- Test: `export_ready_resolves_real_org_id`
</artifacts>

<output>
Create `.planning/phases/25-security-hardening-ii-federation-pki-data-protection-infra/25-08-SUMMARY.md` when done.
</output>
