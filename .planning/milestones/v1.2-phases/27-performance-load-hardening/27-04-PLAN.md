---
phase: 27-performance-load-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - sdks/typescript/src/node/jwks.ts
  - sdks/typescript/test/node/jwks.test.ts
  - sdks/php/src/Auth/JwksVerifier.php
  - sdks/php/tests/JwksSingleFlightTest.php
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "In the TypeScript (Node) SDK, N concurrent verifyAccessToken calls with an unknown kid trigger exactly ONE fetch — proven by a test that mocks global fetch and counts calls; if jose already coalesces, the test proves it, otherwise an inFlightFetch promise guard is added (D-08/D-09)"
    - "In the PHP SDK, the refetch is wrapped in a Guzzle-promise-based in-flight guard so the single-flight guarantee is observable under Guzzle async; the classic-FPM vacuity is documented (D-08/D-09, RESEARCH Pitfall 6)"
    - "The guard wraps only the fetch; jose / firebase-php-jwt verification is unchanged"
  artifacts:
    - "sdks/typescript/src/node/jwks.ts inFlightFetch promise guard (if needed) or a test proving jose coalescing"
    - "sdks/php/src/Auth/JwksVerifier.php Guzzle-promise in-flight guard around ensureFresh"
    - sdks/php/tests/JwksSingleFlightTest.php
  key_links:
    - "TS reuses the existing jwksPromise lazy-singleton pattern for the fetch coalescing"
    - "PHP test exercises the Guzzle async promise path (sendAsync + Promise\\Utils::settle), NOT a plain sequential loop"
---

<objective>
Apply the uniform hand-rolled JWKS single-flight guard (D-08) to the TypeScript (Node)
and PHP SDK verifiers so a concurrent invalid-`kid` cache-miss burst collapses to exactly
one network fetch (D-09). Both delegate crypto to a library (`jose` / `firebase/php-jwt`)
that must not be touched — this is a coalescing wrapper only.

Purpose: Close PERF-03 for the final 2 of the 7 SDKs. These two need careful test
design: TypeScript delegates fetching to `jose`'s `createRemoteJWKSet` (may or may not
coalesce concurrent in-flight fetches — must be verified by test); PHP's single-flight
is only observable under Guzzle's async interface, NOT classic sync PHP-FPM (RESEARCH
Pitfall 6).
Output: fetch coalescing proven/added per SDK, each with a genuinely-concurrent burst
test.
</objective>

## Artifacts this phase produces

- TypeScript: an `inFlightFetch: Promise<...> | null` guard in `node/jwks.ts` (only if the test shows `jose` does not already coalesce) — mirrors the existing `jwksPromise` lazy-singleton
- PHP: a Guzzle-promise in-flight guard around `ensureFresh` in `JwksVerifier.php`
- New PHP test `sdks/php/tests/JwksSingleFlightTest.php`; extended `sdks/typescript/test/node/jwks.test.ts`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-performance-load-hardening/27-CONTEXT.md
@.planning/phases/27-performance-load-hardening/27-RESEARCH.md
@.planning/phases/27-performance-load-hardening/27-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TypeScript (Node) SDK JWKS single-flight</name>
  <files>sdks/typescript/src/node/jwks.ts, sdks/typescript/test/node/jwks.test.ts</files>
  <read_first>
    - sdks/typescript/src/node/jwks.ts (delegates to `jose`'s `createRemoteJWKSet`; the existing `jwksPromise` lazy-singleton coalesces GETTER construction, but the per-verify key lookup / refetch-on-unknown-kid happens INSIDE jose's getter on every `jwtVerify`)
    - sdks/typescript/test/node/jwks.test.ts (existing tests + how `fetch`/jose is mocked)
    - 27-PATTERNS.md § "SDK JWKS verifiers … TypeScript"; 27-RESEARCH.md § Pattern 4 (TypeScript)
  </read_first>
  <behavior>
    - Mock global `fetch`; fire N (e.g. 8) concurrent `verifyAccessToken()` calls with an unknown kid against a cold jose getter → assert `fetch` (to the jwks URL) is called exactly once
    - If jose already coalesces, the test passes as-is and documents that; if not, the added inFlightFetch guard makes it pass
    - A subsequent verify after the fetch reuses the resolved keyset (no extra fetch)
  </behavior>
  <action>
    First write the burst test in `sdks/typescript/test/node/jwks.test.ts`: mock global
    `fetch` with a call counter, fire 8 concurrent `verifyAccessToken()` promises with an
    unknown-kid token against a cold cache via `Promise.all`, and assert the counter ==
    1. Run it. If `jose`'s `createRemoteJWKSet` already coalesces concurrent in-flight
    fetches, the test passes and you document that in a code comment near `jwksPromise`.
    If it does NOT (counter > 1), add an `inFlightFetch: Promise<...> | null` guard in
    `node/jwks.ts` wrapping the getter/fetch call — reuse the SAME lazy-promise-singleton
    shape already used for `jwksPromise`: if `inFlightFetch` is set, await it; otherwise
    set it to the fetch promise, await, then reset to `null` in a `finally`. Do NOT touch
    the `jose` `jwtVerify`/EdDSA verification (D-08). Keep the guard scoped to the fetch
    only.
  </action>
  <verify>
    <automated>cd /home/user/axiam/sdks/typescript && npx vitest run test/node/jwks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sdks/typescript/test/node/jwks.test.ts` contains a concurrent-burst test that mocks `fetch` and asserts exactly 1 call under an unknown-kid burst
    - `sdks/typescript/src/node/jwks.ts` either documents proven jose coalescing OR contains an `inFlightFetch` promise guard reset in a `finally`
    - `cd sdks/typescript && npx vitest run test/node/jwks.test.ts` exits 0
    - `cd sdks/typescript && npx tsc --noEmit` is clean
    - The `jose` verification path is unchanged
  </acceptance_criteria>
  <done>TS Node SDK proven (or made) to coalesce concurrent JWKS fetches to one; burst test green; jose verify untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PHP SDK JWKS single-flight (Guzzle async)</name>
  <files>sdks/php/src/Auth/JwksVerifier.php, sdks/php/tests/JwksSingleFlightTest.php</files>
  <read_first>
    - sdks/php/src/Auth/JwksVerifier.php (fully hand-rolled; `$fetchedAt` int TTL, no shared/PSR cache; the `ensureFresh` refetch path)
    - sdks/php/tests/SingleFlightRefreshTest.php (existing Guzzle-async single-flight TEST pattern for token refresh — mirror its `sendAsync`/promise harness and MockHandler counting)
    - sdks/php/tests/JwtVerifyTest.php (how JWKS/JWT fixtures + Guzzle client are mocked)
    - 27-PATTERNS.md § "SDK JWKS verifiers … PHP"; 27-RESEARCH.md § Pattern 4 (PHP) + Pitfall 6
  </read_first>
  <behavior>
    - Using Guzzle's async interface (`sendAsync` + `Promise\Utils::settle`), interleave N (e.g. 8) in-flight `verify()`-triggered refetches within ONE process against a cold cache with an unknown kid → the counting MockHandler records exactly 1 JWKS request
    - A plain sequential `verify()` loop is NOT used to prove concurrency (it cannot under classic FPM — Pitfall 6)
  </behavior>
  <action>
    Wrap the `ensureFresh` refetch call in `JwksVerifier.php` in a Guzzle-promise-based
    in-flight guard: hold a single shared in-flight promise (a private
    `?PromiseInterface $inFlightFetch` field) so concurrent `verify()`-triggered fetches
    within one process/coroutine await the same promise instead of each issuing their own
    request; reset it to `null` once resolved/settled. Re-check `$fetchedAt` freshness
    before starting a new fetch. Do NOT change the `firebase/php-jwt` verification call
    (D-08). Add `sdks/php/tests/JwksSingleFlightTest.php` that uses Guzzle's async
    interface + a counting `MockHandler` (mirror `SingleFlightRefreshTest.php`) to launch
    8 interleaved in-flight fetches within one process and assert exactly 1 JWKS request.
    Add a short doc comment in `JwksVerifier.php` stating that under classic sync PHP-FPM
    (one request per worker process, no shared memory) the guarantee is vacuous by design
    — cross-process JWKS caching is out of this phase's scope (RESEARCH Pitfall 6).
  </action>
  <verify>
    <automated>cd /home/user/axiam/sdks/php && vendor/bin/phpunit tests/JwksSingleFlightTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `sdks/php/src/Auth/JwksVerifier.php` contains an in-flight promise guard around `ensureFresh` (a shared promise field reset after settle) and a doc comment noting the classic-FPM vacuity
    - `sdks/php/tests/JwksSingleFlightTest.php` exists, uses Guzzle async (`sendAsync`/`Promise\Utils::settle`), and asserts exactly 1 JWKS request under 8 interleaved fetches
    - `cd sdks/php && vendor/bin/phpunit tests/JwksSingleFlightTest.php` exits 0
    - The `firebase/php-jwt` verification call is unchanged
  </acceptance_criteria>
  <done>PHP SDK coalesces concurrent Guzzle-async JWKS fetches to one; async burst test green; FPM vacuity documented; verification untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resource-server (embedding the SDK) → AXIAM JWKS endpoint | Outbound key fetch on cache miss; an invalid-`kid` burst can become a fetch storm (only observable intra-process in Node / Guzzle-async) |
| untrusted bearer token → SDK verifier | Attacker-controlled `kid` drives cache-miss fetches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-26 | Denial of Service | JWKS endpoint under invalid-`kid` burst (Node / PHP-Guzzle-async) | medium | mitigate | Single-flight promise guard collapses concurrent in-flight fetches to 1 (Tasks 1-2) |
| T-27-27 | Spoofing / Tampering | guard honoring a rotated/revoked key across refresh | high | mitigate | Guard wraps only the fetch; jose/php-jwt verification + TTL/refetch-on-unknown-kid unchanged — acceptance criteria require the verify path untouched |
| T-27-28 | Denial of Service (false assurance) | a PHP test that "passes" without actually proving single-flight (sequential FPM loop) | low | mitigate | Test MUST use Guzzle async (Pitfall 6); FPM vacuity documented so the guarantee's scope is not overstated |
</threat_model>

<verification>
- TypeScript: `cd sdks/typescript && npx vitest run test/node/jwks.test.ts` and `npx tsc --noEmit` clean
- PHP: `cd sdks/php && vendor/bin/phpunit tests/JwksSingleFlightTest.php` green
- Each SDK uses its own toolchain/runner; no AXIAM Rust workspace build, so no SWAGGER_UI workaround needed here.
</verification>

<success_criteria>
PERF-03 success criterion is met for the TypeScript and PHP SDKs: a concurrent
invalid-`kid` cache-miss burst triggers exactly one JWKS fetch (observable under the
async path), verification semantics unchanged. With plans 27-02 and 27-03 this completes
PERF-03 across all 7 code SDKs.
</success_criteria>

<output>
Create `.planning/phases/27-performance-load-hardening/27-04-SUMMARY.md` when done
</output>
