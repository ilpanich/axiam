---
phase: 29-structural-quality
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - crates/axiam-api-rest/src/state.rs
  - crates/axiam-api-rest/src/lib.rs
  - crates/axiam-server/src/main.rs
  - crates/axiam-api-rest/src/handlers/
  - crates/axiam-api-rest/src/extractors/auth.rs
  - crates/axiam-api-rest/src/extractors/cert_auth.rs
  - crates/axiam-api-rest/src/middleware/rate_limit_shared.rs
  - crates/axiam-api-rest/src/health.rs
  - crates/axiam-api-rest/tests/
  - crates/axiam-server/tests/
autonomous: true
requirements: [QUAL-01, QUAL-07]
must_haves:
  truths:
    - "main.rs composes a single AppState<C> and registers it once, replacing the 48 inline app_data registrations (QUAL-01/D-01)"
    - "Every REST handler extracts web::Data<AppState<C>> and accesses dependencies as fields (e.g. state.user_repo); the full existing test suite stays green — no behavior change (QUAL-01/D-06)"
    - "Conditionally-registered deps (email_config_repo) are Option<> AppState fields; handlers get None when unconfigured, preserving fail-closed behavior (QUAL-01/D-02)"
    - "The federation/reset/verification services (13 per-request construction sites) are hoisted into AppState as shared singletons constructed once at startup (QUAL-07/D-18)"
  artifacts:
    - "crates/axiam-api-rest/src/state.rs::AppState<C> (new composition-root struct)"
    - "singleton service fields: password_reset_service, email_verification_service, oidc_federation_service, saml_federation_service"
  key_links:
    - "AppState<C: surrealdb::Connection + Clone> mirrors the exact generic bound already on server.rs:61 register_api_v1_routes and every handler fn"
    - "The 7 non-handler extraction sites (extractors/auth.rs, extractors/cert_auth.rs, middleware/rate_limit_shared.rs, health.rs) pull their dependency out of AppState<C>, not the removed individual registrations"
    - "~35 test-harness builder macros construct and register one AppState<C> (they ARE the no-behavior-change gate)"
  prohibitions:
    - "No dependency may be registered outside AppState — the single composition root holds everything (D-01/D-02)"
    - "Do not silently drop the 7 non-handler web::Data sites (extractors/middleware/health) — they return None at runtime (auth-extraction failure = severe regression, not a compile error) if missed"
    - "SamlFederationService must be constructed unconditionally in main.rs (its constructor deps do not require the saml feature) — only the handlers stay #[cfg(feature=\"saml\")]-gated; no cfg attributes on AppState fields"
    - "No repository method signatures change — this is behavior-preserving composition only"
---

<objective>
QUAL-01 — AppState extraction (full migration) + QUAL-07 per-request service hoisting.
Compose a single AppState<C> in main.rs, register it once, and migrate every REST handler
and the 7 non-handler extraction sites to extract web::Data<AppState<C>> with field access.
Hoist the 13 per-request federation/reset/verification service constructions into AppState
singletons. Behavior-preserving: the existing ~35 test-harness files + 13 Playwright specs
are the gate (D-06). Lands after the security-adjacent QUAL-03/04 (D-19) so it wires in
already-final repo method bodies.

Purpose: One composition root instead of 48 inline app_data registrations; per-request
service rebuilds eliminated (CQ-B43/CQ-B27). No behavior change.

Output: crates/axiam-api-rest/src/state.rs (AppState<C>), a single-registration main.rs,
migrated handlers + non-handler sites + test harnesses, hoisted singleton services.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structural-quality/29-CONTEXT.md
@.planning/phases/29-structural-quality/29-RESEARCH.md
@.planning/phases/29-structural-quality/29-PATTERNS.md
@.planning/phases/29-structural-quality/29-VALIDATION.md

# The generic bound to mirror on AppState<C>
@crates/axiam-api-rest/src/server.rs
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Location |
|----------|------|----------|
| `AppState<C: surrealdb::Connection + Clone>` | new struct | `crates/axiam-api-rest/src/state.rs` |
| singleton service fields (password_reset / email_verification / oidc_federation / saml_federation) | hoisted deps | `AppState` fields |
| single `.app_data(web::Data::new(app_state.clone()))` | composition root | `crates/axiam-server/src/main.rs` |

## Execution note (scale + budget)

This is the largest single surface in the phase: 48 main.rs registrations → 1 struct, 283
handler `web::Data<T>` extraction sites across 28 handler files, 7 non-handler sites, ~35
test-harness builder macros (~333 raw app_data calls, but consolidated behind one
`macro_rules! test_app!` per file). Prefer **scripted / bulk find-replace** for the mechanical
`dep: web::Data<XRepo<C>>` → `state: web::Data<AppState<C>>` + `dep.` → `state.dep_field.`
rewrites and the per-file macro edits, then compile and fix stragglers — do NOT hand-edit 283
sites individually. Set `export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip`
before every axiam-api-rest / axiam-server build or test.

<tasks>

<task type="auto">
  <name>Task 1: Define AppState<C>, compose+register once in main.rs, hoist services, update the 7 non-handler sites</name>
  <files>crates/axiam-api-rest/src/state.rs, crates/axiam-api-rest/src/lib.rs, crates/axiam-server/src/main.rs, crates/axiam-api-rest/src/extractors/auth.rs, crates/axiam-api-rest/src/extractors/cert_auth.rs, crates/axiam-api-rest/src/middleware/rate_limit_shared.rs, crates/axiam-api-rest/src/health.rs</files>
  <read_first>
    - crates/axiam-server/src/main.rs:772-838 (the 47 unconditional `.app_data(web::Data::new(...))` calls + the conditional email_config_repo Some/None match at :838 — the exact Option<> pattern for D-02)
    - crates/axiam-api-rest/src/server.rs:61 (`register_api_v1_routes<C: surrealdb::Connection + Clone>` — mirror this exact bound on AppState<C>)
    - crates/axiam-api-rest/src/extractors/auth.rs:97,150,193,207 ; extractors/cert_auth.rs:36 ; middleware/rate_limit_shared.rs:192 ; health.rs:61 (the 7 non-handler web::Data extraction sites that must pull from AppState<C>)
    - crates/axiam-api-rest/src/handlers/password_reset.rs:162,292 ; email_verification.rs:67,102 ; federation.rs:565,667,1217,1322 (OIDC ×4), :849,905,961,1465,1558 (SAML ×5) — the 13 per-request service constructions to hoist (all confirmed to carry no per-request state; tenant_id is always a method arg)
    - crates/axiam-api-rest/Cargo.toml:55-60 (the saml feature gate — handlers only, not constructor deps)
  </read_first>
  <action>
    Create crates/axiam-api-rest/src/state.rs defining `pub struct AppState<C: surrealdb::Connection + Clone>`
    with one field per current main.rs registration (48 fields), using `Option<SurrealEmailConfigRepository<C>>`
    for the conditional email_config_repo (D-02), plus singleton service fields
    `password_reset_service`, `email_verification_service`, `oidc_federation_service`, and
    `saml_federation_service` (each constructed once from app-wide singletons). Construct the
    SamlFederationService field UNCONDITIONALLY (its deps do not need the saml feature) so no cfg attribute lands
    on AppState's field list. Derive/implement Clone. Declare `mod state;` and re-export AppState from
    crates/axiam-api-rest/src/lib.rs. In main.rs, build one AppState instance from the existing locals and register
    it with a single `.app_data(web::Data::new(app_state.clone()))`, removing the 48 individual `.app_data()`
    calls and the conditional email_config_repo match (fold the Option into the struct). Update the 7 non-handler
    sites to look up `web::Data<AppState<C>>` and clone the needed field (e.g. `state.session_validator.clone()`,
    `state.auth_config.clone()`, the DeviceAuthService, the `Surreal<C>` handle, the HealthChecker) — do not leave
    any of them extracting a now-unregistered individual type.
  </action>
  <acceptance_criteria>
    - `export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip && cargo build -p axiam-server` succeeds.
    - `grep -c "\.app_data(web::Data::new(" crates/axiam-server/src/main.rs` returns 1 (only the AppState registration remains).
    - `grep -rn "app_data::<web::Data<" crates/axiam-api-rest/src/extractors crates/axiam-api-rest/src/middleware crates/axiam-api-rest/src/health.rs` shows every remaining lookup is `web::Data<AppState` (no individual-type lookups left).
    - AppState<C> uses the `C: surrealdb::Connection + Clone` bound; email_config_repo is an Option<> field.
  </acceptance_criteria>
  <verify>
    <automated>export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip && cargo build -p axiam-server</automated>
  </verify>
  <done>AppState<C> defined and registered once; the 7 non-handler sites read from it; the 4 hoisted service fields exist and are constructed once (SAML unconditionally); server builds.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 28 handler files to web::Data<AppState<C>>; replace the 13 per-request service constructions</name>
  <files>crates/axiam-api-rest/src/handlers/ (all 28 handler files, incl. federation.rs, auth.rs, password_reset.rs, email_verification.rs, gdpr.rs, oauth2.rs)</files>
  <read_first>
    - crates/axiam-api-rest/src/handlers/ — the 28 handler files with 283 `web::Data<T>` extraction params (per RESEARCH per-file table: federation.rs 71, auth.rs 36, password_reset.rs 22, roles.rs 13, gdpr.rs 12, permissions.rs 8, groups.rs 8, email_verification.rs 8, webhooks.rs 7, resources.rs 7, pgp_keys.rs 7, organizations.rs 7, oauth2.rs 7, certificates.rs 7, webauthn.rs 6, users.rs 6, tenants.rs 6, service_accounts.rs 6, email_config.rs 6, scopes.rs 5, oauth2_clients.rs 5, notification_rules.rs 5, settings.rs 4, ca_certificates.rs 4, bootstrap.rs 3, authz_check.rs 3, mfa_methods.rs 2, audit.rs 2)
    - crates/axiam-api-rest/src/handlers/password_reset.rs:162,292 ; email_verification.rs:67,102 ; federation.rs (OIDC ×4 / SAML ×5 construction sites) — replace `XService::new(...)` with `state.<svc>.method(...)`
  </read_first>
  <action>
    For each handler function, replace each `dep: web::Data<SurrealXRepository<C>>` (and every other individual
    web::Data<T> param) with a single `state: web::Data<AppState<C>>` param, and rewrite body references from
    `dep.method(...)` to `state.<dep_field>.method(...)`. Preserve each handler's existing `<C: Connection ...>`
    generic parameter. Replace the 13 per-request `PasswordResetService::new(...)` / `EmailVerificationService::new(...)` /
    `OidcFederationService::new(...)` / `SamlFederationService::new(...)` call sites with calls on the hoisted
    singletons (`state.password_reset_service.method(...)`, etc.) — tenant_id continues to be passed to the method,
    never the constructor. Keep the `#[cfg(feature = "saml")]` gate on the SAML handler functions only. Use
    scripted bulk find-replace for the mechanical rewrites, then compile and resolve stragglers.
  </action>
  <acceptance_criteria>
    - `export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip && cargo build -p axiam-api-rest` succeeds (default features, saml on).
    - `grep -rn "Service::new(" crates/axiam-api-rest/src/handlers/password_reset.rs crates/axiam-api-rest/src/handlers/email_verification.rs crates/axiam-api-rest/src/handlers/federation.rs` returns 0 (all 13 per-request constructions removed).
    - No handler function still takes an individual `web::Data<SurrealXRepository<C>>` param (spot-check via grep for `web::Data<Surreal` in handlers/).
  </acceptance_criteria>
  <verify>
    <automated>export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip && cargo build -p axiam-api-rest</automated>
  </verify>
  <done>All 28 handler files extract web::Data<AppState<C>> with field access; the 13 per-request service constructions call hoisted singletons; axiam-api-rest builds under default features.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate ~35 test-harness builders to register one AppState<C>; green-gate the crate</name>
  <files>crates/axiam-api-rest/tests/ (32 harness files), crates/axiam-server/tests/ (req5_oidc_e2e.rs, req7_service_account_aud.rs, req7_session_lifecycle.rs)</files>
  <read_first>
    - crates/axiam-api-rest/tests/*.rs — 32 files each with one `macro_rules! test_app! { ... }` (or 2–4 variants in certificate_test.rs / device_auth_test.rs / user_test.rs) building an `App::new()...app_data(...)` chain; health_test.rs / middleware_test.rs build inline without a macro
    - crates/axiam-server/tests/req5_oidc_e2e.rs, req7_service_account_aud.rs, req7_session_lifecycle.rs (3 files, 8/13/17 app_data calls)
    - The new test files created in 29-01 (qual03_error_taxonomy_test.rs) and 29-02 (gdpr_test.rs additions) — migrate their harnesses too
  </read_first>
  <action>
    Update each test file's `test_app!` macro definition (or inline builder) to construct one AppState<C> from the
    test's locals and register it via a single `.app_data(web::Data::new(app_state.clone()))`, replacing the
    per-dependency `.app_data()` chain — editing the ~35 builder definitions, not the ~333 individual call sites.
    Build the SamlFederationService field unconditionally in the harness (matching main.rs). Include the new
    29-01/29-02 test harnesses in the sweep. Run the full axiam-api-rest and axiam-server test suites as the
    per-crate green gate proving no behavior change (D-06).
  </action>
  <acceptance_criteria>
    - `export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip && cargo test -p axiam-api-rest` passes (all migrated harnesses green).
    - `export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip && cargo test -p axiam-server` passes.
    - No test file registers an individual `web::Data<Surreal...Repository<C>>` outside AppState (spot-check grep in tests/).
  </acceptance_criteria>
  <verify>
    <automated>export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip && cargo test -p axiam-api-rest</automated>
  </verify>
  <done>All ~35 test-harness builders register one AppState<C>; axiam-api-rest and axiam-server test suites green — no behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| every authenticated request → extractors/middleware | AuthenticatedUser FromRequest, cert_auth, and the shared rate-limiter run on nearly every request; they read their dependency out of app-data — a dropped registration silently breaks auth extraction at runtime |
| conditional config (email encryption key) → email_config_repo | The Option<> field must preserve fail-closed behavior when the key is unset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-06 | Spoofing / Elevation of Privilege / DoS | 7 non-handler web::Data sites (auth extractor, cert_auth, rate-limiter, health) | high | mitigate | Update all 7 to read from AppState<C>; `.app_data::<T>()` returns Option so a missed site fails auth extraction at runtime (not compile) — locked by the full existing auth/rbac test suite (D-06) — flagged HIGH for execution attention |
| T-29-07 | Information Disclosure / EoP | email_config_repo Option<> field (fail-closed) | medium | mitigate | Model as Option<> per D-02; handlers get None when unconfigured, preserving the 28-04 fail-closed 500/unavailable behavior — proven by the existing email_config test suite |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages added or installed; composition-only refactor. Legitimacy gate N/A |
</threat_model>

<verification>
- `export SWAGGER_UI_DOWNLOAD_URL=... && cargo build -p axiam-server` succeeds (single AppState registration).
- `export SWAGGER_UI_DOWNLOAD_URL=... && cargo test -p axiam-api-rest` and `-p axiam-server` green — the primary no-behavior-change proof for QUAL-01/07 hoisting.
- `cargo clippy -p axiam-api-rest` clean (no new warnings; watch for unused-import churn from the migration).
- HIGH-severity T-29-06 mitigated: confirm all 7 non-handler sites read from AppState and the auth/rbac suites pass.
- Run `cargo clean` after this plan before the next (CLAUDE.md disk hygiene).
</verification>

<success_criteria>
main.rs composes and registers a single AppState<C> (48 registrations → 1); every handler and
the 7 non-handler sites extract from AppState with field access; email_config_repo is an Option<>
field preserving fail-closed behavior; the 13 per-request services are hoisted to singletons;
~35 test harnesses register one AppState<C> and the axiam-api-rest + axiam-server suites stay
green (no behavior change).
</success_criteria>

<output>
Create `.planning/phases/29-structural-quality/29-03-SUMMARY.md` when done.
</output>
