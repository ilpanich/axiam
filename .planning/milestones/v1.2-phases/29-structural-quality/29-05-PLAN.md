---
phase: 29-structural-quality
plan: 05
type: execute
wave: 5
depends_on: ["29-04"]
files_modified:
  - crates/axiam-db/src/repository/ca_certificate.rs
  - crates/axiam-db/src/repository/certificate.rs
  - crates/axiam-db/src/repository/email_config.rs
  - crates/axiam-db/src/repository/email_verification_token.rs
  - crates/axiam-db/src/repository/export_job.rs
  - crates/axiam-db/src/repository/federation_config.rs
  - crates/axiam-db/src/repository/federation_login_state.rs
  - crates/axiam-db/src/repository/federation_link.rs
  - crates/axiam-db/src/repository/notification_rule.rs
  - crates/axiam-db/src/repository/oauth2_auth_code.rs
  - crates/axiam-db/src/repository/oauth2_client.rs
  - crates/axiam-db/src/repository/oauth2_refresh_token.rs
  - crates/axiam-db/src/repository/password_reset_token.rs
  - crates/axiam-db/src/repository/pgp_key.rs
  - crates/axiam-db/src/repository/saml_replay.rs
  - crates/axiam-db/src/repository/webauthn_credential.rs
  - crates/axiam-db/src/repository/webhook.rs
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "All remaining (file-group-B) duplicated CountRow structs collapse to helpers::CountRow, completing the 24-total collapse (QUAL-02)"
    - "Group-B list repos adopt helpers::paginate<T> and route single-record reads through helpers::take_first_or_not_found, completing the 79-site adoption (QUAL-02)"
    - "The duplicate local parse_uuid in federation_link.rs:44 is deleted and its 7 call sites use helpers::parse_uuid(s, field) with the correct field names (QUAL-02/D-07)"
  artifacts:
    - "zero remaining `struct CountRow` definitions in crates/axiam-db/src/repository/*.rs (all 24 collapsed)"
    - "zero remaining local parse_uuid fn in federation_link.rs"
  key_links:
    - "federation_link.rs 7 call sites map to helpers::parse_uuid with field names: record_id (:65), tenant_id (:52,:68), user_id (:53,:69), federation_config_id (:54,:70)"
    - "helpers::CountRow / paginate<T> / take_first_or_not_found (from 29-04) are the single source every group-B repo converges on"
  prohibitions:
    - "No behavior change — pure mechanical dedup; the existing axiam-db repo test suite is the gate (D-03)"
    - "Do not modify the classify-marker detection logic inside saml_replay.rs / federation_login_state.rs — only collapse their local CountRow struct (their marker strings are load-bearing ground truth reused by QUAL-03)"
    - "Do not sweep inline Uuid::parse_str call sites in other repos beyond the federation_link.rs local parse_uuid removal (D-07 narrow scope)"
---

<objective>
QUAL-02 (part 2 of 2) — Finish the backend dedup across the remaining ~16 list repos and
remove the duplicate parse_uuid in federation_link.rs. Collapse the remaining local CountRow
structs to helpers::CountRow, adopt helpers::paginate<T>, and route single-record reads through
helpers::take_first_or_not_found, completing the exhaustive adoption (D-07). Behavior-preserving
(D-03) — the existing axiam-db repo suite is the gate.

Purpose: Complete the 24-CountRow collapse, the 79-site take_first_or_not_found adoption, and
the federation_link parse_uuid duplicate removal that CONTEXT/RESEARCH scoped.

Output: All group-B repos deduped; federation_link.rs parse_uuid removed with 7 call sites fixed;
axiam-db suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structural-quality/29-CONTEXT.md
@.planning/phases/29-structural-quality/29-RESEARCH.md
@.planning/phases/29-structural-quality/29-PATTERNS.md
@.planning/phases/29-structural-quality/29-VALIDATION.md

# Canonical helpers (CountRow / paginate<T> / take_first_or_not_found / parse_uuid)
@crates/axiam-db/src/helpers.rs
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Location |
|----------|------|----------|
| all 24 CountRow duplicates collapsed | dedup completion | `crates/axiam-db/src/repository/*.rs` |
| federation_link parse_uuid removed | dedup | `crates/axiam-db/src/repository/federation_link.rs` |

<tasks>

<task type="auto">
  <name>Task 1: Dedup file-group B repos — CountRow, paginate<T>, take_first_or_not_found</name>
  <files>crates/axiam-db/src/repository/ca_certificate.rs, certificate.rs, email_config.rs, email_verification_token.rs, export_job.rs, federation_config.rs, federation_login_state.rs, notification_rule.rs, oauth2_auth_code.rs, oauth2_client.rs, oauth2_refresh_token.rs, password_reset_token.rs, pgp_key.rs, saml_replay.rs, webauthn_credential.rs, webhook.rs (all under crates/axiam-db/src/repository/)</files>
  <read_first>
    - crates/axiam-db/src/repository/user.rs and role.rs (already-migrated exemplars — `use crate::helpers::{CountRow, parse_uuid};` import shape)
    - crates/axiam-db/src/helpers.rs (CountRow, paginate<T> from 29-04, take_first_or_not_found)
    - Group-B repo files: ca_certificate.rs, certificate.rs, email_config.rs, email_verification_token.rs, export_job.rs, federation_config.rs, federation_login_state.rs, notification_rule.rs, oauth2_auth_code.rs, oauth2_client.rs, oauth2_refresh_token.rs, password_reset_token.rs, pgp_key.rs, saml_replay.rs, webauthn_credential.rs, webhook.rs (their local `struct CountRow`, their `let total = ...; PaginatedResult { ... }` tails, and their inline `.into_iter().next().ok_or_else(|| DbError::NotFound { ... })?` reads)
    - crates/axiam-db/src/repository/webhook.rs:280-319 (the count+total+PaginatedResult tail paginate<T> replaces — a clean group-B exemplar)
  </read_first>
  <action>
    In each group-B repo file, apply the same three behavior-preserving substitutions as 29-04 Task 2, all in one
    pass per file: delete the local `struct CountRow` and import `crate::helpers::CountRow`; replace the
    `let total = ...; PaginatedResult { ... }` list tail with `helpers::paginate(items, count_rows, &pagination)`;
    replace inline `.into_iter().next().ok_or_else(|| DbError::NotFound { ... })?` single-record reads with
    `helpers::take_first_or_not_found(items, "<entity>", &<id>)?`. In saml_replay.rs and federation_login_state.rs,
    collapse ONLY the local CountRow struct — leave their marker-string detection logic and their existing
    BEGIN/COMMIT transaction untouched. Do not alter any query SQL, WHERE clause, or method signature.
  </action>
  <acceptance_criteria>
    - `cargo test -p axiam-db` passes (group-B repos, no behavior change).
    - `grep -rln "struct CountRow" crates/axiam-db/src/repository/` returns 0 (all 24 duplicates collapsed across 29-04 + 29-05; only helpers::CountRow remains).
    - Group-B list methods construct results via `helpers::paginate(...)`; single-record reads use `helpers::take_first_or_not_found(...)`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p axiam-db</automated>
  </verify>
  <done>All remaining CountRow duplicates collapsed; group-B repos adopt paginate<T> and take_first_or_not_found; axiam-db suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove federation_link.rs duplicate parse_uuid; fix its 7 call sites</name>
  <files>crates/axiam-db/src/repository/federation_link.rs</files>
  <read_first>
    - crates/axiam-db/src/repository/federation_link.rs:44 (the local `fn parse_uuid(s: &str) -> Result<Uuid, DbError>` — different 1-arg signature than helpers::parse_uuid(s, field)) and its 7 call sites: record_id (:65), tenant_id (:52, :68), user_id (:53, :69), federation_config_id (:54, :70)
    - crates/axiam-db/src/helpers.rs:33 (helpers::parse_uuid(s: &str, field: &str))
  </read_first>
  <action>
    Delete the local `parse_uuid` fn in federation_link.rs and import `crate::helpers::parse_uuid` (or call it
    fully-qualified). Update the 7 call sites to pass the correct field-name string per the mapping above
    (record_id, tenant_id, user_id, federation_config_id). Apply the same CountRow/paginate/take_first dedup to
    federation_link.rs if it carries those patterns. Run the full axiam-db test suite as the group-B green gate.
  </action>
  <acceptance_criteria>
    - `grep -n "fn parse_uuid" crates/axiam-db/src/repository/federation_link.rs` returns 0 (local duplicate removed).
    - All 7 former call sites now call `helpers::parse_uuid(s, "<field>")` with the correct field names.
    - `cargo test -p axiam-db` passes (full suite, no behavior change).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p axiam-db</automated>
  </verify>
  <done>federation_link.rs local parse_uuid deleted and its 7 call sites route through helpers::parse_uuid with correct field names; axiam-db suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure internal repository-layer dedup; no trust boundary is added, moved, or crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-10 | Tampering (silent behavior drift) | mechanical CountRow/paginate/take_first/parse_uuid dedup | low | mitigate | Behavior-preserving substitution only; the existing axiam-db repo test suite is the regression gate (D-03); marker-detection logic in saml_replay.rs/federation_login_state.rs left untouched |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages added or installed; internal dedup only. Legitimacy gate N/A |
</threat_model>

<verification>
- `cargo test -p axiam-db` green (group-B dedup + federation_link fix, no behavior change).
- `grep -rln "struct CountRow" crates/axiam-db/src/repository/` returns 0 (24 collapsed).
- `grep -n "fn parse_uuid" crates/axiam-db/src/repository/federation_link.rs` returns 0.
- `cargo clippy -p axiam-db` clean (watch for unused-import churn).
- Run `cargo clean` after this plan before the next (CLAUDE.md disk hygiene).
</verification>

<success_criteria>
All 24 duplicated CountRow structs collapsed to helpers::CountRow; group-B repos adopt
paginate<T> and take_first_or_not_found; federation_link.rs local parse_uuid removed with 7
call sites fixed; axiam-db suite green (no behavior change).
</success_criteria>

<output>
Create `.planning/phases/29-structural-quality/29-05-SUMMARY.md` when done.
</output>
