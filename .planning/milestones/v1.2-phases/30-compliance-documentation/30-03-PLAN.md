---
phase: 30-compliance-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api/asyncapi.yml
  - docs/api/openapi.json
  - docs/api/grpc.md
  - docs/api/README.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "docs/api/asyncapi.yml is a valid AsyncAPI 2.6 document describing every AMQP queue/message from connection.rs + messages.rs, transcribed field-for-field (DOCS-01, D-07)"
    - "docs/api/openapi.json publishes the REST spec as the SAME drift-gated artifact as sdks/openapi.json (symlink, zero duplication — D-09/D-10)"
    - "docs/api/grpc.md references proto/axiam/v1/{authorization,token,user}.proto with a short usage guide (D-07)"
    - "docs/api/README.md overviews REST/gRPC/AMQP and documents how to view each spec (no new in-app Swagger UI — D-10)"
  artifacts:
    - "docs/api/asyncapi.yml"
    - "docs/api/openapi.json"
    - "docs/api/grpc.md"
    - "docs/api/README.md"
  key_links:
    - "asyncapi.yml payload schemas transcribe messages.rs structs (AuthzRequest/AuthzResponse/AuditEventMessage/NotificationEvent/OutboundMailMessage/WebhookMessage); channels use connection.rs queue-name constants incl. DLQ + retry chain"
    - "docs/api/openapi.json → ../../sdks/openapi.json (single source of truth, drift-gated by sdk-openapi-drift.yml)"
---

<objective>
Consolidate the API contract docs under `docs/api/` (DOCS-01, D-07). Three
tracks: (1) hand-author the net-new AsyncAPI 2.6 spec for the AMQP surface — the
one genuinely novel artifact with no repo analog; (2) publish the REST OpenAPI
spec as a symlink to the existing drift-gated `sdks/openapi.json` (no duplication,
D-09/D-10) and verify it is current via the existing `--dump-openapi` mechanism;
(3) write a short gRPC usage guide referencing the existing `.proto` files, plus
a `docs/api/README.md` overview of all three protocols and how to view each spec.

Purpose: A single API-docs home covering REST + gRPC + AMQP for integrators.
Output: `docs/api/asyncapi.yml`, `docs/api/openapi.json`, `docs/api/grpc.md`,
`docs/api/README.md`.

Do NOT wire in-app Swagger UI (D-10 — avoids the utoipa-swagger-ui GitHub-egress
build fragility). Do NOT reimplement the OpenAPI generator (Don't-Hand-Roll).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-compliance-documentation/30-CONTEXT.md
@.planning/phases/30-compliance-documentation/30-RESEARCH.md
@.planning/phases/30-compliance-documentation/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-author docs/api/asyncapi.yml (net-new AsyncAPI 2.6) transcribing the AMQP messages + queues field-for-field</name>
  <files>docs/api/asyncapi.yml</files>
  <read_first>
    - crates/axiam-amqp/src/messages.rs (~L1-221) — the source-of-truth structs to transcribe field-for-field into AsyncAPI payload schemas: AuthzRequest (L125-146, incl. hmac_signature + key_version), AuthzResponse (L148-153), AuditEventMessage (L159-174, incl. hmac_signature), NotificationEvent (L187-196), WebhookMessage (L214-221); plus the module doc comment (L1-13) noting OutboundMailMessage/MailType are re-exported from axiam_core::models::mail
    - crates/axiam-core/src/models/mail.rs — OutboundMailMessage / MailType enum shape (PasswordReset/EmailVerification/Notification/DeletionCancel/ExportReady) for the axiam.mail.outbound channel
    - crates/axiam-amqp/src/connection.rs (L12-46, pub mod queues) — the queue-name constants: AUTHZ_REQUEST/RESPONSE(+DLQ), AUDIT_EVENTS(+DLQ), NOTIFICATIONS, MAIL_OUTBOUND(+DLQ), WEBHOOK/WEBHOOK_RETRY/WEBHOOK_DLQ
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "AsyncAPI 2.x Spec Skeleton" (the concrete channels + components.messages YAML template, the queue→message→direction→DLQ table, and the HMAC/fail-closed-webhook description notes) and § "Common Pitfalls" (Pitfall 3: hand-authored spec is a snapshot, note it)
    - .planning/phases/30-compliance-documentation/30-PATTERNS.md § "docs/api/asyncapi.yml" (exact struct field lists + the info title/version convention mirroring openapi.rs)
  </read_first>
  <action>
    Create docs/api/asyncapi.yml as a self-contained AsyncAPI 2.6.0 document
    following the RESEARCH skeleton. info block: title "AXIAM AMQP API", version
    "0.1.0" matching the REST spec, and a description noting it is hand-authored
    from crates/axiam-amqp (v1.2 beta) per D-12. servers: one amqp server. channels:
    one channel per queue constant in connection.rs (axiam.authz.request /
    axiam.authz.response / axiam.audit.events / axiam.notifications /
    axiam.mail.outbound / axiam.webhook / axiam.webhook.retry plus the three .dlq
    queues), each with a bindings.amqp queue binding (name, durable true,
    autoDelete false) and a subscribe or publish message $ref per the direction
    column in the RESEARCH table. components.messages: one message per struct with a
    payload JSON-Schema object transcribing every field from messages.rs
    field-for-field (uuid formats, nullable Option fields, integer key_version).
    Put the HMAC-SHA256 signing convention (hmac_signature + key_version, fail-closed
    nack-no-requeue on absent/invalid) and the fail-closed webhook DLQ/retry chain
    (WEBHOOK → nack-no-requeue on max attempts → WEBHOOK_DLQ; WEBHOOK_RETRY is
    TTL-based with no live consumer) into the relevant message/channel description
    fields, since AsyncAPI 2.6 has no first-class HMAC concept. Keep all $ref
    internal (no external URLs) so validation stays offline.
  </action>
  <acceptance_criteria>
    - `docs/api/asyncapi.yml` exists and declares AsyncAPI 2.6: `grep -Eq '^asyncapi: *"?2\.6' docs/api/asyncapi.yml` exits 0.
    - It is valid YAML and parses: `python3 -c "import yaml; yaml.safe_load(open('docs/api/asyncapi.yml'))"` exits 0.
    - Every queue is represented: `grep -q 'axiam.authz.request' docs/api/asyncapi.yml && grep -q 'axiam.webhook.retry' docs/api/asyncapi.yml && grep -q 'axiam.mail.outbound' docs/api/asyncapi.yml` exits 0.
    - Each of the message structs is present as a component: `grep -q 'AuthzRequest' docs/api/asyncapi.yml && grep -q 'AuditEventMessage' docs/api/asyncapi.yml && grep -q 'WebhookMessage' docs/api/asyncapi.yml` exits 0.
    - The HMAC/fail-closed convention is documented in a description field (hmac_signature named).
    - All $ref targets are internal (no http/https $ref).
    (Full AsyncAPI meta-schema validation via @asyncapi/cli runs in CI — 30-06 — after the package-legitimacy checkpoint.)
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('docs/api/asyncapi.yml'))" && grep -Eq '^asyncapi: *"?2\.6' docs/api/asyncapi.yml && grep -q 'axiam.authz.request' docs/api/asyncapi.yml && grep -q 'axiam.webhook.retry' docs/api/asyncapi.yml && grep -q 'AuthzRequest' docs/api/asyncapi.yml && grep -q 'hmac_signature' docs/api/asyncapi.yml</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Publish docs/api/openapi.json as a symlink to sdks/openapi.json + verify it is current via --dump-openapi</name>
  <files>docs/api/openapi.json</files>
  <read_first>
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "OpenAPI Publishing Mechanism" (recommends option 1: symlink docs/api/openapi.json → ../../sdks/openapi.json; the --dump-openapi mechanism + regenerate command)
    - crates/axiam-server/src/main.rs (L130-144) — the existing --dump-openapi flag (serializes axiam_api_rest::openapi::api_doc(), exits 0, before any infra init)
    - .github/workflows/sdk-openapi-drift.yml — the existing drift gate that keeps sdks/openapi.json current (build --no-default-features, --dump-openapi, diff)
    - CLAUDE.md § "Build & Disk Hygiene" (SWAGGER_UI_DOWNLOAD_URL export + narrow cargo scope)
  </read_first>
  <action>
    Create docs/api/openapi.json as a relative symlink to ../../sdks/openapi.json
    (zero duplication, the existing sdk-openapi-drift.yml remains the single source
    of truth — D-09/D-10). Before/while doing so, confirm sdks/openapi.json is
    current: export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip,
    build axiam-server --no-default-features, run --dump-openapi to a temp file, and
    diff it against the committed sdks/openapi.json — if it drifts, regenerate
    sdks/openapi.json (matching the drift-gate's exact feature set) so the symlink
    target is accurate. Do not add a second committed copy. Do not wire Swagger UI.
  </action>
  <acceptance_criteria>
    - `docs/api/openapi.json` exists and resolves to valid JSON through the symlink: `jq empty docs/api/openapi.json` exits 0.
    - It is a symlink (or otherwise byte-identical to sdks/openapi.json): `test -L docs/api/openapi.json` exits 0.
    - sdks/openapi.json is current: a fresh `--dump-openapi` (SAML off) diffs clean against it (matches the drift gate's expectation).
  </acceptance_criteria>
  <verify>
    <automated>jq empty docs/api/openapi.json && test -L docs/api/openapi.json</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Author docs/api/grpc.md (proto usage guide) + docs/api/README.md (REST/gRPC/AMQP overview + how to view each spec)</name>
  <files>docs/api/grpc.md, docs/api/README.md</files>
  <read_first>
    - proto/axiam/v1/authorization.proto, proto/axiam/v1/token.proto, proto/axiam/v1/user.proto — the gRPC service/message definitions to reference by path (do not copy the whole proto; summarize services + point to the files)
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "OpenAPI Publishing Mechanism" (how to view the committed openapi.json with an external Swagger/Redoc viewer — a documentation instruction, not new app code) and § "Common Pitfalls" (Pitfall 3: note asyncapi.yml is a manual snapshot in the README)
    - .planning/phases/30-compliance-documentation/30-PATTERNS.md § "docs/README.md ... (doc, transform/index)" (the sdks/rust/README.md H1/H2 heading + bold-label-bullet convention to imitate) and § "Shared Patterns" (D-12 version-stamp convention)
    - docs/api/asyncapi.yml + docs/api/openapi.json (the sibling artifacts the README links to)
  </read_first>
  <action>
    Create docs/api/grpc.md: a short usage guide that names the three
    proto/axiam/v1/*.proto files, summarizes their services (authorization, token,
    user), and explains how to consume them (buf/tonic codegen already used by the
    SDKs) — reference the files by relative path, do not transcribe them. Create
    docs/api/README.md as the docs/api landing page following the SDK-README H1/H2
    convention: an overview of the three API protocols, then a "how to view each
    spec" section — REST: point to ./openapi.json and note it can be opened in any
    external Swagger Editor / Redoc viewer (no in-app Swagger UI, D-10); AMQP: point
    to ./asyncapi.yml and note it is a HAND-AUTHORED SNAPSHOT of
    crates/axiam-amqp/src/messages.rs that must be re-verified when messages.rs
    changes (Pitfall 3); gRPC: point to ./grpc.md. Stamp both docs v1.2 Beta +
    last-verified date (D-12). Use relative markdown links throughout.
  </action>
  <acceptance_criteria>
    - `test -f docs/api/grpc.md && test -f docs/api/README.md`.
    - grpc.md references all three proto files: `grep -q 'authorization.proto' docs/api/grpc.md && grep -q 'token.proto' docs/api/grpc.md && grep -q 'user.proto' docs/api/grpc.md` exits 0.
    - README links to all three specs: `grep -q 'openapi.json' docs/api/README.md && grep -q 'asyncapi.yml' docs/api/README.md && grep -q 'grpc.md' docs/api/README.md` exits 0.
    - README notes asyncapi.yml is a manual snapshot (Pitfall 3) and both docs carry the v1.2 stamp: `grep -Eq 'v1\.2' docs/api/README.md` exits 0.
    - No in-app Swagger UI route is added (viewing is via external viewer only).
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/api/grpc.md && test -f docs/api/README.md && grep -q 'authorization.proto' docs/api/grpc.md && grep -q 'asyncapi.yml' docs/api/README.md && grep -q 'openapi.json' docs/api/README.md && grep -Eq 'v1\.2' docs/api/README.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published API spec ← internal source contracts | asyncapi.yml is hand-authored and can drift from messages.rs; openapi.json must stay identical to the drift-gated source |
| integrator ← published specs | Specs describe the API surface consumers integrate against; over-exposure or wrong contracts mislead integrators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-07 | Tampering (AsyncAPI spec drifts from messages.rs) | docs/api/asyncapi.yml | medium | mitigate | Field-for-field transcription from messages.rs L125-221 + connection.rs L12-46; a "hand-authored snapshot, re-verify on messages.rs change" note in docs/api/README.md (Pitfall 3); AsyncAPI meta-schema validate in docs-ci (30-06) |
| T-30-08 | Tampering (OpenAPI doc copy drifts from source) | docs/api/openapi.json | medium | mitigate | Symlink to sdks/openapi.json — no second copy; existing sdk-openapi-drift.yml remains the single drift gate; --dump-openapi diff confirms currency |
| T-30-09 | Information disclosure (spec exposes internal-only endpoints) | openapi.json / asyncapi.yml | low | accept | openapi.json is the already-public, drift-gated SDK artifact; AMQP contracts are the same ones SDKs already implement — no new surface disclosed |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('docs/api/asyncapi.yml'))"` — AsyncAPI parses (full meta-schema validate in 30-06 CI).
- `jq empty docs/api/openapi.json && test -L docs/api/openapi.json` — OpenAPI published as a valid-JSON symlink to the drift-gated source.
- grpc.md references the three .proto files; README overviews REST/gRPC/AMQP + how-to-view; both stamped v1.2 (D-12).
- Internal links validated later by scripts/check-doc-links.sh (30-05) + docs-ci.yml (30-06).
- Run `cargo clean` in the gap after this plan's --dump-openapi build (disk hygiene).
</verification>

<success_criteria>
DOCS-01 API portion satisfied: docs/api/ holds a valid net-new AsyncAPI 2.6 spec covering every AMQP queue/message, the REST OpenAPI spec published as a drift-gated symlink, a gRPC proto usage guide, and a README overviewing all three with view instructions — no in-app Swagger UI, no duplicated OpenAPI copy.
</success_criteria>

<output>
Create `.planning/phases/30-compliance-documentation/30-03-SUMMARY.md` when done.
</output>
