---
phase: 30-compliance-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/deployment/README.md
  - docs/admin/README.md
  - docs/pki/README.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "docs/deployment/README.md documents Docker/K8s deployment for operators: the required AXIAM__* env/secret KEY names and the NetworkPolicies (DOCS-01, D-08)"
    - "docs/admin/README.md documents admin bootstrap + user/role/permission management, task-oriented (D-08)"
    - "docs/pki/README.md documents CA cert issuance, leaf cert issuance, mTLS device binding, and revocation, task-oriented (D-08)"
  artifacts:
    - "docs/deployment/README.md"
    - "docs/admin/README.md"
    - "docs/pki/README.md"
  key_links:
    - "deployment guide transcribes the required secret KEY names from k8s/server/secret.yml and the NetworkPolicies from k8s/network-policy/* — never real secret values"
    - "pki guide maps to certificates.rs (generate/list/get/revoke/bind) + ca_certificates.rs (generate/list/get/revoke) handler operations"
---

<objective>
Author the three operator/integrator, task-oriented guides under `docs/`
(DOCS-01, D-08): a deployment guide (Docker/K8s, required env/secrets,
NetworkPolicies), an admin guide (bootstrap + user/role/permission management),
and a PKI/certificate guide (CA + leaf cert issuance, mTLS binding, revocation).
All three transcribe from existing infra-as-code and handlers — no new infra, no
new code.

Purpose: Practical getting-it-running documentation for operators and admins.
Output: `docs/deployment/README.md`, `docs/admin/README.md`, `docs/pki/README.md`.

Depth is "practical getting-it-running", not exhaustive reference or bare
quickstart (D-08). Reference secret KEY NAMES only — never commit or example real
secret values.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-compliance-documentation/30-CONTEXT.md
@.planning/phases/30-compliance-documentation/30-RESEARCH.md
@.planning/phases/30-compliance-documentation/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/deployment/README.md (Docker/K8s, required env/secret keys, NetworkPolicies)</name>
  <files>docs/deployment/README.md</files>
  <read_first>
    - k8s/server/secret.yml — the canonical required-env-var list, all AXIAM__ double-underscore keys (AXIAM__DB__USERNAME/PASSWORD, AXIAM__AUTH__JWT_PRIVATE_KEY_PEM/JWT_PUBLIC_KEY_PEM, AXIAM__AUTH__MFA_ENCRYPTION_KEY, AXIAM__PKI__ENCRYPTION_KEY, AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY, AXIAM__EMAIL_ENCRYPTION_KEY, AXIAM__GDPR_PSEUDONYM_PEPPER, AXIAM__AUTH__PEPPER) — transcribe the KEY NAMES, never any value
    - k8s/network-policy/ — the 6 policy files (default-deny.yml, allow-dns-egress.yml, allow-ingress-to-{frontend,rabbitmq,server,surrealdb}.yml, server-egress.yml) — the network posture to describe
    - docker/docker-compose.prod.yml + the docker/.secrets/ convention referenced in its comments — the dev-vs-prod secret-sourcing pattern
    - k8s/kustomization.yml, k8s/namespace.yml, k8s/ingress.yml — overall manifest structure to reference
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "Deployment guide sources (confirmed present)" and § "Recommended docs/ Structure"
    - .planning/phases/30-compliance-documentation/30-PATTERNS.md § "docs/README.md ... (doc, transform/index)" (SDK-README H1/H2 convention) and § "Shared Patterns" (D-12 version-stamp)
  </read_first>
  <action>
    Create docs/deployment/README.md following the SDK-README H1/H2 heading
    convention and stamped v1.2 Beta + last-verified date (D-12). Sections: a
    Docker (compose) path referencing docker/docker-compose.prod.yml and the
    docker/.secrets/ sourcing convention; a Kubernetes path referencing
    k8s/kustomization.yml / namespace / ingress; a "Required secrets & environment"
    table transcribing the AXIAM__* KEY NAMES from k8s/server/secret.yml with a
    one-line purpose each (JWT keypair, MFA/PKI/federation/email encryption keys,
    GDPR pseudonym pepper, auth pepper, DB creds) — key names only, using obvious
    placeholders like <set-in-secret-manager> for values; and a "Network policies"
    section summarizing the default-deny posture and the allow-ingress/egress rules
    from k8s/network-policy/. Reference the manifest files by relative path; do not
    duplicate their full YAML. No real secret material anywhere.
  </action>
  <acceptance_criteria>
    - `test -f docs/deployment/README.md`, stamped: `grep -Eq 'v1\.2' docs/deployment/README.md` exits 0.
    - Required secret KEY names are present: `grep -q 'AXIAM__AUTH__JWT_PRIVATE_KEY_PEM' docs/deployment/README.md && grep -q 'AXIAM__GDPR_PSEUDONYM_PEPPER' docs/deployment/README.md` exits 0.
    - NetworkPolicies are documented: `grep -Eq 'default-deny|NetworkPolic' docs/deployment/README.md` exits 0.
    - No real secret VALUE is committed (values are placeholders like <set-in-secret-manager>); a PEM body or base64 key blob does not appear.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/deployment/README.md && grep -Eq 'v1\.2' docs/deployment/README.md && grep -q 'AXIAM__AUTH__JWT_PRIVATE_KEY_PEM' docs/deployment/README.md && grep -q 'AXIAM__GDPR_PSEUDONYM_PEPPER' docs/deployment/README.md && grep -Eq 'default-deny|NetworkPolic' docs/deployment/README.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Author docs/admin/README.md (bootstrap + user/role/permission management, task-oriented)</name>
  <files>docs/admin/README.md</files>
  <read_first>
    - crates/axiam-api-rest/src/handlers/organizations.rs — the org/tenant/user management operations the walkthrough describes
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "Admin + PKI guide sources (confirmed present)" (AXIAM_BOOTSTRAP_ADMIN_EMAIL bootstrap pattern from Phase 3) and § "Recommended docs/ Structure"
    - .planning/phases/30-compliance-documentation/30-PATTERNS.md § "docs/README.md ... (doc, transform/index)" (heading/tone convention) and § "Shared Patterns" (D-12 version-stamp)
    - docs/deployment/README.md (sibling doc — keep tone/heading consistent; cross-link bootstrap env to the deployment secrets section)
  </read_first>
  <action>
    Create docs/admin/README.md, task-oriented, stamped v1.2 Beta + last-verified
    date (D-12), following the shared heading convention. Cover: first-run admin
    bootstrap via the AXIAM_BOOTSTRAP_ADMIN_EMAIL pattern (cross-link to the
    deployment guide's env/secrets section); then walkthroughs for the common admin
    tasks — creating an organization/tenant, creating users, defining roles and
    permissions, and assigning roles (referencing the organizations.rs handler
    operations and the RBAC model). Frame each as a task ("To create a tenant, …")
    rather than an endpoint reference dump. Use relative links to related docs.
  </action>
  <acceptance_criteria>
    - `test -f docs/admin/README.md`, stamped: `grep -Eq 'v1\.2' docs/admin/README.md` exits 0.
    - Bootstrap is documented: `grep -q 'AXIAM_BOOTSTRAP_ADMIN_EMAIL' docs/admin/README.md` exits 0.
    - The guide is task-oriented and covers role/permission management (roles + permissions named).
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/admin/README.md && grep -Eq 'v1\.2' docs/admin/README.md && grep -q 'AXIAM_BOOTSTRAP_ADMIN_EMAIL' docs/admin/README.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Author docs/pki/README.md (CA + leaf cert issuance, mTLS binding, revocation, task-oriented)</name>
  <files>docs/pki/README.md</files>
  <read_first>
    - crates/axiam-api-rest/src/handlers/certificates.rs — leaf cert lifecycle operations: generate / list / get / revoke / bind (device binding / mTLS)
    - crates/axiam-api-rest/src/handlers/ca_certificates.rs — CA cert lifecycle: generate / list / get / revoke
    - crates/axiam-pki/ — the PKI crate the handlers call (skim module layout for the CA-signs-leaf + key-returned-once model)
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "Admin + PKI guide sources (confirmed present)" and § "Recommended docs/ Structure"
    - CLAUDE.md § "Security Standards" (X.509 RSA-4096/Ed25519, private keys never stored — returned once; CA private keys AES-256-GCM at rest)
    - .planning/phases/30-compliance-documentation/30-PATTERNS.md § "Shared Patterns" (D-12 version-stamp)
  </read_first>
  <action>
    Create docs/pki/README.md, task-oriented, stamped v1.2 Beta + last-verified
    date (D-12). Cover the certificate lifecycle as operator tasks: issue an
    organization CA certificate (ca_certificates.rs generate), issue a leaf
    certificate for a user/service/IoT device (certificates.rs generate), bind a
    certificate for mTLS device authentication (certificates.rs bind), and revoke a
    CA or leaf certificate (revoke on both handlers). Note the security model per
    CLAUDE.md: X.509 with RSA-4096 or Ed25519, private keys are returned ONCE and
    never stored server-side, CA signing keys are AES-256-GCM encrypted at rest.
    Reference the handler operations by name; do not transcribe handler code.
  </action>
  <acceptance_criteria>
    - `test -f docs/pki/README.md`, stamped: `grep -Eq 'v1\.2' docs/pki/README.md` exits 0.
    - The cert lifecycle tasks are documented: `grep -Eiq 'issue|generate' docs/pki/README.md && grep -Eiq 'revoke|revocation' docs/pki/README.md && grep -Eiq 'mTLS|bind' docs/pki/README.md` exits 0.
    - The "private key returned once / never stored" model is stated.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/pki/README.md && grep -Eq 'v1\.2' docs/pki/README.md && grep -Eiq 'revoke|revocation' docs/pki/README.md && grep -Eiq 'mTLS|bind' docs/pki/README.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs ← real secret material | The deployment guide references secrets; committing a real value would leak it |
| operator ← guide instructions | Guides drive real deployments; documenting an insecure step would weaken the deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-10 | Information disclosure (deployment guide leaks real secrets/keys) | docs/deployment/README.md | high | mitigate | Transcribe only the AXIAM__* KEY NAMES from k8s/server/secret.yml; use obvious placeholder values (<set-in-secret-manager>); no PEM bodies, no base64 key blobs; verify no real value is committed |
| T-30-11 | Tampering (guide documents an insecure default) | docs/admin/README.md, docs/pki/README.md | low | accept | Guides document the existing hardened defaults (default-deny RBAC, mTLS, keys-returned-once); no new config or endpoint introduced |
| T-30-12 | Elevation (admin guide over-discloses privileged bootstrap) | docs/admin/README.md | low | accept | Bootstrap is via a single env-driven admin email, already the shipped mechanism; guide references it without weakening it |
</threat_model>

<verification>
- All three guides exist, stamped v1.2/beta + last-verified (D-12).
- Deployment: required AXIAM__* key names + NetworkPolicies documented; no real secret values.
- Admin: AXIAM_BOOTSTRAP_ADMIN_EMAIL bootstrap + task-oriented role/permission management.
- PKI: CA/leaf issuance + mTLS binding + revocation + keys-returned-once model.
- Internal links validated later by scripts/check-doc-links.sh (30-05) + docs-ci.yml (30-06).
</verification>

<success_criteria>
DOCS-01 operator-guide portion satisfied: docs/deployment, docs/admin, docs/pki each hold a practical task-oriented guide transcribed from existing infra-as-code / handlers, with the deployment guide's required-secrets table listing KEY NAMES only (no real values) and the NetworkPolicies documented.
</success_criteria>

<output>
Create `.planning/phases/30-compliance-documentation/30-04-SUMMARY.md` when done.
</output>
