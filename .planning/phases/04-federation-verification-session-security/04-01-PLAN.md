---
phase: 04-federation-verification-session-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-auth/src/crypto.rs
  - crates/axiam-auth/src/totp.rs
  - crates/axiam-auth/src/lib.rs
  - crates/axiam-auth/src/config.rs
  - crates/axiam-auth/src/token.rs
  - crates/axiam-auth/src/service.rs
  - crates/axiam-server/src/main.rs
  - crates/axiam-db/src/schema.rs
  - crates/axiam-core/src/models/federation.rs
autonomous: true
requirements: [REQ-5, REQ-7]

must_haves:
  truths:
    - "Federation encryption key loads from AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY at startup"
    - "Access tokens issued by user-login flow carry aud=axiam:user"
    - "Access tokens issued by client-credentials flow carry aud=axiam:m2m"
    - "JWT jti equals the session.id of the issuing session (no longer a random UUID)"
    - "decode_access_token accepts both audiences and tolerates missing aud during back-compat window"
    - "federation_config table has columns allowed_algorithms, idp_signing_cert_pem, client_secret_ciphertext, client_secret_nonce, client_secret_key_version"
    - "AES-256-GCM helpers live in axiam-auth::crypto and are reused by TOTP"
    - "axiam-auth::crypto exposes a split-output variant (encrypt_separate / decrypt_separate) that returns nonce_b64 and ciphertext_b64 separately for D-11 federation storage"
  artifacts:
    - path: "crates/axiam-auth/src/crypto.rs"
      provides: "Domain-neutral AES-256-GCM helpers (bundled + split variants)"
      exports: ["aes256gcm_encrypt", "aes256gcm_decrypt", "encrypt_separate", "decrypt_separate"]
    - path: "crates/axiam-auth/src/config.rs"
      provides: "AuthConfig with federation_encryption_key and allow_missing_aud_as_user fields"
    - path: "crates/axiam-auth/src/token.rs"
      provides: "AccessTokenClaims with aud field; jti=session_id at issuance"
    - path: "crates/axiam-db/src/schema.rs"
      provides: "federation_config schema extended with 5 new columns"
  key_links:
    - from: "crates/axiam-auth/src/service.rs"
      to: "crates/axiam-auth/src/token.rs"
      via: "issue_access_token call sites set jti = session.id and aud = axiam:user"
      pattern: "issue_access_token.*session\\.id"
    - from: "crates/axiam-server/src/main.rs"
      to: "crates/axiam-auth/src/config.rs"
      via: "env var loader populates AuthConfig.federation_encryption_key"
      pattern: "AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY"
---

<objective>
Foundation work shared by every other Phase 4 plan: extract AES-256-GCM helpers to a domain-neutral module (exposing BOTH the legacy bundled format AND a split-output variant required by D-11 federation storage), add the federation encryption key + aud-backcompat config knobs, change `jti` to equal `session.id` (unblocks D-15), add the `aud` claim to user + M2M token issuance + decode, and migrate the SurrealDB schema for federation_config (5 new columns) plus the two new tables (`saml_assertion_replay`, `federation_login_state`). Every later plan depends on this one.

Purpose: Remove the structural blockers identified in 04-RESEARCH.md (jti != session_id; no shared crypto helper; no aud claim; schema doesn't yet hold encrypted columns or replay/state tables) before any verifier or password-change handler is written.
Output: Compiling crates with new exports, schema migration in place, all Phase 4 wave-2 plans able to compile against the new shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-federation-verification-session-security/04-CONTEXT.md
@.planning/phases/04-federation-verification-session-security/04-RESEARCH.md
@.planning/phases/01-cookie-based-authentication/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
Key existing types and call sites — DO NOT re-discover; use directly.

From `crates/axiam-auth/src/totp.rs:15-47`:
- `pub fn encrypt_secret(key: &[u8; 32], plaintext: &[u8]) -> Result<String, AuthError>` — base64 of `nonce(12) || ciphertext || tag`.
- `pub fn decrypt_secret(key: &[u8; 32], encoded: &str) -> Result<Vec<u8>, AuthError>`.
- Uses `aes_gcm::aead::OsRng` for nonces. Domain-neutral logic — only the name ties them to TOTP.

From `crates/axiam-auth/src/config.rs:31`:
- `pub struct AuthConfig { ... pub mfa_encryption_key: Option<[u8; 32]>, ... }` with `#[serde(skip)]` on the key field.

From `crates/axiam-auth/src/token.rs:17`:
- `pub struct AccessTokenClaims { sub, exp, iat, iss, jti, tenant_id, org_id, ... }` — no `aud` field today.
- `pub fn issue_access_token(...)` at `:43` — generates `jti = Uuid::new_v4().to_string()` at `:64`.
- `pub fn issue_client_credentials_token(...)` at `:81`.
- `pub fn decode_access_token(...)` at `:195` — `Validation::new(EdDSA)` with no audience enforcement.

From `crates/axiam-auth/src/service.rs:604`:
- `create_session_and_tokens(...)` — site that constructs `session` and calls `issue_access_token`. The `session.id` is in scope here. Pass it as the `jti` source.
- `refresh(...)` at `:469` — issues replacement access tokens; also has `session.id` in scope.

From `crates/axiam-server/src/main.rs:65-79`:
- Existing MFA key loader pattern — env var name `AXIAM__AUTH__MFA_ENCRYPTION_KEY`, hex decode, expect on length mismatch, `tracing::info!` on success, `tracing::warn!` on absence. Mirror this exactly.
- Other `tokio::spawn` sites at `:266, :285, :303` for reference.

From `crates/axiam-db/src/schema.rs:18-24` (idempotency pattern):
- Every `DEFINE TABLE` / `DEFINE FIELD` / `DEFINE INDEX` uses `IF NOT EXISTS`.
- New columns added via `DEFINE FIELD IF NOT EXISTS xxx ON TABLE federation_config TYPE option<string>` (nullable).

From `crates/axiam-db/src/schema.rs:354-370`:
- Current `federation_config` schema — has `client_secret: string` (plaintext, the legacy column).

From `crates/axiam-core/src/models/federation.rs`:
- `FederationConfig` struct. Add `Option<String>` fields for the three ciphertext columns + `idp_signing_cert_pem`, and `Vec<String>` for `allowed_algorithms` (default `vec!["RS256".into()]` for OIDC configs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AES-256-GCM helpers to axiam-auth::crypto (bundled + split variants) and add federation_encryption_key + allow_missing_aud_as_user to AuthConfig</name>
  <files>
    crates/axiam-auth/src/crypto.rs
    crates/axiam-auth/src/totp.rs
    crates/axiam-auth/src/lib.rs
    crates/axiam-auth/src/config.rs
    crates/axiam-server/src/main.rs
  </files>
  <read_first>
    crates/axiam-auth/src/totp.rs
    crates/axiam-auth/src/lib.rs
    crates/axiam-auth/src/config.rs
    crates/axiam-server/src/main.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — Create `crates/axiam-auth/src/crypto.rs` exposing FOUR `pub fn`s:

    Legacy bundled variant (used by TOTP path — must not change wire format):
    - `aes256gcm_encrypt(key: &[u8; 32], plaintext: &[u8]) -> Result<String, AuthError>` — base64 of `nonce(12) || ciphertext || tag`.
    - `aes256gcm_decrypt(key: &[u8; 32], encoded: &str) -> Result<Vec<u8>, AuthError>` — accepts the bundled format and splits on the 12-byte prefix.

    New split-output variant (REQUIRED by D-11 — federation_config stores `client_secret_nonce` and `client_secret_ciphertext` in SEPARATE columns):
    - `encrypt_separate(key: &[u8; 32], plaintext: &[u8]) -> Result<(String, String), AuthError>` — returns `(nonce_b64, ciphertext_with_tag_b64)`. The nonce is a fresh 12 bytes from `aes_gcm::aead::OsRng`; the ciphertext output contains `ciphertext || tag` (no nonce prefix). Both encoded with `base64::engine::general_purpose::STANDARD`.
    - `decrypt_separate(key: &[u8; 32], nonce_b64: &str, ciphertext_b64: &str) -> Result<Vec<u8>, AuthError>` — inverse: base64-decode both, run AES-256-GCM Open over `ciphertext_b64`'s contents using the decoded nonce.

    Both variants share a private helper (e.g. `fn build_cipher(key: &[u8; 32]) -> Aes256Gcm`) so the cryptographic primitive lives in one place. The crate already depends on `aes-gcm 0.10` and `base64 0.22` via TOTP — no Cargo.toml change.

    Step 2 — Rewrite `crates/axiam-auth/src/totp.rs` `encrypt_secret`/`decrypt_secret` to delegate to `crate::crypto::aes256gcm_encrypt`/`aes256gcm_decrypt` (the BUNDLED variant — TOTP wire format MUST NOT change; existing TOTP-encrypted secrets in the DB stay decryptable). Keep the public function names for back-compat — they become 1-line wrappers. Existing TOTP tests must still pass.

    Step 3 — Add `pub mod crypto;` to `crates/axiam-auth/src/lib.rs`. Verify both `axiam_auth::crypto::aes256gcm_encrypt` AND `axiam_auth::crypto::encrypt_separate` are reachable from external crates (axiam-federation will call `encrypt_separate` in plan 04-02 Task 3).

    Step 4 — Extend `AuthConfig` in `crates/axiam-auth/src/config.rs`:
    add `pub federation_encryption_key: Option<[u8; 32]>` with `#[serde(skip)]` immediately after `mfa_encryption_key`;
    add `pub allow_missing_aud_as_user: bool` with `#[serde(default = "default_true")]` (a free helper returning `true`).
    Update the existing `Default` impl to match.

    Step 5 — In `crates/axiam-server/src/main.rs`, add a loader block after the existing MFA loader (around `:79`) for `AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY`:
    - read env var, hex-decode, `.expect("AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY must be 64 hex chars / 32 bytes")` on length mismatch
    - assign into `config.auth.federation_encryption_key = Some(key)`
    - emit `tracing::info!("Federation encryption key loaded")` on success, `tracing::warn!("AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY not set — federation create/use will fail at runtime")` on absence
    - DO NOT panic on absence (federation is optional per D-10 / RESEARCH §3 "Absence behaviour")

    Also load `AXIAM__AUTH__ALLOW_MISSING_AUD_AS_USER` (bool, default true). Use the project's existing env→config wiring if `bool` already round-trips via `serde + config` crate; otherwise read explicitly with `std::env::var`.

    Step 6 — Unit tests in `crypto.rs` `#[cfg(test)] mod tests`:
    - `bundled_round_trip` — encrypt + decrypt via `aes256gcm_encrypt`/`aes256gcm_decrypt` returns the plaintext.
    - `separate_round_trip` — `encrypt_separate` returns two non-empty base64 strings; `decrypt_separate(key, &nonce_b64, &ct_b64)` returns the plaintext.
    - `separate_wrong_nonce_fails` — feeding a different nonce to `decrypt_separate` produces an error (tag verify fails).
    - `bundled_and_separate_are_independent_formats` — a value encrypted with `aes256gcm_encrypt` cannot be decrypted by `decrypt_separate` (it would interpret the nonce-prefix as raw ciphertext). Asserts the two formats are not interchangeable so a future caller cannot mix them.

    Step 7 — `cargo check -p axiam-auth` and `cargo check -p axiam-server` must both pass. Run `cargo fmt -p axiam-auth -p axiam-server` and `cargo clippy -p axiam-auth -p axiam-server -- -D warnings`.
  </action>
  <verify>
    <automated>cargo check -p axiam-auth -p axiam-server 2>&1 | tail -5 && cargo test -p axiam-auth --lib crypto 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/axiam-auth/src/crypto.rs` exists and exports `pub fn aes256gcm_encrypt` and `pub fn aes256gcm_decrypt`
    - `grep -c 'fn encrypt_separate' crates/axiam-auth/src/crypto.rs` returns 1
    - `grep -c 'fn decrypt_separate' crates/axiam-auth/src/crypto.rs` returns 1
    - `grep -c 'pub fn encrypt_separate\|pub fn decrypt_separate' crates/axiam-auth/src/crypto.rs` returns 2 (both are public)
    - `grep -n 'pub mod crypto' crates/axiam-auth/src/lib.rs` returns at least one match
    - `crates/axiam-auth/src/totp.rs` `encrypt_secret` body calls `crate::crypto::aes256gcm_encrypt`
    - `grep -n 'federation_encryption_key: Option<\[u8; 32\]>' crates/axiam-auth/src/config.rs` returns 1 match
    - `grep -n 'allow_missing_aud_as_user' crates/axiam-auth/src/config.rs` returns at least 1 match
    - `grep -n 'AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY' crates/axiam-server/src/main.rs` returns at least 1 match
    - `cargo check -p axiam-auth -p axiam-server` exits 0
    - `cargo test -p axiam-auth --lib totp` passes (TOTP round-trip still works via the bundled helper)
    - `cargo test -p axiam-auth --lib crypto` passes all four tests (`bundled_round_trip`, `separate_round_trip`, `separate_wrong_nonce_fails`, `bundled_and_separate_are_independent_formats`)
    - `cargo clippy -p axiam-auth -p axiam-server -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Shared AES-256-GCM helper module live in axiam-auth with BOTH bundled and split-output variants; AuthConfig carries both new keys; startup loads the federation key with the same operator pattern as MFA.</done>
</task>

<task type="auto">
  <name>Task 2: Make jti = session.id and add aud claim to AccessTokenClaims; both user and M2M issuance set it; decode accepts both with back-compat</name>
  <files>
    crates/axiam-auth/src/token.rs
    crates/axiam-auth/src/service.rs
  </files>
  <read_first>
    crates/axiam-auth/src/token.rs
    crates/axiam-auth/src/service.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — In `crates/axiam-auth/src/token.rs`:
    - Add `pub aud: Option<String>` field to `AccessTokenClaims` (`Option` so legacy decoded tokens can leave it `None`).
    - Define two crate-level constants at the top of the file: `pub const AUD_USER: &str = "axiam:user";` and `pub const AUD_M2M: &str = "axiam:m2m";` (per D-19).
    - Change the `issue_access_token` signature to accept an explicit `jti: String` parameter and an `aud: &'static str` parameter (default callers pass `AUD_USER`). The function MUST stop generating `Uuid::new_v4()` for `jti` at `:64` — replace with the passed `jti`. Set `claims.aud = Some(aud.to_string())`.
    - Change `issue_client_credentials_token` signature similarly with `aud` defaulting to `AUD_M2M`. (`jti` here can stay random — service accounts have no session row.)
    - Modify `decode_access_token` at `:195`:
        * call `Validation::new(Algorithm::EdDSA)`
        * `validation.set_issuer(&[config.effective_issuer()])`
        * `validation.set_required_spec_claims(&["sub","exp","iat","iss"])` (do NOT require `aud` — back-compat per D-20)
        * `validation.set_audience(&[AUD_USER, AUD_M2M])` (per RESEARCH §5, jsonwebtoken treats missing aud as OK when validate_aud=true; presence is checked for membership only)
        * `validation.leeway = 60`
    - Add a doc comment on `AccessTokenClaims::aud` warning that `None` means "issued before Phase 4" and is treated as `axiam:user` by the REST extractor when `AuthConfig.allow_missing_aud_as_user` is true.

    Step 2 — In `crates/axiam-auth/src/service.rs`:
    - At `create_session_and_tokens` (`:604`): build `session` first (it already happens), then call `issue_access_token(..., session.id.to_string(), AUD_USER)`. The session row's UUID becomes the JWT's `jti`. Import `use crate::token::AUD_USER;`.
    - At `refresh` (`:469`): same change — when issuing the replacement access token, pass the NEW session.id as `jti` and `AUD_USER` as aud.
    - At the client-credentials issuance call sites (search file for `issue_client_credentials_token`): pass `AUD_M2M`.
    - Update every other internal caller of `issue_access_token` to pass an explicit `jti` (use `Uuid::new_v4().to_string()` only where there is no session row, e.g. test helpers) and an aud constant. Compilation will surface missing call sites.

    Step 3 — `cargo check -p axiam-auth` must pass. Run `cargo test -p axiam-auth --lib` — at least the `token` and `service` unit tests must compile; failing-test fixes that depend on new claim shape are acceptable here (broaden assertions). Run `cargo fmt -p axiam-auth` and `cargo clippy -p axiam-auth -- -D warnings`.
  </action>
  <verify>
    <automated>cargo check -p axiam-auth 2>&1 | tail -5 && cargo test -p axiam-auth --lib token 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'pub aud: Option<String>' crates/axiam-auth/src/token.rs` returns 1 match
    - `grep -n 'pub const AUD_USER' crates/axiam-auth/src/token.rs` returns 1 match
    - `grep -n 'pub const AUD_M2M' crates/axiam-auth/src/token.rs` returns 1 match
    - `grep -n 'Uuid::new_v4' crates/axiam-auth/src/token.rs | grep -v '^#' | grep -c jti` returns 0 (jti is no longer generated inside `issue_access_token`)
    - `grep -n 'set_audience' crates/axiam-auth/src/token.rs` returns at least 1 match referencing both AUD_USER and AUD_M2M
    - `grep -n 'leeway = 60\|leeway=60' crates/axiam-auth/src/token.rs` returns 1 match
    - `grep -n 'session.id' crates/axiam-auth/src/service.rs | grep -c 'issue_access_token\|jti'` returns at least 2 (one in create_session_and_tokens, one in refresh)
    - `cargo check -p axiam-auth` exits 0
    - A new or updated unit test in `token.rs` asserts: round-trip an access token issued with `jti="00000000-0000-0000-0000-000000000001"` and `aud=AUD_USER`, decode it, assert decoded `claims.jti == "00000000-..."` AND `claims.aud == Some("axiam:user".into())`
    - `cargo clippy -p axiam-auth -- -D warnings` exits 0
  </acceptance_criteria>
  <done>JWT jti now equals the issuing session.id; aud claim is set on both user and M2M paths; decode tolerates missing aud during back-compat; extractor in plan 04-04 can rely on jti→session_id without DB lookup.</done>
</task>

<task type="auto">
  <name>Task 3: Schema migration — extend federation_config (5 cols), create saml_assertion_replay and federation_login_state tables; extend FederationConfig model</name>
  <files>
    crates/axiam-db/src/schema.rs
    crates/axiam-core/src/models/federation.rs
  </files>
  <read_first>
    crates/axiam-db/src/schema.rs
    crates/axiam-core/src/models/federation.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — In `crates/axiam-db/src/schema.rs`, locate the `federation_config` block (around `:354-370`) and add `DEFINE FIELD IF NOT EXISTS` statements for:
    - `allowed_algorithms ON TABLE federation_config TYPE array<string> DEFAULT []`
    - `idp_signing_cert_pem ON TABLE federation_config TYPE option<string>`
    - `client_secret_ciphertext ON TABLE federation_config TYPE option<string>`
    - `client_secret_nonce ON TABLE federation_config TYPE option<string>` (now actively used per D-11 — see plan 04-02 Task 3 which calls `encrypt_separate` and writes the returned `nonce_b64` here)
    - `client_secret_key_version ON TABLE federation_config TYPE option<int>`
    Leave the existing `client_secret: string` field untouched (legacy column, per D-11; nulled by backfill in plan 04-02).

    Step 2 — Add new tables following the schema.rs:18-24 idempotency pattern:
    - `saml_assertion_replay` with fields `tenant_id: string`, `assertion_id: string`, `expires_at: datetime`, `created_at: datetime DEFAULT time::now()`. UNIQUE index `idx_replay_uniq` on `(tenant_id, assertion_id)`. Non-unique index `idx_replay_expires_at` on `expires_at`.
    - `federation_login_state` with fields `state: string`, `nonce: string`, `tenant_id: string`, `federation_config_id: string`, `redirect_uri: string`, `expires_at: datetime`, `created_at: datetime DEFAULT time::now()`. UNIQUE index `idx_login_state_uniq` on `state`. Non-unique index `idx_login_state_expires_at` on `expires_at`.

    Exact SurrealQL templates in 04-RESEARCH.md §"Replay table" (`saml_assertion_replay`) and §"federation_login_state". Use those verbatim.

    Step 3 — In `crates/axiam-core/src/models/federation.rs`, extend `FederationConfig` struct with:
    - `pub allowed_algorithms: Vec<String>` (default `vec!["RS256".to_string()]` for OIDC configs in constructors; SAML configs default empty)
    - `pub idp_signing_cert_pem: Option<String>`
    - `pub client_secret_ciphertext: Option<String>`
    - `pub client_secret_nonce: Option<String>`
    - `pub client_secret_key_version: Option<i64>`
    Keep `pub client_secret: Option<String>` (or whatever the current shape is — DO NOT remove). Adjust any `new()` / `Default` impls so existing call sites still compile.

    Step 4 — `cargo check -p axiam-db -p axiam-core` must pass. Run `cargo fmt -p axiam-db -p axiam-core` and `cargo clippy -p axiam-db -p axiam-core -- -D warnings`.

    Step 5 — Sanity-check the migration is idempotent: open a SurrealDB shell against the in-memory engine via `cargo test -p axiam-db --lib migrations` (or whatever migration test exists; add one if absent). Migration must run twice in a row without error.
  </action>
  <verify>
    <automated>cargo check -p axiam-db -p axiam-core 2>&1 | tail -5 && cargo test -p axiam-db --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DEFINE FIELD IF NOT EXISTS allowed_algorithms ON TABLE federation_config' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'DEFINE FIELD IF NOT EXISTS idp_signing_cert_pem ON TABLE federation_config' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'DEFINE FIELD IF NOT EXISTS client_secret_ciphertext ON TABLE federation_config' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'DEFINE FIELD IF NOT EXISTS client_secret_nonce ON TABLE federation_config' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'DEFINE FIELD IF NOT EXISTS client_secret_key_version ON TABLE federation_config' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'DEFINE TABLE IF NOT EXISTS saml_assertion_replay' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'DEFINE INDEX IF NOT EXISTS idx_replay_uniq' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'COLUMNS tenant_id, assertion_id UNIQUE' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'DEFINE TABLE IF NOT EXISTS federation_login_state' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'DEFINE INDEX IF NOT EXISTS idx_login_state_uniq' crates/axiam-db/src/schema.rs` returns 1
    - `grep -c 'pub allowed_algorithms: Vec<String>' crates/axiam-core/src/models/federation.rs` returns 1
    - `grep -c 'pub idp_signing_cert_pem: Option<String>' crates/axiam-core/src/models/federation.rs` returns 1
    - `grep -c 'pub client_secret_ciphertext: Option<String>' crates/axiam-core/src/models/federation.rs` returns 1
    - `grep -c 'pub client_secret_nonce: Option<String>' crates/axiam-core/src/models/federation.rs` returns 1
    - `grep -c 'pub client_secret_key_version: Option<i64>' crates/axiam-core/src/models/federation.rs` returns 1
    - `cargo check -p axiam-db -p axiam-core` exits 0
    - Migration test runs twice and returns Ok both times (idempotency)
  </acceptance_criteria>
  <done>Schema migration adds 5 columns to federation_config and creates the two new tables; FederationConfig model carries the new fields; later plans can read/write the new columns without further schema work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → process env | `AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY` reaches the process via secret manager / K8s secret; must not be logged |
| user-issued JWT → REST extractor | `aud` claim is checked at extraction time (plan 04-04); this plan widens the decode to accept both audiences |
| in-flight JWTs at deploy time → new pods | Pre-Phase-4 tokens lack `aud`; the back-compat flag mediates trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Audience confusion (M2M token used on user routes) | mitigate | Plan introduces `AUD_USER`/`AUD_M2M`; per-route narrowing happens in plan 04-04 |
| T-04-02 | Tampering | Encryption key swap reveals all federation secrets | mitigate | Key version column `client_secret_key_version` is added so future rotations don't break decrypt; key loaded from env at boot, never logged (Task 1 emits info on presence only, never the value) |
| T-04-03 | InformationDisclosure | Encryption key leaks via panic backtrace on bad input | mitigate | `.expect("must be 64 hex chars / 32 bytes")` messages avoid printing the key value; only length is referenced |
| T-04-04 | ElevationOfPrivilege | Attacker discovers `jti` equals `session.id` and uses it to enumerate sessions | accept | Session IDs are UUIDv4 — 122 bits of randomness; no enumeration advantage from knowing the relationship. Documented in token.rs doc comment. |
| T-04-05 | Repudiation | Operator denies the back-compat window was open | mitigate | `allow_missing_aud_as_user` is a config knob; `tracing::warn!` is emitted by plan 04-04 on each accepted token-without-aud, creating an audit trail (warn rate-limited there) |
| T-04-06 | DoS | Schema migration locks federation_config under load | accept | `DEFINE FIELD IF NOT EXISTS` is metadata-only in SurrealDB v3; no row-level lock; pattern proven by prior phases |
</threat_model>

<verification>
- `cargo check -p axiam-auth -p axiam-server -p axiam-db -p axiam-core` — every touched crate compiles.
- `cargo fmt --check -p axiam-auth -p axiam-server -p axiam-db -p axiam-core` — formatting clean.
- `cargo clippy -p axiam-auth -p axiam-server -p axiam-db -p axiam-core -- -D warnings` — zero warnings.
- TOTP unit tests still pass (delegation didn't break the AES-GCM round-trip).
- New unit tests in `crypto.rs` cover the split-output variant required by D-11.
- New unit test in `token.rs` asserts jti=session_id and aud round-trip.
- Migration re-runs idempotently.
</verification>

<success_criteria>
- D-10 / D-11: `AuthConfig.federation_encryption_key` loadable from env; `federation_config` schema has the 3 ciphertext columns + `allowed_algorithms` + `idp_signing_cert_pem`; `axiam-auth::crypto` exposes a split-output variant so each of `client_secret_nonce` / `client_secret_ciphertext` lands in its own column (no nonce-prefix bundling for federation rows).
- D-19 / D-20 / D-21: `aud` claim added to `AccessTokenClaims`; both `axiam:user` and `axiam:m2m` issuance paths set it; decode accepts both with 15-min back-compat for missing aud.
- RESEARCH §"jti = session_id" recommendation: `jti` now equals `session.id` at issuance; unblocks D-15 `revoke_all_sessions_except`.
- D-09 / D-24: `saml_assertion_replay` and `federation_login_state` tables exist with UNIQUE indexes.
- TOTP AES-GCM helpers extracted to a domain-neutral module (RESEARCH §3) and reused by TOTP.
</success_criteria>

<output>
After completion, create `.planning/phases/04-federation-verification-session-security/04-01-SUMMARY.md`
</output>
</content>
</invoke>