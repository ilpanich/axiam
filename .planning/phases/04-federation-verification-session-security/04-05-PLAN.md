---
phase: 04-federation-verification-session-security
plan: 05
type: execute
wave: 5
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - crates/axiam-db/src/repository/federation_login_state.rs
  - crates/axiam-db/src/repository/mod.rs
  - crates/axiam-core/src/repository.rs
  - crates/axiam-federation/src/oidc.rs
  - crates/axiam-federation/src/saml.rs
  - crates/axiam-api-rest/src/handlers/federation.rs
  - crates/axiam-api-rest/src/handlers/mod.rs
  - crates/axiam-api-rest/src/server.rs
  - crates/axiam-api-rest/src/permissions.rs
autonomous: true
requirements: [REQ-5]

must_haves:
  truths:
    - "POST /api/v1/auth/federation/oidc/start returns IdP authorize URL + state + nonce; persists row in federation_login_state"
    - "POST /api/v1/auth/federation/oidc/callback consumes the row by state, runs verified OIDC flow (plan 04-02), provisions or links user, returns Set-Cookie response (no token in body)"
    - "POST /api/v1/auth/federation/saml/login returns SAML AuthnRequest POST binding payload + persists state row"
    - "POST /api/v1/auth/federation/saml/acs consumes state row, runs verified SAML flow (plan 04-03), provisions or links user, returns Set-Cookie response"
    - "All four endpoints are reachable WITHOUT authentication (in PUBLIC_PATHS)"
    - "Expired federation_login_state rows reject with 401 'state expired'"
    - "Already-consumed state returns 401 'state not found'"
    - "Tenant-by-slug resolution uses the SAME pattern as Phase 1 login (org_repo.get_by_slug → tenant_repo.get_by_slug(org_id, slug)) — NO TODO left behind"
  artifacts:
    - path: "crates/axiam-db/src/repository/federation_login_state.rs"
      provides: "SurrealFederationLoginStateRepository — insert / consume / cleanup_expired"
      exports: ["SurrealFederationLoginStateRepository"]
    - path: "crates/axiam-api-rest/src/handlers/federation.rs"
      provides: "Four new public handlers: oidc_start, oidc_callback_public, saml_login, saml_acs_public"
  key_links:
    - from: "crates/axiam-api-rest/src/server.rs"
      to: "crates/axiam-api-rest/src/permissions.rs"
      via: "four new /api/v1/auth/federation/* paths added to PUBLIC_PATHS at :179"
      pattern: "/api/v1/auth/federation/"
    - from: "crates/axiam-federation/src/oidc.rs"
      to: "crates/axiam-db/src/repository/federation_login_state.rs"
      via: "OIDC callback looks up nonce by state instead of taking it as a caller arg"
      pattern: "consume_by_state|federation_login_state"
---

<objective>
Add the four new PUBLIC endpoints `/api/v1/auth/federation/oidc/{start,callback}` and `/api/v1/auth/federation/saml/{login,acs}` for FIRST-TIME SSO (D-22, D-23). State + nonce + redirect_uri persist in the `federation_login_state` table (created in plan 04-01) with 10-minute TTL. Callbacks consume the row by `state`, run the verified OIDC/SAML flows from plans 04-02/04-03, provision-or-link the user, and respond with login cookies via the existing `cookie_response_from_output` helper (Phase 1 D-13 shape — no token in JSON body). Extend `PUBLIC_PATHS` at `crates/axiam-api-rest/src/permissions.rs:179` so the authz middleware lets these routes through.

Wave: this plan was bumped from wave 3 to wave 4. Reason: it shares files (`crates/axiam-api-rest/src/server.rs`, `crates/axiam-core/src/repository.rs`, `crates/axiam-api-rest/src/handlers/federation.rs`) with plan 04-04 AND with plan 04-03. After the wave-3 collision fix between 04-03 and 04-04, this plan must run after 04-04 to avoid a fresh in-wave collision.

Purpose: Close the loop on REQ-5 by wiring the cryptographic verifiers built in 04-02 and 04-03 into actually-reachable public endpoints. The link-account flow at `/api/v1/federation/*` (Phase 3) keeps working unchanged; this plan adds the FIRST-TIME LOGIN flow separately.
Output: A working first-time SSO surface — server-side only (frontend deferred per RESEARCH §6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-federation-verification-session-security/04-CONTEXT.md
@.planning/phases/04-federation-verification-session-security/04-RESEARCH.md
@.planning/phases/04-federation-verification-session-security/04-02-PLAN.md
@.planning/phases/04-federation-verification-session-security/04-03-PLAN.md
@.planning/phases/04-federation-verification-session-security/04-04-PLAN.md
@.planning/phases/03-rbac-enforcement/03-CONTEXT.md
@.planning/phases/01-cookie-based-authentication/01-CONTEXT.md

<interfaces>
From `crates/axiam-api-rest/src/permissions.rs:179`:
- `pub const PUBLIC_PATHS: &[&str] = &[ ... ]` — current entries listed in RESEARCH §6 ("Federation callback endpoints" comment, lines 207-210 of permissions.rs). Add 4 new entries here.

From `crates/axiam-api-rest/src/middleware/authz.rs:38`:
- `fn is_public_path(path: &str) -> bool` — iterates `PUBLIC_PATHS`. Test at `:148-155`.

From `crates/axiam-api-rest/src/handlers/federation.rs`:
- Existing authenticated OIDC handlers at `:413, :470` and SAML handlers at `:646, :696, :752` — DO NOT modify (D-23 keeps the link-account flow intact). The new public handlers are siblings.

From `crates/axiam-api-rest/src/handlers/auth.rs:251-286` (CONFIRMED via file read during revision — exact tenant-resolution pattern used by Phase 1 login):
- Phase 1 login resolves workspace identity with this pattern, deliberately mapping slug-lookup failures to `AuthenticationFailed (401)` to avoid disclosing whether the org/tenant exists:
```rust
let org_id = match (b.org_id, b.org_slug.as_deref()) {
    (Some(id), _) => id,
    (None, Some(slug)) => {
        org_repo
            .get_by_slug(slug)
            .await
            .map_err(|_| AxiamError::AuthenticationFailed { reason: "invalid credentials".into() })?
            .id
    }
    (None, None) => return Err(AxiamApiError(AxiamError::Validation { message: "must provide org_id or org_slug".into() })),
};
let tenant_id = match (b.tenant_id, b.tenant_slug.as_deref()) {
    (Some(id), _) => id,
    (None, Some(slug)) => {
        tenant_repo
            .get_by_slug(org_id, slug)
            .await
            .map_err(|_| AxiamError::AuthenticationFailed { reason: "invalid credentials".into() })?
            .id
    }
    (None, None) => return Err(AxiamApiError(AxiamError::Validation { message: "must provide tenant_id or tenant_slug".into() })),
};
```
This plan's four public handlers MUST use this same pattern verbatim — adapt the error message for federation context ("invalid federation request" instead of "invalid credentials") but keep the 401-on-slug-lookup-failure behaviour to avoid org/tenant enumeration. NO TODO is permitted for tenant resolution.

From `crates/axiam-api-rest/src/handlers/auth.rs:176-217`:
- `cookie_response_from_output(output: AuthOutput) -> HttpResponse` — emits `axiam_access` + `axiam_refresh` + `axiam_csrf` Set-Cookies, JSON body `LoginSuccessResponse { user, session_id, expires_in }` with NO token fields. Reused verbatim.

`federation_login_state` table (schema created in plan 04-01):
- Fields: `state, nonce, tenant_id, federation_config_id, redirect_uri, expires_at, created_at`.
- UNIQUE index on `state`.
- 10-minute TTL per D-24.

Plan 04-02 surface to reuse:
- `OidcFederationService` with `cache: Arc<JwksCache>` and the verified `decode_id_token_claims` path.
- The caller-supplied `expected_nonce` argument is the field we now source from `federation_login_state.nonce` rather than from a caller-passed HTTP body field.

Plan 04-03 surface to reuse:
- `SamlFederationService::verify_signature` + replay-check inside the ACS path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SurrealFederationLoginStateRepository — insert / consume_by_state / cleanup_expired</name>
  <files>
    crates/axiam-db/src/repository/federation_login_state.rs
    crates/axiam-db/src/repository/mod.rs
    crates/axiam-core/src/repository.rs
  </files>
  <read_first>
    crates/axiam-core/src/repository.rs
    crates/axiam-db/src/repository/session.rs
    crates/axiam-db/src/repository/federation.rs
    crates/axiam-db/src/repository/mod.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — Trait `FederationLoginStateRepository` in `crates/axiam-core/src/repository.rs`:
    - `pub struct FederationLoginState { pub state: String, pub nonce: String, pub tenant_id: Uuid, pub federation_config_id: Uuid, pub redirect_uri: String, pub expires_at: DateTime<Utc> }` — domain shape.
    - `async fn insert(&self, row: &FederationLoginState) -> AxiamResult<()>`.
    - `async fn consume_by_state(&self, state: &str) -> AxiamResult<Option<FederationLoginState>>` — atomic SELECT + DELETE. If the row exists and `expires_at > now` → return it (after deleting). If row exists but expired → DELETE and return `Ok(None)` (caller treats None as "state not found or expired" → 401). If no row → `Ok(None)`.
    - `async fn cleanup_expired(&self) -> AxiamResult<u64>` — DELETE rows where `expires_at < time::now()`; returns count.

    Step 2 — Implement on `SurrealFederationLoginStateRepository<C: Connection>` in `crates/axiam-db/src/repository/federation_login_state.rs`.
    SurrealQL templates (using MEMORY.md v3 patterns):
    - INSERT: `CREATE type::record('federation_login_state', $id) SET state = $state, nonce = $nonce, tenant_id = $tenant_id, federation_config_id = $config_id, redirect_uri = $redirect_uri, expires_at = $expires_at`. `$id = Uuid::new_v4().to_string()`.
    - CONSUME: two-step or transaction:
        ```
        BEGIN TRANSACTION;
        LET $row = SELECT *, meta::id(id) AS row_id FROM federation_login_state WHERE state = $state LIMIT 1;
        DELETE federation_login_state WHERE state = $state;
        RETURN $row;
        COMMIT TRANSACTION;
        ```
      In code: bind state, take result, decode the row, then check `expires_at > now()` in Rust — if expired, return None.
    - CLEANUP: `DELETE federation_login_state WHERE expires_at < time::now() RETURN BEFORE` then count.

    Step 3 — Wire into `crates/axiam-db/src/repository/mod.rs` — `pub mod federation_login_state;` and re-export. Register as `web::Data` in `axiam-server::main.rs` alongside other repos.

    Step 4 — Integration test in `crates/axiam-db/tests/federation_login_state.rs`:
    - Insert + consume → returns the same row, second consume → `Ok(None)`.
    - Insert with `expires_at = now - 1s` + consume → `Ok(None)` AND row is gone.
    - `cleanup_expired` after inserting one fresh + one expired row returns 1 and leaves the fresh row.
    - UNIQUE constraint on `state` — duplicate INSERT returns an error.

    Step 5 — `cargo check -p axiam-core -p axiam-db`, run the test, `cargo fmt`, `cargo clippy -p axiam-core -p axiam-db -- -D warnings`.
  </action>
  <verify>
    <automated>cargo test -p axiam-db --test federation_login_state 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/axiam-db/src/repository/federation_login_state.rs` exists with `pub struct SurrealFederationLoginStateRepository`
    - `grep -c 'pub struct FederationLoginState' crates/axiam-core/src/repository.rs` returns 1
    - `grep -c 'async fn consume_by_state' crates/axiam-core/src/repository.rs` returns 1
    - `grep -c 'BEGIN TRANSACTION\|DELETE federation_login_state' crates/axiam-db/src/repository/federation_login_state.rs` returns at least 1
    - All 4 integration tests pass
    - `cargo clippy -p axiam-core -p axiam-db -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Repository for the first-time SSO state table is in place with atomic consume + cleanup tested.</done>
</task>

<task type="auto">
  <name>Task 2: Four public handlers — oidc_start, oidc_callback_public, saml_login, saml_acs_public — and route registration. Tenant-by-slug resolution uses the concrete Phase 1 login pattern (no TODO).</name>
  <files>
    crates/axiam-api-rest/src/handlers/federation.rs
    crates/axiam-api-rest/src/handlers/mod.rs
    crates/axiam-api-rest/src/server.rs
    crates/axiam-api-rest/src/permissions.rs
    crates/axiam-federation/src/oidc.rs
    crates/axiam-federation/src/saml.rs
  </files>
  <read_first>
    crates/axiam-api-rest/src/handlers/federation.rs
    crates/axiam-api-rest/src/handlers/auth.rs
    crates/axiam-api-rest/src/server.rs
    crates/axiam-api-rest/src/permissions.rs
    crates/axiam-api-rest/src/middleware/authz.rs
    crates/axiam-federation/src/oidc.rs
    crates/axiam-federation/src/saml.rs
    .planning/phases/04-federation-verification-session-security/04-RESEARCH.md
  </read_first>
  <action>
    Step 1 — Request/response types in `handlers/federation.rs`:
    - `OidcStartRequest { org_slug: Option<String>, org_id: Option<Uuid>, tenant_slug: Option<String>, tenant_id: Option<Uuid>, federation_config_id: Uuid, redirect_uri: String }` — accept BOTH UUIDs and slugs, mirroring `LoginRequest` from Phase 1.
    - `OidcStartResponse { authorize_url: String, state: String, expires_in_secs: i64 }` — nonce stays server-side, not exposed.
    - `OidcCallbackRequest { state: String, code: String }`.
    - `SamlLoginRequest { org_slug: Option<String>, org_id: Option<Uuid>, tenant_slug: Option<String>, tenant_id: Option<Uuid>, federation_config_id: Uuid, redirect_uri: String }`.
    - `SamlLoginResponse { binding: String, sso_url: String, saml_request_b64: String, relay_state: String }` (RelayState carries our `state` value through the IdP round-trip).
    - `SamlAcsRequest { saml_response_b64: String, relay_state: String }`.

    Step 2 — `pub async fn oidc_start_public<C>(...)`:
    - Resolve tenant_id using the EXACT pattern from `handlers/auth.rs::login` (lines 251-286 in the current file). Inject `web::Data<SurrealOrganizationRepository<C>>` and `web::Data<SurrealTenantRepository<C>>`. Concrete:
      ```rust
      let org_id = match (b.org_id, b.org_slug.as_deref()) {
          (Some(id), _) => id,
          (None, Some(slug)) => org_repo.get_by_slug(slug).await
              .map_err(|_| AxiamError::AuthenticationFailed { reason: "invalid federation request".into() })?
              .id,
          (None, None) => return Err(AxiamApiError(AxiamError::Validation { message: "must provide org_id or org_slug".into() })),
      };
      let tenant_id = match (b.tenant_id, b.tenant_slug.as_deref()) {
          (Some(id), _) => id,
          (None, Some(slug)) => tenant_repo.get_by_slug(org_id, slug).await
              .map_err(|_| AxiamError::AuthenticationFailed { reason: "invalid federation request".into() })?
              .id,
          (None, None) => return Err(AxiamApiError(AxiamError::Validation { message: "must provide tenant_id or tenant_slug".into() })),
      };
      ```
    - Validate `federation_config_id` belongs to that tenant; verify it's an OIDC config and `redirect_uri` is in the config's allowed list (existing field on FederationConfig).
    - Generate `state` = 32 random bytes → base64url. Generate `nonce` = 32 random bytes → base64url. Compute `expires_at = now + 10min`.
    - Persist via `login_state_repo.insert(...)`.
    - Build the IdP authorize URL via the existing OidcFederationService method (or a new `build_authorize_url(config, state, nonce, redirect_uri) -> String` helper if missing). Required query params: `response_type=code`, `client_id`, `redirect_uri` (server-side AXIAM callback URL — NOT the user's `redirect_uri`; that's stored to redirect back to the SPA AFTER login), `scope=openid email profile`, `state`, `nonce`.
    - Return `OidcStartResponse`.

    NOTE on redirect_uri: there are two `redirect_uri`s — the one we send to the IdP (an AXIAM-owned URL like `/api/v1/auth/federation/oidc/callback`) and the post-login destination the SPA wants the user back on. Store the SPA URL in `federation_login_state.redirect_uri`; the AXIAM URL is hard-coded based on the request's `Host`. Document this.

    Step 3 — `pub async fn oidc_callback_public<C>(...)`:
    - `login_state = login_state_repo.consume_by_state(&body.state).await?`; `None` → 401 "state not found or expired".
    - Verify `login_state.tenant_id` is non-empty.
    - Exchange `body.code` for tokens via existing `OidcFederationService::exchange_code(...)` — this calls the IdP's token endpoint using the encrypted `client_secret` from plan 04-02.
    - Verify the ID token via the verified path from plan 04-02 — passing `expected_nonce = &login_state.nonce` (NOT a body field).
    - Provision-or-link via existing `OidcFederationService::provision_or_link_user`.
    - Build a local `AuthOutput` (session + access cookie + refresh cookie) — reuse `AuthService::create_session_and_tokens` or whatever Phase 1 login uses.
    - Return `cookie_response_from_output(output)`. The caller's redirect to `login_state.redirect_uri` is the SPA's responsibility (it parses the response and routes). Optionally include `redirect_uri` in the JSON body so the SPA can route after auth.

    Step 4 — `pub async fn saml_login_public<C>(...)`:
    - Resolve tenant using the SAME concrete pattern as Step 2 (org_slug → get_by_slug → tenant_slug → get_by_slug). NO TODO.
    - Validate config (SAML config), generate state (32 bytes) — nonce is NOT used for SAML.
    - Persist a `federation_login_state` row with `nonce = ""` (unused for SAML) and `expires_at = now + 10min`.
    - Build the SAML AuthnRequest via existing `SamlFederationService::build_authn_request(...)`. Pass our `state` as `RelayState` (SAML standard pattern).
    - Return `SamlLoginResponse` with the POST-binding payload (form fields `SAMLRequest`, `RelayState`).

    Step 5 — `pub async fn saml_acs_public<C>(...)`:
    - `login_state = login_state_repo.consume_by_state(&body.relay_state).await?`; `None` → 401 "state not found or expired".
    - The tenant_id is taken from `login_state.tenant_id` (already resolved at saml_login_public time and persisted in the state row — no slug-resolution needed at ACS time).
    - Base64-decode `body.saml_response_b64` → XML bytes.
    - Run the verified SAML flow from plan 04-03 — signature verify, condition validate, replay insert, provision-or-link.
    - Build `AuthOutput` and return `cookie_response_from_output(output)`.

    Step 6 — In `crates/axiam-api-rest/src/server.rs`, register the four routes under `web::scope("/auth/federation")`:
    - `web::resource("/oidc/start").wrap(build_governor(rate_limit_cfg.login_per_min)).route(web::post().to(handlers::federation::oidc_start_public::<C>))`
    - `web::resource("/oidc/callback").wrap(build_governor(rate_limit_cfg.login_per_min)).route(web::post().to(handlers::federation::oidc_callback_public::<C>))`
    - `web::resource("/saml/login").wrap(build_governor(rate_limit_cfg.login_per_min)).route(web::post().to(handlers::federation::saml_login_public::<C>))`
    - `web::resource("/saml/acs").wrap(build_governor(rate_limit_cfg.login_per_min)).route(web::post().to(handlers::federation::saml_acs_public::<C>))`

    Step 7 — In `crates/axiam-api-rest/src/permissions.rs:179` (the `PUBLIC_PATHS` constant), add four entries (per RESEARCH §6 exact path strings):
    - `"/api/v1/auth/federation/oidc/start"`
    - `"/api/v1/auth/federation/oidc/callback"`
    - `"/api/v1/auth/federation/saml/login"`
    - `"/api/v1/auth/federation/saml/acs"`
    Add a comment block above them: `// First-time SSO (Phase 4 D-22) — unauthenticated, distinct from /api/v1/federation/* link-account endpoints (which require auth).`

    Update the unit test at `crates/axiam-api-rest/src/middleware/authz.rs:148-155` to cover the new paths — assert `is_public_path("/api/v1/auth/federation/oidc/start") == true`, etc.

    Step 8 — Integration tests in `crates/axiam-api-rest/tests/federation_first_time_sso.rs`:
    - `oidc_start_persists_state` — POST without auth → 200 with `state` in body; DB has a `federation_login_state` row with matching state and 10-min TTL.
    - `oidc_start_unknown_org_slug_returns_401` — POST with non-existent `org_slug` → 401 (enumeration-resistant — same behaviour as login).
    - `oidc_start_unknown_tenant_slug_returns_401` — POST with valid org_slug + non-existent tenant_slug → 401.
    - `oidc_callback_consumes_state` — manually insert a state row, POST callback with that state + a code → state row gone after; mock OIDC token endpoint via `wiremock`.
    - `oidc_callback_rejects_unknown_state` — POST with `state = "bogus"` → 401.
    - `oidc_callback_rejects_expired_state` — INSERT a row with `expires_at = now - 1s` → 401 and row deleted.
    - `oidc_callback_emits_cookies_not_tokens_in_body` — assert response has `Set-Cookie: axiam_access` + `axiam_refresh` + `axiam_csrf` AND body has NO `access_token` / `refresh_token` fields (per Phase 1 D-13).
    - `saml_acs_consumes_relay_state` — analogous.
    - `public_paths_allow_unauthenticated_access` — request to each of the four routes WITHOUT a JWT cookie reaches the handler (not 401 from authz middleware). Easiest assertion: the request body fails parse → 400, NOT 401-from-authz.

    Step 9 — `cargo check -p axiam-api-rest -p axiam-federation`, run the integration tests, `cargo fmt`, `cargo clippy -p axiam-api-rest -p axiam-federation -- -D warnings`.
  </action>
  <verify>
    <automated>cargo test -p axiam-api-rest --test federation_first_time_sso 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'oidc_start_public\|oidc_callback_public\|saml_login_public\|saml_acs_public' crates/axiam-api-rest/src/handlers/federation.rs` returns at least 4
    - `grep -c '/api/v1/auth/federation/oidc/start' crates/axiam-api-rest/src/permissions.rs` returns 1
    - `grep -c '/api/v1/auth/federation/oidc/callback' crates/axiam-api-rest/src/permissions.rs` returns 1
    - `grep -c '/api/v1/auth/federation/saml/login' crates/axiam-api-rest/src/permissions.rs` returns 1
    - `grep -c '/api/v1/auth/federation/saml/acs' crates/axiam-api-rest/src/permissions.rs` returns 1
    - `grep -c 'consume_by_state' crates/axiam-api-rest/src/handlers/federation.rs` returns at least 2 (OIDC + SAML callbacks)
    - `grep -c 'cookie_response_from_output' crates/axiam-api-rest/src/handlers/federation.rs` returns at least 2
    - `grep -c '/auth/federation' crates/axiam-api-rest/src/server.rs` returns at least 1
    - `rg -c 'get_by_slug' crates/axiam-api-rest/src/handlers/federation.rs` returns at least 4 (org_repo.get_by_slug + tenant_repo.get_by_slug, called from BOTH oidc_start_public AND saml_login_public — 2 endpoints × 2 calls = 4)
    - `grep -c 'TODO' crates/axiam-api-rest/src/handlers/federation.rs | head` shows 0 NEW TODOs introduced by these handlers (existing TODOs from earlier phases can remain)
    - All 9 integration tests pass (including the two new enumeration-resistance tests)
    - `crates/axiam-api-rest/src/middleware/authz.rs` test at `:148-155` updated to cover the new paths and still passes
    - `cargo clippy -p axiam-api-rest -p axiam-federation -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Four public first-time SSO endpoints live; state+nonce correlation via the new table; cookies-only response; PUBLIC_PATHS extended; authz middleware unit test extended; tenant-by-slug uses the concrete Phase 1 login pattern with 401-on-unknown-slug enumeration resistance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → /auth/federation/oidc/start | Unauthenticated; we generate state+nonce server-side (entropy is OUR responsibility) |
| IdP redirect → /auth/federation/*/callback | The state value is the proof that the redirect was triggered by our start endpoint; nonce inside the ID token closes the loop |
| stored state row → consumer | One-time-use; deleted as part of consume; UNIQUE index prevents collision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-28 | Spoofing | Attacker submits crafted state to bypass IdP | mitigate | `state` is server-generated, 256 bits of randomness, stored in DB; `consume_by_state` returns None for unknown values → 401 |
| T-04-29 | Tampering | CSRF — attacker tricks user's browser to consume their own state on a different tab | mitigate | `state` is one-time-use (consume deletes the row); a stolen-then-consumed state is useless on re-replay |
| T-04-30 | Tampering | Replay attack on the OIDC code | mitigate | OIDC `nonce` is bound to `state` in our DB; the ID token's nonce must match — handled by plan 04-02 verifier |
| T-04-31 | InformationDisclosure | Returning `nonce` in oidc_start_public response | mitigate | Response body intentionally omits `nonce`; only `state` + `authorize_url` (which the IdP needs to embed the nonce in the redirect query, NOT our response). Server-side keeps the nonce private. |
| T-04-32 | DoS | Spam to `oidc_start_public` floods federation_login_state table | mitigate | Per-IP rate limit via existing actix-governor builder (Task 2 Step 6); cleanup task (plan 04-06) sweeps expired rows; UNIQUE index on `state` makes collisions infeasible (random 256 bits) |
| T-04-33 | ElevationOfPrivilege | Public endpoint accidentally requires auth → users locked out | accept | PUBLIC_PATHS extension + authz middleware test (Task 2 Step 8) covers this. Phase 7 has an end-to-end test that every registered route either requires auth OR is in PUBLIC_PATHS. |
| T-04-34 | Repudiation | User claims they never initiated SSO login | mitigate | Existing audit-log entry for `federation_login` (issued by `provision_or_link_user`) captures actor + tenant + federation_config_id + timestamp |
| T-04-35 | InformationDisclosure | Org/tenant enumeration via slug-lookup error timing | mitigate | Slug-lookup failures map to 401 AuthenticationFailed (same as Phase 1 login) — caller cannot distinguish "no such tenant" from "no such federation config"; tests `oidc_start_unknown_org_slug_returns_401` and `oidc_start_unknown_tenant_slug_returns_401` lock this in |
</threat_model>

<verification>
- `cargo test -p axiam-api-rest --test federation_first_time_sso` passes (9 tests including enumeration-resistance).
- `cargo test -p axiam-db --test federation_login_state` passes.
- Manual: POST `/api/v1/auth/federation/oidc/start` against a running server with a configured OIDC IdP fixture → receive authorize_url + state; visit the URL; complete IdP login; POST `/api/v1/auth/federation/oidc/callback` with the returned code+state → receive Set-Cookie response and the user appears in `/api/v1/auth/me` afterward.
- `cargo clippy -p axiam-api-rest -p axiam-federation -p axiam-db -p axiam-core -- -D warnings` zero warnings.
</verification>

<success_criteria>
- D-22: All four endpoints registered, public, working.
- D-23: Existing `/api/v1/federation/*` link-account endpoints untouched (verify by running existing integration tests).
- D-24: `federation_login_state` table consumed via insert + consume_by_state; 10-min TTL; cleanup in plan 04-06.
- RESEARCH §6 "Cookies only" recommendation: response shape matches `/auth/login` (Phase 1 D-13).
- REQ-5 SAML+OIDC verifiers (plans 04-02 / 04-03) now reachable to real users via this surface.
- Tenant-by-slug resolution is concrete (no TODO) and mirrors Phase 1 login's enumeration-resistant 401 behaviour.
</success_criteria>

<output>
After completion, create `.planning/phases/04-federation-verification-session-security/04-05-SUMMARY.md`
</output>
</content>
</invoke>