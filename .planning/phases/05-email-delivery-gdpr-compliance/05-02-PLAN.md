---
phase: 05-email-delivery-gdpr-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-amqp/src/connection.rs
  - crates/axiam-amqp/src/messages.rs
  - crates/axiam-amqp/src/lib.rs
autonomous: true
requirements: [REQ-6]

must_haves:
  truths:
    - "A new mail.outbound queue and its dead-letter queue are declared on AMQP startup — all outbound mail travels asynchronously over this queue (D-14)"
    - "OutboundMailMessage serializes/deserializes round-trip with all five MailType variants (password reset, email verification, notification, deletion-cancel, export-ready — D-14)"
    - "The mail.outbound queue is declared with x-dead-letter-exchange pointing at the DLQ so exhausted-retry mail dead-letters rather than being dropped (D-14)"
  artifacts:
    - path: "crates/axiam-amqp/src/messages.rs"
      provides: "OutboundMailMessage + MailType enum (the five async mail types, D-14)"
      contains: "OutboundMailMessage"
    - path: "crates/axiam-amqp/src/connection.rs"
      provides: "MAIL_OUTBOUND + MAIL_OUTBOUND_DLQ queue constants and DLQ-wired declaration (D-14)"
      contains: "MAIL_OUTBOUND"
  key_links:
    - from: "crates/axiam-amqp/src/connection.rs"
      to: "x-dead-letter-exchange"
      via: "FieldTable arg on mail queue declare (D-14 dead-letter path)"
      pattern: "x-dead-letter-exchange"
---

<objective>
Add the AMQP transport for asynchronous mail (D-14): the `OutboundMailMessage` type with the five `MailType` variants, the `axiam.mail.outbound` queue, and the dead-letter queue `axiam.mail.outbound.dlq` wired via `x-dead-letter-exchange`.

Purpose: Decouples request latency from provider latency and unifies retry/dead-lettering. Every mail-enqueuing handler (Plan 04) and the mail consumer (Plan 03) depend on this message shape and these queues.
Output: New message type, two queue constants, DLQ-wired declaration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-email-delivery-gdpr-compliance/05-CONTEXT.md
@.planning/phases/05-email-delivery-gdpr-compliance/05-PATTERNS.md

<interfaces>
Existing queues (connection.rs): AUTHZ_REQUEST, AUTHZ_RESPONSE, AUDIT_EVENTS, NOTIFICATIONS in `mod queues`; ALL_QUEUES array declares them.
Existing message (messages.rs): NotificationEvent { event_type, tenant_id, actor_id, resource_id, timestamp, data } with serde derives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OutboundMailMessage + MailType in messages.rs</name>
  <files>crates/axiam-amqp/src/messages.rs</files>
  <read_first>
    - crates/axiam-amqp/src/messages.rs (FULL — existing NotificationEvent serde pattern, imports for Uuid/DateTime/Utc)
    - 05-PATTERNS.md § messages.rs (exact OutboundMailMessage + MailType shape)
    - 05-RESEARCH.md § AMQP Mail Queue Infrastructure (field semantics, to_address note D-16)
  </read_first>
  <action>
    Add `pub struct OutboundMailMessage { mail_type: MailType, tenant_id: Uuid, org_id: Uuid, user_id: Uuid, to_address: String, template_context: serde_json::Value, attempt_count: u32, enqueued_at: DateTime<Utc> }` with `#[derive(Debug, Clone, Serialize, Deserialize)]`. Add `pub enum MailType { PasswordReset, EmailVerification, Notification, DeletionCancel, ExportReady }` with `#[serde(rename_all = "snake_case")]` and Debug/Clone/Serialize/Deserialize. Document on `to_address`: delivery-only, MUST NOT be logged in audit (D-16). Add a `#[cfg(test)]` round-trip serde test covering all five MailType variants.
  </action>
  <action_note>Implements D-14 (five mail types over AMQP) and encodes the D-16 invariant (to_address present for delivery, excluded from audit).</action_note>
  <verify>
    <automated>cargo test -p axiam-amqp messages 2>&1 | tee /tmp/v05-02-t1.txt; grep -Ev '^#' /tmp/v05-02-t1.txt | grep -Eq 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'pub struct OutboundMailMessage' crates/axiam-amqp/src/messages.rs`
    - All five variants present: grep messages.rs for PasswordReset, EmailVerification, Notification, DeletionCancel, ExportReady
    - serde round-trip test passes: `cargo test -p axiam-amqp messages` shows `test result: ok`
    - `cargo check -p axiam-amqp --tests` no errors in output
  </acceptance_criteria>
  <done>OutboundMailMessage + MailType exist, serde round-trip tested.</done>
</task>

<task type="auto">
  <name>Task 2: mail.outbound queue + DLQ declaration in connection.rs</name>
  <files>crates/axiam-amqp/src/connection.rs, crates/axiam-amqp/src/lib.rs</files>
  <read_first>
    - crates/axiam-amqp/src/connection.rs (FULL — `mod queues`, ALL_QUEUES array, declare_queues method, AMQPValue::LongString usage)
    - crates/axiam-amqp/src/lib.rs (re-exports — confirm whether queues/messages need re-export for downstream crates)
    - 05-PATTERNS.md § connection.rs (DLQ declaration pattern with x-dead-letter-exchange)
    - 05-RESEARCH.md Open Question 3 (do not assume broker-level DLQ routing — declare explicitly)
  </read_first>
  <action>
    Add to `mod queues`: `pub const MAIL_OUTBOUND: &str = "axiam.mail.outbound";` and `pub const MAIL_OUTBOUND_DLQ: &str = "axiam.mail.outbound.dlq";`. Add MAIL_OUTBOUND_DLQ to ALL_QUEUES (declared as a plain durable queue, no DLQ args). Declare MAIL_OUTBOUND explicitly (not via the plain ALL_QUEUES loop) with a FieldTable carrying `"x-dead-letter-exchange" = AMQPValue::LongString(MAIL_OUTBOUND_DLQ.into())` per the 05-PATTERNS.md pattern. Ensure messages + queues are reachable by downstream crates (re-export OutboundMailMessage/MailType + queues from lib.rs if not already public).
  </action>
  <action_note>Implements D-14 dead-letter path. Explicit DLQ declaration (Open Question 3 — never assume broker pre-config).</action_note>
  <verify>
    <automated>cargo check -p axiam-amqp --tests 2>&1 | tee /tmp/v05-02-t2.txt; grep -Eiq 'error\[|error:' /tmp/v05-02-t2.txt && echo FAIL || echo OK; grep -q 'x-dead-letter-exchange' crates/axiam-amqp/src/connection.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MAIL_OUTBOUND' crates/axiam-amqp/src/connection.rs` and `MAIL_OUTBOUND_DLQ` present
    - `grep -q 'x-dead-letter-exchange' crates/axiam-amqp/src/connection.rs`
    - MAIL_OUTBOUND_DLQ appears in ALL_QUEUES; MAIL_OUTBOUND declared separately with the FieldTable arg (visual confirm in declare_queues)
    - `cargo check -p axiam-amqp --tests` output text shows no `error[`/`error:` lines
    - OutboundMailMessage reachable from axiam-amqp public API (grep lib.rs re-export or confirm `pub mod messages`)
  </acceptance_criteria>
  <done>Both queues declared on startup; main queue dead-letters to the DLQ; types public for downstream crates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AMQP message payload → consumer | to_address travels in the message but must never reach audit (D-16) |
| failed delivery → DLQ | exhausted retries must not silently drop mail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-mail-drop | Denial of Service | mail.outbound delivery failures | mitigate | x-dead-letter-exchange to DLQ + delivery_failed audit (consumer, Plan 03); persistent delivery mode on publish |
| T-5-addr-leak | Information Disclosure | to_address in message | mitigate | Field documented delivery-only; D-16 audit keys on user_id (enforced in Plan 03 consumer) |
| T-5-SC | Tampering | cargo installs | accept | No new packages — lapin/serde_json already workspace deps |
</threat_model>

<verification>
- `cargo test -p axiam-amqp messages` green
- `cargo check -p axiam-amqp --tests` no errors
- `cargo fmt` + `cargo clippy -p axiam-amqp -- -D warnings` clean before commit
</verification>

<success_criteria>
OutboundMailMessage/MailType serde round-trip green; mail.outbound + DLQ declared with x-dead-letter-exchange; types public for Plan 03/04 consumption.
</success_criteria>

<output>
Create `.planning/phases/05-email-delivery-gdpr-compliance/05-02-SUMMARY.md` when done.
</output>
</content>
