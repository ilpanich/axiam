---
phase: 06-ci-cd-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deny.toml
  - .hadolint.yaml
  - .github/dependabot.yml
  - .github/workflows/ci.yml
  - Cargo.toml
  - Cargo.lock
  - frontend/package.json
  - frontend/package-lock.json
autonomous: false
requirements: [REQ-9]
user_setup: []

must_haves:
  # CONTEXT.md decisions covered by this plan: D-01, D-02, D-03, D-04, D-05, D-07, D-16
  truths:
    - "A PR with a known vulnerable dependency fails CI"
    - "Container image scan (fs/config) and Dockerfile lint run on every PR"
    - "License metadata reports Apache-2.0 in all four locations"
    - "The 36 existing Dependabot vulns are remediated (cargo audit + npm audit exit 0)"
  artifacts:
    - path: "deny.toml"
      provides: "cargo-deny advisories/licenses/bans/sources policy"
      contains: "[advisories]"
    - path: ".github/dependabot.yml"
      provides: "weekly grouped updates for cargo, npm, github-actions"
      contains: "github-actions"
    - path: ".github/workflows/ci.yml"
      provides: "security-scan job (cargo-audit, cargo-deny, npm audit, hadolint, trivy fs/config + SARIF upload)"
      contains: "security-scan"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "deny.toml"
      via: "EmbarkStudios/cargo-deny-action reads workspace deny.toml"
      pattern: "cargo-deny-action"
    - from: ".github/workflows/ci.yml"
      to: "GitHub Security tab"
      via: "github/codeql-action/upload-sarif@v4"
      pattern: "codeql-action/upload-sarif@v4"
---

<objective>
Wire all PR-time dependency, license, and source/Dockerfile security scanning into CI, create the cargo-deny policy, fix the project's license metadata to Apache-2.0, add Dependabot, and remediate the 36 existing Dependabot vulnerabilities.

Purpose: Make CI automatically catch vulnerable dependencies, unclear/forbidden licenses, and Dockerfile misconfigurations before merge (REQ-9 success criterion #1, #4 partial).
Output: deny.toml, .hadolint.yaml, .github/dependabot.yml, new ci.yml security-scan job, corrected license fields, remediated lockfiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-CONTEXT.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-RESEARCH.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-PATTERNS.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix license metadata + create deny.toml + dependabot.yml</name>
  <read_first>
    - Cargo.toml (line 23 license field) — current value `AGPL-3.0-or-later`
    - frontend/package.json (no license field today)
    - LICENSE (canonical Apache-2.0 — source of truth)
    - 06-PATTERNS.md §"License Fix Locations (D-04)" and §"No Analog Found" (deny.toml / dependabot.yml have no analog — use RESEARCH templates)
    - 06-RESEARCH.md §"deny.toml — Full Template" (lines ~1003-1046) and §"dependabot.yml" (lines ~1147-1178)
  </read_first>
  <action>
    Set `Cargo.toml:23` `license = "Apache-2.0"` (per D-04, was AGPL-3.0-or-later). Add `"license": "Apache-2.0"` to frontend/package.json (D-04). Create `deny.toml` at workspace root from the RESEARCH template (D-02/D-03): `[advisories]` version=2, vulnerability="deny", unmaintained/yanked/notice="warn", empty `ignore=[]` with the documented per-CVE comment format (id + reason + review date); `[licenses]` version=2 permissive allowlist (MIT, Apache-2.0, Apache-2.0 WITH LLVM-exception, BSD-2-Clause, BSD-3-Clause, ISC, Unicode-3.0, Unicode-DFS-2016, Zlib, MPL-2.0, CC0-1.0) confidence-threshold=0.8 empty exceptions; `[bans]` multiple-versions="warn"; `[sources]` allow-registry crates.io-index. Create `.github/dependabot.yml` version 2 with three ecosystems (cargo dir `/`, npm dir `/frontend`, github-actions dir `/`), weekly Monday schedule, grouped minor+patch for cargo and npm (D-16). Do NOT add the Dockerfile.server/Dockerfile.frontend label license fixes here — those belong to Plan 06-03 which owns the Dockerfiles.
  </action>
  <verify>
    <automated>grep -q 'license = "Apache-2.0"' Cargo.toml && grep -q '"license": "Apache-2.0"' frontend/package.json && grep -q '\[advisories\]' deny.toml && grep -qE 'cargo|npm|github-actions' .github/dependabot.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Apache-2.0' Cargo.toml frontend/package.json` shows a hit in each file; no `AGPL` remains in Cargo.toml license field.
    - deny.toml contains `[advisories]`, `[licenses]`, `[bans]`, `[sources]` sections; license allowlist includes MIT and Apache-2.0; `ignore` is `[]` with the documented exception comment format present.
    - `.github/dependabot.yml` lists exactly the three ecosystems cargo/npm/github-actions (`grep -E 'cargo|npm|github-actions' .github/dependabot.yml` returns 3 ecosystem lines).
  </acceptance_criteria>
  <done>License is Apache-2.0 in Cargo.toml + frontend/package.json; deny.toml and dependabot.yml exist and are well-formed.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate 36 Dependabot vulns (cargo + npm) and reconcile deny.toml</name>
  <read_first>
    - .planning/STATE.md §Blockers/Concerns (36 vulns: 13 high / 15 moderate / 8 low)
    - 06-RESEARCH.md §"Research Area 8: D-01 — 36-Vuln Remediation Strategy" (cargo + npm strategy, advisories.ignore syntax)
    - Cargo.lock, frontend/package-lock.json (current dependency graph)
    - deny.toml (created in Task 1)
  </read_first>
  <action>
    Cargo side (D-01): run `cargo update` to pull semver-compatible fixes; for vulns needing a major bump, edit `Cargo.toml` deps directly and resolve API breakage in the affected crates (build per-crate with `-p`, never full workspace). For any advisory with NO upstream fix, add a narrow `deny.toml` `[advisories].ignore` entry carrying the RUSTSEC id + justification + review date (D-02) — never a blanket baseline. npm side (D-01/D-05): in `frontend/`, run `npm audit fix`, then `npm audit fix --force` only for reviewed semver-breaking fixes, verify residual with `npm audit --audit-level=high`. Iterate until `cargo audit`, `cargo deny check advisories`, and `npm audit --audit-level=high` all exit 0 (or every residual is a documented deny.toml ignore). Run `cargo fmt` + `cargo clippy -D warnings` on any crate whose source changed.
  </action>
  <verify>
    <automated>cargo audit && cargo deny check advisories && (cd frontend && npm audit --audit-level=high)</automated>
  </verify>
  <acceptance_criteria>
    - `cargo audit` exits 0 (or only reports advisories that have a matching documented `ignore` entry in deny.toml).
    - `cargo deny check advisories` exits 0.
    - `cd frontend && npm audit --audit-level=high` exits 0.
    - Any `deny.toml` `ignore` entry has a RUSTSEC id, a justification string, and a review date.
    - Changed Rust crates pass `cargo clippy -D warnings`.
  </acceptance_criteria>
  <done>All 36 vulns remediated or documented-ignored; the three audit commands exit clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add security-scan job to ci.yml (cargo-audit, cargo-deny, npm audit, hadolint, trivy fs/config + SARIF)</name>
  <read_first>
    - .github/workflows/ci.yml (existing jobs: fmt, clippy, build, build-no-saml, test; top-level env RUSTFLAGS=-Dwarnings, permissions contents:read)
    - 06-PATTERNS.md §".github/workflows/ci.yml — add security-scan job" (job shape, security-events:write, MUST NOT break build-no-saml)
    - 06-RESEARCH.md §"Research Area 2" (action pins) and §"CI scan job snippet (ci.yml addition)" (lines ~1050-1143)
    - .hadolint.yaml (to be created here)
  </read_first>
  <action>
    Create `.hadolint.yaml` at workspace root suppressing only rules that conflict with the intentional digest-pinned design (e.g. DL3008) — keep it minimal. Add a new `security-scan` job to `.github/workflows/ci.yml` with `permissions: contents: read, security-events: write` and NO `needs:` (parallel with existing jobs). Steps: checkout@v4; dtolnay/rust-toolchain@stable; Swatinem/rust-cache@v2; apt install protobuf-compiler libxmlsec1-dev libxml2-dev; `actions-rust-lang/audit@v1` (cargo-audit); `EmbarkStudios/cargo-deny-action@v2` command=check arguments=`--all-features`; npm audit (`cd frontend && npm ci --ignore-scripts && npm audit --audit-level=high`); `hadolint/hadolint-action@v3.1.0` on both Dockerfiles with `format: sarif`, `no-fail: true`; `aquasecurity/trivy-action@v0.36.0` scan-type=fs (severity HIGH,CRITICAL, ignore-unfixed:true, exit-code:1, sarif output) and scan-type=config (sarif, exit-code:0 advisory); upload every SARIF via `github/codeql-action/upload-sarif@v4` with `if: always()` and distinct `category:` per file (D-07). Do NOT modify the existing `build-no-saml` guard job. Pin third-party actions to commit SHAs where practical (note tags above each SHA for readability per D-05 hardening). Verify the trivy-action version at github.com/aquasecurity/trivy-action/releases before pinning (research flagged v0.36.0 as ASSUMED).
  </action>
  <verify>
    <automated>grep -q 'security-scan' .github/workflows/ci.yml && grep -q 'security-events: write' .github/workflows/ci.yml && grep -q 'codeql-action/upload-sarif@v4' .github/workflows/ci.yml && grep -q 'build-no-saml' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains a `security-scan` job with `security-events: write`.
    - cargo-deny, cargo-audit, npm audit, hadolint (both Dockerfiles), trivy fs + trivy config steps present.
    - All SARIF uploads use `github/codeql-action/upload-sarif@v4` (not v3) with distinct categories.
    - The `build-no-saml` guard job is still present and unmodified.
    - `.hadolint.yaml` exists at workspace root.
  </acceptance_criteria>
  <done>PR-time security-scan job added; build-no-saml untouched; SARIF uploads on v4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>PR-time security scanning (cargo-audit, cargo-deny, npm audit, hadolint, trivy fs/config) wired into ci.yml with SARIF upload; license metadata fixed; 36 vulns remediated.</what-built>
  <how-to-verify>
    Required because success criterion #1 ("a PR with a known vulnerable dependency fails CI") needs a live CI proof that cannot be asserted from a local grep:
    1. On a throwaway branch, add a known-RUSTSEC-flagged crate version (e.g. an old `time` or other advisory-listed dep) to a workspace Cargo.toml.
    2. Push the branch and open a PR. Confirm the `security-scan` job goes RED (cargo-audit/cargo-deny fail).
    3. Confirm SARIF results appear under the repo's Security tab (trivy/hadolint categories).
    4. Delete the throwaway branch.
    Also confirm a clean PR (no planted vuln) shows `security-scan` GREEN.
  </how-to-verify>
  <resume-signal>Type "approved" with the failing-PR run URL, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream registry → workspace lockfile | third-party crate/npm code enters the build via Cargo.lock / package-lock.json |
| Dockerfile source → built image | misconfigured Dockerfile instructions become runtime attack surface |
| PR author → main branch | untrusted dependency/version changes attempt to merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | crate/npm dependency graph | mitigate | cargo-audit + cargo-deny advisories + npm audit fail CI on known vulns (D-01/D-05); 36 existing vulns remediated |
| T-06-02 | Tampering | dependency license/source | mitigate | cargo-deny licenses (permissive allowlist, deny UNKNOWN) + sources (crates.io only) (D-03) |
| T-06-03 | Tampering | Dockerfile source | mitigate | hadolint + trivy config scan on PR, SARIF to Security tab (D-05/D-07) |
| T-06-04 | Information Disclosure | source tree / lockfiles | mitigate | trivy fs scan flags vulnerable deps and leaked secrets at PR time (D-05) |
| T-06-SC | Tampering | npm install (vite-plugin-sri3) | transfer | package legitimacy verified in RESEARCH §Package Legitimacy Audit (npm registry confirmed); install handled in Plan 06-04 |
| T-06-LIC | Repudiation | project license metadata | mitigate | correct license to Apache-2.0 (D-04) so distribution/compliance claims are accurate |
</threat_model>

<verification>
- `cargo audit && cargo deny check && (cd frontend && npm audit --audit-level=high)` all exit 0.
- License grep passes across Cargo.toml + frontend/package.json.
- security-scan job present; build-no-saml intact.
- Human checkpoint: planted-vuln PR goes red on CI; SARIF visible in Security tab.
</verification>

<success_criteria>
ROADMAP success criterion #1 (vulnerable-dep PR fails CI) proven; PR-time scan coverage (criterion #4 partial) in place; license metadata corrected; 36 vulns remediated.
</success_criteria>

<output>
Create `.planning/phases/06-ci-cd-infrastructure-hardening/06-01-SUMMARY.md` when done.
</output>
