---
phase: 06-ci-cd-infrastructure-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/Dockerfile.server
  - docker/Dockerfile.frontend
  - docker/docker-compose.prod.yml
  - crates/axiam-server/src/main.rs
  - crates/axiam-server/Cargo.toml
autonomous: false
requirements: [REQ-10]
user_setup: []

must_haves:
  truths:
    - "Server runtime image uses distroless/cc-debian12 and still runs SAML-ON (xmlsec .so present)"
    - "Docker images run as non-root with a minimal base image"
    - "The distroless image health is probed via the axiam-server healthcheck subcommand (no curl/shell)"
    - "Both Dockerfile base images are digest-pinned and labelled Apache-2.0"
  artifacts:
    - path: "docker/Dockerfile.server"
      provides: "distroless runtime stage + COPY'd xmlsec .so + HEALTHCHECK subcommand"
      contains: "gcr.io/distroless/cc-debian12"
    - path: "crates/axiam-server/src/main.rs"
      provides: "healthcheck subcommand self-probing /health, exit 0/1"
      contains: "healthcheck"
    - path: "docker/docker-compose.prod.yml"
      provides: "healthcheck test using axiam-server healthcheck"
      contains: "healthcheck"
  key_links:
    - from: "docker/Dockerfile.server (HEALTHCHECK)"
      to: "crates/axiam-server healthcheck subcommand"
      via: "CMD axiam-server healthcheck"
      pattern: "axiam-server.*healthcheck"
    - from: "Dockerfile.server runtime stage"
      to: "builder stage xmlsec .so files"
      via: "COPY --from=builder /usr/lib/x86_64-linux-gnu/libxmlsec1*"
      pattern: "COPY --from=builder.*libxmlsec1"
---

<objective>
Migrate the server runtime image to gcr.io/distroless/cc-debian12 while keeping SAML-ON by COPYing the xmlsec native libraries from the builder stage, replace the curl health probe with an `axiam-server healthcheck` subcommand, digest-pin the frontend base images, and fix the Dockerfile license labels to Apache-2.0 (D-08/D-09/D-10/D-04).

Purpose: Docker images on a minimal non-root base with a working healthcheck (REQ-10 success criterion #2).
Output: Rewritten Dockerfile.server runtime stage, digest-pinned Dockerfile.frontend, healthcheck subcommand, updated docker-compose.prod.yml healthcheck.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-CONTEXT.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-RESEARCH.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-PATTERNS.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-VALIDATION.md

<interfaces>
<!-- reqwest is already a dependency of axiam-server. The blocking feature may need enabling. -->
From crates/axiam-server/Cargo.toml: reqwest dep (check for "blocking" feature; workspace dep at Cargo.toml uses features ["json","rustls-tls"]).
Healthcheck target default: http://127.0.0.1:8090/health (override via AXIAM_HEALTHCHECK_URL).
distroless/cc-debian12:nonroot runs as UID 65532 (username "nonroot").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add healthcheck subcommand to axiam-server</name>
  <read_first>
    - crates/axiam-server/src/main.rs (`#[tokio::main]` entry point, ~line 77+; currently no CLI arg parsing)
    - crates/axiam-server/Cargo.toml (reqwest dep — confirm whether `blocking` feature is enabled)
    - 06-PATTERNS.md §"crates/axiam-server/src/main.rs — add healthcheck subcommand" (minimal std::env::args() check before async runtime; reqwest::blocking OR std::net::TcpStream raw-HTTP fallback)
    - 06-RESEARCH.md §"Research Area 4: D-08/D-09" (healthcheck subcommand notes)
  </read_first>
  <action>
    In `crates/axiam-server/src/main.rs`, before tracing init and before the async stack spins up, check `std::env::args()`: if argv[1] == "healthcheck", GET `AXIAM_HEALTHCHECK_URL` (default `http://127.0.0.1:8090/health`), exit 0 on a 2xx response, exit 1 otherwise (D-09). Prefer `reqwest::blocking` (already-present dep) — if the `blocking` feature is not enabled, add it to the axiam-server-specific reqwest dependency in `crates/axiam-server/Cargo.toml` (do not change the workspace-wide default features). If adding the feature is undesirable, use the `std::net::TcpStream` raw-HTTP fallback from PATTERNS. Code MUST be warning-clean (`-Dwarnings` in CI). Add a unit/integration test asserting the subcommand exits 0 against a healthy server and 1 against an unreachable URL (point `AXIAM_HEALTHCHECK_URL` at an unused port). Run `cargo fmt` + `cargo clippy -D warnings -p axiam-server`.
  </action>
  <verify>
    <automated>cargo test -p axiam-server healthcheck && cargo clippy -D warnings -p axiam-server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `axiam-server healthcheck` exits 0 when the server's /health returns 200, exits 1 when unreachable.
    - `cargo test -p axiam-server healthcheck` passes.
    - `cargo clippy -D warnings -p axiam-server` is clean (no warnings).
    - The arg check runs before the tokio async stack and tracing init.
  </acceptance_criteria>
  <done>healthcheck subcommand implemented, tested, warning-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Distroless Dockerfile.server runtime + Dockerfile.frontend digest pins + license labels</name>
  <read_first>
    - docker/Dockerfile.server (builder rust:1.94-bookworm digest-pinned lines 11-70 KEEP; runtime debian:bookworm-slim stage lines 75-118; license label line 81; HEALTHCHECK lines 115-116; ENTRYPOINT line 118)
    - docker/Dockerfile.frontend (node:24-alpine builder line 11; nginx-unprivileged:1.29-alpine runtime line 29; license label line 43)
    - 06-PATTERNS.md §"docker/Dockerfile.server — distroless runtime" and §"docker/Dockerfile.frontend — digest-pin base images, fix license label"
    - 06-RESEARCH.md §"Research Area 4" (cc variant contents, .so COPY list, ldd verification, Pitfall 1) and §"Assumptions Log" A3/A4 (.so minor versions may differ — COPY by versionless soname, verify with ldd)
  </read_first>
  <action>
    Dockerfile.server (D-08): keep the builder stage unchanged. Replace the runtime base with `FROM gcr.io/distroless/cc-debian12:nonroot@sha256:<PIN>` (resolve and pin the current digest). Remove the runtime apt-install + user-creation block (distroless has no apt; nonroot UID 65532 is built in). Before the binary COPY, COPY the xmlsec runtime libs from the builder by versionless soname path (`libxmlsec1.so.1`, `libxmlsec1-openssl.so.1`, `libxml2.so.2`, `libltdl.so.7`, `liblzma.so.5`, and ICU `libicuuc.so.*`/`libicudata.so.*` if ldd shows them needed) into `/usr/lib/x86_64-linux-gnu/` — do NOT hardcode minor versions (A3/A4). Add a `RUN ldd /build/target/release/axiam-server` step in the builder to enumerate actual runtime deps and COPY exactly that delta over what the cc variant already provides (glibc, libgcc, libssl3) — Pitfall 1. COPY the binary with `--chown=65532:65532` to `/usr/local/bin/axiam-server`; set `USER nonroot`; set HEALTHCHECK `CMD ["/usr/local/bin/axiam-server", "healthcheck"]` (D-09); set `ENTRYPOINT ["/usr/local/bin/axiam-server"]`; fix the license label to `org.opencontainers.image.licenses="Apache-2.0"` (D-04). Dockerfile.frontend (D-10/D-04): digest-pin `node:24-alpine@sha256:<PIN>` and `nginxinc/nginx-unprivileged:1.29-alpine@sha256:<PIN>`; fix license label to Apache-2.0.
  </action>
  <verify>
    <automated>grep -q 'gcr.io/distroless/cc-debian12' docker/Dockerfile.server && grep -q 'COPY --from=builder.*libxmlsec1' docker/Dockerfile.server && grep -q 'axiam-server", "healthcheck' docker/Dockerfile.server && grep -q 'Apache-2.0' docker/Dockerfile.server && grep -q '@sha256:' docker/Dockerfile.frontend && grep -q 'Apache-2.0' docker/Dockerfile.frontend</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile.server runtime base is `gcr.io/distroless/cc-debian12:nonroot@sha256:...` (digest-pinned).
    - xmlsec `.so` files COPY'd from builder; a builder `RUN ldd` step verifies the binary's actual deps.
    - HEALTHCHECK runs `axiam-server healthcheck`; USER is nonroot (65532); ENTRYPOINT is absolute path.
    - No `org.opencontainers.image.licenses="AGPL-3.0-or-later"` remains in either Dockerfile; both are Apache-2.0.
    - Dockerfile.frontend node + nginx bases are digest-pinned (`@sha256:`).
  </acceptance_criteria>
  <done>Distroless server runtime with SAML libs, healthcheck, Apache labels; frontend bases digest-pinned and relabelled.</done>
</task>

<task type="auto">
  <name>Task 3: Update docker-compose.prod.yml healthcheck to subcommand</name>
  <read_first>
    - docker/docker-compose.prod.yml (server healthcheck block ~lines 49-54, currently curl-based; environment block lines 26-43)
    - 06-PATTERNS.md §"docker/docker-compose.prod.yml — cookie Secure flag env override" (healthcheck replacement shown; NOTE: the cookie env override (D-18) is owned by Plan 06-04 via docker-compose.dev.yml — do NOT add the cookie env here)
  </read_first>
  <action>
    Replace the server service `healthcheck.test` in docker-compose.prod.yml with `["CMD", "/usr/local/bin/axiam-server", "healthcheck"]` (D-09), keeping interval/timeout/retries/start_period. Do NOT add `AXIAM__AUTH__COOKIE_SECURE` here — that is Plan 06-04's responsibility on docker-compose.dev.yml (avoids file/decision overlap).
  </action>
  <verify>
    <automated>grep -q 'axiam-server", "healthcheck' docker/docker-compose.prod.yml && ! grep -q 'curl' docker/docker-compose.prod.yml</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.prod.yml server healthcheck uses `axiam-server healthcheck` (no curl).
    - No cookie-secure env var added in this file (owned by 06-04).
  </acceptance_criteria>
  <done>Prod compose healthcheck uses the subcommand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Distroless server image (SAML-ON via COPY'd xmlsec .so), healthcheck subcommand, digest-pinned frontend bases, Apache-2.0 labels.</what-built>
  <how-to-verify>
    Required because distroless has no shell — runtime .so completeness can only be proven by building and starting the image (Manual-Only Verifications, REQ-10/D-08):
    1. Build: `docker build -f docker/Dockerfile.server -t axiam-server:p6test .` — confirm the builder `ldd` step lists the xmlsec deps and the build succeeds.
    2. Start the container against a dev DB/broker; confirm NO `error while loading shared libraries` at startup (Pitfall 1) and the server reaches /health.
    3. `docker run --rm axiam-server:p6test healthcheck` against a running instance exits 0 (and exits 1 when /health is down).
    4. Confirm the image runs as UID 65532 (`docker inspect` / process user is nonroot).
  </how-to-verify>
  <resume-signal>Type "approved" with ldd output + startup log, or describe the missing .so / failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container process → host kernel | a compromised app process attempts to escape the container |
| OS base image → runtime | shell/package-manager presence widens post-exploit tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Elevation of Privilege | server container runtime | mitigate | distroless/cc-debian12:nonroot (UID 65532), no shell, no package manager (D-08) |
| T-06-10 | Tampering | base image identity | mitigate | digest-pinned FROM (server runtime + frontend node/nginx) (D-08/D-10) |
| T-06-11 | Denial of Service | broken SAML runtime (missing .so) | mitigate | COPY xmlsec .so from builder + builder `ldd` verification (D-08, Pitfall 1) |
| T-06-12 | Repudiation | image license label | mitigate | correct OCI license label to Apache-2.0 (D-04) |
</threat_model>

<verification>
- Dockerfile greps: distroless base, libxmlsec1 COPY, healthcheck CMD, Apache-2.0 (server); @sha256 + Apache-2.0 (frontend).
- `cargo test -p axiam-server healthcheck` passes; clippy clean.
- prod compose healthcheck uses subcommand, no curl.
- Human checkpoint: image builds, starts with no missing-.so error, healthcheck exits 0, runs as 65532.
</verification>

<success_criteria>
ROADMAP success criterion #2 (non-root + minimal base image) satisfied; SAML-ON preserved; healthcheck works in distroless.
</success_criteria>

<output>
Create `.planning/phases/06-ci-cd-infrastructure-hardening/06-03-SUMMARY.md` when done.
</output>
