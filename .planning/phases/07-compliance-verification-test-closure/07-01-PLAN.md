---
phase: 07-compliance-verification-test-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-pki/tests/ca_test.rs
  - crates/axiam-pki/tests/cert_test.rs
  - crates/axiam-pki/tests/mtls_test.rs
  - crates/axiam-pki/tests/pgp_test.rs
autonomous: true
requirements: [REQ-11]
must_haves:
  truths:
    - "CA keypair generation + self-signed cert produces a valid PEM and fingerprint"
    - "A leaf cert signed by a CA is rejected when the CA is revoked/inactive or outside its validity window"
    - "mTLS device authentication rejects unknown-fingerprint, expired, and inactive certs"
    - "A PGP audit signature can be produced and verified against its public key (roundtrip)"
    - "Ed25519Legacy PGP keys are rejected for encryption (signing-only)"
  artifacts:
    - path: "crates/axiam-pki/tests/ca_test.rs"
      provides: "CA keypair gen + signing happy path + reject case (D-09)"
      contains: "CaService"
    - path: "crates/axiam-pki/tests/cert_test.rs"
      provides: "leaf cert issuance + inactive/expired CA reject"
      contains: "CertService"
    - path: "crates/axiam-pki/tests/mtls_test.rs"
      provides: "DeviceAuthService authenticate + reject cases"
      contains: "DeviceAuthService"
    - path: "crates/axiam-pki/tests/pgp_test.rs"
      provides: "PGP sign+verify roundtrip + Ed25519 encryption reject"
      contains: "PgpService"
  key_links:
    - from: "crates/axiam-pki/tests/*"
      to: "axiam_db::run_migrations + Surreal Mem"
      via: "in-memory DB fixture (authz_engine_test.rs pattern)"
      pattern: "Surreal::new::<Mem>"
---

<objective>
Close the only fully 0-test target crate left: `axiam-pki`. Add critical-path security tests for CA generation, leaf cert issuance/validation chain, mTLS device auth (incl. reject cases), and PGP audit-signing roundtrip — per D-09.

Purpose: REQ-11 AC "PKI/certificate generation tests" and ROADMAP success criterion #4 require axiam-pki coverage. These are security-sensitive code paths (CA key handling, cert chain validation, mTLS trust) with zero tests today.
Output: Four test files under `crates/axiam-pki/tests/` asserting real behavior on the four PKI services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-compliance-verification-test-closure/07-RESEARCH.md
@.planning/phases/07-compliance-verification-test-closure/07-PATTERNS.md

<interfaces>
PKI service surfaces under test (from 07-RESEARCH.md § Research Target 3 — VERIFIED from source):

CaService<R: CaCertificateRepository> — ca.rs
  .generate(CreateCaCertificate{organization_id, subject, key_algorithm, validity_days}) -> GeneratedCaCertificate
  .revoke(org_id, id) -> ()
CertService<CA, CR> — cert.rs
  .generate(org_id, input, max_validity_days) -> GeneratedCertificate  // rejects inactive/expired CA, caps validity
DeviceAuthService<CR> — mtls.rs
  .authenticate(pem: &str) -> DeviceIdentity   // rejects invalid PEM, unknown fingerprint, expired, inactive
PgpService<R> — pgp.rs
  .generate(CreatePgpKey{tenant_id,name,email,algorithm,purpose}) -> GeneratedPgpKey
  .sign_audit_batch(tenant_id, entries) -> SignedAuditBatch
  .encrypt_for_export(tenant_id, key_id, plaintext) -> EncryptedExport  // rejects Ed25519 (pgp.rs:173)

rcgen 0.13: CertificateParams::new(...) has NO key_pair field; use params.self_signed(&key_pair); not_before/not_after use time::OffsetDateTime.
pgp 0.19: KeyType::Ed25519Legacy = signing only; KeyType::Rsa(4096) for encryption; SecretKeyParamsBuilder::default()...build()?.generate(OsRng) returns SignedSecretKey directly; use rand_core::OsRng (rand_core 0.6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CA + leaf cert tests (ca_test.rs, cert_test.rs)</name>
  <files>crates/axiam-pki/tests/ca_test.rs, crates/axiam-pki/tests/cert_test.rs</files>
  <read_first>
    - crates/axiam-pki/src/ca.rs (CaService::generate, rcgen 0.13 call sites at lines 68-101, AES-GCM key encryption)
    - crates/axiam-pki/src/cert.rs (CertService::generate, reject paths: inactive CA, expired CA, validity cap at lines 129-132)
    - crates/axiam-authz/tests/authz_engine_test.rs lines 34-42 (Mem DB + run_migrations setup pattern)
    - crates/axiam-core/src/models/certificate.rs (CreateCaCertificate, KeyAlgorithm, error variants)
    - 07-PATTERNS.md § ca_test.rs and § cert_test.rs (analog code excerpts)
  </read_first>
  <action>Create `setup_db()` (Surreal Mem + use_ns/use_db("test") + axiam_db::run_migrations) and `test_pki_config()` (PkiConfig{encryption_key:[0u8;32]}) shared by both files. In ca_test.rs assert: (a) `CaService::generate` with KeyAlgorithm::Ed25519 returns non-empty private_key_pem, a public_cert_pem containing "CERTIFICATE", and a non-empty fingerprint; (b) generate with validity_days=0 returns Err. In cert_test.rs assert: (a) a leaf cert generated against an active CA returns a GeneratedCertificate whose cert PEM contains "CERTIFICATE"; (b) after `CaService::revoke`, `CertService::generate` returns Err (inactive CA reject); (c) storing a CA row with `not_after` in the past via the repo then calling `CertService::generate` returns Err (expired CA reject). Use `params.self_signed(&key_pair)` style only — there is NO `params.key_pair` field (rcgen 0.13). Use `time::OffsetDateTime` for dates. Keep warning-clean (no unused imports; CI sets RUSTFLAGS=-Dwarnings).</action>
  <verify>
    <automated>cargo test -p axiam-pki --test ca_test --test cert_test</automated>
  </verify>
  <done>ca_test.rs and cert_test.rs compile warning-clean and all assertions pass; the inactive-CA and expired-CA reject cases produce Err, not Ok.</done>
</task>

<task type="auto">
  <name>Task 2: mTLS reject cases + PGP roundtrip (mtls_test.rs, pgp_test.rs)</name>
  <files>crates/axiam-pki/tests/mtls_test.rs, crates/axiam-pki/tests/pgp_test.rs</files>
  <read_first>
    - crates/axiam-pki/src/mtls.rs (DeviceAuthService::authenticate validation chain + the exact error variant for expired vs unknown fingerprint)
    - crates/axiam-pki/src/pgp.rs (PgpService; pgp 0.19 sign at lines 135-141, encrypt at 183-191, Ed25519 encryption reject at line 173, imports at 1-21)
    - crates/axiam-authz/tests/authz_engine_test.rs lines 34-42 (Mem DB setup)
    - 07-PATTERNS.md § mtls_test.rs and § pgp_test.rs (analog excerpts; OsRng note)
  </read_first>
  <action>In mtls_test.rs: register a valid cert via the repo and assert `DeviceAuthService::authenticate(valid_pem)` returns a DeviceIdentity; assert authenticate with a PEM whose fingerprint is not in the DB returns Err; insert a cert row with `not_after = Utc::now() - 1 day` via SurrealCertificateRepository then assert authenticate returns Err (read mtls.rs first to assert the SPECIFIC error variant — NotFound vs Validation — do not guess). In pgp_test.rs: generate an Ed25519Legacy AuditSigning key, call `sign_audit_batch`, assert the returned signature is non-empty, then parse it back with `pgp::composed::Message::from_string()` and verify against the key's public key (roundtrip succeeds); generate a second Ed25519 key and assert `encrypt_for_export` returns Err (Ed25519 signing-only, pgp.rs:173); generate an Rsa(4096) Export key and assert `encrypt_for_export(b"secret")` returns Ok with non-empty ciphertext. Use `rand_core::OsRng` (rand_core 0.6), not `rand::thread_rng()`. Warning-clean.</action>
  <verify>
    <automated>cargo test -p axiam-pki --test mtls_test --test pgp_test</automated>
  </verify>
  <done>mtls_test.rs asserts the concrete error variant for expired and unknown-fingerprint certs; pgp_test.rs completes a sign+verify roundtrip and asserts Ed25519 encryption is rejected while RSA4096 encryption succeeds. All warning-clean and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IoT device → mTLS authenticate | untrusted client cert PEM crosses into DeviceAuthService |
| CA private key → encrypted storage | key material at rest must be AES-256-GCM, never plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | DeviceAuthService::authenticate | mitigate | test asserts unknown-fingerprint / expired / inactive certs are rejected (mtls_test.rs) — ASVS V2.9 |
| T-07-02 | Elevation | CertService::generate against revoked CA | mitigate | test asserts inactive/expired CA cannot sign leaf certs (cert_test.rs) — ASVS V4 |
| T-07-03 | Tampering | PgpService key purpose confusion | mitigate | test asserts Ed25519 key rejected for encryption (pgp_test.rs, pgp.rs:173) — ASVS V6 |
| T-07-04 | Information Disclosure | CA private key at rest | accept (verify) | encryption-at-rest is existing behavior (ca.rs:84); CA roundtrip test confirms key material is recoverable from encrypted store — cited in ASVS V6.2.5 |
</threat_model>

<verification>
- `cargo test -p axiam-pki` runs all four test files green.
- Each reject-case test asserts `Err(...)` (or the specific AxiamError variant), not merely `is_err()` where the variant is load-bearing.
- No `#[allow(...)]` added to silence warnings; `cargo clippy -p axiam-pki --tests -- -Dwarnings` clean.
</verification>

<success_criteria>
- axiam-pki has a `tests/` dir with ca/cert/mtls/pgp coverage (ROADMAP SC #4).
- mTLS reject cases (expired, wrong-CA/unknown) and Ed25519 encryption reject are asserted on real behavior.
- PGP sign+verify roundtrip proven (not just "signature non-empty").
</success_criteria>

<output>
Create `.planning/phases/07-compliance-verification-test-closure/07-01-SUMMARY.md` when done.
</output>
