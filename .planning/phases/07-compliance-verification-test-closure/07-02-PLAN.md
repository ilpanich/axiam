---
phase: 07-compliance-verification-test-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-api-rest/tests/oauth2_conformance.rs
  - crates/axiam-api-rest/tests/oidc_conformance.rs
  - docs/compliance/oauth2-rfc-compliance.md
  - docs/compliance/oidc-conformance.md
  - docs/compliance/FINDINGS.md
autonomous: true
requirements: [REQ-11]
must_haves:
  truths:
    - "POST /oauth2/token with code_challenge_method=plain is rejected (S256-only enforcement)"
    - "PKCE verifier outside 43..128 chars is rejected"
    - "A refresh token issued to client A cannot be redeemed by client B"
    - "Token responses include token_type=Bearer"
    - "OIDC discovery doc contains all required OIDC Core fields and excludes alg none"
    - "id_token iss matches the discovery issuer"
  artifacts:
    - path: "crates/axiam-api-rest/tests/oauth2_conformance.rs"
      provides: "RFC 6749/7636 MUST-gap conformance tests"
      contains: "code_challenge_method"
    - path: "crates/axiam-api-rest/tests/oidc_conformance.rs"
      provides: "OIDC Core MUST-gap conformance tests"
      contains: "openid-configuration"
    - path: "docs/compliance/oauth2-rfc-compliance.md"
      provides: "RFC MUST matrix with test citations"
      contains: "RFC 6749"
    - path: "docs/compliance/oidc-conformance.md"
      provides: "OIDC Core MUST matrix with test citations"
      contains: "OIDC Core"
    - path: "docs/compliance/FINDINGS.md"
      provides: "deferred findings register (D-05)"
      contains: "Severity"
  key_links:
    - from: "oauth2_conformance.rs / oidc_conformance.rs"
      to: "/api/v1/oauth2/token + /.well-known/openid-configuration"
      via: "actix test::call_service against real handlers"
      pattern: "test::call_service"
    - from: "docs/compliance/oauth2-rfc-compliance.md"
      to: "test function names"
      via: "evidence citation per MUST row"
      pattern: "conformance"
---

<objective>
Add executable OAuth2 RFC 6749/7636 + OIDC Core 1.0 conformance tests (D-07/D-08) covering the MUST-level behaviors not yet exercised by the existing 37-test `oauth2_flow_test.rs`, and write the markdown MUST-matrix docs (D-01) that cite each test as evidence. Initialize `docs/compliance/FINDINGS.md` (D-05) so any inline-fix-vs-defer decisions surfaced here are recorded.

Purpose: ROADMAP success criteria #2 (OAuth2 RFC compliance) and #3 (OIDC Core conformance). Protocol regressions become CI-caught.
Output: Two conformance test files in axiam-api-rest, two MUST-matrix docs, and the FINDINGS register seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-compliance-verification-test-closure/07-RESEARCH.md
@.planning/phases/07-compliance-verification-test-closure/07-PATTERNS.md

<interfaces>
Existing harness to copy (07-PATTERNS.md § oauth2_conformance.rs — EXACT match):
- oauth2_flow_test.rs lines 1-37 (imports + type alias), lines 124-165 (`test_app!` macro), helpers: test_keypair(), test_auth_config(), setup_db(), create_admin_user(), mint_token(), pkce_challenge(), create_client(), do_authorize(). House style = copy (no shared util module exists).
- `const TEST_PEER: &str = "127.0.0.1:12345";` required + `.peer_addr(TEST_PEER.parse::<SocketAddr>().unwrap())` on every TestRequest (rate-limiter key extractor).

Already-covered MUSTs (cite in docs, DO NOT re-test): full auth-code flow, PKCE S256 happy + verifier mismatch, single-use code, invalid_client/invalid_grant error bodies, discovery doc basics, JWKS, userinfo, id_token claims, alg:none at service layer (req5_oidc_e2e.rs::oidc_rejects_alg_none).

New MUST gaps to test (07-RESEARCH.md § Target 2):
OAuth2: plain method rejected; verifier length bounds (<43 / >128); WWW-Authenticate header on 401; token_type=Bearer; cross-client refresh rejected.
OIDC: discovery required-field completeness; "none" absent from id_token_signing_alg_values_supported; id_token iss == discovery issuer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OAuth2 RFC 6749/7636 conformance tests</name>
  <files>crates/axiam-api-rest/tests/oauth2_conformance.rs</files>
  <read_first>
    - crates/axiam-api-rest/tests/oauth2_flow_test.rs (copy imports lines 1-37, test_app! macro 124-165, all helpers; request pattern 193-212)
    - crates/axiam-api-rest/src/handlers/oauth2.rs (the /oauth2/token + /oauth2/authorize handlers — check whether plain method and WWW-Authenticate are currently handled BEFORE writing the assert)
    - 07-PATTERNS.md § oauth2_conformance.rs (new-test skeletons + peer-addr requirement)
  </read_first>
  <action>Copy the oauth2_flow_test.rs harness + helpers into oauth2_conformance.rs (house style: copy). Add tests asserting: (1) `pkce_plain_method_rejected` — authorize with `code_challenge_method=plain` yields an error response (400 or error in redirect; assert the RFC error code, not just non-200) per RFC 7636 §4.2; (2) `pkce_verifier_too_short_rejected` (verifier <43 chars at token exchange → invalid_grant) and `pkce_verifier_too_long_rejected` (>128 chars) per RFC 7636 §4.1; (3) `invalid_client_returns_www_authenticate_header` — POST /api/v1/oauth2/token with wrong secret asserts `resp.headers().get("WWW-Authenticate").is_some()` AND body["error"]=="invalid_client" per RFC 6749 §5.2; (4) `token_response_includes_bearer_token_type` — happy-path exchange asserts body["token_type"].as_str()==Some("Bearer") (case-insensitive Bearer) per RFC 6749 §7.1; (5) `refresh_token_bound_to_original_client` — issue refresh to client A, redeem with client B credentials → invalid_grant per RFC 6749 §6. If the WWW-Authenticate header or plain-method rejection is found ABSENT and the fix is small+localized → fix it inline in handlers/oauth2.rs (D-04) and note it in FINDINGS.md as a fixed item; if it requires a larger refactor → record a Deferred row in FINDINGS.md instead of forcing the assertion. Include TEST_PEER + .peer_addr on every request. Warning-clean.</action>
  <verify>
    <automated>cargo test -p axiam-api-rest --test oauth2_conformance</automated>
  </verify>
  <done>oauth2_conformance.rs asserts: plain method rejected, verifier length bounds rejected, WWW-Authenticate present on 401 invalid_client, token_type=Bearer in success, cross-client refresh rejected. Any gap found is either inline-fixed (D-04) or has a FINDINGS.md Deferred row — no silently-skipped MUST.</done>
</task>

<task type="auto">
  <name>Task 2: OIDC Core conformance tests + MUST-matrix docs + FINDINGS seed</name>
  <files>crates/axiam-api-rest/tests/oidc_conformance.rs, docs/compliance/oauth2-rfc-compliance.md, docs/compliance/oidc-conformance.md, docs/compliance/FINDINGS.md</files>
  <read_first>
    - crates/axiam-api-rest/tests/oauth2_flow_test.rs (harness reuse + oidc_discovery_document/oidc_jwks_endpoint/oidc_id_token_in_auth_code_flow for field names)
    - crates/axiam-server/tests/req5_oidc_e2e.rs (oidc_rejects_alg_none — to CITE as evidence)
    - crates/axiam-api-rest/src/handlers/oauth2.rs or oidc handler (whether /oauth2/token has a separate alg check path — determines whether to add HTTP test or only cite)
    - 07-PATTERNS.md § oidc_conformance.rs (discovery completeness + base64url id_token decode skeletons) and § docs/compliance (row formats)
  </read_first>
  <action>PREREQUISITE: run `mkdir -p docs/compliance` before writing any compliance doc (this is the first task in the phase that writes into docs/compliance/). In oidc_conformance.rs (copy the same harness/helpers): (1) `discovery_doc_has_all_required_fields` — GET /.well-known/openid-configuration, assert non-null: issuer, authorization_endpoint, token_endpoint, jwks_uri, response_types_supported, subject_types_supported, id_token_signing_alg_values_supported (OIDC Discovery §3); (2) `discovery_doc_excludes_alg_none` — assert id_token_signing_alg_values_supported array contains no "none" (OIDC Discovery §3); (3) `id_token_iss_matches_discovery_issuer` — run full auth-code flow, base64url-decode the id_token payload (no sig verify needed for field check), GET discovery doc, assert claims["iss"]==doc["issuer"] (OIDC Core §3.1.3.7). For alg:none rejection: if /oauth2/token has its own algorithm-check code path, add an HTTP-level test; otherwise cite req5_oidc_e2e.rs::oidc_rejects_alg_none as evidence (Pitfall 8). Then author `docs/compliance/oauth2-rfc-compliance.md` and `docs/compliance/oidc-conformance.md` as MUST-matrices: each MUST row → RFC/OIDC §ref → status (Pass/Deferred) → evidence (test fn name or file:line), covering both the already-existing covered MUSTs and the new ones. Seed `docs/compliance/FINDINGS.md` with the D-05 row schema (# | Finding | Severity | ASVS/RFC Ref | Deferral Rationale | Issue) and add any deferred rows surfaced in Task 1.</action>
  <verify>
    <automated>mkdir -p docs/compliance && cargo test -p axiam-api-rest --test oidc_conformance && test -d docs/compliance</automated>
  </verify>
  <done>oidc_conformance.rs asserts discovery completeness, "none" absence, and iss/issuer cross-match. oauth2-rfc-compliance.md and oidc-conformance.md each have a MUST row per covered behavior with a test-name/file:line evidence cell; FINDINGS.md exists with the D-05 schema and any deferred rows from this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth2 client → /oauth2/token | untrusted client supplies grant params, PKCE verifier, refresh token |
| RP → OIDC discovery/id_token | relying party trusts discovery metadata + id_token signature alg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | PKCE downgrade (plain method) | mitigate | oauth2_conformance.rs asserts plain rejected, S256-only — ASVS V2 / RFC 7636 §4.2 |
| T-07-06 | Elevation | OIDC alg:none bypass | mitigate | assert "none" absent from discovery alg list + cite req5_oidc_e2e.rs::oidc_rejects_alg_none — ASVS V2.7 / OIDC §3.1.3.7 |
| T-07-07 | Spoofing | cross-client refresh token reuse | mitigate | assert refresh token bound to issuing client — RFC 6749 §6 |
| T-07-08 | Information Disclosure | missing WWW-Authenticate / leaky error body | mitigate | assert RFC 6749 §5.2 error shape + WWW-Authenticate on 401 — ASVS V7.4 |
</threat_model>

<verification>
- `cargo test -p axiam-api-rest --test oauth2_conformance --test oidc_conformance` green.
- Every MUST row in the two docs cites a real test fn name or file:line (no "TODO"/empty evidence cell).
- Any inline fix is warning-clean; `cargo clippy -p axiam-api-rest --tests -- -Dwarnings` clean.
</verification>

<success_criteria>
- OAuth2 RFC 6749/7636 MUST matrix is fully covered (existing + new tests), documented with citations (SC #2).
- OIDC Core 1.0 MUST matrix covered + documented (SC #3).
- FINDINGS.md exists with D-05 schema; no MUST silently skipped (fixed inline or deferred-with-rationale).
</success_criteria>

<output>
Create `.planning/phases/07-compliance-verification-test-closure/07-02-SUMMARY.md` when done.
</output>
