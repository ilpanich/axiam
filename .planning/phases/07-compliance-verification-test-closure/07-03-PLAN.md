---
phase: 07-compliance-verification-test-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-api-grpc/build.rs
  - crates/axiam-api-grpc/Cargo.toml
  - crates/axiam-api-grpc/tests/grpc_authz_test.rs
autonomous: true
requirements: [REQ-11]
must_haves:
  truths:
    - "An in-process tonic server can be started on an ephemeral port and reached by a real client channel"
    - "check_access over gRPC returns allowed=true when a role grants the permission and allowed=false otherwise"
    - "check_access with a malformed UUID returns Status::invalid_argument"
    - "batch_check_access returns a mixed allow/deny result set in one RPC"
    - "N concurrent check_access calls all resolve correctly (engine thread-safety)"
  artifacts:
    - path: "crates/axiam-api-grpc/build.rs"
      provides: "feature-gated client stub generation (build_client behind CARGO_FEATURE_CLIENT)"
      contains: "CARGO_FEATURE_CLIENT"
    - path: "crates/axiam-api-grpc/tests/grpc_authz_test.rs"
      provides: "in-process tonic harness + T19.1 authz + T19.2 batch/concurrent"
      contains: "AuthorizationServiceClient"
  key_links:
    - from: "tests/grpc_authz_test.rs"
      to: "AuthorizationServiceImpl + AuthorizationEngine"
      via: "tonic Server::builder().serve_with_incoming_shutdown over TcpListenerStream"
      pattern: "serve_with_incoming_shutdown"
    - from: "build.rs"
      to: "generated client stubs"
      via: "build_client gated on CARGO_FEATURE_CLIENT"
      pattern: "build_client"
---

<objective>
Establish the net-new in-process tonic test harness for `axiam-api-grpc` (D-10) and use it for gRPC authorization integration tests (T19.1) and concurrent batch authorization tests (T19.2). This is the highest-uncertainty item — `build_client(false)` today means no client stubs exist.

Purpose: REQ-11 ACs "gRPC authorization integration tests (T19.1)" and "Concurrent batch authorization tests (T19.2)", ROADMAP success criterion #4 (axiam-api-grpc has integration tests).
Output: feature-gated client generation in build.rs/Cargo.toml + `grpc_authz_test.rs` with a reusable harness and the T19.1/T19.2 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-compliance-verification-test-closure/07-RESEARCH.md
@.planning/phases/07-compliance-verification-test-closure/07-PATTERNS.md

<interfaces>
Production server (07-RESEARCH.md § Target 1 — VERIFIED):
  start_grpc_server<R,P,Res,S,G,U>(addr, engine: AuthorizationEngine<R,P,Res,S,G>, user_repo: U, auth_config, grpc_config) — all 6 generics concrete at callsite.
  AuthorizationServiceImpl::new(engine); AuthorizationServiceServer::new(impl).
  parse_uuid helper (authorization.rs:40-44) → bad UUID returns Status::invalid_argument.

Engine type alias (copy from authz_engine_test.rs:25-32):
  type TestDb = surrealdb::engine::local::Db;
  type TestEngine = AuthorizationEngine<SurrealRoleRepository<TestDb>, SurrealPermissionRepository<TestDb>, SurrealResourceRepository<TestDb>, SurrealScopeRepository<TestDb>, SurrealGroupRepository<TestDb>>;

build.rs target (07-PATTERNS.md § build.rs): build_client gated on std::env::var("CARGO_FEATURE_CLIENT").is_ok(); add [features] client=[] and tokio-stream dev-dep; tests run `--features client`.

Harness (07-PATTERNS.md § grpc_authz_test.rs): Server::builder().add_service(...).serve_with_incoming_shutdown(TcpListenerStream::new(TcpListener::bind("127.0.0.1:0")), rx). OMIT build_grpc_governor_layer (SmartIpKeyExtractor panics with no real peer IP). Client: Channel::from_shared(format!("http://{addr}")).connect().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature-gate client stub generation (build.rs + Cargo.toml)</name>
  <files>crates/axiam-api-grpc/build.rs, crates/axiam-api-grpc/Cargo.toml</files>
  <read_first>
    - crates/axiam-api-grpc/build.rs (current build_client(false) at line 4; proto paths)
    - crates/axiam-api-grpc/Cargo.toml (deps; confirm tonic transport + tokio-stream availability)
    - 07-PATTERNS.md § build.rs (target modification + feature/dev-dep additions)
    - 07-RESEARCH.md § Target 1 "Test Build.rs for Client Generation" (feature-flag approach + fallback A2)
  </read_first>
  <action>Change build.rs so `build_client` is `std::env::var("CARGO_FEATURE_CLIENT").is_ok()` (keep `build_server(true)` and all proto paths). Add to Cargo.toml: `[features] client = []` and a `[dev-dependencies]` entry for `tokio-stream = { workspace = true }` (and any transport feature the client channel needs). Verify `cargo build -p axiam-api-grpc` (no feature) still builds server-only, and `cargo build -p axiam-api-grpc --features client` generates the AuthorizationServiceClient type. If tonic-prost-build 0.14 does NOT honor CARGO_FEATURE_CLIENT (assumption A2), fall back to the separate `axiam-api-grpc-tests` workspace crate with its own build.rs(build_client(true).build_server(false)) and document the choice in the SUMMARY.</action>
  <verify>
    <automated>cargo build -p axiam-api-grpc && cargo build -p axiam-api-grpc --features client</automated>
  </verify>
  <done>Default build is server-only; `--features client` build succeeds and exposes `axiam_api_grpc::proto::authorization_service_client::AuthorizationServiceClient`. (Or fallback test-crate approach documented + building.)</done>
</task>

<task type="auto">
  <name>Task 2: In-process harness + T19.1 authz + T19.2 batch/concurrent</name>
  <files>crates/axiam-api-grpc/tests/grpc_authz_test.rs</files>
  <read_first>
    - crates/axiam-authz/tests/authz_engine_test.rs (setup() lines 34-78, make_engine 80-89, role/permission grant helper 112-155 — copy these)
    - crates/axiam-api-grpc/src/server.rs lines 47-58 (service wiring to adapt; OMIT governor)
    - crates/axiam-api-grpc/src/services/authorization.rs (CheckAccess + BatchCheckAccess impl + parse_uuid 40-44)
    - crates/axiam-api-grpc/src/middleware/rate_limit.rs:34 (build_grpc_governor_layer — confirm it is NOT attached in tests)
    - 07-PATTERNS.md § grpc_authz_test.rs (full harness + client + test-case excerpts; landmines)
  </read_first>
  <action>Create `start_test_server(engine, ...)` returning (endpoint_string, oneshot::Sender shutdown): bind TcpListener "127.0.0.1:0", wrap in TcpListenerStream, Server::builder().add_service(AuthorizationServiceServer::new(AuthorizationServiceImpl::new(engine))).serve_with_incoming_shutdown(incoming, rx) under tokio::spawn — DO NOT attach build_grpc_governor_layer. Copy the Mem-DB setup, type alias, make_engine, and the role→permission grant helper from authz_engine_test.rs. T19.1 cases (assert real RPC results): check_access allowed=true when role grants permission; allowed=false when no role; allowed=false when wrong action; malformed user_id/tenant_id UUID → `err().unwrap().code() == tonic::Code::InvalidArgument`. T19.2 cases: batch_check_access with a mix of grantable + non-grantable requests returns a result vector with the corresponding allow/deny flags; concurrency: spawn N (>=8) tokio tasks each calling check_access and join_all, assert every result matches expectation (exercises engine Arc Send+Sync). Connect the client via Channel::from_shared(endpoint).connect(). Run with `--features client`. Warning-clean. If serve_with_incoming_shutdown/TcpListenerStream API differs (assumption A1), fall back to serve_with_shutdown on a fixed ephemeral port.</action>
  <verify>
    <automated>cargo test -p axiam-api-grpc --features client --test grpc_authz_test</automated>
  </verify>
  <done>grpc_authz_test.rs starts a real in-process tonic server (no governor), and asserts: allow/deny check_access over the wire, invalid_argument on bad UUID, mixed batch result, and N-concurrent calls all correct. Green under `--features client`, warning-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gRPC client → AuthorizationService | service-mesh peer sends authz check requests (untrusted IDs/actions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Elevation | gRPC check_access default-allow risk | mitigate | test asserts allowed=false when no role / wrong action (default-deny) — ASVS V4.1.1 |
| T-07-10 | Tampering | malformed identifiers | mitigate | test asserts Status::invalid_argument on bad UUID (no fallthrough) — ASVS V7 |
| T-07-11 | DoS | concurrent batch authz races | mitigate | T19.2 N-concurrent test asserts correctness under parallelism (engine thread-safety) |
| T-07-DoS-note | DoS | governor rate-limit layer | accept | governor OMITTED from test harness by design (SmartIpKeyExtractor needs real peer IP); rate-limit verified at config level elsewhere |
</threat_model>

<verification>
- `cargo test -p axiam-api-grpc --features client` green.
- Concurrent test uses >=8 tasks via join_all and asserts each result, not just "no panic".
- `cargo clippy -p axiam-api-grpc --features client --tests -- -Dwarnings` clean.
</verification>

<success_criteria>
- New in-process tonic harness exists and is reusable (D-10 foundation).
- T19.1 (gRPC authz integration) and T19.2 (concurrent batch authz) covered with real RPC assertions.
- axiam-api-grpc has a `tests/` dir (ROADMAP SC #4).
</success_criteria>

<output>
Create `.planning/phases/07-compliance-verification-test-closure/07-03-SUMMARY.md` when done.
</output>
