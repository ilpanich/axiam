---
phase: 07-compliance-verification-test-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/helpers/auth.ts
  - frontend/e2e/login.spec.ts
  - frontend/e2e/federation.spec.ts
  - frontend/e2e/roles.spec.ts
  - frontend/e2e/users.spec.ts
  - frontend/e2e/dashboard.spec.ts
  - frontend/e2e/organizations.spec.ts
  - frontend/e2e/tenants.spec.ts
  - frontend/e2e/certificates.spec.ts
  - frontend/e2e/identity.spec.ts
  - frontend/e2e/service-accounts.spec.ts
  - frontend/e2e/settings.spec.ts
  - frontend/playwright.config.ts
  - docker/docker-compose.e2e.yml
  - .github/workflows/ci.yml
autonomous: true
requirements: [REQ-11]
must_haves:
  truths:
    - "A real login helper authenticates via the UI and the resulting httpOnly cookie keeps the user on an authenticated route"
    - "No E2E spec asserts on sessionStorage/localStorage token values"
    - "RBAC-gated navigation is asserted via UI state (element present/absent), not mocked data"
    - "The federation flow mocks only the external IdP redirect and asserts AXIAM UI state after SSO"
    - "A required CI E2E job runs Playwright against a live axiam-server + seeded DB"
  artifacts:
    - path: "frontend/e2e/helpers/auth.ts"
      provides: "loginAsAdmin real-login helper"
      contains: "loginAsAdmin"
    - path: "docker/docker-compose.e2e.yml"
      provides: "live backend + seeded DB for E2E (COOKIE_SECURE=false)"
      contains: "AXIAM__AUTH__COOKIE_SECURE"
    - path: ".github/workflows/ci.yml"
      provides: "required E2E job (D-14)"
      contains: "playwright"
  key_links:
    - from: "frontend/e2e/*.spec.ts"
      to: "frontend/e2e/helpers/auth.ts"
      via: "test.beforeEach(loginAsAdmin)"
      pattern: "loginAsAdmin"
    - from: ".github/workflows/ci.yml e2e job"
      to: "docker/docker-compose.e2e.yml"
      via: "docker compose up --wait"
      pattern: "docker-compose.e2e"
---

<objective>
Rewrite all 11 stale Playwright specs to the cookie-auth + RBAC reality (D-11) and stand up the required live-backend E2E CI job (D-13/D-14). Every spec currently fakes auth via `sessionStorage.setItem("axiam-auth", ...)` (federation.spec.ts:67) which is inert under Phase 1 httpOnly cookies.

Purpose: ROADMAP success criterion #5 (E2E covers login, RBAC-gated nav, federation) and REQ-11 "Frontend E2E tests for login, RBAC, federation flows".
Output: a shared loginAsAdmin helper, 11 rewritten specs, playwright.config E2E_BASE_URL override, a docker-compose.e2e.yml, and a required CI e2e job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-compliance-verification-test-closure/07-RESEARCH.md
@.planning/phases/07-compliance-verification-test-closure/07-PATTERNS.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-CONTEXT.md

<interfaces>
Login form labels (verified login.spec.ts:8,22,28,33): "Organization slug", "Tenant slug", "Continue" button, "Username or email", "Password", "Sign in" button. Post-login redirect: /dashboard or /.
Cookie-auth assertion style: `await expect(page).not.toHaveURL(/\/login/)` + `await expect(page.getByRole("navigation")).toBeVisible()`. NEVER sessionStorage/localStorage.
Federation (D-13): page.route() to intercept the external IdP redirect (e.g. https://idp.example.com/**) and fulfill a 302 back to /federation/callback?code=...&state=...; let AXIAM handle its own callback; assert AXIAM UI state after SSO. Mirrors req5_oidc_e2e.rs mocked-IdP approach.
docker-compose.e2e.yml (07-PATTERNS.md § docker-compose.e2e.yml): mirror docker-compose.dev.yml; surrealdb memory mode (no volume), rabbitmq, axiam-server with AXIAM__AUTH__COOKIE_SECURE="false" (dev compose line 71) + AXIAM__BOOTSTRAP_ADMIN_EMAIL/PASSWORD.
CI job (07-PATTERNS.md § ci.yml): new `e2e` job, needs:[build], docker compose up --wait, setup-node 20, npm ci + build, npx playwright install chromium, npm test with E2E_* env; preserve build-no-saml guard (D-06) and top-level RUSTFLAGS unchanged.
Open question 4: check axiam-server/src/startup.rs whether RBAC roles/permissions auto-seed or need a CI fixture script.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: loginAsAdmin helper + core specs (login, dashboard, roles, users, federation)</name>
  <files>frontend/e2e/helpers/auth.ts, frontend/e2e/login.spec.ts, frontend/e2e/dashboard.spec.ts, frontend/e2e/roles.spec.ts, frontend/e2e/users.spec.ts, frontend/e2e/federation.spec.ts</files>
  <read_first>
    - frontend/e2e/login.spec.ts (the UI-only, non-mockAuth tests are the CORRECT pattern; field labels)
    - frontend/e2e/federation.spec.ts (lines 53-68 sessionStorage mockAuth — the bug to eliminate)
    - frontend/e2e/roles.spec.ts and frontend/e2e/users.spec.ts (current assertions to port to live backend)
    - 07-PATTERNS.md § helpers/auth.ts (loginAsAdmin target), § frontend/e2e/*.spec.ts (correct structure + federation mock), § Cookie-Auth Assertion shared pattern
    - 07-RESEARCH.md § Target 4 (login helper spec, federation mock strategy, cookie assertion)
  </read_first>
  <action>Create `frontend/e2e/helpers/auth.ts` exporting `loginAsAdmin(page)` that drives the real UI login (Organization slug, Tenant slug, Continue, Username/email, Password, Sign in) using E2E_ORG_SLUG/E2E_TENANT_SLUG/E2E_ADMIN_EMAIL/E2E_ADMIN_PASSWORD env (with the documented defaults) and waits for redirect off /login. Rewrite login.spec.ts (assert successful login lands off /login + nav visible; assert wrong password stays on /login with an error), dashboard.spec.ts (beforeEach loginAsAdmin; dashboard renders, not redirected to /login), roles.spec.ts and users.spec.ts (beforeEach loginAsAdmin; assert the list/page renders against the LIVE backend, and for an RBAC-gated control assert UI visibility — element present when permitted). Rewrite federation.spec.ts to DELETE the sessionStorage mockAuth and instead use page.route() to mock ONLY the external IdP redirect, fulfilling a 302 to the AXIAM /federation/callback, then assert AXIAM UI state after SSO. NO sessionStorage/localStorage assertions anywhere.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>helpers/auth.ts exports loginAsAdmin; login/dashboard/roles/users/federation specs use loginAsAdmin (or real login) and assert UI/network state only; zero sessionStorage/localStorage auth references remain in these 6 files; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining 6 specs rewrite (orgs, tenants, certificates, identity, service-accounts, settings)</name>
  <files>frontend/e2e/organizations.spec.ts, frontend/e2e/tenants.spec.ts, frontend/e2e/certificates.spec.ts, frontend/e2e/identity.spec.ts, frontend/e2e/service-accounts.spec.ts, frontend/e2e/settings.spec.ts</files>
  <read_first>
    - frontend/e2e/helpers/auth.ts (the loginAsAdmin helper from Task 1)
    - each of the 6 target specs (current mockAuth + page.route mocked-data patterns to replace)
    - 07-PATTERNS.md § frontend/e2e/*.spec.ts (correct structure; landmines re: live backend + cookie assertion)
  </read_first>
  <action>Rewrite all 6 specs to `test.beforeEach(async ({page}) => loginAsAdmin(page))` and assert against the live backend: each page renders for an authed admin (not redirected to /login, expected nav/heading visible). Remove every `sessionStorage.setItem("axiam-auth", ...)` / mockAuth and every `page.route("**/api/v1/...")` data mock (D-13 requires live backend) — keep only the federation-style external-IdP route mock if a spec exercises SSO. Where a spec previously asserted on mocked rows, assert instead on the presence of the page's primary table/empty-state for a freshly-seeded DB. NO storage assertions.</action>
  <verify>
    <automated>cd frontend && for f in organizations tenants certificates identity service-accounts settings; do grep -q sessionStorage e2e/$f.spec.ts && { echo "FAIL $f"; exit 1; }; done; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>All 6 specs free of sessionStorage/localStorage auth, use loginAsAdmin, assert UI state against live backend; type-check passes; grep confirms no sessionStorage reference in any of the 6 files.</done>
</task>

<task type="auto">
  <name>Task 3: playwright.config override + docker-compose.e2e.yml + required CI e2e job</name>
  <files>frontend/playwright.config.ts, docker/docker-compose.e2e.yml, .github/workflows/ci.yml</files>
  <read_first>
    - frontend/playwright.config.ts (baseURL, webServer, reuseExistingServer)
    - docker/docker-compose.dev.yml (full structure to mirror; AXIAM__AUTH__COOKIE_SECURE=false at line 71)
    - .github/workflows/ci.yml (existing test: job structure ~166-198; build-no-saml guard ~49-63 MUST stay unchanged; top-level RUSTFLAGS line 14)
    - crates/axiam-server/src/startup.rs (whether RBAC roles/permissions auto-seed from bootstrap admin, or a fixture script is needed — open question 4)
    - 07-PATTERNS.md § docker-compose.e2e.yml and § ci.yml (target content)
  </read_first>
  <action>In playwright.config.ts set `use.baseURL = process.env.E2E_BASE_URL ?? "http://localhost:5173"` and keep `trace: "on-first-retry"` + `reuseExistingServer: !process.env.CI`. Create docker/docker-compose.e2e.yml mirroring dev compose: surrealdb in memory mode (no init/volume), rabbitmq with healthcheck, axiam-server depending on both healthy, exposing its port, with env AXIAM__AUTH__COOKIE_SECURE="false", AXIAM__BOOTSTRAP_ADMIN_EMAIL="admin@axiam.dev", AXIAM__BOOTSTRAP_ADMIN_PASSWORD="test-admin-pass" plus DB/AMQP URLs. If startup.rs does NOT auto-seed the RBAC roles/permissions the admin needs, add a small CI seed step or fixture invocation so loginAsAdmin's admin has the permissions the RBAC specs assert. Append an `e2e` job to ci.yml (needs:[build], runs parallel to test): docker compose -f docker/docker-compose.e2e.yml up -d --wait; setup-node 20 + cache; npm ci; npm run build; npx playwright install chromium; npm test with CI=true and the E2E_BASE_URL/E2E_ORG_SLUG/E2E_TENANT_SLUG/E2E_ADMIN_EMAIL/E2E_ADMIN_PASSWORD env; upload playwright-report artifact (if: always()); docker compose down (if: always()). Do NOT modify the build-no-saml job or add --tests to it (D-06). Note in the SUMMARY that the e2e job must be registered as a required status check (D-14).</action>
  <verify>
    <automated>cd /home/emanuele/git/priv/axiam && docker compose -f docker/docker-compose.e2e.yml config -q && grep -q 'e2e' .github/workflows/ci.yml && grep -q 'AXIAM__AUTH__COOKIE_SECURE' docker/docker-compose.e2e.yml</automated>
  </verify>
  <done>playwright.config honors E2E_BASE_URL; docker-compose.e2e.yml validates via `docker compose config` and sets COOKIE_SECURE=false + bootstrap admin; ci.yml has an e2e job (needs build, runs playwright against live compose) without touching build-no-saml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser JS → auth state | httpOnly cookie set by backend; JS must not be able to read/forge it |
| external IdP → AXIAM federation callback | SSO assertion crosses into AXIAM; mocked at IdP boundary in E2E |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Spoofing | token storage XSS (sessionStorage auth) | mitigate | specs assert auth works via httpOnly cookie ONLY; zero sessionStorage/localStorage auth assertions — ASVS V3.1 |
| T-07-13 | Elevation | RBAC-gated nav bypass | mitigate | E2E asserts gated UI element visibility tied to admin permissions — ASVS V4.1 |
| T-07-14 | Tampering | federation callback forgery | mitigate | E2E mocks only external IdP redirect; AXIAM handles its own callback (verifies state/code) — ASVS V2.7 |
| T-07-15 | Information Disclosure | Secure cookie over http in CI | accept | AXIAM__AUTH__COOKIE_SECURE=false is CI/dev-only (D-18); production ships Secure=true |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes after all rewrites.
- `grep -rn "sessionStorage\|localStorage" frontend/e2e` returns no auth-state usage.
- `docker compose -f docker/docker-compose.e2e.yml config -q` validates.
- ci.yml has an `e2e` job; build-no-saml job byte-unchanged (D-06).
</verification>

<success_criteria>
- All 11 specs rewritten to cookie-auth + RBAC, no storage-based auth (D-11).
- Federation E2E mocks only the external IdP (D-13).
- Required E2E CI job runs Playwright against live axiam-server + seeded DB (D-14).
</success_criteria>

<output>
Create `.planning/phases/07-compliance-verification-test-closure/07-04-SUMMARY.md` when done.
</output>
