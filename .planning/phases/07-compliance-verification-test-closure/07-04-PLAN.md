---
phase: 07-compliance-verification-test-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/helpers/auth.ts
  - frontend/e2e/login.spec.ts
  - frontend/e2e/federation.spec.ts
  - frontend/e2e/roles.spec.ts
  - frontend/e2e/users.spec.ts
  - frontend/e2e/dashboard.spec.ts
  - frontend/e2e/organizations.spec.ts
  - frontend/e2e/tenants.spec.ts
  - frontend/e2e/certificates.spec.ts
  - frontend/e2e/identity.spec.ts
  - frontend/e2e/service-accounts.spec.ts
  - frontend/e2e/settings.spec.ts
  - frontend/playwright.config.ts
  - docker/docker-compose.e2e.yml
  - scripts/e2e-bootstrap.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [REQ-11]
must_haves:
  truths:
    - "A live compose stack (surrealdb mem + rabbitmq + axiam-server built from PR source) comes up and a bootstrap fixture creates the org/tenant/admin before any spec runs"
    - "A real login helper authenticates via the UI and the resulting httpOnly cookie keeps the user on an authenticated route"
    - "No E2E spec asserts on sessionStorage/localStorage token values"
    - "RBAC-gated navigation is asserted via UI state (element present/absent), not mocked data"
    - "The federation flow mocks only the external IdP redirect and asserts AXIAM UI state after SSO (URL no longer /login)"
    - "Rewritten specs are verified by an actual headless Playwright run against the live compose stack, not only tsc/grep"
    - "A required CI E2E job runs Playwright against the PR-built axiam-server + bootstrapped DB"
  artifacts:
    - path: "frontend/e2e/helpers/auth.ts"
      provides: "loginAsAdmin real-login helper (D-11 — enables rewriting all 11 specs to cookie-auth)"
      contains: "loginAsAdmin"
    - path: "docker/docker-compose.e2e.yml"
      provides: "live backend (server built from PR source) + bootstrapped DB (COOKIE_SECURE=false) (D-13 live E2E + mocked external IdP)"
      contains: "build"
    - path: "scripts/e2e-bootstrap.sh"
      provides: "POST /api/v1/admin/bootstrap fixture (creates org/tenant/admin+super-admin role)"
      contains: "admin/bootstrap"
    - path: ".github/workflows/ci.yml"
      provides: "required E2E job (D-14; D-06 build-no-saml guard preserved, no --tests) running Playwright against live PR-built stack"
      contains: "playwright"
  key_links:
    - from: "frontend/e2e/*.spec.ts"
      to: "frontend/e2e/helpers/auth.ts"
      via: "test.beforeEach(loginAsAdmin)"
      pattern: "loginAsAdmin"
    - from: ".github/workflows/ci.yml e2e job"
      to: "docker/docker-compose.e2e.yml + scripts/e2e-bootstrap.sh"
      via: "docker compose up --wait -> bootstrap -> playwright test"
      pattern: "docker-compose.e2e"
---

<objective>
Stand up the required live-backend E2E stack FIRST (D-13/D-14), then rewrite all 11 stale Playwright specs to the cookie-auth + RBAC reality (D-11) and verify each rewrite with a real headless Playwright run against the live stack. Every spec currently fakes auth via `sessionStorage.setItem("axiam-auth", ...)` (federation.spec.ts:67) which is inert under Phase 1 httpOnly cookies.

Purpose: ROADMAP success criterion #5 (E2E covers login, RBAC-gated nav, federation) and REQ-11 "Frontend E2E tests for login, RBAC, federation flows".
Output: a docker-compose.e2e.yml (server built from PR source), a bootstrap fixture script, a required CI e2e job, a shared loginAsAdmin helper, 11 rewritten specs, and playwright.config E2E_BASE_URL override.

NYQUIST NOTE: Task 1 stands up the live stack so the spec-rewrite tasks (Task 2, Task 3) can verify with an ACTUAL Playwright run (behavioral E2E), not just `tsc --noEmit`. No 3 consecutive tasks lack a behavioral verify.

LOCAL-STACK PREREQUISITE for Task 2/3 verify: bring the stack up once locally — `docker compose -f docker/docker-compose.e2e.yml up -d --build --wait && bash scripts/e2e-bootstrap.sh` — then run the per-task `npx playwright test ...` against the live `E2E_BASE_URL`. If Docker is unavailable in the executor sandbox, the rewritten specs may run against a `page.route`-mocked backend ONLY for local sampling, but the CI e2e job (Task 1) remains the authoritative behavioral gate against the real PR-built stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-compliance-verification-test-closure/07-RESEARCH.md
@.planning/phases/07-compliance-verification-test-closure/07-PATTERNS.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-CONTEXT.md

<interfaces>
Login form labels (verified login.spec.ts:8,22,28,33): "Organization slug", "Tenant slug", "Continue" button, "Username or email", "Password", "Sign in" button. Post-login redirect: /dashboard or /.
Cookie-auth assertion style: `await expect(page).not.toHaveURL(/\/login/)` + `await expect(page.getByRole("navigation")).toBeVisible()`. NEVER sessionStorage/localStorage.
Federation (D-13): page.route() to intercept the external IdP redirect and fulfill a 302 back to /federation/callback?code=...&state=...; let AXIAM handle its own callback; assert AXIAM UI state after SSO. The real external IdP URL the app navigates to is the SSO endpoint of the federation connection — verified in frontend/e2e/federation.spec.ts: SAML `sso_url: "https://idp.corp.example.com/sso"` and OIDC `issuer_url: "https://accounts.google.com"`. Intercept the URL the app ACTUALLY navigates to for the connection under test (`https://idp.corp.example.com/**` for SAML, `https://accounts.google.com/**` for OIDC). Mirrors req5_oidc_e2e.rs mocked-IdP approach.
Bootstrap reality (07-RESEARCH § Open Questions Q4 RESOLVED): there is NO env-driven auto-bootstrap. Permission seeding (crates/axiam-server/src/main.rs:240-280) runs only for pre-existing tenants. The org/tenant/admin + super-admin role (all permissions) is created ONLY by POST /api/v1/admin/bootstrap (crates/axiam-api-rest/src/handlers/bootstrap.rs:48-168). So a fixture step MUST POST to /api/v1/admin/bootstrap before Playwright runs. AXIAM__BOOTSTRAP_ADMIN_EMAIL/PASSWORD env vars DO NOT seed anything by themselves.
docker-compose.e2e.yml (07-PATTERNS.md § docker-compose.e2e.yml): mirror docker-compose.dev.yml; surrealdb memory mode (no volume), rabbitmq, axiam-server. CRITICAL: build the server image FROM PR SOURCE, not the stale registry tag. Use `build: { context: .., dockerfile: docker/Dockerfile.server }` (dev compose uses `image: ghcr.io/axiamhq/axiam/server:latest` at line 54 — that tag is stale/absent for PR branches and would silently false-green, W8). Set AXIAM__AUTH__COOKIE_SECURE="false" (dev compose line 71).
Server build: `docker build -f docker/Dockerfile.server -t axiam-server .` (build context = repo root, per Dockerfile.server:4).
CI job (07-PATTERNS.md § ci.yml): new `e2e` job, needs:[build]; `docker compose -f docker/docker-compose.e2e.yml up -d --build --wait` (--build forces PR source); run scripts/e2e-bootstrap.sh; setup-node 20; npm ci + build; npx playwright install chromium; npm test with E2E_* env; preserve build-no-saml guard (D-06) and top-level RUSTFLAGS unchanged.
D-14 required status check (W7): a CI YAML job alone does NOT register itself as a required status check. Either run `gh api -X PUT repos/{owner}/{repo}/branches/main/protection ...` (needs admin token) OR flag it as an explicit manual GitHub-UI step in the task action AND the SUMMARY (Settings → Branches → main → Require status checks → add `e2e`). Do NOT bury it silently in a SUMMARY note.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live E2E stack — compose (PR-built server) + bootstrap fixture + required CI e2e job + playwright.config</name>
  <files>docker/docker-compose.e2e.yml, scripts/e2e-bootstrap.sh, frontend/playwright.config.ts, .github/workflows/ci.yml</files>
  <read_first>
    - docker/docker-compose.dev.yml (full structure to mirror; image line 54 = stale tag to REPLACE with build; AXIAM__AUTH__COOKIE_SECURE=false at line 71)
    - docker/Dockerfile.server (build context = repo root; multi-stage; binary entrypoint)
    - crates/axiam-api-rest/src/handlers/bootstrap.rs (the POST /api/v1/admin/bootstrap request shape — org slug, tenant slug, admin email/password fields)
    - crates/axiam-server/src/main.rs:240-280 (confirms seed runs only for existing tenants — fixture must bootstrap first)
    - frontend/playwright.config.ts (baseURL, webServer, reuseExistingServer)
    - .github/workflows/ci.yml (existing test: job structure ~166-198; build-no-saml guard ~49-63 MUST stay byte-unchanged; top-level RUSTFLAGS line 14)
    - 07-PATTERNS.md § docker-compose.e2e.yml and § ci.yml (target content)
  </read_first>
  <action>Create docker/docker-compose.e2e.yml mirroring dev compose: surrealdb in memory mode (no init/volume), rabbitmq with healthcheck, and axiam-server BUILT FROM PR SOURCE — use `build: { context: .., dockerfile: docker/Dockerfile.server }` (NOT `image: ghcr.io/axiamhq/axiam/server:latest`, which is stale/absent for PR branches → silent false-green, W8), depending on both healthy, exposing its port, with env AXIAM__AUTH__COOKIE_SECURE="false" plus DB/AMQP URLs. Create scripts/e2e-bootstrap.sh: a shell script that POSTs to the server's /api/v1/admin/bootstrap with the org slug / tenant slug / admin email / admin password (matching bootstrap.rs request shape and the E2E_* env defaults), retrying until 2xx (the server may still be warming up) and failing non-zero if bootstrap never succeeds — this creates the org/tenant/admin + super-admin role the RBAC specs need (Q4 RESOLVED: no env auto-seed). In playwright.config.ts set `use.baseURL = process.env.E2E_BASE_URL ?? "http://localhost:5173"`, keep `trace: "on-first-retry"` and `reuseExistingServer: !process.env.CI`. Append an `e2e` job to ci.yml (needs:[build], runs parallel to test): checkout; `docker compose -f docker/docker-compose.e2e.yml up -d --build --wait` (the `--build` flag forces the PR source image); run `bash scripts/e2e-bootstrap.sh`; setup-node 20 + cache; npm ci; npm run build; npx playwright install chromium; `npm test` (Playwright) with CI=true and E2E_BASE_URL/E2E_ORG_SLUG/E2E_TENANT_SLUG/E2E_ADMIN_EMAIL/E2E_ADMIN_PASSWORD env; upload playwright-report artifact (if: always()); `docker compose ... down` (if: always()). Do NOT modify the build-no-saml job or add --tests to it (D-06). For D-14 (W7): the e2e job must become a REQUIRED status check — either add a one-shot `gh api -X PUT repos/{owner}/{repo}/branches/main/protection` step (only if an admin-scoped token is available) OR, if not, explicitly flag in BOTH this task's output AND the SUMMARY that a human must register `e2e` as a required status check in GitHub Settings → Branches → main → Require status checks. Surface this as an explicit action item, never a buried note.</action>
  <verify>
    <automated>cd /home/emanuele/git/priv/axiam && docker compose -f docker/docker-compose.e2e.yml config -q && docker compose -f docker/docker-compose.e2e.yml config | grep -q 'build:' && ! docker compose -f docker/docker-compose.e2e.yml config | grep -q 'ghcr.io/axiamhq/axiam/server:latest' && grep -q 'AXIAM__AUTH__COOKIE_SECURE' docker/docker-compose.e2e.yml && grep -q 'admin/bootstrap' scripts/e2e-bootstrap.sh && grep -q 'e2e' .github/workflows/ci.yml && grep -q 'docker compose' .github/workflows/ci.yml</automated>
  </verify>
  <done>docker-compose.e2e.yml validates via `docker compose config`, BUILDS the server from PR source (build: present, no stale ghcr tag), sets COOKIE_SECURE=false; scripts/e2e-bootstrap.sh POSTs /api/v1/admin/bootstrap with retry; playwright.config honors E2E_BASE_URL; ci.yml has an `e2e` job that `up --build`s the compose, runs the bootstrap fixture, and runs Playwright against the live stack, without touching build-no-saml; D-14 required-status-check is registered via gh api OR flagged as an explicit manual step in the task output + SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: loginAsAdmin helper + core specs (login, dashboard, roles, users, federation) — verified by live Playwright run</name>
  <files>frontend/e2e/helpers/auth.ts, frontend/e2e/login.spec.ts, frontend/e2e/dashboard.spec.ts, frontend/e2e/roles.spec.ts, frontend/e2e/users.spec.ts, frontend/e2e/federation.spec.ts</files>
  <read_first>
    - frontend/e2e/login.spec.ts (the UI-only, non-mockAuth tests are the CORRECT pattern; field labels)
    - frontend/e2e/federation.spec.ts (lines 53-68 sessionStorage mockAuth — the bug to eliminate; lines 14-47 the real IdP URLs to intercept)
    - frontend/e2e/roles.spec.ts and frontend/e2e/users.spec.ts (current assertions to port to live backend)
    - docker/docker-compose.e2e.yml + scripts/e2e-bootstrap.sh (Task 1 — the live stack these specs run against)
    - 07-PATTERNS.md § helpers/auth.ts (loginAsAdmin target), § frontend/e2e/*.spec.ts (correct structure + federation mock), § Cookie-Auth Assertion shared pattern
    - 07-RESEARCH.md § Target 4 (login helper spec, federation mock strategy, cookie assertion)
  </read_first>
  <action>Create `frontend/e2e/helpers/auth.ts` exporting `loginAsAdmin(page)` that drives the real UI login (Organization slug, Tenant slug, Continue, Username/email, Password, Sign in) using E2E_ORG_SLUG/E2E_TENANT_SLUG/E2E_ADMIN_EMAIL/E2E_ADMIN_PASSWORD env (with defaults matching scripts/e2e-bootstrap.sh) and waits for redirect off /login. Rewrite login.spec.ts (assert successful login lands off /login + nav visible; assert wrong password stays on /login with an error), dashboard.spec.ts (beforeEach loginAsAdmin; dashboard renders, not redirected to /login), roles.spec.ts and users.spec.ts (beforeEach loginAsAdmin; assert the list/page renders against the LIVE backend, and for an RBAC-gated control assert UI visibility — element present when permitted). Rewrite federation.spec.ts to DELETE the sessionStorage mockAuth and instead use page.route() to mock ONLY the external IdP redirect for the connection under test (intercept the REAL IdP URL the app navigates to — `https://idp.corp.example.com/**` for SAML or `https://accounts.google.com/**` for OIDC, per federation.spec.ts:14-47), fulfilling a 302 to the AXIAM /federation/callback, then assert AXIAM UI state after SSO. NO sessionStorage/localStorage assertions anywhere.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && CI=true E2E_BASE_URL=${E2E_BASE_URL:-http://localhost:5173} npx playwright test e2e/login.spec.ts e2e/dashboard.spec.ts e2e/roles.spec.ts e2e/users.spec.ts e2e/federation.spec.ts --reporter=line</automated>
  </verify>
  <done>helpers/auth.ts exports loginAsAdmin; login/dashboard/roles/users/federation specs use loginAsAdmin (or real login) and assert UI/network state only; zero sessionStorage/localStorage auth references remain in these 6 files; type-check passes; AND a real headless `playwright test` run of these 5 specs against the live (Task 1) stack passes — login lands off /login with nav visible, RBAC-gated control visible for admin, and federation.spec.ts uses `page.route('https://idp.corp.example.com/**', ...)` (or the OIDC issuer URL) to fulfil the IdP redirect and asserts AXIAM UI state after callback (e.g. `await expect(page).not.toHaveURL(/\/login/)` or /dashboard visible).</done>
</task>

<task type="auto">
  <name>Task 3: Remaining 6 specs rewrite (orgs, tenants, certificates, identity, service-accounts, settings) — verified by live Playwright run</name>
  <files>frontend/e2e/organizations.spec.ts, frontend/e2e/tenants.spec.ts, frontend/e2e/certificates.spec.ts, frontend/e2e/identity.spec.ts, frontend/e2e/service-accounts.spec.ts, frontend/e2e/settings.spec.ts</files>
  <read_first>
    - frontend/e2e/helpers/auth.ts (the loginAsAdmin helper from Task 2)
    - each of the 6 target specs (current mockAuth + page.route mocked-data patterns to replace)
    - docker/docker-compose.e2e.yml + scripts/e2e-bootstrap.sh (Task 1 — live stack these specs run against)
    - 07-PATTERNS.md § frontend/e2e/*.spec.ts (correct structure; landmines re: live backend + cookie assertion)
  </read_first>
  <action>Rewrite all 6 specs to `test.beforeEach(async ({page}) => loginAsAdmin(page))` and assert against the live backend: each page renders for an authed admin (not redirected to /login, expected nav/heading visible). Remove every `sessionStorage.setItem("axiam-auth", ...)` / mockAuth and every `page.route("**/api/v1/...")` data mock (D-13 requires live backend) — keep only the federation-style external-IdP route mock if a spec exercises SSO. Where a spec previously asserted on mocked rows, assert instead on the presence of the page's primary table/empty-state for a freshly-bootstrapped DB. NO storage assertions.</action>
  <verify>
    <automated>cd frontend && for f in organizations tenants certificates identity service-accounts settings; do grep -q sessionStorage e2e/$f.spec.ts && { echo "FAIL $f"; exit 1; }; done; npx tsc --noEmit -p tsconfig.json && CI=true E2E_BASE_URL=${E2E_BASE_URL:-http://localhost:5173} npx playwright test e2e/organizations.spec.ts e2e/tenants.spec.ts e2e/certificates.spec.ts e2e/identity.spec.ts e2e/service-accounts.spec.ts e2e/settings.spec.ts --reporter=line</automated>
  </verify>
  <done>All 6 specs free of sessionStorage/localStorage auth, use loginAsAdmin, assert UI state against live backend; type-check passes; grep confirms no sessionStorage reference in any of the 6 files; AND a real headless `playwright test` run of these 6 specs against the live (Task 1) stack passes (each page renders for the bootstrapped admin, not redirected to /login).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser JS → auth state | httpOnly cookie set by backend; JS must not be able to read/forge it |
| external IdP → AXIAM federation callback | SSO assertion crosses into AXIAM; mocked at IdP boundary in E2E |
| PR source → e2e stack image | the e2e server image must be built from PR source, not a stale registry tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Spoofing | token storage XSS (sessionStorage auth) | mitigate | specs assert auth works via httpOnly cookie ONLY; zero sessionStorage/localStorage auth assertions — ASVS V3.1 |
| T-07-13 | Elevation | RBAC-gated nav bypass | mitigate | E2E asserts gated UI element visibility tied to admin permissions, verified by a live Playwright run — ASVS V4.1 |
| T-07-14 | Tampering | federation callback forgery | mitigate | E2E mocks only external IdP redirect; AXIAM handles its own callback (verifies state/code) — ASVS V2.7 |
| T-07-15 | Information Disclosure | Secure cookie over http in CI | accept | AXIAM__AUTH__COOKIE_SECURE=false is CI/dev-only (D-18); production ships Secure=true |
| T-07-19 | Tampering | stale registry image → false-green E2E | mitigate | docker-compose.e2e.yml BUILDS the server from PR source (`build:`), CI uses `up --build`; verify rejects the stale `ghcr.io/axiamhq/axiam/server:latest` tag — ASVS V10.3 |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes after all rewrites.
- `grep -rn "sessionStorage\|localStorage" frontend/e2e` returns no auth-state usage.
- `docker compose -f docker/docker-compose.e2e.yml config -q` validates AND `build:` is present AND the stale ghcr tag is absent.
- A live `npx playwright test` run (against the compose stack + bootstrap fixture) of the rewritten specs passes — behavioral, not just type-check.
- ci.yml has an `e2e` job that `up --build`s the compose and runs the bootstrap fixture; build-no-saml job byte-unchanged (D-06).
</verification>

<success_criteria>
- All 11 specs rewritten to cookie-auth + RBAC, no storage-based auth (D-11), verified by a real Playwright run.
- Federation E2E mocks only the external IdP and asserts post-SSO UI state (D-13).
- Required E2E CI job runs Playwright against a PR-built axiam-server + bootstrapped DB (D-14); required-status-check registration done via gh api or flagged as an explicit manual step.
</success_criteria>

<output>
Create `.planning/phases/07-compliance-verification-test-closure/07-04-SUMMARY.md` when done.
</output>
