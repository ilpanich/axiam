---
phase: 07-compliance-verification-test-closure
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - docs/compliance/asvs-l2-checklist.md
  - docs/compliance/FINDINGS.md
  - docs/compliance/sc4-coverage.md
autonomous: false
requirements: [REQ-11]
must_haves:
  truths:
    - "Every in-scope ASVS L2 control (V2,V3,V4,V6,V7,V8,V9,V10,V14) has a row with status Pass / N-A / Deferred"
    - "Each Pass row cites concrete evidence (file:line and/or test name)"
    - "V10 (Malicious Code) cites Phase 6 supply-chain evidence (cargo-audit/cargo-deny/trivy/SRI)"
    - "Every Deferred control has a matching FINDINGS.md row and a GitHub compliance issue link"
    - "sc4-coverage.md maps each REQ-11/SC#4 area to the concrete test file that proves it (authz_engine_test.rs, req5_oidc_e2e.rs, req5_saml_e2e.rs, rbac_test.rs, auth_test.rs, gdpr_test.rs)"
    - "GitHub compliance issues are created only AFTER human sign-off (no issue created before the checkpoint approves)"
    - "There are no open items (no in-scope control left without a status)"
  artifacts:
    - path: "docs/compliance/asvs-l2-checklist.md"
      provides: "per-control ASVS L2 checklist with evidence (D-12)"
      contains: "Control ID"
    - path: "docs/compliance/FINDINGS.md"
      provides: "complete deferred-findings register + staged (dry-run) issue proposals"
      contains: "Severity"
    - path: "docs/compliance/sc4-coverage.md"
      provides: "REQ-11/SC#4 test-coverage citation table for already-tested crates"
      contains: "authz_engine_test.rs"
  key_links:
    - from: "docs/compliance/asvs-l2-checklist.md"
      to: "test files + code lines + Phase 6 ci.yml"
      via: "per-control evidence citations"
      pattern: "tests/|src/|ci.yml"
    - from: "docs/compliance/asvs-l2-checklist.md Deferred rows"
      to: "docs/compliance/FINDINGS.md + GitHub issues"
      via: "cross-reference"
      pattern: "FINDINGS"
    - from: "docs/compliance/sc4-coverage.md"
      to: "authz_engine_test.rs / req5_oidc_e2e.rs / req5_saml_e2e.rs / rbac_test.rs / auth_test.rs / gdpr_test.rs"
      via: "REQ-11 AC ↔ test file citation"
      pattern: "_test\\.rs|_e2e\\.rs"
---

<objective>
Author the OWASP ASVS Level 2 checklist for the IAM-relevant + malicious-code families (V2,V3,V4,V6,V7,V8,V9,V10,V14 — D-02) with per-control evidence rows (D-12), write the SC#4 test-coverage citation table (REQ-11), and finalize the FINDINGS register (D-05). This is the milestone compliance gate: "no open items" means every in-scope control is Pass, documented N/A, or Deferred-with-rationale.

This plan runs LAST (Wave 2) because it cites the conformance/PKI/gRPC tests (plans 01-03) and the E2E suite (plan 04) as evidence, and because severity-gated inline fixes from those plans must land before the final "no open items" assertion.

Purpose: ROADMAP success criterion #1 (ASVS L2 checklist has no open items), SC #4 (citation of already-tested crates), and REQ-11 milestone gate.
Output: complete `docs/compliance/asvs-l2-checklist.md`, `docs/compliance/sc4-coverage.md`, and finalized `docs/compliance/FINDINGS.md`.

IRREVERSIBLE SIDE-EFFECT RULE: creating GitHub `compliance` issues is outward-facing and not cleanly reversible. Task 1 performs a DRY-RUN only (lists existing issues, stages proposed issues in FINDINGS.md). The ACTUAL `gh issue create` calls happen in Task 2, AFTER the human approves the deferred-findings set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-compliance-verification-test-closure/07-RESEARCH.md
@.planning/phases/07-compliance-verification-test-closure/07-PATTERNS.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-CONTEXT.md

<interfaces>
ASVS row format (D-12, 07-PATTERNS.md § docs/compliance): | Control ID | Control Text | Status | Evidence | Note |. Status values: Pass / N/A / Deferred (see FINDINGS.md #N).
Control→evidence draft map: 07-RESEARCH.md § Research Target 5 (V2-V14 tables with likely status + evidence). "Verify" items in that map require reading the cited source before marking Pass.
V10 evidence (Phase 6): cargo-audit + cargo-deny + trivy in .github/workflows/ci.yml (~81-89, 116), SRI SHA-384 in dist/index.html (Phase 6 D-17), vite sourcemap:false.
New Phase 7 evidence to cite: crates/axiam-pki/tests/{ca,cert,mtls,pgp}_test.rs (V2.9 mTLS, V6 crypto), oauth2_conformance.rs/oidc_conformance.rs (V2.7 federation alg pinning), grpc_authz_test.rs (V4 access control over gRPC), frontend/e2e/*.spec.ts (V3 cookie auth in browser).
Already-tested crates to cite for SC#4 (CONTEXT reconciliation — DO NOT re-test, only cite): axiam-authz → crates/axiam-authz/tests/authz_engine_test.rs; axiam-federation → crates/axiam-server/tests/req5_oidc_e2e.rs + crates/axiam-server/tests/req5_saml_e2e.rs; RBAC → crates/axiam-api-rest/tests/rbac_test.rs; cookie auth → crates/axiam-api-rest/tests/auth_test.rs; GDPR → crates/axiam-api-rest/tests/gdpr_test.rs (confirm exact paths during read).
FINDINGS row format (D-05): | # | Finding | Severity | ASVS/RFC Ref | Deferral Rationale | Issue |.
gh dry-run: `gh issue list --label compliance` (READ-ONLY) to see what already exists; staging happens in FINDINGS.md, NOT via `gh issue create` in Task 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ASVS L2 checklist + SC#4 coverage table + stage FINDINGS (DRY-RUN issues only)</name>
  <files>docs/compliance/asvs-l2-checklist.md, docs/compliance/sc4-coverage.md, docs/compliance/FINDINGS.md</files>
  <read_first>
    - 07-RESEARCH.md § Research Target 5 (the V2/V3/V4/V6/V7/V8/V9/V10/V14 control→evidence draft tables)
    - .github/workflows/ci.yml (cargo-audit/cargo-deny/trivy steps for V10 citations)
    - crates/axiam-api-rest/tests/auth_test.rs (V3.1/V3.4 Set-Cookie httpOnly/SameSite assertions ~line 253,385; cite as SC#4 cookie-auth evidence)
    - crates/axiam-api-rest/tests/{rbac_test.rs,middleware_test.rs} (V4 default-deny + parity; rbac_test.rs is SC#4 RBAC evidence)
    - crates/axiam-api-rest/tests/gdpr_test.rs (SC#4 GDPR evidence)
    - crates/axiam-authz/tests/authz_engine_test.rs (SC#4 axiam-authz evidence)
    - crates/axiam-server/tests/req5_oidc_e2e.rs and crates/axiam-server/tests/req5_saml_e2e.rs (SC#4 axiam-federation evidence)
    - crates/axiam-pki/tests/* and crates/axiam-api-grpc/tests/grpc_authz_test.rs and crates/axiam-api-rest/tests/{oauth2_conformance.rs,oidc_conformance.rs} (Phase 7 new evidence — confirm test fn names from plans 01-03 SUMMARYs)
    - docs/compliance/FINDINGS.md (seeded in plan 02 — append/finalize, do not overwrite existing rows)
    - 07-PATTERNS.md § docs/compliance (row formats)
  </read_first>
  <action>(1) Author docs/compliance/asvs-l2-checklist.md as per-control rows for every in-scope family (V2,V3,V4,V6,V7,V8,V9,V10,V14) using the D-12 format. For each control: set status Pass / N-A / Deferred and cite concrete evidence (file:line and/or test fn name). For each "Verify"-marked control in the RESEARCH draft, READ the cited source first and only mark Pass if the behavior is actually present; if genuinely not met, decide per D-04 — small/localized or Critical/High → it should already have been fixed in plans 01-04 (cite the fix), otherwise mark Deferred and add a FINDINGS row. Cite V10 controls to the Phase 6 supply-chain CI steps + SRI. Cross-reference each new Phase 7 test as evidence for the relevant control. (2) Author docs/compliance/sc4-coverage.md: a table proving SC#4 / REQ-11 for the already-tested crates with columns | Area | REQ-11 AC | Test File | SC#4 Cross-ref |, with one row each for: axiam-authz → crates/axiam-authz/tests/authz_engine_test.rs; axiam-federation → crates/axiam-server/tests/req5_oidc_e2e.rs AND crates/axiam-server/tests/req5_saml_e2e.rs; RBAC → crates/axiam-api-rest/tests/rbac_test.rs; cookie auth → crates/axiam-api-rest/tests/auth_test.rs; GDPR → crates/axiam-api-rest/tests/gdpr_test.rs. Confirm each file path exists (read it) before writing the row; include the SC#4 cross-reference for each. (3) Finalize docs/compliance/FINDINGS.md: ensure every Deferred control in the checklist has a matching FINDINGS row (# | Finding | Severity | ASVS/RFC Ref | Deferral Rationale | Issue). Run `gh issue list --label compliance` (READ-ONLY dry run) to discover any pre-existing compliance issues; for each Deferred row that does NOT already have an open issue, set its Issue cell to `PENDING (staged — create in Task 2)` and add a "Proposed Issues (staged for Task 2 approval)" section to FINDINGS.md listing the exact `gh issue create` title/body/label for each. DO NOT call `gh issue create` in this task. Assert in the checklist header that there are NO open items (every in-scope control has a status). Beta must ship with NO Deferred row of severity High.</action>
  <verify>
    <automated>grep -Ev '^#' docs/compliance/asvs-l2-checklist.md | grep -cE '^\| V(2|3|4|6|7|8|9|10|14)\.' | { read n; [ "$n" -ge 9 ]; } && ! grep -iE '\bTBD\b|\bTODO\b|open item' docs/compliance/asvs-l2-checklist.md && awk -F'|' '/^\| V[0-9]/{e=$5; gsub(/^[ \t]+|[ \t]+$/,"",e); if(e==""){print "EMPTY EVIDENCE: "$0; exit 1}}' docs/compliance/asvs-l2-checklist.md && for f in authz_engine_test.rs req5_oidc_e2e.rs req5_saml_e2e.rs rbac_test.rs auth_test.rs gdpr_test.rs; do grep -q "$f" docs/compliance/sc4-coverage.md || { echo "MISSING $f in sc4-coverage"; exit 1; }; done && awk -F'|' 'tolower($0) ~ /deferred/ && /^\|/{r=$7; gsub(/^[ \t]+|[ \t]+$/,"",r); if(r==""){print "DEFERRED ROW MISSING ISSUE: "$0; exit 1}}' docs/compliance/FINDINGS.md</automated>
  </verify>
  <done>asvs-l2-checklist.md has per-control rows across all 9 in-scope families with NO empty Evidence cell (column 5) for any in-scope V-row, no TBD/TODO/open-item markers; sc4-coverage.md cites all six SC#4 files (authz_engine_test.rs, req5_oidc_e2e.rs, req5_saml_e2e.rs, rbac_test.rs, auth_test.rs, gdpr_test.rs) each with a REQ-11 AC + SC#4 cross-ref; FINDINGS.md has a row for each Deferred control with non-empty severity + ASVS ref + rationale + Issue cell (PENDING or existing link), no High-severity Deferred row, and a staged "Proposed Issues" section; NO `gh issue create` was executed in this task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Auditor sign-off + create staged compliance issues (no open items, no High deferred)</name>
  <what-built>docs/compliance/asvs-l2-checklist.md (per-control ASVS L2 rows with evidence), docs/compliance/sc4-coverage.md (SC#4 citation table), and the finalized FINDINGS.md deferred register with a STAGED "Proposed Issues" section. No GitHub `compliance` issues have been created yet — Task 1 only dry-ran `gh issue list`.</what-built>
  <how-to-verify>
    1. Open docs/compliance/asvs-l2-checklist.md — confirm every in-scope control (V2,V3,V4,V6,V7,V8,V9,V10,V14) has a status (Pass / N-A / Deferred) and a real evidence citation (no empty cells). Auditor judgment: "no open items" per D-12.
    2. Open docs/compliance/sc4-coverage.md — confirm each of the six already-tested areas (axiam-authz, axiam-federation OIDC + SAML, RBAC, cookie auth, GDPR) cites the right test file with a REQ-11 AC and SC#4 cross-reference.
    3. Open docs/compliance/FINDINGS.md — confirm each Deferred control has a matching row and review the STAGED "Proposed Issues" section. Confirm NO Deferred row has severity High (beta DoD: no known High holes, D-04).
    4. Spot-check 2-3 Pass rows: open the cited file:line / run the cited test name and confirm the evidence actually supports the control.
    5. APPROVE the staged issue set. On approval the executor runs the staged `gh issue create` calls and replaces each FINDINGS `PENDING` Issue cell with the created issue URL.
  </how-to-verify>
  <action>Pause for the developer to audit the ASVS L2 checklist, SC#4 coverage table, and FINDINGS register per the steps above. On "approved": execute the staged `gh issue create --label compliance ...` calls from the FINDINGS "Proposed Issues" section (ONE per Deferred-without-existing-issue row), then update each corresponding FINDINGS Issue cell from `PENDING (staged — create in Task 2)` to the created issue URL, and remove the "Proposed Issues" staging section. This is the ONLY task that creates GitHub issues (irreversible, outward-facing). If corrections are requested instead, loop back to Task 1 and do NOT create any issue. On completion the phase compliance gate (SC #1) is satisfied.</action>
  <resume-signal>Type "approved" to create the staged compliance issues, or list the controls/rows that need correction.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor/community → compliance docs | published evidence must be accurate and traceable to code/tests |
| supply chain → build (V10) | dependency integrity asserted via Phase 6 CI scans |
| executor → GitHub issues | `gh issue create` is irreversible/outward-facing; must not run before human sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Repudiation | unsubstantiated Pass claims | mitigate | every Pass row cites file:line/test name; checkpoint spot-checks evidence — ASVS V7 |
| T-07-17 | Tampering | dependency confusion / supply chain | accept (cite) | cargo-audit + cargo-deny + trivy + SRI from Phase 6 cited under V10 — ASVS V10.1/V10.2/V10.3 |
| T-07-18 | Repudiation | premature/irreversible GitHub issue creation | mitigate | Task 1 dry-runs `gh issue list` and stages proposals in FINDINGS.md ONLY; actual `gh issue create` runs in Task 2 after blocking human approval (cannot un-create an outward-facing issue) — ASVS V1.6/V7 |
| T-07-SC | Tampering | package installs this phase | mitigate | no new packages installed (07-RESEARCH § Package Legitimacy Audit: none); checklist authoring only |
</threat_model>

<verification>
- asvs-l2-checklist.md covers all 9 in-scope families, no open items, no empty Evidence cell, no High Deferred.
- sc4-coverage.md cites authz_engine_test.rs, req5_oidc_e2e.rs, req5_saml_e2e.rs, rbac_test.rs, auth_test.rs, gdpr_test.rs with REQ-11 AC + SC#4 cross-ref.
- Every Deferred control ↔ FINDINGS.md row ↔ (after Task 2) open GitHub `compliance` issue.
- V10 rows cite Phase 6 CI supply-chain evidence.
- No `gh issue create` executed before the Task 2 human checkpoint approves.
</verification>

<success_criteria>
- OWASP ASVS L2 checklist for IAM-relevant controls has no open items (ROADMAP SC #1).
- SC#4 already-tested crates are explicitly cited as REQ-11 evidence (sc4-coverage.md).
- Deferred-findings register is complete and cross-linked to GitHub issues created only after sign-off (D-05).
- Beta ships with no known High security holes (D-04).
</success_criteria>

<output>
Create `.planning/phases/07-compliance-verification-test-closure/07-05-SUMMARY.md` when done.
</output>
