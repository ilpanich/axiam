---
phase: 07-compliance-verification-test-closure
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - docs/compliance/asvs-l2-checklist.md
  - docs/compliance/FINDINGS.md
autonomous: false
requirements: [REQ-11]
must_haves:
  truths:
    - "Every in-scope ASVS L2 control (V2,V3,V4,V6,V7,V8,V9,V10,V14) has a row with status Pass / N-A / Deferred"
    - "Each Pass row cites concrete evidence (file:line and/or test name)"
    - "V10 (Malicious Code) cites Phase 6 supply-chain evidence (cargo-audit/cargo-deny/trivy/SRI)"
    - "Every Deferred control has a matching FINDINGS.md row and a GitHub compliance issue link"
    - "There are no open items (no in-scope control left without a status)"
  artifacts:
    - path: "docs/compliance/asvs-l2-checklist.md"
      provides: "per-control ASVS L2 checklist with evidence (D-12)"
      contains: "Control ID"
    - path: "docs/compliance/FINDINGS.md"
      provides: "complete deferred-findings register"
      contains: "Severity"
  key_links:
    - from: "docs/compliance/asvs-l2-checklist.md"
      to: "test files + code lines + Phase 6 ci.yml"
      via: "per-control evidence citations"
      pattern: "tests/|src/|ci.yml"
    - from: "docs/compliance/asvs-l2-checklist.md Deferred rows"
      to: "docs/compliance/FINDINGS.md + GitHub issues"
      via: "cross-reference"
      pattern: "FINDINGS"
---

<objective>
Author the OWASP ASVS Level 2 checklist for the IAM-relevant + malicious-code families (V2,V3,V4,V6,V7,V8,V9,V10,V14 — D-02) with per-control evidence rows (D-12), and finalize the FINDINGS register (D-05). This is the milestone compliance gate: "no open items" means every in-scope control is Pass, documented N/A, or Deferred-with-rationale.

This plan runs LAST (Wave 2) because it cites the conformance/PKI/gRPC tests (plans 01-03) and the E2E suite (plan 04) as evidence, and because severity-gated inline fixes from those plans must land before the final "no open items" assertion.

Purpose: ROADMAP success criterion #1 (ASVS L2 checklist has no open items) and REQ-11 milestone gate.
Output: complete `docs/compliance/asvs-l2-checklist.md` + finalized `docs/compliance/FINDINGS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-compliance-verification-test-closure/07-RESEARCH.md
@.planning/phases/07-compliance-verification-test-closure/07-PATTERNS.md
@.planning/phases/06-ci-cd-infrastructure-hardening/06-CONTEXT.md

<interfaces>
ASVS row format (D-12, 07-PATTERNS.md § docs/compliance): | Control ID | Control Text | Status | Evidence | Note |. Status values: Pass / N/A / Deferred (see FINDINGS.md #N).
Control→evidence draft map: 07-RESEARCH.md § Research Target 5 (V2-V14 tables with likely status + evidence). "Verify" items in that map require reading the cited source before marking Pass.
V10 evidence (Phase 6): cargo-audit + cargo-deny + trivy in .github/workflows/ci.yml (~81-89, 116), SRI SHA-384 in dist/index.html (Phase 6 D-17), vite sourcemap:false.
New Phase 7 evidence to cite: crates/axiam-pki/tests/{ca,cert,mtls,pgp}_test.rs (V2.9 mTLS, V6 crypto), oauth2_conformance.rs/oidc_conformance.rs (V2.7 federation alg pinning), grpc_authz_test.rs (V4 access control over gRPC), frontend/e2e/*.spec.ts (V3 cookie auth in browser).
FINDINGS row format (D-05): | # | Finding | Severity | ASVS/RFC Ref | Deferral Rationale | Issue |.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ASVS L2 checklist + finalize FINDINGS register</name>
  <files>docs/compliance/asvs-l2-checklist.md, docs/compliance/FINDINGS.md</files>
  <read_first>
    - 07-RESEARCH.md § Research Target 5 (the V2/V3/V4/V6/V7/V8/V9/V10/V14 control→evidence draft tables)
    - .github/workflows/ci.yml (cargo-audit/cargo-deny/trivy steps for V10 citations)
    - crates/axiam-api-rest/tests/auth_test.rs (V3.1/V3.4 Set-Cookie httpOnly/SameSite assertions ~line 253,385)
    - crates/axiam-api-rest/tests/{rbac_test.rs,middleware_test.rs} (V4 default-deny + parity)
    - crates/axiam-pki/tests/* and crates/axiam-api-grpc/tests/grpc_authz_test.rs and crates/axiam-api-rest/tests/{oauth2_conformance.rs,oidc_conformance.rs} (Phase 7 new evidence — confirm test fn names from plans 01-03 SUMMARYs)
    - docs/compliance/FINDINGS.md (seeded in plan 02 — append/finalize, do not overwrite existing rows)
    - 07-PATTERNS.md § docs/compliance (row formats)
  </read_first>
  <action>Author docs/compliance/asvs-l2-checklist.md as per-control rows for every in-scope family (V2,V3,V4,V6,V7,V8,V9,V10,V14) using the D-12 format. For each control: set status Pass / N-A / Deferred and cite concrete evidence (file:line and/or test fn name). For each "Verify"-marked control in the RESEARCH draft, READ the cited source first and only mark Pass if the behavior is actually present; if the control is genuinely not met, decide per D-04 — small/localized or Critical/High → it should already have been fixed in plans 01-04 (cite the fix), otherwise mark Deferred and add a FINDINGS row. Cite V10 controls to the Phase 6 supply-chain CI steps + SRI. Cross-reference each new Phase 7 test as evidence for the relevant control. Finalize docs/compliance/FINDINGS.md: ensure every Deferred control in the checklist has a matching FINDINGS row (# | Finding | Severity | ASVS/RFC Ref | Deferral Rationale | Issue) and that each row's Issue cell links to a GitHub `compliance` issue (open the issue via gh if missing — see Task 2 checkpoint). Assert in the doc header that there are NO open items (every in-scope control has a status). Beta must ship with NO Deferred row of severity High.</action>
  <verify>
    <automated>cd /home/emanuele/git/priv/axiam && grep -Ev '^#' docs/compliance/asvs-l2-checklist.md | grep -cE '^\| V(2|3|4|6|7|8|9|10|14)\.' | { read n; [ "$n" -ge 9 ]; } && ! grep -iE '\bTBD\b|\bTODO\b|open item' docs/compliance/asvs-l2-checklist.md</automated>
  </verify>
  <done>asvs-l2-checklist.md has per-control rows across all 9 in-scope families, every row has a status + evidence, no TBD/TODO/open-item markers remain; FINDINGS.md has a row for each Deferred control with severity + ASVS ref + rationale + issue link; no High-severity Deferred row exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Auditor sign-off on ASVS checklist + FINDINGS (no open items, no High deferred)</name>
  <what-built>docs/compliance/asvs-l2-checklist.md (per-control ASVS L2 rows with evidence) and the finalized FINDINGS.md deferred register. Any GitHub `compliance` issues for deferred controls were opened via gh during Task 1.</what-built>
  <how-to-verify>
    1. Open docs/compliance/asvs-l2-checklist.md — confirm every in-scope control (V2,V3,V4,V6,V7,V8,V9,V10,V14) has a status (Pass / N-A / Deferred) and a real evidence citation (no empty cells). This is auditor judgment: "no open items" per D-12.
    2. Open docs/compliance/FINDINGS.md — confirm each Deferred control has a matching row, and each Issue link resolves to an open GitHub issue labelled `compliance` (`gh issue list --label compliance`).
    3. Confirm NO Deferred row has severity High (beta DoD: no known High holes, D-04).
    4. Spot-check 2-3 Pass rows: open the cited file:line / run the cited test name and confirm the evidence actually supports the control.
  </how-to-verify>
  <action>Pause for the developer to audit the ASVS L2 checklist and FINDINGS register per the steps above. No code or doc changes are made by the executor in this task; on "approved" the phase compliance gate (SC #1) is satisfied. If corrections are requested, loop back to Task 1.</action>
  <resume-signal>Type "approved" or list the controls/rows that need correction.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor/community → compliance docs | published evidence must be accurate and traceable to code/tests |
| supply chain → build (V10) | dependency integrity asserted via Phase 6 CI scans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Repudiation | unsubstantiated Pass claims | mitigate | every Pass row cites file:line/test name; checkpoint spot-checks evidence — ASVS V7 |
| T-07-17 | Tampering | dependency confusion / supply chain | accept (cite) | cargo-audit + cargo-deny + trivy + SRI from Phase 6 cited under V10 — ASVS V10.1/V10.2/V10.3 |
| T-07-SC | Tampering | package installs this phase | mitigate | no new packages installed (07-RESEARCH § Package Legitimacy Audit: none); checklist authoring only |
</threat_model>

<verification>
- asvs-l2-checklist.md covers all 9 in-scope families, no open items, no High Deferred.
- Every Deferred control ↔ FINDINGS.md row ↔ open GitHub `compliance` issue.
- V10 rows cite Phase 6 CI supply-chain evidence.
- This plan opens NO new code paths; it is documentation + (if needed) issue creation only.
</verification>

<success_criteria>
- OWASP ASVS L2 checklist for IAM-relevant controls has no open items (ROADMAP SC #1).
- Deferred-findings register is complete and cross-linked to GitHub issues (D-05).
- Beta ships with no known High security holes (D-04).
</success_criteria>

<output>
Create `.planning/phases/07-compliance-verification-test-closure/07-05-SUMMARY.md` when done.
</output>
