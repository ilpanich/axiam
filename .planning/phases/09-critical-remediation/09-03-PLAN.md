---
phase: 09-critical-remediation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/auth.ts
  - frontend/src/pages/auth/ForgotPasswordPage.tsx
  - frontend/src/pages/auth/ResetPasswordPage.tsx
  - frontend/src/pages/auth/VerifyEmailPage.tsx
  - frontend/src/pages/profile/ChangePasswordPage.tsx
  - frontend/src/pages/profile/ProfilePage.tsx
  - frontend/src/pages/profile/MfaManagementPage.tsx
  - frontend/e2e/auth-contract.spec.ts
autonomous: true
requirements: [REQ-13]
must_haves:
  truths:
    - "All six auth flows call the correct /api/v1/auth/* backend endpoints via a typed auth.ts service"
    - "ForgotPassword submits to /api/v1/auth/reset (not /auth/forgot-password)"
    - "MFA enroll submits to /api/v1/auth/mfa/setup/enroll"
    - "A Playwright contract test asserts each correct endpoint URL"
  artifacts:
    - path: "frontend/src/services/auth.ts"
      provides: "typed authService with 7 methods, all via the api axios instance"
      contains: "/api/v1/auth/reset"
    - path: "frontend/e2e/auth-contract.spec.ts"
      provides: "route-intercept contract test for all 6 pages / 7 endpoints"
  key_links:
    - from: "auth pages"
      to: "auth.ts authService"
      via: "import { authService } from services/auth"
      pattern: "authService\\."
    - from: "auth.ts"
      to: "api axios instance"
      via: "import api from @/lib/api"
      pattern: "import api from"
---

<objective>
Close the wrong-endpoint defect (SEC-044/CQ-F27): all six frontend auth pages call paths missing the `/api/v1/` prefix (and some with wrong segments), so reset/verify/change-password/MFA flows silently fail. Create a typed `auth.ts` service routing every call through the `api` axios instance (cookies + CSRF), rewire the six pages to it, and add a Playwright contract test asserting the correct URLs.

Purpose: Auth self-service flows must reach the real backend routes; a contract test must gate regressions in CI.
Output: New `auth.ts` service, six rewired pages, new Playwright contract spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-critical-remediation/09-RESEARCH.md
@.planning/phases/09-critical-remediation/09-PATTERNS.md
@.planning/phases/09-critical-remediation/09-VALIDATION.md

<interfaces>
URL mismatch table (VERIFIED against backend handlers) — current → correct:

| Page | Current (wrong) | Correct backend URL |
|------|-----------------|---------------------|
| ForgotPasswordPage | POST /auth/forgot-password | POST /api/v1/auth/reset |
| ResetPasswordPage | POST /auth/reset-password | POST /api/v1/auth/reset/confirm |
| VerifyEmailPage | GET /auth/verify-email?token= | GET /api/v1/auth/verify-email?token= |
| ProfilePage (resend) | POST /auth/resend-verification | POST /api/v1/auth/resend-verification |
| ChangePasswordPage | POST /auth/change-password | POST /api/v1/auth/password/change |
| MfaManagementPage (setup) | POST /auth/mfa/setup | POST /api/v1/auth/mfa/setup/enroll |
| MfaManagementPage (confirm) | POST /auth/mfa/confirm | POST /api/v1/auth/mfa/setup/confirm |

Backend body contracts (field names already match the frontend):
- /api/v1/auth/reset → { email }
- /api/v1/auth/reset/confirm → { token, new_password }
- /api/v1/auth/verify-email → ?token=
- /api/v1/auth/resend-verification → {} (authenticated)
- /api/v1/auth/password/change → { current_password, new_password }
- /api/v1/auth/mfa/setup/enroll → {} → { secret, qr_code_uri }
- /api/v1/auth/mfa/setup/confirm → { code }

auth.ts convention (analog services/users.ts):
- `import api from "@/lib/api";` — ALWAYS the api instance, never bare axios (this is the CQ-F28 lesson).
- No-body responses: `.then(() => undefined)`. Body responses: `api.post<T>(...).then(r => r.data)`.
- Named const export `authService = { ... }`.

Playwright contract (analog identity.spec.ts:125-141 route-intercept + federation.spec.ts URL assert):
- `page.route("**/auth/**", route => { capturedUrl = route.request().url(); route.fulfill({status:200, json:{}}); })`
- assert `capturedUrl` toContain the correct `/api/v1/auth/...` path and NOT toContain the old wrong path.
- spec dir is `frontend/e2e/`; `npm test --prefix frontend` runs Playwright.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed auth.ts service</name>
  <files>frontend/src/services/auth.ts</files>
  <read_first>
    - frontend/src/services/users.ts (EXACT analog — import api, typed interfaces, named const service, .then(r => r.data) vs .then(() => undefined))
    - frontend/src/lib/api.ts (the api instance — confirm it is the default export and attaches CSRF + withCredentials)
    - 09-PATTERNS.md (authService skeleton with all 7 methods)
  </read_first>
  <action>
    Create `frontend/src/services/auth.ts` exporting `authService` with: `requestPasswordReset(email)` → POST /api/v1/auth/reset; `confirmPasswordReset(token, new_password)` → POST /api/v1/auth/reset/confirm; `verifyEmail(token)` → GET /api/v1/auth/verify-email?token=<encodeURIComponent>; `resendVerification()` → POST /api/v1/auth/resend-verification; `changePassword(current_password, new_password)` → POST /api/v1/auth/password/change; `enrollMfa()` → POST /api/v1/auth/mfa/setup/enroll returning `{ secret, qr_code_uri }`; `confirmMfa(code)` → POST /api/v1/auth/mfa/setup/confirm. Export request/response TS interfaces. Every call goes through `api` (default import from `@/lib/api`) — never bare `axios`. Use `.then(() => undefined)` for void returns and `api.post<MfaEnrollResponse>(...).then(r => r.data)` for the MFA enroll body.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: auth.ts imports `api` from `@/lib/api` and contains no `axios.` call.
    - SOURCE: auth.ts contains all 7 endpoint strings exactly as in the table (`/api/v1/auth/reset`, `/reset/confirm`, `/verify-email`, `/resend-verification`, `/password/change`, `/mfa/setup/enroll`, `/mfa/setup/confirm`).
    - BUILD: `npx tsc -b` reports no errors (read OUTPUT text).
  </acceptance_criteria>
  <done>Typed authService exists, all 7 correct URLs, compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the six auth pages to authService</name>
  <files>frontend/src/pages/auth/ForgotPasswordPage.tsx, frontend/src/pages/auth/ResetPasswordPage.tsx, frontend/src/pages/auth/VerifyEmailPage.tsx, frontend/src/pages/profile/ChangePasswordPage.tsx, frontend/src/pages/profile/ProfilePage.tsx, frontend/src/pages/profile/MfaManagementPage.tsx</files>
  <read_first>
    - frontend/src/pages/auth/ForgotPasswordPage.tsx (modify — replace inline wrong URL with authService.requestPasswordReset)
    - frontend/src/pages/auth/ResetPasswordPage.tsx (modify — authService.confirmPasswordReset)
    - frontend/src/pages/auth/VerifyEmailPage.tsx (modify — authService.verifyEmail)
    - frontend/src/pages/profile/ChangePasswordPage.tsx (modify — authService.changePassword)
    - frontend/src/pages/profile/ProfilePage.tsx (modify — resend → authService.resendVerification)
    - frontend/src/pages/profile/MfaManagementPage.tsx (modify — setup → enrollMfa, confirm → confirmMfa)
    - frontend/src/services/auth.ts (the new service from Task 1)
  </read_first>
  <action>
    In each page, remove the inline `api.post("/auth/...")` / bare-axios call and replace it with the matching `authService.*` method. Preserve existing loading/error/success UI state handling and component structure — only the network call changes. For VerifyEmailPage the call is on-mount (GET with token from the query string). For MfaManagementPage update both the setup (enroll) and confirm calls. For ProfilePage only the resend-verification call changes. Do not alter unrelated profile/MFA UI.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | tail -15 && grep -rn '"/auth/forgot-password"\|"/auth/reset-password"\|"/auth/change-password"\|"/auth/mfa/setup"\|"/auth/mfa/confirm"' src/pages | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: each of the six pages imports and calls `authService.*` (grep `authService.` matches in all six).
    - SOURCE: the grep for old wrong URLs in `src/pages` returns no matches.
    - BUILD: `npx tsc -b` clean.
  </acceptance_criteria>
  <done>All six pages route through authService; no stale wrong URLs remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Playwright auth-contract spec (Wave 0 gap — new spec)</name>
  <files>frontend/e2e/auth-contract.spec.ts</files>
  <behavior>
    - ForgotPasswordPage submit → intercepted request URL contains /api/v1/auth/reset and NOT /auth/forgot-password.
    - ResetPasswordPage → /api/v1/auth/reset/confirm.
    - VerifyEmailPage → /api/v1/auth/verify-email.
    - ProfilePage resend → /api/v1/auth/resend-verification.
    - ChangePasswordPage → /api/v1/auth/password/change.
    - MfaManagementPage enroll → /api/v1/auth/mfa/setup/enroll; confirm → /api/v1/auth/mfa/setup/confirm.
  </behavior>
  <read_first>
    - frontend/e2e/login.spec.ts (describe/test idiom; baseURL usage)
    - frontend/e2e/identity.spec.ts:125-141 (route-intercept + method-check pattern)
    - frontend/e2e/federation.spec.ts:92-103 (outbound-URL assertion)
    - frontend/playwright.config.ts (testDir ./e2e, baseURL, reuseExistingServer)
    - 09-PATTERNS.md (auth-contract describe wrapper + per-flow test skeletons)
  </read_first>
  <action>
    Create `frontend/e2e/auth-contract.spec.ts` with a `test.describe("Auth endpoint contract")`. For each of the six pages, navigate to it, register `page.route("**/auth/**", ...)` to capture the request URL and fulfill 200, drive the page (fill form + click submit, or rely on on-mount for verify-email), then assert the captured URL `toContain` the correct `/api/v1/auth/...` path and `not.toContain` the old wrong path. Use `page.getByLabel`/`getByRole` selectors consistent with the existing specs. For MFA, cover both enroll and confirm. Place the spec in `frontend/e2e/` so `npm test` picks it up. Use the `--grep "auth contract"` tag in the describe name for the targeted run.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --grep "Auth endpoint contract" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: each flow asserts the captured URL contains the correct endpoint and excludes the old wrong path.
    - TEST: `npm test --prefix frontend -- --grep "Auth endpoint contract"` reports all contract tests passing (read OUTPUT text).
  </acceptance_criteria>
  <done>Contract spec proves all 7 endpoints are called at the correct URLs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend | Auth self-service requests must reach the real authenticated routes; wrong paths fail silently and degrade auth UX/security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 (SEC-044/CQ-F27) | Spoofing / Authentication-bypass (silent failure) | six frontend auth pages | mitigate | Route all calls through typed auth.ts at correct /api/v1/auth/* URLs; Playwright contract test gates CI. Observable: contract test asserts each correct URL. |
</threat_model>

<verification>
- `npx tsc -b` clean (frontend).
- `npm test --prefix frontend -- --grep "Auth endpoint contract"` green.
- No stale wrong-URL strings in `src/pages`.
- `npm run lint --prefix frontend` clean before commit (if lint is part of CI gate).
</verification>

<success_criteria>
SEC-044/CQ-F27 closed: six auth flows call real backend endpoints via typed auth.ts; contract test gates CI (REQ-13 success criterion 3).
</success_criteria>

<output>
Create `.planning/phases/09-critical-remediation/09-03-SUMMARY.md` when done
</output>
