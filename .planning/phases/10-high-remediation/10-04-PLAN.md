---
phase: 10-high-remediation
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - crates/axiam-db/src/repository/settings.rs
  - crates/axiam-db/src/schema.rs
  - crates/axiam-amqp/src/connection.rs
  - crates/axiam-amqp/src/audit_consumer.rs
  - crates/axiam-amqp/src/authz_consumer.rs
  - crates/axiam-server/src/cleanup.rs
  - crates/axiam-core/src/models/gdpr.rs
  - crates/axiam-api-rest/src/handlers/gdpr.rs
  - crates/axiam-db/tests/req14_settings_migration_test.rs
  - crates/axiam-server/tests/req14_gdpr_test.rs
autonomous: true
requirements: [REQ-14]
must_haves:
  truths:
    - "Tenant settings store only sparse overrides; an org baseline change propagates to tenant effective settings"
    - "Migrations run twice against a fresh DB without error (idempotent + transactional)"
    - "Failed audit/authz messages dead-letter instead of being dropped or hot-looping"
    - "GDPR purge is re-selectable after a mid-step failure; export is complete and paginated; failed export jobs reach Failed status"
  artifacts:
    - path: "crates/axiam-db/src/repository/settings.rs"
      provides: "sparse override store (no full merged snapshot)"
      contains: "diff_against_org"
    - path: "crates/axiam-amqp/src/connection.rs"
      provides: "DLX on audit + authz queues"
      contains: "dead-letter"
    - path: "crates/axiam-core/src/models/gdpr.rs"
      provides: "ExportJobStatus::Failed variant"
      contains: "Failed"
  key_links:
    - from: "crates/axiam-db/src/repository/settings.rs"
      to: "diff_against_org + upsert sparse"
      via: "store path mirrors set_tenant_override"
      pattern: "diff_against_org"
    - from: "crates/axiam-server/src/cleanup.rs"
      to: "ExportJobRepository::mark_failed"
      via: "process_export_job error handler"
      pattern: "mark_failed"
---

<objective>
Fix data-correctness defects: sparse tenant settings, idempotent migrations, AMQP dead-lettering, and GDPR purge/export completeness. Closes REQ-14 AC-3 and AC-5 (data-correctness slice): CQ-B03/SEC-033, CQ-B06, CQ-B05, CQ-B38/SEC-056.

Purpose: (1) tenant settings store a fully-merged snapshot, so a later org baseline change does NOT propagate (stale config). (2) migrations apply DDL then record version in two non-atomic queries with no startup lock — a failure mid-way re-runs and v3 DEFINE TABLE errors on existing tables. (3) failed audit messages are silently dropped and bad authz messages hot-loop. (4) GDPR purge can leave a user un-marked on partial failure, export swallows errors / truncates audit at 10k / hardcodes empty arrays, and failed export jobs never reach a Failed state.

Output: sparse settings store; transactional/idempotent migrations; DLQ parity for audit+authz; correct re-selectable, complete, paginated GDPR purge/export with a Failed status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-high-remediation/10-RESEARCH.md

<interfaces>
<!-- settings.rs: reuse the correct pattern already present -->
// diff_against_org(&org, &tenant_row) -> TenantSettingsOverride (sparse Option fields)  [exists]
// set_tenant_override (493-510) already stores sparse + re-diffs  [correct pattern to mirror]
// get_effective_settings (524-542) already re-merges at read time  [correct]
// BUG: store_effective_tenant_settings (482-491) stores the merged snapshot
// Only caller: crates/axiam-api-rest/src/handlers/settings.rs:164

<!-- AMQP: MAIL_OUTBOUND DLX pattern at connection.rs:129-142 is the template -->
// audit_consumer.rs:140 nack default (requeue:false, no DLX → drop)
// authz_consumer.rs:175-181 nack requeue:true → hot-loop

<!-- GDPR: cleanup.rs purge_user 233-308, aggregate_export 440-553, process_export_job ~362-375 -->
// ExportJobStatus enum (gdpr.rs:84-87): Queued, Ready, Downloaded  → add Failed
// gdpr.rs handler download at 210-254 (mark_downloaded then delete — TOCTTOU)
// SurrealDB: never hard-delete (anonymize-in-place); transaction slot offset take(1) (MEMORY.md)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org admin→tenant settings | baseline changes must reach tenants that didn't override |
| broker→consumer | poison messages must not drop silently or hot-loop |
| data-subject→GDPR export/erasure | Art. 15/17 must be complete and recoverable on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | axiam-db settings.rs | mitigate | Store sparse overrides (diff_against_org) so org baseline changes propagate; stale merged snapshots no longer mask updated security settings (CQ-B03/SEC-033) |
| T-10-08 | Denial of Service / Repudiation | axiam-amqp consumers | mitigate | Configure DLX on audit+authz queues; nack(requeue:false) dead-letters poison messages instead of dropping (audit loss) or hot-looping (authz DoS) (CQ-B05) |
| T-10-09 | Repudiation | axiam-server GDPR purge/export | mitigate | Re-orderable purge (anonymize last / transactional), error-propagating + complete + paginated export, Failed job status — Art. 17 erasure is recoverable and Art. 15 export is complete (CQ-B38/SEC-056) |
| T-10-10 | Tampering | axiam-db schema.rs migrations | mitigate | Wrap DDL+version-record in a transaction, add startup lock, ensure IF NOT EXISTS — prevents partial/racing migrations corrupting schema (CQ-B06) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sparse tenant settings + idempotent/transactional migrations</name>
  <files>crates/axiam-db/src/repository/settings.rs, crates/axiam-db/src/schema.rs, crates/axiam-db/tests/req14_settings_migration_test.rs</files>
  <read_first>
    - crates/axiam-db/src/repository/settings.rs (store_effective_tenant_settings 482-491; set_tenant_override 493-510; get_effective_settings 524-542; diff_against_org)
    - crates/axiam-api-rest/src/handlers/settings.rs (caller at line 164)
    - crates/axiam-db/src/schema.rs (run_migrations 979-1031; MIGRATION_TABLE_DDL IF NOT EXISTS model at line 17)
  </read_first>
  <behavior>
    - Test `settings_baseline_propagates`: create org settings; create tenant overriding field X; change org baseline for field Y; read tenant effective settings → Y reflects the new baseline, X keeps the override.
    - Test `migration_runs_twice`: run run_migrations against an in-memory SurrealDB twice → both succeed, no DEFINE-TABLE-exists error.
  </behavior>
  <action>
    (settings) Change `store_effective_tenant_settings` to store only the sparse diff — reuse `diff_against_org` + the upsert from `set_tenant_override` (493-510) rather than persisting the full merged `SecuritySettings`. Update the single caller (settings.rs:164) to the sparse-store semantics. Leave get_effective_settings (524-542) as-is (already re-merges). Per CQ-B03/SEC-033 (REQ-14 AC-3).
    (schema) Wrap each migration's `db.query(migration.sql)` + version-record `CREATE _migration ...` in `BEGIN TRANSACTION ... COMMIT TRANSACTION` (slot offset: first stmt take(1)). Add a `_migration_lock` record (`CREATE ... IF NOT EXISTS`) to guard concurrent startup. Audit SCHEMA_V* DEFINE TABLE/FIELD/INDEX for missing `IF NOT EXISTS`/`OVERWRITE` and add where absent (v3 errors on existing tables — MEMORY.md). Per CQ-B06 (REQ-14 AC-5).
  </action>
  <verify>
    <automated>cargo test -p axiam-db --no-default-features --test req14_settings_migration_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'diff_against_org' crates/axiam-db/src/repository/settings.rs` reflects the store path using it (>= 2 usages: read-merge + store).
    - `grep -c 'BEGIN TRANSACTION' crates/axiam-db/src/schema.rs` >= 1 and `grep -c '_migration_lock' crates/axiam-db/src/schema.rs` >= 1.
    - `cargo test -p axiam-db --no-default-features --test req14_settings_migration_test` both tests pass.
  </acceptance_criteria>
  <done>Settings store sparse + propagate baseline; migrations idempotent and transactional; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: AMQP DLQ parity for audit + authz consumers</name>
  <files>crates/axiam-amqp/src/connection.rs, crates/axiam-amqp/src/audit_consumer.rs, crates/axiam-amqp/src/authz_consumer.rs</files>
  <read_first>
    - crates/axiam-amqp/src/connection.rs (MAIL_OUTBOUND DLX pattern at 129-142; ALL_QUEUES loop)
    - crates/axiam-amqp/src/audit_consumer.rs (nack at 140)
    - crates/axiam-amqp/src/authz_consumer.rs (nack requeue:true at 175-181)
  </read_first>
  <action>
    (connection.rs) Declare `axiam.audit.events.dlq` and `axiam.authz.requests.dlq` as plain-durable queues (mirror MAIL_OUTBOUND_DLQ), then re-declare `axiam.audit.events` and `axiam.authz.requests` with `x-dead-letter-exchange` (FieldTable) pointing at their DLQs — copy the exact MAIL_OUTBOUND block at 129-142. (audit_consumer.rs:140) set `nack(BasicNackOptions { requeue: false, ..Default::default() })` so failures dead-letter. (authz_consumer.rs:175-181) change `requeue: true` → `requeue: false` so a poison message dead-letters instead of hot-looping. Per CQ-B05 (REQ-14 AC-5). No broker defaults — explicit DLX routing only (consistent with prior D-14 decision).
  </action>
  <note>This task is compile-only verified: there is no in-process RabbitMQ broker in unit tests, so DLX routing cannot be asserted at runtime here. A live DLQ smoke test (publish a poison message, confirm it lands on the .dlq queue) is deferred to a later phase's manual/integration verification.</note>
  <verify>
    <automated>cargo check -p axiam-amqp --no-default-features 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dlq\|dead-letter\|x-dead-letter-exchange' crates/axiam-amqp/src/connection.rs` covers both audit and authz queues (>= the MAIL pattern + 2 new).
    - `grep -c 'requeue: false' crates/axiam-amqp/src/audit_consumer.rs` >= 1 and same in authz_consumer.rs.
    - No `requeue: true` remains in authz_consumer.rs.
    - `cargo check -p axiam-amqp --no-default-features` clean.
  </acceptance_criteria>
  <done>Audit+authz queues have DLX; failed messages dead-letter; no hot-loop or silent drop; crate compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GDPR purge re-selectable, complete paginated export, Failed status</name>
  <files>crates/axiam-server/src/cleanup.rs, crates/axiam-core/src/models/gdpr.rs, crates/axiam-api-rest/src/handlers/gdpr.rs, crates/axiam-server/tests/req14_gdpr_test.rs</files>
  <read_first>
    - crates/axiam-server/src/cleanup.rs (purge_user 233-308; aggregate_export_data 440-553; process_export_job ~362-375)
    - crates/axiam-core/src/models/gdpr.rs (ExportJobStatus 84-87)
    - crates/axiam-api-rest/src/handlers/gdpr.rs (download 210-254)
  </read_first>
  <behavior>
    - Test `purge_reselectable_after_partial_failure`: simulate mark_completed failing after anonymize-style step is deferred → re-running purge still finds the pending row and completes (anonymize is the LAST step / or the whole thing is transactional).
    - Test `export_complete_and_paginated`: create a user with >10k audit entries and rows in webauthn/group_membership/assignment tables; export contains all audit pages and non-empty arrays for those categories.
    - Test `export_failure_sets_failed_status`: a failing export job transitions to `ExportJobStatus::Failed` (not stuck Queued).
  </behavior>
  <action>
    (purge ordering) Reorder purge_user so `anonymize_user` runs LAST (after mark_completed + erasure_proof), or wrap the sequence in `BEGIN TRANSACTION ... COMMIT` so a mid-step failure leaves the row re-selectable. (export errors) Replace `unwrap_or_default()` on consent_repo.list_by_user (466) and federation_link_repo (520) with error propagation or `unwrap_or_else(|e| { tracing::warn!(...); Default::default() })` (no silent swallow). (paginated audit) Replace the hardcoded `limit: 10_000` audit export with a loop over Pagination offset until `items.len() < limit`, collecting all pages. (complete export) Populate `sessions`/`assignments`/`group_memberships`/`webauthn_credentials` (534-549) from their repos instead of `[]`. (Failed status) Add `Failed` to `ExportJobStatus` (gdpr.rs), add `mark_failed(job_id)` to ExportJobRepository, call it in process_export_job's error handler. (atomic download) REPLACE the existing two-step `mark_downloaded(...)` + `delete(...)` sequence in gdpr.rs (210-254) with a SINGLE atomic statement `UPDATE export_job SET status = 'Downloaded' WHERE id = $id AND status = 'Ready'`; treat 0 rows-affected as already-used (return the already-consumed path). Remove the standalone `mark_downloaded` call entirely — it must not remain alongside the atomic update. Per CQ-B38/SEC-056 (REQ-14 AC-5).
  </action>
  <verify>
    <automated>cargo test -p axiam-server --no-default-features --test req14_gdpr_test 2>&1 | tail -20 && cargo check -p axiam-api-rest --no-default-features 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Failed' crates/axiam-core/src/models/gdpr.rs` includes the new enum variant; `grep -c 'mark_failed' crates/axiam-server/src/cleanup.rs` >= 1.
    - No `unwrap_or_default()` remains on the consent/federation export reads (replaced by `?` or logged default).
    - No literal `10_000` audit-export cap remains (replaced by a pagination loop).
    - `WHERE status = 'Ready'` atomic update present in gdpr.rs download handler.
    - No standalone `mark_downloaded` call remains in `crates/axiam-api-rest/src/handlers/gdpr.rs` — `grep -c 'mark_downloaded' crates/axiam-api-rest/src/handlers/gdpr.rs` == 0.
    - `cargo test -p axiam-server --no-default-features --test req14_gdpr_test` all tests pass; `cargo check -p axiam-api-rest --no-default-features` clean.
  </acceptance_criteria>
  <done>Purge re-selectable, export complete + paginated, Failed status added, download atomic; tests green.</done>
</task>

</tasks>

<verification>
- `cargo test -p axiam-db --no-default-features --test req14_settings_migration_test` green.
- `cargo check -p axiam-amqp --no-default-features` clean.
- `cargo test -p axiam-server --no-default-features --test req14_gdpr_test` green; `cargo check -p axiam-api-rest --no-default-features` clean.
- Depends on 10-01 + 10-02 green gate (Wave A foundational). No file overlap with 10-03 → runs parallel with 10-03 in wave 3.
</verification>

<success_criteria>
- REQ-14 AC-3 satisfied; AC-5 data-correctness slice satisfied.
- T-10-07, T-10-08, T-10-09, T-10-10 mitigated.
</success_criteria>

<output>
Create `.planning/phases/10-high-remediation/10-04-SUMMARY.md` when done.
</output>
