---
phase: 10-high-remediation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/pgp/PgpKeysPage.tsx
  - frontend/src/components/ConfirmDialog.tsx
  - frontend/src/pages/audit/AuditLogsPage.tsx
  - frontend/src/pages/roles/RoleDetailPage.tsx
  - frontend/src/pages/groups/GroupDetailPage.tsx
  - frontend/src/components/UserSearchDialog.tsx
  - frontend/src/components/layout/Topbar.tsx
  - frontend/src/lib/api.ts
  - frontend/src/pages/organizations/OrganizationDetailPage.tsx
  - frontend/src/pages/tenants/TenantsPage.tsx
  - frontend/src/pages/certificates/CertificatesPage.tsx
  - frontend/src/pages/profile/MfaManagementPage.tsx
  - frontend/src/pages/service-accounts/ServiceAccountsPage.tsx
  - .github/workflows/ci.yml
autonomous: true
requirements: [REQ-14]
must_haves:
  truths:
    - "PgpKeysPage uses the authenticated user's real id, not a hardcoded placeholder"
    - "ConfirmDialog renders a caller-supplied confirm label instead of hardcoded 'Delete'"
    - "AuditLogsPage debounce timers are cleared on unmount and on filter reset"
    - "Role/Group user search uses useQuery (no manual setTimeout debounce)"
    - "Logout calls the backend, clears the query cache, then clears auth"
    - "CI runs eslint and tsc -b for the frontend"
    - "OrganizationDetailPage initializes form from settings and saves"
    - "TenantsPage does not show a fabricated 'active' status"
  artifacts:
    - path: "frontend/src/components/UserSearchDialog.tsx"
      provides: "shared user-search dialog using useQuery"
      contains: "useQuery"
    - path: "frontend/src/components/layout/Topbar.tsx"
      provides: "logout that revokes session + clears query cache"
      contains: "queryClient.clear"
    - path: ".github/workflows/ci.yml"
      provides: "frontend lint + tsc -b gate"
      contains: "tsc -b"
  key_links:
    - from: "frontend/src/pages/pgp/PgpKeysPage.tsx"
      to: "useAuthStore().user.id"
      via: "real current-user id"
      pattern: "user\\?\\.id"
    - from: "frontend/src/components/layout/Topbar.tsx"
      to: "POST /api/v1/auth/logout + queryClient.clear"
      via: "handleLogout"
      pattern: "auth/logout"
---

<objective>
Fix the eight frontend High-severity items and add a CI lint/type-check gate. Closes REQ-14 AC-6 (CQ-F01–F08) and SEC-015 (logout).

Purpose: real correctness and one security defect on the client: PgpKeysPage signs against a fake "current-user" id; ConfirmDialog hardcodes "Delete" even on non-destructive actions; AuditLogsPage leaks debounce timers; Role/Group search hand-rolls debounce; logout never revokes the server session nor clears cached data (stale data leaks to the next user — SEC-015); CI doesn't lint or type-check the frontend; OrganizationDetailPage's settings form never initializes; TenantsPage fabricates an "active" status the model doesn't have.

Output: all eight items fixed, a shared UserSearchDialog, and a CI step running `npm run lint && npx tsc -b`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-high-remediation/10-RESEARCH.md

<interfaces>
<!-- auth store: AuthUser.id: string exists; useAuthStore() exposes user + clearAuth (frontend/src/stores/auth.ts:4,20,43) -->
<!-- userService.list(page, perPage, search) already accepts a search param (services/users.ts:70-75) — search dialog should call list with the search arg -->
<!-- TenantsPage: Tenant model has NO status field (verified: organizations.ts Tenant interface = id,name,slug,description?,org_id,created_at). The "active" column at TenantsPage.tsx:378-381 is fabricated → REMOVE the Status column. -->
<!-- ConfirmDialog hardcodes "Delete" at line 106; add confirmLabel?: string prop -->
<!-- Topbar handleLogout at 86-89: only clearAuth + navigate; api client is frontend/src/lib/api.ts (refresh interceptor ~108) -->
<!-- AuditLogsPage debounce timers actorTimer/actionTimer at 203-204, used 241-250 -->
<!-- RoleDetailPage manual debounce 285-298; GroupDetailPage manual debounce 83-97 -->
<!-- OrganizationDetailPage dead syncedRef at 632-635; handleSubmit at 662-667 already mutates -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user session→shared browser | logout must leave no cached identity/data for the next user |
| client UI→displayed state | UI must not present fabricated or wrong-identity data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | Information Disclosure | Topbar logout | mitigate | Logout calls `POST /api/v1/auth/logout` to revoke the server session AND `queryClient.clear()` so cached data is not shown to the next user on a shared machine (SEC-015/CQ-F05) |
| T-10-17 | Spoofing | PgpKeysPage current-user id | mitigate | Use the authenticated user's real id so PGP keys bind to the actual subject, not a placeholder (CQ-F01) |
| T-10-18 | Tampering (UI integrity) | TenantsPage status | accept→fix | Remove the fabricated "active" badge; do not present data the model does not have (CQ-F08). Residual: none |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Identity + logout security (CQ-F01, CQ-F05/SEC-015, CQ-F08)</name>
  <files>frontend/src/pages/pgp/PgpKeysPage.tsx, frontend/src/components/layout/Topbar.tsx, frontend/src/lib/api.ts, frontend/src/pages/tenants/TenantsPage.tsx</files>
  <read_first>
    - frontend/src/pages/pgp/PgpKeysPage.tsx (hardcoded "current-user" at 267-268, 314)
    - frontend/src/components/layout/Topbar.tsx (handleLogout 86-89)
    - frontend/src/lib/api.ts (refresh-failure path ~108)
    - frontend/src/pages/tenants/TenantsPage.tsx (status column 378-381)
    - frontend/src/stores/auth.ts (useAuthStore, AuthUser.id)
  </read_first>
  <action>
    (CQ-F01) In PgpKeysPage, `const { user } = useAuthStore();` and `const currentUserId = user?.id ?? "";` — replace the `"current-user"` literal at 267-268 (and the use at 314). (CQ-F05/SEC-015) In Topbar, make `handleLogout` async: `try { await api.post("/api/v1/auth/logout", {}); } catch {}` then `queryClient.clear()` (via `useQueryClient()`), then `clearAuth()`, then navigate to /login. In api.ts, on refresh failure (~108) call `queryClient.clear()` before `clearAuth()` (import the shared queryClient instance). (CQ-F08) In TenantsPage, REMOVE the Status column at 378-381 entirely — the Tenant model has no status field, so fabricating "active" is wrong; do not substitute another placeholder. Per CQ-F01/CQ-F05/CQ-F08 (REQ-14 AC-6).
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -15 && npx tsc -b 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"current-user"' frontend/src/pages/pgp/PgpKeysPage.tsx` == 0; `grep -c 'user?.id' frontend/src/pages/pgp/PgpKeysPage.tsx` >= 1.
    - `grep -c 'auth/logout' frontend/src/components/layout/Topbar.tsx` >= 1 and `grep -c 'queryClient.clear' frontend/src/components/layout/Topbar.tsx` >= 1.
    - `grep -c 'status="active"' frontend/src/pages/tenants/TenantsPage.tsx` == 0.
    - `npm run lint` and `npx tsc -b` exit clean (read output text).
  </acceptance_criteria>
  <done>Real user id used for PGP; logout revokes session + clears cache; no fabricated tenant status; lint+types clean.</done>
</task>

<task type="auto">
  <name>Task 2: Debounce + search correctness (CQ-F02, F03, F04, F07)</name>
  <files>frontend/src/components/ConfirmDialog.tsx, frontend/src/pages/audit/AuditLogsPage.tsx, frontend/src/pages/roles/RoleDetailPage.tsx, frontend/src/pages/groups/GroupDetailPage.tsx, frontend/src/components/UserSearchDialog.tsx, frontend/src/pages/organizations/OrganizationDetailPage.tsx, frontend/src/pages/pgp/PgpKeysPage.tsx, frontend/src/pages/certificates/CertificatesPage.tsx, frontend/src/pages/profile/MfaManagementPage.tsx, frontend/src/pages/service-accounts/ServiceAccountsPage.tsx</files>
  <read_first>
    - frontend/src/components/ConfirmDialog.tsx (props 4-11; hardcoded "Delete" 99,106)
    - frontend/src/pages/audit/AuditLogsPage.tsx (timers 203-204, 241-250)
    - frontend/src/pages/roles/RoleDetailPage.tsx (manual debounce 285-298)
    - frontend/src/pages/groups/GroupDetailPage.tsx (manual debounce 83-97)
    - frontend/src/pages/organizations/OrganizationDetailPage.tsx (dead syncedRef 632-635; handleSubmit 662-667)
    - frontend/src/services/users.ts (list(page, perPage, search) at 70-75)
  </read_first>
  <action>
    (CQ-F02) Add `confirmLabel?: string` to `ConfirmDialogProps`; render `{confirmLabel ?? "Delete"}` at line 106. Then update each NON-DELETE ConfirmDialog call site to pass an explicit `confirmLabel` (the optional prop will not force this at the type level, so it must be done by hand at these enumerated sites): PgpKeysPage.tsx "Revoke PGP Key" → `confirmLabel="Revoke"`; CertificatesPage.tsx "Revoke Certificate" → `confirmLabel="Revoke"`; MfaManagementPage.tsx "Remove MFA Method" → `confirmLabel="Remove"`; ServiceAccountsPage.tsx "Rotate Client Secret" → `confirmLabel="Rotate"`. All other call sites are genuine "Delete X" dialogs and correctly keep the default. (CQ-F03) In AuditLogsPage add a `useEffect` cleanup that `clearTimeout`s `actorTimer`/`actionTimer` on unmount, and clear them in the Clear-filters handler. (CQ-F04) Extract a shared `UserSearchDialog` component that uses `useQuery({ queryKey: ["user-search", searchTerm, tenantId], queryFn: () => userService.list(1, 20, searchTerm), enabled: searchTerm.length >= 2 })`; replace the manual setTimeout/clearTimeout debounce in both RoleDetailPage (285-298) and GroupDetailPage (83-97) with this component. (CQ-F07) In OrganizationDetailPage replace the dead `syncedRef` (632-635) with `useEffect(() => { if (settings) setForm(settings); }, [settings?.id])` so the form initializes from loaded settings (handleSubmit at 662-667 already mutates — leave it). Per CQ-F02/F03/F04/F07 (REQ-14 AC-6).
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -15 && npx tsc -b 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'confirmLabel' frontend/src/components/ConfirmDialog.tsx` >= 1 (prop added + rendered).
    - Each enumerated non-delete call site passes `confirmLabel`: `grep -c 'confirmLabel' frontend/src/pages/pgp/PgpKeysPage.tsx` >= 1, same for CertificatesPage.tsx, MfaManagementPage.tsx, ServiceAccountsPage.tsx (4 sites total).
    - `grep -rln 'ConfirmDialog' frontend/src/ | grep -v ConfirmDialog.tsx` reviewed: every Revoke/Remove/Rotate dialog passes a label; only true "Delete X" dialogs use the default.
    - `grep -c 'clearTimeout' frontend/src/pages/audit/AuditLogsPage.tsx` >= 1 inside a useEffect cleanup.
    - `frontend/src/components/UserSearchDialog.tsx` exists and contains `useQuery`; RoleDetailPage and GroupDetailPage no longer contain `setTimeout(` for user search.
    - `grep -c 'syncedRef' frontend/src/pages/organizations/OrganizationDetailPage.tsx` == 0; a `useEffect` initializing form from settings is present.
    - `npm run lint` and `npx tsc -b` clean.
  </acceptance_criteria>
  <done>ConfirmDialog label configurable; audit timers cleaned; shared useQuery search dialog; org settings form initializes; lint+types clean.</done>
</task>

<task type="auto">
  <name>Task 3: CI lint + type-check gate (CQ-F06)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (frontend job; npm ci + npm run build around 242 — no lint/tsc step)
  </read_first>
  <action>
    Resolve any outstanding eslint errors surfaced by `npm run lint` (enumerate with `npm run lint 2>&1`) across the frontend so the gate passes. Then add a CI step in ci.yml's frontend job, after `npm ci` and before `npm run build`: a "Frontend lint and type-check" step with `working-directory: frontend` running `npm run lint && npx tsc -b`. Per CQ-F06 (REQ-14 AC-6).
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -15 && npx tsc -b 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tsc -b' .github/workflows/ci.yml` >= 1 and `grep -c 'npm run lint' .github/workflows/ci.yml` >= 1.
    - `npm run lint` reports zero errors; `npx tsc -b` reports zero errors (read output text).
  </acceptance_criteria>
  <done>Frontend lint+type-check gate added to CI and passing locally.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint && npx tsc -b` clean.
- `npm test --prefix frontend` (vitest) green if existing tests touch these components.
- Fully independent of the backend plans (no shared files) → wave 1, parallel with 10-01/10-02.
</verification>

<success_criteria>
- REQ-14 AC-6 satisfied: all eight frontend High items fixed; CI lint/tsc gate added.
- T-10-16, T-10-17, T-10-18 mitigated.
</success_criteria>

<output>
Create `.planning/phases/10-high-remediation/10-06-SUMMARY.md` when done.
</output>
