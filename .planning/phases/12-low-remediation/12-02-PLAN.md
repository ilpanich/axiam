---
phase: 12-low-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-db/src/repository/user.rs
  - claude_dev/design-document.md
  - /home/emanuele/git/priv/axiam/CLAUDE.md
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [REQ-16]

must_haves:
  truths:
    - "The user list endpoint never returns mfa_secret (explicit column projection excludes it)"
    - "Debug formatting of a UserRow redacts mfa_secret and totp_last_used_step"
    - "Docs accurately describe the RBAC engine as additive-only / default-deny with no explicit deny override"
    - "Every GitHub Actions `uses:` line in ci.yml and release.yml is pinned to a 40-char commit SHA"
  artifacts:
    - path: "crates/axiam-db/src/repository/user.rs"
      provides: "Redacted Debug impl + mfa_secret-excluding list projection"
      contains: "REDACTED"
    - path: ".github/workflows/ci.yml"
      provides: "SHA-pinned Actions"
      contains: "uses:"
  key_links:
    - from: "crates/axiam-db/src/repository/user.rs list query"
      to: "user table"
      via: "explicit column projection excluding mfa_secret"
      pattern: "SELECT meta::id"
---

<objective>
Close the backend security-polish LOW findings: SEC-043 (encrypted mfa_secret no longer Debug-derived or hydrated on list paths), SEC-040 (RBAC docs corrected to reflect the additive-only engine — doc fix, NOT an engine rewrite), SEC-057 (GitHub Actions pinned by commit SHA).

Purpose: Stop the AES-256-GCM-encrypted mfa_secret blob from leaking into debug logs and list responses; remove a false security claim from the design docs; eliminate the mutable-tag supply-chain vector in CI/release workflows.
Output: Custom redacting Debug impls + mfa_secret-excluding list SELECT in user.rs; corrected design-document.md + CLAUDE.md wording; every `uses:` in ci.yml/release.yml SHA-pinned.

NOTE — SEC-040 is a DOCUMENTATION correction only (option b). Do NOT implement a deny-override cascade (new DenyPermission model + migration + engine rewrite + tests) — that is out of scope for a LOW finding and is deferred to post-v1.0-beta. State this explicitly in the commit/SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-low-remediation/12-RESEARCH.md
@.planning/phases/12-low-remediation/12-PATTERNS.md
@/home/emanuele/git/priv/axiam/CLAUDE.md

BUILD-SCOPE (project memory — bake into verify):
- NO whole-workspace cargo test/build. Use `cargo check -p axiam-db --tests --no-default-features` and targeted `cargo test -p axiam-db --lib`.
- Verify cargo via output text, not exit code.

<interfaces>
From crates/axiam-db/src/repository/user.rs:
  struct UserRow         (line ~20) `#[derive(Debug, SurrealValue)]` — includes mfa_secret: Option<String>, totp_last_used_step
  struct UserRowWithId   (line ~45) `#[derive(Debug, SurrealValue)]` — same sensitive fields
  list query (line ~458) `SELECT meta::id(id) AS record_id, * FROM user WHERE tenant_id = $tenant_id ...`  // `*` hydrates mfa_secret

SEC-040 doc target: claude_dev/design-document.md:385 — "role on a parent grants access to children unless an explicit deny exists at a lower level" (the false "explicit deny" claim).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEC-043 — redact mfa_secret in Debug + exclude from list projection</name>
  <files>crates/axiam-db/src/repository/user.rs</files>
  <read_first>
    - crates/axiam-db/src/repository/user.rs:20-68 (UserRow + UserRowWithId derives and fields)
    - crates/axiam-db/src/repository/user.rs:455-468 (list query using SELECT ... * ...)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (user.rs section: custom Debug impl + explicit column list)
    - MEMORY.md (SurrealValue derive requirements; meta::id projection pattern)
  </read_first>
  <behavior>
    - `format!("{:?}", user_row)` prints "[REDACTED]" in place of any present mfa_secret and totp_last_used_step value, and never the underlying bytes/string
    - The list query selects an explicit column list that does NOT include mfa_secret or totp_last_used_step
    - get_by_id still returns the full UserRow (mfa_secret available where legitimately needed for MFA verification)
  </behavior>
  <action>
    Remove `Debug` from the `#[derive(...)]` on UserRow and UserRowWithId; add manual `impl std::fmt::Debug` for each that prints non-sensitive fields verbatim and renders `mfa_secret`/`totp_last_used_step` as `&self.field.as_ref().map(|_| "[REDACTED]")`, ending with `.finish_non_exhaustive()`. Replace the list query `SELECT meta::id(id) AS record_id, * FROM user ...` with an explicit column projection listing only non-secret columns (record_id, tenant_id, username, email, status, mfa_enabled, failed_login_attempts, last_failed_login_at, locked_until, email_verified_at, deletion_pending, scheduled_purge_at, metadata, created_at, updated_at — adjust to the actual UserRowWithId fields). Ensure the list-path row struct sets `mfa_secret: None` and `totp_last_used_step: None` after the projection (simplest: drop those fields from UserRowWithId and default them to None in try_into_user). Keep mfa_secret in UserRow used by get_by_id.
  </action>
  <verify>
    <automated>cargo test -p axiam-db --lib --no-default-features 2>&1 | tail -8; cargo check -p axiam-db --tests --no-default-features 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'derive\(Debug' crates/axiam-db/src/repository/user.rs` no longer matches UserRow / UserRowWithId derives (Debug removed from derive list)
    - `grep -c 'REDACTED' crates/axiam-db/src/repository/user.rs` >= 2 (mfa_secret + totp_last_used_step redacted in the manual Debug impls)
    - The list query body does NOT contain a bare `*` projection for the user table; `grep -n 'mfa_secret' crates/axiam-db/src/repository/user.rs` shows mfa_secret is absent from the list SELECT column list
    - `cargo check -p axiam-db --tests --no-default-features` exits 0 and `cargo test -p axiam-db --lib --no-default-features` shows `test result: ok` (read output text)
  </acceptance_criteria>
  <done>mfa_secret never appears in Debug output and is never hydrated by the list endpoint; db crate compiles + unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-040 — correct RBAC docs to additive-only (no engine change)</name>
  <files>claude_dev/design-document.md, /home/emanuele/git/priv/axiam/CLAUDE.md</files>
  <read_first>
    - claude_dev/design-document.md:385 (the "unless an explicit deny exists" sentence)
    - crates/axiam-authz/src/engine.rs (confirm additive-only: Allow wins, default-deny, no deny mechanism — read enough to confirm before editing docs)
    - .planning/phases/12-low-remediation/12-RESEARCH.md (SEC-040 section, option b)
  </read_first>
  <action>
    In claude_dev/design-document.md:385, remove the "unless an explicit deny exists at a lower level" clause and replace with an accurate statement: the engine is additive-only (allow-wins) with default-deny for missing grants; explicit deny permissions are not implemented in v1.0-beta. In CLAUDE.md, add a one-line note under the authorization/RBAC area: "RBAC engine is additive-only (allow-wins, default-deny); there is no explicit deny-override mechanism." Do NOT modify engine.rs. This is a doc-only non-fix and the SUMMARY must say so (deny-override cascade deferred to post-v1.0-beta / Phase 19).
  </action>
  <verify>
    <automated>grep -n "explicit deny" claude_dev/design-document.md; grep -n "additive-only" claude_dev/design-document.md /home/emanuele/git/priv/axiam/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "unless an explicit deny exists" claude_dev/design-document.md` returns nothing
    - `grep -n "additive-only" claude_dev/design-document.md` matches AND `grep -n "additive-only" /home/emanuele/git/priv/axiam/CLAUDE.md` matches
    - `git diff --stat crates/axiam-authz/src/engine.rs` shows NO changes to engine.rs
  </acceptance_criteria>
  <done>Docs accurately describe the additive-only engine; no engine code changed.</done>
</task>

<task type="auto">
  <name>Task 3: SEC-057 — pin all GitHub Actions to commit SHAs</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/ci.yml (all `uses:` lines)
    - .github/workflows/release.yml (all `uses:` lines)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (SHA-pinning section: action list + known-SHA table — VERIFY each SHA on the action's GitHub releases page before committing)
  </read_first>
  <action>
    For every `uses: org/action@<tag>` line in both workflows, replace the mutable tag with `uses: org/action@<40-char-sha>  # <tag>`. Obtain each SHA from the action's official GitHub releases page (the PATTERNS table lists candidate SHAs but they MUST be verified — use WebFetch on `https://github.com/<org>/<action>/releases/tag/<tag>` or the `pin-github-action` CLI if available). Pin: actions/checkout, dtolnay/rust-toolchain (use a dated commit, not @master), Swatinem/rust-cache, actions-rust-lang/audit, EmbarkStudios/cargo-deny-action, hadolint/hadolint-action (ci.yml); actions/checkout, docker/setup-buildx-action, docker/login-action, docker/metadata-action, docker/build-push-action, aquasecurity/trivy-action, github/codeql-action/upload-sarif, sigstore/cosign-installer, actions/attest-build-provenance (release.yml). Do NOT change `hadolint no-fail: true` or `trivy exit-code` settings (advisory by design). The comment after each SHA records the human-readable version for future bumps.
  </action>
  <verify>
    <automated>grep -nE 'uses:' .github/workflows/ci.yml .github/workflows/release.yml | grep -vE '@[0-9a-f]{40}' | grep -vE '^\s*#'</automated>
  </verify>
  <acceptance_criteria>
    - The verify grep (every `uses:` line NOT pinned to a 40-hex SHA) returns ZERO lines for both workflows
    - Each pinned line retains a trailing `# <tag>` comment recording the version
    - `hadolint no-fail` and `trivy exit-code` settings are unchanged (`git diff` shows only the `uses:` refs changed for those steps)
  </acceptance_criteria>
  <done>Every Actions reference in ci.yml and release.yml is pinned to a verified commit SHA; advisory scan settings untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB row → log/console | UserRow Debug output may reach logs; must not carry the encrypted MFA secret |
| DB → list API response | List endpoint serializes rows to clients; mfa_secret must never be in the projection |
| GitHub Actions registry → CI runtime | A mutable action tag can be repointed by an attacker who compromises the action repo |
| docs → operator trust | A false "explicit deny" claim gives operators a security assurance the engine does not provide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | UserRow Debug derive leaks AES-GCM mfa_secret into logs (SEC-043) | mitigate | Task 1: manual Debug impl renders mfa_secret/totp_last_used_step as "[REDACTED]" |
| T-12-06 | Information Disclosure | list query `SELECT * ` hydrates mfa_secret into list responses (SEC-043) | mitigate | Task 1: explicit column projection excludes mfa_secret + totp_last_used_step |
| T-12-07 | Tampering / Spoofing | GitHub Actions on mutable tags — supply-chain repoint (SEC-057) | mitigate | Task 3: pin every `uses:` to a verified 40-char commit SHA |
| T-12-08 | Spoofing / Elevation of Privilege | docs claim a deny-override that does not exist (SEC-040) | mitigate (doc-only) | Task 2: correct design-document.md + CLAUDE.md to additive-only. EXPLICIT NON-FIX of engine: deny-override cascade deferred to post-v1.0-beta |
</threat_model>

<verification>
- `cargo check -p axiam-db --tests --no-default-features` exits 0; `cargo test -p axiam-db --lib --no-default-features` green
- No `derive(Debug)` on UserRow/UserRowWithId; >=2 REDACTED markers; no bare `*` user list projection
- No "unless an explicit deny exists" string in design-document.md; "additive-only" present in both docs; engine.rs unchanged
- Zero unpinned `uses:` lines in either workflow
</verification>

<success_criteria>
ROADMAP SC #2 satisfied: SEC-040 CLAUDE.md/design-document wording corrected (engine left additive-only, deny-cascade deferred); encrypted mfa_secret blob no longer Debug-derived nor hydrated on list paths (SEC-043); every GitHub Actions reference pinned by commit SHA (SEC-057).
</success_criteria>

<output>
Create `.planning/phases/12-low-remediation/12-02-SUMMARY.md` when done. Explicitly note SEC-040 was resolved by documentation correction (deny-override cascade deferred), and engine.rs was not modified.
</output>
