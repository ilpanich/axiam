---
phase: 12-low-remediation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/placeholders/Placeholder.tsx
  - frontend/package.json
  - frontend/src/pages/users/UsersPage.tsx
  - frontend/src/pages/BootstrapPage.tsx
  - frontend/src/components/DataTable.tsx
  - frontend/src/lib/utils.ts
  - frontend/src/components/ResourceTree.tsx
  - frontend/src/lib/api.ts
  - frontend/src/hooks/usePermissions.ts
  - frontend/src/pages/tenants/TenantsPage.tsx
  - frontend/src/hooks/useAuthInit.ts
autonomous: true
requirements: [REQ-16]

must_haves:
  truths:
    - "Dead Placeholder.tsx is removed and unused radix deps are pruned without breaking the build"
    - "Admin user-create and bootstrap password fields enforce the password policy"
    - "DataTable row key, date formatting, ResourceTree selector, refresh _retry guard, and StrictMode init are all hardened"
  artifacts:
    - path: "frontend/src/pages/users/UsersPage.tsx"
      provides: "PasswordPolicyChecker on admin-create password"
      contains: "PasswordPolicyChecker"
    - path: "frontend/src/hooks/usePermissions.ts"
      provides: "Module-level stable EMPTY array"
      contains: "EMPTY"
  key_links:
    - from: "frontend/src/pages/users/UsersPage.tsx"
      to: "frontend/src/components/PasswordPolicyChecker"
      via: "import { PasswordPolicyChecker, checkPasswordPolicy }"
      pattern: "PasswordPolicyChecker"
---

<objective>
Close the frontend trivial / cleanup LOW findings: CQ-F21 (dead Placeholder.tsx), CQ-F22 (unused radix deps), CQ-F23 (password-policy checker on admin-create + bootstrap), CQ-F24 (safe DataTable row key), CQ-F25 (i18n / no hardcoded en-US), CQ-F26 (CSS.escape in ResourceTree), CQ-F32 (refresh `_retry` guard ordering), CQ-F33 (stable empty permissions array), CQ-F34 (bootstrap 404 handling), CQ-F35 (StrictMode double-fetch), CQ-F20 (TenantsPage loading flash).

Purpose: Remove dead code/deps and apply small correctness/robustness fixes already proven by analog usage elsewhere in the app.
Output: Deleted Placeholder.tsx, pruned package.json, policy checker on two forms, and eight targeted source hardening edits.

RESOLVED FACT (CQ-F34): the backend bootstrap endpoint returns HTTP 404 when an admin already exists (D-09, confirmed in bootstrap.rs + bootstrap_test.rs:328). The frontend mapping `404 → alreadyInitialized` is therefore CORRECT and must be KEPT. Do NOT switch the mapping to 409. The only improvement to make for CQ-F34 is to keep 404→already-initialized and ensure non-404 errors render a generic error (do not invent a 409 branch the backend never returns).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-low-remediation/12-RESEARCH.md
@.planning/phases/12-low-remediation/12-PATTERNS.md
@/home/emanuele/git/priv/axiam/CLAUDE.md

BUILD-SCOPE (frontend): after each fix `cd frontend && npx tsc -b --noEmit`; whole-plan gate `cd frontend && npm run lint && npx tsc -b`. Run `npm install` after package.json edits.

<interfaces>
PasswordPolicyChecker (analog: frontend/src/pages/auth/ResetPasswordPage.tsx:9,42-45):
  import { PasswordPolicyChecker, checkPasswordPolicy } from "@/components/PasswordPolicyChecker";
  const policyMet = checkPasswordPolicy(password);   // gate submit
  <PasswordPolicyChecker password={password} />       // render after the password Input

Confirmed by inspection: @radix-ui/react-{dialog,dropdown-menu,select,separator} have ZERO import sites in frontend/src/ (incl. components/ui/). Only react-label, react-slot, react-toast are imported.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead-code + dep pruning + i18n + DataTable key + CSS.escape</name>
  <files>frontend/src/pages/placeholders/Placeholder.tsx, frontend/package.json, frontend/src/components/DataTable.tsx, frontend/src/lib/utils.ts, frontend/src/components/ResourceTree.tsx</files>
  <read_first>
    - frontend/src/components/DataTable.tsx:79 (unsafe `as string ?? rowIdx` cast)
    - frontend/src/lib/utils.ts:40,49 (hardcoded "en-US")
    - frontend/src/components/ResourceTree.tsx:81 (querySelector without CSS.escape)
    - frontend/package.json:17-23 (radix deps)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (DataTable / utils / ResourceTree / CQ-F22 radix verification protocol)
  </read_first>
  <action>
    CQ-F21: Delete frontend/src/pages/placeholders/Placeholder.tsx (zero import sites confirmed). Remove the placeholders/ directory if it becomes empty.
    CQ-F22: First run `grep -r "@radix-ui/react-dialog\|@radix-ui/react-dropdown-menu\|@radix-ui/react-select\|@radix-ui/react-separator" frontend/src/` to re-confirm zero usages (incl. components/ui/). For each package with zero matches, remove its line from package.json dependencies (`@radix-ui/react-dialog`, `@radix-ui/react-dropdown-menu`, `@radix-ui/react-select`, `@radix-ui/react-separator`). Keep react-label, react-slot, react-toast. Run `cd frontend && npm install` then `npx tsc -b --noEmit`; if tsc reports a missing module, restore that one package.
    CQ-F24: DataTable.tsx:79 — replace the double-cast key with `key={getRowKey ? getRowKey(row, rowIdx) : String((row as Record<string, unknown>).id ?? rowIdx)}`.
    CQ-F25: utils.ts:40,49 — replace both `new Intl.DateTimeFormat("en-US", ...)` with `new Intl.DateTimeFormat(undefined, ...)` (browser locale).
    CQ-F26: ResourceTree.tsx:81 — wrap the id in `CSS.escape(id)` inside the attribute selector.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -8 && grep -rc "@radix-ui/react-dialog\|@radix-ui/react-dropdown-menu\|@radix-ui/react-select\|@radix-ui/react-separator" src/ | grep -v ':0' || echo "no radix usages"</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/pages/placeholders/Placeholder.tsx` → file not found
    - `grep -E '"@radix-ui/react-(dialog|dropdown-menu|select|separator)"' frontend/package.json` returns nothing (assuming zero usages confirmed)
    - `grep -n 'String(' frontend/src/components/DataTable.tsx` matches the row-key line; no `as string ?? rowIdx` remains
    - `grep -n '"en-US"' frontend/src/lib/utils.ts` returns nothing
    - `grep -n 'CSS.escape' frontend/src/components/ResourceTree.tsx` matches
    - `cd frontend && npx tsc -b --noEmit` succeeds (read output text)
  </acceptance_criteria>
  <done>Dead file + unused radix deps gone, build green, three small correctness fixes applied.</done>
</task>

<task type="auto">
  <name>Task 2: PasswordPolicyChecker on admin-create + bootstrap (keep 404 mapping)</name>
  <files>frontend/src/pages/users/UsersPage.tsx, frontend/src/pages/BootstrapPage.tsx</files>
  <read_first>
    - frontend/src/pages/auth/ResetPasswordPage.tsx:9,42-45 (canonical PasswordPolicyChecker + checkPasswordPolicy usage — analog)
    - frontend/src/pages/users/UsersPage.tsx:133-141 (admin create password field)
    - frontend/src/pages/BootstrapPage.tsx:80,202-209 (404 status mapping + inaugural password field)
    - crates/axiam-api-rest/src/handlers/bootstrap.rs:135 (already-initialized branch returns AxiamError::NotFound → HTTP 404, D-09)
    - crates/axiam-api-rest/tests/bootstrap_test.rs:328 (asserts already-initialized bootstrap status == 404)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (PasswordPolicyChecker integration section)
  </read_first>
  <action>
    CQ-F23 UsersPage: import `{ PasswordPolicyChecker, checkPasswordPolicy }` from `@/components/PasswordPolicyChecker`; render `<PasswordPolicyChecker password={password} />` below the create-form password Input; gate the create mutation submit button on `checkPasswordPolicy(password)`.
    CQ-F23 BootstrapPage: same checker on the inaugural admin password field around line 202-209; gate submit on `checkPasswordPolicy(password)`.
    CQ-F34 BootstrapPage: KEEP the existing `status === 404 → setAlreadyInitialized(true)` mapping (backend returns 404 by design, D-09). Ensure other (non-404) errors fall through to a generic error message via the existing `getApiErrorMessage`/error state. Do NOT add a 409 branch. Backend-404 verified against bootstrap.rs:135 + bootstrap_test.rs:328; RESEARCH.md's 409 claim was incorrect.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -6 && grep -l PasswordPolicyChecker src/pages/users/UsersPage.tsx src/pages/BootstrapPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'PasswordPolicyChecker' frontend/src/pages/users/UsersPage.tsx` matches (import + render)
    - `grep -n 'PasswordPolicyChecker' frontend/src/pages/BootstrapPage.tsx` matches (import + render)
    - `grep -n 'checkPasswordPolicy' frontend/src/pages/users/UsersPage.tsx frontend/src/pages/BootstrapPage.tsx` matches in both (submit gating)
    - `grep -n '409' frontend/src/pages/BootstrapPage.tsx` returns nothing (no invented 409 branch); `grep -n '404' frontend/src/pages/BootstrapPage.tsx` still maps to already-initialized
    - `grep -nE 'NotFound|404' crates/axiam-api-rest/src/handlers/bootstrap.rs` returns the already-initialized branch (PINS backend status = 404, not 409 — makes the kept-404 mapping falsifiable)
    - `cd frontend && npx tsc -b --noEmit` succeeds (read output text)
  </acceptance_criteria>
  <done>Both password forms enforce policy; bootstrap 404 handling correct and unchanged in intent.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh _retry ordering, stable empty permissions, StrictMode init, TenantsPage flash</name>
  <files>frontend/src/lib/api.ts, frontend/src/hooks/usePermissions.ts, frontend/src/hooks/useAuthInit.ts, frontend/src/pages/tenants/TenantsPage.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts:88-115 (401 interceptor; `_retry` set after retry)
    - frontend/src/hooks/usePermissions.ts:15 (`?? []` new-array-per-render)
    - frontend/src/hooks/useAuthInit.ts:22-58 (useEffect + dep array with `setInitializing` never called in body)
    - frontend/src/pages/tenants/TenantsPage.tsx:194-211 (orgs-then-tenants loading flash)
    - .planning/phases/12-low-remediation/12-PATTERNS.md (api.ts / usePermissions / useAuthInit / TenantsPage sections)
  </read_first>
  <action>
    CQ-F32: In api.ts, move `originalRequest._retry = true;` to immediately inside the outer 401 guard block, BEFORE the `isRefreshing` queue check and the `api.post("/api/v1/auth/refresh")` call (so a 401 on the replayed request cannot trigger a second refresh). The `getCookie` regex (AXIAM_CSRF_RE) is already a static literal — leave it as-is.
    CQ-F33: In usePermissions.ts, declare `const EMPTY_PERMISSIONS: string[] = [];` at module scope and use it as the fallback: `s.user?.permissions ?? EMPTY_PERMISSIONS` (stable reference across renders).
    CQ-F35: In useAuthInit.ts, remove `setInitializing` from the useEffect dependency array; add a `const initialized = useRef(false)` once-guard at the top of the effect (`if (initialized.current) return; initialized.current = true;`) so the boot fetch fires once under React 18 StrictMode. Keep the existing `cancelled` flag.
    CQ-F20: In TenantsPage.tsx, add an `isLoadingOrgs` (orgs query `isLoading`) composite guard so the table renders a loading state instead of "No tenants found" while organizations are still loading. The per-org fan-out (Promise.all) is acceptable at small org count — no N+1 refactor.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -6 && grep -n 'EMPTY_PERMISSIONS' src/hooks/usePermissions.ts && grep -n 'useRef' src/hooks/useAuthInit.ts</automated>
  </verify>
  <acceptance_criteria>
    - In api.ts, `originalRequest._retry = true` appears BEFORE the `api.post(".../auth/refresh"` call (read the interceptor block to confirm ordering)
    - `grep -n 'EMPTY_PERMISSIONS' frontend/src/hooks/usePermissions.ts` matches at module scope and in the selector fallback
    - `grep -n 'setInitializing' frontend/src/hooks/useAuthInit.ts` does NOT appear in the useEffect dependency array; `grep -n 'useRef' frontend/src/hooks/useAuthInit.ts` matches (once-guard)
    - TenantsPage renders a loading guard tied to the orgs query before showing the empty state
    - `cd frontend && npx tsc -b --noEmit` succeeds (read output text)
  </acceptance_criteria>
  <done>Refresh loop guarded, stable empty permissions, single StrictMode boot fetch, no tenants-empty flash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin/bootstrap form → backend | Weak passwords must be screened client-side (policy checker) before submit |
| 401 refresh loop | A mis-ordered `_retry` flag can cause an unbounded refresh loop (availability) |
| bundled deps → shipped JS | Unused radix packages bloat the bundle and widen the dependency attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Elevation of Privilege | admin-create/bootstrap accept weak inaugural passwords (CQ-F23) | mitigate | Task 2: PasswordPolicyChecker gates both submit buttons |
| T-12-10 | Denial of Service | `_retry` set after retry → potential infinite refresh loop (CQ-F32) | mitigate | Task 3: set `_retry = true` before the refresh attempt |
| T-12-11 | Tampering | unused radix deps enlarge supply-chain surface (CQ-F22) | mitigate | Task 1: remove packages with zero import sites (verified by grep) |
| T-12-12 | Information Disclosure | (none — frontend secret/token handling is covered in Plan 12-04) | accept | Out of scope for this plan; see Plan 12-04 SEC-036/037/041 |
</threat_model>

<verification>
- `cd frontend && npm run lint && npx tsc -b` green
- Placeholder.tsx gone; 4 radix packages removed (zero-usage); policy checker on both forms; eight source fixes applied per acceptance criteria
</verification>

<success_criteria>
ROADMAP SC #3 satisfied: dead Placeholder.tsx removed, unused radix deps removed, password-policy checker on admin-create + bootstrap, safe DataTable row key, i18n (no hardcoded en-US), CSS.escape in ResourceTree, `_retry` guard, bootstrap 404 handling kept correct, StrictMode double-fetch fixed (plus TenantsPage flash) — all green per acceptance criteria.
</success_criteria>

<output>
Create `.planning/phases/12-low-remediation/12-03-SUMMARY.md` when done. Note which radix packages were actually removed and confirm the 404 bootstrap mapping was kept (not switched to 409).
</output>
