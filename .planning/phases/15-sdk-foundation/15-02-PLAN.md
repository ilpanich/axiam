---
phase: 15-sdk-foundation
plan: 02
type: execute
wave: 2
depends_on: [15-01, 15-05]
files_modified:
  - crates/axiam-server/src/main.rs
  - sdks/openapi.json
  - .github/workflows/sdk-openapi-drift.yml
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "axiam-server --dump-openapi prints the OpenAPI JSON and exits 0 without connecting to SurrealDB or AMQP"
    - "sdks/openapi.json is committed and contains the /api/v1/authz/check and /check/batch paths from Plan 01"
    - "CI drift gate fails the build when the committed spec diverges from a fresh --no-default-features export"
    - "The --dump-openapi flag is discoverable (documented)"
  artifacts:
    - path: "sdks/openapi.json"
      provides: "committed OpenAPI source of truth for REST SDK codegen (no-default-features export)"
      contains: "/api/v1/authz/check"
    - path: ".github/workflows/sdk-openapi-drift.yml"
      provides: "path-filtered per-PR drift gate + release-tag re-export"
    - path: "crates/axiam-server/src/main.rs"
      provides: "--dump-openapi early-exit branch before DB/AMQP init"
      contains: "--dump-openapi"
  key_links:
    - from: "crates/axiam-server/src/main.rs"
      to: "axiam_api_rest::openapi::api_doc"
      via: "serde_json::to_string_pretty(&api_doc())"
      pattern: "dump-openapi"
    - from: ".github/workflows/sdk-openapi-drift.yml"
      to: "sdks/openapi.json"
      via: "git diff / diff against fresh export"
      pattern: "openapi"
---

<objective>
Add a `--dump-openapi` flag to `axiam-server` that prints `api_doc()` JSON and exits BEFORE any SurrealDB/AMQP init, commit the first export as `sdks/openapi.json`, and add a path-filtered CI drift gate (D-03) plus release-tag re-export (D-04).

Purpose: Implements FND-01. The committed spec is the REST codegen source of truth for every SDK.
Output: `--dump-openapi` branch in main.rs, committed `sdks/openapi.json` (includes the Plan-01 authz routes), and `sdk-openapi-drift.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-sdk-foundation/15-RESEARCH.md
@.planning/phases/15-sdk-foundation/15-PATTERNS.md

<interfaces>
crates/axiam-server/src/main.rs — healthcheck early-exit analog (lines 106-116) runs BEFORE tracing_subscriber::fmt() (line 121) and load_config(). `axiam_api_rest::openapi` is `pub mod` (lib.rs:11); `serde_json` is a direct dep (Cargo.toml:39).
axiam_api_rest::openapi::api_doc() -> utoipa::openapi::OpenApi (impl Serialize). SAML paths are #[cfg(feature="saml")] — committed export uses --no-default-features (deterministic, no libxmlsec1-dev).
.github/workflows/ci.yml:59-75 — build-no-saml job: pinned SHAs (actions/checkout 11bd71901bbe5b1630ceea73d27597364c9af683, dtolnay/rust-toolchain 3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9, Swatinem/rust-cache 9d47c6ad4b02e050fd481d890b2ea34778fd09d6), installs protobuf-compiler, runs cargo with --no-default-features. Do NOT install libxmlsec1-dev (SAML-leak guard).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --dump-openapi early-exit branch to axiam-server</name>
  <files>crates/axiam-server/src/main.rs</files>
  <read_first>
    - crates/axiam-server/src/main.rs (the healthcheck early-exit block ~106-116, the tracing init ~121, and any --help/usage handling)
    - 15-PATTERNS.md section "crates/axiam-server/src/main.rs" (exact insertion block)
  </read_first>
  <action>
    Insert a new early-exit block immediately AFTER the healthcheck block (line 116) and BEFORE `tracing_subscriber::fmt()` (line 121): collect `std::env::args()`, and if `args.get(1) == Some("--dump-openapi")`, run `let json = serde_json::to_string_pretty(&axiam_api_rest::openapi::api_doc()).expect("OpenAPI serialization failed"); println!("{json}"); std::process::exit(0);`. This must NOT call `load_config()` or open any DB/AMQP connection. Document the flag: if a usage/`--help` string exists in main.rs add a line for `--dump-openapi`; otherwise add a doc-comment above the block stating the flag prints the OpenAPI spec and exits without DB/AMQP. (Internal structure is Claude's discretion per CONTEXT.)
  </action>
  <verify>
    <automated>cargo build -p axiam-server --no-default-features 2>&1 | tail -5 && AXIAM__DB__URL=ws://127.0.0.1:9 ./target/debug/axiam-server --dump-openapi 2>/dev/null | head -c 80 | grep -q openapi && echo DUMP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `axiam-server --dump-openapi` prints JSON beginning with the OpenAPI document and exits 0 even with a bogus/unreachable DB URL (proves no DB connection)
    - The branch sits before `tracing_subscriber::fmt()` and before any config/DB/AMQP init
    - `cargo build -p axiam-server --no-default-features` shows no errors (read output text)
  </acceptance_criteria>
  <done>The flag prints the spec and exits without touching SurrealDB/AMQP.</done>
</task>

<task type="auto">
  <name>Task 2: Generate & commit sdks/openapi.json and add the drift-gate CI workflow</name>
  <files>sdks/openapi.json, .github/workflows/sdk-openapi-drift.yml</files>
  <read_first>
    - .github/workflows/ci.yml (build-no-saml job lines 59-75; pinned action SHAs; trigger block lines 3-6)
    - 15-PATTERNS.md section ".github/workflows/sdk-openapi-drift.yml"
    - 15-RESEARCH.md §FND-01 "Feature determinism" (the exact export + diff commands)
  </read_first>
  <action>
    Generate the committed spec with SAML excluded for determinism: `cargo build -p axiam-server --no-default-features && ./target/debug/axiam-server --dump-openapi > sdks/openapi.json` (create `sdks/` if absent). The output MUST include `/api/v1/authz/check` and `/api/v1/authz/check/batch` (Plan 01 must have landed). Commit `sdks/openapi.json`.
    Create `.github/workflows/sdk-openapi-drift.yml`: trigger on `pull_request` AND `push` to `main` with `paths:` `crates/axiam-api-rest/**` and `crates/axiam-server/**`; also trigger on release tags (`push: tags: ['v*']`) for the belt-and-suspenders re-export (D-04). Single job reusing the build-no-saml step pattern with verbatim pinned SHAs and `protobuf-compiler` install (NO libxmlsec1-dev). Steps: build `-p axiam-server --no-default-features`, export to `/tmp/openapi-fresh.json`, then `diff sdks/openapi.json /tmp/openapi-fresh.json` (fails the job on any drift). Pin the SAML-off feature flag in BOTH the documented export and the gate (Pitfall 2).
  </action>
  <verify>
    <automated>grep -q '/api/v1/authz/check' sdks/openapi.json && grep -q '/api/v1/authz/check/batch' sdks/openapi.json && diff sdks/openapi.json <(./target/debug/axiam-server --dump-openapi) && echo DRIFT_CLEAN && grep -q 'paths:' .github/workflows/sdk-openapi-drift.yml</automated>
  </verify>
  <acceptance_criteria>
    - `sdks/openapi.json` exists, is valid JSON, and contains both authz-check paths
    - `diff sdks/openapi.json <(axiam-server --dump-openapi)` is clean (no drift) immediately after generation
    - `sdk-openapi-drift.yml` is path-filtered on `crates/axiam-api-rest/**` + `crates/axiam-server/**`, uses verbatim pinned action SHAs, runs `--no-default-features`, does NOT install libxmlsec1-dev, and fails on `diff`
    - Workflow includes a release-tag trigger for the final re-export (D-04)
  </acceptance_criteria>
  <done>Committed spec includes authz routes; drift gate is path-filtered and feature-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo | Generated spec must be deterministic regardless of host (libxmlsec1-dev presence) |
| committed spec → public SDK consumers | Spec must expose only the public ApiDoc, no secrets/internal-only routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Information Disclosure | `--dump-openapi` output | mitigate | Output is `api_doc()` (the public utoipa ApiDoc) only; SAML behind feature flag, excluded via --no-default-features; no secrets in the spec (Task 1/2) |
| T-15-07 | Tampering | non-deterministic drift gate (feature mismatch) | mitigate | Both export and gate pin `--no-default-features` (Pitfall 2); gate runs the identical command (Task 2) |
| T-15-SC | Tampering | CI action supply chain | mitigate | Verbatim SHA-pinned actions copied from ci.yml; no floating tags |
</threat_model>

<verification>
- `axiam-server --dump-openapi` exits 0 with bogus DB URL
- `sdks/openapi.json` contains both authz paths; fresh export diff is clean
- Workflow path-filtered, feature-pinned, SHA-pinned
</verification>

<success_criteria>
ROADMAP Phase 15 criterion #1 satisfied: `--dump-openapi` exits without SurrealDB/AMQP; `sdks/openapi.json` committed; CI drift gate fails on divergence.
</success_criteria>

<output>
Create `.planning/phases/15-sdk-foundation/15-02-SUMMARY.md` when done.
</output>
