---
phase: 15-sdk-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - sdks/rust/LICENSE
  - sdks/rust/Cargo.toml
  - sdks/rust/src/lib.rs
  - sdks/rust/README.md
  - sdks/typescript/LICENSE
  - sdks/typescript/package.json
  - sdks/typescript/src/index.ts
  - sdks/typescript/README.md
  - sdks/python/LICENSE
  - sdks/python/pyproject.toml
  - sdks/python/axiam_sdk/__init__.py
  - sdks/python/README.md
  - sdks/java/LICENSE
  - sdks/java/pom.xml
  - sdks/java/README.md
  - sdks/csharp/LICENSE
  - sdks/csharp/Axiam.Sdk/Axiam.Sdk.csproj
  - sdks/csharp/README.md
  - sdks/php/LICENSE
  - sdks/php/composer.json
  - sdks/php/README.md
  - sdks/go/LICENSE
  - sdks/go/go.mod
  - sdks/go/README.md
  - .github/workflows/sdk-ci-rust.yml
  - .github/workflows/sdk-ci-typescript.yml
  - .github/workflows/sdk-ci-python.yml
  - .github/workflows/sdk-ci-java.yml
  - .github/workflows/sdk-ci-csharp.yml
  - .github/workflows/sdk-ci-php.yml
  - .github/workflows/sdk-ci-go.yml
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "sdks/{rust,typescript,python,java,csharp,php,go}/ each exist with an Apache-2.0 LICENSE matching the repo root LICENSE verbatim"
    - "Each SDK has a package manifest with the locked package identity (D-11/D-12/D-13)"
    - "Each SDK README references CONTRACT.md §1-§10"
    - "Each SDK has a path-filtered CI workflow triggering only on its own paths + shared artifacts"
  artifacts:
    - path: "sdks/go/go.mod"
      provides: "Go module declaration (D-13)"
      contains: "github.com/axiam/axiam/sdks/go"
    - path: "sdks/rust/Cargo.toml"
      provides: "Rust crate identity axiam-sdk"
      contains: "axiam-sdk"
    - path: ".github/workflows/sdk-ci-rust.yml"
      provides: "path-filtered scaffold-check CI for the Rust SDK"
  key_links:
    - from: ".github/workflows/sdk-ci-rust.yml"
      to: "sdks/rust/"
      via: "paths: filter"
      pattern: "sdks/rust"
---

<objective>
Create the `sdks/` monorepo scaffold — seven language directories each with an Apache-2.0 LICENSE, a publish-ready package manifest using the locked package identities (D-11/D-12/D-13), a stub source file, and a README that references `CONTRACT.md` — plus seven path-filtered per-SDK CI workflows (O(1) CI per change).

Purpose: Implements FND-05. Establishes the monorepo layout and cheap per-SDK CI that Phases 16-22 build on.
Output: 7 SDK directories + 7 `sdk-ci-{lang}.yml` workflows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-sdk-foundation/15-RESEARCH.md
@.planning/phases/15-sdk-foundation/15-PATTERNS.md
@.planning/phases/15-sdk-foundation/15-CONTEXT.md

<interfaces>
Locked package identities (D-11/D-12/D-13):
  Rust crate `axiam-sdk` (Cargo.toml [package] name, publish=true)
  npm `axiam-sdk` (package.json name)
  PyPI `axiam-sdk` (pyproject.toml [project] name = "axiam-sdk")
  Maven `io.axiam:axiam-sdk` (pom.xml groupId io.axiam, artifactId axiam-sdk)
  NuGet `Axiam.Sdk` (Axiam.Sdk/Axiam.Sdk.csproj)
  Packagist `axiam/axiam-sdk` (composer.json name)
  Go module `github.com/axiam/axiam/sdks/go` (go.mod, D-13)
LICENSE: copy the repo-root `LICENSE` file (Apache-2.0) verbatim — resolve repo root via `git rev-parse --show-toplevel`; do NOT trust the stale Cargo.toml license field.
CI analog: ci.yml:59-75 — pinned actions/checkout SHA 11bd71901bbe5b1630ceea73d27597364c9af683. Phase-15 CI = scaffold-check only (verify LICENSE present); per-SDK build/test added in Phases 16-22.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 7 SDK directories with LICENSE + stub source</name>
  <files>sdks/rust/LICENSE, sdks/rust/src/lib.rs, sdks/typescript/LICENSE, sdks/typescript/src/index.ts, sdks/python/LICENSE, sdks/python/axiam_sdk/__init__.py, sdks/java/LICENSE, sdks/csharp/LICENSE, sdks/php/LICENSE, sdks/go/LICENSE</files>
  <read_first>
    - <repo-root>/LICENSE (root Apache-2.0 — copy verbatim; repo root = `git rev-parse --show-toplevel`)
    - 15-RESEARCH.md §FND-05 (directory structure)
    - project memory note: license is Apache-2.0, not the stale Cargo.toml field
  </read_first>
  <action>
    Create the seven SDK directories. Copy the repo-root `LICENSE` file verbatim into each `sdks/<lang>/LICENSE` (rust, typescript, python, java, csharp, php, go). Add a minimal stub source per language that compiles/loads as an empty package: `sdks/rust/src/lib.rs` (empty crate root with a doc comment), `sdks/typescript/src/index.ts` (empty export), `sdks/python/axiam_sdk/__init__.py` (package marker). Java/C#/PHP/Go source stubs are created by their manifests in Task 2 (Go needs only go.mod for a valid module; Java/C#/PHP scaffolds get a placeholder under their manifest). No SDK client logic — Phases 16-22 fill these.
  </action>
  <verify>
    <automated>for l in rust typescript python java csharp php go; do test -f sdks/$l/LICENSE || { echo "MISSING $l LICENSE"; exit 1; }; grep -q 'Apache License' sdks/$l/LICENSE || { echo "$l LICENSE not Apache"; exit 1; }; done && echo LICENSES_OK</automated>
  </verify>
  <acceptance_criteria>
    - All seven `sdks/<lang>/LICENSE` files exist and contain the Apache-2.0 license text (matching root LICENSE)
    - `diff sdks/rust/LICENSE LICENSE` is clean (verbatim copy)
    - Stub sources exist for rust/typescript/python
  </acceptance_criteria>
  <done>Seven SDK dirs scaffolded with verbatim Apache-2.0 LICENSE.</done>
</task>

<task type="auto">
  <name>Task 2: Add package manifests + READMEs with locked identities</name>
  <files>sdks/rust/Cargo.toml, sdks/rust/README.md, sdks/typescript/package.json, sdks/typescript/README.md, sdks/python/pyproject.toml, sdks/python/README.md, sdks/java/pom.xml, sdks/java/README.md, sdks/csharp/Axiam.Sdk/Axiam.Sdk.csproj, sdks/csharp/README.md, sdks/php/composer.json, sdks/php/README.md, sdks/go/go.mod, sdks/go/README.md</files>
  <read_first>
    - 15-CONTEXT.md D-11, D-12, D-13 (locked package identities)
    - 15-RESEARCH.md §FND-05 (manifest layout) + "D-13 ROADMAP Fixup" (Go module path/tag)
  </read_first>
  <action>
    Author each manifest with its locked identity and `license = "Apache-2.0"`:
    - `sdks/rust/Cargo.toml`: `[package] name = "axiam-sdk"`, `license = "Apache-2.0"`, `publish = true`, edition 2024, version 0.0.0 (placeholder).
    - `sdks/typescript/package.json`: `"name": "axiam-sdk"`, `"license": "Apache-2.0"`, `"version": "0.0.0"`.
    - `sdks/python/pyproject.toml`: `[project] name = "axiam-sdk"`, `license = "Apache-2.0"`, version 0.0.0.
    - `sdks/java/pom.xml`: `<groupId>io.axiam</groupId>`, `<artifactId>axiam-sdk</artifactId>`, Apache-2.0 license block, version 0.0.0.
    - `sdks/csharp/Axiam.Sdk/Axiam.Sdk.csproj`: `<PackageId>Axiam.Sdk</PackageId>`, `<PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>`. (Grpc.Tools wiring is Phase 21; document the exception in README.)
    - `sdks/php/composer.json`: `"name": "axiam/axiam-sdk"`, `"license": "Apache-2.0"`.
    - `sdks/go/go.mod`: `module github.com/axiam/axiam/sdks/go` (D-13 — exactly this path), a current `go` directive version.
    Author a short `README.md` per SDK that names the package identity AND contains the exact line: "This SDK conforms to CONTRACT.md §1-§10." (link to `../CONTRACT.md`). The C# README must also note the `Grpc.Tools` MSBuild codegen exception to the buf pipeline.
  </action>
  <verify>
    <automated>grep -q 'name = "axiam-sdk"' sdks/rust/Cargo.toml && grep -q '"axiam-sdk"' sdks/typescript/package.json && grep -q 'axiam/axiam-sdk' sdks/php/composer.json && grep -q 'module github.com/axiam/axiam/sdks/go' sdks/go/go.mod && grep -q 'io.axiam' sdks/java/pom.xml && grep -q 'Axiam.Sdk' sdks/csharp/Axiam.Sdk/Axiam.Sdk.csproj && for l in rust typescript python java csharp php go; do grep -q 'CONTRACT.md' sdks/$l/README.md || { echo "$l README missing CONTRACT ref"; exit 1; }; done && echo MANIFESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Each manifest declares the exact locked package identity (D-11/D-12) and `Apache-2.0` license
    - `sdks/go/go.mod` declares `module github.com/axiam/axiam/sdks/go` (D-13, no `axiam-go-sdk`)
    - All seven READMEs contain "CONTRACT.md §1-§10"
    - The C# README documents the Grpc.Tools exception
  </acceptance_criteria>
  <done>All seven package identities locked into manifests; READMEs cross-reference the contract.</done>
</task>

<task type="auto">
  <name>Task 3: Create 7 path-filtered per-SDK CI workflows</name>
  <files>.github/workflows/sdk-ci-rust.yml, .github/workflows/sdk-ci-typescript.yml, .github/workflows/sdk-ci-python.yml, .github/workflows/sdk-ci-java.yml, .github/workflows/sdk-ci-csharp.yml, .github/workflows/sdk-ci-php.yml, .github/workflows/sdk-ci-go.yml</files>
  <read_first>
    - 15-PATTERNS.md section ".github/workflows/sdk-ci-{lang}.yml" (stub job + path filter + pinned SHA)
    - .github/workflows/ci.yml (build-no-saml job, pinned actions/checkout SHA)
  </read_first>
  <action>
    Create one workflow per SDK. Each triggers on `pull_request` to `main` with `paths:` for its own dir (`sdks/<lang>/**`) plus the shared artifacts (`sdks/openapi.json`, `sdks/buf.yaml`, `sdks/buf.gen.yaml`). Single `scaffold-check` job: `actions/checkout` pinned to SHA `11bd71901bbe5b1630ceea73d27597364c9af683` (verbatim from ci.yml), then a step `test -f sdks/<lang>/LICENSE`. These are Phase-15 stubs — real build/test steps are added per-SDK in Phases 16-22. Do NOT add `libxmlsec1-dev` anywhere (SAML-leak guard convention).
  </action>
  <verify>
    <automated>for l in rust typescript python java csharp php go; do f=.github/workflows/sdk-ci-$l.yml; test -f $f || { echo "MISSING $f"; exit 1; }; grep -q "sdks/$l/" $f || { echo "$f missing path filter"; exit 1; }; grep -q '11bd71901bbe5b1630ceea73d27597364c9af683' $f || { echo "$f checkout not SHA-pinned"; exit 1; }; done && ! grep -rq 'libxmlsec1-dev' .github/workflows/sdk-ci-*.yml && echo SDK_CI_OK</automated>
  </verify>
  <acceptance_criteria>
    - Seven `sdk-ci-{lang}.yml` files exist, each path-filtered on `sdks/<lang>/**` + shared artifacts
    - Each pins `actions/checkout` to the verbatim ci.yml SHA and verifies `sdks/<lang>/LICENSE` exists
    - No workflow installs `libxmlsec1-dev`
  </acceptance_criteria>
  <done>Seven O(1) path-filtered SDK CI workflows in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package manifest identity → registry publish | A wrong/squatted name would publish under an unintended identity |
| CI action references → CI runner | Unpinned actions are a supply-chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-13 | Spoofing | package identity in manifests | mitigate | Identities are the D-11/D-12/D-13 locked names; availability is verified separately in Plan 06 before any publish |
| T-15-14 | Tampering | CI action supply chain | mitigate | `actions/checkout` SHA-pinned verbatim from ci.yml; no floating tags |
| T-15-15 | License non-compliance | LICENSE mismatch | mitigate | LICENSE copied verbatim from root Apache-2.0; not from stale Cargo.toml field |
</threat_model>

<verification>
- All 7 LICENSE files Apache-2.0 (verbatim)
- All 7 manifests carry locked identities; Go module path = D-13
- All 7 READMEs reference CONTRACT.md; 7 CI workflows path-filtered + SHA-pinned
</verification>

<success_criteria>
ROADMAP Phase 15 criterion #5 satisfied: `sdks/{rust,typescript,python,java,csharp,php,go}/` exist with Apache-2.0 LICENSE and per-SDK path-filtered CI workflows.
</success_criteria>

<output>
Create `.planning/phases/15-sdk-foundation/15-05-SUMMARY.md` when done.
</output>
