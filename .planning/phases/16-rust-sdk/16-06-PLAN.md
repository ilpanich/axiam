---
phase: 16-rust-sdk
plan: 06
type: execute
wave: 4
depends_on: ["16-02", "16-03", "16-04", "16-05"]
files_modified:
  - sdks/rust/Cargo.toml
  - sdks/rust/examples/login_mfa.rs
  - sdks/rust/examples/rest_check_access.rs
  - sdks/rust/examples/grpc_check_access.rs
  - sdks/rust/examples/amqp_consumer.rs
  - sdks/rust/examples/actix_route_guard.rs
  - sdks/rust/README.md
  - .github/workflows/sdk-ci-rust.yml
autonomous: true
requirements: [RUST-01]
user_setup:
  - service: crates.io
    why: "Publishing the axiam-sdk crate on release tags"
    env_vars:
      - name: CARGO_REGISTRY_TOKEN
        source: "crates.io -> Account Settings -> API Tokens (store as the CRATES_IO_TOKEN GitHub Actions secret)"
must_haves:
  truths:
    - "`cargo publish --dry-run -p axiam-sdk` succeeds with the buf/build.rs-bundled gRPC stubs (self-contained artifact, D-09) (SC#5)"
    - "The SDK Rust CI workflow runs the `grep -r 'eyJ' target/debug/` leak gate (expect empty), the insecure-skip TLS lint gate (expect empty), and the dry-run gate on PRs touching `sdks/rust/**` (SC#3/#5/§6)"
    - "A real crates.io publish job is triggered by the tag `sdks/rust/vX.Y.Z` and bundles regenerated buf stubs so crates.io consumers (who cannot run buf) get a complete crate (D-09)"
    - "Five runnable examples exist (login+MFA, REST check_access/can/batch, gRPC CheckAccess+BatchCheckAccess, AMQP consumer, Actix route guard) demonstrating §1–§10 conformance (D-08)"
    - "The README documents MSRV 1.88, the feature layout, and states 'This SDK conforms to CONTRACT.md §1–§10' (§ Conformance Statement)"
  artifacts:
    - sdks/rust/examples/login_mfa.rs
    - sdks/rust/examples/rest_check_access.rs
    - sdks/rust/examples/grpc_check_access.rs
    - sdks/rust/examples/amqp_consumer.rs
    - sdks/rust/examples/actix_route_guard.rs
    - .github/workflows/sdk-ci-rust.yml
    - sdks/rust/README.md
  key_links:
    - "The publish job's buf-bundle step makes the crates.io artifact self-contained (gRPC stubs are gitignored locally per 16-01); the dry-run gate proves packageability before tag"
---

<objective>
Ship the per-capability examples (D-08), the README/conformance fill-in, and the crates.io publish pipeline + CI gates: the `grep -r 'eyJ' target/debug/` leak gate (SC#3), the insecure-skip TLS lint gate (§6), the `cargo publish --dry-run` gate (SC#5), and the tag-triggered real publish that bundles regenerated buf stubs (D-09).

Purpose: Closes success criteria #3 (CI leak gate), #5 (dry-run + publish pipeline), the §1–§10 conformance demonstration (examples + README statement), and the §6 CI lint gate. Depends on all four transport plans being complete so the examples compile and the dry-run packages a real crate.
Output: A publishable, CI-gated crate with runnable examples and an operational release pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@sdks/CONTRACT.md
@.planning/phases/16-rust-sdk/16-RESEARCH.md
@.planning/phases/16-rust-sdk/16-VALIDATION.md
@.github/workflows/sdk-ci-rust.yml
@sdks/buf.gen.yaml
</context>

## Artifacts this phase produces

This plan produces: five example binaries (`examples/login_mfa.rs`, `rest_check_access.rs`, `grpc_check_access.rs`, `amqp_consumer.rs`, `actix_route_guard.rs`); the filled-in `README.md` (MSRV, features, conformance statement); the extended SDK Rust CI workflow (`.github/workflows/sdk-ci-rust.yml`) with leak gate, TLS-lint gate, dry-run gate, and tag-triggered publish + buf-bundle. Consumes all transport modules from 16-02..16-05. (Full phase inventory is listed in 16-01-PLAN.md.)

<tasks>

<task type="auto">
  <name>Task 1: Five per-capability examples + README conformance fill-in</name>
  <files>sdks/rust/examples/login_mfa.rs, sdks/rust/examples/rest_check_access.rs, sdks/rust/examples/grpc_check_access.rs, sdks/rust/examples/amqp_consumer.rs, sdks/rust/examples/actix_route_guard.rs, sdks/rust/Cargo.toml, sdks/rust/README.md</files>
  <read_first>
    - sdks/rust/README.md (the scaffold README to fill — currently states "Scaffold placeholder")
    - sdks/rust/src/client.rs, src/rest/auth.rs, src/rest/authz.rs (16-02), src/grpc/client.rs (16-03), src/amqp/consumer.rs (16-04), src/middleware/actix.rs (16-05) — the public APIs the examples call
    - .planning/phases/16-rust-sdk/16-CONTEXT.md D-08 (full per-capability example set as the §1–§10 conformance demonstration) and D-10 (MSRV documented in README)
    - .planning/phases/16-rust-sdk/16-PATTERNS.md § examples table (analogs per example)
    - sdks/CONTRACT.md § Conformance Statement (README must state "This SDK conforms to CONTRACT.md §1–§10")
  </read_first>
  <action>
    Create five runnable examples under `sdks/rust/examples/` and register them in `Cargo.toml` with the feature requirements each needs (`[[example]]` entries with `required-features`):
    - `login_mfa.rs` — construct `AxiamClient` with a `tenant_slug`, call `login`, branch on `LoginResult.mfa_required`, call `verify_mfa` (login+MFA two-phase flow; feature `rest`).
    - `rest_check_access.rs` — `check_access` + `can` + `batch_check` against the REST authz endpoints (feature `rest`).
    - `grpc_check_access.rs` — gRPC `check_access` + `batch_check` via the shared channel (feature `grpc`).
    - `amqp_consumer.rs` — `consume(queue, |event| async {...})` with a per-tenant signing key, demonstrating the verify-before-handler contract (feature `amqp`).
    - `actix_route_guard.rs` — a minimal Actix-Web app with a route guarded by the `AxiamUser` `FromRequest` extractor (feature `actix`).
    Each example is illustrative/compilable (uses placeholder base_url/credentials read from env; does not require a live server to `cargo build --example`). These doubles as the §1–§10 conformance demonstration (D-08).
    Fill `sdks/rust/README.md`: keep package identity + Apache-2.0; add a Features section (default `rest`/`grpc`/`amqp`; opt-in `observability`, `actix`; how to disable defaults for REST-only); document MSRV `1.88` (D-10); add a short usage snippet per capability pointing at the examples; retain the line "This SDK conforms to CONTRACT.md §1–§10." and remove the "Scaffold placeholder" status line.
  </action>
  <verify>
    <automated>cd sdks/rust && cargo build --examples --all-features 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --examples --all-features` compiles all five examples.
    - Each example is registered in `Cargo.toml` with `required-features` matching its transport (grep the `[[example]]` entries).
    - `grep -c 'conforms to CONTRACT.md' sdks/rust/README.md` is ≥ 1 and `grep -c '1.88' sdks/rust/README.md` is ≥ 1.
    - `grep -c 'Scaffold placeholder' sdks/rust/README.md` is 0.
  </acceptance_criteria>
  <done>Five runnable examples cover login+MFA, REST authz, gRPC authz, AMQP consumer, and the Actix guard; the README documents features + MSRV 1.88 and carries the conformance statement.</done>
</task>

<task type="auto">
  <name>Task 2: SDK Rust CI — leak gate, TLS-lint gate, dry-run gate, tag-triggered publish with buf bundle</name>
  <files>.github/workflows/sdk-ci-rust.yml, sdks/rust/Cargo.toml</files>
  <read_first>
    - .github/workflows/sdk-ci-rust.yml (the Phase 15 scaffold to extend — currently only a LICENSE-existence scaffold-check; keep the `paths:` filter)
    - .github/workflows/sdk-buf-gates.yml and sdks/buf.gen.yaml (the buf pipeline the publish job's regenerate-and-bundle step invokes; `neoeinstein-prost`/`neoeinstein-tonic` → `rust/src/gen`)
    - .planning/phases/16-rust-sdk/16-CONTEXT.md D-09 (path-tag publish + regenerate-and-bundle), D-13 monorepo tag convention `sdks/rust/vX.Y.Z`
    - .planning/phases/16-rust-sdk/16-VALIDATION.md § Wave 0 Requirements (CI steps: `grep -r 'eyJ' target/debug/` and `grep -rn 'danger_accept_invalid_certs|insecure' sdks/rust/src/`) and § Manual-Only Verifications (real publish on tag)
    - .planning/phases/16-rust-sdk/16-RESEARCH.md § Validation Architecture (commands) and Environment Availability (buf is CI-only; bundle stubs at publish)
    - sdks/CONTRACT.md §6 (TLS-bypass CI lint gate requirement)
  </read_first>
  <action>
    Extend `.github/workflows/sdk-ci-rust.yml` (keep the existing `paths:` trigger on `sdks/rust/**` + shared codegen files; keep SHA-pinned actions per the Phase 12 SEC-057 convention). Add a PR `test` job (Rust toolchain pinned to MSRV 1.88 for the MSRV-enforcement build per D-10, plus a stable job): runs `cargo build -p axiam-sdk --all-features`, `cargo test -p axiam-sdk --all-features`, then the gates:
    - **Leak gate (SC#3):** `cargo build -p axiam-sdk --all-features` then fail if `grep -r 'eyJ' sdks/rust/target/debug/ | wc -l` is non-zero (use the correct target dir; the SDK is standalone so its target is `sdks/rust/target`). Filter out any false positives from dependency source if needed, but the intent is zero JWT-shaped strings in the SDK's own compiled artifacts.
    - **TLS-lint gate (§6):** fail if `grep -rni 'danger_accept_invalid_certs\|insecureskipverify\|skip_tls\|allow_insecure\|verify_peer(false)' sdks/rust/src/` matches anything (mirrors the Go SDK `InsecureSkipVerify` gate pattern).
    - **Dry-run gate (SC#5):** run `cargo publish --dry-run -p axiam-sdk` — this requires the gRPC stubs to be present, so the job FIRST runs the buf generate (or build.rs codegen) step to materialize `sdks/rust/src/gen/` before packaging (the stubs are gitignored per 16-01/D-09).
    Add a separate `publish` job triggered ONLY on tags matching `sdks/rust/v*` (the D-13 monorepo convention): it (1) regenerates the buf gRPC stubs into `sdks/rust/src/gen/` so the published crate is self-contained (D-09 — crates.io consumers cannot run buf), (2) runs `cargo publish -p axiam-sdk` using `CARGO_REGISTRY_TOKEN` from the `CRATES_IO_TOKEN` secret. Ensure `Cargo.toml`'s `include`/package settings ship the bundled `src/gen/` in the package (override the `.gitignore` exclusion for packaging — `cargo package` follows `include`/`exclude`, not `.gitignore`, so add an `include` list or a packaging note that bundles `src/gen/**`). Keep the existing `scaffold-check` LICENSE step or fold it into the test job.
    Document in the README/workflow comment that the real publish is a manual-on-tag, side-effecting step (VALIDATION § Manual-Only Verifications).
  </action>
  <verify>
    <automated>cd /home/user/axiam && python3 -c "import yaml; w=yaml.safe_load(open('.github/workflows/sdk-ci-rust.yml')); jobs=list(w['jobs']); print('jobs:', jobs); assert any('publish' in j for j in jobs), 'no publish job'; assert w['on'].get('push',{}).get('tags'), 'no tag trigger'" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The workflow parses as valid YAML and contains a `publish` job plus a `push.tags` trigger matching `sdks/rust/v*`.
    - The `test` job contains the leak gate (`grep -r 'eyJ' sdks/rust/target/debug/` expecting empty), the TLS-lint gate (`grep -rni` for bypass patterns expecting empty), and the `cargo publish --dry-run -p axiam-sdk` step.
    - The `publish` job runs a buf/build.rs stub-generation step BEFORE `cargo publish` and references `CARGO_REGISTRY_TOKEN` from a secret.
    - Actions remain SHA-pinned (no floating `@v4`-style tags without a pinned SHA comment), consistent with the repo SEC-057 convention.
  </acceptance_criteria>
  <done>The SDK Rust CI runs the leak, TLS-lint, and dry-run gates on PRs touching `sdks/rust/**`, and a tag-triggered publish job bundles regenerated buf stubs before publishing to crates.io.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI build artifacts → public visibility | Compiled debug artifacts must not contain token-shaped secrets |
| release pipeline → crates.io | The publish credential and the published artifact cross here; the artifact must be self-contained and free of TLS-bypass code |
| npm/crates dependency installs | New SDK dependencies were vetted in RESEARCH § Package Legitimacy Audit (all OK verdicts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-22 | Information Disclosure | tokens in compiled debug artifacts | high | mitigate | CI leak gate `grep -r 'eyJ' sdks/rust/target/debug/` (expect empty); `Sensitive<T>` redaction from 16-01 (SC#3) |
| T-16-23 | Spoofing / Tampering | TLS-bypass code reaching the published crate | high | mitigate | CI TLS-lint gate fails on any insecure/skip-verify pattern in `sdks/rust/src/` before publish (§6) |
| T-16-24 | Tampering | incomplete/non-self-contained published artifact | medium | mitigate | Publish job regenerates + bundles buf gRPC stubs so crates.io consumers get a complete crate; dry-run gate proves packageability pre-tag (D-09) |
| T-16-25 | Tampering | supply-chain (dependency installs) | high | mitigate | All SDK crates carry OK verdicts in RESEARCH § Package Legitimacy Audit (none ASSUMED/SUS/SLOP); four protocol crates pin to existing workspace versions |
| T-16-26 | Spoofing | leaked/over-scoped publish credential | medium | mitigate | `CARGO_REGISTRY_TOKEN` sourced from a GitHub Actions secret used only in the tag-triggered publish job; not exposed to PR jobs |
</threat_model>

<verification>
- `cargo build --examples --all-features` compiles all five examples.
- `sdk-ci-rust.yml` parses as valid YAML with a `publish` job + `sdks/rust/v*` tag trigger and the three gates in the `test` job.
- `cargo publish --dry-run -p axiam-sdk` succeeds locally after stub generation (SC#5).
- README documents MSRV 1.88 + features + conformance statement.
</verification>

<success_criteria>
- Success criterion #5 proven: `cargo publish --dry-run` succeeds; crates.io publish CI runs on the release tag (manual side-effecting step on tag).
- Success criterion #3 CI half proven: the `grep -r 'eyJ' target/debug/` gate is wired into CI.
- §6 CI lint gate and §1–§10 conformance demonstration (examples + README statement) complete.
</success_criteria>

<output>
Create `.planning/phases/16-rust-sdk/16-06-SUMMARY.md` when done.
</output>