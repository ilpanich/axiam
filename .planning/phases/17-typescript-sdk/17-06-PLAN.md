---
phase: 17-typescript-sdk
plan: 06
type: execute
wave: 5
depends_on: ["17-02", "17-03", "17-04", "17-05"]
files_modified:
  - .github/workflows/sdk-ci-typescript.yml
  - sdks/typescript/README.md
  - sdks/typescript/package.json
  - sdks/typescript/scripts/bundle-grep.mjs
  - sdks/CONTRACT.md
autonomous: true
requirements: [TS-01]
user_setup:
  - service: npm
    why: "Tag-triggered publish of the axiam-sdk package to the npm registry with provenance (OIDC). PR dry-run gate needs no secret."
    env_vars:
      - name: NPM_TOKEN
        source: "npmjs.com -> Access Tokens -> Granular automation token for the axiam-sdk package (or configure npm Trusted Publisher/OIDC so no token is needed); store as the GitHub Actions secret NPM_TOKEN"
    dashboard_config:
      - task: "Verify the axiam-sdk package name is claimed/owned on npm before the first tag publish"
        location: "npmjs.com -> axiam-sdk"

must_haves:
  truths:
    - "The TypeScript SDK CI workflow runs on PRs touching sdks/typescript/** and on sdks/typescript/vX.Y.Z tags: install, buf generate, build, test, the SC#1 bundle-and-grep gate, the token-leak gate, the TLS-lint gate, and npm publish --dry-run on PRs; real npm publish --provenance on tag."
    - "The SC#1 gate bundles a /rest fixture with esbuild --platform=browser and fails if @grpc/grpc-js or amqplib appear in the output (D-02)."
    - "The publish job regenerates and bundles the compiled ts-proto stubs so npm consumers never run buf (D-20), and publishes with provenance via id-token: write (D-21)."
    - "CONTRACT.md §3 is updated so cookie double-submit (read axiam_csrf -> echo X-CSRF-Token) is the canonical BROWSER behavior, and the AximaClient references are corrected to AxiamClient (D-28 / D-14)."
    - "The README states 'This SDK conforms to CONTRACT.md §1–§10.' and documents both personas + subpath entries + npm install."
  artifacts:
    - .github/workflows/sdk-ci-typescript.yml
    - sdks/typescript/scripts/bundle-grep.mjs
    - sdks/typescript/README.md
  key_links:
    - "The bundle-grep gate is the end-to-end proof of SC#1 that the dependency-free core (17-01) + splitting:false (17-01) set up; config alone is not trusted (D-02)."
    - "The §3 CONTRACT edit changes the cross-language contract and MUST match the actual server mechanism (double-submit) confirmed in RESEARCH — precise wording, not a browser-only divergence note (D-28)."
---

<objective>
Ship the TypeScript SDK CI/publish pipeline and the cross-cutting verification gates, then apply the
scoped shared-contract documentation edits. The workflow (modeled on sdk-ci-rust.yml) runs on PRs and
on `sdks/typescript/vX.Y.Z` tags: install -> buf generate -> build -> test -> the SC#1 bundle-and-grep
tree-shaking gate (D-02) -> a token-leak gate -> a TLS-lint gate -> `npm publish --dry-run` (SC#5) on
PRs, and a tag-triggered real `npm publish --access public --provenance` with `id-token: write`
(D-21). It regenerates-and-bundles the compiled ts-proto stubs so npm consumers never run buf (D-20).
Finally it fills in the README (CONTRACT conformance statement + persona/entry/install docs) and
applies the D-28 / D-14 edits to the shared `sdks/CONTRACT.md`.

Purpose: Prove SC#1 and SC#5 as CI gates, wire the release pipeline, and resolve the deferred
shared-contract §3/naming update without silently diverging.
Output: `.github/workflows/sdk-ci-typescript.yml` with all gates + publish job, a bundle-grep script,
a completed README, and the CONTRACT.md §3/naming edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-typescript-sdk/17-CONTEXT.md
@.planning/phases/17-typescript-sdk/17-RESEARCH.md
@sdks/CONTRACT.md
@.github/workflows/sdk-ci-rust.yml
@.github/workflows/sdk-ci-typescript.yml
@.github/workflows/sdk-buf-gates.yml
@sdks/typescript/package.json
@sdks/typescript/README.md
@frontend/src/lib/api.ts
</context>

<artifacts_this_phase_produces>
- **CI jobs:** in `.github/workflows/sdk-ci-typescript.yml` — a `test` job (install/generate/build/test/
  bundle-grep/leak/TLS-lint/dry-run) and a `publish` job (regenerate-bundle + `npm publish --provenance`).
- **`sdks/typescript/scripts/bundle-grep.mjs`** — the SC#1 bundle-and-grep helper (esbuild browser
  bundle of a /rest fixture; fail on @grpc/grpc-js|amqplib).
- **README.md** — CONTRACT conformance statement + persona/entry/install/usage docs.
- **CONTRACT.md edits** — §3 canonical browser double-submit + AximaClient->AxiamClient corrections.
- **package.json** — final `files` list / publish scripts as needed for the tarball.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SC#1 bundle-grep gate + token-leak gate + TLS-lint gate + TypeScript CI workflow with dry-run + tag publish (D-02/D-20/D-21, SC#1/SC#5)</name>
  <read_first>
    - .github/workflows/sdk-ci-rust.yml (the reference: PR-only test job vs tag-only publish job; leak gate grep 'eyJ'; TLS-lint gate grep; dry-run gate; regenerate-stubs-before-package; pinned action SHAs)
    - .github/workflows/sdk-ci-typescript.yml (current scaffold-check-only workflow to replace)
    - .github/workflows/sdk-buf-gates.yml (how buf is invoked in CI — bufbuild/buf-action@v1.4.0 pinned)
    - .planning/phases/17-typescript-sdk/17-RESEARCH.md Area 1 (bundle-and-grep mechanics: esbuild --platform=browser fixture importing /rest; fail on @grpc/grpc-js|amqplib), Area 9 (full workflow YAML: PR test job + tag publish job; id-token: write; npm publish --provenance; prebuild=buf generate; regenerate-and-bundle), Security Domain (leak gate grep 'eyJ' dist/; TLS grep rejectUnauthorized:false|NODE_TLS_REJECT_UNAUTHORIZED)
    - sdks/CONTRACT.md §6 (no TLS-bypass surface — what the TLS-lint gate enforces)
  </read_first>
  <files>.github/workflows/sdk-ci-typescript.yml, sdks/typescript/scripts/bundle-grep.mjs, sdks/typescript/package.json</files>
  <action>
    Create sdks/typescript/scripts/bundle-grep.mjs: write a temp fixture that imports `{ AxiamClient }`
    from the built `dist/rest/index.mjs`, bundle it with esbuild programmatically (or shell out to the
    esbuild CLI) using `platform: 'browser'`, `format: 'esm'`, `bundle: true`; read the output and exit
    1 if it matches `/@grpc\/grpc-js|amqplib/`, else exit 0 with an OK message. `--platform=browser` is
    load-bearing: it makes a Node built-in (net/tls/dns) pulled in transitively by grpc/amqp an error
    rather than a silent polyfill (RESEARCH Area 1). Add an npm script `"bundle-grep": "node
    scripts/bundle-grep.mjs"`.
    Rewrite .github/workflows/sdk-ci-typescript.yml modeled on sdk-ci-rust.yml. Keep the existing PR
    path filters (sdks/typescript/**, sdks/openapi.json, sdks/buf.yaml, sdks/buf.gen.yaml) and ADD the
    push trigger `tags: ['sdks/typescript/v*']`. Two jobs:
    (A) `test` (if: pull_request): checkout (pinned SHA), setup-node@<pinned-sha> (node 22, registry-url
    npmjs), install buf (via bufbuild/buf-action or the same mechanism sdk-buf-gates uses — do NOT
    invent a new unpinned action; reuse the pinned bufbuild/buf-action@v1.4.0 or run buf generate via
    the already-available toolchain), `npm ci`, `npm run generate` (buf generate), `npm run build`,
    `npm test -- --run`, `npm run bundle-grep` (SC#1), the token-leak gate (`grep -r 'eyJ' dist/`
    filtered like the Rust job — fail if a JWT-shaped string is found in built output), the TLS-lint
    gate (`grep -rniE 'rejectUnauthorized\s*:\s*false|NODE_TLS_REJECT_UNAUTHORIZED|insecureskipverify'
    src/` must be empty per §6), and `npm publish --dry-run` (SC#5).
    (B) `publish` (if: push && startsWith(github.ref,'refs/tags/sdks/typescript/v')): permissions
    contents:read + id-token:write (npm provenance/OIDC, D-21); checkout, setup-node (registry-url set),
    `npm ci`, `npm run generate && npm run build` (regenerate-and-bundle compiled stubs, D-20), then
    `npm publish --access public --provenance` with `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}`. The
    publish job never runs on PRs so the token is never exposed to PR-triggered runs.
    Ensure package.json `files` includes `dist` (and the compiled stubs land in dist via tsup so the
    tarball is self-contained without src/gen); confirm `npm publish --dry-run` would include dist and
    exclude src/gen and node_modules. Pin every GitHub Action to a commit SHA (repo convention,
    SEC-057) — reuse the exact SHAs already present in sdk-ci-rust.yml for checkout; for setup-node use
    a commit-pinned actions/setup-node reference (add the pin, do not use a floating tag).
  </action>
  <verify>
    <automated>cd sdks/typescript && npm run build && npm run bundle-grep && npm publish --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `cd sdks/typescript && npm run build && npm run bundle-grep` exits 0 and prints an OK message; a deliberately-broken fixture (importing /grpc) would exit 1 (the script's grep matches @grpc/grpc-js|amqplib).
    - `cd sdks/typescript && npm publish --dry-run` succeeds and the packed file list includes `dist/` and excludes `src/gen` and `node_modules` (SC#5).
    - `.github/workflows/sdk-ci-typescript.yml` contains a `test` job with the bundle-grep, leak, TLS-lint, and dry-run steps, and a `publish` job gated on `refs/tags/sdks/typescript/v` with `id-token: write` and `npm publish --provenance`.
    - Every `uses:` in the workflow is pinned to a commit SHA (grep shows no floating `@v` tag except the already-pinned bufbuild/buf-action reference used elsewhere in the repo).
    - `grep -rniE 'rejectUnauthorized\s*:\s*false|NODE_TLS_REJECT_UNAUTHORIZED|insecureskipverify' sdks/typescript/src/` returns empty (TLS-lint gate passes locally).
  </acceptance_criteria>
  <done>The TS CI workflow runs install/generate/build/test/bundle-grep/leak/TLS-lint/dry-run on PRs and a provenance publish on tags; the bundle-grep script proves SC#1; dry-run proves SC#5; actions are SHA-pinned; stubs are regenerated-and-bundled (D-20).</done>
</task>

<task type="auto">
  <name>Task 2: README fill-in + scoped CONTRACT.md §3 canonical-browser + AxiamClient naming edits (D-28 / D-14)</name>
  <read_first>
    - sdks/CONTRACT.md §3 (current browser note "read X-CSRF-Token from response header" to be corrected to the double-submit mechanism) and all AximaClient occurrences (§4/§5 code samples) to correct to AxiamClient
    - sdks/typescript/README.md (scaffold README to fill in)
    - .planning/phases/17-typescript-sdk/17-CONTEXT.md D-28 (exact direction: cookie double-submit canonical for browser; non-browser SDKs capture X-CSRF-Token from the response header) and D-14 (AxiamClient canonical), <deferred> "Do not lose"
    - .planning/phases/17-typescript-sdk/17-RESEARCH.md Area 2 (server validates X-CSRF-Token header against axiam_csrf cookie directly; recommend §3 wording "read the axiam_csrf cookie" for browser; Rust also reads from the jar, i.e. same cookie-read mechanism)
    - frontend/src/lib/api.ts (the proven double-submit browser reference the §3 update should describe)
  </read_first>
  <files>sdks/typescript/README.md, sdks/CONTRACT.md</files>
  <action>
    Edit sdks/CONTRACT.md §3 (scoped edit — do NOT rewrite other sections): update the browser
    implementation note so cookie double-submit is the canonical BROWSER behavior — the browser reads
    the `axiam_csrf` cookie (via `document.cookie`) and echoes it as the `X-CSRF-Token` request header
    on state-changing requests; state that non-browser SDKs capture the `X-CSRF-Token` value from the
    server response and forward it on subsequent state-changing requests (D-28). Keep §3's numbered
    top-level rules coherent with this (the existing rule #1 "capture X-CSRF-Token from the response
    header" should be scoped to non-browser SDKs; add the browser double-submit path explicitly). Do
    not leave §3 describing a mechanism no client uses. Also correct every `AximaClient` occurrence in
    CONTRACT.md (notably the §4 rationale, §5 code samples, §6 TypeScript builder sample) to
    `AxiamClient` (D-14), and fix the §6 TypeScript sample to `new AxiamClient({ baseUrl, tenantSlug,
    customCa })`. Do NOT change the locked §1–§10 method vocabulary or the behavioral requirements —
    only the §3 CSRF description and the class-name typos.
    Fill in sdks/typescript/README.md: include the exact conformance sentence "This SDK conforms to
    CONTRACT.md §1–§10." Document the two personas (browser REST-only via `axiam-sdk`/`axiam-sdk/rest`;
    Node REST+gRPC+AMQP via `axiam-sdk/grpc`/`axiam-sdk/amqp`), the subpath entries and their
    tree-shaking guarantee (SC#1), `npm install axiam-sdk`, construction (`new AxiamClient({ baseUrl,
    tenantSlug, customCa? })`), a short usage snippet per persona (login discriminated union, can/
    batchCheck, gRPC checkAccess, AMQP consume, Express/Fastify middleware), the Apache-2.0 license, and
    the `sdks/typescript/vX.Y.Z` release-tag convention. Use `AxiamClient` throughout (never
    AximClient/AximaClient).
  </action>
  <verify>
    <automated>grep -c "AxiamClient" sdks/CONTRACT.md && ! grep -q "AximaClient\|AximClient" sdks/CONTRACT.md sdks/typescript/README.md && grep -q "conforms to CONTRACT.md" sdks/typescript/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AximaClient\|AximClient" sdks/CONTRACT.md sdks/typescript/README.md` returns no match (D-14 applied to the shared contract + README).
    - CONTRACT.md §3 states the browser double-submit path (read axiam_csrf cookie -> X-CSRF-Token header) as canonical browser behavior and scopes response-header capture to non-browser SDKs (D-28) — verified by a grep for "axiam_csrf" and "double-submit"/"double submit" within §3.
    - `grep -q "This SDK conforms to CONTRACT.md §1–§10." sdks/typescript/README.md` succeeds.
    - README documents both personas, the four subpath entries, `npm install axiam-sdk`, and the `sdks/typescript/vX.Y.Z` tag convention.
    - Only §3 and class-name typos changed in CONTRACT.md; §1–§10 method vocabulary and behavioral rules unchanged (diff review confirms no method-map edits).
  </acceptance_criteria>
  <done>CONTRACT.md §3 makes cookie double-submit the canonical browser behavior and scopes header-capture to non-browser SDKs (D-28), and all AximaClient typos become AxiamClient (D-14); the README states CONTRACT conformance and documents both personas, entries, install, and the release tag convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI publish job -> npm registry | A side-effecting release; the NPM_TOKEN must never be exposed to PR-triggered runs. |
| consumer bundler -> published /rest tarball | Node-only transports must be absent from a browser bundle; the bundle-grep gate proves it pre-publish. |
| published tarball -> npm consumers | Consumers must not need buf; compiled stubs must be bundled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-23 | Tampering | Node transports leaking into browser bundle | high | mitigate | SC#1 bundle-and-grep gate (esbuild --platform=browser; fail on @grpc/grpc-js|amqplib) — end-to-end proof, not config trust (D-02). |
| T-17-24 | Information Disclosure | JWT-shaped string in built artifacts | high | mitigate | Token-leak gate greps 'eyJ' in dist/ (analog to the Rust leak gate); Sensitive<T> upstream keeps tokens out. |
| T-17-25 | Tampering | TLS-bypass pattern creeping into SDK source | high | mitigate | TLS-lint gate greps rejectUnauthorized:false / NODE_TLS_REJECT_UNAUTHORIZED / insecureSkipVerify in src/ (§6). |
| T-17-26 | Elevation of Privilege | NPM_TOKEN exposed to PR runs | high | mitigate | Publish job gated on tag ref only (never pull_request); id-token:write scoped to the publish job; provenance via OIDC (D-21). |
| T-17-SC | Tampering | npm/buf package installs | high | mitigate | RESEARCH Package Legitimacy Audit approved all deps (SUS verdicts are registry-download-null false positives; msw SLOP is its documented postinstall); no [SLOP] package retained; buf actions SHA-pinned; SHA-pinned GitHub Actions (SEC-057). |
</threat_model>

<verification>
- `cd sdks/typescript && npm run build && npm run bundle-grep && npm publish --dry-run` all succeed locally (SC#1 + SC#5).
- Workflow has PR gates (bundle-grep/leak/TLS-lint/dry-run) + tag publish (provenance, id-token:write); actions SHA-pinned.
- CONTRACT §3 + naming edits applied (D-28/D-14); README states CONTRACT conformance.
</verification>

<success_criteria>
- SC#1 (bundle-grep tree-shaking) and SC#5 (dry-run + tag-triggered provenance publish) are CI gates.
- Compiled stubs bundled so npm consumers never run buf (D-20); publish uses provenance (D-21).
- CONTRACT.md §3 canonical-browser update + AxiamClient naming corrected; README complete.
</success_criteria>

<output>
Create `.planning/phases/17-typescript-sdk/17-06-SUMMARY.md` when done.
</output>
