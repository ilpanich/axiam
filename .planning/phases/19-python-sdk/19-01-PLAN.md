---
phase: 19-python-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdks/python/pyproject.toml
  - sdks/python/src/axiam_sdk/__init__.py
  - sdks/python/src/axiam_sdk/py.typed
  - sdks/python/src/axiam_sdk/grpc/__init__.py
  - sdks/python/src/axiam_sdk/grpc/gen/__init__.py
  - sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2.py
  - sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2.pyi
  - sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2_grpc.py
  - sdks/python/src/axiam_sdk/amqp/__init__.py
  - sdks/python/src/axiam_sdk/amqp/_hmac.py
  - sdks/python/scripts/gen_grpc.sh
  - sdks/python/tests/__init__.py
  - sdks/python/tests/conftest.py
  - sdks/python/tests/fixtures/amqp_hmac_vectors.json
  - sdks/python/tests/test_amqp_hmac.py
autonomous: true
requirements: [PY-01]
user_setup: []

must_haves:
  truths:
    - "python -m build succeeds against sdks/python and twine check dist/* passes (valid src-layout package with committed gRPC stubs shipped as package-data)"
    - "importing axiam_sdk.grpc.gen.authorization_pb2_grpc succeeds (no ModuleNotFoundError) because the generated bare import is fixed to a package-relative import"
    - "the Python verify_hmac() accepts a real server-signed AuthzRequest/AuditEventMessage fixture and rejects a tampered copy — byte-for-byte match with crates/axiam-amqp/src/messages.rs"
    - "regenerating the gRPC stubs with the pinned command produces no git diff (drift-free committed stubs)"
  artifacts:
    - sdks/python/pyproject.toml
    - sdks/python/src/axiam_sdk/py.typed
    - sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2_grpc.py
    - sdks/python/src/axiam_sdk/amqp/_hmac.py
    - sdks/python/tests/fixtures/amqp_hmac_vectors.json
    - sdks/python/tests/test_amqp_hmac.py
    - sdks/python/scripts/gen_grpc.sh
  key_links:
    - "verify_hmac() canonicalization order == crates/axiam-amqp/src/messages.rs serde struct declaration order (NOT alphabetical / NOT sort_keys)"
    - "pyproject [tool.setuptools.package-data] ships grpc/gen/*.py + *.pyi so pip-install consumers receive the stubs"
    - "scripts/gen_grpc.sh output == committed grpc/gen/ (drift-check anchor for the CI job added in 19-07)"
  prohibitions:
    - "MUST NOT use json.dumps(..., sort_keys=True) in verify_hmac — struct field order is not alphabetical (Pitfall 2 / Assumption A2)"
    - "MUST NOT gitignore grpc/gen/ — pip consumers cannot run protoc (D-04)"
    - "MUST NOT retain the flat axiam_sdk/ layout or the broken setuptools.backends.legacy:build backend"
---

<objective>
Land the Wave-0 foundation for the Python SDK: fix the broken scaffold packaging (build backend, Python floor, src-layout, py.typed, tool config, package-data), commit the gRPC stubs generated from `proto/axiam/v1/authorization.proto` with the mandatory import fixup, and — highest priority — prove the AMQP HMAC canonicalization matches the Rust server byte-for-byte via a real cross-language fixture BEFORE any other AMQP or transport work builds on an unverified assumption.

Purpose: Everything else in Phase 19 depends on a valid, buildable package tree, importable committed gRPC stubs, and a verified HMAC protocol. This plan removes the single highest-risk unknown in the phase (Assumption A2 / Pitfall 2).
Output: A `python -m build`-able `sdks/python/` src-layout package with committed+drift-checkable gRPC stubs, an `_hmac.py` verifier, and a passing fixture-based HMAC cross-language test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-python-sdk/19-CONTEXT.md
@.planning/phases/19-python-sdk/19-RESEARCH.md
@.planning/phases/19-python-sdk/19-PATTERNS.md
@sdks/CONTRACT.md
@sdks/buf.gen.yaml
@crates/axiam-amqp/src/messages.rs
@sdks/go/amqp/hmac.go
@sdks/go/amqp/hmac_test.go
@proto/axiam/v1/authorization.proto
</context>

## Artifacts this phase produces (this plan)

New symbols/paths created here (drift verification MUST treat these as newly-created, not pre-existing):
- `sdks/python/pyproject.toml` (rewritten: `setuptools.build_meta`, `requires-python=">=3.10"`, `[tool.setuptools.packages.find]` on `src`, `[tool.setuptools.package-data]`, `[project.optional-dependencies]` groups `fastapi`/`django`/`dev`, `[tool.pytest.ini_options]`, `[tool.mypy]`, `[tool.ruff]`)
- `src/axiam_sdk/__init__.py` (placeholder public-export surface — populated by later plans; must import cleanly with only runtime deps)
- `src/axiam_sdk/py.typed` (empty PEP 561 marker)
- `src/axiam_sdk/grpc/__init__.py`, `src/axiam_sdk/grpc/gen/__init__.py`
- `src/axiam_sdk/grpc/gen/authorization_pb2.py`, `authorization_pb2.pyi`, `authorization_pb2_grpc.py` (committed generated stubs, D-04)
- `src/axiam_sdk/amqp/__init__.py` (package marker only; consumer added in 19-05), `src/axiam_sdk/amqp/_hmac.py` (function `verify_hmac(signing_key: bytes, body: bytes) -> bool`)
- `scripts/gen_grpc.sh` (codegen + import-fixup script; drift-check anchor)
- `tests/__init__.py`, `tests/conftest.py` (shared fixtures: `respx` mock transport, JWKS mock, `signing_key` fixture)
- `tests/fixtures/amqp_hmac_vectors.json` (real cross-language HMAC vectors)
- `tests/test_amqp_hmac.py`

<tasks>

<task type="auto">
  <name>Task 1: Fix the scaffold — src-layout, build backend, tool config, package-data</name>
  <files>sdks/python/pyproject.toml, sdks/python/src/axiam_sdk/__init__.py, sdks/python/src/axiam_sdk/py.typed, sdks/python/src/axiam_sdk/grpc/__init__.py, sdks/python/src/axiam_sdk/amqp/__init__.py</files>
  <read_first>
    - sdks/python/pyproject.toml (existing broken scaffold — build-backend `setuptools.backends.legacy:build`, `requires-python=">=3.9"`, flat layout)
    - sdks/python/axiam_sdk/__init__.py (flat scaffold to move into src/)
    - 19-PATTERNS.md § `sdks/python/pyproject.toml` (fix-in-place bug list)
    - 19-RESEARCH.md § Recommended Project Structure (module layout) + Standard Stack (pinned versions)
  </read_first>
  <action>
    Restructure `sdks/python/` to src-layout (D-14): move the flat `axiam_sdk/__init__.py` to `src/axiam_sdk/__init__.py` and delete the old flat `axiam_sdk/` dir. Create empty `src/axiam_sdk/py.typed` (D-20, PEP 561). Create package markers `src/axiam_sdk/grpc/__init__.py`, `src/axiam_sdk/grpc/gen/__init__.py`, `src/axiam_sdk/amqp/__init__.py`.

    Keep `src/axiam_sdk/__init__.py` a minimal placeholder for now — a module docstring plus `__version__` — importable with ONLY runtime deps present; do NOT import `fastapi`/`django` from it (Anti-Pattern: optional extras must not be top-level imports). Later plans populate the public re-export surface.

    Rewrite `pyproject.toml`:
    - `[build-system]`: `requires = ["setuptools>=68", "wheel"]`, `build-backend = "setuptools.build_meta"` (D-03).
    - `[project]`: `requires-python = ">=3.10"` (D-11); drop the `Programming Language :: Python :: 3.9` classifier and add `3.13`; add `[project.dependencies]` = `httpx==0.27.*`, `grpcio==1.78.*`, `aio-pika==9.6.*`, `pydantic>=2`, `PyJWT>=2.13` (PY-01 pins). Keep Apache-2.0 license + existing URLs.
    - `[project.optional-dependencies]`: `fastapi = ["fastapi"]`, `django = ["django"]`, `dev = ["grpcio-tools", "mypy", "ruff", "build", "twine", "pytest", "pytest-asyncio", "respx"]` (Assumption A4 — optional extras, not hard deps).
    - `[tool.setuptools.packages.find]` with `where = ["src"]`; `[tool.setuptools.package-data]` mapping `axiam_sdk = ["py.typed", "grpc/gen/*.py", "grpc/gen/*.pyi"]` so the committed gRPC stubs ship in wheel AND sdist (D-04).
    - `[tool.pytest.ini_options]`: `asyncio_mode = "auto"`, `testpaths = ["tests"]`.
    - `[tool.mypy]`: `strict = true`, targeting `src` (D-20).
    - `[tool.ruff]`: line length + `target-version = "py310"`; enable `ruff format` compatibility (D-20).
  </action>
  <acceptance_criteria>
    - `cd sdks/python && python -m build` produces a wheel and sdist without error (SC#5 partial).
    - `cd sdks/python && twine check dist/*` reports PASSED for both artifacts.
    - `grep -c 'setuptools.backends.legacy' sdks/python/pyproject.toml` returns `0`.
    - `grep -c 'build_meta' sdks/python/pyproject.toml` returns `1`.
    - `grep -c 'requires-python = ">=3.10"' sdks/python/pyproject.toml` returns `1`.
    - `test -f sdks/python/src/axiam_sdk/py.typed` succeeds; `test -d sdks/python/axiam_sdk` fails (flat layout removed).
    - `python -c "import axiam_sdk"` succeeds after `pip install -e sdks/python` (imports with runtime deps only, no fastapi/django).
    - The built wheel contains `axiam_sdk/py.typed` and `axiam_sdk/grpc/gen/authorization_pb2.py` (verify with `python -m zipfile -l sdks/python/dist/*.whl | grep -E 'py.typed|grpc/gen'`).
  </acceptance_criteria>
  <verify>
    <automated>cd sdks/python && rm -rf dist build && python -m build && twine check dist/*</automated>
  </verify>
  <done>src-layout package builds cleanly, ships py.typed + gRPC stubs as package-data, twine check passes, and the broken backend/3.9 floor are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + commit gRPC stubs with the import fixup (D-04)</name>
  <files>sdks/python/scripts/gen_grpc.sh, sdks/python/src/axiam_sdk/grpc/gen/__init__.py, sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2.py, sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2.pyi, sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2_grpc.py</files>
  <read_first>
    - proto/axiam/v1/authorization.proto (CheckAccess/BatchCheckAccess request/response shapes)
    - sdks/buf.gen.yaml (declares buf.build/protocolbuffers/python + buf.build/grpc/python — the equivalent plugins)
    - 19-RESEARCH.md § Pattern 3 (grpc_tools.protoc codegen command) + Pitfall 1 (bare-import fixup)
    - 19-PATTERNS.md § `sdks/python/src/axiam_sdk/grpc/gen/*` (committed-stub precedent = Go internal/gen)
  </read_first>
  <action>
    Create `sdks/python/scripts/gen_grpc.sh`: a deterministic codegen script that runs `python -m grpc_tools.protoc -I proto --python_out=sdks/python/src/axiam_sdk/grpc/gen --grpc_python_out=sdks/python/src/axiam_sdk/grpc/gen --pyi_out=sdks/python/src/axiam_sdk/grpc/gen proto/axiam/v1/authorization.proto` from the repo root, then applies the Pitfall-1 import fixup to the generated `*_pb2_grpc.py`: rewrite the bare `import authorization_pb2 as authorization__pb2` line to the package-relative form `from . import authorization_pb2 as authorization__pb2` (use a targeted `sed`/`python` in-place edit keyed to the exact generated import statement; do not blanket-rewrite unrelated lines). Standardize on `grpc_tools.protoc` for BOTH local and CI (Pitfall 5 — `buf` CLI is absent; matches Phase 18 precedent).

    Run the script, commit the three generated files `authorization_pb2.py`, `authorization_pb2.pyi`, `authorization_pb2_grpc.py` under `src/axiam_sdk/grpc/gen/` (D-04 — committed, NOT gitignored). The generated `_pb2` message file is fine standalone; only the `_pb2_grpc.py` service file needs the fixup.
  </action>
  <acceptance_criteria>
    - `python -c "from axiam_sdk.grpc.gen import authorization_pb2_grpc"` succeeds with no `ModuleNotFoundError`/`ImportError` (proves the import fixup worked).
    - `grep -c '^from . import authorization_pb2' sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2_grpc.py` returns `1`.
    - `grep -c '^import authorization_pb2 ' sdks/python/src/axiam_sdk/grpc/gen/authorization_pb2_grpc.py` returns `0` (bare import fully replaced).
    - Re-running `bash sdks/python/scripts/gen_grpc.sh` followed by `git diff --exit-code sdks/python/src/axiam_sdk/grpc/gen` exits `0` (deterministic, drift-free — this is the anchor for the CI drift job in 19-07).
    - `python -c "from axiam_sdk.grpc.gen.authorization_pb2 import CheckAccessRequest, CheckAccessResponse, BatchCheckAccessRequest, BatchCheckAccessResponse"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && bash sdks/python/scripts/gen_grpc.sh && git diff --exit-code sdks/python/src/axiam_sdk/grpc/gen && python -c "from axiam_sdk.grpc.gen import authorization_pb2_grpc"</automated>
  </verify>
  <done>gRPC stubs are committed, import-fixed (relative import), importable as a nested package, and regenerate byte-identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AMQP HMAC verifier + real cross-language fixture test (Assumption A2 / Pitfall 2)</name>
  <files>sdks/python/src/axiam_sdk/amqp/_hmac.py, sdks/python/tests/__init__.py, sdks/python/tests/conftest.py, sdks/python/tests/fixtures/amqp_hmac_vectors.json, sdks/python/tests/test_amqp_hmac.py</files>
  <read_first>
    - crates/axiam-amqp/src/messages.rs (sign_payload/verify_payload; AuthzRequest field order: correlation_id,tenant_id,subject_id,action,resource_id,scope,hmac_signature; AuditEventMessage field order; `skip_serializing_if = "Option::is_none"` means None fields are ABSENT, not null)
    - sdks/go/amqp/hmac.go + sdks/go/amqp/hmac_test.go (Go analog + test-vector structure)
    - sdks/CONTRACT.md §8 (HMAC protocol: extract signature, remove field, canonical JSON, HMAC-SHA256, hex, constant-time compare)
    - 19-RESEARCH.md § Pattern 4 (critical correctness note) + Pitfall 2 + Assumption A2
  </read_first>
  <behavior>
    - verify_hmac(signing_key, body) returns True for a body whose hex hmac_signature matches HMAC-SHA256 over the canonical JSON of the remaining fields in their WIRE/insertion order.
    - Returns False (never raises) for: malformed JSON, missing hmac_signature, non-hex signature, wrong-length signature, tampered body, wrong key.
    - Canonicalization preserves the received dict key order (json.dumps with separators=(",",":") and NO sort_keys) — matching Rust struct declaration order.
    - A tampered copy of a valid fixture (one field mutated) fails verification (non-vacuous proof).
  </behavior>
  <action>
    Implement `src/axiam_sdk/amqp/_hmac.py` with `verify_hmac(signing_key: bytes, body: bytes) -> bool` exactly per 19-PATTERNS.md § `_hmac.py`: `json.loads(body)` into a dict (PEP 468 insertion-order preserved), `msg.pop("hmac_signature", None)` (None → reject), `json.dumps(msg, separators=(",", ":"))` WITHOUT `sort_keys`, `bytes.fromhex` (ValueError → reject), `hmac.new(signing_key, canonical, hashlib.sha256).digest()`, and `hmac.compare_digest(computed, expected)` for constant-time compare. Never log/return the signature bytes.

    Create `tests/__init__.py` and `tests/conftest.py` with shared fixtures used by this and later plans: a `respx`-based mock httpx transport fixture, a JWKS-mock fixture placeholder, and a `signing_key: bytes` fixture (a fixed test key).

    Create the REAL cross-language fixture `tests/fixtures/amqp_hmac_vectors.json`. Produce it deterministically by invoking the canonical Rust signer so the vectors are genuinely server-computed (not synthetic): add a tiny throwaway harness OR use an existing Rust test in `crates/axiam-amqp` to emit, for a known signing key + a known `AuthzRequest` and a known `AuditEventMessage` payload (serialized with `hmac_signature` absent, matching `skip_serializing_if`), the `sign_payload` hex output; record `{ signing_key_hex, message (with hmac_signature filled in), expected_valid: true }` vectors plus at least one `expected_valid: false` tampered vector. The fixture MUST be reproducible: document the exact command used to derive it in a comment/README note so a reviewer can regenerate. Do NOT hand-fabricate the hex signature — it must come from the Rust `sign_payload` path so this test genuinely proves byte-for-byte cross-language parity (this is the entire point of Pitfall 2).

    Create `tests/test_amqp_hmac.py`: load each fixture vector, reconstruct the wire `body` bytes (the full message JSON including `hmac_signature`, in the same key order the server emitted), and assert `verify_hmac(bytes.fromhex(signing_key_hex), body)` equals the vector's `expected_valid`. Include the negative/tampered vector and a malformed-JSON case.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_amqp_hmac.py -x -q` passes.
    - At least one fixture vector's `hmac_signature` was produced by `crates/axiam-amqp/src/messages.rs::sign_payload` (documented reproduction command present in the fixture file or an adjacent note) — NOT hand-written.
    - The test proves both a valid vector returns True AND a tampered vector returns False (non-vacuous).
    - `grep -c 'sort_keys' sdks/python/src/axiam_sdk/amqp/_hmac.py` returns `0` (canonicalization preserves wire order, per Pitfall 2).
    - `grep -c 'compare_digest' sdks/python/src/axiam_sdk/amqp/_hmac.py` returns `1` (constant-time compare).
    - verify_hmac returns False (does not raise) when passed `b'{not json'` and when passed a body with no `hmac_signature`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_amqp_hmac.py -x -q</automated>
  </verify>
  <done>The Python HMAC verifier is proven byte-for-byte compatible with the Rust server signer via a real (server-computed) fixture, with a non-vacuous tampered control case; no other AMQP work may proceed on an unverified assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AMQP broker → SDK consumer | Untrusted message bytes cross here; only HMAC-verified messages may reach a handler |
| Proto source → committed stubs | Generated code committed into the tree; drift between proto and stubs is a supply-chain/correctness risk |
| pip registry → build tooling | grpcio-tools / build / twine pulled at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | AMQP message body | high | mitigate | `verify_hmac` constant-time (`hmac.compare_digest`) HMAC-SHA256, wire-order canonicalization proven against the Rust signer via a real fixture; tampered vector rejected |
| T-19-02 | Tampering | committed gRPC stubs vs proto | medium | mitigate | Deterministic `gen_grpc.sh` + `git diff --exit-code` drift anchor (CI job wired in 19-07) |
| T-19-03 | Information Disclosure | HMAC failure logging | medium | mitigate | `_hmac.py` never returns/logs the signature or computed bytes; only a boolean |
| T-19-SC | Tampering | pip installs (grpcio-tools, build, twine, grpcio, aio-pika, httpx, pydantic, PyJWT) | high | mitigate | All packages classified OK in 19-RESEARCH.md § Package Legitimacy Audit (canonical PyPI projects, verified via `pip index versions` + official GitHub orgs); no `[ASSUMED]`/`[SUS]`/`[SLOP]` remain — no human-verify checkpoint required |
</threat_model>

<verification>
- `cd sdks/python && python -m build && twine check dist/*` passes (SC#5 foundation).
- `bash sdks/python/scripts/gen_grpc.sh && git diff --exit-code sdks/python/src/axiam_sdk/grpc/gen` exits 0 (D-04 drift-free).
- `pytest sdks/python/tests/test_amqp_hmac.py -x -q` passes with a real cross-language fixture (§8 / Assumption A2).
- `python -c "from axiam_sdk.grpc.gen import authorization_pb2_grpc"` imports cleanly (Pitfall 1 fixed).
</verification>

<success_criteria>
- src-layout package builds and passes `twine check`; committed gRPC stubs import as a nested package and regenerate drift-free; AMQP HMAC verifier proven byte-for-byte against the server via a real, reproducible fixture with a non-vacuous tampered control.
</success_criteria>

<output>
Create `.planning/phases/19-python-sdk/19-01-SUMMARY.md` when done
</output>
