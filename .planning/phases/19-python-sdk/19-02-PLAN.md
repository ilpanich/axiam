---
phase: 19-python-sdk
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - sdks/python/src/axiam_sdk/_errors.py
  - sdks/python/src/axiam_sdk/_models.py
  - sdks/python/src/axiam_sdk/_jwks.py
  - sdks/python/src/axiam_sdk/token/__init__.py
  - sdks/python/src/axiam_sdk/token/refresh_guard.py
  - sdks/python/tests/test_error_redaction.py
  - sdks/python/tests/test_single_flight.py
  - sdks/python/tests/test_jwks.py
autonomous: true
requirements: [PY-01]
user_setup: []

must_haves:
  truths:
    - "NetworkError never exposes a raw axiam_access/axiam_refresh token value or Set-Cookie/Authorization/Cookie header in repr/str/json/__cause__ (redact-before-wrap; CR-04 carry-forward)"
    - "5 concurrent asyncio tasks refreshing an expired token trigger exactly 1 refresh call (asyncio.Lock single-flight, SC#2)"
    - "the JWKS verifier rejects any token whose alg is not EdDSA BEFORE any keyset lookup (algorithm-confusion defense)"
    - "LoginResult is a single Pydantic model with a mfa_required: bool field; token-bearing fields are SecretStr and redact in repr/model_dump (D-21/D-07)"
  artifacts:
    - sdks/python/src/axiam_sdk/_errors.py
    - sdks/python/src/axiam_sdk/_models.py
    - sdks/python/src/axiam_sdk/_jwks.py
    - sdks/python/src/axiam_sdk/token/refresh_guard.py
    - sdks/python/tests/test_error_redaction.py
    - sdks/python/tests/test_single_flight.py
    - sdks/python/tests/test_jwks.py
  key_links:
    - "error_from_http_status/error_from_grpc_status is the ONLY constructor path for NetworkError from a response (single redaction chokepoint)"
    - "RefreshGuard exposes non-blocking cached_access_token() consumed by the gRPC interceptor (19-04) on the hot path"
    - "JwksVerifier.verify() used by FastAPI/Django integrations (19-06); does NOT check exp (caller's job)"
  prohibitions:
    - "MUST NOT construct NetworkError from a raw httpx.Response anywhere except through _sanitize_response (no NetworkError(response.headers) call sites)"
    - "MUST NOT unify the sync threading.Lock and async asyncio.Lock into one lock (RESEARCH.md anti-pattern — blocking-in-async / not-thread-safe)"
    - "MUST NOT call jwt.decode with algorithms=None or infer alg from the token header (algorithm-confusion)"
    - "MUST NOT enable PyJWKClient(cache_keys=True) (no-TTL per-key LRU — Pattern 5 Pitfall)"
---

<objective>
Build the transport-independent core primitives every later plan reuses: the exception taxonomy with redact-before-wrap (D-08 / CR-04), the Pydantic v2 typed models with `SecretStr` token redaction (D-06/D-07/D-21), the local JWKS verifier with an EdDSA-only allowlist (D-16/CF-07), and the dual-lock single-flight refresh guard (CF-05, SC#2). Each ships with its own automated regression test.

Purpose: These modules have no transport dependencies and are consumed by the REST client (19-03), gRPC client (19-04), AMQP consumer (19-05), and framework integrations (19-06). Landing them first with tests de-risks every downstream plan and closes SC#2 + the CR-04 leak class before any I/O code exists.
Output: `_errors.py`, `_models.py`, `_jwks.py`, `token/refresh_guard.py`, plus redaction/single-flight/JWKS regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-python-sdk/19-CONTEXT.md
@.planning/phases/19-python-sdk/19-RESEARCH.md
@.planning/phases/19-python-sdk/19-PATTERNS.md
@sdks/CONTRACT.md
@sdks/go/errors.go
@sdks/go/internal/refreshguard/guard.go
@sdks/go/internal/jwks/verifier.go
@sdks/typescript/src/core/errorMapper.ts
</context>

## Artifacts this phase produces (this plan)

New symbols/paths (drift verification MUST treat as newly-created):
- `_errors.py`: classes `AuthError`, `AuthzError`, `NetworkError`; functions `error_from_http_status(status, message, response=None)`, `error_from_grpc_status(code, message)`; helper `_sanitize_response(response)`; constant `_SENSITIVE_RESPONSE_HEADERS`.
- `_models.py`: Pydantic models `LoginResult` (fields incl. `mfa_required: bool`, `mfa_token: SecretStr | None`, `user_id`, `tenant_id`, `session_id`, `expires_in`), `User`, `AccessCheck`, `AccessResult`, `BatchCheckResult`.
- `_jwks.py`: `JwksVerifier` (`verify(token) -> dict`), constant `JWKS_PATH = "/oauth2/jwks"`, cache TTL constant.
- `token/__init__.py`, `token/refresh_guard.py`: class `RefreshGuard` with `refresh_if_needed_async`, `refresh_if_needed_sync`, `cached_access_token()`, `cached_refresh_token()`, `seed(...)`.
- Tests: `test_error_redaction.py`, `test_single_flight.py`, `test_jwks.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exception taxonomy + redact-before-wrap + LoginResult/models (D-08/D-06/D-07/D-21)</name>
  <files>sdks/python/src/axiam_sdk/_errors.py, sdks/python/src/axiam_sdk/_models.py, sdks/python/tests/test_error_redaction.py</files>
  <read_first>
    - sdks/go/errors.go (three classes; AuthzError carries action/resource_id; sanitizeResponse chokepoint lines 85-127; status table lines 129-174)
    - sdks/typescript/src/core/errorMapper.ts (sanitizeAxiosError redaction idiom)
    - sdks/CONTRACT.md §2 (HTTP + gRPC status→error tables) + §7 (Sensitive)
    - 19-RESEARCH.md § Code Examples (central error mapper + LoginResult model + redaction regression test — copy-ready)
    - 19-PATTERNS.md § `_errors.py` (redact-before-wrap CRITICAL note) + `_models.py`
    - crates/axiam-api-rest/src/handlers/auth.rs (LoginSuccessResponse/MfaRequiredResponse wire shapes: challenge_token, session_id, expires_in)
  </read_first>
  <behavior>
    - error_from_http_status(401,...) → AuthError; (403 or 409,...) → AuthzError; else → NetworkError with a sanitized cause.
    - error_from_grpc_status(UNAUTHENTICATED,...) → AuthError; (PERMISSION_DENIED,...) → AuthzError; else → NetworkError.
    - A NetworkError built from a response carrying Set-Cookie: axiam_access=<secret> never exposes <secret> in repr/str/repr(__cause__).
    - A non-sensitive header (x-request-id) DOES survive into the sanitized cause (proves redaction is selective, non-vacuous).
    - LoginResult(mfa_required=True, mfa_token=SecretStr("t")) redacts mfa_token in repr() and model_dump().
  </behavior>
  <action>
    Implement `_errors.py` per 19-PATTERNS.md § `_errors.py` and 19-RESEARCH.md § Code Examples: `AuthError`, `AuthzError` (optional `action`/`resource_id`), `NetworkError` (redacted `cause` via `__cause__`). Add `_SENSITIVE_RESPONSE_HEADERS = {"set-cookie", "authorization", "cookie"}` and `_sanitize_response(response)` that builds a string from status + headers filtered case-insensitively. `error_from_http_status(status, message, response=None)` and `error_from_grpc_status(code, message)` are the SOLE constructors — port the Go invariant that any caller-supplied cause is IGNORED in favor of the sanitized-response-derived cause, so no call site can smuggle raw response data. Do NOT expose a bare `NetworkError(headers)` construction path.

    Implement `_models.py` (Pydantic v2): `LoginResult` = single model with `mfa_required: bool` (+ optional `mfa_token: SecretStr | None`, `user_id`, `tenant_id`, `session_id`, `expires_in`), `model_config = {"frozen": True}` (D-21). Map the server wire field `challenge_token` → `mfa_token: SecretStr` (Claude's-discretion rename, snake_case-preserving). Token-bearing fields use `SecretStr` (D-07). Add `User`, `AccessCheck` (fields action/resource_id/scope-optional), `AccessResult` (fields allowed/reason-optional), `BatchCheckResult`.

    Implement `tests/test_error_redaction.py` (mirror 19-RESEARCH.md § Code Examples): assert the raw `axiam_access`/`axiam_refresh` value never appears in `repr(err)`/`str(err)`/`repr(err.__cause__)`; add the non-vacuous control asserting a non-sensitive header value DOES survive.
  </action>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_error_redaction.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_error_redaction.py -x -q` passes including the non-vacuous control case.
    - `python -c "from axiam_sdk._errors import AuthError, AuthzError, NetworkError, error_from_http_status, error_from_grpc_status"` succeeds.
    - `python -c "from axiam_sdk._models import LoginResult; r=LoginResult(mfa_required=True, mfa_token='secret-token'); assert 'secret-token' not in repr(r) and 'secret-token' not in str(r.model_dump())"` exits 0.
    - `grep -rEc 'NetworkError\(.*headers' sdks/python/src/axiam_sdk` returns `0` (no raw-header construction outside the chokepoint).
    - A NetworkError built via `error_from_http_status(401, "x", response=<resp with Set-Cookie: axiam_access=SEKRIT>)` has `"SEKRIT" not in repr(err) and "SEKRIT" not in repr(err.__cause__)`.
  </acceptance_criteria>
  <done>Exception taxonomy maps HTTP/gRPC statuses per CONTRACT §2, redacts sensitive headers through a single chokepoint (non-vacuously proven), and LoginResult redacts its token fields via SecretStr.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dual-lock single-flight refresh guard (SC#2, CF-05)</name>
  <files>sdks/python/src/axiam_sdk/token/__init__.py, sdks/python/src/axiam_sdk/token/refresh_guard.py, sdks/python/tests/test_single_flight.py</files>
  <read_first>
    - sdks/go/internal/refreshguard/guard.go (double-check-after-lock lines 63-88; cached accessors lines 90-115; Seed lines 117-129)
    - sdks/go/internal/refreshguard/guard_test.go (concurrency assertion — literal port target)
    - sdks/CONTRACT.md §9 (Python row: asyncio.Lock + shared asyncio.Future; no-retry-on-failure §9.3)
    - 19-RESEARCH.md § Pattern 2 (dual-lock, double-check, anti-pattern warning) + the ready pytest-asyncio test
    - 19-PATTERNS.md § `token/refresh_guard.py`
  </read_first>
  <behavior>
    - refresh_if_needed_async(observed_access, do_refresh): 5 concurrent tasks with an expired token call do_refresh exactly once; all 5 receive the new token.
    - After lock acquisition, if the cached access differs from observed_access, return cached without calling do_refresh (double-check).
    - do_refresh failure propagates as-is with no retry loop (§9.3).
    - cached_access_token()/cached_refresh_token() are non-blocking reads (no lock acquisition on the hot path).
    - seed(access, refresh, exp) primes the guard after login/verify_mfa.
  </behavior>
  <action>
    Implement `token/refresh_guard.py` with class `RefreshGuard` per 19-PATTERNS.md § `token/refresh_guard.py` and 19-RESEARCH.md § Pattern 2. Two INDEPENDENT locks: `_async_lock: asyncio.Lock`, `_sync_lock: threading.Lock`; each entry point (`refresh_if_needed_async`, `refresh_if_needed_sync`) has its own double-check-after-lock body reading/writing shared `_cached_access`/`_cached_refresh`/`_cached_exp`. Do NOT unify the locks (anti-pattern). Add non-blocking `cached_access_token()`, `cached_refresh_token()`, `cached_exp()` plain-attribute reads (back the gRPC interceptor's non-blocking token func), and `seed(access, refresh, exp)`. `do_refresh` is a caller-supplied callable (async: `Callable[[], Awaitable[...]]`; sync: `Callable[[], ...]`), decoupling this module from the REST session (no import of `_client`).

    Implement `tests/test_single_flight.py`: port the ready pytest-asyncio test — `asyncio.gather` 5 tasks against `RefreshGuard.refresh_if_needed_async` with a `fake_refresh` that increments a counter and `await asyncio.sleep(0.01)`; assert counter == 1 and all results equal the new token.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_single_flight.py -x -q` passes; the counter assertion is exactly `== 1` for 5 concurrent tasks.
    - `grep -c 'asyncio.Lock' sdks/python/src/axiam_sdk/token/refresh_guard.py` returns `1` and `grep -c 'threading.Lock' sdks/python/src/axiam_sdk/token/refresh_guard.py` returns `1` (two independent locks).
    - `cached_access_token()` acquires no lock (source review: it is a plain attribute read; assert via `grep -A3 'def cached_access_token' ... | grep -c 'acquire\|with self._'` returns `0`).
    - do_refresh raising is re-raised by `refresh_if_needed_async` without a retry (test asserts the exception propagates and do_refresh was called once).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_single_flight.py -x -q</automated>
  </verify>
  <done>Exactly one refresh fires under 5 concurrent asyncio tasks (SC#2 proven), locks are independent, and cached reads are non-blocking for the gRPC hot path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Local JWKS verifier — EdDSA-only allowlist + rotate-on-unknown-kid (D-16/CF-07)</name>
  <files>sdks/python/src/axiam_sdk/_jwks.py, sdks/python/tests/test_jwks.py</files>
  <read_first>
    - sdks/go/internal/jwks/verifier.go (jwksPath="/oauth2/jwks"; minRefetch=60s/maxCache=300s; EdDSA-only allowlist BEFORE keyset lookup lines 81-100; forced-refetch-once lines 107-119; verify does NOT check exp)
    - sdks/rust/src/token/jwks.rs (JWKS_CACHE_TTL=300s / FORCED_REFETCH_MIN_INTERVAL=60s timing precedent)
    - 19-RESEARCH.md § Pattern 5 (PyJWKClient two-tier cache; Pitfall: do NOT enable cache_keys; Assumption A3: verify jwk_set_cache attribute against installed PyJWT 2.13)
    - 19-PATTERNS.md § `_jwks.py`
  </read_first>
  <behavior>
    - verify(token) rejects any token whose header alg != "EdDSA" BEFORE any keyset lookup (raises before touching PyJWKClient).
    - On an unknown-kid PyJWKClientError, force exactly one JWKS refetch and retry once; a second failure propagates.
    - verify() returns decoded claims dict and does NOT enforce exp itself (caller's responsibility, documented).
    - A token signed with the mock JWKS key verifies; a wrong-key/HS256 token is rejected.
  </behavior>
  <action>
    Implement `_jwks.py` with `JwksVerifier` per 19-PATTERNS.md § `_jwks.py` and 19-RESEARCH.md § Pattern 5. `JWKS_PATH = "/oauth2/jwks"` (org-wide, NOT tenant-scoped, NOT generic OIDC discovery). Construct `PyJWKClient(base_url.rstrip("/") + JWKS_PATH, cache_jwk_set=True, lifespan=300)` — do NOT pass `cache_keys=True`. `verify(token)`: read `jwt.get_unverified_header(token)`, raise a `ValueError`/`AuthError` if `alg != "EdDSA"` BEFORE calling `get_signing_key_from_jwt` (algorithm-confusion defense). On `PyJWKClientError`, force one cache invalidation + retry (confirm the exact PyJWT 2.13 cache-invalidation attribute at implementation time per Assumption A3 — `pip show PyJWT` then inspect `jwt/jwks_client.py`; use the actual supported invalidation path, rate-limited to one forced refetch per 60s to avoid a rotating-kid hammer). Call `jwt.decode(token, key, algorithms=["EdDSA"], options={"require": ["sub"]})`; do NOT put `exp` enforcement here.

    Implement `tests/test_jwks.py`: generate an Ed25519 keypair in-test, serve a mock JWKS (via `respx`/a local JWKS dict), sign a valid EdDSA token → `verify` returns claims; sign/craft an `alg: HS256` (or `none`) token → `verify` raises before any network fetch; a rotated/unknown-kid path triggers exactly one refetch.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_jwks.py -x -q` passes.
    - `grep -c 'get_unverified_header' sdks/python/src/axiam_sdk/_jwks.py` returns `1` and the alg check precedes any `get_signing_key_from_jwt` call (source-order review).
    - `grep -c 'cache_keys=True' sdks/python/src/axiam_sdk/_jwks.py` returns `0` (per Pattern 5 Pitfall).
    - `grep -c 'algorithms=None' sdks/python/src/axiam_sdk/_jwks.py` returns `0`.
    - `grep -c '"/oauth2/jwks"' sdks/python/src/axiam_sdk/_jwks.py` returns `1` (correct org-wide path).
    - A test asserts an `alg: HS256`/`none` token is rejected with NO JWKS network fetch performed.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_jwks.py -x -q</automated>
  </verify>
  <done>JWKS verification uses PyJWKClient against the org-wide endpoint with an EdDSA-only allowlist enforced before keyset lookup, rotates once on unknown kid, and never enables the no-TTL per-key cache.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → SDK exception | Response headers (Set-Cookie/Authorization) must be redacted before entering any exception surface |
| inbound JWT → local verification | Attacker-controlled `alg`/`kid` header must not weaken verification |
| concurrent callers → refresh endpoint | Thundering-herd refresh must collapse to one call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04 | Information Disclosure | NetworkError repr/logs | high | mitigate | redact-before-wrap single chokepoint (`_sanitize_response`); non-vacuous regression test (CR-04 carry-forward, D-08) |
| T-19-05 | Spoofing | JWT alg confusion (`alg: none`/HS256) | high | mitigate | explicit EdDSA-only allowlist checked BEFORE keyset lookup; `algorithms=["EdDSA"]` on decode (D-16) |
| T-19-06 | Denial of Service | thundering-herd token refresh | medium | mitigate | single-flight guard — exactly 1 in-flight refresh (§9, SC#2) |
| T-19-07 | Information Disclosure | token values in models | medium | mitigate | `SecretStr` on all token-bearing model fields (D-07); redaction test |
</threat_model>

<verification>
- `pytest sdks/python/tests/test_error_redaction.py sdks/python/tests/test_single_flight.py sdks/python/tests/test_jwks.py -x -q` all pass.
- `mypy --strict sdks/python/src/axiam_sdk/_errors.py sdks/python/src/axiam_sdk/_models.py sdks/python/src/axiam_sdk/_jwks.py sdks/python/src/axiam_sdk/token/refresh_guard.py` passes.
- No NetworkError constructed from raw response headers outside `_sanitize_response` (grep gate).
</verification>

<success_criteria>
- Transport-independent core primitives (errors+redaction, models+SecretStr, JWKS+EdDSA allowlist, dual-lock single-flight) all land with passing regression tests; SC#2 and the CR-04 leak class are closed before any I/O code exists.
</success_criteria>

<output>
Create `.planning/phases/19-python-sdk/19-02-SUMMARY.md` when done
</output>
