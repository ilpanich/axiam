---
phase: 19-python-sdk
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - sdks/python/src/axiam_sdk/_session.py
  - sdks/python/src/axiam_sdk/_client.py
  - sdks/python/src/axiam_sdk/__init__.py
  - sdks/python/tests/test_client_login.py
  - sdks/python/tests/test_session_cookies.py
autonomous: true
requirements: [PY-01]
user_setup: []

must_haves:
  truths:
    - "client.login(email, password) (sync) AND await client.async_login(email, password) both exist on one AxiamClient and both return a typed LoginResult with mfa_required (SC#1)"
    - "the httpx client is constructed with verify=True hardcoded (or a custom-CA path) — verify=False never appears in source"
    - "check_access/can/batch_check call POST /api/v1/authz/check(/batch); a 401 triggers exactly one single-flight refresh then one retry"
    - "sync and async calls on one client share the same cookie jar (login via sync then async_check_access reuses the session)"
    - "with AxiamClient(...) as c and async with AxiamClient(...) as c both deterministically close the underlying httpx clients (D-19)"
  artifacts:
    - sdks/python/src/axiam_sdk/_session.py
    - sdks/python/src/axiam_sdk/_client.py
    - sdks/python/tests/test_client_login.py
    - sdks/python/tests/test_session_cookies.py
  key_links:
    - "AxiamClient reuses the RefreshGuard, JwksVerifier, _errors mapper, and LoginResult model from 19-02 (no re-implementation)"
    - "the shared httpx.Cookies jar instance is passed to BOTH httpx.Client and httpx.AsyncClient"
    - "refresh POSTs the exact literal path /api/v1/auth/refresh so the path-scoped axiam_refresh cookie attaches (Pitfall 4)"
    - "login/refresh request bodies include org_slug/org_id (Pitfall 3), resolved from the access token's org_id claim if not supplied"
  prohibitions:
    - "MUST NOT accept a caller-supplied httpx client/verify value that could carry verify=False; the only TLS escape hatch is a custom-CA path/SSLContext (CF-03/SC#3)"
    - "MUST NOT eagerly construct both httpx.Client and httpx.AsyncClient in __init__ (lazy per paradigm)"
    - "MUST NOT default tenant_slug — empty/missing tenant raises at construction (CF-04)"
    - "MUST NOT retry a 401 on the refresh call itself (no refresh retry loop, §9.3)"
---

<objective>
Build the public REST surface: the shared `_Session` (one cookie jar, CSRF capture, tenant/org header injection, lazy sync+async httpx clients, dual refresh guard wired in) and the unified `AxiamClient` exposing sync methods + `async_*` variants for `login`/`verify_mfa`/`refresh`/`logout` and `check_access`/`can`/`batch_check`, with context-manager lifecycle. This closes SC#1 (both `login` and `async_login` return `LoginResult`) and the REST half of SC#2/SC#3.

Purpose: This is the primary user-facing entry point and the core of PY-01. It composes the 19-02 primitives (errors, models, JWKS, refresh guard) into a working, tested REST client honoring the real server facts (org_id requirement, path-scoped refresh cookie).
Output: `_session.py`, `_client.py`, the populated `__init__.py` public surface, and login/cookie-sharing regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-python-sdk/19-CONTEXT.md
@.planning/phases/19-python-sdk/19-RESEARCH.md
@.planning/phases/19-python-sdk/19-PATTERNS.md
@sdks/CONTRACT.md
@sdks/go/client.go
@sdks/go/login.go
@sdks/go/authz.go
@.planning/phases/19-python-sdk/19-02-SUMMARY.md
</context>

## Artifacts this phase produces (this plan)

New symbols/paths (drift verification MUST treat as newly-created):
- `_session.py`: class `_Session` (`base_url`, `tenant_slug`, shared `httpx.Cookies` jar, `_csrf_token` + `threading.Lock`, lazy `sync_client`/`async_client` properties, `_prepare_request`, `_capture_csrf`, `_send_sync`, `_send_async`, dual `RefreshGuard`, `verify` = True|custom-CA, `close`/`aclose`).
- `_client.py`: class `AxiamClient` with sync methods `login`, `verify_mfa`, `refresh`, `logout`, `check_access`, `can`, `batch_check`; async `async_login`, `async_verify_mfa`, `async_refresh`, `async_logout`, `async_check_access`, `async_can`, `async_batch_check`; `__enter__`/`__exit__`/`__aenter__`/`__aexit__`, `close()`/`aclose()`; constructor kwargs `base_url`, `tenant_slug`, `org_slug=`/`org_id=` (mutually exclusive), `custom_ca=`, `timeout=`, `logger=`; internal `resolved_org_id()`.
- `__init__.py`: re-exports `AxiamClient`, `LoginResult`, `AuthError`, `AuthzError`, `NetworkError`, `AccessCheck`, `AccessResult`, `User`.
- Tests: `test_client_login.py`, `test_session_cookies.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared _Session — cookie jar, CSRF capture, TLS, lazy dual httpx clients (CF-01/CF-02/CF-03)</name>
  <files>sdks/python/src/axiam_sdk/_session.py, sdks/python/tests/test_session_cookies.py</files>
  <read_first>
    - sdks/go/client.go (construction/options lines 39-162; decorateRequest CSRF lines 220-255; captureCSRFFromResponse; doRequest chokepoint lines 257-269; resolvedOrgID lines 271-295)
    - 19-RESEARCH.md § Pattern 1 (lazy sync/async httpx clients, shared jar, verify=True hardcoded) + Pitfall 3 (org_id) + Pitfall 4 (refresh cookie path)
    - 19-PATTERNS.md § `_client.py` + `_session.py` (CSRF capture verbatim; single choke-point; jar-sharing unit test requirement A1)
    - sdks/CONTRACT.md §3 (non-browser CSRF: capture X-CSRF-Token from response header) + §4 (cookie jar) + §5 (tenant) + §6 (TLS)
  </read_first>
  <behavior>
    - sync_client and async_client are constructed lazily on first use with verify=True (or the custom-CA value), sharing one httpx.Cookies jar instance.
    - _prepare_request sets X-Tenant-ID always and echoes captured X-CSRF-Token on POST/PUT/PATCH/DELETE.
    - _capture_csrf(response) stores a fresh X-CSRF-Token from any response header (guarded by threading.Lock).
    - sync_client.cookies.jar and async_client.cookies.jar reference the same underlying jar (Assumption A1 empirical check).
  </behavior>
  <action>
    Implement `_session.py` per 19-RESEARCH.md § Pattern 1 and 19-PATTERNS.md § `_session.py`. One `httpx.Cookies()` jar passed to BOTH lazily-built `httpx.Client` and `httpx.AsyncClient`. `verify` field = `custom_ca` if supplied else `True` — NEVER `False`; the constructor accepts only a custom-CA path/SSLContext escape hatch, never a boolean or a caller-supplied `httpx.Client` that could carry `verify=False` (CF-03). `_prepare_request(request)` sets `X-Tenant-ID` = tenant_slug always and echoes the captured CSRF token on state-changing methods; `_capture_csrf(response)` stores `X-CSRF-Token` under a `threading.Lock` (touched from both sync+async paths). `_send_sync`/`_send_async` are the single choke points routing every REST call through `_prepare_request`/`_capture_csrf`. Hold the `RefreshGuard` (from 19-02) here. Add `close()` (closes sync client) and `aclose()` (closes async client) — only closing the client actually constructed.

    Add an empirical Assumption-A1 unit test in `tests/test_session_cookies.py` asserting the sync and async clients share one jar (identity or a behavioral test: set a cookie via the sync client's jar, read it via the async client's jar), pinned to the installed httpx 0.27.x.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_session_cookies.py -x -q` passes (jar-sharing proven for the pinned httpx).
    - `grep -rEc 'verify\s*=\s*(False|0)' sdks/python/src/axiam_sdk/_session.py` returns `0` (SC#3).
    - `grep -c 'httpx.Cookies' sdks/python/src/axiam_sdk/_session.py` returns `1` (single shared jar instance).
    - Source review confirms sync_client/async_client are `@property`-lazy (not built in `__init__`).
    - `_capture_csrf`/`_prepare_request` exist and X-Tenant-ID is set on every request (test asserts the header is present on an outgoing mocked request).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_session_cookies.py -x -q && ! grep -rEn 'verify\s*=\s*(False|0)' sdks/python/src/axiam_sdk/_session.py</automated>
  </verify>
  <done>_Session shares one cookie jar across lazily-built sync+async httpx clients with verify=True hardcoded, captures/echoes CSRF via a single request choke point, and holds the refresh guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AxiamClient — sync+async login/verify_mfa/refresh/logout + org_id + lifecycle (SC#1, D-01/D-19)</name>
  <files>sdks/python/src/axiam_sdk/_client.py, sdks/python/src/axiam_sdk/__init__.py, sdks/python/tests/test_client_login.py</files>
  <read_first>
    - sdks/go/login.go (loginRequestBody with org_id/org_slug/tenant_id/tenant_slug; mfaVerifyRequestBody challenge_token/totp_code; refresh/logout bodies; cookie names axiam_access/axiam_refresh; paths /api/v1/auth/login|mfa/verify|refresh|logout)
    - sdks/go/client.go (resolvedOrgID fallback from access-token org_id claim)
    - 19-RESEARCH.md § Pattern 1 + Pitfall 3 (org_id required) + Pitfall 4 (refresh path)
    - 19-PATTERNS.md § `_client.py`
    - sdks/CONTRACT.md §1 (method map: login/verify_mfa/refresh/logout) + §5 (tenant required)
    - 19-02-SUMMARY.md (LoginResult, RefreshGuard.seed, error mapper signatures)
  </read_first>
  <behavior>
    - AxiamClient(base_url=..., tenant_slug="acme") constructs; empty/missing tenant_slug raises AuthError at construction.
    - login(email, password) returns LoginResult(mfa_required=...); on mfa_required, verify_mfa(mfa_token, code) completes the flow. async_login/async_verify_mfa mirror this on the same object.
    - login/refresh request bodies carry org_slug or org_id (mutually exclusive); if neither supplied, org_id is resolved from the access token's org_id claim after first login.
    - refresh POSTs exactly /api/v1/auth/refresh; a 401 on the refresh call itself surfaces AuthError with no retry.
    - with AxiamClient(...) as c / async with AxiamClient(...) as c close the constructed httpx client(s) on exit.
  </behavior>
  <action>
    Implement `_client.py` `AxiamClient` per 19-PATTERNS.md § `_client.py`. Keyword-only constructor: `base_url` (required), `tenant_slug` (required — empty raises `AuthError`, CF-04), `org_slug=`/`org_id=` (mutually exclusive; raise if both, Pitfall 3), `custom_ca=`, `timeout=` (sane default per D-17), `logger=` (injectable, NullHandler default, D-15). Build request bodies mirroring `sdks/go/login.go` wire shapes (`username_or_email`, `password`, `tenant_slug`/`tenant_id`, `org_slug`/`org_id`; mfa `challenge_token`/`totp_code`). After login/verify_mfa parse the `LoginResult`, `seed()` the RefreshGuard, and populate `resolved_org_id()` from the access token's `org_id` claim if org was not explicitly set. `refresh` POSTs the literal `/api/v1/auth/refresh` (Pitfall 4). Every sync method has an `async_*` twin sharing the same private helpers/session. Implement `__enter__`/`__exit__` (call `close()`) and `__aenter__`/`__aexit__` (call `aclose()`) plus explicit `close()`/`aclose()` (D-19). Populate `__init__.py` re-exports.

    Implement `tests/test_client_login.py` using `respx` to mock `/api/v1/auth/login` + `/mfa/verify`: assert BOTH `client.login(...)` and `await client.async_login(...)` return a `LoginResult` with `mfa_required`; assert the two-phase MFA flow; assert an mfa-required response yields `mfa_required=True` and a masked `mfa_token`. Assert empty `tenant_slug` raises at construction. Assert `org_slug` appears in the mocked login request body.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_client_login.py -x -q` passes.
    - Test proves `client.login(...)` returns `LoginResult` AND `await client.async_login(...)` returns `LoginResult`, both with a `mfa_required` attribute (SC#1 literal).
    - `python -c "from axiam_sdk import AxiamClient, LoginResult, AuthError, AuthzError, NetworkError"` succeeds.
    - Constructing `AxiamClient(base_url='https://x', tenant_slug='')` raises `AuthError` (test).
    - Passing both `org_slug` and `org_id` raises (test).
    - `grep -c '"/api/v1/auth/refresh"' sdks/python/src/axiam_sdk/_client.py` returns at least `1` (exact literal refresh path, Pitfall 4).
    - `mypy --strict sdks/python/src/axiam_sdk/_client.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_client_login.py -x -q</automated>
  </verify>
  <done>The unified AxiamClient exposes sync + async login/verify_mfa/refresh/logout returning typed LoginResult (SC#1), honors org_id + path-scoped refresh, requires tenant_slug, and cleans up via context managers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REST authz (check_access/can/batch_check) + 401 single-flight refresh-and-retry</name>
  <files>sdks/python/src/axiam_sdk/_client.py, sdks/python/tests/test_client_login.py</files>
  <read_first>
    - sdks/go/authz.go (checkPath /api/v1/authz/check; batchCheckPath /api/v1/authz/check/batch; AccessCheck/AccessResult shapes; CF-01 idempotent retry bound)
    - sdks/go/client.go (401 → refresh guard → retry-exactly-once path)
    - sdks/CONTRACT.md §1 (check_access + can + batch_check) + §9 (single-flight)
    - 19-PATTERNS.md § Shared Patterns (single-flight-retry-exactly-once)
    - 19-02-SUMMARY.md (RefreshGuard.refresh_if_needed_* signatures, AccessCheck/AccessResult models)
  </read_first>
  <behavior>
    - check_access(action, resource_id, scope=None) POSTs /api/v1/authz/check and returns an AccessResult{allowed, reason?}; can(...) returns the bool.
    - batch_check(checks) POSTs /api/v1/authz/check/batch and returns a list of AccessResult.
    - A 401 on an authz call routes through the shared single-flight guard: exactly 1 refresh across concurrent callers, then the failed call retries exactly once; a second failure propagates as AuthError.
    - 403/PERMISSION_DENIED surfaces AuthzError; a transient 429/503 on the idempotent authz call may retry with bounded backoff (D-17).
    - async_check_access/async_can/async_batch_check mirror the sync methods on the same client, sharing the session + guard.
  </behavior>
  <action>
    Implement the authz methods on `AxiamClient` (in `_client.py`) per 19-PATTERNS.md § Shared Patterns and `sdks/go/authz.go`. `check_access`/`async_check_access` POST `/api/v1/authz/check`; `batch_check`/`async_batch_check` POST `/api/v1/authz/check/batch`; `can`/`async_can` = `check_access(...).allowed`. On a 401, call the shared `RefreshGuard.refresh_if_needed_sync`/`_async` (observed access token → do_refresh closure that POSTs `/api/v1/auth/refresh`), then retry the failed authz call EXACTLY once; propagate a second failure via the `_errors` mapper (§9.3, no loop). Map 403 → AuthzError, 401-on-refresh → AuthError. Apply bounded backoff-with-jitter on 429/503 for these idempotent reads only (D-17); state-changing auth calls never retry. Reuse the central `error_from_http_status` mapper — do not re-derive the status table.

    Extend `tests/test_client_login.py` (or add cases) with `respx`: mock an authz endpoint returning 401 once then 200 after a mocked refresh; assert exactly one refresh call and one retry; assert `can(...)` returns the bool; assert 403 → AuthzError.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_client_login.py -x -q` passes including the authz/401-retry cases.
    - A test asserts a single 401 on an authz call triggers exactly one refresh POST to `/api/v1/auth/refresh` and one retry of the authz call.
    - A test asserts a 403 authz response raises `AuthzError`.
    - `grep -c '"/api/v1/authz/check"' sdks/python/src/axiam_sdk/_client.py` returns at least `1`; `grep -c '"/api/v1/authz/check/batch"' sdks/python/src/axiam_sdk/_client.py` returns at least `1`.
    - No independent status→error table exists in `_client.py` (it delegates to `_errors.error_from_http_status`): `grep -c 'error_from_http_status' sdks/python/src/axiam_sdk/_client.py` returns at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_client_login.py -x -q && mypy --strict sdks/python/src/axiam_sdk/_client.py sdks/python/src/axiam_sdk/_session.py</automated>
  </verify>
  <done>REST check_access/can/batch_check work over FND-04, a 401 collapses to one single-flight refresh + one retry, and authz errors map through the central taxonomy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK client → AXIAM REST API | TLS-protected; cookies + CSRF cross here; token values must never leak |
| concurrent callers → refresh endpoint | thundering-herd must collapse to one refresh |
| caller config → TLS verification | no path may disable certificate verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-08 | Tampering / Spoofing | TLS downgrade / cert bypass | high | mitigate | `verify=True` hardcoded; only escape hatch is a custom-CA path; no boolean/caller-httpx bypass accepted (SC#3, CF-03) |
| T-19-09 | Information Disclosure | tokens in error/log surfaces | high | mitigate | reuses 19-02 redact-before-wrap mapper; SecretStr models; injectable logger never emits token values (D-08/D-07/D-15) |
| T-19-10 | Denial of Service | concurrent-401 refresh storm | medium | mitigate | shared single-flight guard across REST paths — exactly 1 refresh, retry-once (§9) |
| T-19-11 | Spoofing | CSRF on state-changing requests | medium | mitigate | X-CSRF-Token captured from response header and echoed on POST/PUT/PATCH/DELETE (§3, non-browser) |
</threat_model>

<verification>
- `pytest sdks/python/tests/test_client_login.py sdks/python/tests/test_session_cookies.py -x -q` pass.
- `! grep -rEn 'verify\s*=\s*(False|0)' sdks/python/src/axiam_sdk` (SC#3).
- `mypy --strict sdks/python/src/axiam_sdk/_client.py sdks/python/src/axiam_sdk/_session.py` passes.
- CONTRACT.md §1/§3/§4/§5/§6/§9 conformance for the REST surface.
</verification>

<success_criteria>
- One AxiamClient exposes sync `login` and async `async_login` both returning typed `LoginResult` (SC#1); REST authz works with single-flight refresh; TLS is strict-by-default; the session shares one cookie jar across paradigms and cleans up deterministically.
</success_criteria>

<output>
Create `.planning/phases/19-python-sdk/19-03-SUMMARY.md` when done
</output>
