---
phase: 19-python-sdk
plan: 04
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - sdks/python/src/axiam_sdk/grpc/__init__.py
  - sdks/python/src/axiam_sdk/grpc/_interceptor.py
  - sdks/python/src/axiam_sdk/grpc/client.py
  - sdks/python/src/axiam_sdk/grpc/_tls.py
  - sdks/python/tests/test_grpc_client.py
  - sdks/python/tests/test_grpc_interceptor.py
autonomous: true
requirements: [PY-01]
user_setup: []

must_haves:
  truths:
    - "both a sync (grpcio) AuthzGrpcClient and an async (grpc.aio) AsyncAuthzGrpcClient perform CheckAccess and BatchCheckAccess (D-12)"
    - "the auth/tenant interceptor reads the token via a non-blocking cached accessor and never acquires the refresh lock on the RPC hot path"
    - "a gRPC UNAUTHENTICATED response triggers exactly one refresh via the caller-supplied refresh closure then one retry of the call"
    - "the gRPC channel uses TLS credentials with no insecure/skip-verification path"
  artifacts:
    - sdks/python/src/axiam_sdk/grpc/_interceptor.py
    - sdks/python/src/axiam_sdk/grpc/client.py
    - sdks/python/src/axiam_sdk/grpc/_tls.py
    - sdks/python/tests/test_grpc_client.py
    - sdks/python/tests/test_grpc_interceptor.py
  key_links:
    - "the interceptor's token func reads RefreshGuard.cached_access_token() (19-02) — non-blocking"
    - "gRPC error mapping delegates to _errors.error_from_grpc_status (19-02) — no duplicate status table"
    - "the refresh closure is caller-supplied (Callable / Awaitable), decoupling grpc/ from _client.py (no import cycle)"
  prohibitions:
    - "MUST NOT use grpc.insecure_channel / grpc.aio.insecure_channel or any TLS-skip channel in SDK source"
    - "MUST NOT call the blocking refresh path from inside an interceptor (only cached read)"
    - "MUST NOT import _client.py from grpc/client.py (accept a refresh closure instead)"
    - "MUST NOT retry an UNAUTHENTICATED call more than once after a successful refresh (§9.3)"
---

<objective>
Deliver the gRPC transport: sync (`grpcio`) and async (`grpc.aio`) authorization clients for `CheckAccess`/`BatchCheckAccess` (D-12), a sync-safe auth/tenant metadata interceptor for both variants, strict-TLS channel construction, and UNAUTHENTICATED → single-flight-refresh → retry-once handling. Built on the committed stubs (19-01) and the core primitives (19-02).

Purpose: PY-01 requires gRPC authz coverage in both sync and async forms. This mirrors the unified client's dual surface for the authz transport so async callers get a first-class (not thread-pool-bridged) path.
Output: `grpc/_interceptor.py`, `grpc/client.py`, `grpc/_tls.py`, and interceptor + client tests using an in-process test server.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-python-sdk/19-CONTEXT.md
@.planning/phases/19-python-sdk/19-RESEARCH.md
@.planning/phases/19-python-sdk/19-PATTERNS.md
@sdks/CONTRACT.md
@sdks/go/grpc/interceptor.go
@sdks/go/grpc/client.go
@sdks/go/grpc/tls.go
@proto/axiam/v1/authorization.proto
@.planning/phases/19-python-sdk/19-02-SUMMARY.md
</context>

## Artifacts this phase produces (this plan)

New symbols/paths (drift verification MUST treat as newly-created):
- `grpc/_interceptor.py`: `_AuthMetadataMixin` (`_build_metadata`), `SyncAuthInterceptor(grpc.UnaryUnaryClientInterceptor)`, `AsyncAuthInterceptor(grpc.aio.UnaryUnaryClientInterceptor)`.
- `grpc/client.py`: `AuthzGrpcClient` (sync: `check_access`, `batch_check`), `AsyncAuthzGrpcClient` (async: `check_access`, `batch_check`); constructor accepts `target`, `token_fn`, `tenant_id`, `refresh_fn`, `custom_ca=`.
- `grpc/_tls.py`: `build_channel_credentials(custom_ca=None)` (strict TLS; `grpc.ssl_channel_credentials`).
- `grpc/__init__.py`: re-exports `AuthzGrpcClient`, `AsyncAuthzGrpcClient`.
- Tests: `test_grpc_client.py`, `test_grpc_interceptor.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sync + async auth/tenant interceptors (non-blocking token func)</name>
  <files>sdks/python/src/axiam_sdk/grpc/_interceptor.py, sdks/python/tests/test_grpc_interceptor.py</files>
  <read_first>
    - sdks/go/grpc/interceptor.go (TokenFunc non-blocking invariant lines 10-26; metadata append authorization Bearer + x-tenant-id)
    - 19-RESEARCH.md § Pattern 3 (SyncAuthInterceptor + AsyncAuthInterceptor; async must await continuation; different base classes)
    - 19-PATTERNS.md § `grpc/_interceptor.py` + Shared Patterns (non-blocking cached read)
    - 19-02-SUMMARY.md (RefreshGuard.cached_access_token() accessor)
  </read_first>
  <behavior>
    - _build_metadata(existing) appends ("authorization", "Bearer <token>") when the non-blocking token func returns a token, and always ("x-tenant-id", tenant_id).
    - SyncAuthInterceptor.intercept_unary_unary calls continuation synchronously with the augmented metadata.
    - AsyncAuthInterceptor.intercept_unary_unary is async and awaits continuation with the augmented metadata.
    - The token func is invoked as a plain callable and never acquires the refresh lock.
  </behavior>
  <action>
    Implement `grpc/_interceptor.py` per 19-RESEARCH.md § Pattern 3 and 19-PATTERNS.md § `grpc/_interceptor.py`. `_AuthMetadataMixin(token_fn, tenant_id)` with `_build_metadata(existing)` appending the Bearer authorization + `x-tenant-id` metadata. `SyncAuthInterceptor(_AuthMetadataMixin, grpc.UnaryUnaryClientInterceptor)` with a synchronous `intercept_unary_unary`; `AsyncAuthInterceptor(_AuthMetadataMixin, grpc.aio.UnaryUnaryClientInterceptor)` with `async def intercept_unary_unary` awaiting `continuation`. `token_fn` MUST be documented and used as a non-blocking cache read (backed by `RefreshGuard.cached_access_token()`); never acquire the refresh lock here. Name the classes generically (Auth*, not Unary*) to avoid a rename when streaming is added.

    Implement `tests/test_grpc_interceptor.py`: assert `_build_metadata` produces the expected metadata pairs given a token func returning a token vs None; assert the async interceptor awaits its continuation (a mocked continuation); assert the token func is called exactly once per intercept and acquires no lock.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_grpc_interceptor.py -x -q` passes.
    - `grep -c 'class SyncAuthInterceptor' sdks/python/src/axiam_sdk/grpc/_interceptor.py` returns `1` and `grep -c 'class AsyncAuthInterceptor' ...` returns `1`.
    - `grep -c 'grpc.aio.UnaryUnaryClientInterceptor' sdks/python/src/axiam_sdk/grpc/_interceptor.py` returns `1` (correct async base class).
    - Source review: `_build_metadata`/`token_fn` performs no lock acquisition (`grep -c 'acquire\|Lock' sdks/python/src/axiam_sdk/grpc/_interceptor.py` returns `0`).
    - A test asserts the async interceptor's `intercept_unary_unary` is a coroutine that awaits its continuation.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_grpc_interceptor.py -x -q</automated>
  </verify>
  <done>Both interceptor variants inject Bearer + tenant metadata from a non-blocking token func, with the async variant correctly awaiting its continuation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strict-TLS channel + sync/async AuthzGrpcClient with UNAUTHENTICATED retry-once</name>
  <files>sdks/python/src/axiam_sdk/grpc/_tls.py, sdks/python/src/axiam_sdk/grpc/client.py, sdks/python/src/axiam_sdk/grpc/__init__.py, sdks/python/tests/test_grpc_client.py</files>
  <read_first>
    - sdks/go/grpc/client.go (CheckAccess UNAUTHENTICATED single-flight-retry-once lines 82-98; BatchCheck lines 103-128; RefreshFunc decoupling lines 28-32; status mapping delegates to errors.go)
    - sdks/go/grpc/tls.go (strict TLS credentials, no insecure path)
    - proto/axiam/v1/authorization.proto (CheckAccessRequest tenant_id/subject_id/action/resource_id/scope; CheckAccessResponse allowed/deny_reason; Batch shapes)
    - 19-RESEARCH.md § Pattern 3 (same generated stub for sync+async; secure_channel only)
    - 19-PATTERNS.md § `grpc/client.py` + Shared Patterns (single-flight-retry-once; reuse _errors mapper)
    - 19-02-SUMMARY.md (error_from_grpc_status, AccessResult model)
  </read_first>
  <behavior>
    - AuthzGrpcClient.check_access(subject_id, action, resource_id, scope=None) returns AccessResult{allowed, reason}; batch_check(checks) returns list[AccessResult].
    - AsyncAuthzGrpcClient mirrors both methods with async def, awaiting the aio stub.
    - On UNAUTHENTICATED, the client calls the caller-supplied refresh_fn exactly once then retries the RPC exactly once; a second failure maps via error_from_grpc_status (AuthError).
    - PERMISSION_DENIED maps to AuthzError.
    - The channel is built with TLS credentials (grpc.secure_channel / grpc.aio.secure_channel) — no insecure channel anywhere.
  </behavior>
  <action>
    Implement `grpc/_tls.py` `build_channel_credentials(custom_ca=None)` using `grpc.ssl_channel_credentials` (root certs from the custom CA PEM when supplied, else system roots) — no `grpc.insecure_channel` anywhere. Implement `grpc/client.py` per 19-PATTERNS.md § `grpc/client.py`: `AuthzGrpcClient` builds a `grpc.secure_channel` + `SyncAuthInterceptor`; `AsyncAuthzGrpcClient` builds a `grpc.aio.secure_channel` + `AsyncAuthInterceptor`. Both instantiate `authorization_pb2_grpc.AuthorizationServiceStub` (from 19-01's committed stubs). `check_access`/`batch_check` build the proto request from `tenant_id` (the client's configured tenant), `subject_id`, `action`, `resource_id`, `scope`, call the stub, and on `grpc.StatusCode.UNAUTHENTICATED` invoke the caller-supplied `refresh_fn` (sync `Callable[[], None]` / async `Callable[[], Awaitable[None]]`) then retry EXACTLY once (§9.3, no loop); map any final error via `_errors.error_from_grpc_status` (no duplicate table). Accept `refresh_fn` as a constructor arg so `grpc/client.py` never imports `_client.py` (no import cycle). Map `deny_reason` → `AccessResult.reason`.

    Implement `tests/test_grpc_client.py`: stand up an in-process test `AuthorizationService` (using `grpc` server + the committed servicer) that returns allowed/denied and can be scripted to return UNAUTHENTICATED once. Assert sync AND async `check_access`/`batch_check` succeed; assert an UNAUTHENTICATED-then-OK sequence calls `refresh_fn` exactly once and retries once; assert PERMISSION_DENIED → AuthzError.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_grpc_client.py -x -q` passes for both sync and async clients.
    - `grep -rEc 'insecure_channel|insecure\(' sdks/python/src/axiam_sdk/grpc` returns `0` (no TLS-skip; SC#3-adjacent).
    - A test asserts an UNAUTHENTICATED response triggers exactly one `refresh_fn` call and exactly one RPC retry.
    - A test asserts PERMISSION_DENIED maps to `AuthzError`.
    - `grep -c 'error_from_grpc_status' sdks/python/src/axiam_sdk/grpc/client.py` returns at least `1` (delegates to central mapper, no duplicate table).
    - `grep -c 'import' sdks/python/src/axiam_sdk/grpc/client.py` shows no import of `_client` (`grep -c 'from .._client\|import _client' sdks/python/src/axiam_sdk/grpc/client.py` returns `0`).
    - `mypy --strict sdks/python/src/axiam_sdk/grpc` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_grpc_client.py -x -q && ! grep -rEn 'insecure_channel|insecure\(' sdks/python/src/axiam_sdk/grpc</automated>
  </verify>
  <done>Sync and async gRPC authz clients perform CheckAccess/BatchCheckAccess over strict TLS, retry exactly once on UNAUTHENTICATED via a decoupled refresh closure, and map errors through the central taxonomy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK gRPC client → AXIAM gRPC service | TLS channel; bearer token + tenant metadata cross here |
| interceptor → refresh guard | hot RPC path must not block on refresh |
| caller config → channel TLS | no insecure channel may be constructed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-12 | Tampering / Spoofing | gRPC channel TLS bypass | high | mitigate | `grpc.ssl_channel_credentials` only; no `insecure_channel` path anywhere in `grpc/` (CI grep gate in 19-07) |
| T-19-13 | Denial of Service | interceptor blocking on refresh | medium | mitigate | interceptor reads only the non-blocking cached token; never acquires the refresh lock |
| T-19-14 | Elevation of Privilege | UNAUTHENTICATED retry loop | medium | mitigate | retry EXACTLY once after one refresh; no loop (§9.3) |
| T-19-15 | Information Disclosure | gRPC error → token leak | medium | mitigate | errors mapped via central `error_from_grpc_status`; no raw metadata/token in error text |
</threat_model>

<verification>
- `pytest sdks/python/tests/test_grpc_client.py sdks/python/tests/test_grpc_interceptor.py -x -q` pass.
- `! grep -rEn 'insecure_channel|insecure\(' sdks/python/src/axiam_sdk/grpc`.
- `mypy --strict sdks/python/src/axiam_sdk/grpc` passes.
- CONTRACT.md §1 (CheckAccess/BatchCheckAccess) + §6 (TLS) + §9 (single-flight) conformance for gRPC.
</verification>

<success_criteria>
- Dual sync+async gRPC authz clients work against an in-process server over strict TLS, with a non-blocking interceptor and exactly-once UNAUTHENTICATED refresh-and-retry via a decoupled refresh closure.
</success_criteria>

<output>
Create `.planning/phases/19-python-sdk/19-04-SUMMARY.md` when done
</output>
