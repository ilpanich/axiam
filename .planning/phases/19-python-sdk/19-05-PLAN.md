---
phase: 19-python-sdk
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - sdks/python/src/axiam_sdk/amqp/__init__.py
  - sdks/python/src/axiam_sdk/amqp/_consumer.py
  - sdks/python/tests/test_amqp_consumer.py
autonomous: true
requirements: [PY-01]
user_setup: []

must_haves:
  truths:
    - "the async closure-handler consumer verifies HMAC-SHA256 BEFORE the handler is ever invoked; an unverified message never reaches the handler"
    - "HMAC-fail and post-verify parse-fail both nack WITHOUT requeue and emit a security log that never contains the signature value"
    - "handler returns None → ack; handler raises the ErrDrop sentinel → nack WITHOUT requeue; handler raises any other exception → nack WITH requeue"
    - "the consumer owns the ack/nack loop (SDK decides), not aio-pika's automatic context manager"
  artifacts:
    - sdks/python/src/axiam_sdk/amqp/_consumer.py
    - sdks/python/src/axiam_sdk/amqp/__init__.py
    - sdks/python/tests/test_amqp_consumer.py
  key_links:
    - "consume() calls verify_hmac() from 19-01 amqp/_hmac.py (proven byte-for-byte against the server)"
    - "the ErrDrop sentinel is exported from axiam_sdk.amqp and mirrors Go's ErrDrop"
    - "prefetch default = 10 (channel.set_qos(prefetch_count=prefetch))"
  prohibitions:
    - "MUST NOT invoke the handler before HMAC verification succeeds"
    - "MUST NOT requeue on HMAC failure, post-verify parse failure, or ErrDrop (requeue=False)"
    - "MUST NOT log/interpolate the received or computed HMAC signature bytes in the security event"
---

<objective>
Deliver the async-only AMQP event consumer (`aio-pika`, D-02): a closure-handler `consume()` that verifies each message's HMAC-SHA256 (via 19-01's proven `verify_hmac`) BEFORE invoking the handler, owns the ack/nack loop, and enforces the exact §8 decision matrix — HMAC-fail/parse-fail → nack-without-requeue + security log; handler `None` → ack; handler `ErrDrop` → nack-without-requeue; other handler exception → nack-with-requeue.

Purpose: PY-01 requires the AMQP consumer with mandatory HMAC verify-before-handler. This is the security-critical event path; it builds directly on the byte-for-byte-verified HMAC primitive from Wave 0.
Output: `amqp/_consumer.py`, the `amqp/__init__.py` public surface (`consume`, `ErrDrop`), and a consumer test proving the full ack/nack decision matrix without a live broker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-python-sdk/19-CONTEXT.md
@.planning/phases/19-python-sdk/19-RESEARCH.md
@.planning/phases/19-python-sdk/19-PATTERNS.md
@sdks/CONTRACT.md
@sdks/go/amqp/consumer.go
@sdks/go/amqp/event.go
@sdks/go/amqp/errdrop.go
@.planning/phases/19-python-sdk/19-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan)

New symbols/paths (drift verification MUST treat as newly-created):
- `amqp/_consumer.py`: async function `consume(channel, queue_name, signing_key, handler, *, prefetch=10, logger=None)`; class/sentinel `ErrDrop(Exception)`; internal `_on_message` dispatch.
- `amqp/__init__.py`: re-exports `consume`, `ErrDrop` (and re-exports `verify_hmac` from `_hmac` if useful).
- Tests: `test_amqp_consumer.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Async closure-handler consumer with verify-before-handler + ack/nack matrix (D-02, §8)</name>
  <files>sdks/python/src/axiam_sdk/amqp/_consumer.py, sdks/python/src/axiam_sdk/amqp/__init__.py, sdks/python/tests/test_amqp_consumer.py</files>
  <read_first>
    - sdks/go/amqp/consumer.go (verifyAndDispatch ack/nack matrix lines 108-155; AckableDelivery seam for hermetic testing; defaultPrefetch=10; securityLogger never logs signature)
    - sdks/go/amqp/errdrop.go (ErrDrop sentinel semantics) + sdks/go/amqp/event.go (parseEvent AFTER verify)
    - sdks/CONTRACT.md §8 (verify → process/nack; missing signature → nack strict; security event must NOT log HMAC value)
    - 19-RESEARCH.md § Pattern 4 (aio-pika message.process(ignore_processed=True) + ack/nack; ErrDrop class) + Anti-Patterns
    - 19-PATTERNS.md § `amqp/__init__.py` (ack/nack matrix; security-log-never-includes-signature invariant)
    - 19-01-SUMMARY.md (verify_hmac signature + location)
  </read_first>
  <behavior>
    - consume() sets channel QoS prefetch (default 10), declares the queue passively, and registers an async _on_message callback with no_ack=False.
    - For each delivery: verify_hmac(signing_key, body) is called FIRST. On failure → nack(requeue=False) + a security-log warning that does NOT include the signature; handler never invoked.
    - After a successful verify, the body is parsed (hmac_signature removed) and passed to handler. Parse failure → nack(requeue=False) + security log; handler not invoked.
    - handler returns None → message.ack(). handler raises ErrDrop → nack(requeue=False). handler raises any other exception → nack(requeue=True).
    - The SDK owns ack/nack (message.process(ignore_processed=True) so aio-pika does not auto-ack).
  </behavior>
  <action>
    Implement `amqp/_consumer.py` per 19-RESEARCH.md § Pattern 4 and 19-PATTERNS.md § `amqp/__init__.py`. Define `ErrDrop(Exception)` (poison-message sentinel mirroring Go's exported `ErrDrop`). `async def consume(channel, queue_name, signing_key, handler, *, prefetch=10, logger=None)`: `await channel.set_qos(prefetch_count=prefetch)`, `await channel.declare_queue(queue_name, durable=True, passive=True)`, register `_on_message`. In `_on_message`, wrap in `async with message.process(ignore_processed=True)` so the SDK drives ack/nack. Call `verify_hmac(signing_key, message.body)` (from 19-01) FIRST; on failure `logger.warning(...)` (a security event that NEVER includes the signature bytes) then `await message.nack(requeue=False)` and return — handler never runs. After verify, parse the body (removing `hmac_signature`); on parse failure, same nack-without-requeue + security log. Then `await handler(event)`; `ErrDrop` → `nack(requeue=False)`; any other exception → `nack(requeue=True)`; success (None) → `await message.ack()`. Export `consume`/`ErrDrop` from `amqp/__init__.py`.

    Implement `tests/test_amqp_consumer.py`: use a recording fake `AbstractIncomingMessage` (a lightweight double exposing `.body`, an async `.process()` context manager, and recording `.ack`/`.nack(requeue=...)` calls) — no live broker — mirroring Go's `AckableDelivery`/`recordingDelivery` seam. Drive all five paths: (1) valid HMAC + handler None → ack; (2) invalid HMAC → nack(requeue=False), handler NOT called, security log emitted without the signature; (3) valid HMAC + handler raises ErrDrop → nack(requeue=False); (4) valid HMAC + handler raises other → nack(requeue=True); (5) valid HMAC but non-object body → nack(requeue=False). Reuse the real HMAC fixture/signing key from 19-01's `tests/fixtures/amqp_hmac_vectors.json` for the valid case.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_amqp_consumer.py -x -q` passes for all five ack/nack paths.
    - A test asserts the handler is NOT invoked when HMAC verification fails (verify-before-handler).
    - A test asserts HMAC-fail, parse-fail, and ErrDrop all produce `nack(requeue=False)`; only a plain handler exception produces `nack(requeue=True)`.
    - A test asserts the security-log message on HMAC failure contains no hex signature substring (assert the recorded log string does not contain the fixture's signature).
    - `grep -c 'requeue=False' sdks/python/src/axiam_sdk/amqp/_consumer.py` returns at least `3` (HMAC-fail, parse-fail, ErrDrop).
    - `grep -c 'verify_hmac' sdks/python/src/axiam_sdk/amqp/_consumer.py` returns at least `1` (reuses 19-01 primitive, not a re-implementation).
    - `python -c "from axiam_sdk.amqp import consume, ErrDrop"` succeeds.
    - `mypy --strict sdks/python/src/axiam_sdk/amqp` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_amqp_consumer.py -x -q</automated>
  </verify>
  <done>The async consumer verifies HMAC before the handler ever runs, enforces the exact §8 ack/nack matrix (nack-without-requeue on HMAC-fail/parse-fail/ErrDrop; requeue on transient handler errors), and never logs the signature — proven hermetically via a recording delivery double.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AMQP broker → SDK handler | Only HMAC-verified, well-formed messages may reach the consumer handler |
| HMAC failure → logs | Security event must record the failure without leaking the signature |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-16 | Tampering / Spoofing | forged/unsigned AMQP message | high | mitigate | verify-before-handler using the 19-01 byte-for-byte-proven `verify_hmac`; unverified message never reaches the handler; nack-without-requeue on failure (§8) |
| T-19-17 | Information Disclosure | HMAC value in security logs | medium | mitigate | security event logs only the fact of failure (queue/routing context), never the received/computed signature (§8.4); test asserts absence |
| T-19-18 | Denial of Service | poison-message requeue loop | medium | mitigate | HMAC-fail/parse-fail/ErrDrop all nack WITHOUT requeue; only transient handler errors requeue |
</threat_model>

<verification>
- `pytest sdks/python/tests/test_amqp_consumer.py -x -q` passes (all five paths).
- `mypy --strict sdks/python/src/axiam_sdk/amqp` passes.
- CONTRACT.md §8 conformance (verify-before-handler, nack-without-requeue, no-signature-in-log).
</verification>

<success_criteria>
- The async AMQP consumer enforces mandatory HMAC verify-before-handler and the full §8 ack/nack decision matrix, proven hermetically with a recording delivery double reusing the Wave-0 real HMAC fixture.
</success_criteria>

<output>
Create `.planning/phases/19-python-sdk/19-05-SUMMARY.md` when done
</output>
