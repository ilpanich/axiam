---
phase: 19-python-sdk
plan: 06
type: execute
wave: 4
depends_on: ["19-02"]
files_modified:
  - sdks/python/src/axiam_sdk/fastapi/__init__.py
  - sdks/python/src/axiam_sdk/django/__init__.py
  - sdks/python/src/axiam_sdk/django/middleware.py
  - sdks/python/tests/test_fastapi_dependency.py
  - sdks/python/tests/test_django_middleware.py
  - sdks/python/tests/test_middleware_tenant.py
autonomous: true
requirements: [PY-01]
user_setup: []

must_haves:
  truths:
    - "a FastAPI Depends(...) callable verifies the session locally via the JWKS verifier and returns the authenticated identity (user_id, tenant_id, roles), raising HTTPException 401 on AuthError / 403 on AuthzError (D-09)"
    - "a Django middleware class attaches request.axiam_user, declares sync_capable + async_capable, and returns standardized 401/403 JSON (D-10)"
    - "BOTH integrations reject a token whose claims[tenant_id] != configured_tenant BEFORE trusting it (cross-tenant replay defense — org-wide JWKS makes signature validity insufficient)"
    - "both integrations extract the token from Authorization: Bearer, falling back to the axiam_access cookie"
    - "importing axiam_sdk (top level) does NOT import fastapi or django (optional extras stay import-safe)"
  artifacts:
    - sdks/python/src/axiam_sdk/fastapi/__init__.py
    - sdks/python/src/axiam_sdk/django/middleware.py
    - sdks/python/tests/test_fastapi_dependency.py
    - sdks/python/tests/test_django_middleware.py
    - sdks/python/tests/test_middleware_tenant.py
  key_links:
    - "both integrations use JwksVerifier.verify() from 19-02 and independently check exp (verify() does not)"
    - "the cross-tenant claims[tenant_id]==configured_tenant check is enforced in both, before any claim is trusted"
    - "axiam_sdk.fastapi / axiam_sdk.django are separate importable submodules, never imported from the top-level __init__.py"
  prohibitions:
    - "MUST NOT import fastapi or django from the top-level axiam_sdk/__init__.py (breaks pure-REST consumers, Anti-Pattern)"
    - "MUST NOT trust any claim before the tenant_id-matches-configured-tenant check"
    - "MUST NOT include a raw token value in any 401/403 response body or log"
---

<objective>
Deliver the two first-class framework integrations (SC#4): a FastAPI `Depends(...)` dependency-injection callable (local JWKS verify → identity or `HTTPException`, D-09) and a Django middleware class (`request.axiam_user`, sync-WSGI-primary + ASGI-capable, D-10). Both enforce the mandatory cross-tenant token-replay defense (`claims[tenant_id] == configured_tenant`) that the org-wide JWKS makes non-optional, and both ship as import-safe optional extras.

Purpose: PY-01 requires FastAPI + Django helpers as first-class integrations; the cross-tenant check is a MUST-carry-forward security control (org-wide JWKS means a signature-valid token could belong to a different tenant).
Output: `fastapi/__init__.py`, `django/middleware.py`, and dependency/middleware/tenant-check tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-python-sdk/19-CONTEXT.md
@.planning/phases/19-python-sdk/19-RESEARCH.md
@.planning/phases/19-python-sdk/19-PATTERNS.md
@sdks/CONTRACT.md
@sdks/go/middleware/nethttp.go
@.planning/phases/19-python-sdk/19-02-SUMMARY.md
</context>

## Artifacts this phase produces (this plan)

New symbols/paths (drift verification MUST treat as newly-created):
- `fastapi/__init__.py`: class `AxiamUser` (`user_id`, `tenant_id`, `roles`); factory `require_authenticated_user(verifier, configured_tenant)` returning a `Depends`-compatible async callable; internal `_extract_token(request)`.
- `django/__init__.py` (package marker); `django/middleware.py`: class `AxiamAuthMiddleware` (`sync_capable = True`, `async_capable = True`, `__call__`, `__acall__`, `_authenticate`, `_extract_token`, `_build_user`); reads verifier + configured tenant from Django settings.
- Tests: `test_fastapi_dependency.py`, `test_django_middleware.py`, `test_middleware_tenant.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI Depends dependency — local verify + tenant check + identity injection (D-09, SC#4)</name>
  <files>sdks/python/src/axiam_sdk/fastapi/__init__.py, sdks/python/tests/test_fastapi_dependency.py</files>
  <read_first>
    - sdks/go/middleware/nethttp.go (extractToken Bearer→cookie order lines 109-125; tenant check lines 78-95; standardized error body lines 22-27/136-145; exp check lines 67-76)
    - 19-RESEARCH.md § Pattern 6 (FastAPI Depends factory; AxiamUser; _extract_token; tenant_id mismatch → 401)
    - 19-PATTERNS.md § `fastapi/__init__.py` (NO in-repo analog; cross-tenant check MUST carry from nethttp.go) + Shared Patterns (tenant defense, token extraction)
    - sdks/CONTRACT.md §10 (FastAPI Depends row; surface AuthError→401, AuthzError→403; identity user_id/tenant_id/roles)
    - 19-02-SUMMARY.md (JwksVerifier.verify does NOT check exp; error classes)
  </read_first>
  <behavior>
    - require_authenticated_user(verifier, configured_tenant) returns an async Depends callable that extracts the token (Authorization Bearer, else axiam_access cookie), verifies it via the JWKS verifier, checks exp, checks tenant_id == configured_tenant, and returns AxiamUser(user_id, tenant_id, roles).
    - Missing credentials → HTTPException 401. Invalid/expired token → 401. tenant_id mismatch → 401. (AuthzError, if surfaced by downstream authz, → 403.)
    - No token value appears in any HTTPException detail.
    - Importing axiam_sdk.fastapi requires fastapi installed (optional extra); importing top-level axiam_sdk does not.
  </behavior>
  <action>
    Implement `fastapi/__init__.py` per 19-RESEARCH.md § Pattern 6 and 19-PATTERNS.md § `fastapi/__init__.py`. `AxiamUser` dataclass/class with `user_id`, `tenant_id`, `roles`. `require_authenticated_user(verifier, configured_tenant)` is a FACTORY returning an async `_dependency(request)` (mirrors Go's `Middleware(verifier, configuredTenant, opts...)` factory). `_extract_token(request)` = Authorization Bearer then `axiam_access` cookie fallback (port from `nethttp.go` extractToken verbatim ordering). Verify via `verifier.verify(token)`; independently check `exp` (the verifier does not, per 19-02); enforce `claims.get("tenant_id") == configured_tenant` BEFORE trusting any claim (cross-tenant replay defense — Shared Pattern, MUST-carry). Raise `HTTPException(401)` on any auth failure with a standardized detail that never includes the token; roles from `claims.get("scope", "").split()`. This module is imported only as `axiam_sdk.fastapi` — never from the top-level `__init__.py`.

    Implement `tests/test_fastapi_dependency.py` using FastAPI's `TestClient`: a sample `@app.get('/me')` guarded by `Depends(require_authenticated_user(verifier, 'acme'))`; assert a valid same-tenant EdDSA token yields 200 + identity; missing token → 401; expired token → 401; a valid-signature token with `tenant_id='other'` → 401 (cross-tenant). Reuse the in-test Ed25519 keypair + mock JWKS pattern from 19-02's `test_jwks.py`.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_fastapi_dependency.py -x -q` passes.
    - A test asserts a signature-valid token whose `tenant_id` != configured tenant is rejected with 401 (cross-tenant replay defense).
    - A test asserts an expired token is rejected (dependency checks exp independently of the verifier).
    - `python -c "import axiam_sdk"` succeeds in an env WITHOUT fastapi installed (top-level import does not pull fastapi); `grep -rc 'import fastapi\|from fastapi' sdks/python/src/axiam_sdk/__init__.py` returns `0`.
    - No token value appears in any raised HTTPException detail (test asserts the detail string does not contain the token).
    - `mypy --strict sdks/python/src/axiam_sdk/fastapi` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pip install -e 'sdks/python[fastapi,dev]' >/dev/null 2>&1; pytest sdks/python/tests/test_fastapi_dependency.py -x -q</automated>
  </verify>
  <done>The FastAPI dependency verifies tokens locally, enforces exp + the cross-tenant tenant_id check, injects AxiamUser, and stays import-safe as an optional extra.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Django middleware — sync+async dual dispatch + tenant check + request.axiam_user (D-10, SC#4)</name>
  <files>sdks/python/src/axiam_sdk/django/__init__.py, sdks/python/src/axiam_sdk/django/middleware.py, sdks/python/tests/test_django_middleware.py</files>
  <read_first>
    - sdks/go/middleware/nethttp.go (extract/verify/tenant-check/inject/error-response structure; standardized JSON body)
    - 19-RESEARCH.md § Pattern 7 (sync_capable/async_capable; markcoroutinefunction; iscoroutinefunction dispatch; settings-configured verifier)
    - 19-PATTERNS.md § `django/middleware.py` (extractToken port; standardized error body) + Shared Patterns (tenant defense)
    - sdks/CONTRACT.md §10 (Django MIDDLEWARE row; 401/403 standardized JSON)
    - 19-02-SUMMARY.md (JwksVerifier.verify, error classes)
  </read_first>
  <behavior>
    - AxiamAuthMiddleware declares sync_capable = True and async_capable = True; at __init__ it marks itself a coroutine function iff get_response is a coroutine (markcoroutinefunction).
    - Under WSGI (sync get_response) __call__ runs the sync path; under ASGI (async get_response) it runs __acall__.
    - _authenticate extracts the token (Bearer → axiam_access cookie), verifies via the settings-configured JWKS verifier, checks exp, checks tenant_id == configured tenant, and on success attaches request.axiam_user; on failure returns a JsonResponse 401 with body {"error":"authentication_failed","message":...}.
    - No token value appears in any response body.
  </behavior>
  <action>
    Implement `django/middleware.py` per 19-RESEARCH.md § Pattern 7 and 19-PATTERNS.md § `django/middleware.py`. `AxiamAuthMiddleware` with `sync_capable = True`/`async_capable = True`; `__init__(get_response)` calls `markcoroutinefunction(self)` when `iscoroutinefunction(get_response)`. `__call__` dispatches to `_sync_call` or `__acall__` based on `iscoroutinefunction(get_response)`. `_authenticate(request)` ports the Go extract→verify→tenant-check→inject→error flow: extract token (Authorization Bearer, fallback `request.COOKIES.get("axiam_access")`), verify via a verifier resolved from Django settings (e.g. `AXIAM_JWKS_BASE_URL` + `AXIAM_TENANT_SLUG`; exact keys are Claude's Discretion), independently check `exp`, enforce `claims["tenant_id"] == configured_tenant` BEFORE trusting claims (cross-tenant defense), set `request.axiam_user = _build_user(claims)`, and return `None` on success or a standardized `JsonResponse({"error":"authentication_failed","message":...}, status=401)` on failure — never including the token. Add `django/__init__.py` marker. Imported only as `axiam_sdk.django.middleware`, never from the top-level `__init__.py`.

    Implement `tests/test_django_middleware.py`: configure minimal Django settings in-test; instantiate the middleware with a sync `get_response` fake and an async fake; assert a valid same-tenant token attaches `request.axiam_user` and calls through; missing token → 401 JSON; a cross-tenant token → 401; verify both the sync and async dispatch paths.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_django_middleware.py -x -q` passes for both sync and async dispatch.
    - `grep -c 'sync_capable = True' sdks/python/src/axiam_sdk/django/middleware.py` returns `1` and `grep -c 'async_capable = True' ...` returns `1`.
    - `grep -c 'markcoroutinefunction' sdks/python/src/axiam_sdk/django/middleware.py` returns at least `1`.
    - A test asserts a signature-valid cross-tenant token → 401 (tenant defense).
    - A test asserts `request.axiam_user` is populated with user_id/tenant_id/roles on success.
    - `python -c "import axiam_sdk"` succeeds without django installed; `grep -rc 'import django\|from django' sdks/python/src/axiam_sdk/__init__.py` returns `0`.
    - `mypy --strict sdks/python/src/axiam_sdk/django` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pip install -e 'sdks/python[django,dev]' >/dev/null 2>&1; pytest sdks/python/tests/test_django_middleware.py -x -q</automated>
  </verify>
  <done>The Django middleware supports sync-WSGI + ASGI dispatch, verifies tokens locally with the exp + cross-tenant checks, attaches request.axiam_user, returns standardized 401/403 JSON, and stays import-safe as an optional extra.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared cross-tenant replay-defense regression test (both integrations)</name>
  <files>sdks/python/tests/test_middleware_tenant.py</files>
  <read_first>
    - 19-PATTERNS.md § Shared Patterns (Cross-tenant replay defense — MUST-carry-forward from nethttp.go lines 78-95)
    - 19-RESEARCH.md § Security Domain (cross-tenant token replay; org-wide JWKS)
    - Task 1 + Task 2 outputs (require_authenticated_user, AxiamAuthMiddleware)
  </read_first>
  <behavior>
    - A token that is signature-valid and non-expired but carries tenant_id != configured tenant is rejected by BOTH the FastAPI dependency and the Django middleware.
    - The same token WITH the matching tenant_id is accepted by both (proves the test is non-vacuous — rejection is specifically due to tenant mismatch, not a broken token).
  </behavior>
  <action>
    Implement `tests/test_middleware_tenant.py` as a focused, shared regression test parametrized across both integrations: mint two otherwise-identical valid EdDSA tokens differing ONLY in the `tenant_id` claim (one matching the configured tenant, one not). Assert the FastAPI dependency and the Django middleware BOTH accept the matching-tenant token and BOTH reject the mismatched-tenant token — proving the org-wide-JWKS cross-tenant defense holds in both surfaces and the check is non-vacuous.
  </action>
  <acceptance_criteria>
    - `pytest sdks/python/tests/test_middleware_tenant.py -x -q` passes.
    - The test proves BOTH the matching-tenant token is accepted AND the mismatched-tenant token is rejected, in BOTH integrations (non-vacuous).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && pytest sdks/python/tests/test_middleware_tenant.py -x -q</automated>
  </verify>
  <done>A shared, non-vacuous regression proves the cross-tenant replay defense holds identically in the FastAPI dependency and the Django middleware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound HTTP request → resource server | Bearer/cookie token crosses here; must be locally verified before trust |
| org-wide JWKS → per-tenant resource | signature validity alone does not prove tenant authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-19 | Spoofing / Elevation of Privilege | cross-tenant token replay | high | mitigate | `claims[tenant_id] == configured_tenant` enforced BEFORE trusting claims, in BOTH FastAPI + Django; non-vacuous shared regression test (org-wide JWKS makes signature validity insufficient) |
| T-19-20 | Spoofing | expired-but-valid-signature token accepted | high | mitigate | both integrations independently check `exp` (the JWKS verifier does not) before injecting identity |
| T-19-21 | Information Disclosure | token in 401/403 body/logs | medium | mitigate | standardized error bodies never include the token value; roles derived from scope claim only |
| T-19-22 | Denial of Service / breakage | optional-extra import breaks pure consumers | low | accept-then-mitigate | fastapi/django imported only as submodules, never from top-level `__init__.py`; verified by grep |
</threat_model>

<verification>
- `pytest sdks/python/tests/test_fastapi_dependency.py sdks/python/tests/test_django_middleware.py sdks/python/tests/test_middleware_tenant.py -x -q` pass.
- `python -c "import axiam_sdk"` succeeds without fastapi/django installed (top-level import-safe).
- `mypy --strict sdks/python/src/axiam_sdk/fastapi sdks/python/src/axiam_sdk/django` passes.
- CONTRACT.md §10 conformance (FastAPI Depends + Django MIDDLEWARE; 401/403; identity injection).
</verification>

<success_criteria>
- FastAPI dependency + Django middleware both verify tokens locally, enforce exp + the cross-tenant tenant_id check, inject identity, return standardized 401/403, and stay import-safe as optional extras (SC#4).
</success_criteria>

<output>
Create `.planning/phases/19-python-sdk/19-06-SUMMARY.md` when done
</output>
