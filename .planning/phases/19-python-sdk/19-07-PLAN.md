---
phase: 19-python-sdk
plan: 07
type: execute
wave: 5
depends_on: ["19-03", "19-04", "19-05", "19-06"]
files_modified:
  - sdks/python/examples/login_mfa.py
  - sdks/python/examples/rest_authz.py
  - sdks/python/examples/grpc_checkaccess.py
  - sdks/python/examples/amqp_consumer.py
  - sdks/python/examples/fastapi_dependency.py
  - sdks/python/examples/django_middleware.py
  - sdks/python/README.md
  - .github/workflows/sdk-ci-python.yml
autonomous: true
requirements: [PY-01]
user_setup:
  - service: PyPI
    why: "Tag-triggered PyPI publish of axiam-sdk via Trusted Publishing (OIDC)"
    dashboard_config:
      - task: "Register the GitHub Actions Trusted Publisher (OIDC) for the axiam-sdk PyPI project (repo + workflow + environment) so the publish job needs no stored API token"
        location: "PyPI -> axiam-sdk project -> Publishing -> Add a new pending/trusted publisher"

must_haves:
  truths:
    - "six runnable example scripts exist: login+MFA, REST authz, gRPC, AMQP consumer, FastAPI dependency, Django middleware (D-13); each imports only the public SDK surface"
    - "the Python SDK CI workflow runs pytest across Python 3.10/3.11/3.12/3.13 on ubuntu-latest (D-18)"
    - "CI includes a verify=False grep gate (SC#3), a gRPC-stub drift-check (D-04), mypy --strict + ruff (D-20), and python -m build + twine check (SC#5)"
    - "CI publishes axiam-sdk to PyPI via Trusted Publishing on a sdks/python/vX.Y.Z tag; the publish job never runs on a PR (D-05)"
    - "the README states 'This SDK conforms to CONTRACT.md §1–§10.'"
  artifacts:
    - sdks/python/examples/login_mfa.py
    - sdks/python/examples/rest_authz.py
    - sdks/python/examples/grpc_checkaccess.py
    - sdks/python/examples/amqp_consumer.py
    - sdks/python/examples/fastapi_dependency.py
    - sdks/python/examples/django_middleware.py
    - sdks/python/README.md
    - .github/workflows/sdk-ci-python.yml
  key_links:
    - "the CI drift-check regenerates via sdks/python/scripts/gen_grpc.sh (19-01) + git diff --exit-code (no buf CLI)"
    - "examples import only public entry points (axiam_sdk, axiam_sdk.grpc, axiam_sdk.amqp, axiam_sdk.fastapi, axiam_sdk.django)"
    - "the verify=False gate greps src + examples + tests (extended to ssl._create_unverified_context and verify=0)"
    - "the publish job triggers only on refs/tags/sdks/python/v* (Phase 15 D-13 tag convention)"
  prohibitions:
    - "MUST NOT put verify=False in any example (SC#3 gate covers examples)"
    - "MUST NOT trigger the publish job from a pull_request event (no secret/OIDC exposure on PRs)"
    - "MUST NOT rely on the buf CLI in CI (absent — use grpc_tools.protoc via gen_grpc.sh, Pitfall 5)"
    - "examples MUST NOT import underscore-prefixed internal modules (_client/_session/etc.)"
---

<objective>
Complete the phase deliverables: six runnable per-capability example scripts (login+MFA, REST authz, gRPC, AMQP consumer, FastAPI dependency, Django middleware — D-13, SC#4 is a subset), a README conformance statement, and the full Python SDK CI workflow — matrix 3.10–3.13, pytest, the `verify=False` grep gate (SC#3), gRPC-stub drift-check (D-04), `mypy --strict`+`ruff` (D-20), `python -m build`+`twine check` (SC#5), and tag-triggered PyPI Trusted-Publishing (D-05).

Purpose: This closes SC#3, SC#4 (demonstrated), SC#5, and the D-13/D-18/D-20 CI+examples requirements, and wires the drift/leak/TLS gates that protect every prior wave's guarantees on every future change.
Output: `examples/*.py`, `README.md`, and `.github/workflows/sdk-ci-python.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-python-sdk/19-CONTEXT.md
@.planning/phases/19-python-sdk/19-RESEARCH.md
@.planning/phases/19-python-sdk/19-PATTERNS.md
@sdks/CONTRACT.md
@.github/workflows/sdk-ci-go.yml
@.github/workflows/sdk-ci-python.yml
@sdks/go/examples/login-mfa/main.go
@sdks/go/examples/authz-check/main.go
@sdks/go/examples/grpc-checkaccess/main.go
@sdks/go/examples/amqp-consumer/main.go
@.planning/phases/19-python-sdk/19-03-SUMMARY.md
@.planning/phases/19-python-sdk/19-04-SUMMARY.md
@.planning/phases/19-python-sdk/19-05-SUMMARY.md
@.planning/phases/19-python-sdk/19-06-SUMMARY.md
</context>

## Artifacts this phase produces (this plan)

New symbols/paths (drift verification MUST treat as newly-created):
- `examples/login_mfa.py`, `examples/rest_authz.py`, `examples/grpc_checkaccess.py`, `examples/amqp_consumer.py`, `examples/fastapi_dependency.py`, `examples/django_middleware.py`.
- `README.md` (rewritten: install, quickstart per transport, `This SDK conforms to CONTRACT.md §1–§10.`).
- `.github/workflows/sdk-ci-python.yml` jobs: `scaffold-check`, `test` (matrix 3.10–3.13), `tls-bypass-gate`, `grpc-drift-check`, `lint` (mypy+ruff), `build-check` (build+twine), `publish` (tag-triggered PyPI Trusted Publishing).

<tasks>

<task type="auto">
  <name>Task 1: Six runnable per-capability example scripts (D-13, public-entry-points only)</name>
  <files>sdks/python/examples/login_mfa.py, sdks/python/examples/rest_authz.py, sdks/python/examples/grpc_checkaccess.py, sdks/python/examples/amqp_consumer.py, sdks/python/examples/fastapi_dependency.py, sdks/python/examples/django_middleware.py</files>
  <read_first>
    - sdks/go/examples/login-mfa/main.go, authz-check/main.go, grpc-checkaccess/main.go, amqp-consumer/main.go (call sequence/output-shape to port narratively)
    - 19-PATTERNS.md § `sdks/python/examples/*.py` (1:1 Go analog map; import public entry points only)
    - 19-RESEARCH.md § Pattern 6 (FastAPI example) + Pattern 7 (Django example)
    - 19-03/04/05/06-SUMMARY.md (final public API surfaces: AxiamClient methods, grpc clients, amqp.consume/ErrDrop, fastapi require_authenticated_user, django AxiamAuthMiddleware)
  </read_first>
  <action>
    Write six runnable example scripts, each importing ONLY the public SDK surface (`axiam_sdk`, `axiam_sdk.grpc`, `axiam_sdk.amqp`, `axiam_sdk.fastapi`, `axiam_sdk.django.middleware`) — never underscore-prefixed internal modules — porting the narrative structure (construct client → call → print result → handle typed errors) from the Go analogs:
    - `login_mfa.py`: `AxiamClient(base_url, tenant_slug=..., org_slug=...)`, `login(email, password)`, branch on `mfa_required`, `verify_mfa(mfa_token, code)`; catch `AuthError`.
    - `rest_authz.py`: `check_access`/`can`/`batch_check` over the REST FND-04 endpoint; catch `AuthzError`.
    - `grpc_checkaccess.py`: both `AuthzGrpcClient` (sync) and `AsyncAuthzGrpcClient` (async) `check_access`/`batch_check`.
    - `amqp_consumer.py`: `axiam_sdk.amqp.consume(...)` with a handler and the `ErrDrop` sentinel demonstrated.
    - `fastapi_dependency.py`: a minimal FastAPI app with a route guarded by `Depends(require_authenticated_user(verifier, tenant))`.
    - `django_middleware.py`: a minimal Django settings/urls snippet registering `axiam_sdk.django.middleware.AxiamAuthMiddleware` and reading `request.axiam_user`.
    All examples hardcode `verify=True` behavior (never `verify=False`) and use placeholder connection details.
  </action>
  <acceptance_criteria>
    - All six files exist under `sdks/python/examples/`.
    - Each example byte-compiles: `python -m py_compile sdks/python/examples/*.py` exits 0.
    - `grep -rlE '(^|[^.])_client|_session|axiam_sdk\._' sdks/python/examples` returns no file (no internal-module imports).
    - `grep -rEc 'verify\s*=\s*(False|0)' sdks/python/examples` returns `0` (SC#3 covers examples).
    - Each example imports from a public entry point (`axiam_sdk`, `axiam_sdk.grpc`, `axiam_sdk.amqp`, `axiam_sdk.fastapi`, or `axiam_sdk.django.middleware`).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && python -m py_compile sdks/python/examples/*.py && ! grep -rEn 'verify\s*=\s*(False|0)' sdks/python/examples</automated>
  </verify>
  <done>Six runnable examples cover every transport + both framework integrations, import only the public surface, and contain no TLS-bypass idiom.</done>
</task>

<task type="auto">
  <name>Task 2: README conformance + quickstart</name>
  <files>sdks/python/README.md</files>
  <read_first>
    - sdks/python/README.md (existing scaffold README)
    - sdks/CONTRACT.md § Closing Notes (Conformance Statement — required verbatim line)
    - 19-CONTEXT.md § Established Patterns (CONTRACT.md conformance is a required acceptance item)
  </read_first>
  <action>
    Rewrite `sdks/python/README.md`: install (`pip install axiam-sdk`, extras `axiam-sdk[fastapi]`/`axiam-sdk[django]`), a short quickstart for each transport (sync + async login, REST authz, gRPC, AMQP consumer, FastAPI, Django) pointing at the `examples/`, the `>=3.10` requirement, and the required verbatim conformance line: `This SDK conforms to CONTRACT.md §1–§10.` Note the codegen path (committed stubs via `scripts/gen_grpc.sh`, D-04) and the strict-TLS policy.
  </action>
  <acceptance_criteria>
    - `grep -c 'This SDK conforms to CONTRACT.md §1–§10.' sdks/python/README.md` returns `1` (required conformance statement).
    - README references `pip install axiam-sdk` and the `[fastapi]`/`[django]` extras.
    - README references the `examples/` directory.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && grep -q 'This SDK conforms to CONTRACT.md §1–§10.' sdks/python/README.md && grep -q 'axiam-sdk\[fastapi\]' sdks/python/README.md</automated>
  </verify>
  <done>README documents install/quickstart per transport and carries the required CONTRACT.md §1–§10 conformance statement.</done>
</task>

<task type="auto">
  <name>Task 3: Full Python SDK CI workflow — matrix, gates, drift-check, publish (D-18/SC#3/D-04/D-20/SC#5/D-05)</name>
  <files>.github/workflows/sdk-ci-python.yml</files>
  <read_first>
    - .github/workflows/sdk-ci-go.yml (job structure: scaffold-check, test, tls-bypass-gate, buf-drift-check, publish; tag-trigger condition; SHA-pinned actions)
    - .github/workflows/sdk-ci-python.yml (current placeholder to replace)
    - 19-PATTERNS.md § `.github/workflows/sdk-ci-python.yml` (job-by-job port; grep pattern; grpc_tools.protoc drift-check; PyPI Trusted Publishing)
    - 19-RESEARCH.md § Code Examples (verify=False grep gate script) + Pitfall 5 (no buf CLI — use grpc_tools.protoc) + Don't Hand-Roll (pypa/gh-action-pypi-publish, id-token: write)
    - 19-01-SUMMARY.md (scripts/gen_grpc.sh drift-check command)
  </read_first>
  <action>
    Rewrite `.github/workflows/sdk-ci-python.yml` porting the Go workflow job structure (keep SHA-pinned action refs consistent with the Go workflow's `actions/checkout` pin), triggered on `pull_request` (paths `sdks/python/**`, `sdks/openapi.json`, `sdks/buf.yaml`, `sdks/buf.gen.yaml`) and `push` tags `sdks/python/v*`. Jobs:
    - `scaffold-check`: `test -f sdks/python/LICENSE` (keep).
    - `test`: matrix `python-version: ['3.10','3.11','3.12','3.13']` on `ubuntu-latest` (D-18); `pip install -e 'sdks/python[dev,fastapi,django]'`; `pytest sdks/python/tests -v`; `python -m py_compile sdks/python/examples/*.py`.
    - `tls-bypass-gate` (SC#3): grep `sdks/python/src sdks/python/examples sdks/python/tests` for `verify=False`, extended to `ssl._create_unverified_context` and `verify=0`; fail if any match (use the ready script from 19-RESEARCH.md § Code Examples).
    - `grpc-drift-check` (D-04): install `grpcio-tools`, run `bash sdks/python/scripts/gen_grpc.sh`, then `git diff --exit-code sdks/python/src/axiam_sdk/grpc/gen` — standardize on `grpc_tools.protoc` (Pitfall 5: buf CLI absent).
    - `lint` (D-20): `mypy --strict sdks/python/src` + `ruff check sdks/python` + `ruff format --check sdks/python`.
    - `build-check` (SC#5, PR gate): `cd sdks/python && python -m build && twine check dist/*`.
    - `publish` (D-05): `if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/sdks/python/v')`; re-run the drift-check + build + `twine check`, then `pypa/gh-action-pypi-publish` with `permissions: id-token: write` (Trusted Publishing, no stored token). This job never runs on a PR.
    Keep `permissions: contents: read` at the top; the publish job adds `id-token: write`.
  </action>
  <acceptance_criteria>
    - The workflow YAML parses (validate with `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/sdk-ci-python.yml'))"`).
    - `grep -c "python-version: \['3.10', '3.11', '3.12', '3.13'\]" .github/workflows/sdk-ci-python.yml` returns `1` (or an equivalent 4-version matrix list — assert all four of 3.10/3.11/3.12/3.13 appear: `grep -c "'3.10'" ... && '3.11' && '3.12' && '3.13'`).
    - `grep -c 'verify=False' .github/workflows/sdk-ci-python.yml` returns at least `1` (the gate pattern is present).
    - `grep -c 'gen_grpc.sh' .github/workflows/sdk-ci-python.yml` returns at least `1` and `grep -c 'git diff --exit-code' ...` returns at least `1` (drift-check wired).
    - `grep -c 'gh-action-pypi-publish' .github/workflows/sdk-ci-python.yml` returns at least `1` and `grep -c 'id-token: write' ...` returns at least `1` (Trusted Publishing).
    - `grep -c "refs/tags/sdks/python/v" .github/workflows/sdk-ci-python.yml` returns at least `1` (correct tag trigger) and the publish job is guarded by `github.event_name == 'push'`.
    - `grep -c 'buf generate' .github/workflows/sdk-ci-python.yml` returns `0` (no buf CLI dependency — Pitfall 5).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/user/axiam && python -c "import yaml; yaml.safe_load(open('.github/workflows/sdk-ci-python.yml'))" && grep -q 'gh-action-pypi-publish' .github/workflows/sdk-ci-python.yml && grep -q 'gen_grpc.sh' .github/workflows/sdk-ci-python.yml && ! grep -q 'buf generate' .github/workflows/sdk-ci-python.yml</automated>
  </verify>
  <done>The Python SDK CI runs the full matrix + all gates (TLS-bypass, gRPC drift, mypy/ruff, build/twine) on PRs and publishes to PyPI via Trusted Publishing only on a sdks/python/vX.Y.Z tag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR change → committed stubs/TLS policy | CI gates must catch drift and TLS-bypass reintroduction on every change |
| release tag → PyPI | publish credentials/OIDC must never be exposed to PR-triggered jobs |
| example scripts → SDK public API | examples must exercise only supported entry points |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-23 | Tampering / Spoofing | TLS-bypass reintroduced in a future change | high | mitigate | `verify=False`/`_create_unverified_context`/`verify=0` grep gate over src+examples+tests on every PR (SC#3) |
| T-19-24 | Tampering | proto/stub drift | medium | mitigate | `gen_grpc.sh` regenerate + `git diff --exit-code` drift-check job (D-04); re-run before publish |
| T-19-25 | Elevation of Privilege | publish secret exposed to a PR | high | mitigate | publish job gated `github.event_name == 'push' && refs/tags/sdks/python/v*`; PyPI Trusted Publishing (OIDC `id-token: write`), no stored long-lived token (D-05) |
| T-19-SC | Tampering | pip installs in CI (grpcio-tools/build/twine/etc.) | high | mitigate | packages classified OK in 19-RESEARCH.md Package Legitimacy Audit; SHA-pinned GitHub Actions carried from the Go workflow; no `[ASSUMED]`/`[SUS]` remain |
</threat_model>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/sdk-ci-python.yml'))"` parses.
- `python -m py_compile sdks/python/examples/*.py` succeeds; `! grep -rEn 'verify\s*=\s*(False|0)' sdks/python/examples`.
- README carries the CONTRACT.md §1–§10 conformance line.
- Full suite (Wave 5 merge gate): `pytest sdks/python/tests -v` + `mypy --strict sdks/python/src` + `ruff check sdks/python` + the verify=False gate + `bash sdks/python/scripts/gen_grpc.sh && git diff --exit-code sdks/python/src/axiam_sdk/grpc/gen` + `cd sdks/python && python -m build && twine check dist/*`.
</verification>

<success_criteria>
- Six runnable examples cover every capability; the README carries the §1–§10 conformance statement; CI runs the 3.10–3.13 matrix with the TLS-bypass gate (SC#3), gRPC drift-check (D-04), mypy/ruff (D-20), build/twine (SC#5), and tag-triggered PyPI Trusted Publishing (D-05).
</success_criteria>

<output>
Create `.planning/phases/19-python-sdk/19-07-SUMMARY.md` when done
</output>