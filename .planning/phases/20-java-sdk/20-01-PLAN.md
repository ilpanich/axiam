---
phase: 20-java-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdks/java/pom.xml
  - sdks/java/.gitignore
  - sdks/java/src/main/java/io/axiam/sdk/package-info.java
  - sdks/java/scripts/tls-bypass-gate.sh
  - sdks/buf.gen.yaml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [JAVA-01]
must_haves:
  truths:
    - "`mvn -f sdks/java/pom.xml generate-sources compile` produces the gRPC stub classes into target/generated-sources (gitignored) and the tree compiles on Java 21"
    - "`io.axiam:axiam-sdk` POM declares maven.compiler.release=21 and the full JAVA-01-pinned dependency set"
    - "`sdks/java/scripts/tls-bypass-gate.sh` exits 0 (no TLS-bypass idioms) over sdks/java/src + sdks/java/examples"
    - "REQUIREMENTS.md JAVA-01 lists both io.axiam:axiam-sdk and io.axiam:axiam-bom (D-23 reconciled)"
  artifacts:
    - sdks/java/pom.xml
    - sdks/java/.gitignore
    - sdks/java/src/main/java/io/axiam/sdk/package-info.java
    - sdks/java/scripts/tls-bypass-gate.sh
  key_links:
    - "protobuf-maven-plugin <protoSourceRoot> -> ../../proto (single source of truth, no .proto duplication)"
    - "maven-gpg-plugin <skip>${gpg.skip}</skip> with gpg.skip=true default -> flipped only in CI (20-09)"
    - "maven-jar-plugin Automatic-Module-Name: io.axiam.sdk (D-24)"
---

<objective>
Land the Wave-1 foundation for the Java SDK: rewrite the stale Java-11 scaffold POM to a Java 21 build with the complete JAVA-01-pinned dependency set and the GPG-signed Sonatype Central Portal plugin chain; wire `protobuf-maven-plugin` + `os-maven-plugin` as the gRPC codegen path (buf CLI is absent in this environment) generating into the gitignored `target/generated-sources`; demote `sdks/buf.gen.yaml`'s Java entries to drift-check-only (Pitfall 1); anchor the package tree with a `@NullMarked` `package-info.java` (D-05); ship the extended TLS-bypass CI grep gate script (SC#4); and reconcile REQUIREMENTS.md JAVA-01 to list the `io.axiam:axiam-bom` coordinate (D-23, Deferred — do not lose).

Purpose: Every downstream plan compiles/tests against this POM; nothing can run `mvn test` until it exists.
Output: A buildable `sdks/java/` skeleton, the codegen pipeline, the TLS gate, and the reconciled requirement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@sdks/CONTRACT.md
@.planning/phases/20-java-sdk/20-CONTEXT.md
@.planning/phases/20-java-sdk/20-RESEARCH.md
@.planning/phases/20-java-sdk/20-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols/files created by this plan:
- `sdks/java/pom.xml` — rewritten: `groupId=io.axiam`, `artifactId=axiam-sdk`, `version=0.1.0`, `maven.compiler.release=21`, property `gpg.skip=true`, property `protobuf.version`; dependencies: okhttp 4.12.0, grpc-netty-shaded 1.82.0, grpc-protobuf 1.82.0, grpc-stub 1.82.0, tomcat annotations-api (provided), amqp-client 5.22.0, nimbus-jose-jwt 10.7, tink, jackson-databind 2.22.0 (+ jackson-datatype-jsr310), jspecify 1.0.0, slf4j-api, spring-boot-starter-security (provided/optional), jakarta.servlet-api (provided), junit-jupiter 5.14.4 (test), okhttp3:mockwebserver 4.12.0 (test); build: os-maven-plugin extension, protobuf-maven-plugin (protoSourceRoot=../../proto), maven-jar-plugin (Automatic-Module-Name: io.axiam.sdk), maven-source-plugin, maven-javadoc-plugin, maven-gpg-plugin (skip=${gpg.skip}), central-publishing-maven-plugin.
- `sdks/java/.gitignore` — ignores `target/`.
- `sdks/java/src/main/java/io/axiam/sdk/package-info.java` — `@org.jspecify.annotations.NullMarked` root package anchor.
- `sdks/java/scripts/tls-bypass-gate.sh` — extended TLS-bypass grep gate over `sdks/java/src` + `sdks/java/examples`.
- `sdks/buf.gen.yaml` — Java plugin entries demoted to a documentation/drift-check-only comment block.
- `.planning/REQUIREMENTS.md` — JAVA-01 acceptance updated to list `io.axiam:axiam-sdk` + `io.axiam:axiam-bom`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite pom.xml to Java 21 with full dependency + publish plugin chain (D-01/D-20/D-22/D-24)</name>
  <files>sdks/java/pom.xml, sdks/java/.gitignore, sdks/java/src/main/java/io/axiam/sdk/package-info.java</files>
  <read_first>
    - sdks/java/pom.xml (existing stale Java-11 scaffold: groupId io.axiam, artifactId axiam-sdk, empty <dependencies/>, licenses/scm already present — PRESERVE these)
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Code Examples "Complete pom.xml skeleton" (lines ~1325-1566) — the authoritative POM to transcribe, and § Standard Stack (verified versions + [ASSUMED] provenance notes)
    - .planning/phases/20-java-sdk/20-CONTEXT.md D-01, D-20, D-22, D-24
    - sdks/CONTRACT.md closing notes (Java stays on buf/native codegen; C# is the only Grpc.Tools exception)
  </read_first>
  <action>
    Replace `sdks/java/pom.xml` with the RESEARCH § "Complete pom.xml skeleton". Set `<version>0.1.0</version>`, `<maven.compiler.release>21</maven.compiler.release>`, `<gpg.skip>true</gpg.skip>`. Preserve the existing `<licenses>` (Apache-2.0), `<scm>`, `<url>`. Add `<developers>` (Sonatype Central requires it, D-22). Populate `<dependencies>` exactly per Standard Stack: okhttp 4.12.0; grpc-netty-shaded/grpc-protobuf/grpc-stub 1.82.0; org.apache.tomcat:annotations-api 6.0.53 (scope provided, for generated-stub annotations on Java 9+); amqp-client 5.22.0; nimbus-jose-jwt 10.7; com.google.crypto.tink:tink; jackson-databind 2.22.0 plus jackson-datatype-jsr310; jspecify 1.0.0; slf4j-api 2.x; spring-boot-starter-security (scope provided + <optional>true</optional>, D-14) and jakarta.servlet-api (scope provided); test scope junit-jupiter 5.14.4 and com.squareup.okhttp3:mockwebserver 4.12.0 (the OkHttp-4.x `okhttp3.mockwebserver` module — NOT `mockwebserver3`, Pitfall 3). Build section: os-maven-plugin extension; maven-jar-plugin with manifest entry `Automatic-Module-Name: io.axiam.sdk` (D-24); maven-source-plugin (jar-no-fork) and maven-javadoc-plugin (jar) attached; maven-gpg-plugin sign@verify with `<skip>${gpg.skip}</skip>` + loopback pinentry; central-publishing-maven-plugin with publishingServerId=central, autoPublish=false. Per Assumptions A1/A2/A6: after writing, run `mvn -f sdks/java/pom.xml dependency:tree` and align `protobuf.version` to the exact protobuf-java version grpc-netty-shaded 1.82.0 pulls transitively (do not trust the placeholder). Create `sdks/java/.gitignore` containing `target/`. Create `sdks/java/src/main/java/io/axiam/sdk/package-info.java` annotated `@org.jspecify.annotations.NullMarked` (D-05 package default) so the whole SDK is null-marked from the root.
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml -q -o help:effective-pom -Dexpression=maven.compiler.release >/dev/null 2>&1 || mvn -f sdks/java/pom.xml validate</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f sdks/java/pom.xml validate` succeeds and `grep -c 'maven.compiler.release>21' sdks/java/pom.xml` returns 1
    - `mvn -f sdks/java/pom.xml dependency:resolve` resolves okhttp 4.12.0, grpc-netty-shaded 1.82.0, amqp-client 5.22.0, nimbus-jose-jwt 10.7 (assert via `mvn dependency:tree` output containing each coordinate)
    - `sdks/java/.gitignore` contains `target/`; `package-info.java` references `NullMarked`
  </acceptance_criteria>
  <done>POM builds on Java 21 with all JAVA-01-pinned deps resolvable; target/ gitignored; root package null-marked.</done>
</task>

<task type="auto">
  <name>Task 2: Wire protobuf-maven-plugin codegen from shared proto + demote buf.gen.yaml Java entries (D-21, Pitfall 1)</name>
  <files>sdks/java/pom.xml, sdks/buf.gen.yaml</files>
  <read_first>
    - proto/axiam/v1/authorization.proto (CheckAccess/BatchCheckAccess wire shapes the stubs cover), user.proto, token.proto
    - sdks/buf.gen.yaml (current Java entries `out: java/src/main/java` — conflicts with D-21's gitignored-output model)
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Common Pitfalls "Pitfall 1" + § Standard Stack (protobuf-maven-plugin 0.6.1 / os-maven-plugin 1.7.1 rationale) + Environment Availability (buf CLI absent)
    - .planning/phases/20-java-sdk/20-CONTEXT.md D-21
  </read_first>
  <action>
    In `sdks/java/pom.xml`, confirm the `protobuf-maven-plugin` execution binds `compile` + `compile-custom` goals to `generate-sources`, with `<protoSourceRoot>${project.basedir}/../../proto</protoSourceRoot>` (points at the repo-canonical `proto/axiam/v1/*.proto` — one source of truth, no file duplication into sdks/java), `<protocArtifact>com.google.protobuf:protoc:${protobuf.version}:exe:${os.detected.classifier}</protocArtifact>`, `<pluginId>grpc-java</pluginId>`, `<pluginArtifact>io.grpc:protoc-gen-grpc-java:1.82.0:exe:${os.detected.classifier}</pluginArtifact>`. Generated sources land in the default `target/generated-sources/protobuf` (gitignored per Task 1). Compiled stub classes are bundled into the jar (never distributed as source). In `sdks/buf.gen.yaml`, demote the two Java plugin entries (`buf.build/protocolbuffers/java`, `buf.build/grpc/java`) to a comment block explaining they are documentation/optional-drift-check-only and that the authoritative Java codegen is `mvn generate-sources` via protobuf-maven-plugin (Pitfall 1) — do NOT leave an active `out: java/src/main/java` entry that would write generated files into the committed source tree.
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml generate-sources -q && test -d sdks/java/target/generated-sources</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f sdks/java/pom.xml generate-sources compile` generates `AuthorizationServiceGrpc` + message classes under `sdks/java/target/generated-sources/` and compiles them
    - `git status --porcelain sdks/java/src/main/java` shows NO generated `.java` files appearing in the committed source tree
    - `sdks/buf.gen.yaml` contains no active `out: java/src/main/java` entry (grep for `out: java/src/main/java` returns 0 matches outside a comment)
  </acceptance_criteria>
  <done>gRPC stubs generate-on-build into gitignored target/; buf.gen.yaml Java entries are drift-check-only; no stubs in git.</done>
</task>

<task type="auto">
  <name>Task 3: TLS-bypass grep gate script + REQUIREMENTS.md BOM reconciliation (SC#4, D-23)</name>
  <files>sdks/java/scripts/tls-bypass-gate.sh, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Code Examples "TLS-bypass CI grep gate" (lines ~1586-1607) + § Security Domain (TLS-bypass STRIDE row)
    - .planning/research/PITFALLS.md (Java "TrustManager that accepts all certs" finding) if present
    - .planning/REQUIREMENTS.md § JAVA-01 (acceptance criteria — the "Maven Central publish (io.axiam:axiam-sdk)" line to reconcile)
    - .planning/phases/20-java-sdk/20-CONTEXT.md D-23 + Deferred Ideas ("JAVA-01 ↔ BOM coordinate reconciliation — do not lose")
  </read_first>
  <action>
    Create `sdks/java/scripts/tls-bypass-gate.sh` (executable, `set -euo pipefail`) implementing the RESEARCH extended-pattern grep gate: it scans `sdks/java/src` and `sdks/java/examples` (NOT scripts/ or the gate itself) for the TLS-bypass idiom class — the permissive-hostname-verifier idiom, custom `sslSocketFactory(...)` overrides, empty-body `checkServerTrusted` overrides, hand-rolled trust-all `X509TrustManager`, and the named all-trust/all-hostname constants — and exits 1 (printing offenders) if any match, else prints an OK line and exits 0. Use the exact extended `PATTERN` from RESEARCH so the gate catches semantic bypasses, not only the two literal SC#4 substrings. <!-- planner-discipline-allow: hostnameVerifier --> <!-- planner-discipline-allow: sslSocketFactory --> <!-- planner-discipline-allow: X509TrustManager --> Then edit `.planning/REQUIREMENTS.md` JAVA-01: change the "Maven Central publish (`io.axiam:axiam-sdk`)" acceptance bullet to name BOTH coordinates — `io.axiam:axiam-sdk` (SDK jar) AND `io.axiam:axiam-bom` (Bill-of-Materials, D-23) — with a parenthetical noting the BOM was added in Phase 20 discuss (D-23) to align consumer dependency versions.
  </action>
  <verify>
    <automated>bash sdks/java/scripts/tls-bypass-gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash sdks/java/scripts/tls-bypass-gate.sh` exits 0 on the current (empty) src/examples tree and prints an OK line
    - `grep -c 'axiam-bom' .planning/REQUIREMENTS.md` returns >= 1 and the JAVA-01 section names both `io.axiam:axiam-sdk` and `io.axiam:axiam-bom`
    - The gate script scope excludes `sdks/java/scripts` and `sdks/java/src/test` so a future reflection-based TLS test cannot self-trip it
  </acceptance_criteria>
  <done>TLS grep gate runnable and green; JAVA-01 reconciled to list the BOM coordinate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor/CI machine -> Maven Central | build-time dependency + plugin/protoc artifact download (untrusted-until-verified) |
| SDK source tree -> published jar | TLS-bypass idioms must never enter source that ships |
| repo docs -> release matrix | REQUIREMENTS.md defines which coordinates get signed/published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering/Info-Disclosure | TLS transport config in SDK source | high | mitigate | Ship `tls-bypass-gate.sh` (extended pattern set) run in CI over src+examples; SC#4 |
| T-20-SC | Tampering | Maven dependency + protoc/plugin supply chain | high | mitigate | All packages WebSearch-audited as official (RESEARCH Package Legitimacy Audit, no [SUS]/[SLOP]); CI `mvn dependency:tree` re-verification step; pinned exact versions |
| T-20-08 | Spoofing | `io.axiam` Maven Central namespace squatting | medium | transfer | Sonatype Central Portal enforces namespace ownership verification before any publish; coordinates (incl. BOM) reconciled in REQUIREMENTS.md |
</threat_model>

<verification>
- `mvn -f sdks/java/pom.xml validate` and `dependency:resolve` succeed on Java 21.
- `mvn -f sdks/java/pom.xml generate-sources compile` produces gRPC stubs into gitignored target/; no generated `.java` in git.
- `bash sdks/java/scripts/tls-bypass-gate.sh` exits 0.
- REQUIREMENTS.md JAVA-01 lists both `io.axiam:axiam-sdk` and `io.axiam:axiam-bom`.
</verification>

<success_criteria>
The Java SDK build compiles on the Java 21 baseline with all JAVA-01-pinned dependencies resolvable, the gRPC codegen path is Maven-native and gitignored, the TLS-bypass gate is in place, and the BOM coordinate is reconciled — unblocking all Wave-2 plans.
</success_criteria>

<output>
Create `.planning/phases/20-java-sdk/20-01-SUMMARY.md` when done.
</output>
