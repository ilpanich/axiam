---
phase: 20-java-sdk
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - sdks/java/src/main/java/io/axiam/sdk/Sensitive.java
  - sdks/java/src/main/java/io/axiam/sdk/LoginResult.java
  - sdks/java/src/main/java/io/axiam/sdk/AxiamUser.java
  - sdks/java/src/main/java/io/axiam/sdk/errors/package-info.java
  - sdks/java/src/main/java/io/axiam/sdk/errors/AuthError.java
  - sdks/java/src/main/java/io/axiam/sdk/errors/AuthzError.java
  - sdks/java/src/main/java/io/axiam/sdk/errors/NetworkError.java
  - sdks/java/src/main/java/io/axiam/sdk/errors/ErrorMapper.java
  - sdks/java/src/test/java/io/axiam/sdk/SensitiveTest.java
  - sdks/java/src/test/java/io/axiam/sdk/errors/ErrorRedactionTest.java
autonomous: true
requirements: [JAVA-01]
must_haves:
  truths:
    - "`Sensitive.toString()` returns `[SENSITIVE]`, its Jackson serialization emits `[SENSITIVE]`, and it is not `Serializable`; the raw value is reachable only via a package-internal accessor"
    - "`NetworkError` never carries a raw `Set-Cookie`/`Authorization`/`Cookie` value — redaction happens BEFORE wrap at the single `ErrorMapper` choke point (CR-04)"
    - "`ErrorMapper` maps HTTP/gRPC status to AuthError/AuthzError/NetworkError exactly per CONTRACT.md §2"
  artifacts:
    - sdks/java/src/main/java/io/axiam/sdk/Sensitive.java
    - sdks/java/src/main/java/io/axiam/sdk/errors/ErrorMapper.java
    - sdks/java/src/main/java/io/axiam/sdk/LoginResult.java
    - sdks/java/src/main/java/io/axiam/sdk/AxiamUser.java
  key_links:
    - "ErrorMapper.sanitize(Response) -> only path from a live okhttp3.Response into NetworkError (single choke point, redact-before-wrap)"
    - "@JsonSerialize(using=Sensitive.Redactor.class) -> [SENSITIVE] on any Jackson path"
---

<objective>
Build the transport-independent token-safety and error-taxonomy core every later plan reuses: the hardened `Sensitive` final class (D-17), the three unchecked error types + the central status->error `ErrorMapper` with redact-before-wrap (D-18 / CR-04 carry-forward), and the immutable `LoginResult` / `AxiamUser` records (D-04). Each ships with a non-vacuous regression test (`SensitiveTest`, `ErrorRedactionTest`) proving raw tokens never leak.

Purpose: `Sensitive` and `NetworkError` are the CR-04 leak-class control every sibling SDK had to carry forward; getting them right here means the REST/gRPC/AMQP transports inherit a safe error surface.
Output: Token wrapper, error taxonomy + mapper, DTO records, and leak-regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@sdks/CONTRACT.md
@.planning/phases/20-java-sdk/20-RESEARCH.md
@.planning/phases/20-java-sdk/20-PATTERNS.md
@sdks/go/sensitive.go
@sdks/go/errors.go
@sdks/typescript/src/core/errorMapper.ts
</context>

<artifacts_this_phase_produces>
- `io.axiam.sdk.Sensitive` — `final` class; `static Sensitive of(String)`; `toString()` -> `[SENSITIVE]`; `@JsonSerialize(using=Sensitive.Redactor.class)`; NOT `Serializable`; package-internal `String expose()`; nested `static final class Redactor extends StdSerializer<Sensitive>`.
- `io.axiam.sdk.errors.AuthError` / `AuthzError` / `NetworkError` — all `extends RuntimeException` (D-03). `AuthzError` optionally carries `action`/`resourceId`; `NetworkError` carries a redacted-summary cause.
- `io.axiam.sdk.errors.ErrorMapper` — `fromHttpStatus(int,String,Response)`, `fromHttpResponse(...)`, `fromGrpcStatus(io.grpc.Status.Code,String)`, private `sanitize(Response)`; constant `SENSITIVE_HEADERS = {set-cookie, authorization, cookie}`.
- `io.axiam.sdk.LoginResult` (record) — `boolean mfaRequired` + optional fields (challengeToken, user); `io.axiam.sdk.AxiamUser` (record) — `userId`, `tenantId`, `roles`.
- `io.axiam.sdk.errors.package-info.java` — `@NullMarked`.
- Tests: `SensitiveTest`, `ErrorRedactionTest`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sensitive hardened final class + LoginResult/AxiamUser records (D-17, D-04)</name>
  <files>sdks/java/src/main/java/io/axiam/sdk/Sensitive.java, sdks/java/src/main/java/io/axiam/sdk/LoginResult.java, sdks/java/src/main/java/io/axiam/sdk/AxiamUser.java, sdks/java/src/test/java/io/axiam/sdk/SensitiveTest.java</files>
  <read_first>
    - sdks/go/sensitive.go (String/Format/GoString/MarshalJSON quartet + package-internal `expose()`) and sdks/typescript/src/core/sensitive.ts (private #value)
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Pattern 6 (lines ~885-936, the ready-to-use Java port incl. the non-`Serializable` rationale)
    - sdks/CONTRACT.md §7 (Java row: "Final class; toString() returns [SENSITIVE]") + the "MUST NOT appear in logs/errors/stack traces/serialized output" list
    - .planning/phases/20-java-sdk/20-CONTEXT.md D-17, D-04
  </read_first>
  <action>
    Create `io.axiam.sdk.Sensitive` per RESEARCH Pattern 6: `final` class, private `String value`, private constructor + `static Sensitive of(String)`, `toString()` returning the `[SENSITIVE]` placeholder, class-annotated `@JsonSerialize(using = Sensitive.Redactor.class)` where the nested `static final class Redactor extends StdSerializer<Sensitive>` writes the placeholder string on any Jackson path, and a PACKAGE-PRIVATE `String expose()` accessor (never public). Do NOT implement `java.io.Serializable` (so a diagnostic object graph containing a Sensitive field fails closed with `NotSerializableException` rather than leaking `value` via reflective serialization). Do NOT add an `equals`/`hashCode` that exposes `value` via a timing side channel. Create the immutable records `LoginResult` (`boolean mfaRequired` + a nullable `challengeToken` for the MFA branch + a nullable `AxiamUser user`; MFA-required is a flag, never an exception — D-03/D-04) and `AxiamUser` (`String userId`, `String tenantId`, `List<String> roles`), both `@NullMarked`-covered by the root package-info. Write `SensitiveTest` asserting: `toString()` equals the placeholder; `new ObjectMapper().writeValueAsString(Sensitive.of("super-secret-token"))` contains the placeholder and NOT `super-secret-token`; and `Sensitive` does not implement `Serializable` (`assertFalse(Serializable.class.isAssignableFrom(Sensitive.class))`).
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml test -Dtest=SensitiveTest</automated>
  </verify>
  <acceptance_criteria>
    - `SensitiveTest` proves the raw value `super-secret-token` never appears in `toString()` or Jackson output, and that `Sensitive` is not `Serializable`
    - `expose()` is package-private (no `public`/`protected` modifier) — a test in another package cannot call it
    - `LoginResult` and `AxiamUser` are `record` types; `LoginResult.mfaRequired()` accessor exists
  </acceptance_criteria>
  <done>Sensitive redacts on every surface and fails closed on serialization; DTO records present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Error taxonomy + central ErrorMapper with redact-before-wrap + ErrorRedactionTest (D-18, CR-04)</name>
  <files>sdks/java/src/main/java/io/axiam/sdk/errors/AuthError.java, sdks/java/src/main/java/io/axiam/sdk/errors/AuthzError.java, sdks/java/src/main/java/io/axiam/sdk/errors/NetworkError.java, sdks/java/src/main/java/io/axiam/sdk/errors/ErrorMapper.java, sdks/java/src/main/java/io/axiam/sdk/errors/package-info.java, sdks/java/src/test/java/io/axiam/sdk/errors/ErrorRedactionTest.java</files>
  <read_first>
    - sdks/go/errors.go (`newNetworkError` single choke point + `sanitizeResponse`; `errorFromHTTPStatus`/`errorFromGRPCStatus` tables) and sdks/typescript/src/core/errorMapper.ts (`sanitizeAxiosError` — the exact CR-04 fix)
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Pattern 7 (lines ~938-1004, ErrorMapper.fromHttpResponse/sanitize + the non-vacuous control-case requirement)
    - .planning/phases/20-java-sdk/20-PATTERNS.md § "errors/... redact-before-wrap" (single choke-point invariant) + § Shared Patterns (redact-before-wrap)
    - sdks/CONTRACT.md §2 (HTTP + gRPC status->error tables; "Errors MUST NOT expose raw token strings")
    - .planning/phases/17-typescript-sdk/17-REVIEW.md §CR-04 if present (origin of the leak class)
  </read_first>
  <action>
    Create `AuthError`, `AuthzError`, `NetworkError` all `extends RuntimeException` (unchecked, D-03). Exception messages are English-only, no i18n — classification is via typed exceptions + codes, not localized text (D-29). `AuthzError` may carry optional `action`/`resourceId` (from the response body when available, §2). `NetworkError` carries a `cause`/redacted-summary that is ALREADY sanitized — never a live `okhttp3.Response`. Create `ErrorMapper` transcribing CONTRACT.md §2 EXACTLY: `fromHttpStatus(int,String,Response)` returns `AuthError` for 401, `AuthzError` for 403/409, else routes through `fromHttpResponse` (400/408/429/5xx/other -> `NetworkError`); `fromGrpcStatus(Status.Code,String)` returns `AuthError` for UNAUTHENTICATED, `AuthzError` for PERMISSION_DENIED, else `NetworkError` (UNAVAILABLE/DEADLINE_EXCEEDED/INTERNAL/RESOURCE_EXHAUSTED/other). Make `fromHttpResponse`/`sanitize` the SINGLE and ONLY path from a live `okhttp3.Response` into a `NetworkError`: `sanitize(Response)` copies headers, DROPPING any whose lowercased name is in `SENSITIVE_HEADERS = {set-cookie, authorization, cookie}`, and returns a lightweight redacted STRING summary (`"http status N, headers: ..."`) — it must NEVER retain the live Response object (which holds the unredacted headers). Create `errors/package-info.java` `@NullMarked`. Write `ErrorRedactionTest` (CR-04 regression, non-vacuous): build an `okhttp3.Response` carrying `Set-Cookie: axiam_access=super-secret-token; HttpOnly` AND a benign `X-Request-Id: req-123`; map it via `fromHttpStatus`; assert `super-secret-token` appears NOWHERE in the resulting `NetworkError`'s `toString()`/`getMessage()`/cause chain, AND assert the control header `req-123` DOES survive into the sanitized summary (proving redaction is selective, not a vacuous redact-everything).
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml test -Dtest=ErrorRedactionTest</automated>
  </verify>
  <acceptance_criteria>
    - `ErrorRedactionTest` asserts BOTH: the raw `axiam_access` token value never appears in the thrown `NetworkError`, and the benign `X-Request-Id` value survives (non-vacuous control)
    - `sanitize` is the only constructor path from an `okhttp3.Response` to `NetworkError` (grep confirms `new NetworkError(` is not called with a raw `Response` elsewhere in this plan's files)
    - status mapping matches §2: 401->AuthError, 403/409->AuthzError, 400/429/5xx->NetworkError; UNAUTHENTICATED->AuthError, PERMISSION_DENIED->AuthzError
  </acceptance_criteria>
  <done>One central mapper implements §2; NetworkError is redact-before-wrap with a proven non-vacuous regression test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AXIAM server response -> SDK error surface | response headers may carry tokens that must not enter errors/logs |
| SDK internals -> consumer diagnostics | token-carrying fields must redact on every output path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02 | Info-Disclosure | `NetworkError` wrapping a raw OkHttp `Response` with `Set-Cookie` | high | mitigate | D-18 redact-before-wrap at the single `ErrorMapper.sanitize` choke point; non-vacuous `ErrorRedactionTest` (CR-04 carry-forward) |
| T-20-02b | Info-Disclosure | token value via `toString`/Jackson/Java-serialization of `Sensitive` | high | mitigate | D-17 hardened final class: placeholder `toString`, Jackson `Redactor`, non-`Serializable`, package-internal accessor; `SensitiveTest` |
</threat_model>

<verification>
- `mvn -f sdks/java/pom.xml test -Dtest=SensitiveTest,ErrorRedactionTest` green.
- Static: `Sensitive` not `Serializable`; `NetworkError` never constructed from a live `Response` outside `ErrorMapper.sanitize`.
</verification>

<success_criteria>
Token wrapper and error taxonomy redact raw tokens on every surface, the central mapper implements §2 exactly, and the CR-04 leak class is closed with non-vacuous regression tests — giving all transports a safe error/token core.
</success_criteria>

<output>
Create `.planning/phases/20-java-sdk/20-03-SUMMARY.md` when done.
</output>
