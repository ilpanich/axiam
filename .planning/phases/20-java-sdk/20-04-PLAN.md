---
phase: 20-java-sdk
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - sdks/java/src/main/java/io/axiam/sdk/internal/package-info.java
  - sdks/java/src/main/java/io/axiam/sdk/internal/TokenPair.java
  - sdks/java/src/main/java/io/axiam/sdk/internal/RefreshGuard.java
  - sdks/java/src/main/java/io/axiam/sdk/internal/JwksVerifier.java
  - sdks/java/src/main/java/io/axiam/sdk/internal/Retry.java
  - sdks/java/src/test/java/io/axiam/sdk/internal/RefreshGuardSingleFlightTest.java
  - sdks/java/src/test/java/io/axiam/sdk/internal/JwksVerifierTest.java
  - sdks/java/src/test/java/io/axiam/sdk/internal/RetryTest.java
autonomous: true
requirements: [JAVA-01]
must_haves:
  truths:
    - "5 concurrent threads observing the same expired token trigger exactly 1 refresh call (SC#2)"
    - "a failed refresh propagates its exception to every waiter once, with no retry loop (§9.3)"
    - "JwksVerifier pins alg=EdDSA before key lookup, rotates on unknown kid, and exposes a cross-tenant claim check helper"
    - "Retry applies bounded exponential backoff + jitter to idempotent ops only (transient network/429/503, honoring Retry-After); state-changing requests never auto-retry (D-26)"
  artifacts:
    - sdks/java/src/main/java/io/axiam/sdk/internal/RefreshGuard.java
    - sdks/java/src/main/java/io/axiam/sdk/internal/JwksVerifier.java
    - sdks/java/src/main/java/io/axiam/sdk/internal/TokenPair.java
    - sdks/java/src/main/java/io/axiam/sdk/internal/Retry.java
  key_links:
    - "RefreshGuard (one instance per AxiamClient) -> shared by REST interceptor/authenticator (20-05) AND gRPC client (20-08) — D-07 one guard"
    - "JwksVerifier -> RemoteJWKSet({baseUrl}/oauth2/jwks) -> JWSVerificationKeySelector(EdDSA) -> cross-tenant tenant_id check by 20-06 Spring filter"
---

<objective>
Deliver the two transport-independent verification/concurrency primitives that REST (20-05), gRPC (20-08), and Spring (20-06) share: the `ReentrantLock` + `CompletableFuture`-in-`AtomicReference` single-flight `RefreshGuard` (D-07/§9) proven by the SC#2 5-thread test, and the nimbus `RemoteJWKSet` + EdDSA-pinned `JwksVerifier` (D-19) with the mandatory cross-tenant claim-check helper. Includes the internal `TokenPair` value type the guard resolves.

Purpose: SC#2 (exactly-1-refresh) is a phase acceptance criterion; the single guard must be shared across REST + gRPC. The EdDSA-pinned verifier closes the algorithm-confusion class and, with the cross-tenant check, the cross-tenant token-replay class.
Output: `RefreshGuard`, `JwksVerifier`, `TokenPair`, and their tests (SC#2 + JWKS behavior).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@sdks/CONTRACT.md
@.planning/phases/20-java-sdk/20-RESEARCH.md
@.planning/phases/20-java-sdk/20-PATTERNS.md
@sdks/go/internal/refreshguard/guard.go
@sdks/go/internal/jwks/verifier.go
</context>

<artifacts_this_phase_produces>
- `io.axiam.sdk.internal.TokenPair` (record) — `access` (String or Sensitive), `refresh`, `expiresAtEpochMs`; accessor `access()`.
- `io.axiam.sdk.internal.RefreshGuard` — `ReentrantLock lock`; `AtomicReference<CompletableFuture<TokenPair>> inFlight`; `volatile TokenPair current`; `TokenPair refreshIfNeeded(String observedAccessToken, Supplier<TokenPair> doRefresh)`; non-blocking `Optional<TokenPair> cached()` / `String cachedAccessToken()` accessor for interceptor hot paths.
- `io.axiam.sdk.internal.JwksVerifier` — constructed from `baseUrl`; wraps `RemoteJWKSet` + `DefaultJWKSetCache(300,60,SECONDS)` + `DefaultJWTProcessor` with `JWSVerificationKeySelector(JWSAlgorithm.EdDSA, jwkSource)`; `JWTClaimsSet verify(String token)`; static helper `assertTenant(JWTClaimsSet, String configuredTenantId)`.
- `io.axiam.sdk.internal.Retry` — hand-rolled bounded exponential-backoff-with-jitter helper (D-26); `static <T> T withRetry(int maxAttempts, ...)` for IDEMPOTENT ops only; honors a `Retry-After` hint; no external dependency.
- `io.axiam.sdk.internal.package-info.java` — `@NullMarked`.
- Tests: `RefreshGuardSingleFlightTest` (SC#2), `JwksVerifierTest`, `RetryTest`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RefreshGuard single-flight (D-07/§9) + TokenPair + SC#2 5-thread test</name>
  <files>sdks/java/src/main/java/io/axiam/sdk/internal/RefreshGuard.java, sdks/java/src/main/java/io/axiam/sdk/internal/TokenPair.java, sdks/java/src/main/java/io/axiam/sdk/internal/package-info.java, sdks/java/src/test/java/io/axiam/sdk/internal/RefreshGuardSingleFlightTest.java</files>
  <behavior>
    - N (>=5) threads calling refreshIfNeeded with the same expired token cause exactly 1 doRefresh invocation
    - after the in-flight refresh resolves, all waiters receive the same resolved TokenPair
    - a doRefresh that throws propagates the same exception to every waiter, and no second doRefresh is attempted (§9.3)
    - a caller whose observedAccessToken already differs from the cached current token gets the cached token without a new refresh (double-check)
  </behavior>
  <read_first>
    - sdks/go/internal/refreshguard/guard.go (`RefreshIfNeeded` double-check-after-lock, `CachedAccessToken`, no-retry-on-failure) and its guard_test.go concurrency shape
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Pattern 2 (lines ~452-601 — the near-final RefreshGuard port + the JUnit 5 CountDownLatch/counting-dispatcher test skeleton) — NOTE Assumption A3: the pinned test dep is the OkHttp-4.x `okhttp3.mockwebserver` package, adjust imports away from Pattern 2's illustrative `mockwebserver3` names
    - .planning/phases/20-java-sdk/20-PATTERNS.md § "internal/RefreshGuard.java" (Mutex->ReentrantLock port, lock-release-before-join, no-retry §9.3) + § "never lock in the interceptor" anti-pattern
    - sdks/CONTRACT.md §9 (Java row + Test requirement: >=5 concurrent, exactly 1 refresh)
  </read_first>
  <action>
    Create `io.axiam.sdk.internal.TokenPair` as a record (`String access`, `String refresh`, `long expiresAtEpochMs`) with an `access()` accessor. Create `io.axiam.sdk.internal.RefreshGuard` per RESEARCH Pattern 2: a `ReentrantLock`, an `AtomicReference<CompletableFuture<TokenPair>> inFlight`, and a `volatile TokenPair current`. `refreshIfNeeded(String observedAccessToken, Supplier<TokenPair> doRefresh)`: acquire the lock; double-check — if `current != null` and `current.access()` differs from `observedAccessToken`, another thread already refreshed, return `current`; if an `inFlight` future exists, release the lock and `join()` it (re-acquire in a finally so the outer unlock stays balanced), returning its result; otherwise install a fresh `CompletableFuture`, release the lock, run `doRefresh.get()` OUTSIDE the lock (the actual POST /api/v1/auth/refresh), set `current`, complete the future, and on exception `completeExceptionally` + rethrow as-is (NO retry loop, §9.3); always clear `inFlight` in a finally. Add a non-blocking `cachedAccessToken()` (volatile read of `current`, never acquires the lock — for the interceptor hot path). Write `RefreshGuardSingleFlightTest`: 5 threads released simultaneously via a `CountDownLatch` start barrier, each calling `refreshIfNeeded(expiredToken, counter-incrementing supplier)`; assert the supplier ran exactly once. Add a second test asserting a throwing supplier propagates to all 5 waiters and runs once.
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml test -Dtest=RefreshGuardSingleFlightTest</automated>
  </verify>
  <acceptance_criteria>
    - `RefreshGuardSingleFlightTest` asserts exactly 1 refresh across 5 concurrent threads (SC#2's literal target)
    - a failing-refresh test asserts the exception reaches all waiters and `doRefresh` ran once (no retry loop, §9.3)
    - `cachedAccessToken()` performs no `lock.lock()` (grep confirms it is a volatile read)
  </acceptance_criteria>
  <done>Single-flight guard proven to collapse 5 concurrent refreshes into 1; failure propagates without retry.</done>
</task>

<task type="auto">
  <name>Task 2: JwksVerifier — RemoteJWKSet + EdDSA pinning + cross-tenant helper (D-19)</name>
  <files>sdks/java/src/main/java/io/axiam/sdk/internal/JwksVerifier.java, sdks/java/src/test/java/io/axiam/sdk/internal/JwksVerifierTest.java</files>
  <read_first>
    - sdks/go/internal/jwks/verifier.go + claims.go (JWKS cache/rotation, EdDSA-only allowlist, cross-tenant claim check origin)
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Pattern 4 (lines ~696-765 — RemoteJWKSet + DefaultJWKSetCache(300,60) + DefaultJWTProcessor + JWSVerificationKeySelector(EdDSA); Tink runtime-dependency smoke-test note) + § Open Questions #1 / Assumption A5 (verify the exact 10.7 RemoteJWKSet/DefaultJWKSetCache constructor via `mvn dependency:resolve-sources`/javadoc and adjust if the signature differs)
    - .planning/phases/20-java-sdk/20-PATTERNS.md § "internal/JwksVerifier.java" + § Shared Patterns "Cross-tenant JWKS claim check (MUST-carry-forward control)"
    - .planning/phases/20-java-sdk/20-CONTEXT.md D-19; RESEARCH § Security Domain (algorithm-confusion + cross-tenant rows)
  </read_first>
  <action>
    Create `io.axiam.sdk.internal.JwksVerifier(String baseUrl)`: build a `RemoteJWKSet<SecurityContext>` bound to `{baseUrl}/oauth2/jwks` (org-wide, NOT tenant-scoped — confirmed in the server route table) with a `DefaultJWKSetCache(300, 60, TimeUnit.SECONDS)` (TTL 300s / forced-refetch cooldown 60s, matching sibling defaults), and a `DefaultJWTProcessor` whose key selector is `new JWSVerificationKeySelector<>(JWSAlgorithm.EdDSA, jwkSource)` so nimbus rejects any JWS whose `alg` is not EdDSA BEFORE key lookup (closes algorithm confusion natively — no hand-rolled alg check). Expose `JWTClaimsSet verify(String token)` that parses the SignedJWT and runs the processor (throwing on bad signature/wrong alg). Add a static helper `assertTenant(JWTClaimsSet claims, String configuredTenantId)` that throws `AuthError` if `claims.getStringClaim("tenant_id")` is null or not equal to `configuredTenantId` (the MUST-carry-forward cross-tenant control — signature validity alone is insufficient since JWKS is org-wide; 20-06 Spring filter calls this). Before finalizing, verify the exact nimbus 10.7 `RemoteJWKSet`/`DefaultJWKSetCache` constructor signatures against resolved sources and adjust the call if needed (Assumption A5). Write `JwksVerifierTest` with a mocked JWKS `MockWebServer`: (a) a valid EdDSA token verifies and its claims are returned; (b) a token signed with a non-EdDSA alg (e.g. HS256) is rejected; (c) `assertTenant` throws `AuthError` when `tenant_id` mismatches and passes when it matches; (d) a Tink-presence smoke assertion that verifying a real Ed25519 token does not raise `NoClassDefFoundError` (catches a missing Tink runtime dep early).
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml test -Dtest=JwksVerifierTest</automated>
  </verify>
  <acceptance_criteria>
    - `JwksVerifierTest` proves an EdDSA token verifies, a non-EdDSA-alg token is rejected, and the Ed25519 path does not throw `NoClassDefFoundError` (Tink present)
    - `assertTenant` throws `AuthError` on a `tenant_id` mismatch and returns normally on a match
    - the JWKS URL is `{baseUrl}/oauth2/jwks` and the key selector pins `JWSAlgorithm.EdDSA`
  </acceptance_criteria>
  <done>Local JWKS verification pins EdDSA, rotates on unknown kid, and enforces the cross-tenant claim control.</done>
</task>

<task type="auto">
  <name>Task 3: Retry — hand-rolled bounded backoff + jitter for idempotent ops (D-26)</name>
  <files>sdks/java/src/main/java/io/axiam/sdk/internal/Retry.java, sdks/java/src/test/java/io/axiam/sdk/internal/RetryTest.java</files>
  <read_first>
    - sdks/go/client.go (transient-retry / Retry-After handling if present) for the idempotent-only + honor-Retry-After semantics
    - .planning/phases/20-java-sdk/20-RESEARCH.md § "Don't Hand-Roll" (the retry row: this is THE one explicitly hand-rolled piece by user decision, ~30 lines, no Resilience4j/Failsafe) + § Summary (3-attempt bounded backoff default)
    - .planning/phases/20-java-sdk/20-CONTEXT.md D-26 (idempotent ops only: transient network/429/503, honor Retry-After; state-changing requests NEVER auto-retry; numeric values = planner) + Deferred Ideas (Resilience4j/Failsafe rejected)
  </read_first>
  <action>
    Create `io.axiam.sdk.internal.Retry`: a dependency-free bounded exponential-backoff-with-jitter helper — `static <T> T withRetry(int maxAttempts, java.util.function.Supplier<T> op, java.util.function.Predicate<RuntimeException> retryable, ...)` (default maxAttempts 3, base delay ~200ms, jitter, cap). It retries ONLY when the supplied `retryable` predicate matches (transient `NetworkError` classes: connection failures / HTTP 429 / 503) and, when the error carries a `Retry-After` hint, waits at least that long instead of the computed backoff. It MUST be applied by callers to IDEMPOTENT operations only; there is no auto-retry path for state-changing requests (the REST client in 20-05 applies it to GET/authz-check reads, never to login/logout/mutations). Never pull in Resilience4j/Failsafe (D-26/Deferred). Write `RetryTest`: (a) an op that fails twice with a transient error then succeeds returns the success within maxAttempts and records exactly 3 attempts; (b) a non-retryable error (e.g. `AuthzError`) is thrown immediately with 1 attempt; (c) exceeding maxAttempts rethrows the last error; (d) a `Retry-After`-carrying error causes the wait to be at least the hinted duration (assert via an injected clock/sleeper, not a real sleep).
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml test -Dtest=RetryTest</automated>
  </verify>
  <acceptance_criteria>
    - `RetryTest` proves: transient-then-success within maxAttempts (3 attempts), non-retryable errors are not retried (1 attempt), max-attempts exhaustion rethrows, and Retry-After is honored (via injected sleeper, no real wall-clock sleep)
    - `Retry` pulls in no external retry/circuit-breaker dependency (grep confirms no resilience4j/failsafe import)
    - the helper is idempotent-only by contract (documented; callers pass state-changing ops through without retry)
  </acceptance_criteria>
  <done>Bounded backoff+jitter retry for idempotent ops implemented with no extra dependency (D-26).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent SDK callers -> single refresh token | one rotating single-use refresh token must not be spent by a thundering herd |
| org-wide JWKS issuer -> tenant-scoped trust | a validly-signed token for another tenant must not be accepted |
| attacker-chosen JWS `alg` -> verifier | alg must be pinned before key lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Denial-of-Service (self-inflicted) | concurrent 401 refresh storm | high | mitigate | §9 `RefreshGuard` single-flight (ReentrantLock + CompletableFuture-in-AtomicReference); SC#2 test |
| T-20-06 | Spoofing/Tampering | algorithm confusion (`alg:none`/`HS256` vs EdDSA) | high | mitigate | `JWSVerificationKeySelector(EdDSA, ...)` pins alg pre-lookup (nimbus library feature); `JwksVerifierTest` |
| T-20-07 | Elevation-of-Privilege | cross-tenant token replay (org-wide JWKS) | high | mitigate | `assertTenant` claim check (`tenant_id == configuredTenantId`); enforced by 20-06 Spring filter |
</threat_model>

<verification>
- `mvn -f sdks/java/pom.xml test -Dtest=RefreshGuardSingleFlightTest,JwksVerifierTest` green.
- Static: `cachedAccessToken()` is lock-free; key selector pins EdDSA.
</verification>

<success_criteria>
The single-flight guard collapses concurrent refreshes to exactly 1 (SC#2), and the JWKS verifier pins EdDSA and enforces the cross-tenant control — the shared verification/concurrency core the transports and Spring filter build on.
</success_criteria>

<output>
Create `.planning/phases/20-java-sdk/20-04-SUMMARY.md` when done.
</output>
