---
phase: 20-java-sdk
plan: 05
type: execute
wave: 3
depends_on: ["20-03", "20-04"]
files_modified:
  - sdks/java/src/main/java/io/axiam/sdk/rest/package-info.java
  - sdks/java/src/main/java/io/axiam/sdk/internal/SessionState.java
  - sdks/java/src/main/java/io/axiam/sdk/rest/AuthInterceptor.java
  - sdks/java/src/main/java/io/axiam/sdk/rest/AuthAuthenticator.java
  - sdks/java/src/main/java/io/axiam/sdk/AxiamClient.java
  - sdks/java/src/test/java/io/axiam/sdk/AxiamClientBuilderTest.java
  - sdks/java/src/test/java/io/axiam/sdk/rest/CsrfInterceptorTest.java
  - sdks/java/src/test/java/io/axiam/sdk/rest/AuthFlowTest.java
  - sdks/java/src/test/java/io/axiam/sdk/rest/AuthzTest.java
autonomous: true
requirements: [JAVA-01]
must_haves:
  truths:
    - "`AxiamClient.builder(baseUrl, tenantId)` is the only construction path; a blank tenantId is rejected and there is no no-arg builder (SC#1)"
    - "`login(email,password)` returns a typed `LoginResult`; `verifyMfa`/`refresh`/`logout` and `checkAccess`/`can`/`batchCheck` work over REST"
    - "the OkHttpClient always uses CookieManager and strict TLS even when a custom OkHttpClient is supplied (SC#4/D-27)"
    - "X-CSRF-Token is captured from responses and echoed on POST/PUT/PATCH/DELETE; X-Tenant-Id is injected on every request"
  artifacts:
    - sdks/java/src/main/java/io/axiam/sdk/AxiamClient.java
    - sdks/java/src/main/java/io/axiam/sdk/internal/SessionState.java
    - sdks/java/src/main/java/io/axiam/sdk/rest/AuthInterceptor.java
    - sdks/java/src/main/java/io/axiam/sdk/rest/AuthAuthenticator.java
  key_links:
    - "AxiamClient owns ONE RefreshGuard (20-04) exposed via a package-internal accessor -> reused by gRPC (20-08), never a second guard"
    - "Builder.build() re-applies CookieManager + strict TLS over any supplied OkHttpClient via newBuilder() (D-27)"
    - "AuthInterceptor (proactive, near-expiry) + AuthAuthenticator (reactive 401) both funnel into the same RefreshGuard (D-08)"
---

<objective>
Build the public REST surface. Deliver `SessionState` (per-client cookie jar via `CookieManager`, CSRF capture/echo §3/CF-01, tenant+org header injection §5), the `AuthInterceptor` (proactive near-expiry refresh + header injection, application-layer) and `AuthAuthenticator` (reactive 401 fallback) — both funnelling into the single shared `RefreshGuard` (D-08) — and the `AxiamClient` builder+facade: `tenantId` compiler-required (SC#1), optional `orgSlug`/`orgId` (real-endpoint requirement, Pitfall 2), client-override safety (D-27), `AutoCloseable` lifecycle (D-09), the auth methods (`login`/`verifyMfa`/`refresh`/`logout`) and the authz methods (`checkAccess`/`can`/`batchCheck`) against the FND-04 REST endpoints. This closes SC#1 and the REST half of SC#4.

Purpose: This is the primary consumer-facing surface and the anchor the gRPC/AMQP/Spring plans reference; it must expose the shared-guard seam without later plans editing `AxiamClient`.
Output: REST client, session, interceptors, and their MockWebServer tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@sdks/CONTRACT.md
@.planning/phases/20-java-sdk/20-RESEARCH.md
@.planning/phases/20-java-sdk/20-PATTERNS.md
@sdks/go/client.go
@sdks/go/login.go
@sdks/go/authz.go
@crates/axiam-api-rest/src/handlers/auth.rs
@crates/axiam-api-rest/src/handlers/authz_check.rs
</context>

<artifacts_this_phase_produces>
- `io.axiam.sdk.AxiamClient implements AutoCloseable` — `static Builder builder(String baseUrl, String tenantId)` (blank tenantId -> AuthError; no no-arg path); nested `static final class Builder` with `orgSlug(String)`, `orgId(UUID)`, `customCa(byte[])`, `httpClient(OkHttpClient)`, `connectTimeout/readTimeout/writeTimeout(Duration)`, `build()`; instance methods `login(String,String)`, `verifyMfa(...)`, `refresh()`, `logout()`, `checkAccess(...)`, `can(...)`, `batchCheck(...)` (+ `*Async()` CompletableFuture variants, D-02); `close()`; package-internal accessors `refreshGuard()`, `tenantId()`, `baseUrl()`, `okHttpClient()`, `sslConfig()`/`customCa()` for the gRPC seam (20-08).
- `io.axiam.sdk.internal.SessionState` — cookie/CSRF/tenant/org state; `doHttpRefresh()` (Supplier<TokenPair> for the guard); `resolvedOrgId()`; CSRF capture/echo; `cachedAccessToken()`.
- `io.axiam.sdk.rest.AuthInterceptor implements okhttp3.Interceptor`; `io.axiam.sdk.rest.AuthAuthenticator implements okhttp3.Authenticator`.
- `io.axiam.sdk.rest.package-info.java` — `@NullMarked`.
- Tests: `AxiamClientBuilderTest` (SC#1), `CsrfInterceptorTest`, `AuthFlowTest`, `AuthzTest`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SessionState + AuthInterceptor + AuthAuthenticator (§3/§4/§5, D-08)</name>
  <files>sdks/java/src/main/java/io/axiam/sdk/internal/SessionState.java, sdks/java/src/main/java/io/axiam/sdk/rest/AuthInterceptor.java, sdks/java/src/main/java/io/axiam/sdk/rest/AuthAuthenticator.java, sdks/java/src/main/java/io/axiam/sdk/rest/package-info.java, sdks/java/src/test/java/io/axiam/sdk/rest/CsrfInterceptorTest.java</files>
  <read_first>
    - sdks/go/client.go (`decorateRequest` X-Tenant-ID + state-changing CSRF echo; `captureCSRFFromResponse`; `resolvedOrgID`)
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Pattern 3 (lines ~603-693 — AuthInterceptor proactive refresh + header injection + CSRF capture/echo, AuthAuthenticator reactive 401, responseCount guard; "never lock in the interceptor" — do a non-blocking cachedAccessToken read first) + § Architecture (application-interceptor vs network-interceptor choice)
    - .planning/phases/20-java-sdk/20-PATTERNS.md § "AxiamClient + Builder" (CSRF capture/echo moves into AuthInterceptor) + § "internal/RefreshGuard" (non-blocking cached read)
    - sdks/CONTRACT.md §3 (non-browser: capture X-CSRF-Token response header, echo on mutating methods), §4 (CookieManager), §5 (X-Tenant-ID every request)
  </read_first>
  <action>
    Create `io.axiam.sdk.internal.SessionState` holding: the per-client `java.net.CookieManager` (wrapped for OkHttp via `JavaNetCookieJar`), the configured `tenantId`, optional org identity (`orgSlug`/`orgId`) plus a `resolvedOrgId()` populated from the access token's `org_id` claim after first login (Pitfall 2), a thread-safe stored CSRF token (`csrfToken()`/`setCsrfToken()`), a non-blocking `cachedAccessToken()`, and a `doHttpRefresh()` Supplier<TokenPair> that performs the actual `POST {baseUrl}/api/v1/auth/refresh` (body includes the resolved `org_id`, non-optional per the real handler) and is passed to `RefreshGuard`. Create `io.axiam.sdk.rest.AuthInterceptor implements okhttp3.Interceptor` per RESEARCH Pattern 3, registered as an APPLICATION interceptor: do a non-blocking `cachedAccessToken()` read (never acquire the guard lock synchronously here); if the token is near expiry per the local JWKS/exp check, proactively call `refreshGuard.refreshIfNeeded(access, session::doHttpRefresh)`; inject `X-Tenant-Id` on every request and `Authorization: Bearer` when a token exists; echo the stored `X-CSRF-Token` on POST/PUT/PATCH/DELETE; after `chain.proceed`, capture any `X-CSRF-Token` response header into the session for the next request. Create `io.axiam.sdk.rest.AuthAuthenticator implements okhttp3.Authenticator`: on a 401, guard against loops via a `priorResponse` count (>=2 returns null, §9.3), call the SAME `refreshGuard.refreshIfNeeded(...)`, and re-issue the request with the new bearer. Create `rest/package-info.java` `@NullMarked`. Write `CsrfInterceptorTest` with `MockWebServer`: a first response sets `X-CSRF-Token: csrf-abc`; assert a subsequent POST carries `X-CSRF-Token: csrf-abc` and every request carries `X-Tenant-Id`; assert a GET does NOT carry the CSRF header.
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml test -Dtest=CsrfInterceptorTest</automated>
  </verify>
  <acceptance_criteria>
    - `CsrfInterceptorTest` proves X-CSRF-Token round-trips (captured from response, echoed on the next mutating request) and X-Tenant-Id is present on every request; GET omits the CSRF header
    - `AuthInterceptor.intercept` performs a non-blocking cached-token read before any refresh (no synchronous `lock.lock()` on the hot path)
    - `AuthAuthenticator` returns null after 2 prior responses (no retry loop, §9.3)
  </acceptance_criteria>
  <done>Session, cookie jar, CSRF capture/echo, tenant header, and proactive+reactive refresh all wired through one guard.</done>
</task>

<task type="auto">
  <name>Task 2: AxiamClient builder (SC#1, D-06/D-09/D-27) + auth methods (login/verifyMfa/refresh/logout)</name>
  <files>sdks/java/src/main/java/io/axiam/sdk/AxiamClient.java, sdks/java/src/test/java/io/axiam/sdk/AxiamClientBuilderTest.java, sdks/java/src/test/java/io/axiam/sdk/rest/AuthFlowTest.java</files>
  <read_first>
    - sdks/go/client.go (NewClient, buildHTTPClient override safety — jar + TLS re-applied) + sdks/go/login.go (login/verifyMfa/refresh/logout, org_id resolution)
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Pattern 1 (lines ~383-449 — Builder(baseUrl, tenantId), orgSlug/orgId, customCa, httpClient override, no no-arg path) + § Common Pitfalls "Pitfall 2" (real login/refresh need org_id/org_slug) + § Standard Stack (mockwebserver 4.x package, Assumption A3)
    - .planning/phases/20-java-sdk/20-PATTERNS.md § "AxiamClient.java + Builder" (tenantId guard, client-override safety, org resolution, CSRF)
    - crates/axiam-api-rest/src/handlers/auth.rs (LoginRequest org_id/org_slug "one required"; RefreshRequest.org_id non-optional; LoginSuccessResponse / mfa challenge_token shape)
    - sdks/CONTRACT.md §5 (tenant required), §1 (method map: login/verifyMfa/refresh/logout), §6 (TLS strict; only customCa), §4 (CookieManager)
  </read_first>
  <action>
    Create `io.axiam.sdk.AxiamClient implements AutoCloseable`. Entry point is `static Builder builder(String baseUrl, String tenantId)` which throws `AuthError` if `tenantId` is null/blank (runtime guard backing the compile-time guarantee); the `Builder` constructor is package-private so there is NO no-arg construction path (SC#1). Builder fluent methods: `orgSlug(String)`/`orgId(UUID)` (mutually exclusive, last-wins — real endpoints need an org identifier, Pitfall 2), `customCa(byte[] pem)` (the ONLY TLS escape hatch, §6), `httpClient(OkHttpClient)` (override), `connectTimeout`/`readTimeout`/`writeTimeout(Duration)` (sane defaults 10s/30s/30s, overridable, D-26). `build()`: construct the effective `OkHttpClient` — if an override was supplied, derive via `overrideClient.newBuilder()` and ALWAYS re-apply the SDK's own `CookieManager`-backed `JavaNetCookieJar` (§4) and strict TLS/trust config (with `customCa` added to the chain if provided) so an override can never drop the jar or weaken TLS (D-27, SC#4). Add the application `AuthInterceptor` + the `AuthAuthenticator`. Instantiate ONE `RefreshGuard`, ONE `SessionState`, ONE `JwksVerifier`; expose package-internal accessors `refreshGuard()`, `tenantId()`, `baseUrl()`, `okHttpClient()`, `customCa()` so the gRPC plan (20-08) can build its channel from the same session/guard without editing this class (documented seam). Implement `close()` (D-09) to shut down the OkHttp dispatcher/executor + connection pool (and later the gRPC channel / AMQP connection if held). Implement `login(String email, String password)` -> `POST /api/v1/auth/login` (body carries tenant + org identifier), returning a typed `LoginResult` (`mfaRequired` flag when the server returns the MFA challenge; capture `challenge_token`), `verifyMfa(...)`, `refresh()` (delegates to the guard), `logout()` (clears cookies/CSRF/local state); add `*Async()` CompletableFuture variants (D-02). Cache the resolved `org_id` from the access token claim after login so `refresh` succeeds. Write `AxiamClientBuilderTest`: assert `builder(base, "")` throws `AuthError`; assert (compile-shape) there is no no-arg builder; assert a supplied `OkHttpClient` with no cookie jar still ends up with a `JavaNetCookieJar` after build (override safety); assert `login` returns a `LoginResult`. Write `AuthFlowTest` (MockWebServer): a login returning the MFA challenge yields `LoginResult.mfaRequired()==true`; a plain login yields `false` + populated tokens; `refresh` posts to `/api/v1/auth/refresh` with the org_id.
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml test -Dtest=AxiamClientBuilderTest,AuthFlowTest</automated>
  </verify>
  <acceptance_criteria>
    - `AxiamClientBuilderTest` proves a blank tenantId throws and a supplied OkHttpClient is rebuilt with the SDK's CookieManager jar + strict TLS (D-27, SC#4)
    - `AuthFlowTest` proves `login` returns a typed `LoginResult`, the MFA branch sets `mfaRequired`, and `refresh` includes `org_id` in the body (Pitfall 2)
    - `AxiamClient` implements `AutoCloseable`; construction of a second client does not share the first client's RefreshGuard (one guard per client)
  </acceptance_criteria>
  <done>tenantId compiler-required (SC#1), override-safe cookie/TLS (D-27), auth flow returns typed results, org_id resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Authz methods checkAccess/can/batchCheck via FND-04 REST (§1, CF-05)</name>
  <files>sdks/java/src/main/java/io/axiam/sdk/AxiamClient.java, sdks/java/src/test/java/io/axiam/sdk/rest/AuthzTest.java</files>
  <read_first>
    - sdks/go/authz.go (checkAccess/batchCheck request/response shapes, error mapping) 
    - crates/axiam-api-rest/src/handlers/authz_check.rs (POST /api/v1/authz/check and /api/v1/authz/check/batch request `{action, resource_id, scope?}` -> `{allowed, reason?}`; batch order preserved)
    - .planning/phases/20-java-sdk/20-PATTERNS.md § "rest/*Authz*" + § Shared Patterns (redact-before-wrap error handling applies to authz call sites too)
    - sdks/CONTRACT.md §1 (checkAccess/can/batchCheck), §2 (status mapping via ErrorMapper from 20-03)
  </read_first>
  <action>
    Add authz methods to `AxiamClient`: `checkAccess(action, resourceId, scope?)` -> `POST /api/v1/authz/check` returning a typed result record (`allowed`, nullable `reason`); `can(...)` as the browser-alias identical call; `batchCheck(List<...>)` -> `POST /api/v1/authz/check/batch` returning results in input order. Route all non-2xx responses through the central `ErrorMapper.fromHttpStatus(...)` (20-03) so 403/409 -> `AuthzError`, 401 -> `AuthError` (triggering refresh), transport failures -> `NetworkError` (redact-before-wrap). Wrap the authz-check reads (idempotent) in the `internal.Retry` helper (20-04) so a transient `NetworkError`/429/503 backs off and retries per D-26 — but NEVER auto-retry the state-changing auth calls (login/verifyMfa/logout). Add `*Async()` variants. Write `AuthzTest` (MockWebServer): a 200 `{allowed:true}` yields allowed; a 200 `{allowed:false, reason:"..."}` yields the reason; a 403 yields `AuthzError`; a `batchCheck` of 3 returns 3 results in order.
  </action>
  <verify>
    <automated>mvn -f sdks/java/pom.xml test -Dtest=AuthzTest</automated>
  </verify>
  <acceptance_criteria>
    - `AuthzTest` proves checkAccess/can hit `/api/v1/authz/check`, batchCheck hits `/api/v1/authz/check/batch` and preserves input order
    - a 403 maps to `AuthzError` and a transport failure to `NetworkError` via the central mapper (no divergent local mapping)
  </acceptance_criteria>
  <done>checkAccess/can/batchCheck implemented over FND-04 with §2-conformant error mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer -> AXIAM REST API | tenant/org context + CSRF must be attached; TLS strict |
| supplied OkHttpClient override -> SDK invariants | override must not drop the cookie jar or weaken TLS |
| access token -> transport | tokens flow via httpOnly cookie jar, never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering/Info-Disclosure | TLS + cookie jar on a supplied OkHttpClient | high | mitigate | D-27 always re-applies CookieManager + strict TLS via newBuilder(); only `customCa` escape hatch; SC#4 (grep gate from 20-01 covers rest/) |
| T-20-05 | DoS (self) | concurrent 401 refresh | high | mitigate | one shared `RefreshGuard`; interceptor + authenticator both funnel into it (D-08) |
| T-20-09 | Spoofing (CSRF) | state-changing REST calls | medium | mitigate | §3 capture X-CSRF-Token from response, echo on POST/PUT/PATCH/DELETE (CsrfInterceptorTest) |
| T-20-02 | Info-Disclosure | authz/auth error surfaces | high | mitigate | all error construction routed through `ErrorMapper` redact-before-wrap (20-03) |
</threat_model>

<verification>
- `mvn -f sdks/java/pom.xml test -Dtest=AxiamClientBuilderTest,CsrfInterceptorTest,AuthFlowTest,AuthzTest` green.
- `bash sdks/java/scripts/tls-bypass-gate.sh` still exits 0 with the new rest/ source.
</verification>

<success_criteria>
The REST surface satisfies SC#1 (io.axiam:axiam-sdk POM, tenantId compiler-required, login -> LoginResult) and the REST half of SC#4 (CookieManager + no TLS bypass), exposes the shared-guard seam for gRPC, and maps all errors per §2.
</success_criteria>

<output>
Create `.planning/phases/20-java-sdk/20-05-SUMMARY.md` when done.
</output>
