---
phase: 20-java-sdk
plan: 09
type: execute
wave: 5
depends_on: ["20-05", "20-06", "20-07", "20-08"]
files_modified:
  - sdks/java/examples/login-mfa/LoginMfaExample.java
  - sdks/java/examples/rest-authz/RestAuthzExample.java
  - sdks/java/examples/grpc-checkaccess/GrpcCheckAccessExample.java
  - sdks/java/examples/amqp-consumer/AmqpConsumerExample.java
  - sdks/java/examples/spring-boot-app/pom.xml
  - sdks/java/examples/spring-boot-app/src/main/java/io/axiam/example/springboot/SpringBootExampleApplication.java
  - sdks/java/examples/spring-boot-app/src/main/java/io/axiam/example/springboot/SecurityConfig.java
  - sdks/java/examples/spring-boot-app/src/main/java/io/axiam/example/springboot/HelloController.java
  - sdks/java/examples/spring-boot-app/src/test/java/io/axiam/example/springboot/SpringBootExampleIT.java
  - sdks/java/README.md
  - sdks/java-bom/pom.xml
  - .github/workflows/sdk-ci-java.yml
autonomous: true
requirements: [JAVA-01]
user_setup:
  - service: sonatype-central-portal
    why: "Publish io.axiam:axiam-sdk + io.axiam:axiam-bom to Maven Central (D-22, SC#5); namespace io.axiam must be verified"
    env_vars:
      - name: CENTRAL_TOKEN_USERNAME
        source: "Sonatype Central Portal -> Account -> Generate User Token (username half)"
      - name: CENTRAL_TOKEN_PASSWORD
        source: "Sonatype Central Portal -> Account -> Generate User Token (password half)"
      - name: GPG_PRIVATE_KEY
        source: "base64-encoded ASCII-armored real signing key (repo secret; tag-publish job ONLY, never PR jobs)"
      - name: GPG_PASSPHRASE
        source: "passphrase for the real GPG signing key (repo secret; tag-publish job ONLY)"
    dashboard_config:
      - task: "Verify the io.axiam namespace (DNS TXT or GitHub-repo ownership proof)"
        location: "Sonatype Central Portal -> Namespaces"
must_haves:
  truths:
    - "`mvn -f sdks/java/pom.xml verify -Dgpg.skip=false` passes including GPG signing (ephemeral key) + attaches javadoc/sources jars (SC#5)"
    - "the complete Spring Boot example app boots a full application context and its protected endpoint is guarded by the SDK filter (SC#3)"
    - "sdk-ci-java.yml runs mvn test, the TLS-bypass grep gate, ephemeral-key signed verify, and a tag-triggered Central Portal publish for both io.axiam:axiam-sdk and io.axiam:axiam-bom; the real GPG secret is scoped to the tag job only"
  artifacts:
    - sdks/java/examples/spring-boot-app/
    - sdks/java-bom/pom.xml
    - .github/workflows/sdk-ci-java.yml
    - sdks/java/README.md
  key_links:
    - "tag sdks/java/vX.Y.Z -> publish job (real GPG + Central token) -> Maven Central (Phase 15 D-13 tag scheme)"
    - "gpg.skip=true default -> PR job flips to false with an EPHEMERAL key -> real key never in a PR job (Pitfall 4)"
    - "example SecurityConfig SecurityFilterChain @Bean -> AxiamAuthenticationFilter (20-06) -> protected endpoint (SC#3)"
---

<objective>
Complete the phase deliverables: runnable per-capability examples (login+MFA, REST authz, gRPC CheckAccess, AMQP consumer); the COMPLETE working Spring Boot 3.x example application context wiring the SDK filter explicitly in a `SecurityFilterChain @Bean` and protecting an endpoint (SC#3's "complete working application context"); the README conformance statement (§1–§10); the published BOM coordinate `io.axiam:axiam-bom` (D-23); and the full Java CI workflow — `mvn test`, the TLS-bypass grep gate (SC#4), `mvn verify -Dgpg.skip=false` with an ephemeral signing key + javadoc/sources jars (SC#5), a gRPC-codegen drift check (D-21), and a tag-triggered (`sdks/java/vX.Y.Z`) Sonatype Central Portal publish job whose real GPG secret is scoped exclusively to the tag job (Pitfall 4).

Purpose: Closes SC#3 (complete app context) and SC#5 (mvn verify incl. signing + operational publish pipeline), and lands the BOM the requirement was reconciled to include.
Output: examples, complete Spring Boot app, README, BOM pom, CI workflow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@sdks/CONTRACT.md
@.planning/phases/20-java-sdk/20-RESEARCH.md
@.planning/phases/20-java-sdk/20-VALIDATION.md
@.github/workflows/sdk-ci-python.yml
@sdks/java/pom.xml
</context>

<artifacts_this_phase_produces>
- `sdks/java/examples/{login-mfa,rest-authz,grpc-checkaccess,amqp-consumer}/*.java` — 4 runnable capability examples importing only public entry points.
- `sdks/java/examples/spring-boot-app/` — complete Spring Boot 3.x app: `pom.xml` (depends on io.axiam:axiam-sdk + spring-boot-starter-web/security), `SpringBootExampleApplication`, `SecurityConfig` (explicit `SecurityFilterChain @Bean` adding `AxiamAuthenticationFilter` before `UsernamePasswordAuthenticationFilter`, CSRF disabled since AXIAM handles §3), `HelloController` (protected `/hello`), and `SpringBootExampleIT` (`@SpringBootTest` boots the context + asserts the protected endpoint is guarded).
- `sdks/java/README.md` — updated getting-started + the mandatory conformance line "This SDK conforms to CONTRACT.md §1–§10."
- `sdks/java-bom/pom.xml` — `io.axiam:axiam-bom` (`packaging=pom`) with `<dependencyManagement>` pinning `io.axiam:axiam-sdk`; full POM metadata + signing plugins mirroring the SDK (D-23).
- `.github/workflows/sdk-ci-java.yml` — PR-gate job (setup-java 21, mvn test, tls-bypass-gate.sh, `mvn verify -Dgpg.skip=false` with an ephemeral GPG key, `mvn dependency:tree` re-verification, codegen drift-check) + tag-triggered publish job (real GPG import + Central Portal deploy for both coordinates); paths filter includes `sdks/java/**` and `sdks/java-bom/**`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Four capability examples + README conformance statement (D-13, §1–§10)</name>
  <files>sdks/java/examples/login-mfa/LoginMfaExample.java, sdks/java/examples/rest-authz/RestAuthzExample.java, sdks/java/examples/grpc-checkaccess/GrpcCheckAccessExample.java, sdks/java/examples/amqp-consumer/AmqpConsumerExample.java, sdks/java/README.md</files>
  <read_first>
    - sdks/java/README.md (existing scaffold README — preserve coordinates + reserved-namespace note; add the conformance statement)
    - sdks/python/examples (sibling example structure, for parity of which capabilities get an example) if present
    - sdks/CONTRACT.md Closing Notes (mandatory README conformance line) + §1 method map
    - AxiamClient/GrpcAuthzClient/AmqpConsumer public APIs from 20-05/20-07/20-08
  </read_first>
  <action>
    Write four small runnable examples, each importing ONLY public SDK entry points (`io.axiam.sdk.AxiamClient`, `io.axiam.sdk.grpc.GrpcAuthzClient`, `io.axiam.sdk.amqp.AmqpConsumer`): (1) `LoginMfaExample` — `AxiamClient.builder(baseUrl, tenantId).build()` then `login` -> branch on `LoginResult.mfaRequired()` -> `verifyMfa`; (2) `RestAuthzExample` — `can`/`checkAccess`/`batchCheck` over REST; (3) `GrpcCheckAccessExample` — `GrpcAuthzClient.checkAccess`/`batchCheck`; (4) `AmqpConsumerExample` — `AmqpConsumer.consume(...)` with a handler and an `ErrDrop` demonstration. Update `sdks/java/README.md` with a getting-started (Maven `<dependency>` + Gradle snippets for `io.axiam:axiam-sdk`, `tenantId` required, `try`-with-resources lifecycle) and the EXACT mandatory line: "This SDK conforms to CONTRACT.md §1–§10." Mention the BOM (`io.axiam:axiam-bom`) for version alignment.
  </action>
  <verify>
    <automated>test -f sdks/java/examples/login-mfa/LoginMfaExample.java && grep -q "conforms to CONTRACT.md" sdks/java/README.md</automated>
  </verify>
  <acceptance_criteria>
    - all four example files exist and reference only public entry points (no `io.axiam.sdk.internal.*` imports)
    - `grep -c "This SDK conforms to CONTRACT.md" sdks/java/README.md` returns >= 1
    - README documents `io.axiam:axiam-sdk`, the required `tenantId`, and the `io.axiam:axiam-bom` coordinate
  </acceptance_criteria>
  <done>Runnable examples cover every capability; README states §1–§10 conformance and both coordinates.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Spring Boot 3.x example app + boot integration test (SC#3)</name>
  <files>sdks/java/examples/spring-boot-app/pom.xml, sdks/java/examples/spring-boot-app/src/main/java/io/axiam/example/springboot/SpringBootExampleApplication.java, sdks/java/examples/spring-boot-app/src/main/java/io/axiam/example/springboot/SecurityConfig.java, sdks/java/examples/spring-boot-app/src/main/java/io/axiam/example/springboot/HelloController.java, sdks/java/examples/spring-boot-app/src/test/java/io/axiam/example/springboot/SpringBootExampleIT.java</files>
  <read_first>
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Pattern 8 "Explicit SecurityFilterChain wiring (example app, SC#3)" (lines ~1099-1130 — SecurityConfig @Bean adds AxiamAuthenticationFilter before UsernamePasswordAuthenticationFilter, CSRF disabled since AXIAM §3 supersedes) + D-15/D-16 (Spring Boot 3.2+/Security 6.x, jakarta.*)
    - .planning/phases/20-java-sdk/20-VALIDATION.md § Per-Task Verification Map (SC#3 row: `mvn -f sdks/java/examples/spring-boot-app/pom.xml verify` compiles + boots)
    - io.axiam.sdk.spring.AxiamAuthenticationFilter (20-06) public constructor
  </read_first>
  <action>
    Create the complete Spring Boot 3.x example under `examples/spring-boot-app/` with its own `pom.xml` (Java 21, spring-boot-starter-parent 3.5.x line, dependencies on `io.axiam:axiam-sdk` + spring-boot-starter-web + spring-boot-starter-security). `SpringBootExampleApplication` (`@SpringBootMain`). `SecurityConfig` (`@Configuration`) with a `SecurityFilterChain @Bean` that: constructs a `JwksVerifier` (or receives one), constructs `AxiamAuthenticationFilter(verifier, "<example-tenant>")`, disables Spring's own CSRF (AXIAM's §3 X-CSRF-Token flow supersedes it — do not double-protect), permits `/public/**`, requires auth for everything else, and `addFilterBefore(axiamFilter, UsernamePasswordAuthenticationFilter.class)`. `HelloController` with a protected `GET /hello` returning the authenticated principal. `SpringBootExampleIT` (`@SpringBootTest` + `MockMvc`/`TestRestTemplate`): asserts the context boots (proving a "complete working application context", SC#3) and that `/hello` returns 401 without a valid token and 200 with one whose tenant matches. Point the example's `io.axiam:axiam-sdk` dependency at the phase version so the example builds against the just-built SDK.
  </action>
  <verify>
    <automated>mvn -f sdks/java/examples/spring-boot-app/pom.xml -q -DskipTests package && mvn -f sdks/java/examples/spring-boot-app/pom.xml test -Dtest=SpringBootExampleIT</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -f sdks/java/examples/spring-boot-app/pom.xml verify` compiles and boots the full application context against Spring Boot 3.x (SC#3)
    - `SpringBootExampleIT` proves `/hello` is protected: 401 without a token, 200 with a matching-tenant token
    - `SecurityConfig` wires `AxiamAuthenticationFilter` via an explicit `SecurityFilterChain @Bean` (SC#3 "complete working application context")
  </acceptance_criteria>
  <done>A complete, bootable Spring Boot 3.x app protects an endpoint with the SDK filter (SC#3).</done>
</task>

<task type="auto">
  <name>Task 3: BOM coordinate + full CI/publish pipeline (D-22/D-23, SC#4/SC#5, Pitfall 4)</name>
  <files>sdks/java-bom/pom.xml, .github/workflows/sdk-ci-java.yml</files>
  <read_first>
    - .github/workflows/sdk-ci-python.yml (structure: paths filter, PR-gate vs tag-triggered publish job separation, secret-scoping to the tag job)
    - .planning/phases/20-java-sdk/20-RESEARCH.md § Code Examples "mvn verify recipe" (lines ~1569-1584 — ephemeral key for PR gate, real key import for tag publish) + § Common Pitfalls "Pitfall 4" (gpg.skip default true; real key ONLY in the tag job) + "TLS-bypass CI grep gate" + § Standard Stack (central-publishing-maven-plugin, javadoc/sources required)
    - .planning/phases/20-java-sdk/20-VALIDATION.md § Manual-Only Verifications (live first publish may be a maintainer action) + § Wave 0 Requirements (sdk-ci-java.yml needs mvn test/gate/verify/drift/publish)
    - sdks/CONTRACT.md closing notes (tag scheme `sdks/<lang>/vX.Y.Z`, Phase 15 D-13) + existing .github/workflows/sdk-ci-java.yml (currently only a scaffold-check job — replace/extend)
  </read_first>
  <action>
    Create `sdks/java-bom/pom.xml`: `io.axiam:axiam-bom` with `<packaging>pom</packaging>`, complete POM metadata (licenses/scm/developers/url mirroring the SDK, D-22), a `<dependencyManagement>` entry pinning `io.axiam:axiam-sdk` at the phase version, and the same gpg + central-publishing plugin chain (so the BOM is also GPG-signed and publishable, D-23). Rewrite `.github/workflows/sdk-ci-java.yml`: keep the `paths` filter but ADD `sdks/java-bom/**`; a PR-gate job on ubuntu-latest that (1) `actions/setup-java@<pinned>` java-version 21, (2) `mvn -f sdks/java/pom.xml test`, (3) `bash sdks/java/scripts/tls-bypass-gate.sh` (SC#4), (4) generates an EPHEMERAL throwaway GPG key (`gpg --quick-generate-key ... 1d`) and runs `mvn -f sdks/java/pom.xml verify -Dgpg.skip=false` (proves SC#5 signing + javadoc/sources jars WITHOUT any real secret — Pitfall 4), (5) `mvn -f sdks/java/pom.xml dependency:tree` re-verification (RESEARCH Assumptions A1/A6), (6) a gRPC-codegen drift check (`mvn generate-sources` succeeds; assert no generated `.java` under sdks/java/src, D-21); and a SEPARATE tag-triggered publish job (trigger on tags matching `sdks/java/v*`) that imports the REAL `GPG_PRIVATE_KEY`/`GPG_PASSPHRASE` from repo secrets and runs `mvn deploy -Dgpg.skip=false` with Central Portal credentials (`CENTRAL_TOKEN_*`) for BOTH `sdks/java/pom.xml` and `sdks/java-bom/pom.xml`. The real GPG/Central secrets MUST be referenced ONLY in the tag-triggered job, never the PR job. Pin all third-party actions by commit SHA (matching repo convention); if a new SHA cannot be sourced in this environment, pin by major tag and flag it for a maintainer follow-up (consistent with 19-07). The live first Central publish is a maintainer action if the namespace/creds are absent (declared in user_setup / Manual-Only).
  </action>
  <verify>
    <automated>mvn -f sdks/java-bom/pom.xml validate && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/sdk-ci-java.yml')); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `sdks/java-bom/pom.xml` validates, is `packaging=pom`, and pins `io.axiam:axiam-sdk` in `<dependencyManagement>`
    - `sdk-ci-java.yml` parses as valid YAML; the PR job runs test + tls-bypass-gate + `mvn verify -Dgpg.skip=false` (ephemeral key) + drift-check; a SEPARATE tag-triggered (`sdks/java/v*`) job publishes both coordinates
    - the strings `GPG_PRIVATE_KEY`/`GPG_PASSPHRASE`/`CENTRAL_TOKEN_*` appear ONLY in the tag-triggered job, never the pull_request job (Pitfall 4)
  </acceptance_criteria>
  <done>BOM coordinate published + signed; CI proves SC#4/SC#5 with an ephemeral key on PRs and scopes real secrets to the tag-publish job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted fork PR -> CI secrets | PR jobs must never receive the real signing key / Central token |
| tag push -> Maven Central | only a verified-namespace, signed artifact may publish |
| consumer app request -> example protected endpoint | example proves the filter guards real endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-12 | Info-Disclosure/Spoofing | real GPG key exposure via PR-triggered CI | high | mitigate | gpg.skip=true default; PR job uses an ephemeral throwaway key; real `GPG_PRIVATE_KEY`/`GPG_PASSPHRASE` scoped to the tag-publish job only (Pitfall 4) |
| T-20-08 | Spoofing | `io.axiam` Maven Central namespace/coordinate squatting | medium | transfer | Sonatype Central Portal namespace ownership verification (platform control); both coordinates (sdk + bom) signed + published under the verified namespace |
| T-20-01 | Tampering | TLS bypass entering examples | high | mitigate | tls-bypass-gate.sh runs in CI over src + examples (SC#4) |
| T-20-SC | Tampering | dependency drift / unverified patch versions | medium | mitigate | CI `mvn dependency:tree` re-verification step + pinned versions |
</threat_model>

<verification>
- `mvn -f sdks/java/pom.xml verify -Dgpg.skip=false` (ephemeral key) passes incl. signing + javadoc/sources (SC#5).
- `mvn -f sdks/java/examples/spring-boot-app/pom.xml verify` boots the complete context (SC#3).
- `sdks/java-bom/pom.xml` validates; `sdk-ci-java.yml` is valid YAML with real secrets scoped to the tag job only.
- `bash sdks/java/scripts/tls-bypass-gate.sh` exits 0 over the full tree incl. examples.
</verification>

<success_criteria>
SC#3 (complete Spring Boot app context) and SC#5 (mvn verify incl. GPG signing + operational Central Portal publish pipeline) are proven; the BOM coordinate is published/signed; examples cover every capability; and the README states §1–§10 conformance — completing the Java SDK.
</success_criteria>

<output>
Create `.planning/phases/20-java-sdk/20-09-SUMMARY.md` when done. Note in the summary that the live first Maven Central publish is a maintainer action pending io.axiam namespace verification + CI secrets (D-22 deferred).
</output>
