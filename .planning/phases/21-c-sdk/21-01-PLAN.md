---
phase: 21-c-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdks/csharp/Axiam.Sdk/Axiam.Sdk.csproj
  - sdks/csharp/Axiam.Sdk.AspNetCore/Axiam.Sdk.AspNetCore.csproj
  - sdks/csharp/Axiam.Sdk.sln
  - sdks/csharp/.gitignore
  - sdks/csharp/Axiam.Sdk/Core/Sensitive.cs
  - sdks/csharp/Axiam.Sdk/Core/ErrorMapper.cs
  - sdks/csharp/Axiam.Sdk/Core/NetworkError.cs
  - sdks/csharp/Axiam.Sdk/Core/AuthError.cs
  - sdks/csharp/Axiam.Sdk/Core/AuthzError.cs
  - sdks/csharp/Axiam.Sdk/Auth/TokenPair.cs
  - sdks/csharp/tests/Axiam.Sdk.Tests/Axiam.Sdk.Tests.csproj
  - sdks/csharp/tests/Axiam.Sdk.Tests/Fixtures/amqp_hmac_vectors.json
  - sdks/csharp/tests/Axiam.Sdk.Tests/Fixtures/JwksFixture.cs
  - sdks/csharp/tests/Axiam.Sdk.Tests/SensitiveRedactionTests.cs
autonomous: true
requirements: [CS-01]

must_haves:
  truths:
    - "`dotnet build sdks/csharp/Axiam.Sdk` compiles both packages and generates gRPC stubs from proto at build time (SC#4 codegen half)."
    - "A raw token value never appears in NetworkError's ToString/JSON/log output (CR-04 carry-forward, D-12)."
    - "`Sensitive<T>.ToString()` and its JSON converter always emit `[SENSITIVE]` (§7)."
  artifacts:
    - "sdks/csharp/Axiam.Sdk/Axiam.Sdk.csproj (net8.0, pinned deps, Grpc.Tools <Protobuf> codegen)"
    - "sdks/csharp/Axiam.Sdk.AspNetCore/Axiam.Sdk.AspNetCore.csproj (FrameworkReference Microsoft.AspNetCore.App)"
    - "sdks/csharp/Axiam.Sdk/Core/Sensitive.cs, ErrorMapper.cs, NetworkError.cs, AuthError.cs, AuthzError.cs"
    - "sdks/csharp/tests/Axiam.Sdk.Tests/ (xUnit scaffold + fixtures + SensitiveRedactionTests)"
  key_links:
    - "Grpc.Tools <Protobuf Include=../../../proto/axiam/v1/*.proto GrpcServices=Client> generates stubs into obj/ (gitignored)."
    - "Every error surfaces through ErrorMapper; NetworkError has a single redact-before-wrap construction path."
---

<objective>
Establish the `sdks/csharp/` solution foundation: the two csproj packages (`Axiam.Sdk` core + `Axiam.Sdk.AspNetCore` companion) with all pinned dependencies and `Grpc.Tools` build-time gRPC codegen (D-05, the documented buf exception), the shared Core primitives (`Sensitive<T>`, the central error taxonomy with redact-before-wrap `NetworkError`), and the xUnit test project + committed cross-SDK fixtures. This is the Wave 0 scaffold every downstream plan builds on.

Purpose: Unblock all Wave 2+ parallel work with a compiling solution, a shared token-safety/error vocabulary (D-12), and the test harness + fixtures (per 21-VALIDATION.md Wave 0 Requirements).
Output: A buildable two-package solution, gRPC codegen wired, Core primitives, and a green `SensitiveRedactionTests` proving the CR-04 leak class is closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-c-sdk/21-CONTEXT.md
@.planning/phases/21-c-sdk/21-RESEARCH.md
@.planning/phases/21-c-sdk/21-PATTERNS.md
@.planning/phases/21-c-sdk/21-VALIDATION.md
@sdks/CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-package solution scaffold + Grpc.Tools MSBuild codegen (D-01, D-03, D-05)</name>
  <files>sdks/csharp/Axiam.Sdk/Axiam.Sdk.csproj, sdks/csharp/Axiam.Sdk.AspNetCore/Axiam.Sdk.AspNetCore.csproj, sdks/csharp/Axiam.Sdk.sln, sdks/csharp/.gitignore</files>
  <read_first>
    - sdks/csharp/Axiam.Sdk/Axiam.Sdk.csproj (existing scaffold — net8.0, PackageId=Axiam.Sdk, Nullable=enable, Apache-2.0; extend, do not recreate the PropertyGroup identity)
    - .planning/phases/21-c-sdk/21-RESEARCH.md § Standard Stack (Core table: exact pinned versions) + Pattern 4 (Grpc.Tools <Protobuf> item + PrivateAssets/IncludeAssets)
    - sdks/CONTRACT.md § "C# Grpc.Tools Exception" (lines 321-323) — the buf-pipeline deviation this codegen implements
    - .planning/phases/21-c-sdk/21-CONTEXT.md D-01/D-03/D-05
    - proto/axiam/v1/authorization.proto, user.proto, token.proto (the proto surface the <Protobuf> item globs)
  </read_first>
  <action>
    Extend `Axiam.Sdk.csproj` (keep existing identity PropertyGroup) with pinned PackageReferences per RESEARCH § Standard Stack: `Grpc.Net.Client` 2.80.0, `Google.Protobuf` (let restore resolve the 2.80.x-compatible version, then lock it — Open Question 2), `RabbitMQ.Client` 7.2.1, `BouncyCastle.Cryptography` 2.6.2, `Microsoft.Extensions.Http` 8.0.1, `Microsoft.Extensions.Logging.Abstractions` 8.0.3. Add `Grpc.Tools` 2.80.0 as a build-only reference with `<PrivateAssets>all</PrivateAssets>` and `<IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>`. Add a `<Protobuf>` ItemGroup: `<Protobuf Include="../../../proto/axiam/v1/*.proto" GrpcServices="Client" ProtoRoot="../../../proto" />` (client-only codegen — the SDK never hosts a gRPC server). Enable deterministic build (`<Deterministic>true</Deterministic>`) and `<GenerateDocumentationFile>true</GenerateDocumentationFile>`. Do NOT commit generated sources — they land in `obj/` (D-05). Create `Axiam.Sdk.AspNetCore.csproj` (net8.0, PackageId=Axiam.Sdk.AspNetCore, Nullable=enable, Apache-2.0, `<FrameworkReference Include="Microsoft.AspNetCore.App" />`, `<ProjectReference Include="../Axiam.Sdk/Axiam.Sdk.csproj" />`) — this keeps core framework-free per D-03. Create `Axiam.Sdk.sln` referencing both projects + the test project. Create `sdks/csharp/.gitignore` ignoring `obj/`, `bin/`, and generated stub output. Do NOT add a C# entry to `sdks/buf.gen.yaml` — C# is the codegen exception. Note the CS-01 divergence: BouncyCastle is used (not "native EdDSA") per the resolved D-02 research flag — record this in a csproj XML comment referencing 21-RESEARCH.md Pitfall 1.
  </action>
  <acceptance_criteria>
    - `dotnet build sdks/csharp/Axiam.Sdk -c Release` succeeds and the gRPC client stub types (`Axiam.V1.AuthorizationService*`) are generated under `obj/` (SC#4 codegen half).
    - `dotnet build sdks/csharp/Axiam.Sdk.AspNetCore -c Release` succeeds with the ASP.NET Core FrameworkReference resolving.
    - Source assertion: `Axiam.Sdk.csproj` contains `Include="../../../proto/axiam/v1/*.proto"`, `GrpcServices="Client"`, `Version="2.80.0"` (Grpc.Net.Client), `Version="2.6.2"` (BouncyCastle.Cryptography), `Version="7.2.1"` (RabbitMQ.Client).
    - `sdks/buf.gen.yaml` is unchanged (no csharp plugin entry added).
    - `git check-ignore sdks/csharp/Axiam.Sdk/obj/Debug/net8.0` returns the path (obj is ignored).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build sdks/csharp/Axiam.Sdk -c Release && dotnet build sdks/csharp/Axiam.Sdk.AspNetCore -c Release</automated>
  </verify>
  <done>Both packages compile on net8.0; gRPC stubs generate at build time from proto (D-05); AspNetCore references core + ASP.NET Core shared framework; obj/ gitignored; buf.gen.yaml untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core primitives — Sensitive&lt;T&gt; + central error taxonomy with redact-before-wrap (D-12, §2, §7, CR-04)</name>
  <files>sdks/csharp/Axiam.Sdk/Core/Sensitive.cs, sdks/csharp/Axiam.Sdk/Core/ErrorMapper.cs, sdks/csharp/Axiam.Sdk/Core/NetworkError.cs, sdks/csharp/Axiam.Sdk/Core/AuthError.cs, sdks/csharp/Axiam.Sdk/Core/AuthzError.cs, sdks/csharp/Axiam.Sdk/Auth/TokenPair.cs</files>
  <read_first>
    - .planning/phases/21-c-sdk/21-RESEARCH.md Pattern 7 (Sensitive struct + SensitiveJsonConverter + NetworkError.FromResponse/FromException/SanitizeMessage — the primary template)
    - .planning/phases/21-c-sdk/21-PATTERNS.md § ErrorMapper/NetworkError (Java+Go analogs: single choke-point constructor, sensitiveResponseHeaders list) + § Sensitive (Java invariants)
    - sdks/java/src/main/java/io/axiam/sdk/errors/ErrorMapper.java, NetworkError.java (choke-point + no-live-response-overload invariant)
    - sdks/go/errors.go (sanitizeResponse/newNetworkError — redact-before-wrap reference)
    - sdks/typescript/src/core/errorMapper.ts (sanitizeAxiosError — the original Phase 17 CR-04 fix)
    - sdks/CONTRACT.md §2 (error taxonomy + status mapping) + §7 (Sensitive C# row: "Struct with ToString() override returning [SENSITIVE]")
  </read_first>
  <behavior>
    - `Sensitive<T>.ToString()` returns `[SENSITIVE]`; `SensitiveJsonConverter<T>.Write` emits `[SENSITIVE]`; `Read` throws NotSupported. Reveal() is internal-only.
    - `ErrorMapper` maps HTTP status → error class: 401 → AuthError; 403 and 409 → AuthzError; all other non-2xx → NetworkError. gRPC status: UNAUTHENTICATED → AuthError; PERMISSION_DENIED → AuthzError; else → NetworkError. Both transports route through this one mapper.
    - `NetworkError.FromResponse(response, context)` strips `Set-Cookie`/`Authorization`/`Cookie` headers before building the message; the live `HttpResponseMessage` is never stored as InnerException/Data. `SanitizeMessage` regex-redacts any leaked `set-cookie|authorization|cookie: ...` fragment.
    - There is exactly one construction path that accepts a live response; no public constructor accepts a raw response (structural CR-04 defense, mirrors Java).
  </behavior>
  <action>
    Implement `readonly struct Sensitive<T>` with an internal constructor, internal `Reveal()`, `ToString() => "[SENSITIVE]"`, and a `[JsonConverter(typeof(SensitiveJsonConverter<T>))]` attribute wiring `SensitiveJsonConverter<T>` (Write emits `[SENSITIVE]`, Read throws `NotSupportedException`) — do NOT override `Equals`/`GetHashCode` in a way that could leak the value (§7 invariant from PATTERNS.md). Add a `Sensitive.Of<T>(T)` internal factory. Implement `TokenPair` as `sealed record TokenPair(Sensitive<string> AccessToken, Sensitive<string> RefreshToken, DateTimeOffset ExpiresAt)`. Implement `AuthError`/`AuthzError`/`NetworkError` (all `sealed class : Exception`); `NetworkError` per RESEARCH Pattern 7 — private base ctor, static `FromResponse(HttpResponseMessage, string)` and `FromException(Exception, string)` factories only, plus `SanitizeMessage`. Implement `ErrorMapper` as the single status→error source of truth (HTTP §2 table + gRPC `StatusCode` codes) so REST and gRPC cannot drift. Every error class references decision `per D-12`.
  </action>
  <acceptance_criteria>
    - Source assertion: `NetworkError.cs` exposes no public/internal constructor taking `HttpResponseMessage`; the only response-accepting member is the static `FromResponse`. The header filter references the literal set Set-Cookie, Authorization, Cookie.
    - Source assertion: `ErrorMapper.cs` maps 401→AuthError, 403/409→AuthzError, UNAUTHENTICATED→AuthError, PERMISSION_DENIED→AuthzError.
    - `dotnet build sdks/csharp/Axiam.Sdk` succeeds.
    - Behaviors above are covered by `SensitiveRedactionTests` (Task 3).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build sdks/csharp/Axiam.Sdk -c Release</automated>
  </verify>
  <done>Sensitive&lt;T&gt; redacts across ToString + JSON; TokenPair wraps tokens in Sensitive; the error taxonomy routes through one mapper; NetworkError has a single redact-before-wrap construction path with no live-response overload.</done>
</task>

<task type="auto">
  <name>Task 3: xUnit test project + cross-SDK fixtures + SensitiveRedactionTests (Wave 0, CR-04)</name>
  <files>sdks/csharp/tests/Axiam.Sdk.Tests/Axiam.Sdk.Tests.csproj, sdks/csharp/tests/Axiam.Sdk.Tests/Fixtures/amqp_hmac_vectors.json, sdks/csharp/tests/Axiam.Sdk.Tests/Fixtures/JwksFixture.cs, sdks/csharp/tests/Axiam.Sdk.Tests/SensitiveRedactionTests.cs</files>
  <read_first>
    - .planning/phases/21-c-sdk/21-VALIDATION.md § Wave 0 Requirements (test project + HMAC + JWKS fixtures) + § Test Infrastructure (xUnit 2.9.3, Moq 4.20.72, Category=Fast filter)
    - sdks/java/src/test/resources/amqp_hmac_vectors.json (the committed Rust-signed HMAC fixture to copy verbatim — PATTERNS.md line 144: "do not regenerate; same vectors guarantee cross-SDK byte-for-byte parity")
    - .planning/phases/21-c-sdk/21-RESEARCH.md § Validation Architecture (Wave 0 Gaps) + Pattern 1 (JWK shape: kty=OKP, crv=Ed25519, x=base64url raw 32-byte key, kid) + Pattern 7 regression-test shape (non-vacuous control case)
    - sdks/java/src/test/java/io/axiam/sdk/errors/ErrorRedactionTest.java (non-vacuous control-case analog)
  </read_first>
  <action>
    Create `Axiam.Sdk.Tests.csproj` (net8.0) referencing `xunit` 2.9.3, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`, `Moq` 4.20.72, and a `<ProjectReference>` to `Axiam.Sdk` (BouncyCastle flows transitively for the JWKS fixture signer). Glob fixtures with `<None Include="Fixtures/**" CopyToOutputDirectory="PreserveNewest" />` so fixtures added by later plans are copied automatically. Copy `sdks/java/src/test/resources/amqp_hmac_vectors.json` byte-for-byte to `Fixtures/amqp_hmac_vectors.json` (reused by 21-02's HMAC tests). Create `Fixtures/JwksFixture.cs` — a committed helper that generates an Ed25519 keypair via BouncyCastle `Ed25519KeyPairGenerator`, builds an AXIAM-shaped JWKS document (`kty=OKP`, `crv=Ed25519`, `x`=base64url raw public key, deterministic `kid`), and signs an AXIAM-shaped JWT (header `alg=EdDSA,kid`; payload with `sub`, `tenant_id`, `roles`, `exp`) using `Ed25519Signer(forSigning:true)` — the signer path is independent of `JwksVerifier`'s verify path, so consuming tests are NOT a vacuous self-round-trip. Expose helpers to mint tampered-signature and wrong-tenant variants. Add `SensitiveRedactionTests.cs` (`[Trait("Category","Fast")]`): construct a `NetworkError.FromResponse` from a fixture `HttpResponseMessage` carrying `Set-Cookie: axiam_access=abc123token`; assert `"abc123token"` never appears in `ToString()`, `Message`, or `JsonSerializer.Serialize(error)`; include a non-vacuous control asserting a non-secret header (`Content-Type`) DOES survive.
  </action>
  <acceptance_criteria>
    - `dotnet test sdks/csharp/tests/Axiam.Sdk.Tests --filter FullyQualifiedName~SensitiveRedactionTests` passes.
    - `Fixtures/amqp_hmac_vectors.json` is byte-identical to the Java copy (`git diff --no-index sdks/java/src/test/resources/amqp_hmac_vectors.json sdks/csharp/tests/Axiam.Sdk.Tests/Fixtures/amqp_hmac_vectors.json` is empty).
    - The redaction test contains a non-vacuous control assertion (Content-Type survives) — not a trivially-passing test.
    - `JwksFixture.cs` signs via BouncyCastle `Ed25519Signer(forSigning:true)`, not via the SDK's `JwksVerifier`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test sdks/csharp/tests/Axiam.Sdk.Tests --filter FullyQualifiedName~SensitiveRedactionTests</automated>
  </verify>
  <done>Test project builds; the Rust-signed HMAC fixture and Ed25519/JWKS/JWT fixture helper are committed (Wave 0 complete); the CR-04 redaction regression is green with a non-vacuous control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK internals → error/log surface | Tokens (cookies, bearer, refresh) must never cross into an exception, ToString, or log sink. |
| Build toolchain → published package | Third-party NuGet packages are pulled at restore/build; a compromised or typosquatted package would execute in consumer builds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Information Disclosure | NetworkError / Sensitive&lt;T&gt; | high | mitigate | Redact-before-wrap single choke-point (Task 2) + `Sensitive<T>` `[SENSITIVE]` on ToString/JSON; proven by `SensitiveRedactionTests` non-vacuous control (Task 3) — CR-04 carry-forward (D-12). |
| T-21-SC | Tampering | NuGet package installs (Grpc.Net.Client, RabbitMQ.Client, BouncyCastle.Cryptography, Google.Protobuf, Microsoft.Extensions.*, xunit, Moq) | high | mitigate | All packages carry an "OK / Approved" disposition in 21-RESEARCH.md § Package Legitimacy Audit (verified against nuget.org v3 registry + GitHub source, 2026-07-02). No `[SUS]`/`[SLOP]` package is installed (`ScottBrady.IdentityModel` alternative deliberately NOT chosen — direct BouncyCastle path selected). Versions pinned exactly in csproj (Task 1). |
| T-21-02 | Spoofing | Grpc.Tools generated stubs | low | accept | Stubs generate from the repo's own `proto/axiam/v1/*.proto` at build (D-05); no external stub source. |
</threat_model>

<verification>
- `dotnet build sdks/csharp/Axiam.Sdk -c Release` and `dotnet build sdks/csharp/Axiam.Sdk.AspNetCore -c Release` succeed.
- `dotnet test sdks/csharp/tests/Axiam.Sdk.Tests --filter FullyQualifiedName~SensitiveRedactionTests` green.
- gRPC stubs present under `obj/`; `sdks/buf.gen.yaml` unchanged; `obj/` gitignored.
</verification>

<success_criteria>
Two-package solution compiles with build-time gRPC codegen (SC#4 codegen half); Core token-safety + error vocabulary established with the CR-04 leak class closed; Wave 0 test harness + committed HMAC/JWKS fixtures in place, unblocking all Wave 2+ plans.
</success_criteria>

## Artifacts this phase produces (this plan)

- Packages/csproj: `Axiam.Sdk` (`Axiam.Sdk.csproj`), `Axiam.Sdk.AspNetCore` (`Axiam.Sdk.AspNetCore.csproj`), `Axiam.Sdk.sln`, `sdks/csharp/.gitignore`.
- MSBuild: `<Protobuf Include="../../../proto/axiam/v1/*.proto" GrpcServices="Client">` (build-time gRPC codegen; generated `Axiam.V1.*` stubs in obj/).
- Public types (namespace `Axiam.Sdk.Core`): `Sensitive<T>`, `SensitiveJsonConverter<T>`, `ErrorMapper`, `NetworkError`, `AuthError`, `AuthzError`.
- Public type (namespace `Axiam.Sdk.Auth`): `TokenPair`.
- Test project `Axiam.Sdk.Tests` (`Axiam.Sdk.Tests.csproj`) + `Fixtures/amqp_hmac_vectors.json`, `Fixtures/JwksFixture.cs`, `SensitiveRedactionTests`.

<output>
Create `.planning/phases/21-c-sdk/21-01-SUMMARY.md` when done.
</output>
