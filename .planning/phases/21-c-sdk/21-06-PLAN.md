---
phase: 21-c-sdk
plan: 06
type: execute
wave: 4
depends_on: [21-04]
files_modified:
  - sdks/csharp/Axiam.Sdk.AspNetCore/AxiamAuthMiddleware.cs
  - sdks/csharp/Axiam.Sdk.AspNetCore/AxiamPolicyHandler.cs
  - sdks/csharp/Axiam.Sdk.AspNetCore/AxiamPolicyProvider.cs
  - sdks/csharp/Axiam.Sdk.AspNetCore/AxiamRequirement.cs
  - sdks/csharp/Axiam.Sdk.AspNetCore/AxiamOptions.cs
  - sdks/csharp/Axiam.Sdk.AspNetCore/ServiceCollectionExtensions.cs
  - sdks/csharp/tests/Axiam.Sdk.AspNetCore.Tests/Axiam.Sdk.AspNetCore.Tests.csproj
  - sdks/csharp/tests/Axiam.Sdk.AspNetCore.Tests/AspNetCoreMiddlewareTests.cs
autonomous: true
requirements: [CS-01]

must_haves:
  truths:
    - "AxiamAuthMiddleware verifies the token (alg-pinned signature + exp + cross-tenant claim), sets HttpContext.User to a ClaimsPrincipal (user_id, tenant_id, roles), and never caches verification beyond the token's TTL (D-06, §10, SC#3)."
    - "[Authorize] yields 401 with the standardized JSON body on missing/invalid token; [Authorize(Policy=\"resource:action\")] calls CheckAccessAsync and yields 403 on deny (D-08)."
    - "AddAxiam()/AddAxiamAspNetCore() register via TryAdd* so an explicit consumer registration always wins (D-07)."
  artifacts:
    - "sdks/csharp/Axiam.Sdk.AspNetCore/AxiamAuthMiddleware.cs, AxiamPolicyHandler.cs, AxiamPolicyProvider.cs, AxiamRequirement.cs, AxiamOptions.cs, ServiceCollectionExtensions.cs"
    - "sdks/csharp/tests/Axiam.Sdk.AspNetCore.Tests/ (WebApplicationFactory integration)"
  key_links:
    - "Middleware uses AxiamClient's exposed JwksVerifier fast-path with reactive fallback; policy handler calls client.Authz.CheckAccessAsync fresh (no client-side authz cache)."
    - "Error bodies written via WriteAsJsonAsync (System.Text.Json), never string concatenation (JSON-injection safe)."
---

<objective>
Deliver the `Axiam.Sdk.AspNetCore` companion package (D-06, D-07, D-08, §10): the `AxiamAuthMiddleware` that verifies tokens and injects a `ClaimsPrincipal` so standard `[Authorize]` works (401), the DI extensions + Options pattern, and the policy-based authorization handler/provider so `[Authorize(Policy="resource:action")]` calls `CheckAccessAsync` (403).

Purpose: Meet SC#3 (middleware protects a sample ASP.NET Core 8+ endpoint) with the idiomatic .NET authn+authz surface enterprise consumers expect, mirroring Java's Spring SecurityContext integration.
Output: The six `Axiam.Sdk.AspNetCore` types + a `WebApplicationFactory`-based integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-c-sdk/21-CONTEXT.md
@.planning/phases/21-c-sdk/21-RESEARCH.md
@.planning/phases/21-c-sdk/21-PATTERNS.md
@sdks/CONTRACT.md
@sdks/java/src/main/java/io/axiam/sdk/spring/AxiamAuthenticationFilter.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: AxiamAuthMiddleware — verify → ClaimsPrincipal → 401 (D-06, §10, SC#3)</name>
  <files>sdks/csharp/Axiam.Sdk.AspNetCore/AxiamAuthMiddleware.cs, sdks/csharp/Axiam.Sdk.AspNetCore/AxiamOptions.cs</files>
  <read_first>
    - sdks/java/src/main/java/io/axiam/sdk/spring/AxiamAuthenticationFilter.java (full authoritative sequence lines 74-112: extractToken [Authorization Bearer then axiam_access cookie]; no-credential passthrough; verify; explicit exp check; mandatory assertTenant; scope/roles→authorities; set identity; AuthzError→403 / AuthError→401 / other→401; writeJsonError via ObjectNode not string concat, lines 159-167)
    - .planning/phases/21-c-sdk/21-RESEARCH.md Pattern 5 (AxiamAuthMiddleware.InvokeAsync draft: X-Tenant-ID header or configured default [never silent], ExtractToken, VerifyLocallyOrRemotelyAsync, ClaimsIdentity user_id/tenant_id/roles, WriteErrorAsync) + Pattern 1 (local verify fast-path) + Pitfall 3 (cross-tenant)
    - .planning/phases/21-c-sdk/21-PATTERNS.md § AxiamAuthMiddleware.cs (Java sequence/invariants) + § Standardized JSON error body (WriteAsJsonAsync, never string concat)
    - sdks/csharp/Axiam.Sdk/AxiamClient.cs (exposed JwksVerifier + token/session seam from 21-04) + Auth/JwksVerifier.cs (21-03)
    - sdks/CONTRACT.md §10 (C# row: app.UseMiddleware<AxiamAuthMiddleware>() in Program.cs) + .planning/phases/21-c-sdk/21-CONTEXT.md D-06
  </read_first>
  <action>
    Implement `sealed class AxiamAuthMiddleware` (`InvokeAsync(HttpContext, AxiamClient, AxiamOptions)`) per RESEARCH Pattern 5 + the Java filter sequence: resolve tenant from `X-Tenant-ID` header or the configured default (never a silent default — throw/401 if neither, §5); `ExtractToken` = `Authorization: Bearer` then `axiam_access` cookie; if NO token was presented, pass through unauthenticated so the framework's own `[Authorize]` 401s it (do not reject at the middleware when no credential exists); if a token IS present, verify via the client's `JwksVerifier` local fast-path (alg-pinned signature) with reactive fallback, perform an explicit `exp` check (defense in depth), and the mandatory cross-tenant `tenant_id == configuredTenant` check (Pitfall 3). On success set `context.User = new ClaimsPrincipal(new ClaimsIdentity("Axiam", ...))` with claims `user_id` (from `sub`), `tenant_id`, and one `ClaimTypes.Role` per role — NEVER cached beyond the token's remaining TTL (§10). On failure write a standardized JSON body via `WriteAsJsonAsync` (System.Text.Json — never string concatenation, JSON-injection safe): `AuthError`→401. Implement `AxiamOptions` (typed: `BaseUrl`, `DefaultTenantId`, JWKS cache TTL). Per D-06 / §10.
  </action>
  <acceptance_criteria>
    - `dotnet build sdks/csharp/Axiam.Sdk.AspNetCore -c Release` succeeds.
    - Source assertion: `AxiamAuthMiddleware.cs` extracts `Authorization`/`axiam_access`, checks `exp` and `tenant_id` after signature verification, sets `HttpContext.User` with `user_id`/`tenant_id`/`roles` claims, and writes error bodies via `WriteAsJsonAsync` (no manual JSON string building).
    - Source assertion: a request with no credential passes through (framework 401), a request with an invalid/expired/wrong-tenant token gets a 401 JSON body.
    - Verified by the integration test in Task 3.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build sdks/csharp/Axiam.Sdk.AspNetCore -c Release</automated>
  </verify>
  <done>Middleware verifies (alg-pin + exp + cross-tenant), injects a ClaimsPrincipal, never caches beyond TTL, and 401s invalid tokens with a JSON-injection-safe body — the SC#3 authn half.</done>
</task>

<task type="auto">
  <name>Task 2: Policy-based authz (403) + DI extensions with TryAdd precedence (D-07, D-08)</name>
  <files>sdks/csharp/Axiam.Sdk.AspNetCore/AxiamPolicyHandler.cs, sdks/csharp/Axiam.Sdk.AspNetCore/AxiamPolicyProvider.cs, sdks/csharp/Axiam.Sdk.AspNetCore/AxiamRequirement.cs, sdks/csharp/Axiam.Sdk.AspNetCore/ServiceCollectionExtensions.cs</files>
  <read_first>
    - .planning/phases/21-c-sdk/21-RESEARCH.md Pattern 5 (ServiceCollectionExtensions AddAxiam/AddAxiamAspNetCore; AxiamPolicyHandler : AuthorizationHandler<AxiamRequirement> calling client.Authz.CheckAccessAsync; 403 via unsatisfied requirement) + § project constraint (no client-side authz cache beyond the request)
    - .planning/phases/21-c-sdk/21-PATTERNS.md § AxiamPolicyHandler.cs (most novel file — no direct Spring 1:1; reuse Java AuthzError→403 JSON-body convention + Go authz.go CheckAccess shape) + § ServiceCollectionExtensions.cs (Java AxiamAutoConfiguration @ConditionalOnMissingBean ≈ TryAdd*; explicit consumer registration wins) + § AxiamPolicyProvider.cs (novel .NET IAuthorizationPolicyProvider — build from Pattern 5)
    - sdks/csharp/Axiam.Sdk/AxiamClient.cs (client.Authz.CheckAccessAsync from 21-04) + Core/AuthzError.cs (21-01)
    - sdks/CONTRACT.md §2 (AuthzError→403) + .planning/phases/21-c-sdk/21-CONTEXT.md D-07/D-08 + CLAUDE.md (additive-only RBAC — no deny-override, no long-lived authz cache)
  </read_first>
  <action>
    Implement `AxiamRequirement : IAuthorizationRequirement` (carrying `Resource` + `Action` parsed from the `resource:action` policy name). Implement `AxiamPolicyProvider : IAuthorizationPolicyProvider` that, for a policy name shaped `resource:action`, builds a policy with an `AxiamRequirement` (falling back to a `DefaultAuthorizationPolicyProvider` for other names) — this is the novel .NET-idiom file (build from RESEARCH Pattern 5). Implement `AxiamPolicyHandler : AuthorizationHandler<AxiamRequirement>`: read `user_id` from `context.User` (if absent, return unsatisfied — `[Authorize]` already 401s upstream), call `client.Authz.CheckAccessAsync(userId, requirement.Resource, requirement.Action, ct)` FRESH every time (no client-side cache — additive-only RBAC constraint); `context.Succeed(requirement)` on allow, leave unsatisfied on deny (ASP.NET Core returns 403). Ensure the 403 body is a standardized JSON body (via a custom `IAuthorizationMiddlewareResultHandler` or the middleware's error writer, reusing the AuthzError→403 convention). Implement `ServiceCollectionExtensions` with `AddAxiam(this IServiceCollection, Action<AxiamOptions>)` (registers `AxiamClient` — IHttpClientFactory path from D-09, the SDK re-applies cookie-jar+TLS regardless) and `AddAxiamAspNetCore(...)` (calls `AddAxiam` then registers the policy handler + provider). Use `TryAddSingleton`/`TryAddScoped` (NOT `AddSingleton`) so an explicit consumer registration always wins (Java `@ConditionalOnMissingBean` precedence, PATTERNS.md § ServiceCollectionExtensions). Per D-07 / D-08.
  </action>
  <acceptance_criteria>
    - `dotnet build sdks/csharp/Axiam.Sdk.AspNetCore -c Release` succeeds.
    - Source assertion: `AxiamPolicyHandler.cs` calls `CheckAccessAsync` with no local decision cache; `ServiceCollectionExtensions.cs` uses `TryAdd*` registration; `AxiamPolicyProvider.cs` builds an `AxiamRequirement` for `resource:action` policy names.
    - Behavior verified by Task 3: `[Authorize(Policy=...)]` → 403 on deny, 200 on allow.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build sdks/csharp/Axiam.Sdk.AspNetCore -c Release</automated>
  </verify>
  <done>Policy-based authz routes [Authorize(Policy="resource:action")] to a fresh CheckAccessAsync (403 on deny); DI extensions use TryAdd precedence so explicit consumer wiring wins (D-07/D-08).</done>
</task>

<task type="auto">
  <name>Task 3: WebApplicationFactory integration test — protected endpoint (SC#3)</name>
  <files>sdks/csharp/tests/Axiam.Sdk.AspNetCore.Tests/Axiam.Sdk.AspNetCore.Tests.csproj, sdks/csharp/tests/Axiam.Sdk.AspNetCore.Tests/AspNetCoreMiddlewareTests.cs</files>
  <read_first>
    - sdks/java/src/test/java/io/axiam/sdk/spring/AxiamAuthenticationFilterIT.java (MockFilterChain-level integration analog: no-token→401, valid→pass, wrong-tenant→401/403)
    - .planning/phases/21-c-sdk/21-VALIDATION.md § Per-Requirement Verification Map (SC#3: WebApplicationFactory) + § Test Infrastructure (full suite includes WebApplicationFactory integration)
    - sdks/csharp/tests/Axiam.Sdk.Tests/Fixtures/JwksFixture.cs (Ed25519 keypair + JWKS + signed JWT + wrong-tenant variant, from 21-01) — reuse to mint valid/invalid tokens and serve the JWKS doc
    - sdks/csharp/Axiam.Sdk.AspNetCore/AxiamAuthMiddleware.cs, AxiamPolicyHandler.cs (Tasks 1-2)
  </read_first>
  <action>
    Create `Axiam.Sdk.AspNetCore.Tests.csproj` (net8.0) referencing `Microsoft.AspNetCore.Mvc.Testing`, `xunit` 2.9.3, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`, and `<ProjectReference>` to `Axiam.Sdk.AspNetCore` + `Axiam.Sdk` (for the JwksFixture helper reference or reuse a shared fixture). Build a minimal in-memory host via `WebApplicationFactory` (or `WebApplication.CreateBuilder`) that wires `AddAxiamAspNetCore(...)`, `UseMiddleware<AxiamAuthMiddleware>()`, and maps a `[Authorize]` endpoint plus an `[Authorize(Policy="documents:read")]` endpoint. Point the middleware's JwksVerifier at a fake JWKS-serving handler seeded from `JwksFixture`. Assert: (a) no token → 401; (b) valid token (correct tenant) → the `[Authorize]` endpoint returns 200 with a ClaimsPrincipal present; (c) wrong-tenant token → 401; (d) valid token but authz-deny → the policy endpoint returns 403; (e) valid token + authz-allow → 200. Mark `[Trait("Category","Integration")]`. Per SC#3.
  </action>
  <acceptance_criteria>
    - `dotnet test sdks/csharp/tests/Axiam.Sdk.AspNetCore.Tests --filter FullyQualifiedName~AspNetCoreMiddlewareTests` passes with all five assertions (no-token 401, valid 200, wrong-tenant 401, policy-deny 403, policy-allow 200).
    - The test exercises the real `AxiamAuthMiddleware` + `AxiamPolicyHandler` through the ASP.NET Core pipeline (not a unit stub) — SC#3.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test sdks/csharp/tests/Axiam.Sdk.AspNetCore.Tests --filter FullyQualifiedName~AspNetCoreMiddlewareTests</automated>
  </verify>
  <done>A WebApplicationFactory integration test proves the middleware protects a sample ASP.NET Core 8+ endpoint (401/403/200 paths) — SC#3 met in automated CI ahead of the runnable example in 21-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Incoming HTTP request → protected app endpoint | The middleware is the trust gate; an unverified or wrong-tenant token must never yield an authenticated ClaimsPrincipal. |
| Policy evaluation → server RBAC | Authorization must reflect the server's live additive-only decision, never a client-side cache/deny-override. |
| Error message → HTTP response body | An error body built by string concatenation could be a JSON-injection vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-17 | Spoofing / Elevation of Privilege | AxiamAuthMiddleware | high | mitigate | Alg-pinned signature verification + explicit `exp` check + mandatory cross-tenant `tenant_id` check before setting `HttpContext.User`; never cached beyond token TTL (Task 1, D-06/§10/Pitfall 3). Proven by the wrong-tenant 401 integration test (Task 3). |
| T-21-18 | Elevation of Privilege | AxiamPolicyHandler (stale/deny-override authz) | high | mitigate | Every policy check calls `CheckAccessAsync` FRESH; no client-side authz cache or deny-override (Task 2, additive-only RBAC constraint). Proven by policy-deny 403 test. |
| T-21-19 | Tampering (JSON injection) | error response body | medium | mitigate | Error bodies written via `WriteAsJsonAsync` (System.Text.Json), never string concatenation (Task 1, PATTERNS.md § Standardized JSON error body). |
| T-21-20 | Information Disclosure | verification caching | medium | mitigate | Middleware never caches a verification result beyond the token's remaining TTL (Task 1, §10). |
</threat_model>

<verification>
- `dotnet test sdks/csharp/tests/Axiam.Sdk.AspNetCore.Tests --filter FullyQualifiedName~AspNetCoreMiddlewareTests` green (all five paths).
- `dotnet build sdks/csharp/Axiam.Sdk.AspNetCore -c Release` succeeds.
- `AxiamClient.cs` unchanged (middleware consumes its exposed seam).
</verification>

<success_criteria>
SC#3 met: the `Axiam.Sdk.AspNetCore` middleware protects a sample endpoint (401), `[Authorize(Policy="resource:action")]` yields 403 on deny via a fresh CheckAccessAsync, DI extensions use TryAdd precedence, and error bodies are JSON-injection safe — the idiomatic .NET authn+authz surface (D-06/D-07/D-08/§10).
</success_criteria>

## Artifacts this phase produces (this plan)

- Public types (namespace `Axiam.Sdk.AspNetCore`): `AxiamAuthMiddleware`, `AxiamPolicyHandler`, `AxiamPolicyProvider`, `AxiamRequirement`, `AxiamOptions`, `ServiceCollectionExtensions` (`AddAxiam`/`AddAxiamAspNetCore`).
- Test project `Axiam.Sdk.AspNetCore.Tests` (`Axiam.Sdk.AspNetCore.Tests.csproj`) + `AspNetCoreMiddlewareTests` (WebApplicationFactory; SC#3).

<output>
Create `.planning/phases/21-c-sdk/21-06-SUMMARY.md` when done.
</output>
