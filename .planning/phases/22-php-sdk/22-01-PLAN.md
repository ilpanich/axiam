---
phase: 22-php-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdks/php/composer.json
  - sdks/php/phpunit.xml.dist
  - sdks/php/.gitignore
  - sdks/php/src/Core/Sensitive.php
  - sdks/php/src/Core/AxiamException.php
  - sdks/php/src/Core/AuthError.php
  - sdks/php/src/Core/AuthzError.php
  - sdks/php/src/Core/NetworkError.php
  - sdks/php/src/Core/ErrorMapper.php
  - sdks/php/tests/SensitiveRedactionTest.php
autonomous: true
requirements: [PHP-01]
must_haves:
  truths:
    - "composer install resolves guzzlehttp/guzzle ^7.13, php-amqplib/php-amqplib ^3.7, firebase/php-jwt ^6.11, psr/log ^3.0 with zero framework runtime deps (D-01/D-07)"
    - "Sensitive::__toString() and its JsonSerializable output both return the literal [SENSITIVE], never the wrapped secret (D-11, CONTRACT §7)"
    - "NetworkError redacts Set-Cookie / Authorization / Cookie from any wrapped PSR-7 response BEFORE storing it; the raw token never reaches __toString/getMessage/JSON (D-10/D-11, CR-04)"
    - "AuthError, AuthzError, NetworkError all extend AxiamException; ErrorMapper is the one place HTTP status maps to a typed exception (D-10)"
  artifacts:
    - "sdks/php/composer.json (filled require + require-dev + scripts.test)"
    - "sdks/php/phpunit.xml.dist (unit/integration testsuite split)"
    - "sdks/php/src/Core/{Sensitive,AxiamException,AuthError,AuthzError,NetworkError,ErrorMapper}.php"
    - "sdks/php/tests/SensitiveRedactionTest.php"
  key_links:
    - "NetworkError::fromResponse() constructor-time redaction — the CR-04 leak site"
    - "composer test script wiring PHPUnit — SC#5 entry point every later plan reuses"
  prohibitions:
    - "No framework package (illuminate/*, symfony/*) in composer.json require (runtime) block — dev-only (D-01)"
    - "Raw token value never present in any exception __toString / getMessage / JSON / log line"
    - "No flat enum-of-codes on a single exception type — the taxonomy is a class hierarchy (D-10)"
---

<objective>
Fill the existing `sdks/php/` Composer scaffold with pinned dependencies, PHPUnit test
infrastructure, and the Phase-22 error/redaction foundation every later plan builds on: the
`Sensitive` wrapper (D-11), the `AxiamException` → `AuthError`/`AuthzError`/`NetworkError`
taxonomy (D-10), and the central `ErrorMapper`. This plan closes the CR-04 token-leak-via-error
bug class in PHP before any transport code exists.

Purpose: establish a zero-framework-runtime-dep package (D-01/D-07) with a green `composer test`
harness (SC#5 entry point) and a redaction contract proven by a non-vacuous regression test
(D-11, CR-04 carry-forward from TS Phase 17).
Output: `composer.json`, `phpunit.xml.dist`, `src/Core/*`, `tests/SensitiveRedactionTest.php`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-php-sdk/22-CONTEXT.md
@.planning/phases/22-php-sdk/22-RESEARCH.md
@sdks/CONTRACT.md
</context>

## Artifacts this phase produces (plan 22-01)

- `Axiam\Sdk\Core\Sensitive` — token/secret wrapper; `__toString()` → `[SENSITIVE]`, implements `JsonSerializable`
- `Axiam\Sdk\Core\AxiamException` — base exception
- `Axiam\Sdk\Core\AuthError`, `Axiam\Sdk\Core\AuthzError`, `Axiam\Sdk\Core\NetworkError` — §2 typed taxonomy
- `Axiam\Sdk\Core\NetworkError::fromResponse()` — redact-before-wrap factory
- `Axiam\Sdk\Core\ErrorMapper` — central HTTP-status → typed-exception mapper
- `sdks/php/composer.json` (pinned deps + `composer test`), `sdks/php/phpunit.xml.dist`, `sdks/php/.gitignore`

<tasks>

<task type="auto">
  <name>Task 1: Fill composer.json + PHPUnit scaffold</name>
  <files>sdks/php/composer.json, sdks/php/phpunit.xml.dist, sdks/php/.gitignore</files>
  <read_first>
    - sdks/php/composer.json (existing scaffold — keep name/license/PSR-4/keywords/support)
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Standard Stack table + Installation block + Validation Architecture / Wave 0 Requirements)
    - sdks/CONTRACT.md §4/§9 (Guzzle CookieJar + HandlerStack are contract-pinned — no PSR-18 swap)
    - .github/workflows/sdk-ci-python.yml (interpreted-ecosystem CI ordering reference)
  </read_first>
  <action>
    In `require`, add `guzzlehttp/guzzle: ^7.13`, `php-amqplib/php-amqplib: ^3.7`,
    `firebase/php-jwt: ^6.11`, `psr/log: ^3.0` alongside the existing `php: >=8.1`. Add a `suggest`
    block for `ext-grpc` and `grpc/grpc` describing the optional long-running-runtime gRPC transport
    and the automatic REST fallback (D-03). In `require-dev`, add `phpunit/phpunit: ^9.6`,
    `phpstan/phpstan: ^2.2`, `friendsofphp/php-cs-fixer: ^3.95`, and the dev-only framework-bridge
    deps `illuminate/support` + `illuminate/contracts` (`^11.0 || ^12.0`) and
    `symfony/security-core` + `symfony/http-kernel` + `symfony/event-dispatcher-contracts`
    (`^7.0 || ^8.0`) — declared now so wave-5 bridge plans never re-edit composer.json require-dev
    (D-01: core runtime stays framework-free). Add `scripts.test` = `phpunit` and
    `scripts.test-unit` = `phpunit --testsuite=unit` (SC#5). Keep `"license": "Apache-2.0"`.
    Create `phpunit.xml.dist` with two testsuites, `unit` (`tests/` default) and `integration`
    (framework `*MiddlewareTest`/`*SubscriberTest`), bootstrap `vendor/autoload.php`, and
    `failOnWarning`/`failOnRisky` true. Create `.gitignore` ignoring `/vendor/` and
    `composer.lock` per library convention.
  </action>
  <verify>
    <automated>cd sdks/php && composer validate --no-check-publish && composer install --no-interaction && vendor/bin/phpunit --version</automated>
  </verify>
  <acceptance_criteria>
    - `composer validate --no-check-publish` exits 0.
    - `composer install` resolves without pulling illuminate/* or symfony/* into the runtime
      (they appear only under require-dev): `composer show --tree | grep -E '^(illuminate|symfony)/'`
      returns matches only reachable from require-dev, and `composer show --self` require list is
      exactly php/guzzle/php-amqplib/firebase-php-jwt/psr-log.
    - `vendor/bin/phpunit --version` reports a 9.6.x line.
  </acceptance_criteria>
  <done>composer validate passes; install succeeds; PHPUnit 9.6.x present; runtime require block is framework-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sensitive wrapper + error taxonomy + ErrorMapper</name>
  <files>sdks/php/src/Core/Sensitive.php, sdks/php/src/Core/AxiamException.php, sdks/php/src/Core/AuthError.php, sdks/php/src/Core/AuthzError.php, sdks/php/src/Core/NetworkError.php, sdks/php/src/Core/ErrorMapper.php</files>
  <read_first>
    - sdks/csharp/Axiam.Sdk/Core/Sensitive.cs (redaction wrapper shape)
    - sdks/csharp/Axiam.Sdk/Core/NetworkError.cs (constructor-time redaction pattern)
    - sdks/csharp/Axiam.Sdk/Core/ErrorMapper.cs (single status→error mapper)
    - sdks/csharp/Axiam.Sdk/Core/{AuthError,AuthzError}.cs (typed subclass shape)
    - sdks/typescript/src/core/errorMapper.ts (sanitizeAxiosError — CR-04 origin fix)
    - sdks/CONTRACT.md §2 (error taxonomy + HTTP-status mapping table) and §7 (Sensitive floor)
  </read_first>
  <behavior>
    - Sensitive::__toString() returns `[SENSITIVE]`; jsonSerialize() returns `[SENSITIVE]`; a reveal()/expose() accessor returns the real value only when explicitly called.
    - var_export/print_r/json_encode of a Sensitive never emit the wrapped value.
    - NetworkError::fromResponse(ResponseInterface) strips Set-Cookie/Authorization/Cookie headers before storing a summary; getMessage() and __toString() contain neither header nor any cookie value.
    - ErrorMapper maps 401→AuthError, 403→AuthzError, network/5xx/transport→NetworkError per §2; each is instanceof AxiamException.
  </behavior>
  <action>
    Implement `Sensitive` as a `final` class holding a private string; `__toString(): string` and
    `jsonSerialize(): mixed` both return the literal `[SENSITIVE]` (CONTRACT §7 / D-11 PHP JSON
    hardening — C# has no JsonSerializable analog, this is new PHP work); add an explicit
    `reveal(): string` accessor as the only way to obtain the underlying value. Implement
    `AxiamException extends \RuntimeException` as the base; `AuthError`, `AuthzError`, `NetworkError`
    extend it (D-10 — a class hierarchy, NOT a flat enum-of-codes). `NetworkError::fromResponse()`
    (and any factory that accepts a PSR-7 `ResponseInterface`/`RequestException`) MUST redact the
    `Set-Cookie`, `Authorization`, and `Cookie` headers into a sanitized status/summary string
    BEFORE assigning any property — mirror `NetworkError.cs`'s constructor-time redaction and
    `sanitizeAxiosError`; never store the raw response object. `ErrorMapper::fromStatus(int, ...)` is
    the single translation point (401→AuthError, 403→AuthzError, 5xx/transport→NetworkError) per
    CONTRACT §2; no other class hand-rolls status branching.
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpunit --filter 'Sensitive|ErrorMapper' && vendor/bin/phpstan analyse src --level=6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnl 'class \(AuthError\|AuthzError\|NetworkError\)' src/Core` shows each `extends AxiamException` (`grep -A2 'class NetworkError'` contains `extends AxiamException`).
    - PHPStan level 6 on `src/` reports no errors.
    - Sensitive/ErrorMapper unit tests pass (see Task 3 for the redaction regression).
  </acceptance_criteria>
  <done>Sensitive + three-tier taxonomy + central ErrorMapper compile, pass PHPStan L6, and redact at construction time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Non-vacuous redaction regression test (CR-04)</name>
  <files>sdks/php/tests/SensitiveRedactionTest.php</files>
  <read_first>
    - sdks/csharp/tests/Axiam.Sdk.Tests/SensitiveRedactionTests.cs (non-vacuous control case)
    - sdks/php/src/Core/Sensitive.php, sdks/php/src/Core/NetworkError.php (targets under test)
    - .planning/phases/22-php-sdk/22-CONTEXT.md (D-11 / Specific Ideas — assert raw axiam_access/axiam_refresh never appears in __toString/JSON/log)
  </read_first>
  <behavior>
    - Given a Sensitive wrapping `axiam_access=secret-abc`, assert `(string)$s`, `json_encode($s)`, and `print_r($s, true)` contain `[SENSITIVE]` and NOT `secret-abc`.
    - Given a NetworkError built from a PSR-7 response carrying `Set-Cookie: axiam_refresh=refresh-xyz` and `Authorization: Bearer tok-123`, assert getMessage()/__toString()/json_encode contain neither `refresh-xyz` nor `tok-123`.
    - Non-vacuous control: a plain (non-secret) response body substring IS asserted present, so the test would fail if redaction over-redacted or if the assertions were vacuous.
  </behavior>
  <action>
    Write `SensitiveRedactionTest` with the three behaviors above. Include the explicit non-vacuous
    control assertion (a benign marker string that MUST survive) so removing the redaction logic
    makes the test go red, not green — mirror the C#/Python sibling tests' control-case discipline
    (D-11).
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpunit --filter SensitiveRedactionTest</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter SensitiveRedactionTest` passes.
    - Temporarily returning the raw value from `Sensitive::__toString()` makes the test fail (control-case proof — verify manually, do not commit the mutation).
  </acceptance_criteria>
  <done>Redaction regression test green with a proven non-vacuous control case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → SDK error object | Untrusted `Set-Cookie`/`Authorization` headers cross into exception/log surfaces |
| SDK value → application log/serializer | Token-carrying values cross into developer-visible output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Information Disclosure | `NetworkError::fromResponse` | high | mitigate | Redact Set-Cookie/Authorization/Cookie at construction BEFORE property assignment (CR-04 carry-forward); regression test with non-vacuous control (Task 3) |
| T-22-02 | Information Disclosure | `Sensitive::__toString`/`jsonSerialize` | high | mitigate | Both return literal `[SENSITIVE]`; JsonSerializable hardening (D-11); asserted by SensitiveRedactionTest |
| T-22-03 | Tampering | Composer dependency set | medium | mitigate | Only user-locked, Packagist-verified deps (RESEARCH Package Legitimacy Audit — all Approved, none [ASSUMED]/[SUS]); `composer validate` gate |
</threat_model>

<verification>
- `composer validate --no-check-publish` exits 0; runtime require block is framework-free.
- `vendor/bin/phpunit --testsuite=unit` green (Sensitive, ErrorMapper, redaction regression).
- `vendor/bin/phpstan analyse src --level=6` clean.
- No raw token string reachable via any exception/serialization surface (Task 3 control case).
</verification>

<success_criteria>
Package installs with pinned deps; `composer test` harness runs; Sensitive + the three-tier
exception taxonomy + central ErrorMapper exist and redact tokens; CR-04 regression proven.
</success_criteria>

<output>
Create `.planning/phases/22-php-sdk/22-01-SUMMARY.md` when done.
</output>
