---
phase: 22-php-sdk
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - sdks/php/src/Amqp/Hmac.php
  - sdks/php/src/Amqp/Consumer.php
  - sdks/php/src/Amqp/AmqpDropMessage.php
  - sdks/php/bin/axiam-amqp-worker.php
  - sdks/php/tests/HmacVerifyTest.php
  - sdks/php/tests/Fixtures/amqp_hmac_vectors.json
autonomous: true
requirements: [PHP-01]
must_haves:
  truths:
    - "Hmac::verify canonicalizes with JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE, matching the server's serde_json bytes for slash/non-ASCII payloads (Pitfall 1, T-amqp-tamper)"
    - "Verification strips hmac_signature preserving original insertion order (not alphabetized), hex-decodes the signature, and compares with hash_equals (constant-time)"
    - "Consumer verifies HMAC BEFORE the app handler runs; the handler never sees an unverified message (D-04)"
    - "HMAC-fail / parse-fail / drop-sentinel => nack WITHOUT requeue + security log; retryable handler failure => nack WITH requeue; success => ack (D-04)"
  artifacts:
    - "sdks/php/src/Amqp/{Hmac,Consumer,AmqpDropMessage}.php"
    - "sdks/php/bin/axiam-amqp-worker.php"
    - "sdks/php/tests/HmacVerifyTest.php + tests/Fixtures/amqp_hmac_vectors.json"
  key_links:
    - "Hmac::verify byte-exact parity with crates/axiam-amqp/src/messages.rs::sign_payload/verify_payload"
    - "Consumer basic_consume callback three-way ack/nack branching"
  prohibitions:
    - "Never ksort()/alphabetize the message array before HMAC canonicalization (breaks 100% of verifications)"
    - "Never omit JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE from the canonicalization json_encode"
    - "Never requeue on HMAC-fail / parse-fail / drop-sentinel (poison-loop risk)"
    - "Never log the HMAC signature value or message body on verification failure"
    - "Never use == / === for the signature compare — hash_equals only"
---

<objective>
Implement the AMQP CLI-worker consumer (D-04): `Hmac::verify` byte-for-byte matching the server's
`crates/axiam-amqp/src/messages.rs` protocol (including the PHP-specific
`JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE` escaping fix, Pitfall 1), a `Consumer` that
verifies-before-handler with correct three-way ack/nack semantics, and a standalone
`bin/axiam-amqp-worker.php` entry point. This is not a web-request path — it runs on a long-running
runtime (SC#3), documented for process-supervised restart (Pitfall 6).

Purpose: deliver tamper-proof event consumption (SC#4-AMQP) with fail-closed semantics on any
signature/parse failure, proven by a fixture including a slash + non-ASCII payload (the exact bug
class every prior SDK hit).
Output: `src/Amqp/*`, `bin/axiam-amqp-worker.php`, `tests/HmacVerifyTest.php`, HMAC vectors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-php-sdk/22-CONTEXT.md
@.planning/phases/22-php-sdk/22-RESEARCH.md
@sdks/CONTRACT.md
@crates/axiam-amqp/src/messages.rs
</context>

## Artifacts this phase produces (plan 22-03)

- `Axiam\Sdk\Amqp\Hmac` — `verify(string $signingKey, string $body): bool` (constant-time, escaping-correct)
- `Axiam\Sdk\Amqp\Consumer` — php-amqplib blocking consume loop, verify-before-handler
- `Axiam\Sdk\Amqp\AmqpDropMessage` — poison-message sentinel exception
- `sdks/php/bin/axiam-amqp-worker.php` — standalone CLI worker entry point (SC#4 runnable example)
- `sdks/php/tests/Fixtures/amqp_hmac_vectors.json` — real signed vectors incl. slash + non-ASCII

<tasks>

<task type="auto">
  <name>Task 1: Generate + commit real HMAC vectors (incl. slash + non-ASCII)</name>
  <files>sdks/php/tests/Fixtures/amqp_hmac_vectors.json</files>
  <read_first>
    - crates/axiam-amqp/src/messages.rs (sign_payload/verify_payload — the byte-exact contract, lines 30-50)
    - sdks/python/tests/test_amqp_hmac.py (sibling fixture-based vector approach)
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pitfall 1 — ASCII/UUID-only fixtures hide the escaping bug; VALIDATION Wave 0)
  </read_first>
  <action>
    Produce signed test vectors and commit them as `amqp_hmac_vectors.json`. Each vector has: a
    known signing key, a message object (with `hmac_signature` set to the hex HMAC-SHA256 over the
    canonical bytes of the message with `hmac_signature` removed), and an `expected: true/false`
    flag. Include at minimum: (a) a plain UUID/enum-only valid vector; (b) a valid vector whose
    payload contains a forward slash `/` AND non-ASCII text (the Pitfall-1 regression — this vector
    MUST fail if the canonicalization omits the unescaping flags); (c) a tampered-body invalid
    vector; (d) a wrong-key invalid vector. Generate the signatures with a throwaway Rust call into
    `crates/axiam-amqp`'s `sign_payload` (or a small standalone signer) — commit the fixed bytes +
    hex; do NOT depend on `axiam-amqp` at SDK runtime.
  </action>
  <verify>
    <automated>cd sdks/php && php -r 'json_decode(file_get_contents("tests/Fixtures/amqp_hmac_vectors.json")); exit(json_last_error()===JSON_ERROR_NONE?0:1);'</automated>
  </verify>
  <acceptance_criteria>
    - `amqp_hmac_vectors.json` parses as valid JSON and contains at least one vector whose payload includes both a `/` and a non-ASCII character with `expected: true`.
    - At least one `expected: false` tampered/wrong-key vector present.
  </acceptance_criteria>
  <done>Committed real HMAC vectors including the slash + non-ASCII regression case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hmac::verify (byte-exact, constant-time) + HmacVerifyTest</name>
  <files>sdks/php/src/Amqp/Hmac.php, sdks/php/tests/HmacVerifyTest.php</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 4 — complete Hmac PHP code; Anti-Patterns; Pitfall 1)
    - crates/axiam-amqp/src/messages.rs (normative byte contract)
    - sdks/python/src/axiam_sdk/amqp/_hmac.py (verify-then-strip-field sibling shape)
  </read_first>
  <behavior>
    - verify(key, body) returns true for every `expected:true` vector and false for every `expected:false` vector in the fixture.
    - The slash + non-ASCII valid vector passes only because JSON_UNESCAPED_SLASHES|JSON_UNESCAPED_UNICODE is used — removing the flags makes that specific vector go red while ASCII-only vectors still pass (regression proof).
    - Missing/non-string hmac_signature => false; non-hex signature => false; non-object body => false; never throws.
  </behavior>
  <action>
    Port `22-RESEARCH.md` Pattern 4 `Hmac::verify` verbatim: `json_decode($body, true)` (preserves
    insertion order — do NOT `ksort`), reject if not an array or if `hmac_signature` missing/non-string,
    `unset` the signature key, canonicalize with
    `json_encode($msg, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE)`, `@hex2bin` the signature
    (reject false), compute `hash_hmac('sha256', $canonical, $signingKey, true)`, and compare with
    `hash_equals()` (constant-time). Never throws on attacker input. Write `HmacVerifyTest` iterating
    every fixture vector plus an explicit assertion that the slash/non-ASCII vector is the escaping
    regression guard.
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpunit --filter HmacVerifyTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "JSON_UNESCAPED_SLASHES" src/Amqp/Hmac.php` and `grep -n "JSON_UNESCAPED_UNICODE" src/Amqp/Hmac.php` both match on the canonicalization call.
    - `grep -n "hash_equals" src/Amqp/Hmac.php` matches; `grep -nE "ksort|asort|sort\(" src/Amqp/Hmac.php` returns empty.
    - `vendor/bin/phpunit --filter HmacVerifyTest` passes across all fixture vectors.
  </acceptance_criteria>
  <done>Byte-exact constant-time HMAC verify green against real vectors incl. the slash/non-ASCII regression.</done>
</task>

<task type="auto">
  <name>Task 3: Consumer (verify-before-handler) + CLI worker</name>
  <files>sdks/php/src/Amqp/Consumer.php, sdks/php/src/Amqp/AmqpDropMessage.php, sdks/php/bin/axiam-amqp-worker.php</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 4 Consumer + worker code; Pitfall 6 — no auto-reconnect)
    - sdks/python/src/axiam_sdk/amqp/_consumer.py (three-way ack/nack-requeue/nack-no-requeue analog)
    - sdks/python/examples/amqp_consumer.py (CLI worker shape)
    - sdks/php/src/Amqp/Hmac.php (verify seam from Task 2)
  </read_first>
  <action>
    Implement `Consumer` on `php-amqplib` 3.7: constructor takes the caller-supplied per-tenant
    `signingKey` (A2 — no server endpoint fetches it) + a PSR-3 logger defaulting to `NullLogger`.
    `consume(...)` opens `AMQPStreamConnection`, sets `basic_qos`, and registers a `basic_consume`
    callback that: (1) calls `Hmac::verify` FIRST — on failure, log an `axiam_sdk_security` warning
    (without the signature/body) and `basic_nack(tag, false, false)` (no requeue), return; (2) on
    success, `json_decode` + `unset hmac_signature`, invoke the app handler; `basic_ack` on success;
    catch `AmqpDropMessage` => `basic_nack(tag, false, false)` (poison, no requeue); catch other
    `\Throwable` => `basic_nack(tag, false, true)` (transient, requeue). Blocking `while
    is_consuming(): wait()` loop. `AmqpDropMessage extends \RuntimeException`. `bin/axiam-amqp-worker.php`
    is an executable CLI script wiring env vars to `Consumer::consume`; it MUST exit non-zero on a
    caught connection exception (Pitfall 6 — rely on process supervision, no in-SDK reconnect loop).
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpstan analyse src/Amqp bin --level=6 && php -l bin/axiam-amqp-worker.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "basic_nack" src/Amqp/Consumer.php` shows a no-requeue call (`, false, false`) on the HMAC-fail branch and a requeue call (`, false, true`) on the transient-catch branch.
    - The HMAC verify call textually precedes the handler invocation in `Consumer.php` (verify-before-handler).
    - `php -l bin/axiam-amqp-worker.php` reports no syntax errors; the worker has a non-zero exit path on connection failure.
    - PHPStan L6 clean on `src/Amqp` + `bin`.
  </acceptance_criteria>
  <done>Consumer verifies before handler with correct three-way ack/nack; runnable CLI worker with fail-fast connection handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ delivery → SDK handler | Untrusted/forgeable message bytes cross into application processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-08 | Tampering | `Consumer` verify-before-handler + `Hmac::verify` | high | mitigate | HMAC-SHA256 verify BEFORE handler; constant-time `hash_equals`; fail-closed nack-no-requeue on any failure (D-04, Pitfall 1) |
| T-22-09 | Information Disclosure | AMQP security log on failure | medium | mitigate | Log an `axiam_sdk_security` event without the signature or message body |
| T-22-10 | Denial of Service | poison-message requeue loop | medium | mitigate | HMAC-fail/parse-fail/`AmqpDropMessage` => nack WITHOUT requeue; only transient handler errors requeue (D-04) |
| T-22-11 | Denial of Service | dropped connection (no auto-reconnect) | low | accept | Worker exits non-zero on connection loss; process supervisor restarts (Pitfall 6, documented SC#3 requirement) |
</threat_model>

<verification>
- `vendor/bin/phpunit --filter HmacVerifyTest` green including the slash/non-ASCII escaping regression.
- `grep` gates confirm unescaping flags, `hash_equals`, no `ksort`, verify-before-handler, nack-no-requeue on failure.
- PHPStan L6 clean on `src/Amqp` + `bin`; worker passes `php -l` and exits non-zero on connection loss.
</verification>

<success_criteria>
AMQP consumer verifies HMAC-SHA256 byte-exact with the server before the handler and nacks without
requeue on failure (SC#4-AMQP); runnable CLI worker ships; slash/non-ASCII regression proven.
</success_criteria>

<output>
Create `.planning/phases/22-php-sdk/22-03-SUMMARY.md` when done.
</output>
