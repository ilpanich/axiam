---
phase: 22-php-sdk
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - sdks/php/src/Session.php
  - sdks/php/src/Auth/RefreshGuard.php
  - sdks/php/src/Rest/AuthMiddleware.php
  - sdks/php/src/Rest/RefreshMiddleware.php
  - sdks/php/tests/SingleFlightRefreshTest.php
autonomous: true
requirements: [PHP-01]
must_haves:
  truths:
    - "N concurrent Guzzle async promises on an expired token trigger exactly ONE /api/v1/auth/refresh call (SC#2, D-06)"
    - "The shared refresh Promise is check-and-stored synchronously on Session before any await; concurrent callers receive the SAME promise instance (fiber-safe by construction)"
    - "The refresh Promise is cleared on BOTH success and failure so the next 401 starts a fresh attempt; refresh failure raises AuthError with no retry loop (§9.3)"
    - "AuthMiddleware injects Authorization + X-Tenant-ID on every request and X-CSRF-Token (captured from the response header) on state-changing requests (§3 non-browser CSRF)"
    - "Guzzle client is constructed with cookies: true and verify: true — never verify disabled (§4/§6, D-12/D-13 tenant required)"
  artifacts:
    - "sdks/php/src/Session.php (cookie jar + CSRF capture + shared refresh promise)"
    - "sdks/php/src/Auth/RefreshGuard.php"
    - "sdks/php/src/Rest/{AuthMiddleware,RefreshMiddleware}.php"
    - "sdks/php/tests/SingleFlightRefreshTest.php (MockHandler-counted, deliberately-ordered 401 queue)"
  key_links:
    - "Session::refreshIfNeeded() shared-promise check-and-store — the single-flight guard"
    - "RefreshMiddleware 401-interceptor retry-exactly-once wiring on the HandlerStack"
  prohibitions:
    - "Never construct the Guzzle client with the verify option disabled anywhere (§6/D-12); only a customCa bundle path is allowed"
    - "Never retry-loop on refresh failure — clear the promise and raise AuthError once (§9.3)"
    - "Session tenant is a required constructor parameter with no nullable default (D-13)"
    - "Never build a second independent refresh mechanism — one shared Session promise across REST (and later gRPC)"
---

<objective>
Implement the REST transport heart: `Session` (Guzzle `CookieJar` + CSRF capture + the single
shared refresh `Promise`, D-06), `RefreshGuard`, and the two `HandlerStack` middlewares
(`AuthMiddleware` header injection, `RefreshMiddleware` 401-single-flight-retry). Prove SC#2 —
N concurrent Guzzle async promises on an expired token trigger exactly one refresh call.

Purpose: satisfy CONTRACT §9's single-flight guarantee with a shared promise (no `revolt/event-loop`
mutex — fiber-safe by construction, D-06) and establish the header/CSRF/TLS invariants (§3/§4/§6)
every REST call inherits.
Output: `src/Session.php`, `src/Auth/RefreshGuard.php`, `src/Rest/{AuthMiddleware,RefreshMiddleware}.php`,
`tests/SingleFlightRefreshTest.php`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-php-sdk/22-CONTEXT.md
@.planning/phases/22-php-sdk/22-RESEARCH.md
@sdks/CONTRACT.md
</context>

## Artifacts this phase produces (plan 22-04)

- `Axiam\Sdk\Session` — cookie jar + CSRF capture + `refreshIfNeeded(): PromiseInterface` shared-promise single-flight; tenant required ctor (D-13)
- `Axiam\Sdk\Auth\RefreshGuard` — shared-promise guard helper (clear-on-both-paths semantics)
- `Axiam\Sdk\Rest\AuthMiddleware` — HandlerStack middleware: inject Authorization + X-Tenant-ID + X-CSRF-Token
- `Axiam\Sdk\Rest\RefreshMiddleware` — HandlerStack middleware: on 401, single-flight refresh + retry-once
- `sdks/php/tests/SingleFlightRefreshTest.php` — SC#2 concurrency proof

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session + RefreshGuard shared-promise single-flight</name>
  <files>sdks/php/src/Session.php, sdks/php/src/Auth/RefreshGuard.php</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 1 — complete Session::refreshIfNeeded PHP code + fiber-safety rationale)
    - sdks/csharp/Axiam.Sdk/Auth/RefreshGuard.cs (guard-clear-on-both-paths semantics to mirror)
    - sdks/php/src/Core/AuthError.php (raised on refresh failure)
    - sdks/CONTRACT.md §4 (Guzzle CookieJar cookies:true), §3 (non-browser CSRF = X-CSRF-Token response header), §9 (single-flight), §5 (tenant required)
  </read_first>
  <behavior>
    - refreshIfNeeded() returns the SAME PromiseInterface to every caller until it resolves; check-and-store is synchronous (no await between null-check and assignment).
    - On refresh success: capture X-CSRF-Token from the response header, then clear the stored promise.
    - On refresh failure: clear the stored promise and reject with AuthError (no retry loop, §9.3).
    - Session tenant is a required constructor parameter; there is no nullable default.
  </behavior>
  <action>
    Port `22-RESEARCH.md` Pattern 1 `Session` verbatim as the starting point. `Session` holds a
    Guzzle `CookieJar`, a nullable `?PromiseInterface $refreshPromise`, the required `tenant` (D-13,
    no nullable default), and a `?string $csrfToken`. `refreshIfNeeded(): PromiseInterface` returns
    the stored promise if non-null; otherwise stores `postAsync('/api/v1/auth/refresh', ...)->then(
    onSuccess: capture X-CSRF-Token + null the promise, onFailure: null the promise + throw
    AuthError)` and returns it — the check-and-store completes before any `await` (fiber-safe, D-06;
    no `revolt/event-loop` dep). `captureCsrfToken()` reads the `X-CSRF-Token` response header (§3).
    Expose `cookieJar()` and `csrfToken()` accessors. `RefreshGuard` factors out the
    shared-promise clear-on-both-paths helper so REST and (later) gRPC reuse ONE mechanism.
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpstan analyse src/Session.php src/Auth/RefreshGuard.php --level=6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "refreshPromise" src/Session.php` shows the promise nulled in BOTH the success and failure `then` callbacks.
    - `grep -nE "revolt|event-loop" src/Session.php src/Auth/RefreshGuard.php` returns empty (no extra mutex dep, D-06).
    - Session constructor signature has `tenant` with no default value (D-13).
    - PHPStan L6 clean on both files.
  </acceptance_criteria>
  <done>Session shared-promise single-flight with clear-on-both-paths and required tenant, fiber-safe with no extra dep.</done>
</task>

<task type="auto">
  <name>Task 2: AuthMiddleware + RefreshMiddleware on the HandlerStack</name>
  <files>sdks/php/src/Rest/AuthMiddleware.php, sdks/php/src/Rest/RefreshMiddleware.php</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 1 — RefreshMiddleware::__invoke code + HandlerStack wiring block)
    - sdks/python/src/axiam_sdk/_session.py (header-injection analog)
    - sdks/php/src/Session.php (refreshIfNeeded seam from Task 1)
    - sdks/CONTRACT.md §3 (X-CSRF-Token on state-changing requests), §5 (X-Tenant-ID)
  </read_first>
  <action>
    `AuthMiddleware` is a `HandlerStack` middleware (higher-order `function (callable $handler):
    callable`) that injects `Authorization` (current access token) and `X-Tenant-ID` on every
    request, and `X-CSRF-Token` (from `Session::csrfToken()`) on state-changing methods
    (POST/PUT/PATCH/DELETE) per §3 non-browser CSRF. `RefreshMiddleware` intercepts responses:
    on a non-401 it passes through; on 401 it calls `Session::refreshIfNeeded()` (all concurrent
    401s share the SAME promise, D-06) and retries the original request EXACTLY once via the inner
    handler — no loop. Port `22-RESEARCH.md` Pattern 1's `RefreshMiddleware::__invoke` verbatim.
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpstan analyse src/Rest/AuthMiddleware.php src/Rest/RefreshMiddleware.php --level=6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "X-CSRF-Token" src/Rest/AuthMiddleware.php` matches; `grep -n "X-Tenant-ID" src/Rest/AuthMiddleware.php` matches.
    - `grep -n "refreshIfNeeded" src/Rest/RefreshMiddleware.php` matches; the retry occurs exactly once (no while/recursion loop around the retry).
    - PHPStan L6 clean on both files.
  </acceptance_criteria>
  <done>Both HandlerStack middlewares implemented; single-flight refresh-and-retry-once wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SingleFlightRefreshTest (SC#2, MockHandler-counted)</name>
  <files>sdks/php/tests/SingleFlightRefreshTest.php</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 1 SC#2 test code + the load-bearing queued-401-first ordering note)
    - sdks/csharp/tests/Axiam.Sdk.Tests/RefreshGuardSingleFlightTests.cs (sibling concurrency assertion)
    - sdks/php/src/Rest/RefreshMiddleware.php, sdks/php/src/Session.php (under test)
  </read_first>
  <behavior>
    - Fire 5 concurrent Guzzle async promises against a MockHandler whose first 5 responses are 401, then one refresh 200, then 5 success 200s; resolve via Promise\Utils::all()->wait().
    - Exactly one request path equals /api/v1/auth/refresh (counted via Middleware::history), asserted == 1.
    - The queued-401-first ordering is preserved so the assertion is non-trivial (all five observe "no refresh yet" before the guard resolves).
  </behavior>
  <action>
    Port `22-RESEARCH.md` Pattern 1's `SingleFlightRefreshTest` verbatim, honoring the critical
    test-design note: queue all five 401s BEFORE the single refresh 200 so the `MockHandler`'s
    synchronous resolution still forces genuine single-flight overlap. Assert exactly one
    `/api/v1/auth/refresh` call via `Middleware::history`.
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpunit --filter SingleFlightRefreshTest</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter SingleFlightRefreshTest` passes with the assertion `assertCount(1, $refreshCalls)`.
    - The MockHandler response queue lists the five 401s before the refresh 200 (load-bearing ordering).
  </acceptance_criteria>
  <done>SC#2 proven: 5 concurrent expired-token requests => exactly 1 refresh call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK client → AXIAM server (TLS) | Transport-security policy enforced at Guzzle client construction |
| concurrent callers → shared refresh state | Race on the single-flight guard within one PHP process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-12 | Denial of Service | `Session::refreshIfNeeded` (thundering herd) | high | mitigate | Shared-`Promise` single-flight guard proven by SC#2 test; no retry loop on failure (§9.3) |
| T-22-13 | Information Disclosure | refresh-failure error path | high | mitigate | Refresh failure raises `AuthError` (redacted taxonomy from plan 01); no raw token in the error |
| T-22-14 | Tampering / Information Disclosure | Guzzle client TLS config | high | mitigate | `verify: true` + `cookies: true` at construction; only a `customCa` bundle path allowed (§6/D-12) — CI grep gate in plan 09 |
| T-22-15 | Spoofing | CSRF on state-changing requests | medium | mitigate | Capture `X-CSRF-Token` response header and echo it on mutating methods (§3 non-browser) |
</threat_model>

<verification>
- `vendor/bin/phpunit --filter SingleFlightRefreshTest` green (exactly 1 refresh across 5 concurrent).
- `grep` gates confirm clear-on-both-paths, no revolt dep, required tenant, CSRF/tenant header injection.
- PHPStan L6 clean on `src/Session.php`, `src/Auth/RefreshGuard.php`, `src/Rest/*Middleware.php`.
</verification>

<success_criteria>
CONTRACT §9 single-flight guarantee met with a shared promise (SC#2); REST header/CSRF/TLS
invariants established; required-tenant Session with no extra concurrency dependency.
</success_criteria>

<output>
Create `.planning/phases/22-php-sdk/22-04-SUMMARY.md` when done.
</output>
