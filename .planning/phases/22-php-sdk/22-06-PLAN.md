---
phase: 22-php-sdk
plan: 06
type: execute
wave: 4
depends_on: ["22-05", "22-04", "22-02", "22-01"]
files_modified:
  - sdks/php/src/AxiamClient.php
  - sdks/php/tests/ClientConstructionTest.php
  - sdks/php/examples/login_mfa.php
  - sdks/php/examples/rest_authz.php
autonomous: true
requirements: [PHP-01]
must_haves:
  truths:
    - "new AxiamClient(...) requires a tenant slug — a required constructor parameter with no nullable default (SC#1, D-13)"
    - "login(email, password) returns a typed LoginResult; a two-phase MFA path via verifyMfa(...) exists (SC#1, CONTRACT §1)"
    - "checkAccess/can/batchCheck delegate to AuthzDispatcher (REST default, gRPC when available) — client never hand-rolls transport selection (D-03)"
    - "The Guzzle client is built with cookies: true and verify: true; a customCa bundle path is the only TLS escape hatch (§4/§6, D-12)"
    - "verifyLocallyOrFallback(token, tenant) uses JwksVerifier then falls back to the reactive refresh path — reused by the framework bridges (D-02)"
  artifacts:
    - "sdks/php/src/AxiamClient.php (public entry point wiring Session + HandlerStack + AuthzDispatcher + JwksVerifier)"
    - "sdks/php/tests/ClientConstructionTest.php (SC#1)"
    - "sdks/php/examples/login_mfa.php, examples/rest_authz.php"
  key_links:
    - "AxiamClient constructor -> HandlerStack (AuthMiddleware + RefreshMiddleware) -> Session -> AuthzDispatcher"
    - "login()/verifyMfa()/refresh()/logout() mapping to /api/v1/auth/* endpoints"
  prohibitions:
    - "tenant must be a required constructor parameter with no nullable default (D-13)"
    - "Never construct the Guzzle client with TLS verification disabled (§6/D-12); only a customCa path"
    - "login()/verifyMfa() must return typed LoginResult — never a raw array/stdClass (D-09)"
    - "Never expose a raw access/refresh token as a plain string on the public surface (§7/D-11)"
---

<objective>
Assemble the public `AxiamClient` facade (SC#1): tenant-required constructor (D-13), the auth flow
(`login` → typed `LoginResult`, two-phase `verifyMfa`, `refresh`, `logout`), and
`checkAccess`/`can`/`batchCheck` delegating to the `AuthzDispatcher`. Wire the Guzzle `HandlerStack`
(AuthMiddleware + RefreshMiddleware) + `CookieJar` (`cookies: true`, `verify: true`) + `JwksVerifier`
+ `Session` into one entry point, plus the `verifyLocallyOrFallback()` seam the framework bridges
reuse (D-02).

Purpose: deliver the one-line `composer require` + `$client->login(...)` developer experience proven
by `ClientConstructionTest` (SC#1), tying the wave-2/3 building blocks together.
Output: `src/AxiamClient.php`, `tests/ClientConstructionTest.php`, `examples/{login_mfa,rest_authz}.php`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-php-sdk/22-CONTEXT.md
@.planning/phases/22-php-sdk/22-RESEARCH.md
@sdks/CONTRACT.md
@sdks/openapi.json
</context>

## Artifacts this phase produces (plan 22-06)

- `Axiam\Sdk\AxiamClient` — public entry point: `login`, `verifyMfa`, `refresh`, `logout`, `checkAccess`, `can`, `batchCheck`, `verifyLocallyOrFallback`
- `sdks/php/tests/ClientConstructionTest.php` — SC#1 (required tenant + typed LoginResult)
- `sdks/php/examples/login_mfa.php`, `examples/rest_authz.php` — runnable usage examples

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AxiamClient facade — constructor wiring + auth flows + authz delegation</name>
  <files>sdks/php/src/AxiamClient.php</files>
  <read_first>
    - sdks/csharp/Axiam.Sdk/AxiamClient.cs (facade composition analog)
    - sdks/python/src/axiam_sdk/_client.py (login/verify_mfa/refresh/logout flow analog)
    - sdks/php/src/Session.php, src/AuthzDispatcher.php, src/Auth/JwksVerifier.php, src/Auth/LoginResult.php (composed collaborators)
    - sdks/openapi.json (/api/v1/auth/{login,mfa/verify,refresh,logout} request/response schemas)
    - sdks/CONTRACT.md §1 (camelCase method map, PHP row), §5 (tenant required), §6 (TLS)
  </read_first>
  <behavior>
    - Constructor requires baseUrl + tenant (no nullable default); optional orgSlug/orgId, customCa, logger, restOnly, cacheTtl.
    - The Guzzle client is built with cookies: true and verify: true; a customCa value sets verify to the CA bundle path (never disables verification).
    - login($email,$password) returns a LoginResult; when the server signals MFA, LoginResult.mfaRequired is true and verifyMfa($challengeToken,$code) completes the second phase returning a LoginResult.
    - checkAccess/can/batchCheck delegate to AuthzDispatcher; logout clears cookies/CSRF/local state.
    - verifyLocallyOrFallback($token,$tenant) returns claims via JwksVerifier, else attempts the reactive refresh path, else null.
  </behavior>
  <action>
    Implement `AxiamClient` composing the wave-2/3 pieces. Constructor signature makes `tenant` a
    required parameter with no nullable default (D-13); build the `HandlerStack` (push
    `AuthMiddleware` then `RefreshMiddleware`), the `CookieJar`, and the Guzzle `Client` with
    `cookies: true` + `verify: true` (or `verify` = the `customCa` bundle path when provided — the
    ONLY TLS escape hatch, §6/D-12). `login()` POSTs `/api/v1/auth/login` and maps the response to a
    readonly `LoginResult` (D-09); `verifyMfa()` POSTs `/api/v1/auth/mfa/verify`; `refresh()` and
    `logout()` map to their endpoints (logout clears cookies/CSRF/local state). `checkAccess`/`can`/
    `batchCheck` delegate to `AuthzDispatcher` (D-03). `verifyLocallyOrFallback()` calls
    `JwksVerifier::verify()` then falls back to the reactive refresh path — the seam the Laravel/
    Symfony bridges call (D-02). Method names follow CONTRACT §1 PHP camelCase. Include org
    identifier options (orgSlug/orgId) beyond the tenant-only minimum, as the sibling SDKs did, since
    the real login/refresh handlers require an org identifier (Rule 2 / STATE.md 16-01 precedent).
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpstan analyse src/AxiamClient.php --level=6</automated>
  </verify>
  <acceptance_criteria>
    - AxiamClient constructor has a `tenant` parameter with no default value (D-13); `grep -n "function __construct" src/AxiamClient.php` shows tenant is required.
    - `grep -nE "'verify'\\s*=>\\s*true|verify.*true" src/AxiamClient.php` shows verify:true; no TLS-disable pattern present.
    - `grep -n ": LoginResult" src/AxiamClient.php` shows login/verifyMfa return the typed DTO.
    - `grep -n "AuthzDispatcher" src/AxiamClient.php` shows authz delegation.
    - PHPStan L6 clean on the file.
  </acceptance_criteria>
  <done>AxiamClient wires Session/HandlerStack/AuthzDispatcher/JwksVerifier with required tenant, typed auth flows, and strict TLS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClientConstructionTest (SC#1)</name>
  <files>sdks/php/tests/ClientConstructionTest.php</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-VALIDATION.md (SC#1 row — required tenant, typed LoginResult)
    - sdks/php/src/AxiamClient.php, src/Auth/LoginResult.php (targets)
    - sdks/csharp/tests/Axiam.Sdk.Tests (ClientConstructionTests analog)
  </read_first>
  <behavior>
    - Constructing AxiamClient without a tenant is a compile-time/type error (assert via a reflection check that the tenant parameter is required and non-nullable-defaulted).
    - login() against a MockHandler-seeded success response returns an instance of LoginResult; mfaRequired reflects the response.
    - A customCa path sets Guzzle verify to that path (assert via a testable accessor), never to a disabled state.
  </behavior>
  <action>
    Write `ClientConstructionTest`: (1) reflect on `AxiamClient::__construct` to assert the `tenant`
    parameter is required with no default (SC#1/D-13); (2) drive `login()` through a `MockHandler`
    returning a canned login success and assert `instanceof LoginResult` and the `mfaRequired` value;
    (3) assert a `customCa` path flows to Guzzle's verify option (via a test seam) and that the
    default is strict verification.
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpunit --filter ClientConstructionTest</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter ClientConstructionTest` passes.
    - The reflection assertion confirms `tenant` is a required, non-defaulted constructor parameter.
    - The login() assertion confirms a typed `LoginResult` return.
  </acceptance_criteria>
  <done>SC#1 proven: required tenant + typed LoginResult + strict-TLS default with customCa escape.</done>
</task>

<task type="auto">
  <name>Task 3: Runnable REST examples (login/MFA + authz)</name>
  <files>sdks/php/examples/login_mfa.php, sdks/php/examples/rest_authz.php</files>
  <read_first>
    - sdks/php/src/AxiamClient.php (public API surface used by examples)
    - sdks/python/examples (sibling example shape)
    - sdks/CONTRACT.md §1 (method names the examples call)
  </read_first>
  <action>
    Write two standalone runnable examples using ONLY the public `AxiamClient` API: `login_mfa.php`
    demonstrates `login()` → branch on `mfaRequired` → `verifyMfa()`; `rest_authz.php` demonstrates
    `checkAccess()`/`can()` over REST. Each requires `vendor/autoload.php`, constructs the client
    with a required tenant, and reads config from env vars. No TLS-disable anywhere.
  </action>
  <verify>
    <automated>cd sdks/php && php -l examples/login_mfa.php && php -l examples/rest_authz.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` reports no syntax errors for both examples.
    - Both construct `AxiamClient` with a tenant argument and call only public methods.
    - Neither example contains a TLS-disable pattern (`grep` clean).
  </acceptance_criteria>
  <done>Two runnable REST examples exercising login/MFA and authz via the public API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → AxiamClient public API | Credentials/tenant cross into the SDK; misconfiguration surfaces here |
| SDK → AXIAM server (TLS) | Transport security enforced at client construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-20 | Tampering / Information Disclosure | Guzzle client TLS config | high | mitigate | `verify: true` default; only a `customCa` bundle path escape (§6/D-12); CI grep gate (plan 09) |
| T-22-21 | Elevation of Privilege | missing tenant context | medium | mitigate | `tenant` required constructor param (no nullable default), injected per request (§5/D-13) |
| T-22-22 | Information Disclosure | tokens on the public surface | high | mitigate | Auth flows return `LoginResult` with `Sensitive`-typed token fields; no raw token exposed (§7/D-11) |
</threat_model>

<verification>
- `vendor/bin/phpunit --filter ClientConstructionTest` green (SC#1).
- `php -l` clean on both examples; no TLS-disable pattern anywhere in this plan's files.
- PHPStan L6 clean on `src/AxiamClient.php`; auth flows return typed `LoginResult`.
</verification>

<success_criteria>
`composer require` + `$client->login($email, $password)` returns a typed `LoginResult`, tenant is a
required constructor parameter, and authz delegates transparently — SC#1 satisfied end-to-end.
</success_criteria>

<output>
Create `.planning/phases/22-php-sdk/22-06-SUMMARY.md` when done.
</output>
