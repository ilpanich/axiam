---
phase: 22-php-sdk
plan: 07
type: execute
wave: 5
depends_on: ["22-06"]
files_modified:
  - sdks/php/composer.json
  - sdks/php/src/Laravel/AxiamServiceProvider.php
  - sdks/php/src/Laravel/AxiamMiddleware.php
  - sdks/php/src/Laravel/AxiamGate.php
  - sdks/php/tests/LaravelMiddlewareTest.php
  - sdks/php/examples/laravel_app/routes.php
  - sdks/php/examples/laravel_app/README.md
autonomous: true
requirements: [PHP-01]
must_haves:
  truths:
    - "The Laravel ServiceProvider is auto-discovered via composer.json extra.laravel.providers — zero manual wiring after composer require (D-01)"
    - "AxiamMiddleware verifies the token (local JWKS) and populates framework identity with user_id/tenant_id/roles; 401 on missing/invalid token (D-02, §10)"
    - "AxiamGate calls can(resource, action) -> 403 on deny; the server additive-only RBAC is authoritative — no client-side deny-override or caching beyond token TTL (D-02)"
    - "The runnable Laravel example demonstrates BOTH auth middleware and can()->403 (SC#4)"
    - "All Laravel classes are guarded by class_exists('Illuminate...') so the core has zero framework runtime deps (D-01)"
  artifacts:
    - "sdks/php/src/Laravel/{AxiamServiceProvider,AxiamMiddleware,AxiamGate}.php"
    - "sdks/php/composer.json extra.laravel.providers block"
    - "sdks/php/tests/LaravelMiddlewareTest.php, examples/laravel_app/"
  key_links:
    - "extra.laravel.providers -> AxiamServiceProvider auto-discovery"
    - "AxiamMiddleware -> AxiamClient::verifyLocallyOrFallback (no duplicated verify logic)"
  prohibitions:
    - "Never add illuminate/* to composer.json require (runtime) — dev-only + class_exists guards (D-01)"
    - "Never duplicate JWKS-verify or refresh logic in the bridge — call AxiamClient methods (D-02)"
    - "Never implement client-side deny-override or authz caching beyond the token TTL (project RBAC constraint)"
---

<objective>
Deliver the first-class Laravel bridge (D-01/D-02): an auto-discovered `AxiamServiceProvider`,
`AxiamMiddleware` (auth: local-JWKS verify + identity population, 401 on failure), and `AxiamGate`
(authz: `can()` -> 403), all guarded by `class_exists('Illuminate\...')` so the core stays
framework-free. Ship a runnable example demonstrating BOTH auth and `can()`->403 (SC#4).

Purpose: give Laravel developers zero-config integration beyond `composer require` (the accurate
half of D-01's auto-discovered bridges), reusing the core client verify/authz methods (D-02).
Output: `src/Laravel/*`, `composer.json` `extra.laravel.providers`, `tests/LaravelMiddlewareTest.php`,
`examples/laravel_app/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-php-sdk/22-CONTEXT.md
@.planning/phases/22-php-sdk/22-RESEARCH.md
@sdks/CONTRACT.md
</context>

## Artifacts this phase produces (plan 22-07)

- `Axiam\Sdk\Laravel\AxiamServiceProvider` — auto-discovered provider (registers Gate + middleware alias)
- `Axiam\Sdk\Laravel\AxiamMiddleware` — auth middleware (local verify + identity population, 401)
- `Axiam\Sdk\Laravel\AxiamGate` — authz gate calling `can()` -> 403
- `sdks/php/composer.json` `extra.laravel.providers` entry
- `sdks/php/tests/LaravelMiddlewareTest.php`, `sdks/php/examples/laravel_app/`

<tasks>

<task type="auto">
  <name>Task 1: composer.json auto-discovery + Laravel ServiceProvider/Middleware/Gate</name>
  <files>sdks/php/composer.json, sdks/php/src/Laravel/AxiamServiceProvider.php, sdks/php/src/Laravel/AxiamMiddleware.php, sdks/php/src/Laravel/AxiamGate.php</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 3 — full Laravel code + extra.laravel.providers fragment; Pitfall 5)
    - sdks/python/src/axiam_sdk/django/middleware.py (identity-population auth-middleware analog)
    - sdks/php/src/AxiamClient.php (verifyLocallyOrFallback + can seams — do not duplicate)
    - sdks/CONTRACT.md §10 (Middleware Laravel row), §2 (401/403 mapping)
  </read_first>
  <action>
    Add an `extra.laravel.providers` array to `composer.json` listing
    `Axiam\\Sdk\\Laravel\\AxiamServiceProvider` (true zero-config auto-discovery, D-01). Do NOT add
    illuminate/* to runtime `require` (already dev-only from plan 01). Port `22-RESEARCH.md` Pattern 3
    Laravel code: `AxiamMiddleware::handle()` reads the bearer/cookie token, calls
    `AxiamClient::verifyLocallyOrFallback()` (401 on missing/invalid), and sets an `axiam_user`
    request attribute with `user_id`/`tenant_id`/`roles` (D-02, §10). `AxiamServiceProvider::boot()`
    registers a `Gate::before`/named ability delegating to `can()` (403 on deny) and a middleware
    alias; `AxiamGate` encapsulates the ability. Every Laravel class reference is reached only inside
    a Laravel app (class_exists guard), so a non-Laravel consumer never needs illuminate installed (D-01).
  </action>
  <verify>
    <automated>cd sdks/php && composer validate --no-check-publish && vendor/bin/phpstan analyse src/Laravel --level=6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "laravel" composer.json` shows an `extra.laravel.providers` entry naming `AxiamServiceProvider`.
    - `grep -rn "verifyLocallyOrFallback" src/Laravel/AxiamMiddleware.php` matches (no duplicated verify logic).
    - `grep -rn "illuminate/" composer.json` shows illuminate only under require-dev, never runtime require.
    - `composer validate` passes; PHPStan L6 clean on `src/Laravel`.
  </acceptance_criteria>
  <done>Laravel provider/middleware/gate implemented + auto-discovery wired; core stays framework-free.</done>
</task>

<task type="auto">
  <name>Task 2: LaravelMiddlewareTest (auth + can()->403)</name>
  <files>sdks/php/tests/LaravelMiddlewareTest.php</files>
  <read_first>
    - sdks/php/src/Laravel/AxiamMiddleware.php, src/Laravel/AxiamGate.php (targets)
    - .planning/phases/22-php-sdk/22-VALIDATION.md (SC#4 integration row — Laravel TestCase)
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 3 route usage)
  </read_first>
  <action>
    Write `LaravelMiddlewareTest` (Illuminate testing, in the `integration` testsuite): assert a
    request with no token yields 401; a request with a valid (mock-verified) token populates the
    `axiam_user` attribute and passes; a `can()` deny yields 403 while an allow passes. Inject a
    stubbed `AxiamClient` whose `verifyLocallyOrFallback`/`can` are controlled so the test needs no
    live server.
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpunit --filter LaravelMiddlewareTest</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter LaravelMiddlewareTest` passes.
    - Test covers the 401 (no token), authenticated pass, 403 (can deny), and allow paths.
  </acceptance_criteria>
  <done>Laravel middleware + gate proven for auth (401) and authz (403) with a stubbed client.</done>
</task>

<task type="auto">
  <name>Task 3: Runnable Laravel example</name>
  <files>sdks/php/examples/laravel_app/routes.php, sdks/php/examples/laravel_app/README.md</files>
  <read_first>
    - sdks/php/src/Laravel/AxiamServiceProvider.php, AxiamMiddleware.php, AxiamGate.php
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 3 route usage comment)
  </read_first>
  <action>
    Provide `routes.php` defining a protected route using the `axiam.auth` middleware alias and the
    `can:axiam,documents,read` gate (SC#4: demonstrates BOTH auth and `can()`->403). `README.md`
    shows the zero-config auto-discovery story (no manual provider registration needed) and the env
    vars, referencing CONTRACT.md conformance. No TLS-disable anywhere.
  </action>
  <verify>
    <automated>cd sdks/php && php -l examples/laravel_app/routes.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l examples/laravel_app/routes.php` reports no syntax errors.
    - `routes.php` uses the auth middleware alias AND a `can:`-style gate check (SC#4 both halves).
    - README states auto-discovery requires no manual provider registration for Laravel.
  </acceptance_criteria>
  <done>Runnable Laravel example demonstrating auth middleware + can()->403 with auto-discovery docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incoming HTTP request → Laravel app (SDK middleware) | Untrusted bearer/cookie token crosses into identity population |
| framework authz decision → server RBAC | Local gate must defer to the server's authoritative decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-23 | Spoofing | `AxiamMiddleware` token handling | high | mitigate | Local JWKS verify (alg-pin + tenant_id, plan 02) before identity population; 401 on failure (D-02) |
| T-22-24 | Elevation of Privilege | `AxiamGate` authz | high | mitigate | `can()` delegates to server additive-only RBAC; no client-side deny-override or caching beyond token TTL (D-02) |
| T-22-25 | Tampering | dependency footprint | low | mitigate | illuminate/* dev-only + class_exists guards; core runtime stays framework-free (D-01) |
</threat_model>

<verification>
- `vendor/bin/phpunit --filter LaravelMiddlewareTest` green (401 / authenticated / 403 / allow).
- `composer validate` passes; `extra.laravel.providers` present; illuminate/* dev-only.
- PHPStan L6 clean on `src/Laravel`; example passes `php -l`; no TLS-disable pattern.
</verification>

<success_criteria>
Laravel bridge auto-discovers, authenticates (local verify + identity, 401) and authorizes
(`can()`->403) with a runnable example (SC#4-Laravel), reusing core client methods and keeping the
core framework-free.
</success_criteria>

<output>
Create `.planning/phases/22-php-sdk/22-07-SUMMARY.md` when done.
</output>
