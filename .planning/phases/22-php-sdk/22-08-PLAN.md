---
phase: 22-php-sdk
plan: 08
type: execute
wave: 5
depends_on: ["22-06"]
files_modified:
  - sdks/php/src/Symfony/AxiamBundle.php
  - sdks/php/src/Symfony/AxiamAuthSubscriber.php
  - sdks/php/src/Symfony/AxiamVoter.php
  - sdks/php/tests/SymfonyAuthSubscriberTest.php
  - sdks/php/examples/symfony_app/bundles.php
  - sdks/php/examples/symfony_app/services.yaml
  - sdks/php/examples/symfony_app/README.md
autonomous: true
requirements: [PHP-01]
must_haves:
  truths:
    - "AxiamAuthSubscriber (kernel.request) verifies the token (local JWKS) and populates the Symfony security identity with user_id/tenant_id/roles; 401 on missing/invalid token (D-02, §10)"
    - "AxiamVoter calls can(resource, action) -> deny -> 403; server additive-only RBAC is authoritative (D-02)"
    - "The Symfony bridge requires MANUAL registration (config/bundles.php + services.yaml) — documented honestly, NOT described as auto-discovered like Laravel (Pitfall 5)"
    - "The runnable Symfony example demonstrates BOTH auth subscriber and Voter->403 (SC#4)"
    - "All Symfony classes are guarded by class_exists/interface_exists so the core has zero framework runtime deps (D-01)"
  artifacts:
    - "sdks/php/src/Symfony/{AxiamBundle,AxiamAuthSubscriber,AxiamVoter}.php"
    - "sdks/php/tests/SymfonyAuthSubscriberTest.php, examples/symfony_app/ (bundles.php + services.yaml + README)"
  key_links:
    - "AxiamAuthSubscriber -> AxiamClient::verifyLocallyOrFallback (no duplicated verify logic)"
    - "AxiamVoter::voteOnAttribute -> AxiamClient::can"
  prohibitions:
    - "Never add symfony/* to composer.json require (runtime) — dev-only + class_exists guards (D-01)"
    - "Never describe the Symfony bridge as auto-discovered / zero-config like Laravel (Pitfall 5)"
    - "Never duplicate JWKS-verify or refresh logic in the bridge — call AxiamClient methods (D-02)"
    - "Never implement client-side deny-override or authz caching beyond the token TTL (project RBAC constraint)"
---

<objective>
Deliver the Symfony bridge (D-01/D-02): `AxiamBundle`, `AxiamAuthSubscriber` (kernel.request auth:
local-JWKS verify + identity population, 401), and `AxiamVoter` (authz: `can()` → 403), all guarded
by framework class/interface presence so the core stays framework-free. Ship a runnable example
demonstrating BOTH auth and Voter→403 (SC#4) — with HONEST manual-registration docs (Pitfall 5:
Symfony has no Laravel-style zero-config discovery without a Flex recipe, which is out of scope).

Purpose: give Symfony developers first-class integration without misrepresenting the registration
experience, reusing the core client verify/authz methods (D-02).
Output: `src/Symfony/*`, `tests/SymfonyAuthSubscriberTest.php`, `examples/symfony_app/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-php-sdk/22-CONTEXT.md
@.planning/phases/22-php-sdk/22-RESEARCH.md
@sdks/CONTRACT.md
</context>

## Artifacts this phase produces (plan 22-08)

- `Axiam\Sdk\Symfony\AxiamBundle` — bundle bootstrap (manually registered)
- `Axiam\Sdk\Symfony\AxiamAuthSubscriber` — kernel.request auth subscriber (local verify + identity, 401)
- `Axiam\Sdk\Symfony\AxiamVoter` — authz voter calling `can()` → 403
- `sdks/php/tests/SymfonyAuthSubscriberTest.php`, `sdks/php/examples/symfony_app/` (manual-registration example)

<tasks>

<task type="auto">
  <name>Task 1: AxiamBundle + AxiamAuthSubscriber + AxiamVoter</name>
  <files>sdks/php/src/Symfony/AxiamBundle.php, sdks/php/src/Symfony/AxiamAuthSubscriber.php, sdks/php/src/Symfony/AxiamVoter.php</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 3 — full Symfony EventSubscriber/Voter PHP code; Pitfall 5 manual registration)
    - sdks/python/src/axiam_sdk/django/middleware.py (identity-population analog)
    - sdks/php/src/AxiamClient.php (verifyLocallyOrFallback + can seams — do not duplicate)
    - sdks/CONTRACT.md §10 (EventSubscriber Symfony row), §2 (401/403 mapping)
  </read_first>
  <action>
    Port `22-RESEARCH.md` Pattern 3 Symfony code: `AxiamAuthSubscriber implements
    EventSubscriberInterface` subscribing to `KernelEvents::REQUEST`; `onKernelRequest` reads the
    cookie/Authorization token, calls `AxiamClient::verifyLocallyOrFallback()` (sets a 401
    JsonResponse on missing/invalid), and populates the request identity with
    `user_id`/`tenant_id`/`roles` (D-02, §10). `AxiamVoter extends Voter`; `supports` matches
    `resource:action` attributes; `voteOnAttribute` returns `AxiamClient::can($resource, $action)`
    (server additive-only RBAC authoritative — no client deny-override). `AxiamBundle` is the bundle
    bootstrap. Every Symfony class reference is guarded by `class_exists`/`interface_exists` so a
    non-Symfony consumer never needs symfony/* installed (D-01, dev-only from plan 01).
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpstan analyse src/Symfony --level=6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "verifyLocallyOrFallback" src/Symfony/AxiamAuthSubscriber.php` matches (no duplicated verify logic).
    - `grep -rn "->can(" src/Symfony/AxiamVoter.php` matches (delegates to server RBAC).
    - `grep -rn "symfony/" composer.json` shows symfony only under require-dev, never runtime require.
    - PHPStan L6 clean on `src/Symfony`.
  </acceptance_criteria>
  <done>Symfony bundle/subscriber/voter implemented; core stays framework-free.</done>
</task>

<task type="auto">
  <name>Task 2: SymfonyAuthSubscriberTest (auth + Voter->403)</name>
  <files>sdks/php/tests/SymfonyAuthSubscriberTest.php</files>
  <read_first>
    - sdks/php/src/Symfony/AxiamAuthSubscriber.php, src/Symfony/AxiamVoter.php (targets)
    - .planning/phases/22-php-sdk/22-VALIDATION.md (SC#4 integration row — Symfony KernelTestCase)
  </read_first>
  <action>
    Write `SymfonyAuthSubscriberTest` (in the `integration` testsuite): construct a `RequestEvent`
    with no token and assert a 401 JsonResponse is set; with a valid (stub-verified) token assert the
    identity attributes are populated and no response is short-circuited; drive `AxiamVoter` with a
    denied `can()` and assert ACCESS_DENIED (→403) and an allowed `can()` for ACCESS_GRANTED. Inject
    a stubbed `AxiamClient` so no live server is needed.
  </action>
  <verify>
    <automated>cd sdks/php && vendor/bin/phpunit --filter SymfonyAuthSubscriberTest</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter SymfonyAuthSubscriberTest` passes.
    - Test covers 401 (no token), authenticated pass, Voter deny (403), and Voter allow.
  </acceptance_criteria>
  <done>Symfony subscriber + voter proven for auth (401) and authz (403) with a stubbed client.</done>
</task>

<task type="auto">
  <name>Task 3: Runnable Symfony example with honest manual-registration docs</name>
  <files>sdks/php/examples/symfony_app/bundles.php, sdks/php/examples/symfony_app/services.yaml, sdks/php/examples/symfony_app/README.md</files>
  <read_first>
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pattern 3 config/bundles.php + services.yaml block; Pitfall 5)
    - sdks/php/src/Symfony/AxiamBundle.php, AxiamAuthSubscriber.php, AxiamVoter.php
  </read_first>
  <action>
    Provide `bundles.php` registering `Axiam\Sdk\Symfony\AxiamBundle` and `services.yaml` tagging
    `AxiamAuthSubscriber` (`kernel.event_subscriber`) and `AxiamVoter` (`security.voter`) — the
    MANUAL registration Symfony requires (Pitfall 5). `README.md` documents these manual steps
    explicitly and prominently and states Symfony does NOT get Laravel-style zero-config discovery,
    plus references CONTRACT.md conformance. The example demonstrates both auth and Voter->403 (SC#4).
  </action>
  <verify>
    <automated>cd sdks/php && php -l examples/symfony_app/bundles.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l examples/symfony_app/bundles.php` reports no syntax errors.
    - `services.yaml` tags the subscriber (`kernel.event_subscriber`) and the voter (`security.voter`).
    - README explicitly states manual `config/bundles.php` + `services.yaml` registration is required (no auto-discovery claim, Pitfall 5).
  </acceptance_criteria>
  <done>Runnable Symfony example with honest manual-registration docs demonstrating auth + Voter->403.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incoming HTTP request → Symfony app (SDK subscriber) | Untrusted cookie/Authorization token crosses into identity population |
| framework authz decision → server RBAC | Voter must defer to the server's authoritative decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-26 | Spoofing | `AxiamAuthSubscriber` token handling | high | mitigate | Local JWKS verify (alg-pin + tenant_id, plan 02) before identity population; 401 on failure (D-02) |
| T-22-27 | Elevation of Privilege | `AxiamVoter` authz | high | mitigate | `can()` delegates to server additive-only RBAC; no client-side deny-override or caching beyond token TTL (D-02) |
| T-22-28 | Tampering | dependency footprint | low | mitigate | symfony/* dev-only + class_exists/interface_exists guards; core runtime stays framework-free (D-01) |
</threat_model>

<verification>
- `vendor/bin/phpunit --filter SymfonyAuthSubscriberTest` green (401 / authenticated / 403 / allow).
- `grep` gates confirm no duplicated verify logic, symfony dev-only, manual-registration example.
- PHPStan L6 clean on `src/Symfony`; example passes `php -l`; README honest about manual registration (Pitfall 5).
</verification>

<success_criteria>
Symfony bridge authenticates (local verify + identity, 401) and authorizes (Voter->403) with a
runnable, honestly-documented manual-registration example (SC#4-Symfony), reusing core client
methods and keeping the core framework-free.
</success_criteria>

<output>
Create `.planning/phases/22-php-sdk/22-08-SUMMARY.md` when done.
</output>
