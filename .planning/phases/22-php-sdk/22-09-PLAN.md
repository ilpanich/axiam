---
phase: 22-php-sdk
plan: 09
type: execute
wave: 6
depends_on: ["22-07", "22-08"]
files_modified:
  - .github/workflows/sdk-ci-php.yml
  - sdks/php/README.md
autonomous: true
requirements: [PHP-01]
user_setup:
  - service: packagist
    why: "Live Packagist publish of axiam/axiam-sdk via the read-only subtree-split mirror repo (D-05); pipeline passes in-phase, first live publish may be a maintainer action"
    dashboard_config:
      - task: "Create the read-only mirror repo (e.g. ilpanich/axiam-php-sdk) and register it on Packagist with the GitHub webhook"
        location: "GitHub (new repo) + packagist.org (Submit package)"
      - task: "Add the mirror push deploy key / token as a CI secret (e.g. PHP_SDK_MIRROR_TOKEN); absent secret degrades the publish job to a documented no-op"
        location: "Repo Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "sdk-ci-php.yml runs composer validate + composer install + composer test (full PHPUnit suite incl. SC#2 single-flight, HMAC, redaction, JWKS, framework bridges) on PRs touching sdks/php/** (SC#5)"
    - "The TLS-bypass grep gate over sdks/php/ (source + examples + tests) returns empty for the Guzzle TLS-disable pattern, excluding customCa (D-12/SC#4)"
    - "On a sdks/php/vX.Y.Z tag push, a publish job subtree-splits sdks/php/ to the mirror repo and triggers the Packagist update (D-05), gated behind the passing build-test job"
    - "README documents CONTRACT.md §1-§10 conformance, the Swoole/RoadRunner + process-supervision runtime requirement (SC#3), and Symfony manual registration (Pitfall 5)"
  artifacts:
    - ".github/workflows/sdk-ci-php.yml (build/test/TLS-gate/subtree-split->mirror->Packagist)"
    - "sdks/php/README.md (conformance + runtime-requirement + Symfony-manual-registration callouts)"
  key_links:
    - "publish job needs: build-test -> only publishes after test + grep-gate pass on the tagged commit"
    - "subtree-split -> mirror push -> Packagist webhook update"
  prohibitions:
    - "The publish job must NOT fail the pipeline when the mirror/Packagist credentials are absent — degrade to a documented no-op warning (D-05, same posture as C# NUGET_API_KEY-absent)"
    - "README must NOT describe the Symfony bridge as auto-discovered/zero-config like Laravel (Pitfall 5)"
    - "The TLS-bypass gate must NOT be weakened to allow any TLS-verification-disable pattern; only customCa is excluded"
---

<objective>
Extend the `sdk-ci-php.yml` scaffold-check stub into a full lifecycle (SC#5): `composer validate` +
`composer install` + `composer test` (the whole PHPUnit suite from plans 01-08) + the TLS-bypass
grep gate (D-12/SC#4) on PRs, and a tag-triggered publish job that subtree-splits `sdks/php/` to a
read-only mirror repo and triggers the Packagist update (D-05), degrading gracefully when
mirror/Packagist credentials are absent. Rewrite the README with CONTRACT conformance, the
Swoole/RoadRunner + process-supervision runtime callout (SC#3), and honest Symfony manual-registration
docs (Pitfall 5).

Purpose: close SC#5 (green `composer test` + Packagist automation) and the SC#3/SC#4 documentation
obligations, wiring every prior plan's tests into one CI gate.
Output: `.github/workflows/sdk-ci-php.yml`, `sdks/php/README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-php-sdk/22-CONTEXT.md
@.planning/phases/22-php-sdk/22-RESEARCH.md
@sdks/CONTRACT.md
</context>

## Artifacts this phase produces (plan 22-09)

- `.github/workflows/sdk-ci-php.yml` — build-test job (validate/install/test/TLS-gate) + publish job (subtree-split → mirror → Packagist)
- `sdks/php/README.md` — CONTRACT §1-§10 conformance, runtime-requirement callout (SC#3), Symfony manual-registration note (Pitfall 5)

<tasks>

<task type="auto">
  <name>Task 1: sdk-ci-php.yml — build/test + TLS-bypass gate + subtree-split Packagist publish</name>
  <files>.github/workflows/sdk-ci-php.yml</files>
  <read_first>
    - .github/workflows/sdk-ci-php.yml (existing scaffold-check stub — extend, keep the paths filter)
    - .github/workflows/sdk-ci-csharp.yml (full lifecycle: build-test + tag-gated publish + credential-absent graceful no-op)
    - .github/workflows/sdk-ci-python.yml (interpreted-ecosystem ordering: install deps before test)
    - .planning/phases/22-php-sdk/22-PATTERNS.md (Shared Patterns: CI lifecycle + subtree-split step + TLS grep gate shape)
    - .planning/phases/22-php-sdk/22-CONTEXT.md (D-05 subtree-split to mirror + tag sdks/php/vX.Y.Z)
  </read_first>
  <action>
    Extend the workflow: keep the `pull_request` `paths:` filter and add a `push: tags:
    ['sdks/php/v*']` trigger. Add a `build-test` job (no event-name restriction so it also runs on
    the tag) that sets up PHP 8.1 (setup-php action, major-tag pinned with the same documented
    SHA-pin-follow-up note the C#/Python workflows use), runs `composer validate --no-check-publish`,
    `composer install`, then `composer test` (full suite). Add a TLS-bypass gate step that greps
    `sdks/php/` (`--include=*.php`, source + examples + tests) for the Guzzle TLS-verification-disable
    pattern and fails if any match remains after excluding the `customCa` escape hatch; the gate is
    <!-- planner-discipline-allow: verify.*=>.*false -->
    the regex `verify.*=>.*false` piped through `grep -v customCa`, expecting empty output (D-12/SC#4).
    Add a `publish` job gated `if: github.event_name == 'push' && startsWith(github.ref,
    'refs/tags/sdks/php/v')` and `needs: build-test`: it `git subtree split`s (or splitsh-lite)
    `sdks/php/` and pushes to the read-only mirror repo re-tagged `vX.Y.Z`, letting Packagist
    auto-update via its webhook. When the mirror token / Packagist credential secret is absent, the
    step degrades to a `::warning::` no-op (NOT a failure) — the same posture as
    `sdk-ci-csharp.yml`'s `NUGET_API_KEY`-absent branch (D-05). Keep `actions/checkout` SHA-pinned
    to the repo-wide pin.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/sdk-ci-php.yml')); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The workflow YAML parses (yaml.safe_load succeeds).
    - `grep -n "composer test" .github/workflows/sdk-ci-php.yml` and `grep -n "composer validate" .github/workflows/sdk-ci-php.yml` both match (SC#5).
    - A TLS-bypass gate step is present, greps `sdks/php` `--include=*.php`, and excludes `customCa` (D-12/SC#4).
    - A `publish` job exists gated on `refs/tags/sdks/php/v`, `needs: build-test`, with a subtree-split step and a credential-absent graceful-no-op branch (D-05).
  </acceptance_criteria>
  <done>Full PHP CI lifecycle: validate/install/test + TLS gate on PRs, subtree-split Packagist publish on tag with graceful degradation.</done>
</task>

<task type="auto">
  <name>Task 2: README — conformance + runtime requirement + Symfony manual registration</name>
  <files>sdks/php/README.md</files>
  <read_first>
    - sdks/php/README.md (existing scaffold)
    - sdks/CONTRACT.md (§1-§10 the README must reference as conformance)
    - .planning/phases/22-php-sdk/22-RESEARCH.md (Pitfall 5 Symfony manual registration; Pitfall 6 process supervision; SC#3 runtime callout)
    - sdks/php/examples/laravel_app/README.md, examples/symfony_app/README.md (bridge-specific docs to link)
  </read_first>
  <action>
    Rewrite `README.md`: (1) a one-line install (`composer require axiam/axiam-sdk`) + a quickstart
    constructing `AxiamClient` with the required tenant and calling `login()`; (2) a prominent
    "Runtime requirements" section stating gRPC (`ext-grpc`) + the AMQP consumer require a
    long-running runtime (Swoole/RoadRunner/CLI), not standard PHP-FPM, and that the AMQP worker
    relies on process supervision to restart on connection loss (SC#3, Pitfall 6); (3) a statement
    that the SDK conforms to `sdks/CONTRACT.md` §1-§10; (4) framework sections: Laravel is
    auto-discovered, Symfony requires MANUAL `config/bundles.php` + `services.yaml` registration
    (Pitfall 5 — do NOT claim Symfony auto-discovery); (5) the TLS policy (strict verify, `customCa`
    only) and the `Sensitive` redaction note. Link to the two example apps.
  </action>
  <verify>
    <automated>cd sdks/php && grep -q "CONTRACT.md" README.md && grep -qi "RoadRunner\|Swoole" README.md && grep -qi "manual" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README references `sdks/CONTRACT.md` conformance.
    - README has a prominent runtime-requirement callout naming Swoole/RoadRunner (SC#3) and process supervision (Pitfall 6).
    - README documents Symfony MANUAL registration and does NOT describe it as auto-discovered (Pitfall 5).
    - README documents strict TLS + `customCa`-only escape and the `Sensitive` redaction behavior.
  </acceptance_criteria>
  <done>README states CONTRACT conformance, runtime requirements (SC#3), Symfony manual registration (Pitfall 5), and TLS/Sensitive policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → published Packagist artifact | Build/release integrity crosses into the distributed package |
| CI secrets → publish job | Missing credentials must degrade safely, never bypass a security gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-29 | Tampering / Information Disclosure | TLS-bypass in shipped source | high | mitigate | CI grep gate over `sdks/php/**.php` returns empty for the TLS-verification-disable pattern (excluding customCa); belt-and-suspenders to the in-code default (D-12/SC#4) |
| T-22-30 | Tampering | supply-chain / publish integrity | medium | mitigate | `publish` job `needs: build-test` (test + TLS gate pass on the tagged commit); SHA-pinned checkout; only user-locked Packagist-verified deps (RESEARCH Package Legitimacy Audit — all Approved) |
| T-22-31 | Elevation of Privilege | absent-credential publish path | medium | mitigate | Credential-absent step degrades to a `::warning::` no-op — never falls back to an insecure or unauthenticated publish (D-05) |
</threat_model>

<verification>
- `.github/workflows/sdk-ci-php.yml` parses; contains `composer validate`/`composer test`, the TLS-bypass gate, and the tag-gated subtree-split publish job with graceful degradation.
- `composer test` (the aggregate of plans 01-08 suites) is the CI gate (SC#5).
- README documents CONTRACT conformance, SC#3 runtime requirement, Symfony manual registration (Pitfall 5), and TLS/Sensitive policy.
</verification>

<success_criteria>
`composer test` passes in CI and Packagist automation (`axiam/axiam-sdk`) runs on the
`sdks/php/vX.Y.Z` release tag via subtree-split (SC#5); the TLS-bypass gate is empty (SC#4); the
runtime requirement is documented prominently (SC#3).
</success_criteria>

<output>
Create `.planning/phases/22-php-sdk/22-09-SUMMARY.md` when done.
</output>
