---
phase: 25-security-hardening-ii-federation-pki-data-protection-infra
plan: 05
type: execute
wave: 2
depends_on: [25-04]
files_modified:
  - crates/axiam-server/src/cleanup.rs
  - crates/axiam-api-rest/src/handlers/gdpr.rs
  - crates/axiam-server/tests/cleanup_task.rs
  - crates/axiam-api-rest/tests/gdpr_test.rs
autonomous: true
requirements: [SECHRD-06, SECHRD-08]

must_haves:
  truths:
    - "A GDPR purge whose pseudonymize_actor step fails leaves the user re-selectable and writes NO erasure proof (SECHRD-06 / D-03a)"
    - "The erasure proof is the strictly-last write, after anonymize_user succeeds (SECHRD-06 / D-03a, proof-last)"
    - "The GDPR export contains real (metadata-only) sessions data with token material redacted, not a hardcoded [] (SECHRD-06 / D-03c)"
    - "The ExportReady producer resolves the real org_id from the tenant — cleanup.rs no longer enqueues Uuid::nil() (SECHRD-08 / D-05d)"
  artifacts:
    - "run_erasure_pipeline<A,EP,U> extracted free function in cleanup.rs"
    - "cleanup.rs / handlers/gdpr.rs export assembly wired to SessionRepository::list_by_user"
    - "cleanup.rs ExportReady org_id resolved from tenant; actor_id nil fix"
  key_links:
    - "run_erasure_pipeline: pseudonymize_actor? -> anonymize_user? -> erasure_proof.create? (proof strictly last, all fatal)"
    - "export session assembly consumes list_by_user from plan 25-04 and drops token_hash"
    - "ExportReady message carries a real org_id resolved from the tenant repo before enqueue"
---

<objective>
Make GDPR erasure durable and the export complete (SECHRD-06), and fix the undeliverable-mail root cause on the producer side (SECHRD-08 / D-05d). Reorder `cleanup.rs::purge_single_user` so a failed `pseudonymize_actor` is FATAL (today it is swallowed with `tracing::warn!` by deliberate prior design — RESEARCH Pitfall 2) and the erasure proof is written strictly LAST, only after every PII-bearing step succeeds (D-03a). Extract the erasure step sequence into a small trait-generic free function `run_erasure_pipeline` so a test can inject a failing audit repo (RESEARCH Pattern 3 — `CleanupTask` is not generic over repo traits). Wire the export session assembly to `SessionRepository::list_by_user` (plan 25-04), redacting token material (D-03c). Resolve the real `org_id` from the tenant before enqueuing ExportReady mail, replacing `Uuid::nil()` (D-05d), and fix the `actor_id: Uuid::nil()` placeholder.

Purpose: Never certify an erasure while PII survives; export real sessions; make ExportReady mail carry a real org_id (SECHRD-06 + SECHRD-08 producer half).
Output: reordered/extracted purge pipeline + real-sessions export + org_id resolution + two negative tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-security-hardening-ii-federation-pki-data-protection-infra/25-RESEARCH.md
@.planning/phases/25-security-hardening-ii-federation-pki-data-protection-infra/25-PATTERNS.md
@.planning/phases/25-security-hardening-ii-federation-pki-data-protection-infra/25-04-SUMMARY.md
@crates/axiam-server/src/cleanup.rs
@crates/axiam-server/tests/cleanup_task.rs
</context>

<assumption_delta_decision>
noun: n/a
decision: no-change
rationale: The assumption-delta detector fired a FALSE POSITIVE — it substring-matched "selectable" inside "re-selectable" in success criterion 3, which describes GDPR erasure *atomicity* (a failed `pseudonymize_actor` leaves the user row eligible for a retry), NOT a derived→chosen architectural transition and NOT a new identity/primary-key decision. No identity model is restructured on account of this; the erasure "re-selection flags" are the pre-existing `deletion_pending`/`find_due_for_purge` mechanism from Phase 10 (CQ-B38), preserved unchanged.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Extract run_erasure_pipeline; make pseudonymize_actor fatal; write erasure proof strictly last</name>
  <files>crates/axiam-server/src/cleanup.rs</files>
  <read_first>
    - crates/axiam-server/src/cleanup.rs:246-411 (`purge_single_user` body — current step order (d) pseudonymize_actor [swallowed], (e) erasure_proof.create, (f) mark completed, (g) anonymize_user) and :327-344 (the `if let Err(e) = ... { tracing::warn!(...) }` swallow to REMOVE), :337-380 (erasure-proof rows), :391 (`actor_id: Uuid::nil()` to fix), :54-80 (concrete `Arc<SurrealXxxRepository<C>>` fields — why extraction is needed)
    - 25-RESEARCH.md "Pattern 3: Test-Seam Extraction" + Pitfalls 2 and 3 (pseudonymize is non-fatal by deliberate comment, not oversight; proof currently precedes anonymize_user) and Assumptions Log A3 (recommended order: pseudonymize -> anonymize_user -> erasure_proof.create; anonymize_user stays the flag-clearing step so re-selectability is preserved on partial failure)
    - 25-PATTERNS.md lines 223-248 (the exact `run_erasure_pipeline<A,EP,U>` shape and the Phase-24 GDPR audit dead-letter that must be preserved additively)
    - .planning/phases/24-security-hardening-i-authentication-access-control-surfaces/24-CONTEXT.md D-02 note (the audit dead-letter in cleanup.rs to keep, not conflict with)
  </read_first>
  <action>
    Extract a free function `async fn run_erasure_pipeline<A, EP, U>(audit_repo: &A, erasure_proof_repo: &EP, user_repo: &U, tenant_id: Uuid, user_id: Uuid, pseudonym: &str, email_hash: &str) -> AxiamResult<()>` bounded `A: AuditLogRepository, EP: ErasureProofRepository, U: UserRepository`. Body order: `audit_repo.pseudonymize_actor(...).await?;` (FATAL now — replace the swallowing `if let Err ... tracing::warn!` guard with `?`), then `user_repo.anonymize_user(...).await?;`, then LAST `erasure_proof_repo.create(CreateErasureProof { ... }).await?;` (the DB UNIQUE index from plan 25-04 makes a retry's duplicate CREATE idempotent — D-03b). Do not reorder anonymize_user to be the very last statement; it stays before the proof so `find_due_for_purge`'s `deletion_pending` flag is the re-selection anchor (A3 — re-selectability preserved).
    Have `CleanupTask::purge_single_user` call `run_erasure_pipeline` with its concrete `Arc<SurrealXxxRepository<C>>` fields (auto-deref to the trait). Preserve the Phase-24 audit dead-letter (file + syslog) behavior additively — the proof-last reorder wraps around it, it is not removed.
    Fix `actor_id: Uuid::nil()` (:391) to the correct actor identity for the erasure audit event (resolve from the deletion request / operator context already available in `purge_single_user`).
    No `unwrap`/`expect`/`.ok()` swallow on any PII-bearing step; every step propagates with `?`.
  </action>
  <verify>
    <automated>export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip; cargo test -p axiam-server --lib cleanup 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `run_erasure_pipeline` exists, is trait-generic over `AuditLogRepository`/`ErasureProofRepository`/`UserRepository`, and orders steps pseudonymize -> anonymize -> proof (proof strictly last)
    - `pseudonymize_actor` failure propagates with `?` (the `tracing::warn!` swallow is gone)
    - `actor_id: Uuid::nil()` at the erasure audit event is replaced with a real actor id
    - Phase-24 audit dead-letter behavior is preserved (not removed)
    - `cargo clippy -p axiam-server --lib -- -D warnings` clean
  </acceptance_criteria>
  <done>The purge pipeline is extracted, pseudonymize is fatal, the proof is last, and the audit dead-letter still fires.</done>
</task>

<task type="auto">
  <name>Task 2: Wire real (metadata-only) sessions into the export; resolve ExportReady org_id from the tenant</name>
  <files>crates/axiam-server/src/cleanup.rs, crates/axiam-api-rest/src/handlers/gdpr.rs</files>
  <read_first>
    - crates/axiam-server/src/cleanup.rs:503-519 (ExportReady enqueue) and :508 (`org_id: Uuid::nil()` to replace) — the export sweep + producer site
    - crates/axiam-api-rest/src/handlers/gdpr.rs (the synchronous Art. 15 export assembly — grep for the hardcoded `sessions` / `[]` field to locate where session data is assembled; wire `SessionRepository::list_by_user` there and/or in the cleanup.rs sweep, wherever the `[]` currently is)
    - 25-04-SUMMARY.md (`SessionRepository::list_by_user` signature) and 25-RESEARCH.md Pitfall 4 (export exactly `{id, created_at, expires_at, ip_address, user_agent}`; exclude `token_hash`) + D-03c
    - 25-PATTERNS.md line 219 (`org_id` resolution producer-side; mirror how `oidc_start_public` resolves org_id/tenant_id from slugs — the "resolve real id before use, never a placeholder" convention)
  </read_first>
  <action>
    Locate the hardcoded empty `sessions` (`[]`) in the export assembly (cleanup.rs export sweep and/or handlers/gdpr.rs) and replace it with a projection over `session_repo.list_by_user(tenant_id, user_id)`, emitting per session exactly `{id, created_at, expires_at, ip_address, user_agent}` — OMIT `token_hash` (live credential material, D-03c) and do not reference a non-existent `last_seen`. Honor the existing per-item shutdown checks in the sweep (CQ-B38 residual).
    In the ExportReady producer (cleanup.rs:508), resolve the real `org_id` from the tenant before building the message (via the org/tenant repo already available to `CleanupTask`), replacing `Uuid::nil()` (D-05d, producer-side per planner discretion). The mail-retry/backoff and end-to-end deliverability test live in plan 25-08 (mail consumer); this task only guarantees the enqueued message carries a real org_id.
    Thread `session_repo` into `CleanupTask`/the export handler if not already present (a `web::Data`/`Arc` field addition, mirroring the other repo fields).
  </action>
  <verify>
    <automated>export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip; cargo test -p axiam-api-rest --test gdpr_test export_includes_real_session_metadata 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p axiam-api-rest --test gdpr_test export_includes_real_session_metadata` passes: after creating a session, the export's `sessions` array is non-empty and contains `{id, created_at, expires_at, ip_address, user_agent}` with NO `token_hash` (SECHRD-06 negative test, SC #3 export half)
    - The ExportReady message built in cleanup.rs carries a real `org_id` resolved from the tenant, not `Uuid::nil()` (SECHRD-08 producer half, D-05d)
    - No hardcoded `sessions: []` remains in the export assembly; no `last_seen`/`token_hash` in the export
    - `cargo clippy -p axiam-server --lib -- -D warnings` clean (with SWAGGER_UI env set)
  </acceptance_criteria>
  <done>The export carries real redacted session metadata and ExportReady enqueues a real org_id.</done>
</task>

<task type="auto">
  <name>Task 3: Negative test — failed pseudonymize leaves user re-selectable and writes no proof</name>
  <files>crates/axiam-server/tests/cleanup_task.rs</files>
  <read_first>
    - crates/axiam-server/tests/cleanup_task.rs (its own comment documenting that it does NOT exercise `CleanupTask` directly — the test-seam limitation Pattern 3 resolves) and crates/axiam-server/tests/req14_gdpr_test.rs (the narration-via-repo-methods precedent)
    - 25-RESEARCH.md Pattern 3 + "Test Doubles Needed" (a synthetic `AuditLogRepository` double whose `pseudonymize_actor` always returns `Err`, used with the extracted `run_erasure_pipeline`, alongside real in-memory repos for user/erasure_proof) and Pitfall 3 "Warning signs"
    - 25-VALIDATION.md row SC 3 (`erasure_pipeline_fatal_on_pseudonymize_failure`)
  </read_first>
  <action>
    Add `erasure_pipeline_fatal_on_pseudonymize_failure`: construct a local test-double struct implementing `AuditLogRepository` whose `pseudonymize_actor` returns `Err(AxiamError::Internal(...))`; pair it with a real in-memory SurrealDB `UserRepository` + `ErasureProofRepository`. Call `run_erasure_pipeline(...)` and assert: (1) it returns `Err`; (2) the user's `deletion_pending` (re-selection) flag is still true / the user is still returned by `find_due_for_purge` (re-selectable); (3) NO `erasure_proof` row exists for that user. This test must fail before Task 1's reorder and pass after.
  </action>
  <verify>
    <automated>export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip; cargo test -p axiam-server --test cleanup_task erasure_pipeline_fatal_on_pseudonymize_failure 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p axiam-server --test cleanup_task erasure_pipeline_fatal_on_pseudonymize_failure` passes: forced `pseudonymize_actor` failure → `run_erasure_pipeline` returns Err, user remains re-selectable, and NO erasure proof is written (SECHRD-06 negative test, SC #3 atomicity half)
    - The test injects a synthetic failing `AuditLogRepository` double against the extracted function (not the non-generic `CleanupTask`)
  </acceptance_criteria>
  <done>The fatal-pseudonymize negative test passes, proving erasure atomicity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data subject (GDPR Art. 17 erasure / Art. 15 export) → cleanup pipeline | Erasure must be all-or-nothing; a written proof must certify actual PII removal; export must not leak live credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-13 | Repudiation (false erasure certification) | purge_single_user ordering | high | mitigate | pseudonymize fatal + proof strictly last (Task 1); negative test `erasure_pipeline_fatal_on_pseudonymize_failure` (Task 3) |
| T-25-14 | Information Disclosure | export hands back token_hash | high | mitigate | Export projects metadata-only session fields, token_hash omitted (Task 2); negative test asserts no token_hash |
| T-25-15 | Denial of mail delivery | ExportReady with Uuid::nil() org_id | medium | mitigate | Producer resolves real org_id from tenant (Task 2); end-to-end deliverability proven in plan 25-08 |
| T-25-16 | Tampering (duplicate ledger) | retried erasure proof | high | mitigate | UNIQUE index (plan 25-04) + proof-last idempotent retry (Task 1) |
</threat_model>

<verification>
- `cargo test -p axiam-server --test cleanup_task erasure_pipeline_fatal_on_pseudonymize_failure` green
- `cargo test -p axiam-api-rest --test gdpr_test export_includes_real_session_metadata` green
- `cargo test -p axiam-server --lib` green; clippy/fmt clean (SWAGGER_UI env set for api-rest/server builds)
</verification>

<success_criteria>
A failed pseudonymize leaves the user re-selectable with no proof written; the export carries real redacted session metadata; ExportReady enqueues a real org_id — proven by two passing negative tests (SECHRD-06 SC #3; SECHRD-08 producer half).
</success_criteria>

<artifacts>
## Artifacts this plan produces (new symbols — exclude from source-drift checks)
- `run_erasure_pipeline<A, EP, U>(...)` free function in cleanup.rs
- Export session-projection helper (metadata-only) in cleanup.rs/handlers/gdpr.rs
- Tests: `erasure_pipeline_fatal_on_pseudonymize_failure`, `export_includes_real_session_metadata`
</artifacts>

<output>
Create `.planning/phases/25-security-hardening-ii-federation-pki-data-protection-infra/25-05-SUMMARY.md` when done.
</output>
