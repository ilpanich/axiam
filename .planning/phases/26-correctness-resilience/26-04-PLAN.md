---
phase: 26-correctness-resilience
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - frontend/e2e/auth-contract.spec.ts
autonomous: true
requirements: [CORR-04]
must_haves:
  truths:
    - "The CI e2e job runs `npx playwright test` (via `npm run test:e2e`) against the seeded backend as a blocking step, with `vitest run` kept as its own separate blocking step (D-11)"
    - "All present Playwright specs gate the build; any spec covering an unfinished feature is test.skip-ed with a tracking note (green means green, D-12)"
    - "The contract spec asserts request BODIES (not just paths), catching a SECFIX-06 regression"
    - "The existing playwright-report artifact upload now reflects real Playwright runs (D-13)"
  artifacts:
    - ".github/workflows/ci.yml e2e job with a blocking test:e2e step + a separate blocking vitest step"
    - "frontend/e2e/auth-contract.spec.ts asserting request bodies (SECFIX-06)"
  key_links:
    - "ci.yml e2e job -> served frontend (localhost:5173) + seeded backend -> playwright test -> playwright-report artifact"
---

<objective>
Fix CORR-04: the CI "e2e" job's "run E2E tests" step actually runs `npm test` (= `vitest run`) — Playwright never executes, so all 13 `frontend/e2e/*.spec.ts` specs (including the SECFIX-06 body-asserting `auth-contract.spec.ts`) have never gated a build. Wire `npx playwright test` into the existing e2e job as a distinct blocking step against the already-seeded backend (D-11), keep `vitest run` as its own separate blocking step, ensure all present specs gate the build (fixing or `test.skip`-with-note any covering unfinished features, D-12), and confirm the contract spec asserts request bodies.

Purpose: Make the auth/login/contract e2e specs actually run and block merges, so regressions (e.g. SECFIX-06's tenant_id/email request bodies) are caught.
Output: A corrected CI e2e job and a verified body-asserting contract spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-correctness-resilience/26-RESEARCH.md
@.planning/phases/26-correctness-resilience/26-PATTERNS.md
</context>

<artifacts_this_plan_produces>
- `.github/workflows/ci.yml`: the e2e job's "Serve frontend and run E2E tests" step is changed so it runs `npm run test:e2e` (Playwright, blocking) against the served build + seeded backend, plus a separate blocking `npm test` (vitest run) step. The existing `npx playwright install chromium`, backend seeding, and `playwright-report` upload steps are retained.
- `frontend/e2e/auth-contract.spec.ts`: confirmed/strengthened to assert request BODIES for the SECFIX-06 reset/resend flows.
- Any `frontend/e2e/*.spec.ts` covering an unfinished feature: `test.skip` with an inline tracking note (only if genuinely broken; enumerate at execution time).
- No new packages (@playwright/test already a devDependency).
</artifacts_this_plan_produces>

<sandbox_note>
Local Playwright execution is blocked in this sandbox — the proxy denies the Chromium browser-binary download (documented in 23-06-SUMMARY.md when auth-contract.spec.ts/logout.spec.ts were authored). Therefore the load-bearing GREEN for these specs is CI-gated, not locally reproducible. This plan's automated verifications assert: (a) the CI YAML wires the Playwright + vitest steps, (b) specs type-check/lint clean and are discoverable via `playwright test --list` where the environment allows, and (c) the contract spec source asserts request bodies. The true end-to-end Playwright pass is a CI verification (see Manual/CI-Only Verifications).
</sandbox_note>

<tasks>

<task type="auto">
  <name>Task 1: Run Playwright in the CI e2e job as a blocking step; keep vitest separate (D-11)</name>
  <read_first>
    - .github/workflows/ci.yml (the `e2e` job, lines 279-364 — note line 344 runs `npm test` = vitest, the serve/E2E_BASE_URL env at 334-352, the `npx playwright install chromium` step at 330-332, and the playwright-report upload at 354-359)
    - frontend/package.json (scripts: `test` = `vitest run`, `test:e2e` = `playwright test`)
    - frontend/playwright.config.ts (confirm reuseExistingServer + E2E_BASE_URL usage so no extra plumbing is needed)
    - .planning/phases/26-correctness-resilience/26-RESEARCH.md § "Don't Hand-Roll" (CI test runner glue) and § CORR-04 findings
    - .planning/phases/26-correctness-resilience/26-PATTERNS.md § ci.yml
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <behavior>
    - The e2e job serves the built frontend on localhost:5173 (as today), then runs `npm run test:e2e` (Playwright) as a blocking step whose non-zero exit fails the job.
    - `vitest run` (`npm test`) remains as its own separate blocking step (both required per D-11), not conflated into the e2e run.
    - The seeded backend, `npx playwright install chromium`, and `playwright-report` upload (`if: always()`) steps are unchanged and reused.
    - E2E_BASE_URL and the E2E_* env remain available to the Playwright step.
  </behavior>
  <action>
    In the `e2e` job's "Serve frontend and run E2E tests" step, replace the `npm test` invocation (line 344) with `npm run test:e2e` (Playwright) so the served build + seeded backend are actually exercised by Playwright; keep the serve/SERVE_PID wait/kill scaffolding and the `env:` block (CI, E2E_BASE_URL, E2E_ORG_SLUG, E2E_TENANT_SLUG, E2E_ADMIN_EMAIL, E2E_ADMIN_PASSWORD). Add a SEPARATE blocking step (e.g. "Run frontend unit tests") in the same job running `npm test` (= `vitest run`) in `working-directory: frontend` — it does not require the served frontend, so it can run before or after the Playwright step, but it must be its own step so both suites independently gate the build. Do not remove the `npx playwright install chromium` step or the `playwright-report` upload step. Keep all existing action SHA pins unchanged.
  </action>
  <verify>
    <automated>cd /home/user/axiam && grep -q "npm run test:e2e" .github/workflows/ci.yml && grep -Eq "vitest run|npm test" .github/workflows/ci.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The `e2e` job contains a step running `npm run test:e2e` (Playwright) — `grep -q "npm run test:e2e" .github/workflows/ci.yml` succeeds.
    - The `e2e` job still runs `npm test` / `vitest run` as its own step.
    - The `npx playwright install chromium` step and the `playwright-report` upload step (`if: always()`) remain present.
    - The workflow YAML parses (no syntax error) — `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (or an equivalent YAML lint).
  </acceptance_criteria>
  <done>The CI e2e job runs Playwright against the seeded served frontend as a blocking step, with vitest as a separate blocking step; browser install and report upload retained.</done>
</task>

<task type="auto">
  <name>Task 2: Triage all e2e specs to gate the build; verify contract spec asserts bodies (D-12/SECFIX-06)</name>
  <read_first>
    - frontend/e2e/auth-contract.spec.ts (the SECFIX-06 contract spec — confirm it asserts request BODIES, not just paths, for requestPasswordReset/confirmPasswordReset/resendVerification)
    - frontend/e2e/login.spec.ts, frontend/e2e/logout.spec.ts (auth flow specs referenced by SECFIX-05/06)
    - .planning/REQUIREMENTS.md §SECFIX-06 (lines 652-662 — the exact reset/resend body shape the contract spec must assert: {tenant_id, email/…})
    - .planning/phases/26-correctness-resilience/26-RESEARCH.md § "Open Questions" (13 specs, enumerate at plan time; do not hardcode 12) and § "Assumptions Log" A4
    - .planning/phases/26-correctness-resilience/26-PATTERNS.md § ci.yml (the 13-spec list)
  </read_first>
  <files>frontend/e2e/auth-contract.spec.ts</files>
  <behavior>
    - Enumerate the actual spec files present (13 as of research: auth-contract, certificates, dashboard, federation, identity, login, logout, organizations, roles, service-accounts, settings, tenants, users). All present specs are collected by `playwright test` (none are excluded by config).
    - auth-contract.spec.ts asserts the REQUEST BODY of the reset/resend calls contains tenant_id (and email for resend), matching the backend DTOs — a path-only assertion is insufficient.
    - Any spec that covers a feature not yet finished (e.g. a flow this phase or a later phase completes) is marked `test.skip` with an inline tracking note (comment referencing the requirement/CQ id) rather than left failing — "green means green" (D-12). Do NOT skip auth/login/contract specs; those must execute.
  </behavior>
  <action>
    Run `npx playwright test --list` (does not launch a browser) to enumerate every collected spec and confirm all present files compile and are discovered; if `--list` is unavailable in-sandbox, fall back to `npx tsc -b --noEmit` + `npm run lint` in `frontend/` to prove the specs type-check. Read `auth-contract.spec.ts` and confirm it asserts the request body of each SECFIX-06 call (parse the intercepted request `postData`/JSON and assert `tenant_id` present, plus `email` for resend). If any assertion only checks the URL/path, strengthen it to assert the body per §SECFIX-06. For any spec found to cover an unfinished feature, wrap the offending `test(...)` in `test.skip(...)` with a one-line comment citing the tracking id (do not silently delete). Leave the auth/login/logout/contract specs executable. Do not add new dependencies.
  </action>
  <verify>
    <automated>cd /home/user/axiam/frontend && grep -Eq "tenant_id" e2e/auth-contract.spec.ts && grep -Eq "postData|request\(\)|\.body|JSON.parse" e2e/auth-contract.spec.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `auth-contract.spec.ts` asserts the request BODY (intercepted postData/JSON) contains `tenant_id` (and `email` for resend), not merely the request path.
    - Every `frontend/e2e/*.spec.ts` present is either executable or `test.skip`-ed with an inline tracking-note comment (no silently-failing spec).
    - `npx tsc -b --noEmit` and `npm run lint` in `frontend/` exit 0 (specs compile/lint clean).
  </acceptance_criteria>
  <done>All present e2e specs gate the build (broken ones skipped-with-note), and the contract spec asserts SECFIX-06 request bodies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate -> merge | A misconfigured "e2e" job that runs the wrong suite lets auth/contract regressions merge undetected |
| frontend -> auth API (reset/resend) | The contract spec is the regression guard that these requests carry the tenant_id/email the backend requires (SECFIX-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-04-01 | Tampering (process integrity) | e2e job running vitest instead of Playwright | high | mitigate | Wire `npm run test:e2e` as a blocking step so the auth/login/contract specs actually gate the build (Task 1) |
| T-26-04-02 | Repudiation/Spoofing (enumeration) | contract spec must not weaken enumeration-safe auth | high | mitigate | The contract spec asserts request bodies only; it does NOT assert account-existence-dependent responses — the SECFIX-06 constant-response/enumeration-safe behavior stays intact and unweakened (Task 2 reads §SECFIX-06 to keep assertions body-shape-only) |
| T-26-04-03 | Repudiation | silently skipped specs hiding failures | medium | mitigate | Skips require an inline tracking note; auth/login/contract specs may never be skipped (D-12) |
| T-26-04-SC | Tampering | npm installs | low | accept | No new packages this phase; @playwright/test already a devDependency |
</threat_model>

<verification>
- `grep -q "npm run test:e2e" .github/workflows/ci.yml` succeeds and the workflow YAML parses.
- `auth-contract.spec.ts` asserts request bodies containing tenant_id/email (SECFIX-06).
- `npx tsc -b --noEmit` + `npm run lint` in `frontend/` green.
- CI-gated: the e2e job's Playwright step passes on CI (see Manual/CI-Only Verifications).
</verification>

<manual_ci_only_verifications>
| Behavior | Requirement | Why not local | Verification |
|----------|-------------|---------------|--------------|
| Playwright specs actually pass against the seeded backend | CORR-04 | Sandbox proxy blocks the Chromium binary download (23-06-SUMMARY) | Push the branch; confirm the CI `E2E Tests` job runs `npx playwright test`, is green/blocking, and uploads a real `playwright-report` reflecting the run |
</manual_ci_only_verifications>

<success_criteria>
The CI e2e job runs `npx playwright test` against the seeded backend (vitest kept separate), the auth/login/contract specs gate the build, and the contract spec asserts request bodies (ROADMAP SC #4 / CORR-04 acceptance criteria).
</success_criteria>

<output>
Create `.planning/phases/26-correctness-resilience/26-04-SUMMARY.md` when done.
</output>
