---
phase: 27-performance-load-hardening
plan: 07
type: execute
wave: 3
depends_on: ["27-05"]
files_modified:
  - crates/axiam-auth/Cargo.toml
  - crates/axiam-auth/benches/auth_bench.rs
  - crates/axiam-pki/Cargo.toml
  - crates/axiam-pki/benches/cert_bench.rs
  - crates/axiam-authz/Cargo.toml
  - crates/axiam-authz/benches/authz_bench.rs
  - claude_dev/performance-report.md
autonomous: true
requirements: [PERF-05]
must_haves:
  truths:
    - "criterion micro-benches exist for the three critical paths — auth (Argon2id verify + EdDSA token mint), authz (single CheckAccess + a sequential-vs-concurrent BatchCheckAccess comparison), cert-validation (X.509 chain verify) — each under its owning crate's benches/ dir (D-14)"
    - "The authz batch bench injects artificial per-call latency so the sequential-vs-concurrent comparison is meaningful (kv-mem is near-zero latency, RESEARCH Open Question 2)"
    - "claude_dev/performance-report.md records baseline-vs-optimized numbers for auth, authz-check, and cert validation (D-16)"
    - "Benches are manual/local only — NOT wired into CI (D-15)"
  artifacts:
    - crates/axiam-auth/benches/auth_bench.rs
    - crates/axiam-authz/benches/authz_bench.rs
    - crates/axiam-pki/benches/cert_bench.rs
    - claude_dev/performance-report.md
    - "criterion dev-dependency + [[bench]] harness=false stanzas in axiam-auth/authz/pki"
  key_links:
    - "auth_bench calls AuthConfig::resolve_keys() ONCE outside the timed closure (steady-state signing cost)"
    - "cert_bench uses rcgen fixtures generated once, benches only the parse + verify_signature step"
---

<objective>
Add greenfield `criterion` micro-benchmarks for AXIAM's three critical paths — auth,
authz-check, cert-validation — living under each owning crate's `benches/` dir (D-14),
and record baseline-vs-optimized numbers in `claude_dev/performance-report.md` (D-16).
Benches are manual/local, NOT a CI gate (D-15). Measurable optimizations are applied only
where warranted (D-17) — for this phase the material optimization is PERF-02's concurrent
batch, which the authz bench documents as baseline (sequential) vs optimized (concurrent).

Purpose: Close PERF-05. Every item benches an existing, already-correct core (Argon2/EdDSA,
the authz decision, X.509 verify) — the bench measures AROUND it and must never touch the
crypto/verification logic (V6). Report is documentation-only, no regression gate.
Output: three criterion benches + a populated performance report.
</objective>

## Artifacts this phase produces

- New files: `crates/axiam-auth/benches/auth_bench.rs`, `crates/axiam-authz/benches/authz_bench.rs`, `crates/axiam-pki/benches/cert_bench.rs`, `claude_dev/performance-report.md`
- New dev-dependencies: `criterion` (`html_reports`) in axiam-auth/authz/pki; `futures` (dev) in axiam-authz for the batch comparison (already in Cargo.lock)
- New `[[bench]] harness = false` stanzas in the three crates' Cargo.toml

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-performance-load-hardening/27-CONTEXT.md
@.planning/phases/27-performance-load-hardening/27-RESEARCH.md
@.planning/phases/27-performance-load-hardening/27-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: auth_bench + cert_bench (criterion greenfield)</name>
  <files>crates/axiam-auth/Cargo.toml, crates/axiam-auth/benches/auth_bench.rs, crates/axiam-pki/Cargo.toml, crates/axiam-pki/benches/cert_bench.rs</files>
  <read_first>
    - crates/axiam-auth/src/password.rs lines 1-75 (`hash_password(password, pepper)`, `verify_password(password, hash, pepper)` — pure sync)
    - crates/axiam-auth/src/token.rs lines 1-100 (`issue_access_token(...)`; `AuthConfig::resolve_keys()` pre-parses the Ed25519 PEM — call it ONCE outside the timed closure)
    - crates/axiam-pki/src/mtls.rs (the isolated `client_x509.verify_signature(Some(ca_x509.public_key()))` step; NOT the full `authenticate()` which needs DB repos); `rcgen` is already an axiam-pki dependency
    - 27-PATTERNS.md § "Criterion benches"; 27-RESEARCH.md § "PERF-05 bench targets" + § Standard Stack (criterion resolves at execution time — re-run `cargo add --dry-run`)
  </read_first>
  <action>
    Add `criterion = { version = "0.5", features = ["html_reports"] }` as a `[dev-dependencies]`
    entry to `axiam-auth/Cargo.toml` and `axiam-pki/Cargo.toml`, and a `[[bench]] name = "auth_bench"`
    / `name = "cert_bench"` with `harness = false` stanza in each. Run `cargo add criterion --dev --dry-run -p axiam-auth`
    first to resolve and confirm the current 0.5.x from crates.io (RESEARCH marks the exact patch
    [ASSUMED]). Write `benches/auth_bench.rs` with criterion groups for: `verify_password` (steady state),
    `hash_password`, and `issue_access_token` — call `AuthConfig::resolve_keys()` ONCE outside the timed
    closure so the bench measures steady-state signing cost, not PEM parse. Write `benches/cert_bench.rs`:
    use `rcgen` to generate a self-signed CA + one leaf cert signed by it ONCE outside the timed closure,
    then bench just the `parse_x509_certificate` + `verify_signature(Some(ca.public_key()))` step. Use
    `criterion::black_box` on inputs. Do NOT modify any password/token/mtls source — benches only measure
    around the existing functions (V6).
  </action>
  <verify>
    <automated>cd /home/user/axiam && cargo bench -p axiam-auth --no-run && cargo bench -p axiam-pki --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `crates/axiam-auth/benches/auth_bench.rs` and `crates/axiam-pki/benches/cert_bench.rs` exist and use `criterion_group!`/`criterion_main!`
    - `axiam-auth/Cargo.toml` and `axiam-pki/Cargo.toml` each contain a `[[bench]]` with `harness = false` and a `criterion` dev-dependency
    - auth_bench calls `resolve_keys()` outside the timed closure; cert_bench builds rcgen fixtures outside the timed closure
    - `cargo bench -p axiam-auth --no-run` and `cargo bench -p axiam-pki --no-run` exit 0 (benches compile)
    - No source file under `src/` of axiam-auth or axiam-pki is modified (benches only)
  </acceptance_criteria>
  <done>auth_bench + cert_bench compile as criterion harness=false benches; no crypto source touched.</done>
</task>

<task type="auto">
  <name>Task 2: authz_bench — single CheckAccess + sequential-vs-concurrent batch</name>
  <files>crates/axiam-authz/Cargo.toml, crates/axiam-authz/benches/authz_bench.rs</files>
  <read_first>
    - crates/axiam-authz/src/engine.rs lines 1-140 (`AuthorizationEngine::check_access(&self, request: &AccessRequest) -> AxiamResult<AccessDecision>`)
    - crates/axiam-authz/Cargo.toml (already has `surrealdb` kv-mem dev-dep at line 23 + `tokio-test`; add `criterion` + `futures` dev-deps)
    - crates/axiam-authz's own existing tests (for the kv-mem seeding pattern of a role/permission/resource graph)
    - 27-PATTERNS.md § "authz_bench.rs"; 27-RESEARCH.md § Pattern 2 "Benchmark" note + § Open Question 2 (inject artificial per-call latency — kv-mem is near-zero latency)
  </read_first>
  <action>
    Add `criterion` (`html_reports`) and `futures` as `[dev-dependencies]` to `axiam-authz/Cargo.toml`
    (both already resolved in Cargo.lock) plus a `[[bench]] name = "authz_bench"` `harness = false` stanza.
    Write `benches/authz_bench.rs` using a `tokio::runtime::Runtime` + criterion's async bench
    (`b.to_async(&rt).iter(...)`). Bench group 1: a single `AuthorizationEngine::check_access` against a
    seeded kv-mem graph (or in-memory repo stubs) — the CPU-cost baseline. Bench group 2: the
    sequential-vs-concurrent BatchCheckAccess comparison — over a bench-only repo (or wrapper) that injects
    a deliberate `tokio::time::sleep(Duration::from_millis(2))` per `check_access` call to simulate realistic
    I/O, compare a sequential loop / `buffer_unordered(1)` baseline against `futures::stream::...buffer_unordered(16)`
    for an N-item batch. This produces the baseline-vs-optimized evidence for PERF-02's "faster than sequential"
    criterion. Do NOT modify `engine.rs`.
  </action>
  <verify>
    <automated>cd /home/user/axiam && cargo bench -p axiam-authz --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `crates/axiam-authz/benches/authz_bench.rs` exists with a single-check group and a sequential-vs-concurrent batch group that injects per-call latency
    - `axiam-authz/Cargo.toml` contains `criterion` + `futures` dev-dependencies and a `[[bench]] harness = false`
    - `cargo bench -p axiam-authz --no-run` exits 0 (bench compiles)
    - `crates/axiam-authz/src/engine.rs` is unmodified
  </acceptance_criteria>
  <done>authz_bench compiles: single check_access + a meaningful sequential-vs-concurrent batch comparison with injected latency.</done>
</task>

<task type="auto">
  <name>Task 3: Run benches + author performance-report.md</name>
  <files>claude_dev/performance-report.md</files>
  <read_first>
    - claude_dev/security-audit.md (sibling doc for TONE/FORMAT only — content is greenfield)
    - 27-CONTEXT.md § PERF-05 (D-14..D-17); 27-RESEARCH.md § Environment Availability (cargo-flamegraph may be unavailable in the sandbox — document the invocation as a manual step even if generation is deferred)
  </read_first>
  <action>
    Run `cargo bench -p axiam-auth`, `cargo bench -p axiam-authz`, and `cargo bench -p axiam-pki`
    (scoped per crate; `cargo clean` only BETWEEN plans, not during this run). Create
    `claude_dev/performance-report.md` recording, for auth / authz-check / cert-validation: the
    criterion output numbers (mean/median with units), and for the authz batch group the
    baseline (sequential) vs optimized (concurrent) numbers that evidence PERF-02's improvement
    (D-16). Document the `cargo-flamegraph` invocation as a manual step (e.g.
    `cargo flamegraph --bench authz_bench`) and note if flamegraph generation is deferred because
    OS-level profiling (`perf`) is unavailable in the sandbox (RESEARCH Environment Availability).
    State explicitly that these benches are manual/local and NOT a CI gate (D-15) and that the
    report is documentation-only with no regression gate (D-16). If a live SurrealDB is not
    available for a real-RTT authz number, record the injected-latency simulation result and note
    the caveat.
  </action>
  <verify>
    <automated>cd /home/user/axiam && test -f claude_dev/performance-report.md && grep -qi 'baseline' claude_dev/performance-report.md && grep -qi 'optimized' claude_dev/performance-report.md && echo REPORT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `claude_dev/performance-report.md` exists and contains baseline-vs-optimized numbers for auth, authz-check, and cert validation
    - The report states benches are manual/local and NOT a CI gate (D-15/D-16) and documents the cargo-flamegraph invocation
    - `test -f claude_dev/performance-report.md && grep -qi 'baseline' && grep -qi 'optimized'` succeeds (REPORT_OK)
  </acceptance_criteria>
  <done>All three benches run; performance-report.md records baseline-vs-optimized numbers with the manual/non-CI framing and flamegraph note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench harness → crypto/verification functions | Benches call (measure) `verify_password`/`issue_access_token`/`verify_signature`/`check_access` — they must never modify these functions |
| dev tooling → CI | Benches are dev-only; wiring them into CI would introduce perf-flakiness (explicitly avoided, D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-40 | Tampering | bench/profiling code accidentally altering crypto/verify logic | low | mitigate | Acceptance criteria require zero `src/` changes in axiam-auth/authz/pki — benches only measure around existing functions (V6) |
| T-27-41 | Denial of Service (CI flakiness) | benches wired as a CI gate producing flaky perf failures | low | accept | D-15 locked: benches are manual/local, non-CI; the report is documentation-only with no regression gate (D-16) |
| T-27-SC | Tampering (supply chain) | adding `criterion` (+ dev `futures`) as dev-dependencies | low | mitigate | Dev-dependencies only — never shipped in the server binary; RESEARCH Package Legitimacy Audit verdict OK/Approved (github.com/bheisler/criterion.rs; futures rust-lang, already in Cargo.lock); `cargo add --dry-run` confirms the crates.io source/version before commit |
</threat_model>

<verification>
- `cargo bench -p axiam-auth --no-run`, `-p axiam-authz --no-run`, `-p axiam-pki --no-run` all compile
- `cargo bench -p axiam-auth`, `-p axiam-authz`, `-p axiam-pki` run and produce numbers (manual/local)
- `claude_dev/performance-report.md` present with baseline-vs-optimized numbers
- These benches build only low-level crates (axiam-auth/authz/pki), none of which depend on axiam-api-rest, so the SWAGGER_UI workaround is not required here
- Build-hygiene: scope every cargo command to `-p <crate>`; `cargo clean` only BETWEEN plans
</verification>

<success_criteria>
PERF-05 success criterion #5 is met: criterion benches exist for auth, authz-check, and
cert-validation, and claude_dev/performance-report.md records baseline-vs-optimized numbers
(manual/local, non-CI-gated).
</success_criteria>

<output>
Create `.planning/phases/27-performance-load-hardening/27-07-SUMMARY.md` when done
</output>
