---
phase: 28-functional-completeness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/axiam-api-rest/tests/federation_first_time_sso_test.rs
autonomous: true
requirements: [FUNC-01, FUNC-02, FUNC-05]
must_haves:
  truths:
    - "A first-time OIDC SSO user with no pre-existing local account completes start→callback and receives AXIAM access/refresh cookies; a subsequent /auth/me returns 200 (FUNC-01, closes CQ-B40)"
    - "The federation metadata endpoint is reachable with no auth header (FUNC-01)"
    - "After a password reset, the original session cookie is rejected with 401 (FUNC-02)"
    - "POST /auth/login OpenAPI documents 200/202/403/401 as distinct responses (FUNC-05)"
  artifacts:
    - "crates/axiam-api-rest/tests/federation_first_time_sso_test.rs — new e2e test for first-time OIDC SSO + public metadata"
    - "Green run of the existing password_reset_revokes_sessions e2e (FUNC-02 proof)"
    - "Confirmation of the login response schema in openapi.rs (FUNC-05 proof)"
  key_links:
    - "oidc_start_public → oidc_callback_public → create_session_and_tokens → Set-Cookie (axiam_access/refresh/csrf)"
    - "PUBLIC_PATHS metadata bypass → metadata reachable without JWT"
---

<objective>
Close the verify-and-narrow-gap requirements. FUNC-01: add the missing end-to-end HTTP test that creates a
federation config via the API, drives the public OIDC `start → callback` flow against a mock IdP, and asserts
AXIAM cookies are set and `/auth/me` succeeds for the newly-provisioned first-time user (closes CQ-B40), plus
an assertion that the federation metadata endpoint is reachable with no auth header. FUNC-02 and FUNC-05 are
already implemented per RESEARCH — this plan VERIFIES them (runs the existing session-revocation e2e; confirms
the login OpenAPI documents 200/202/403/401) with zero production code.

Purpose: Prove the first-time SSO path end to end and confirm the two already-shipped requirements are green.
Output: A new passing e2e for first-time OIDC SSO; documented confirmation that FUNC-02 and FUNC-05 hold.

NOTE ON FUNC-01 SCOPE SPLIT: FUNC-01's OpenAPI documentation of the four public SSO handlers (D-12) is
delivered by plan 28-04 Task 2 (it shares openapi.rs with the email-config route triangle). This plan owns
FUNC-01's runtime e2e + public-metadata verification. Do NOT edit openapi.rs here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-functional-completeness/28-CONTEXT.md
@.planning/phases/28-functional-completeness/28-RESEARCH.md
@.planning/phases/28-functional-completeness/28-PATTERNS.md
@crates/axiam-api-rest/tests/password_reset_revokes_sessions.rs
@crates/axiam-api-rest/tests/req5_oidc_e2e.rs
@crates/axiam-api-rest/src/handlers/federation.rs
@crates/axiam-api-rest/src/permissions.rs
@crates/axiam-api-rest/src/openapi.rs
</context>

## Artifacts this plan produces

- `crates/axiam-api-rest/tests/federation_first_time_sso_test.rs` (NEW) — an e2e test driving public
  `/api/v1/auth/federation/oidc/start` → `/callback` against a wiremock mock IdP, asserting Set-Cookie
  (axiam_access/axiam_refresh/axiam_csrf) + `/api/v1/auth/me` 200 for a newly-provisioned user, plus a
  no-auth-header assertion on the federation metadata endpoint.
- Verification evidence (recorded in SUMMARY) that `password_reset_revokes_sessions` passes (FUNC-02) and that
  the login OpenAPI documents 200/202/403/401 distinctly (FUNC-05).

<plan_decision id="FUNC-01-org-id-nil">
RESEARCH flags a pre-existing `TODO(T19.15)`: SSO-provisioned tokens carry `org_id: Uuid::nil()`. This is NOT
in FUNC-01's acceptance criteria (which require only that AXIAM tokens are returned for a first-time user) and
is out of this phase's scope. The e2e asserts cookies are set and /auth/me succeeds; it does not assert a
resolved org_id. Do not fix T19.15 here.
</plan_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: First-time OIDC SSO e2e + public-metadata assertion (FUNC-01, closes CQ-B40)</name>
  <files>crates/axiam-api-rest/tests/federation_first_time_sso_test.rs</files>
  <read_first>
    - crates/axiam-api-rest/tests/password_reset_revokes_sessions.rs — test_app! macro, setup_db(), cookie extraction from resp.response().cookies(), me_status() helper (~230-249), assertion style
    - crates/axiam-api-rest/tests/req5_oidc_e2e.rs — the wiremock MockServer pattern for OIDC JWKS/discovery and how OidcVerificationService is exercised; reuse its mock-IdP setup
    - crates/axiam-api-rest/src/handlers/federation.rs — oidc_start_public (~1150) and oidc_callback_public (~1278-1383): the request DTOs they accept, that tokens are returned as Set-Cookie (axiam_access/refresh/csrf) NOT JSON body, and SsoLoginSuccessResponse body fields; the public saml metadata handler (~952) and its route path
    - .planning/phases/28-functional-completeness/28-RESEARCH.md §"FUNC-01" (the exact e2e steps; cookies-not-JSON assertion; metadata public via PUBLIC_PATHS)
  </read_first>
  <behavior>
    - Creating a federation (OIDC) config via the authenticated API, then driving public /oidc/start and /oidc/callback (with the mock IdP), sets axiam_access/axiam_refresh/axiam_csrf cookies and returns a success body.
    - Using those cookies, /api/v1/auth/me returns 200 for the newly-provisioned first-time user (no pre-existing local account).
    - GET on the federation metadata endpoint with NO Authorization header / no session cookie returns a success status (public), not 401/403.
  </behavior>
  <action>
    Create federation_first_time_sso_test.rs. Reuse the test_app! macro and setup_db()/cookie helpers from
    password_reset_revokes_sessions.rs and the wiremock mock-IdP setup from req5_oidc_e2e.rs. Steps: (1) create an
    OIDC federation config via the authenticated config API pointing discovery/JWKS at the mock IdP; (2) call the
    public `/api/v1/auth/federation/oidc/start` and capture state; (3) call the public
    `/api/v1/auth/federation/oidc/callback` with the mock code+state so provision_new_user runs for a brand-new
    subject; (4) assert axiam_access and axiam_refresh cookies are present on the callback response (assert
    cookies, NOT JSON tokens); (5) call `/api/v1/auth/me` with those cookies and assert 200. Add a separate case
    that GETs the federation metadata endpoint with no auth and asserts a non-401/403 success status. Follow the
    established cookie-based-auth assertion conventions.
  </action>
  <verify>
    <automated>SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip cargo test -p axiam-api-rest --test federation_first_time_sso_test</automated>
  </verify>
  <acceptance_criteria>
    - The test drives public /oidc/start then /oidc/callback and asserts axiam_access + axiam_refresh cookies are set.
    - The test asserts /api/v1/auth/me returns 200 for the newly-provisioned user using those cookies.
    - The test asserts the federation metadata endpoint returns success with no auth header.
    - `cargo test -p axiam-api-rest --test federation_first_time_sso_test` (SWAGGER_UI_DOWNLOAD_URL exported) exits 0.
  </acceptance_criteria>
  <done>First-time OIDC SSO returns AXIAM cookies end to end and metadata is proven public (CQ-B40 closed).</done>
</task>

<task type="auto">
  <name>Task 2: Verify session invalidation on password reset (FUNC-02, zero code)</name>
  <files>crates/axiam-api-rest/tests/password_reset_revokes_sessions.rs</files>
  <read_first>
    - crates/axiam-api-rest/tests/password_reset_revokes_sessions.rs — password_reset_confirm_revokes_existing_sessions: logs in, confirms /auth/me 200, mints a reset token, POST /api/v1/auth/reset/confirm, re-checks /auth/me with the ORIGINAL cookie and asserts 401
    - crates/axiam-auth/src/password_reset.rs — confirm_reset invalidate_user_sessions + revoke_all_for_user (~293-302) — confirm it is unchanged this phase
    - .planning/phases/28-functional-completeness/28-RESEARCH.md §"FUNC-02" (VERIFY-ONLY)
  </read_first>
  <behavior>
    - After POST /api/v1/auth/reset/confirm, the pre-reset session cookie is rejected (401) on /auth/me.
  </behavior>
  <action>
    Run the existing e2e and confirm it passes with no changes. This is a VERIFY-ONLY task — do NOT modify
    password_reset.rs or the test unless the run fails. If (unexpectedly) the test does not already assert that
    both session tokens AND OAuth2 refresh tokens are rejected after reset, add that missing assertion; otherwise
    make no code change. Record the pass in the SUMMARY as FUNC-02's closure evidence.
  </action>
  <verify>
    <automated>SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip cargo test -p axiam-api-rest --test password_reset_revokes_sessions</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p axiam-api-rest --test password_reset_revokes_sessions` (SWAGGER_UI_DOWNLOAD_URL exported) exits 0.
    - SUMMARY records FUNC-02 as verified with the test name as evidence.
  </acceptance_criteria>
  <done>Session/refresh-token revocation on password reset is proven by a green existing e2e.</done>
</task>

<task type="auto">
  <name>Task 3: Verify login OpenAPI documents success + MFA-required distinctly (FUNC-05, zero code)</name>
  <files>crates/axiam-api-rest/src/openapi.rs</files>
  <read_first>
    - crates/axiam-api-rest/src/handlers/auth.rs — login #[utoipa::path] responses (~259: 200 LoginSuccessResponse / 202 MfaRequiredResponse / 403 MfaSetupRequiredResponse / 401)
    - crates/axiam-api-rest/src/openapi.rs — schema registrations (~189/196) for LoginSuccessResponse / MfaRequiredResponse / MfaSetupRequiredResponse
    - .planning/phases/28-functional-completeness/28-RESEARCH.md §"FUNC-05" (already satisfied) and §"Validation Architecture" (route_openapi_parity note)
  </read_first>
  <behavior>
    - The login endpoint's OpenAPI documents 200, 202, 403, 401 as distinct responses with their respective bodies registered as component schemas.
  </behavior>
  <action>
    Confirm by reading auth.rs's login utoipa::path and openapi.rs's component registrations that all four
    statuses (200/202/403/401) and their response bodies are documented distinctly. Run the parity/openapi lib
    test to confirm the spec builds cleanly. This is VERIFY-ONLY — make NO edits to openapi.rs (do not touch the
    file; 28-04 owns openapi.rs edits this phase). Record the confirmation in the SUMMARY as FUNC-05's closure
    evidence.
  </action>
  <verify>
    <automated>SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip cargo test -p axiam-api-rest --lib route_openapi_parity</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY documents that login OpenAPI covers 200/202/403/401 distinctly with references to auth.rs and openapi.rs line locations.
    - `cargo test -p axiam-api-rest --lib route_openapi_parity` (SWAGGER_UI_DOWNLOAD_URL exported) exits 0.
    - No modification is made to openapi.rs in this plan.
  </acceptance_criteria>
  <done>The login response schema is confirmed SDK-accurate (200/202/403/401 distinct); FUNC-05 closed.</done>
</task>

</tasks>

<threat_model>
security_asvs_level: 1
block_on: high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external IdP → oidc_callback_public | IdP-asserted identity provisions a first-time AXIAM account |
| unauthenticated client → public SSO / metadata routes | These routes intentionally bypass JWT auth |
| pre-reset session cookie → protected route | Must be rejected after a password reset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-04 | Spoofing | First-time SSO account provisioning / IdP-assertion trust | high | mitigate | Existing controls: consume_by_state single-use, server-stored nonce, ID-token signature/iss/aud/exp verify (OidcFederationService) — Task 1 e2e exercises the real callback path proving the flow terminates in AXIAM tokens only after verification |
| T-28-05 | Elevation of Privilege | Stale session reuse after password reset | high | mitigate | FUNC-02: confirm_reset revokes sessions + refresh tokens (already implemented) — Task 2 proves the original cookie is 401 after reset |
| T-28-16 | Information Disclosure | Federation metadata over-exposing secrets on the public endpoint | low | accept | Metadata is public by design (SP/entity descriptor XML — no secrets); Task 1 asserts only reachability, matching the established PUBLIC_PATHS posture |
| T-28-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies added this plan/phase |
</threat_model>

<verification>
- `cargo test -p axiam-api-rest --test federation_first_time_sso_test` green (new e2e).
- `cargo test -p axiam-api-rest --test password_reset_revokes_sessions` green (FUNC-02 verify).
- `cargo test -p axiam-api-rest --lib route_openapi_parity` green (FUNC-05 verify).
- ALL commands require `export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip` first.
- Signed commit per task (the two verify-only tasks commit their SUMMARY evidence; Task 1 commits the new test).
</verification>

<success_criteria>
- A first-time OIDC SSO user receives AXIAM cookies and /auth/me 200; metadata is public (FUNC-01, CQ-B40 closed).
- Post-reset session rejection is proven green (FUNC-02).
- Login OpenAPI documents 200/202/403/401 distinctly (FUNC-05).
</success_criteria>

<output>
Create `.planning/phases/28-functional-completeness/28-05-SUMMARY.md` when done, recording the e2e test name,
the FUNC-02 verification result, and the FUNC-05 openapi confirmation with line references.
</output>
