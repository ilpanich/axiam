---
phase: 29-structural-quality
plan: 04
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - crates/axiam-db/src/helpers.rs
  - crates/axiam-db/src/lib.rs
  - crates/axiam-db/src/repository/mod.rs
  - crates/axiam-db/src/repository/user.rs
  - crates/axiam-db/src/repository/role.rs
  - crates/axiam-db/src/repository/group.rs
  - crates/axiam-db/src/repository/organization.rs
  - crates/axiam-db/src/repository/tenant.rs
  - crates/axiam-db/src/repository/permission.rs
  - crates/axiam-db/src/repository/resource.rs
  - crates/axiam-db/src/repository/scope.rs
  - crates/axiam-db/src/repository/service_account.rs
  - crates/axiam-db/src/repository/session.rs
  - crates/axiam-db/src/repository/audit.rs
  - crates/axiam-db/tests/user_repository_test.rs
autonomous: true
requirements: [QUAL-02, QUAL-07]
must_haves:
  truths:
    - "A generic paginate<T> helper exists in helpers.rs with unit tests and is adopted in the file-group-A list repos (QUAL-02)"
    - "The CountRow struct duplicates in file-group-A repos collapse to helpers::CountRow (QUAL-02)"
    - "File-group-A single-record reads route through helpers::take_first_or_not_found instead of inline .into_iter().next().ok_or_else(...) (QUAL-02)"
    - "The pepper-less second verify_password impl (user.rs, re-exported at mod.rs and lib.rs) is deleted; no live (non-test) caller remains; the canonical axiam-auth::password::verify_password survives (QUAL-07/D-17)"
  artifacts:
    - "crates/axiam-db/src/helpers.rs::paginate<T> (new helper + unit tests)"
  key_links:
    - "user.rs / role.rs are the already-migrated CountRow exemplars; every group-A repo converges on `use crate::helpers::{CountRow, parse_uuid};` + helpers::paginate + helpers::take_first_or_not_found"
    - "crates/axiam-db/tests/user_repository_test.rs swaps `use axiam_db::verify_password;` for `use axiam_auth::password::verify_password;` (signatures match; only the error type differs, irrelevant to its .unwrap() assertions)"
  prohibitions:
    - "No behavior change — this is a pure mechanical dedup pass; the existing repo test suite is the gate (D-03)"
    - "Do not delete the canonical axiam-auth::password::verify_password — only the axiam-db duplicate (D-17)"
    - "Do not sweep inline Uuid::parse_str call sites in other repos — the federation_link.rs local parse_uuid removal is scoped to plan 29-05 (D-07 narrow scope)"
---

<objective>
QUAL-02 (part 1 of 2) — Generic pagination + shared repo-helper adoption across file-group A,
plus QUAL-07 dead-code removal of the pepper-less second verify_password. Add the missing
generic paginate<T> helper, then mechanically collapse the duplicated CountRow structs, adopt
paginate<T>, and route single-record reads through take_first_or_not_found in the group-A list
repos. Delete the axiam-db verify_password duplicate and repoint its only (test) caller at the
canonical axiam-auth impl. Behavior-preserving (D-03) — the existing repo test suite is the gate.
Lands after AppState (D-19) so the dedup sweeps already-final repo method bodies.

Purpose: Finish the "unadopted shared assets" migration (CONTEXT scouting finding) and remove
the pepper-less-caller trap (CQ-B47).

Output: helpers::paginate<T> + unit tests; group-A repos deduped; verify_password deleted;
user_repository_test.rs repointed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structural-quality/29-CONTEXT.md
@.planning/phases/29-structural-quality/29-RESEARCH.md
@.planning/phases/29-structural-quality/29-PATTERNS.md
@.planning/phases/29-structural-quality/29-VALIDATION.md

# Canonical helper home + the parse_uuid/take_first test style to mirror
@crates/axiam-db/src/helpers.rs
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Location |
|----------|------|----------|
| `paginate<T>(items, count_rows, pagination) -> PaginatedResult<T>` | new fn + unit tests | `crates/axiam-db/src/helpers.rs` |
| verify_password deletion | dead-code removal | `crates/axiam-db/src/repository/user.rs` + re-exports in `mod.rs`, `lib.rs` |

<assumption_delta_decision>
Noun: password verification.
Trigger: the assumption-delta detector fired on "second" in "the pepper-less second verify_password impl".
Decision: **no-change** — this is a plural→singular *consolidation* (promote the canonical
`axiam-auth::password::verify_password`, delete the duplicate `axiam-db` impl per QUAL-07/D-17).
It does NOT introduce a second case that changes the identity model; it removes one. No scope
change, no generalization manufactured. FALSE POSITIVE confirmed.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add paginate<T> helper + tests; delete the pepper-less verify_password (QUAL-07/D-17)</name>
  <files>crates/axiam-db/src/helpers.rs, crates/axiam-db/src/repository/user.rs, crates/axiam-db/src/repository/mod.rs, crates/axiam-db/src/lib.rs, crates/axiam-db/tests/user_repository_test.rs</files>
  <read_first>
    - crates/axiam-db/src/helpers.rs:1-51 (CountRow at :18, parse_uuid at :33, take_first_or_not_found at :46) and :57-118 (the `#[cfg(test)] mod tests` style with one `#[test] fn <name>_<condition>_<expectation>()` per case)
    - crates/axiam-core/src/repository.rs:63,80 (Pagination / PaginatedResult<T> — no changes needed)
    - crates/axiam-db/src/repository/webhook.rs:280-319 (the exact 5-line count+total+PaginatedResult tail paginate<T> replaces)
    - crates/axiam-db/src/repository/user.rs:872-889 (the pepper-less verify_password to delete), crates/axiam-db/src/repository/mod.rs:71 and crates/axiam-db/src/lib.rs:34 (the two re-exports to remove)
    - crates/axiam-db/tests/user_repository_test.rs:12,98,101,122,125 (`use axiam_db::verify_password;` + 4 .unwrap() call sites) ; crates/axiam-auth/src/password.rs:53 (the canonical verifier that survives)
  </read_first>
  <behavior>
    - paginate(items, [] , pagination) => PaginatedResult with total 0 and the pagination's offset/limit preserved
    - paginate(items, [CountRow{total:N}], pagination) => PaginatedResult { total: N, offset, limit }
    - after deletion, `axiam_db::verify_password` no longer resolves; user_repository_test.rs compiles against axiam_auth::password::verify_password and passes
  </behavior>
  <action>
    Add `pub fn paginate<T>(items: Vec<T>, count_rows: Vec<CountRow>, pagination: &Pagination) -> PaginatedResult<T>`
    to helpers.rs following the file's existing doc-comment convention: extract `total` from the first CountRow
    (unwrap_or 0) and construct PaginatedResult with the passed offset/limit. Add unit tests
    `paginate_empty_count_rows_defaults_to_zero` and `paginate_preserves_pagination_offset_and_limit` in the
    existing `#[cfg(test)] mod tests`.
    Delete the `verify_password` fn from user.rs and its two re-exports (mod.rs:71 `pub use user::{..., verify_password}`,
    lib.rs:34). Repoint user_repository_test.rs's import to `use axiam_auth::password::verify_password;` (axiam-auth
    is already a non-dev dependency of axiam-db per Cargo.toml:12); the 4 call sites are unchanged (signatures match,
    only the error type differs and the test only .unwrap()s). Confirm via workspace grep that no live (non-test)
    caller referenced `axiam_db::verify_password`.
  </action>
  <acceptance_criteria>
    - `cargo test -p axiam-db --lib` passes including the two paginate unit tests.
    - `grep -rn "pub use user::{.*verify_password" crates/axiam-db/src/repository/mod.rs` and `grep -n "verify_password" crates/axiam-db/src/lib.rs` return 0 (re-exports gone).
    - `grep -rn "axiam_db::verify_password" crates/*/src crates/*/tests` returns 0 outside the repointed test import.
    - `cargo test -p axiam-db --test user_repository_test` passes against the canonical axiam-auth verifier.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p axiam-db --lib && cargo test -p axiam-db --test user_repository_test</automated>
  </verify>
  <done>paginate<T> exists with unit tests; the pepper-less verify_password and its re-exports are deleted; the only caller (a test) uses the canonical axiam-auth verifier.</done>
</task>

<task type="auto">
  <name>Task 2: Dedup file-group A repos — CountRow, paginate<T>, take_first_or_not_found</name>
  <files>crates/axiam-db/src/repository/organization.rs, tenant.rs, permission.rs, resource.rs, scope.rs, service_account.rs, session.rs, audit.rs, group.rs (all under crates/axiam-db/src/repository/)</files>
  <read_first>
    - crates/axiam-db/src/repository/user.rs and role.rs (already-migrated CountRow/parse_uuid exemplars — grep `use crate::helpers::{CountRow, parse_uuid};` for the target import shape)
    - crates/axiam-db/src/helpers.rs (CountRow, paginate<T> from Task 1, take_first_or_not_found)
    - Group-A repo files to dedup: organization.rs, tenant.rs, permission.rs, resource.rs, service_account.rs, session.rs, audit.rs, group.rs — the 8 files that DO carry a local `struct CountRow` (verified `grep -rl "struct CountRow"`); each has its local `struct CountRow`, its `let total = count_rows.first()...; PaginatedResult { ... }` tails, and inline `.into_iter().next().ok_or_else(|| DbError::NotFound { ... })?` single-record reads.
    - scope.rs — take_first-ONLY adoption site: it has NO local `struct CountRow` (it uses `ScopeRow`) and no paginated count-list, but it DOES have inline `.into_iter().next().ok_or_else(|| DbError::NotFound { ... })?` single-record reads (~:94, :130, :189). Apply only the `take_first_or_not_found` substitution here; do not add a `CountRow` import or a `paginate` call.
    - RESEARCH § QUAL-02: the 1:1 substitution shape (e.g. tenant.rs:126 → `helpers::take_first_or_not_found(items, "tenant", &id_str)?`)
  </read_first>
  <action>
    In each group-A repo file: delete the local `struct CountRow` and import `crate::helpers::CountRow` (extend an
    existing `use crate::helpers::{...}` line where present). Replace the local `let total = count_rows.first()...;
    PaginatedResult { items, total, offset, limit }` tail in each list method with `helpers::paginate(items,
    count_rows, &pagination)`. Replace each inline `items.into_iter().next().ok_or_else(|| DbError::NotFound {
    entity: ..., id: ... })?` single-record read with `helpers::take_first_or_not_found(items, "<entity>", &<id>)?`.
    Do all three substitutions per file in one pass (they touch the same files) rather than three global sweeps.
    Exception — scope.rs: it has no local `struct CountRow` and no paginated list, so apply ONLY the
    `take_first_or_not_found` substitution to its single-record reads; skip the CountRow-import and paginate steps for it.
    This is a behavior-preserving mechanical pass — do NOT alter any query SQL, WHERE clause, or method signature.
    (File-group B — the remaining ~15 repos + federation_link.rs parse_uuid removal — is plan 29-05.)
  </action>
  <acceptance_criteria>
    - `cargo test -p axiam-db` passes (group-A repo integration + lib tests) with no behavior change.
    - `grep -rln "struct CountRow" crates/axiam-db/src/repository/{organization,tenant,permission,resource,service_account,session,audit,group}.rs` returns 0 (all 8 group-A local CountRow definitions collapsed).
    - Group-A list methods construct results via `helpers::paginate(...)`; single-record reads (including scope.rs's) use `helpers::take_first_or_not_found(...)`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p axiam-db</automated>
  </verify>
  <done>Group-A repos import helpers::CountRow, adopt paginate<T>, and route single-record reads through take_first_or_not_found; axiam-db suite green (no behavior change).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| password verification callers → verifier impl | Deleting the wrong (pepper-less) impl and repointing to the canonical one must not leave any live caller on a pepper-less path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-08 | Tampering (silent behavior drift) | mechanical CountRow/paginate/take_first dedup | low | mitigate | Pure behavior-preserving substitution; the existing axiam-db repo test suite is the regression gate (D-03) |
| T-29-09 | Spoofing / Authentication bypass | verify_password consolidation (CQ-B47) | high | mitigate | Keep the canonical axiam-auth::password::verify_password; delete only the axiam-db duplicate; workspace grep confirms no live caller depended on the deleted impl (D-17) — flagged HIGH for execution attention |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages added or installed; internal dedup + dead-code removal. Legitimacy gate N/A |
</threat_model>

<verification>
- `cargo test -p axiam-db --lib` green (paginate unit tests).
- `cargo test -p axiam-db` green (group-A repo dedup, no behavior change).
- `cargo test -p axiam-db --test user_repository_test` green (canonical verifier).
- `grep -rn "axiam_db::verify_password" crates/*/src crates/*/tests` returns 0 (outside the repointed test import) — HIGH-severity T-29-09 closed.
- `cargo clippy -p axiam-db` clean.
- Run `cargo clean` after this plan before the next (CLAUDE.md disk hygiene).
</verification>

<success_criteria>
paginate<T> exists and is adopted in group-A repos; group-A CountRow duplicates collapsed to
helpers::CountRow; group-A single-record reads use take_first_or_not_found; the pepper-less
verify_password and its re-exports are deleted with the sole caller repointed to the canonical
axiam-auth impl; axiam-db suite green (no behavior change).
</success_criteria>

<output>
Create `.planning/phases/29-structural-quality/29-04-SUMMARY.md` when done.
</output>
