---
phase: 29-structural-quality
plan: 07
type: execute
wave: 7
depends_on: ["29-06"]
files_modified:
  - frontend/src/components/shared.tsx
  - frontend/src/pages/UserDetailPage.tsx
  - frontend/src/pages/UsersPage.tsx
  - frontend/src/pages/FederationPage.tsx
  - frontend/src/pages/NotificationRulesPage.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/RolesPage.tsx
  - frontend/src/pages/WebhooksPage.tsx
  - frontend/src/pages/ServiceAccountsPage.tsx
  - frontend/src/pages/RoleDetailPage.tsx
  - frontend/src/pages/GroupDetailPage.tsx
  - frontend/src/pages/PermissionsPage.tsx
  - frontend/src/pages/OrganizationsPage.tsx
  - frontend/src/pages/OrganizationDetailPage.tsx
  - frontend/src/pages/ProfilePage.tsx
  - frontend/src/pages/MfaManagementPage.tsx
autonomous: false
requirements: [QUAL-06]
must_haves:
  truths:
    - "The 11 pages with local ToggleField/SectionCard/InfoRow/ActionBadge re-implementations import the canonical frontend/src/components/shared.tsx versions; local duplicates are removed (QUAL-06/D-15)"
    - "shared.tsx ActionBadge is fixed to lowercase the action before the color-map lookup and use a single canonical fallback class BEFORE adoption, so color behavior is parity-preserved (QUAL-06/D-15)"
    - "OrganizationsPage/OrganizationDetailPage use lib/utils.ts slugify instead of local copies (QUAL-06/D-15)"
    - "ProfilePage/MfaManagementPage call the typed services/users.ts userService methods instead of inline api.get/api.put/api.delete (QUAL-06/D-16)"
  artifacts:
    - "shared.tsx ActionBadge case-normalization fix (single fallback class)"
    - "11 pages migrated to shared components; 2 pages migrated to shared slugify; profile/MFA migrated to userService"
  key_links:
    - "shared.tsx / lib/utils.ts / services/users.ts / hooks/useCrudMutations.ts are the canonical modules pages wire to (they exist today with zero/partial adoption)"
    - "RoleDetailPage.tsx's exported local ActionBadge (a dead export with no external importers) is removed as part of adoption"
  prohibitions:
    - "Behavior-preserving on the pure component/service swaps (D-03); the existing 13 Playwright e2e specs + tsc -b are the gate"
    - "The ActionBadge case-sensitivity + fallback-class divergence MUST be fixed in shared.tsx before pages adopt it — do not migrate pages onto the divergent behavior"
    - "The one accepted UX delta (useCrudMutations always toasts on error, where RolesPage's delete was previously silent) is explicitly accepted per D-15/RESEARCH A2 — flag it, do not ship it silently"
---

<objective>
QUAL-06 — Frontend shared components & services adoption. Wire the 11 pages with local
ToggleField/SectionCard/InfoRow/ActionBadge copies to the canonical shared.tsx modules (after
fixing ActionBadge's case-sensitivity + fallback-class divergence), migrate slugify and the
profile/MFA data calls to the shared utils/userService, and adopt useCrudMutations where it
cleanly fits. Behavior-preserving (D-03) — the existing Playwright specs + tsc -b are the gate,
with a manual smoke checkpoint for pages lacking direct e2e coverage. Its own plan/diff, last
in the phase (D-19) for review separability.

Purpose: Reduce frontend duplication (CQ-F15/17/39) by adopting already-existing shared modules.

Output: ActionBadge fix; 11 pages on shared components; 2 pages on shared slugify; profile/MFA
on userService; useCrudMutations adopted where clean; tsc/eslint/Playwright green + manual smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structural-quality/29-CONTEXT.md
@.planning/phases/29-structural-quality/29-RESEARCH.md
@.planning/phases/29-structural-quality/29-PATTERNS.md
@.planning/phases/29-structural-quality/29-VALIDATION.md

# Canonical shared modules (already exist, zero/partial adoption)
@frontend/src/components/shared.tsx
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Location |
|----------|------|----------|
| ActionBadge case-normalization + single fallback class | behavior-parity fix | `frontend/src/components/shared.tsx` |
| 11 pages on shared components; RoleDetailPage dead ActionBadge export removed | dedup adoption | `frontend/src/pages/*` |
| profile/MFA on `userService` | typed-service adoption | `ProfilePage.tsx`, `MfaManagementPage.tsx` |

<tasks>

<task type="auto">
  <name>Task 1: Fix shared.tsx ActionBadge, then migrate 11 pages to shared components + shared slugify</name>
  <files>frontend/src/components/shared.tsx, frontend/src/pages/UserDetailPage.tsx, UsersPage.tsx, FederationPage.tsx, NotificationRulesPage.tsx, SettingsPage.tsx, RolesPage.tsx, WebhooksPage.tsx, ServiceAccountsPage.tsx, RoleDetailPage.tsx, GroupDetailPage.tsx, PermissionsPage.tsx, OrganizationsPage.tsx, OrganizationDetailPage.tsx</files>
  <read_first>
    - frontend/src/components/shared.tsx:76-97 (ActionBadge looks up ACTION_COLOR_MAP[action] WITHOUT lowercasing; fallback class "bg-white/5 text-muted-foreground border-white/10") and its ToggleField/SectionCard/InfoRow
    - frontend/src/pages/RoleDetailPage.tsx:76-97 (local ActionBadge does colorMap[action.toLowerCase()] with a DIFFERENT fallback "bg-white/10 text-foreground/70 border-white/20"; this copy is exported but has zero external importers — a dead export)
    - The 11 pages with local duplicates: ToggleField in UserDetailPage/UsersPage/FederationPage/NotificationRulesPage/SettingsPage/RolesPage/WebhooksPage/ServiceAccountsPage; SectionCard+InfoRow in UserDetailPage/RoleDetailPage/GroupDetailPage; ActionBadge in PermissionsPage/RoleDetailPage
    - frontend/src/lib/utils.ts:59 (exported slugify) ; OrganizationsPage.tsx:21 and OrganizationDetailPage.tsx:36 (local slugify copies; both files already import formatDate from lib/utils.ts)
  </read_first>
  <action>
    First fix shared.tsx ActionBadge: lowercase `action` before the ACTION_COLOR_MAP lookup and settle on a single
    canonical fallback class (reconcile the shared.tsx vs RoleDetailPage fallback divergence to one value), so the
    behavior is deterministic parity regardless of action casing — do this BEFORE any page adopts it.
    Then migrate each of the 11 pages: replace the local ToggleField/SectionCard/InfoRow/ActionBadge fn with an
    import from components/shared and delete the local definition. Remove RoleDetailPage.tsx's exported dead local
    ActionBadge. Replace the local slugify in OrganizationsPage.tsx and OrganizationDetailPage.tsx with the shared
    lib/utils.ts slugify (add it to the existing formatDate import line). SectionCard/InfoRow/ToggleField are
    byte-identical to their local copies — direct swaps.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx tsc -b` clean (no type errors after the swaps).
    - `cd frontend && npx eslint .` clean.
    - `grep -rn "function ActionBadge\|const ActionBadge" frontend/src/pages/` returns 0 (no local ActionBadge copies remain); shared.tsx ActionBadge lowercases the action before the map lookup.
    - `grep -rn "function slugify\|const slugify" frontend/src/pages/OrganizationsPage.tsx frontend/src/pages/OrganizationDetailPage.tsx` returns 0 (local slugify removed).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b && npx eslint .</automated>
  </verify>
  <done>ActionBadge case/fallback divergence fixed; 11 pages import shared ToggleField/SectionCard/InfoRow/ActionBadge; 2 pages use shared slugify; tsc/eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Profile/MFA → userService (D-16); adopt useCrudMutations where it cleanly fits</name>
  <files>frontend/src/pages/ProfilePage.tsx, frontend/src/pages/MfaManagementPage.tsx, frontend/src/pages/RolesPage.tsx</files>
  <read_first>
    - frontend/src/pages/ProfilePage.tsx:5,63,81,86 (imports api from @/lib/api; calls api.get/api.put/api.get for user + MFA methods while type-only importing from services/users) and MfaManagementPage.tsx:4,36,43 (api.get/api.delete)
    - frontend/src/services/users.ts:41-56,92-175 (userService typed methods: getById/get, update, MFA list/delete — confirm exact method names at implementation time)
    - frontend/src/hooks/useCrudMutations.ts (typed generic CRUD mutation hook, zero current adopters; its onError ALWAYS fires toast({variant:"destructive"}))
    - frontend/src/pages/RolesPage.tsx:146-236 (createMutation/editMutation/deleteMutation mapping onto the hook's queryKey/createFn/updateFn/deleteFn/onCreateSuccess/onCreateError; deleteMutation has NO onError today — silent failure) ; also GroupsPage.tsx, PermissionsPage.tsx, WebhooksPage.tsx, ServiceAccountsPage.tsx, NotificationRulesPage.tsx (per-page diff at implementation time to confirm fit)
  </read_first>
  <action>
    Swap the inline api.get/api.put/api.delete calls in ProfilePage.tsx and MfaManagementPage.tsx for the
    equivalent userService methods (get/update/MFA list+delete), keeping the existing type imports. Adopt
    useCrudMutations on RolesPage.tsx, mapping its create/edit/delete mutations onto the hook's options. Accept and
    flag the one UX delta: the hook always toasts on error (RolesPage's delete was previously silent) — record this
    explicitly in the SUMMARY per D-15/RESEARCH A2. For GroupsPage/PermissionsPage/WebhooksPage/ServiceAccountsPage/
    NotificationRulesPage, do a per-page diff against the hook's options interface and adopt only where the shape
    fits cleanly without a per-page callback mismatch; leave any that need bespoke handling as-is and note why.
    Run the frontend gate: tsc -b, eslint, and the existing Playwright e2e suite.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx tsc -b && npx eslint .` clean.
    - `grep -n "from \"@/lib/api\"" frontend/src/pages/ProfilePage.tsx frontend/src/pages/MfaManagementPage.tsx` shows the raw api import is gone (or unused) — data calls go through userService.
    - `cd frontend && npx playwright test` passes (existing 13 specs — the no-behavior-change gate).
    - The SUMMARY records which pages adopted useCrudMutations and the accepted toast-on-error UX delta.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b && npx eslint . && npx playwright test</automated>
  </verify>
  <done>Profile/MFA data calls route through userService; useCrudMutations adopted where it cleanly fits (toast-on-error delta flagged); tsc/eslint/Playwright green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify manual smoke of migrated pages without direct e2e coverage</name>
  <files>frontend/src/pages/ (migrated pages), frontend/src/components/shared.tsx</files>
  <action>Human performs the manual smoke steps below; this task makes no automated code change — it gates on human confirmation that the migrated pages render/behave identically.</action>
  <what-built>
    QUAL-06 frontend shared-component/service adoption: 11 pages migrated to shared.tsx components (with the
    ActionBadge case/fallback fix), 2 pages on shared slugify, profile/MFA on userService, and useCrudMutations
    adopted where it fit. Automated gates (tsc -b, eslint, Playwright 13 specs) are green.
  </what-built>
  <how-to-verify>
    Manual smoke of the migrated pages that lack direct Playwright coverage (per 29-VALIDATION Manual-Only table):
    1. `cd frontend && npm run dev` (or the project's dev command) and log in.
    2. Visit Users, User detail, Roles, Role detail, Permissions, Federation, Settings, Notification rules,
       Service accounts, Webhooks, Groups/Group detail, Organizations/Organization detail, Profile, MFA management.
    3. Confirm each page renders and functions identically to before (toggles, section cards, info rows, badges,
       slug fields, profile/MFA read+update flows).
    4. Specifically confirm ActionBadge colors are correct/unchanged on Permissions and Role detail (the
       case-sensitivity + fallback-class divergence that was reconciled in shared.tsx).
    5. Confirm the accepted toast-on-error behavior on create/update/delete failures is acceptable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any page whose rendering/behavior regressed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side React refactor; no backend contract, auth, or data-access boundary changes. Server RBAC remains authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-13 | Tampering (UI behavior drift) | ActionBadge color divergence + useCrudMutations toast delta | low | mitigate | Fix ActionBadge case/fallback in shared.tsx BEFORE adoption; existing 13 Playwright specs + tsc -b + manual smoke are the gate; the toast-on-error delta is explicitly accepted and flagged (D-15/A2) |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No new npm packages added or installed; wiring to already-present shared modules only. Legitimacy gate N/A |
</threat_model>

<verification>
- `cd frontend && npx tsc -b` clean; `npx eslint .` clean.
- `cd frontend && npx playwright test` passes (13 specs — the no-behavior-change gate for QUAL-06).
- Human-verify checkpoint approved (manual smoke of pages without direct e2e coverage + ActionBadge color parity).
- No `cargo` build in this plan (frontend/npm only); no cargo disk contention.
</verification>

<success_criteria>
The 11 pages import the shared ToggleField/SectionCard/InfoRow/ActionBadge (local duplicates removed,
ActionBadge divergence fixed first); OrganizationsPage/OrganizationDetailPage use shared slugify;
ProfilePage/MfaManagementPage call the typed userService; useCrudMutations adopted where it fits
(toast-on-error delta flagged); tsc/eslint/Playwright green and the manual smoke checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/29-structural-quality/29-07-SUMMARY.md` when done.
</output>
