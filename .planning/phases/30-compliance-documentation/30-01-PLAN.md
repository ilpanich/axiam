---
phase: 30-compliance-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude_dev/security-audit.md
autonomous: false
requirements: [CMPL-01]
must_haves:
  truths:
    - "claude_dev/security-audit.md maps every authentication, session, access-control, cryptography, and PKI control to a pass/fail with an evidence POINTER against OWASP ASVS L2, ISO 27001, and the CyberSecurity Act (CMPL-01)"
    - "Open/deferred items are each cross-referenced to a v1.2 REQ-ID (CMPL-01 acceptance criterion)"
    - "The doc is framed near the top as an internal self-assessment at control-family granularity (not a certified ISO 27001 ISMS audit) and is stamped v1.2 / beta + a last-verified date (D-02, D-12, Pitfall 4)"
    - "ISO 27001 Annex A is mapped at control-FAMILY level and CyberSecurity Act at essential-requirement THEME level (D-02); rows CITE docs/compliance evidence, never transcribe it (D-01, Pitfall 2)"
  artifacts:
    - "claude_dev/security-audit.md"
  key_links:
    - "security-audit.md rows link INTO docs/compliance/{asvs-l2-checklist,FINDINGS,oauth2-rfc-compliance,oidc-conformance,sc4-coverage}.md (citation, not copy)"
    - "CyberSecurity Act 'security updates / vulnerability handling' row → .github/dependabot.yml (confirmed present)"
---

<objective>
Author `claude_dev/security-audit.md`, the single top-level v1.2 compliance
certification document (CMPL-01). It is a CITATION INDEX (D-01) over the
already-proven evidence in `docs/compliance/` and the Phase 23–29 verification
trail (D-03) — it maps auth / session / access-control / cryptography / PKI
controls to pass/fail with evidence POINTERS against OWASP ASVS L2, ISO 27001
Annex A (control-family level, D-02), and the CyberSecurity Act (theme level,
D-02), and cross-references every open item to a v1.2 REQ-ID.

Purpose: Certify the finished, hardened MVP state without re-auditing controls
already proven in Phase 7 and Phases 23–29 (cite, do not re-prove).
Output: `claude_dev/security-audit.md`.

This is the FINAL phase of the v1.2 milestone — a verify+document phase, not a
build phase. Do NOT rebuild shipped machinery; cite existing evidence.
</objective>

<assumption_delta_decision>
- **noun:** (none — no identity model changed by this phase)
- **decision:** no-change
- **rationale:** This is a documentation/certification phase (CONTEXT domain
  boundary — adds no new product capability). The assumption-delta detector fired
  on the token "optional" in "optional PGP encryption", which is a shipped
  SECHRD-06 export characteristic (locked in D-05) being DOCUMENTED, not a newly
  generalized identity or a singular→plural / required→optional / derived→chosen
  architectural transition. No invariant test required.
</assumption_delta_decision>

<artifacts_produced>
Every new/modified artifact THIS PHASE (Phase 30) creates, across all six plans
— the executor of this plan owns only the first row:

| Artifact | Owning plan |
|----------|-------------|
| `claude_dev/security-audit.md` | 30-01 (this plan) |
| `docs/compliance/gdpr-compliance.md` | 30-02 |
| `docs/api/asyncapi.yml` (net-new AsyncAPI 2.6 spec) | 30-03 |
| `docs/api/openapi.json` (symlink → `../../sdks/openapi.json`) | 30-03 |
| `docs/api/grpc.md` | 30-03 |
| `docs/api/README.md` | 30-03 |
| `docs/deployment/README.md` | 30-04 |
| `docs/admin/README.md` | 30-04 |
| `docs/pki/README.md` | 30-04 |
| `docs/README.md` (top-level index) | 30-05 |
| `scripts/check-doc-links.sh` (zero-dependency link-check) | 30-05 |
| `.github/workflows/docs-ci.yml` | 30-06 |
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-compliance-documentation/30-CONTEXT.md
@.planning/phases/30-compliance-documentation/30-RESEARCH.md
@.planning/phases/30-compliance-documentation/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author security-audit.md skeleton — header, self-assessment framing, ASVS L2 summary, OAuth2/OIDC/federation citations, open-items register</name>
  <files>claude_dev/security-audit.md</files>
  <read_first>
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "security-audit.md Master-Doc Structure" (the recommended §1–§8 layout + confirmed evidence pointers) and § "Common Pitfalls" (Pitfall 2 citation-not-duplication, Pitfall 4 no-overclaiming)
    - .planning/phases/30-compliance-documentation/30-PATTERNS.md § "claude_dev/security-audit.md" (header/version-stamp analog from docs/compliance/asvs-l2-checklist.md L1-16; findings-register row shape from docs/compliance/FINDINGS.md L1-12; provenance/glyph convention from claude_dev/security-review-postremediation.md L1-9)
    - docs/compliance/asvs-l2-checklist.md (the ASVS L2 control-by-control backing to CITE — read the header + status-summary rows; 103 controls, 94 Pass / 4 N/A / 5 Deferred, zero High/Critical deferred)
    - docs/compliance/FINDINGS.md (the F-01..F-05 deferred/finding rows to cross-reference in §7)
    - docs/compliance/oauth2-rfc-compliance.md, docs/compliance/oidc-conformance.md, docs/compliance/sc4-coverage.md (headers only — to write one-paragraph-summary + link citations, NOT copies)
    - claude_dev/security-review-postremediation.md (the post-remediation findings feeding v1.2 REQ-IDs; provenance metadata-block convention)
  </read_first>
  <action>
    Create claude_dev/security-audit.md following the RESEARCH §"Master-Doc Structure"
    §1–§8 layout. §1 "How to Read This Document": one paragraph stating this is a
    master CITATION INDEX over docs/compliance/ and the Phase 23–29 verification
    trail (D-01, D-03) — it does not re-run tests, it cites where they live — AND
    an explicit statement that this is an INTERNAL SELF-ASSESSMENT at control-family
    granularity, not a certified ISO 27001 ISMS audit (D-02 framing, Pitfall 4
    anti-overclaim). §2 "OWASP ASVS Level 2 — Status Summary": a category-level
    table (V2/V3/V4/V6/V7/V8/V9/V14) with pass/deferred counts and a relative link
    into docs/compliance/asvs-l2-checklist.md section anchors — one-line summaries,
    never transcribed control text (D-01, Pitfall 2). §5 "OAuth2 / OIDC Conformance"
    and §6 "Federation / Test-Coverage Cross-Reference": one-paragraph summary + link
    into oauth2-rfc-compliance.md / oidc-conformance.md / sc4-coverage.md. §7 "Open
    Items / Deferred Findings": reuse the FINDINGS.md row shape (# | Finding |
    Severity | Ref | Status | Disposition) and ADD a v1.2 REQ-ID column so every
    open/deferred row (e.g. F-05 CSP header) is cross-referenced to a REQ-ID
    (REQ-/SECFIX-/SECHRD-/CMPL-/DOCS-). §8 "Version & Provenance": the D-12 stamp.
    Adopt the header preamble shape (Standard / Scope / Out-of-scope / Status-values)
    from asvs-l2-checklist.md plus a Date / Milestone metadata line, honoring D-12
    (milestone v1.2 Beta + last-verified date = today's execution date). Leave §3
    (ISO 27001) and §4 (CyberSecurity Act) as placeholder headings for Task 2.
    Every ASVS/OAuth2/OIDC/federation reference is a relative markdown link of the
    form label plus parenthesized relative path — never a re-transcription.
  </action>
  <acceptance_criteria>
    - `claude_dev/security-audit.md` exists with §1–§8 headings present.
    - §1 contains an explicit internal-self-assessment / not-a-certified-audit statement (Pitfall 4) AND a cite-don't-re-prove statement (D-01/D-03).
    - `grep -Eq 'v1\.2' claude_dev/security-audit.md` exits 0 AND a last-verified date line is present (D-12 stamp).
    - `grep -Eq 'docs/compliance/asvs-l2-checklist\.md' claude_dev/security-audit.md` exits 0 (ASVS is cited by link, not copied).
    - §7 open-items rows each carry a v1.2 REQ-ID: `grep -Eq 'REQ-|SECFIX-|SECHRD-|CMPL-|DOCS-' claude_dev/security-audit.md` exits 0.
    - No ASVS control text is transcribed verbatim (spot-check: §2 is a category summary table with links, not per-control prose).
  </acceptance_criteria>
  <verify>
    <automated>test -f claude_dev/security-audit.md && grep -Eq 'v1\.2' claude_dev/security-audit.md && grep -Eq 'docs/compliance/asvs-l2-checklist\.md' claude_dev/security-audit.md && grep -Eq 'REQ-|SECFIX-|SECHRD-|CMPL-|DOCS-' claude_dev/security-audit.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add the ISO 27001 Annex A family table + CyberSecurity Act theme table (control-family/theme level, [ASSUMED] flags surfaced)</name>
  <files>claude_dev/security-audit.md</files>
  <read_first>
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "ISO 27001 Annex A + CyberSecurity Act Mapping" (the concrete family table with Annex A 2022 IDs A.5/A.6/A.7/A.8 and the CSA/CRA theme table — use these largely as-is per D-02 Claude's Discretion), § "Assumptions Log" (A1 family-grouping, A2 CyberSecurity-Act-interpretation EU CRA vs Reg 2019/881, A4 SBOM/security-updates rows), and § "Open Questions" (Q1 CRA-vs-2019/881, Q2 dependabot)
    - .planning/phases/30-compliance-documentation/30-CONTEXT.md D-02 (control-family + evidence-pointer altitude) and D-03 (cite phase evidence, spot-verify)
    - .github/dependabot.yml (confirmed present — the evidence pointer for the CyberSecurity Act "security updates / vulnerability handling" theme row; upgrade that row from Partial to Pass and cite this file)
    - docs/compliance/asvs-l2-checklist.md (the V2/V4/V6/V8 rows that ISO A.8.5/A.8.24 and the CSA access-control/crypto themes point back to)
  </read_first>
  <action>
    Fill §3 "ISO 27001 Annex A — Control-Family Mapping" using the RESEARCH family
    table: one row per applicable Annex A 2022 FAMILY (A.5 Organizational, A.6 People,
    A.7 Physical, A.8 Technological with the A.8.x sub-family groupings) — Pass /
    N/A / Partial + an evidence POINTER (a code path, a test file, or the ASVS row
    it corresponds to) per D-02. Fill §4 "CyberSecurity Act — Essential-Requirement
    Theme Mapping" using the RESEARCH CRA theme table (secure-by-design, no-known-
    exploitable-vulns, confidentiality, data-minimization, access-control/authn,
    vulnerability-handling, security-updates, SBOM). Set the "security updates" row
    to Pass and cite .github/dependabot.yml as its evidence pointer (resolves
    RESEARCH Open Question 2). Surface — do NOT silently assert — the interpretive
    mappings as explicit assumptions: tag the ISO A.6/A.7 N/A rows, the SBOM
    N/A/Deferred row, and the "CyberSecurity Act = EU CRA (not EU Reg 2019/881)"
    interpretation each with a visible [ASSUMED] marker and a one-line note that a
    human reviewer must confirm (RESEARCH A1/A2/A4). Cross-reference SBOM (if left
    Deferred) to an open item in §7 with a v1.2 REQ-ID. Keep every row a
    one-line-summary + pointer; do not transcribe ASVS control text.
  </action>
  <acceptance_criteria>
    - §3 present with ISO 27001:2022 Annex A family IDs: `grep -Eq 'A\.5|A\.8' claude_dev/security-audit.md` exits 0 (2022 numbering, not legacy A.9/A.10/A.12/A.14).
    - §4 present with the CyberSecurity Act theme table; the security-updates row cites dependabot: `grep -q 'dependabot' claude_dev/security-audit.md` exits 0.
    - Interpretive mappings are surfaced, not asserted silently: `grep -q 'ASSUMED' claude_dev/security-audit.md` exits 0 (covers ISO A.6/A.7 N/A, SBOM, and the CRA-vs-2019/881 interpretation).
    - The "CyberSecurity Act" framework interpretation (EU CRA) is stated explicitly with an [ASSUMED] flag for human confirmation.
  </acceptance_criteria>
  <verify>
    <automated>grep -Eq 'A\.5|A\.8' claude_dev/security-audit.md && grep -q 'dependabot' claude_dev/security-audit.md && grep -q 'ASSUMED' claude_dev/security-audit.md</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the ISO 27001 / CyberSecurity Act mappings, interpretation, and no over-claiming</name>
  <action>
    Pause execution and present claude_dev/security-audit.md's §3/§4 mapping tables,
    the [ASSUMED] flags, and the §1 self-assessment framing for human confirmation
    per the how-to-verify steps below. Do NOT proceed to the next plan until the
    reviewer approves or supplies corrections (this is a compliance-framework
    interpretation gate — RESEARCH Assumptions A1/A2).
  </action>
  <what-built>
    claude_dev/security-audit.md — the CMPL-01 master compliance doc: ASVS L2
    status summary (cited from docs/compliance/asvs-l2-checklist.md), the net-new
    ISO 27001 Annex A control-family mapping table (§3), the net-new CyberSecurity
    Act essential-requirement theme mapping table (§4), OAuth2/OIDC/federation
    citations (§5/§6), and the open-items register cross-referenced to v1.2 REQ-IDs
    (§7). Interpretive mappings are tagged [ASSUMED].
  </what-built>
  <how-to-verify>
    The ISO 27001 / CyberSecurity Act mappings are interpretive (no canonical
    ASVS↔ISO crosswalk exists — RESEARCH Assumptions A1/A2) and a compliance
    framework interpretation, so they require human confirmation before the audit
    doc is considered final. Review claude_dev/security-audit.md and confirm:
    1. The "CyberSecurity Act" interpretation (EU Cyber Resilience Act, not EU
       Regulation 2019/881) is correct for AXIAM's intended framework — see the
       [ASSUMED] flag in §4. If it should be 2019/881, say so and the theme table
       will be re-anchored.
    2. The ISO 27001 Annex A family→AXIAM-control groupings in §3 (esp. which
       sub-controls fall under A.8.5 vs A.8.24, and the A.6/A.7 N/A rows) are
       defensible.
    3. §1 does NOT overclaim: it reads as an internal self-assessment at
       control-family granularity, not a certified ISO 27001 ISMS audit.
    4. Spot-check 2–3 §2 ASVS "Pass" rows against the cited
       docs/compliance/asvs-l2-checklist.md anchors (D-03 spot-verify).
  </how-to-verify>
  <resume-signal>Type "approved" or list the mapping rows / interpretation to correct</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external stakeholder ← internal self-assessment | A reader may treat "security audit" as an externally-certified compliance claim; over-claiming crosses this boundary |
| audit doc ← cited evidence | The doc asserts pass/fail; if a citation is wrong or a mapping is fabricated, coverage is misrepresented |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Repudiation (documentation-integrity / over-claiming compliance status) | claude_dev/security-audit.md | high | mitigate | §1 internal-self-assessment framing (Pitfall 4); [ASSUMED] flags on ISO A.6/A.7 N/A, SBOM, and CRA-interpretation rows; blocking human-verify checkpoint on the mappings; D-12 v1.2/beta + last-verified stamp |
| T-30-02 | Tampering (interpretive mapping misrepresents coverage) | ISO/CSA mapping tables (§3/§4) | medium | mitigate | Every family/theme row carries an evidence pointer into docs/compliance or code (D-01); human review confirms family/theme groupings (A1/A2) |
| T-30-03 | Information disclosure (audit doc reveals a real weakness as "closed") | §2/§7 rows | low | accept | D-03 spot-verify of a representative sample against cited docs/compliance evidence; no secrets in doc |
</threat_model>

<verification>
- `test -f claude_dev/security-audit.md` — doc exists.
- `grep -Eq 'v1\.2' claude_dev/security-audit.md` — D-12 milestone stamp present.
- `grep -Eq 'REQ-|SECFIX-|SECHRD-|CMPL-|DOCS-' claude_dev/security-audit.md` — open items cross-referenced to v1.2 REQ-IDs.
- `grep -Eq 'A\.5|A\.8' claude_dev/security-audit.md` — ISO 2022 Annex A families present.
- `grep -q 'dependabot' claude_dev/security-audit.md` — CSA security-updates row cites .github/dependabot.yml.
- `grep -q 'ASSUMED' claude_dev/security-audit.md` — interpretive mappings surfaced for human confirmation.
- Human-verify checkpoint approved (ISO/CSA mappings + CyberSecurity Act interpretation + no over-claiming).
- Internal-link resolvability is enforced later by scripts/check-doc-links.sh (30-05) and docs-ci.yml (30-06).
</verification>

<success_criteria>
CMPL-01 satisfied: claude_dev/security-audit.md maps every auth/session/access-control/crypto/PKI control to pass/fail with an evidence pointer against ASVS L2 + ISO 27001 + CyberSecurity Act, cites (does not duplicate) the docs/compliance evidence, cross-references open items to v1.2 REQ-IDs, is framed as an internal self-assessment, and is stamped v1.2/beta + last-verified date. Interpretive mappings are human-confirmed.
</success_criteria>

<output>
Create `.planning/phases/30-compliance-documentation/30-01-SUMMARY.md` when done.
</output>
