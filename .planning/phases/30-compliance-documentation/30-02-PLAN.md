---
phase: 30-compliance-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/compliance/gdpr-compliance.md
autonomous: true
requirements: [CMPL-02]
must_haves:
  truths:
    - "GDPR export covers every user-owned table incl. real sessions (optional PGP), proven by re-running the existing gdpr_test.rs export_completeness + export_includes_real_session_metadata tests (CMPL-02)"
    - "Account deletion durably pseudonymizes audit PII (SECHRD-06), proven by re-running gdpr_test.rs deletion_pseudonymization"
    - "Consent is recorded and exportable, proven by gdpr_test.rs consent_on_registration + inclusion in the export blob's consents block"
    - "docs/compliance/gdpr-compliance.md documents the async POST /account/export → GET /account/export/{token} flow as canonical and explicitly reconciles it with the roadmap GET /api/v1/users/:id/export shorthand (D-05, honest closure); consent UI/withdrawal is documented as deferred (D-06)"
  artifacts:
    - "docs/compliance/gdpr-compliance.md"
  key_links:
    - "doc cites cleanup.rs::aggregate_export_data (export completeness), gdpr.rs handlers (async export/delete/cancel), and gdpr_test.rs test names as executable evidence"
    - "erasure durability cites SECHRD-06 (Phase 25): pseudonymize_actor + erasure_proof UNIQUE index"
---

<objective>
Verify (trust-but-verify, D-03) and DOCUMENT GDPR completeness (CMPL-02). The
machinery already exists and is already tested — this plan RE-RUNS the existing
`gdpr_test.rs` suite as executable evidence, does a planning-time sanity check
for any user-owned table added since Phase 25, and then authors
`docs/compliance/gdpr-compliance.md` citing that evidence. It documents the async
export design as canonical and reconciles it with the roadmap's
`GET /users/:id/export` shorthand (D-05), and records the consent scope boundary
(record + export present; UI/withdrawal deferred — D-06).

Purpose: Close CMPL-02 honestly by citing proven behavior, not rebuilding it.
Output: `docs/compliance/gdpr-compliance.md` (and a code fix ONLY if the
spot-verification surfaces a genuine gap RESEARCH did not find — none expected).

Do NOT add a literal synchronous `GET /users/:id/export` endpoint (D-05,
Pitfall 1 / Anti-Pattern — explicitly deferred). Do NOT build consent UI (D-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-compliance-documentation/30-CONTEXT.md
@.planning/phases/30-compliance-documentation/30-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the existing gdpr_test.rs suite as executable CMPL-02 evidence + sanity-check for post-Phase-25 tables</name>
  <files>docs/compliance/gdpr-compliance.md</files>
  <read_first>
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "CMPL-02 Verification Map" (the exact assertion→evidence table — every claim already traced to code lines and passing tests) and § "Validation Architecture" (the four gdpr_test commands)
    - crates/axiam-api-rest/tests/gdpr_test.rs (the four evidence tests: export_completeness, export_includes_real_session_metadata (~L322-428), deletion_pseudonymization (~L612-792), consent_on_registration (~L797-839))
    - crates/axiam-server/src/cleanup.rs — aggregate_export_data (~L624-799): the export blob assembly (profile / consents_json / sessions_json metadata-only / mfa flag / federation_identities / assignments_json / group_memberships / paginated audit_json / webauthn_json) and the sweep_pending_exports AES-256-GCM + optional-PGP encrypt call (~L560-620)
    - crates/axiam-api-rest/src/handlers/gdpr.rs — request_account_export (~L273) enqueue, download_account_export (~L344) single-use token, request_account_delete (~L442), cancel_account_delete (~L561)
    - CLAUDE.md § "Build & Disk Hygiene" (SWAGGER_UI_DOWNLOAD_URL export + narrow cargo scope + cargo clean between plan steps)
  </read_first>
  <action>
    Export SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip
    then run the four existing GDPR evidence tests narrowly scoped to axiam-api-rest:
    export_completeness, export_includes_real_session_metadata,
    deletion_pseudonymization, consent_on_registration. Capture their PASS status
    as the executable evidence the doc will cite (D-03 "trust but verify" —
    re-run, do not skip). As a planning-time completeness sanity check, list the
    repository modules under crates/axiam-db/src/repository/ and confirm the set of
    user-owned tables serialized by aggregate_export_data still covers them — i.e.
    no new user-owned table was added after Phase 25 that is absent from the export
    blob. Record the test results and the table cross-check as an evidence block to
    be transcribed into the doc in Task 2. Only if the sanity check surfaces a
    genuine missing table (RESEARCH found none) escalate to a minimal export-fix
    task and re-run export_completeness; otherwise make NO production code change.
  </action>
  <acceptance_criteria>
    - All four gdpr_test cases pass: `cargo test -p axiam-api-rest --test gdpr_test export_completeness export_includes_real_session_metadata deletion_pseudonymization consent_on_registration` exits 0 (with SWAGGER_UI_DOWNLOAD_URL exported).
    - A cross-check of crates/axiam-db/src/repository/ against aggregate_export_data's serialized tables is recorded; either "no gap" (expected) or a specific missing table is named.
    - No production Rust file is modified unless a genuine export gap was found (this plan is verify+document).
  </acceptance_criteria>
  <verify>
    <automated>SWAGGER_UI_DOWNLOAD_URL=file:///home/user/.axiam-build-cache/swagger-ui-5.17.14.zip cargo test -p axiam-api-rest --test gdpr_test export_completeness export_includes_real_session_metadata deletion_pseudonymization consent_on_registration</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Author docs/compliance/gdpr-compliance.md citing the verification map + D-05 reconciliation + D-06 consent scope</name>
  <files>docs/compliance/gdpr-compliance.md</files>
  <read_first>
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "CMPL-02 Verification Map" (the assertion→evidence table to transcribe as citations) and § "Common Pitfalls" (Pitfall 1: do not add GET /users/:id/export)
    - .planning/phases/30-compliance-documentation/30-CONTEXT.md D-04 (verify+close+document), D-05 (async export canonical, reconcile the roadmap shorthand), D-06 (consent = record+export; UI/withdrawal deferred)
    - .planning/phases/30-compliance-documentation/30-PATTERNS.md § "Shared Patterns" (version-stamp / last-verified convention D-12; citation-over-duplication relative-link syntax)
    - docs/compliance/asvs-l2-checklist.md (header/version-stamp convention to mirror for a sibling docs/compliance/ file)
    - The Task 1 evidence block (test results + table cross-check)
  </read_first>
  <action>
    Author docs/compliance/gdpr-compliance.md as a sibling of the existing
    docs/compliance/ files (mirror their header/version-stamp convention; stamp
    v1.2 Beta + last-verified date per D-12). Structure: (1) an export-completeness
    section that CITES cleanup.rs::aggregate_export_data and lists each covered
    block (profile, consents, real sessions metadata-only, mfa flag, federation
    identities, role assignments, group memberships, paginated audit entries,
    webauthn credentials) with the export_completeness +
    export_includes_real_session_metadata tests named as executable evidence, and
    notes the AES-256-GCM + optional-PGP encryption; (2) an erasure section citing
    the deletion_pseudonymization test and SECHRD-06 (DELETED_USER_<hash>
    pseudonym, actor_id → nil UUID + actor_pseudonym metadata, ip_address nulled,
    erasure_proof durable write with its UNIQUE index); (3) a consent section
    citing consent_on_registration + the consents block in the export, explicitly
    scoping consent to record+export and documenting consent-capture-UI /
    withdrawal as DEFERRED (D-06); (4) an API-reconciliation section documenting
    the async POST /api/v1/account/export (enqueue) → GET
    /api/v1/account/export/{token} (single-use encrypted download) flow as
    canonical and explicitly reconciling it against the roadmap SC's
    GET /api/v1/users/:id/export descriptive shorthand — stating no such literal
    synchronous endpoint exists and this async design is a deliberate SECHRD-06
    choice that fulfills CMPL-02's intent (D-05, honest closure). Use relative
    markdown links for every citation; do not transcribe test bodies or code.
  </action>
  <acceptance_criteria>
    - `test -f docs/compliance/gdpr-compliance.md` and it is stamped: `grep -Eq 'v1\.2' docs/compliance/gdpr-compliance.md` exits 0 with a last-verified date line.
    - The D-05 reconciliation is present and honest: `grep -q 'account/export' docs/compliance/gdpr-compliance.md` exits 0 AND the roadmap shorthand `GET /api/v1/users/:id/export` is named and reconciled: `grep -q 'users/:id/export' docs/compliance/gdpr-compliance.md` exits 0.
    - Erasure durability cites SECHRD-06: `grep -q 'SECHRD-06' docs/compliance/gdpr-compliance.md` exits 0.
    - The four evidence test names are cited: `grep -q 'consent_on_registration' docs/compliance/gdpr-compliance.md` exits 0.
    - Consent UI/withdrawal is documented as deferred (D-06).
    - No new production endpoint or route was added (Pitfall 1 respected).
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/compliance/gdpr-compliance.md && grep -Eq 'v1\.2' docs/compliance/gdpr-compliance.md && grep -q 'account/export' docs/compliance/gdpr-compliance.md && grep -q 'users/:id/export' docs/compliance/gdpr-compliance.md && grep -q 'SECHRD-06' docs/compliance/gdpr-compliance.md && grep -q 'consent_on_registration' docs/compliance/gdpr-compliance.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GDPR compliance claim ← actual code behavior | The doc asserts export/erasure/consent completeness; a shallow verification could document a real gap as "closed" |
| data subject ← export/erasure implementation | The export/erasure paths handle PII; the doc must describe controls without leaking PII or secret material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Repudiation (compliance-integrity — documenting a gap as closed on a too-shallow check) | docs/compliance/gdpr-compliance.md | high | mitigate | Re-run the existing gdpr_test.rs suite (export_completeness / export_includes_real_session_metadata / deletion_pseudonymization / consent_on_registration) as executable evidence — trust-but-verify (D-03) — plus a planning-time repository cross-check for post-Phase-25 tables |
| T-30-05 | Information disclosure (doc reveals PII-handling weakness or leaks a secret) | doc content | low | accept | Doc describes controls and cites code/tests by path; contains no PII, no keys, no export blobs |
| T-30-06 | Tampering (roadmap-literal endpoint added, expanding attack surface unnecessarily) | crates/axiam-api-rest routes | low | accept | D-05 reconciliation documents the async flow as canonical; no new endpoint added (Pitfall 1) |
</threat_model>

<verification>
- `SWAGGER_UI_DOWNLOAD_URL=... cargo test -p axiam-api-rest --test gdpr_test <4 names>` exits 0 — executable CMPL-02 evidence.
- `test -f docs/compliance/gdpr-compliance.md` — doc exists, stamped v1.2/beta + last-verified (D-12).
- D-05 async-vs-shorthand reconciliation present and honest; SECHRD-06 cited for erasure; consent scope + deferral (D-06) documented.
- Internal links validated later by scripts/check-doc-links.sh (30-05) + docs-ci.yml (30-06).
- Run `cargo clean` in the gap before the next Rust-compiling plan step (disk hygiene).
</verification>

<success_criteria>
CMPL-02 satisfied: export covers every table incl. real sessions (optional PGP), erasure durably pseudonymizes audit PII (SECHRD-06), and consent is recorded and exportable — each proven by a re-run existing test and documented in docs/compliance/gdpr-compliance.md with the D-05 async reconciliation and D-06 consent scope stated honestly. No new endpoint, no consent UI.
</success_criteria>

<output>
Create `.planning/phases/30-compliance-documentation/30-02-SUMMARY.md` when done.
</output>
