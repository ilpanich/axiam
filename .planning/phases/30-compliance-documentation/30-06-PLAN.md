---
phase: 30-compliance-documentation
plan: 06
type: execute
wave: 3
depends_on: [30-03, 30-05]
files_modified:
  - .github/workflows/docs-ci.yml
autonomous: false
requirements: [DOCS-01]
user_setup:
  - service: npm (@asyncapi/cli)
    why: "AsyncAPI 2.6 meta-schema validation of docs/api/asyncapi.yml in docs CI (official AsyncAPI Initiative tool; flagged [SUS] only by the sandbox download-count lookup)"
    dashboard_config:
      - task: "Confirm @asyncapi/cli legitimacy at npmjs.com/package/@asyncapi/cli (repo github.com/asyncapi/cli, official asyncapi.com org) before its first CI install"
        location: "npmjs.com/package/@asyncapi/cli"
must_haves:
  truths:
    - ".github/workflows/docs-ci.yml validates the OpenAPI + AsyncAPI specs parse and runs the internal link-check on doc changes, path-filtered (DOCS-01, D-11)"
    - "@asyncapi/cli legitimacy is human-verified (blocking) before its first CI install (SUS-verdict supply-chain gate)"
    - "The workflow follows the sdk-openapi-drift.yml conventions: SHA-pinned actions, permissions: contents: read, dual pull_request + push:[main] path-filtered triggers"
  artifacts:
    - ".github/workflows/docs-ci.yml"
  key_links:
    - "docs-ci.yml invokes scripts/check-doc-links.sh (30-05) and validates docs/api/asyncapi.yml + docs/api/openapi.json (30-03)"
    - "AsyncAPI validation half has a documented fallback (manual/local validate) if npm egress proves unreliable; OpenAPI parse + link-check stay enforced"
---

<objective>
Add the light docs CI (DOCS-01, D-11): `.github/workflows/docs-ci.yml`, a
path-filtered job that spec-validates the OpenAPI + AsyncAPI specs and runs the
zero-dependency internal link-check. Because the AsyncAPI validator
`@asyncapi/cli` was flagged `[SUS]` by the automated legitimacy check (it IS the
official AsyncAPI Initiative tool — the SUS verdict is a sandbox
download-count-lookup artifact), its first CI install is gated behind a
blocking-human package-legitimacy checkpoint (T-30-SC).

Purpose: A cheap, egress-conscious drift/broken-link guard for the docs.
Output: `.github/workflows/docs-ci.yml`.

This is Wave 3 — it consumes the AsyncAPI/OpenAPI specs (30-03) and the
link-check script + index (30-05). Prefer keeping OpenAPI-parse + link-check
always-enforced; the AsyncAPI-validate half has a documented fallback if npm
egress is unreliable (RESEARCH Environment Availability).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-compliance-documentation/30-CONTEXT.md
@.planning/phases/30-compliance-documentation/30-RESEARCH.md
@.planning/phases/30-compliance-documentation/30-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Blocking-human package-legitimacy checkpoint for @asyncapi/cli (SUS verdict)</name>
  <action>
    Pause execution BEFORE any @asyncapi/cli install and present the legitimacy
    evidence below for human confirmation per the how-to-verify steps. This
    [SUS]-verdict gate is never auto-approvable (workflow.auto_advance is ignored).
    Do NOT install @asyncapi/cli or author its CI step until the reviewer approves,
    or directs the documented fallback (drop the AsyncAPI-validate step, keep
    OpenAPI-parse + link-check).
  </action>
  <what-built>
    Nothing installed yet — this is a package-legitimacy gate BEFORE the docs-CI
    workflow (Task 2) introduces `npx @asyncapi/cli validate` as its AsyncAPI
    validation step. `@asyncapi/cli` received a `[SUS]` verdict from the automated
    Package Legitimacy Audit (RESEARCH § "Package Legitimacy Audit"): the verdict
    stems from the sandbox being unable to fetch npm weekly-download telemetry
    ("too-new" + "unknown-downloads"), NOT from a missing repo, anomalous recency,
    or a risky postinstall network call. It is the official AsyncAPI Initiative CLI
    (repo github.com/asyncapi/cli, same org that publishes the AsyncAPI spec at
    asyncapi.com). It has a postinstall script (node ./scripts/enableAutoComplete.js
    — shell-completion setup, not network/filesystem-external).
  </what-built>
  <how-to-verify>
    Per the SUS-verdict protocol this checkpoint is NOT auto-approvable. Confirm
    @asyncapi/cli's legitimacy before it is first installed in CI:
    1. Open https://www.npmjs.com/package/@asyncapi/cli and confirm the package is
       published by the AsyncAPI Initiative org and links to
       github.com/asyncapi/cli.
    2. Confirm the source repo is the official AsyncAPI org (github.com/asyncapi).
    3. Confirm you accept the documented fallback if you'd rather not add the npm
       dependency to CI: drop ONLY the AsyncAPI-validate step from docs-ci.yml and
       validate asyncapi.yml manually/locally pre-commit (documented in
       docs/api/README.md), keeping the OpenAPI parse + link-check steps enforced
       (RESEARCH § "Environment Availability").
    Approve to install @asyncapi/cli in docs-CI, OR direct the executor to take the
    fallback (link-check + OpenAPI-parse only).
  </how-to-verify>
  <resume-signal>Type "approved" to use @asyncapi/cli in CI, or "fallback" to drop the AsyncAPI-CI half and validate it locally only</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author .github/workflows/docs-ci.yml (spec-validate + link-check), path-filtered, SHA-pinned, least-privilege</name>
  <files>.github/workflows/docs-ci.yml</files>
  <read_first>
    - .github/workflows/sdk-openapi-drift.yml — the near-clone analog (read in full): name header, on.pull_request + on.push both branches:[main] with a paths filter, permissions: contents: read at workflow level, SHA-pinned actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 (v4.2.2) + dtolnay/rust-toolchain + Swatinem/rust-cache, single descriptively-named job, the --dump-openapi export step
    - .planning/phases/30-compliance-documentation/30-RESEARCH.md § "Light Docs CI" (the three steps: OpenAPI parse via --dump-openapi + jq empty; AsyncAPI validate via npx @asyncapi/cli validate; internal link-check via scripts/check-doc-links.sh) and § "Environment Availability" (the npm-egress fallback for the AsyncAPI half)
    - .planning/phases/30-compliance-documentation/30-PATTERNS.md § ".github/workflows/docs-ci.yml" (the exact shape to copy incl. the paths filter ['docs/**','claude_dev/security-audit.md','crates/axiam-amqp/**','crates/axiam-api-rest/src/openapi.rs'] and CI conventions)
    - scripts/check-doc-links.sh (30-05 — the link-check step invokes this) and docs/api/asyncapi.yml + docs/api/openapi.json (30-03 — the specs validated)
    - CLAUDE.md § "Build & Disk Hygiene" (SWAGGER_UI_DOWNLOAD_URL for any axiam-api-rest/axiam-server build; --no-default-features SAML-off like the drift gate)
  </read_first>
  <action>
    Create .github/workflows/docs-ci.yml by cloning the sdk-openapi-drift.yml shape:
    a descriptive name, on.pull_request + on.push both branches:[main] with a
    paths filter of ['docs/**','claude_dev/security-audit.md','crates/axiam-amqp/**','crates/axiam-api-rest/src/openapi.rs'],
    permissions: contents: read at the workflow level, and a single job on
    ubuntu-latest. Pin every action by the SAME commit SHA + version comment used
    across the existing workflows (actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
    # v4.2.2, and the same rust-toolchain/rust-cache pins the drift gate uses).
    Steps: (1) internal link-check — run scripts/check-doc-links.sh (always
    enforced, zero-dependency, egress-free); (2) OpenAPI parse — build
    axiam-server --no-default-features (export SWAGGER_UI_DOWNLOAD_URL to the cached
    zip; install protobuf-compiler as the drift gate does), run --dump-openapi to a
    temp file, and assert valid JSON with jq empty (always enforced); (3) AsyncAPI
    validate — per the checkpoint outcome: if approved, npx @asyncapi/cli validate
    docs/api/asyncapi.yml; if the checkpoint returned "fallback", OMIT this step and
    add a comment referencing the docs/api/README.md manual-validation note
    (RESEARCH Environment Availability). Give every job/step a human-readable name.
  </action>
  <acceptance_criteria>
    - `test -f .github/workflows/docs-ci.yml` and it parses as YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docs-ci.yml'))"` exits 0.
    - Least privilege: `grep -Eq 'contents: *read' .github/workflows/docs-ci.yml` exits 0 and no write/secrets permission scope is present.
    - Actions are SHA-pinned: `grep -q 'actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683' .github/workflows/docs-ci.yml` exits 0 (matches the repo convention).
    - Path-filtered on docs + spec sources: `grep -q 'docs/' .github/workflows/docs-ci.yml && grep -q 'claude_dev/security-audit.md' .github/workflows/docs-ci.yml` exits 0.
    - The link-check step invokes the script: `grep -q 'check-doc-links.sh' .github/workflows/docs-ci.yml` exits 0.
    - The OpenAPI parse step is present (`jq empty` or json.tool over the dumped spec).
    - The AsyncAPI step reflects the checkpoint outcome: either invokes `@asyncapi/cli validate docs/api/asyncapi.yml`, or is omitted with a fallback comment referencing the manual local-validation note.
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/docs-ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docs-ci.yml'))" && grep -Eq 'contents: *read' .github/workflows/docs-ci.yml && grep -q 'actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683' .github/workflows/docs-ci.yml && grep -q 'check-doc-links.sh' .github/workflows/docs-ci.yml</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner ← npm registry | The docs-CI job installs @asyncapi/cli from npm — an untrusted supply-chain fetch crosses into the CI runner |
| CI workflow ← repo permissions | An over-privileged workflow could be abused to write to the repo or read secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-SC | Tampering | npm install of @asyncapi/cli in docs-ci | high | mitigate | Blocking-human package-legitimacy checkpoint (npmjs.com/package/@asyncapi/cli, official AsyncAPI Initiative org) BEFORE first CI install — never auto-approvable; prefer the zero-dependency link-check script (no npm) for link-checking; documented fallback drops only the AsyncAPI-validate step (manual/local validate) if npm egress is unreliable, keeping OpenAPI-parse + link-check enforced |
| T-30-16 | Elevation of privilege (over-privileged CI workflow) | .github/workflows/docs-ci.yml | medium | mitigate | permissions: contents: read at the workflow level (matches sdk-openapi-drift.yml); no write/packages/secrets scopes; SHA-pinned actions |
| T-30-17 | Tampering (compromised action version) | pinned GitHub Actions | medium | mitigate | Every action pinned by commit SHA + version comment, reusing the exact pins already vetted across .github/workflows/ |
</threat_model>

<verification>
- Blocking-human checkpoint approved (or "fallback" chosen) before @asyncapi/cli first CI install — SUS-verdict gate, never auto-approved.
- `test -f .github/workflows/docs-ci.yml && python3 -c "import yaml; yaml.safe_load(...)"` — workflow exists and is valid YAML.
- `grep -Eq 'contents: *read' ...` — least-privilege permissions; no write/secrets scope.
- Actions SHA-pinned to the repo-standard commit; path-filtered on docs + spec sources.
- Link-check step invokes scripts/check-doc-links.sh; OpenAPI parse step present; AsyncAPI step reflects the checkpoint outcome.
- Phase gate: docs-CI job green in CI + unaffected ci.yml build/clippy/test (this phase touches no non-docs Rust code).
</verification>

<success_criteria>
DOCS-01 CI portion satisfied: .github/workflows/docs-ci.yml is a path-filtered, SHA-pinned, least-privilege job that link-checks the docs and spec-validates the OpenAPI (always) + AsyncAPI (gated behind the blocking-human @asyncapi/cli legitimacy checkpoint, with a documented fallback) — a cheap egress-conscious drift/broken-link guard scoped to docs (D-11).
</success_criteria>

<output>
Create `.planning/phases/30-compliance-documentation/30-06-SUMMARY.md` when done.
</output>