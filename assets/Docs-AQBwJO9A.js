import{i as e,n as t,r as n,t as r}from"./index-o5T_Wdi9.js";var i=e(),a=[{slug:`overview`,section:`Getting started`,navLabel:`What is AXIAM?`,title:`What is AXIAM?`,intro:`A multi-tenant identity and access management server written in Rust — OAuth2 and OpenID Connect provider, RBAC authorization engine, PKI, and an audit trail, reachable over REST, gRPC and AMQP.`,blocks:[{type:`h`,id:`what`,text:`In one paragraph`},{type:`p`,text:`AXIAM issues and validates identities, and answers the question *may this principal perform this action on this resource?* It is a complete OAuth2 authorization server and OpenID Connect provider, a role-based authorization engine that evaluates over a hierarchy of resources, a certificate authority for machine identities, and an append-only audit log — all of it partitioned by tenant, so one deployment can serve many isolated customers without any of them being able to observe another.`},{type:`p`,text:`It is built for the two workloads that stress an IAM system in different directions: **microservices**, where an authorization check happens on nearly every request and tail latency is the constraint, and **IoT fleets**, where identities are certificates rather than passwords and devices commission themselves without a browser.`},{type:`h`,id:`capabilities`,text:`What it does`},{type:`table`,proseFirstCol:!0,headers:[`Area`,`Capability`],rows:[[`Authentication`,`Password (Argon2id), OPAQUE (RFC 9807, password never leaves the client), TOTP, WebAuthn passkeys and security keys, X.509 client certificates, and federated sign-in via SAML or OIDC.`],[`Authorization`,`Role-based, default-deny, with deny-override precedence, a cascading resource hierarchy, sub-resource scopes, groups, service accounts, and UMA 2.0 for resource servers that guard what they do not own.`],[`Standards`,`OAuth2 (authorization code + PKCE, client credentials, refresh, device grant, token exchange, PAR), OpenID Connect with discovery and RP-initiated plus back-channel logout, FAPI 2.0 Security Profile, SCIM 2.0 provisioning.`],[`Machine identity`,`Per-tenant X.509 issuance under an organization CA, mTLS authentication terminated in-process, certificate-bound access tokens, and OpenPGP keys for audit signing and encrypted exports.`],[`Operations`,`Append-only signed audit log, webhooks, Reactors (external hook actors on the AMQP bus), pluggable secret providers including HashiCorp Vault, and GDPR export/erasure endpoints.`]]},{type:`h`,id:`protocols`,text:`Three protocols, one engine`},{type:`p`,text:`The same domain logic is reachable three ways. Choosing between them is a transport decision, not a capability one — an authorization check returns the same answer whichever door it arrives through.`},{type:`table`,proseFirstCol:!0,headers:[`Protocol`,`Use it for`,`Spec`],rows:[[`REST`,`Administration, the admin console, and any HTTP integrator. The broadest surface — every entity is manageable here.`,"OpenAPI 3.1 (`sdks/openapi.json`)"],[`gRPC`,`The hot path inside a service mesh: authorization checks, token validation and user lookups where connection reuse and binary framing matter.`,"Protocol Buffers (`proto/axiam/v1/`)"],[`AMQP`,`Deferred authorization decisions, audit ingestion, mail, webhook and notification delivery, and Reactor hooks.`,"AsyncAPI 2.6 (`docs/api/asyncapi.yml`)"]]},{type:`h`,id:`shape`,text:`How a deployment is shaped`},{type:`code`,caption:`the runtime picture`,code:`  Browsers · Mobile · IoT devices · Services · Admin console
        |             |              |
     REST/HTTPS    gRPC/mTLS       AMQP
        |             |              |
        v             v              v
  +-------------------------------------------+
  |               axiam-server                |
  |   Actix-Web  |  Tonic  |  Lapin consumer  |
  +-------------------------------------------+
  |  AuthN · AuthZ · OAuth2/OIDC · Federation |
  |  PKI · SCIM · Audit · Email · Reactors    |
  +-------------------------------------------+
        |                        |
        v                        v
   SurrealDB                RabbitMQ
  (all domain state)   (async work + events)
        |
        v
   HashiCorp Vault  (long-lived secrets, production)`},{type:`p`,text:"`axiam-server` is a single binary composing a workspace of focused crates. It is stateless: every piece of durable state lives in SurrealDB, so replicas scale horizontally behind an ordinary load balancer."},{type:`h`,id:`status`,text:`Project status`},{type:`warn`,text:`AXIAM is pre-1.0 and under active development. It should not carry production identity traffic until it reaches a stable release. The security posture described throughout these docs is a self-assessment backed by tests and a threat model — not a certified third-party audit.`},{type:`h`,id:`start`,text:`Where to go next`},{type:`cards`,cards:[{title:`Quickstart →`,body:`Run the stack and make your first authorization check.`,to:`docs`,doc:`quickstart`},{title:`Browse the SDKs →`,body:`Install snippets and quickstarts for eleven languages.`,to:`sdks`},{title:`See the benchmarks →`,body:`Measured throughput, latency and the cost of each security tier.`,to:`bench`},{title:`Read the security model →`,body:`Threat model, cryptography choices and shared responsibility.`,to:`security`}]}]},{slug:`quickstart`,section:`Getting started`,navLabel:`Quickstart`,title:`Quickstart`,intro:`Get a local AXIAM instance running, create your first admin, then make an authenticated authorization check — in about ten minutes.`,blocks:[{type:`h`,id:`prereq`,text:`Prerequisites`},{type:`p`,text:"AXIAM builds with Rust 1.93+ and runs its dev infrastructure (SurrealDB + RabbitMQ) in Docker. The `just` task runner wraps the common commands. If you only want to *use* a running instance, you need none of this — skip to [your first check](#authz) and point an SDK at your server."},{type:`h`,id:`run`,text:`1. Start the stack`},{type:`code`,caption:`terminal`,code:`# clone and enter the repository
git clone https://github.com/ilpanich/axiam.git
cd axiam

# start dev infrastructure (SurrealDB + RabbitMQ)
just dev-up

# build and run the server
just build
just run`},{type:`p`,text:"The REST API comes up on `http://localhost:8090` and gRPC on `localhost:50051`. Confirm it is alive and that it can reach the database:"},{type:`code`,code:`curl -s localhost:8090/health   # liveness  — always 200 if the process is up
curl -s localhost:8090/ready    # readiness — 200 only when SurrealDB answers`},{type:`h`,id:`admin`,text:`2. Create the first admin`},{type:`p`,text:`A fresh deployment has no users at all, and the bootstrap endpoint that creates the first one is fail-closed — it refuses unless you prove you are the operator. The full procedure, both gates and the seeded role set are on [First organization, tenant and admin](#/docs/bootstrap).`},{type:`code`,caption:`the short version`,code:`# Lock bootstrap to a known address before starting the server
export AXIAM_BOOTSTRAP_ADMIN_EMAIL=admin@acme.dev

curl -X POST localhost:8090/api/v1/admin/bootstrap \\
  -H 'content-type: application/json' \\
  -d '{
        "organization_name": "Acme",
        "tenant_name": "Production",
        "email": "admin@acme.dev",
        "username": "admin",
        "password": "'"$ADMIN_PASSWORD"'"
      }'`},{type:`h`,id:`authz`,text:`3. Your first authorization check`},{type:`p`,text:"Install a client SDK, construct a client for your tenant, sign in, then call `can()`. Tenant is always explicit — AXIAM is multi-tenant and has no default tenant, so there is no ambient context to get wrong."},{type:`codegroup`,caption:`authenticate, then check`,tabs:[{label:`TypeScript`,code:`import { AxiamClient } from 'axiam-sdk';

const client = new AxiamClient({
  baseUrl: 'https://iam.acme.dev',
  tenantSlug: 'acme',
  orgSlug: 'acme',
});

const result = await client.login(email, password);
if (result.status === 'mfa_required') {
  await client.verifyMfa(result.mfaToken, code);
}

const allowed = await client.can('read', 'doc:1');`},{label:`Python`,code:`from axiam_sdk import AxiamClient

with AxiamClient(
    base_url="https://iam.acme.dev",
    tenant_slug="acme",
    org_slug="acme",
) as client:
    result = client.login(email, password)
    if result.mfa_required:
        result = client.verify_mfa(result.mfa_token, totp_code)

    allowed = client.can("read", resource_id)`},{label:`Rust`,code:`use axiam_sdk::AxiamClient;

let client = AxiamClient::builder()
    .base_url("https://iam.acme.dev")?
    .tenant_slug("acme")
    .org_slug("acme")
    .build()?;

let result = client.login("user@acme.dev", &password).await?;
if result.mfa_required {
    client.verify_mfa("123456").await?;
}

let allowed = client.can("read", resource_id, None).await?;`},{label:`Go`,code:`import axiam "github.com/ilpanich/axiam-go-sdk"

// tenantSlug is required — there is no default tenant. Login also needs
// organization context: a tenant slug is only unique within an org.
client, err := axiam.NewClient(baseURL, "acme", axiam.WithOrgSlug("acme"))

result, err := client.Login(ctx, email, password)
if result.MFARequired {
    // complete the challenge
}

allowed, err := client.Can(ctx, "read", resourceID)`},{label:`Java`,code:`try (AxiamClient client = AxiamClient.builder("https://iam.acme.dev", "acme")
        .orgSlug("acme")
        .build()) {
    LoginResult result = client.login("user@acme.dev", password);
    if (result.mfaRequired()) {
        result = client.verifyMfa(result.challengeToken(), "123456");
    }

    boolean allowed = client.can("read", "doc:1");
}`}]},{type:`note`,text:"In a browser, tokens arrive only via `httpOnly` cookies — CSRF forwarding and single-flight refresh are handled for you, and browser code imports only from `axiam-sdk/rest`. There is no code path in any SDK that puts a token in `localStorage`."},{type:`h`,id:`curl`,text:`The same thing over plain HTTP`},{type:`p`,text:"No SDK required — every capability is reachable with an HTTP client. Two things to know before you start: **login returns no token in its body**. It sets three cookies — `axiam_access`, `axiam_refresh` and a non-`httpOnly` `axiam_csrf` — so a raw HTTP client needs a cookie jar. And every state-changing request must echo the CSRF cookie back in an `X-CSRF-Token` header; `/auth/login`, the OPAQUE start endpoints and everything under `/oauth2/` are exempt, because their callers have no CSRF cookie yet."},{type:`code`,caption:`login, then check — with a cookie jar`,code:`# 1. authenticate. The tokens come back as cookies, not in the body.
curl -sS -c jar.txt -X POST https://iam.acme.dev/api/v1/auth/login \\
  -H 'content-type: application/json' \\
  -d '{"org_slug":"acme","tenant_slug":"production",
       "username_or_email":"admin@acme.dev","password":"..."}'

# 2. state-changing calls echo the CSRF cookie in a header.
CSRF=$(awk '/axiam_csrf/ {print $7}' jar.txt)

curl -sS -b jar.txt -X POST https://iam.acme.dev/api/v1/authz/check \\
  -H "x-csrf-token: $CSRF" \\
  -H 'content-type: application/json' \\
  -d '{"action":"read","resource_id":"doc:1"}'`},{type:`note`,text:"A **machine** client does not do any of this. It uses the OAuth2 client-credentials grant, which *does* return `access_token` in the response body, and then sends `Authorization: Bearer <token>` — no cookie jar and no CSRF header. See [Service accounts](#/docs/service-accounts)."},{type:`h`,id:`next`,text:`Next steps`},{type:`cards`,cards:[{title:`Browse the SDKs →`,body:`Quickstarts for all eleven languages.`,to:`sdks`},{title:`See the benchmarks →`,body:`Performance, efficiency and security posture.`,to:`bench`}]}]},{slug:`installation`,section:`Getting started`,navLabel:`Installation`,title:`Installation`,intro:`Build AXIAM from source, or run the whole stack in containers. Both paths, and what each one is good for.`,blocks:[{type:`h`,id:`which`,text:`Which path?`},{type:`table`,proseFirstCol:!0,headers:[`You want to…`,`Use`,`Notes`],rows:[[`Develop against AXIAM, or on it`,"`just dev-up` + a natively-run server",`Only SurrealDB and RabbitMQ run in Docker; the server runs on your machine so you get fast rebuilds and a debugger.`],[`Validate the whole stack end to end`,"`just prod-up`",`Server, admin console, database and broker in Compose. Documented as *not* for real production — it exists to prove the pieces fit.`],[`Run it for real`,"The Kubernetes manifests in `k8s/`",`See [Docker & Kubernetes](#/docs/deploy) and [Production hardening](#/docs/hardening).`]]},{type:`h`,id:`toolchain`,text:`Toolchain`},{type:`p`,text:"AXIAM is a Cargo workspace of focused crates — `axiam-core` holds the domain types, `axiam-db` the SurrealDB repositories, `axiam-auth`, `axiam-authz`, `axiam-oauth2`, `axiam-pki`, `axiam-scim` and the rest hold one concern each, and `axiam-server` composes them into the runnable binary. You need a Rust 1.93+ toolchain, Docker, and the `just` task runner."},{type:`code`,caption:`install the task runner`,code:`# macOS
brew install just

# or via cargo, anywhere
cargo install just`},{type:`h`,id:`services`,text:`Backing services`},{type:`p`,text:"`just dev-up` starts the development infrastructure in Docker: a SurrealDB node (the document/graph store behind every domain entity) and a RabbitMQ broker (async authorization, audit ingestion, mail and event delivery). `just dev-down` stops them again."},{type:`code`,code:`just dev-up      # start SurrealDB + RabbitMQ
just dev-down    # stop them`},{type:`h`,id:`build`,text:`Build & verify`},{type:`code`,code:`just build       # compile the workspace
just test        # run all tests
just check       # fmt + lint + test — the same gate CI enforces`},{type:`h`,id:`features`,text:`Optional build features`},{type:`p`,text:"SAML federation is behind a default-on `saml` feature because it links `libxml`, which needs system libxml2 headers. Where those are unavailable, build without default features — this is exactly what CI's *Build (SAML off)* job does, and everything except the SAML service-provider path is unaffected."},{type:`code`,code:`cargo build -p axiam-server --no-default-features`},{type:`h`,id:`compose`,text:`The full stack in Compose`},{type:`p`,text:"`just prod-up` builds and starts `axiam-server`, the admin console, SurrealDB and RabbitMQ together. It generates a local-only Ed25519 signing keypair under `docker/.secrets/` on first run and refuses to start if the database and broker credentials are not set in your shell — the Compose file uses fail-fast variable syntax rather than silently defaulting."},{type:`code`,code:`just prod-up      # build + start everything
#   admin console  http://localhost:8081
#   REST API       http://localhost:8090
#   gRPC           localhost:50051

just prod-down    # stop, keep volumes
just prod-clean   # stop and remove volumes`},{type:`warn`,text:"`docker-compose.prod.yml` is a workstation validation tool, not a production deployment. It is named for the production *image* it builds, not for where it should run. Use the Kubernetes manifests for anything real."}]},{slug:`bootstrap`,section:`Getting started`,navLabel:`First org, tenant & admin`,title:`The bootstrap procedure`,intro:`A fresh AXIAM has no organizations, no tenants and no users — and every administrative endpoint requires an authenticated caller who holds an explicit grant. Bootstrap is the one call that resolves that circularity, and it is built so that it can only ever be made by the operator, exactly once.`,blocks:[{type:`h`,id:`why`,text:`The problem bootstrap solves`},{type:`p`,text:"AXIAM is default-deny all the way down: a caller needs a token, and the principal behind that token needs a permission grant, for every administrative action. On a brand-new deployment nobody satisfies either condition, so something has to create the first administrator out of nothing. That something is `POST /api/v1/admin/bootstrap` — the only privileged endpoint reachable without an access token."},{type:`p`,text:`An endpoint like that is worth exactly as much as the gate in front of it, and as much as its guarantee that it cannot run twice. Both are enforced in the database rather than by a check in the handler, which is the part worth understanding before you run it.`},{type:`h`,id:`does`,text:`What one call does`},{type:`p`,text:`Bootstrap is a single call that provisions the entire first-run state, inside one transaction. There is no partially-bootstrapped outcome: either all of the following exists afterwards, or none of it does.`},{type:`steps`,steps:[{title:`Creates the organization`,body:"From `organization_name`, with `organization_slug` derived from it when you do not supply one. Get-or-create by slug, so retrying after a transient failure reuses the organization rather than duplicating it."},{title:`Creates the default tenant`,body:'From `tenant_name` (default `"Default"`) and `tenant_slug` (default `"default"`). Also get-or-create by slug.'},{title:`Seeds the permission registry into the tenant`,body:"All 113 built-in permissions across 25 families — `users:*`, `roles:*`, `resources:*`, `oauth2_clients:*`, `certificates:*`, `audit_logs:*`, `reactors:*`, `gdpr:*` and the rest. These are the actions the REST API's own route guards check against."},{title:`Seeds three default roles`,body:"`super-admin` (full system access — every permission), `admin` (all entity CRUD) and `viewer` (read-only: list and get). Seeding is idempotent — already-granted pairs are skipped rather than re-granted."},{title:`Creates the admin user and binds the super-admin role`,body:`The password is hashed with Argon2id server-side. If you asked for an OPAQUE baseline, the registration record is created here too.`},{title:`Takes the bootstrap lock`,body:"A uniqueness-invariant `bootstrap_lock:global` row is created in the same transaction. This is what makes the operation one-shot — see below."}]},{type:`note`,text:"Bootstrap issues **no token**. The new admin authenticates through `POST /api/v1/auth/login` like anybody else. That is deliberate: a provisioning endpoint that also hands out a session is a provisioning endpoint that is worth attacking twice."},{type:`h`,id:`gates`,text:`The gate — fail-closed, two ways to satisfy it`},{type:`p`,text:"A request is refused with `403` unless **one** of two gates is satisfied. Neither gate being configured does not mean *allow*; it means the endpoint is unusable until an operator configures one. There is no deployment in which an arbitrary caller can create the first admin."},{type:`table`,proseFirstCol:!0,headers:[`Gate`,`How it works`,`When to prefer it`],rows:[["`AXIAM_BOOTSTRAP_ADMIN_EMAIL`","Set this environment variable on the `axiam-server` process before it starts. The request's `email` field must match it **exactly**, or the call is refused. When this variable is set, `setup_token` is ignored entirely.",`Automated and declarative deployments. The operator names the first admin before the server exists, and nothing has to be recovered from a log.`],[`One-time setup token`,"Used only when the variable is unset. On first boot — and only while no admin has ever been bootstrapped — the server mints a random token, stores its SHA-256 hash, and logs the token once at `info`. Pass it as `setup_token`.",`Interactive installs and local exploration, where you are watching the server start.`]]},{type:`p`,text:"The token is single-use, and that is enforced twice. A pre-check rejects a hash that is unknown or already consumed; the authoritative guarantee is a `bootstrap_setup_token_consumed` row created inside the same transaction as the admin user, so two requests racing past the pre-check still leave exactly one winner — the loser hits a uniqueness violation and its whole transaction rolls back."},{type:`warn`,text:"The setup token is written to the server log exactly once, at `info` level. If your log pipeline drops `info`, buffers it, or you simply scroll past it, there is no way to re-read or re-mint it. Set `AXIAM_BOOTSTRAP_ADMIN_EMAIL` and restart instead — for anything unattended, prefer that gate in the first place."},{type:`note`,text:"`AXIAM_BOOTSTRAP_ADMIN_EMAIL` uses a **single** underscore after the prefix, unlike the `AXIAM__*` configuration variables. It is read straight from the environment rather than through the layered config loader, which is why it does not follow that convention — and why doubling the underscore here silently disables the gate."},{type:`h`,id:`once`,text:`Why it can only ever run once`},{type:`p`,text:"The one-shot property is not a `SELECT` followed by an `INSERT` — that pattern has a window between the two in which a second caller can also pass. Instead, creating the first super-admin includes creating `bootstrap_lock:global`, a row whose record id is a uniqueness invariant, in the *same* transaction."},{type:`list`,items:["**Two concurrent first-run requests** — both attempt the lock, one wins, the loser gets `409 Conflict` and rolls back completely. At most one super-admin can exist, with no orphaned role assignment left behind.","**Any later call** — hits the same uniqueness violation and is refused with `409`. Additional organizations, tenants and admins are created through the authenticated admin API from then on."]},{type:`h`,id:`request`,text:`Request fields`},{type:`table`,headers:[`Field`,`Required`,`Meaning`],rows:[[`organization_name`,`yes`,`Display name of the organization to create.`],[`organization_slug`,`no`,"URL-safe slug. Derived from `organization_name` when omitted or blank — ASCII alphanumerics are lower-cased and every other run of characters collapses to a single dash."],[`tenant_name`,`no`,"Display name of the default tenant. Defaults to `Default`."],[`tenant_slug`,`no`,"URL-safe slug for that tenant. Defaults to `default`."],[`email`,`yes`,"Admin email address. Must match `AXIAM_BOOTSTRAP_ADMIN_EMAIL` when that gate is in use."],[`username`,`yes`,`Admin username.`],[`password`,`yes`,`Admin password. Hashed with Argon2id before storage.`],[`setup_token`,`conditional`,"Required when `AXIAM_BOOTSTRAP_ADMIN_EMAIL` is not set; ignored when it is."],[`opaque_mode`,`no`,"Seeds the new organization's OPAQUE baseline: `disabled` (the default), `optional` or `required`."],[`opaque_suite`,`no`,"RFC 9807 ciphersuite for that baseline. Defaults to `ristretto255_sha512`."],[`opaque_ksf`,`no`,"Client key-stretching function for that baseline. Defaults to `argon2id`."]]},{type:`h`,id:`call`,text:`Making the call`},{type:`code`,caption:`gate 1 — AXIAM_BOOTSTRAP_ADMIN_EMAIL`,code:`# Set on the axiam-server process, BEFORE it starts.
export AXIAM_BOOTSTRAP_ADMIN_EMAIL=admin@acme.dev

curl -X POST https://iam.acme.dev/api/v1/admin/bootstrap \\
  -H 'content-type: application/json' \\
  -d '{
        "organization_name": "Acme Corporation",
        "organization_slug": "acme",
        "tenant_name": "Production",
        "tenant_slug": "production",
        "email": "admin@acme.dev",
        "username": "admin",
        "password": "'"$ADMIN_PASSWORD"'"
      }'`},{type:`code`,caption:`gate 2 — the one-time setup token`,code:`# Capture the token the server logged once at first boot:
#   AXIAM first-run bootstrap setup token minted. Use this token ONCE ...
export SETUP_TOKEN=...

curl -X POST https://iam.acme.dev/api/v1/admin/bootstrap \\
  -H 'content-type: application/json' \\
  -d '{
        "organization_name": "Acme Corporation",
        "email": "admin@acme.dev",
        "username": "admin",
        "password": "'"$ADMIN_PASSWORD"'",
        "setup_token": "'"$SETUP_TOKEN"'"
      }'`},{type:`code`,caption:`201 Created`,code:`{
  "message": "Bootstrap completed",
  "organization_id": "0f8c...",
  "organization_slug": "acme",
  "tenant_id": "3a91...",
  "tenant_slug": "production",
  "user_id": "c47b..."
}`},{type:`p`,text:"Keep `organization_slug` and `tenant_slug` — every SDK constructor takes them, and they are how a client names the tenant it is talking to."},{type:`h`,id:`responses`,text:`Response codes`},{type:`table`,headers:[`Status`,`Meaning`,`What to do`],rows:[[`201`,`Organization, tenant and admin created.`,`Log in and continue with the checklist below.`],[`400`,"A required field is missing or blank (`organization_name`, `email`, `username`, `password`).",`Fix the body. Nothing was created.`],[`403`,`The gate was not satisfied — email mismatch, or a missing/unknown/already-consumed setup token. The error names which.`,"Check that `AXIAM_BOOTSTRAP_ADMIN_EMAIL` is set on the *server* process and matches byte-for-byte, or that you are using a fresh token."],[`409`,`Bootstrap has already completed on this deployment, or a concurrent request won the race.`,`Not an error to retry. Log in as the existing admin; create further tenants and admins through the authenticated API.`]]},{type:`h`,id:`opaque`,text:`Seeding an OPAQUE baseline at bootstrap`},{type:`p`,text:"`opaque_mode` is settable here, and not only through the settings API afterwards, for one specific reason: OPAQUE's `required` mode cannot be turned on retroactively. A registration record has to be built from the plaintext password, and a stored Argon2id hash is not invertible — so nobody can be enrolled after the fact. If you switched a deployment to `required` while its only account had no record, you would lock the sole administrator out of their own system with no second admin to undo it."},{type:`p`,text:"Bootstrap is also the one enrolment path that does not take a client-built record, and that is not a weakening. Every other path requires the client to build the record because the server must never see the plaintext; bootstrap takes `password` in the request body by construction, so it runs the client half of the registration itself. The resulting record is indistinguishable from a client-built one."},{type:`note`,text:`If you want an OPAQUE record the server provably never held material for, change the admin password once after bootstrap through the ordinary password-change flow — that path is client-built like every other one.`},{type:`h`,id:`after`,text:`What to do immediately after`},{type:`steps`,steps:[{title:`Enrol a second factor on the admin account`,body:`A single-factor super-admin is the most valuable credential in the deployment. Register a passkey or a TOTP authenticator before anything else.`},{title:`Create a second administrator`,body:`One admin account is one lost device away from an unrecoverable deployment, and bootstrap cannot be run again to rescue you.`,code:`POST /api/v1/users            # create the user
POST /api/v1/roles/{role_id}/users   # bind them to super-admin`},{title:`Create the tenants you actually need`,body:`The bootstrap tenant is a starting point, not a container for everything. Tenants are the isolation boundary — one per customer, environment or business unit.`,code:`POST /api/v1/organizations/{org_id}/tenants
{ "name": "Acme Staging", "slug": "acme-staging" }`},{title:`Review the settings baseline`,body:`Password policy, lockout, token lifetimes, MFA enforcement and the OPAQUE mode all default at the organization and are tightened per tenant.`,code:`GET /api/v1/organizations/{org_id}/settings`},{title:`Unset the bootstrap gate`,body:"Once bootstrap has completed, `AXIAM_BOOTSTRAP_ADMIN_EMAIL` does nothing — the lock refuses every further call regardless. Removing it keeps your deployment manifest honest about what is still load-bearing."}]},{type:`cards`,cards:[{title:`Settings & policies →`,body:`The org baseline, tenant tighten-only overrides, and every policy field.`,to:`docs`,doc:`settings`},{title:`Production hardening →`,body:`The checklist to work through before this deployment carries real traffic.`,to:`docs`,doc:`hardening`}]}]},{slug:`concepts`,section:`Getting started`,navLabel:`Core concepts`,title:`Core concepts`,intro:`The domain model AXIAM is built around — organizations and tenants at the top, and the entities scoped inside them.`,blocks:[{type:`h`,id:`tenancy`,text:`Organizations & tenants`},{type:`p`,text:`**Organizations** are the top-level entity. They own the CA certificates, hold the settings baseline, and contain one or more tenants. **Tenants** are the isolation boundary: each has its own users, groups, roles, permissions, resources, certificates, OAuth2 clients, federation configuration and OPAQUE key material. Nothing crosses a tenant boundary implicitly.`},{type:`p`,text:"There is no default tenant and no ambient tenant context. Every SDK constructor takes one, every gRPC request message carries `tenant_id`, and every OAuth2 endpoint takes it as a query parameter. This is deliberate: the most common multi-tenancy bug is a query that forgot to filter, and an API that cannot express *unscoped* cannot express that bug."},{type:`code`,caption:`the containment hierarchy`,code:`Organization  (CA certificates, settings baseline)
 └── Tenant   (the isolation boundary)
      ├── Users, Groups, Service accounts
      ├── Roles → Permissions (action + resource, allow or deny)
      ├── Resources (a tree) → Scopes
      ├── OAuth2 clients, Federation configs
      ├── Certificates, PGP keys
      └── Webhooks, Reactors, Notification rules`},{type:`h`,id:`identity`,text:`Identities`},{type:`table`,proseFirstCol:!0,headers:[`Entity`,`What it is`],rows:[[`User`,`A human identity. Authenticates with a password, an OPAQUE record, a federated IdP, a passkey, or a client certificate — with optional MFA on top.`],[`Group`,`A named collection of users. Roles assigned to a group are inherited by every member, which is how you avoid per-user grants.`],[`Service account`,`A non-human identity for machine-to-machine access. Authenticates with client credentials or a bound X.509 certificate, and can have its secret rotated in place.`],[`OAuth2 client`,`A registered application. Confidential or public, with its own redirect URIs, grant types, scopes and — optionally — a FAPI 2.0 constraint bundle.`]]},{type:`h`,id:`access`,text:`Roles, permissions & resources`},{type:`p`,text:`A **permission** is an action on a resource, carrying an effect of either allow or deny. A **role** is a collection of permissions, assignable to users or to groups, and either global or bound to a specific resource. **Resources** form a tree, and a role assigned on a parent cascades to its descendants. **Scopes** add sub-resource granularity where a permission needs to apply to only part of a resource.`},{type:`p`,text:'Evaluation is **default-deny with deny-override**: nothing is permitted unless a grant allows it, and an explicit `effect: "deny"` refuses regardless of what else allows it — at any depth of the hierarchy and at equal specificity. See [the authorization engine](#/docs/authz) for the precedence table and why deny-override rather than most-specific-wins.'},{type:`note`,text:`Earlier revisions of this page described the engine as additive-only with no explicit deny. That was accurate before deny grants shipped; it is not accurate now. Deny is a first-class effect on a permission grant.`},{type:`h`,id:`sessions`,text:`Sessions, tokens & credentials`},{type:`p`,text:"Authenticating produces a **session**, identified by a `sid` that survives token refresh. Access tokens are short-lived EdDSA-signed JWTs; refresh tokens are opaque, server-stored and single-use. In a browser both live only in `httpOnly` cookies. Logging out ends the session — and, if relying parties are registered for back-channel logout, tells them so."},{type:`h`,id:`audit`,text:`Everything is audited`},{type:`p`,text:`Every authentication, authorization decision and administrative mutation is written to an append-only, cryptographically signed audit log. There are no UPDATE or DELETE paths on it. This is a property of the platform rather than a feature you enable.`},{type:`cards`,cards:[{title:`Authorization engine →`,body:`Precedence, cascade, scopes and how a decision is actually computed.`,to:`docs`,doc:`authz`},{title:`The security model →`,body:`Threat model, cryptography and shared responsibility.`,to:`security`}]}]}],o=[{slug:`auth`,section:`Authentication`,navLabel:`Passwords & sessions`,title:`Passwords, sessions & tokens`,intro:`How AXIAM verifies a credential, what it hands back, and how that session is kept alive — or ended.`,blocks:[{type:`h`,id:`passwords`,text:`Password storage`},{type:`p`,text:`Passwords are hashed with **Argon2id** using OWASP-recommended parameters, with a server-side pepper mixed in before hashing. AXIAM never stores or logs a plaintext credential, and every SDK wraps secrets in a redacting type so they cannot leak into a log line, a debug print or an error message.`},{type:`p`,text:"Each in-flight Argon2id operation allocates roughly a 19 MiB arena, which makes unbounded concurrency a memory-exhaustion vector rather than a throughput problem. A process-wide semaphore caps the number of concurrent arenas and sheds excess load with a `503` instead of queueing without limit. The cost parameters themselves are never weakened to buy throughput — see `AXIAM__AUTH__MAX_CONCURRENT_HASHES` in [Configuration](#/docs/configuration)."},{type:`note`,text:`If the password should never reach the server at all, AXIAM speaks OPAQUE (RFC 9807). It is off by default and enabled per organization or tenant — see [OPAQUE](#/docs/opaque).`},{type:`h`,id:`policy`,text:`Password policy`},{type:`p`,text:"Complexity, history and breach-checking are policy, set as an organization baseline and tightened per tenant. The fields are `min_length`, `require_uppercase`, `require_lowercase`, `require_digits`, `require_symbols`, `password_history_count` (how many previous hashes a new password is checked against) and `hibp_check_enabled` (reject passwords appearing in known-breach corpora). See [Settings & policies](#/docs/settings)."},{type:`h`,id:`lockout`,text:`Lockout`},{type:`p`,text:"Repeated failures lock an account with exponential backoff rather than a flat window: `max_failed_login_attempts` triggers a lock of `lockout_duration_secs`, multiplied by `lockout_backoff_multiplier` on each subsequent lock and clamped at `max_lockout_duration_secs`. An administrator holding `users:admin` can clear a lock early."},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/users/{user_id}/unlock`,summary:`Clear an active lockout on an account.`}]},{type:`h`,id:`tokens`,text:`Tokens`},{type:`table`,proseFirstCol:!0,headers:[`Token`,`Form`,`Lifetime & rules`],rows:[[`Access token`,`JWT signed with EdDSA (Ed25519)`,"Short-lived — 15 minutes by default, set per tenant with `access_token_lifetime_secs`. Verifiable offline against the tenant's JWKS."],[`Refresh token`,`Opaque, server-stored`,`**Single-use**: every refresh rotates it and invalidates the previous one. Reuse of a spent token is detectable and treated as compromise.`],[`ID token`,`JWT (OpenID Connect)`,"Carries `sid`, the session identifier, which is stable across refresh so a relying party can still match a logout token to its own session."]]},{type:`p`,text:"In a browser, none of these are handed to JavaScript. They live in `httpOnly` cookies, and the SDKs handle CSRF token forwarding and single-flight refresh — concurrent 401s collapse into one refresh request rather than a thundering herd of them."},{type:`h`,id:`flow`,text:`The login flow`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/login`,summary:`Authenticate with username/email and password.`,public:!0},{method:`POST`,path:`/api/v1/auth/refresh`,summary:`Exchange a refresh token for a new pair. Rotates.`,public:!0},{method:`POST`,path:`/api/v1/auth/logout`,summary:`End the session.`},{method:`POST`,path:`/api/v1/auth/verify-email`,summary:`Confirm an email address from a mailed token.`,public:!0},{method:`POST`,path:`/api/v1/auth/resend-verification`,summary:`Re-send the verification mail.`,public:!0},{method:`POST`,path:`/api/v1/auth/reset`,summary:`Begin a password reset.`,public:!0},{method:`POST`,path:`/api/v1/auth/reset/confirm`,summary:`Complete a reset with the mailed token.`,public:!0},{method:`GET`,path:`/api/v1/auth/reset/context`,summary:`Policy context a reset page needs to render.`,public:!0}]},{type:`codegroup`,caption:`sign in, then handle a step-up`,tabs:[{label:`TypeScript`,code:`const result = await client.login(email, password);

switch (result.status) {
  case 'mfa_required': {
    const code = await promptForMfaCode(result.availableMethods);
    await client.verifyMfa(result.mfaToken, code);
    break;
  }
  case 'authenticated':
    console.log(\`Authenticated as \${result.user.username}\`);
    break;
}`},{label:`Python`,code:`result = client.login(email, password)

if result.mfa_required:
    result = client.verify_mfa(result.mfa_token, totp_code)

print(result.session_id, result.expires_in)`},{label:`Rust`,code:`let result = client.login("user@acme.dev", &password).await?;

if result.mfa_required {
    client.verify_mfa("123456").await?;
}`},{label:`Go`,code:`result, err := client.Login(ctx, email, password)
if err != nil {
    return err
}

if result.MFARequired {
    // complete the challenge before the session is established
}`},{label:`Java`,code:`LoginResult result = client.login("user@acme.dev", password);

if (result.mfaRequired()) {
    result = client.verifyMfa(result.challengeToken(), "123456");
}`}]},{type:`h`,id:`sessions`,text:`Sessions end, not users`},{type:`p`,text:`Logout operates on a session, not on an identity. A user signed in on a phone and a laptop who logs out on the laptop expects the phone to stay signed in, and that is what happens. When relying parties are registered for back-channel logout, ending a session also notifies them — see [Logout & session management](#/docs/logout).`},{type:`h`,id:`rate`,text:`Rate limiting`},{type:`p`,text:"`/auth/login` is always keyed per source IP regardless of the deployment's rate-limit key mode, and stays strict under every posture preset. Register, password-reset and MFA endpoints are likewise per-IP. The reasoning — and why `client_id` keying is a fairness control rather than an abuse control — is in [Configuration](#/docs/configuration)."}]},{slug:`opaque`,section:`Authentication`,navLabel:`OPAQUE (RFC 9807)`,title:`OPAQUE — the password never reaches the server`,intro:`An augmented PAKE in which AXIAM authenticates you without ever receiving your password, and in which a stolen credential database is not offline-crackable on its own.`,blocks:[{type:`h`,id:`what`,text:`What it changes`},{type:`p`,text:`In an ordinary password login the plaintext travels to the server inside the TLS session, and the server hashes it. TLS protects it in transit, but the server *has* it — which means a TLS-terminating proxy, an accidentally verbose request log, a heap dump or a memory-scraping exploit can capture it.`},{type:`p`,text:`With **OPAQUE** ([RFC 9807](https://datatracker.ietf.org/doc/rfc9807/)), the CFRG's augmented PAKE, the client blinds the password and the server evaluates an oblivious PRF over it without learning either the input or the output. The client uses the result to open a sealed envelope containing its long-term key material, and the two sides complete an authenticated key exchange. The server never holds a plaintext password at any point in the exchange.`},{type:`table`,proseFirstCol:!0,headers:[`Exposure`,`Password + Argon2id`,`OPAQUE`],rows:[[`TLS-terminating proxy sees the password`,`Yes`,`No — it sees a blinded element`],[`Verbose request log captures it`,`Yes`,`No`],[`Server heap dump contains it`,`Yes`,`No`],[`Stolen credential database is offline-crackable`,`Yes, at the cost of the KDF`,`**No** — an attacker additionally needs the tenant's OPRF seed, encrypted at rest under a key that is not in the database`],[`Compromised AXIAM server can steal it`,`Yes`,`Yes — see the limits below`]]},{type:`h`,id:`limits`,text:`What it does not do`},{type:`warn`,text:`OPAQUE does not protect against a compromised AXIAM server. A server that wants your password can serve a login page that collects it. For **browser** clients the benefit is bounded for exactly this reason — AXIAM serves the code that performs the computation. The strong case is native SDK clients, IoT devices, and deployments sitting behind infrastructure the tenant does not control.`},{type:`h`,id:`modes`,text:`Enabling it`},{type:`p`,text:"OPAQUE is **off by default** and configured as a policy: an organization sets the baseline and a tenant may only tighten it. The relevant fields are `opaque_mode`, `opaque_suite` (default `ristretto255_sha512`) and `opaque_ksf` (default `argon2id`, with `scrypt` as the alternative rung)."},{type:`table`,headers:[`Mode`,`Meaning`],rows:[[`disabled`,`The default. Only password login is offered.`],[`optional`,`Both paths work. Users enrol an OPAQUE record as they sign in or change password; those who have not yet keep using the password path.`],[`required`,`Only OPAQUE login is accepted, tenant-wide.`]]},{type:`warn`,text:"`required` cannot be turned on safely until every user in the tenant has enrolled. A registration record must be built from the plaintext password, and a stored Argon2id hash is not invertible — so **nobody can be enrolled retroactively**. Switching to `required` early locks out every user who has no record. Run `optional` until enrolment is complete, then flip."},{type:`h`,id:`flow`,text:`The exchange`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/opaque/register/start`,summary:`Begin enrolment: server evaluates the OPRF over the blinded password.`,public:!0},{method:`POST`,path:`/api/v1/auth/opaque/login/start`,summary:`Send KE1; receive KE2 plus the KSF parameters to use.`,public:!0},{method:`POST`,path:`/api/v1/auth/opaque/login/finish`,summary:`Send KE3; the session is established.`,public:!0}]},{type:`p`,text:`Login is two round trips rather than one, because the OPRF needs the server. Two rules matter to anyone integrating it directly:`},{type:`list`,items:["**Build the key-stretching function from what the server named**, never from a local default. `login/start` returns the KSF and its parameters; using anything else produces an envelope that cannot be opened.","**A failure at `finish` is indistinguishable by design.** Wrong password, unknown account, and a server holding no record all fail identically — and nothing further may be posted after one."]},{type:`code`,caption:`login · TypeScript, via the shared WASM core`,code:`import init, { OpaqueLogin, OpaqueKsf } from '@axiam/opaque-wasm';

await init();

// 1. Blind the password and start the exchange.
const login = new OpaqueLogin(password);
const started = await postJson('/api/v1/auth/opaque/login/start', {
  org_slug, tenant_slug, username_or_email, ke1: login.ke1,
});

// 2. Build the KSF from what the SERVER named.
const ksf = started.ksf === 'argon2id'
  ? OpaqueKsf.argon2id(started.memory_kib, started.iterations, started.parallelism)
  : OpaqueKsf.scrypt(started.log_n, started.r, started.p);

// 3. Open the envelope, then finish.
const finished = login.finish(password, started.ke2, ksf);

await postJson('/api/v1/auth/opaque/login/finish', {
  opaque_session: started.opaque_session,
  ke3: finished.ke3,
});`},{type:`note`,text:"`login` is **consumed** by `finish`. Calling it twice throws rather than reusing one OPRF blind across two exchanges."},{type:`h`,id:`one-impl`,text:`One implementation, twelve callers`},{type:`p`,text:"AXIAM speaks OPAQUE from twelve places — the server, the admin console and eleven SDKs — and every one of them must agree on every byte. OPAQUE is not a protocol it is reasonable to hand-write once per language: it needs an OPRF, `hash_to_curve`, `expand_message_xmd`, an envelope construction and a three-message AKE."},{type:`p`,text:"So there is exactly one implementation, the `axiam-opaque` crate, and every client binds it: compiled directly (Rust, the server), through WebAssembly as `@axiam/opaque-wasm` (TypeScript, the admin console), or behind a C ABI (everything else). The cross-language contract is `sdks/CONTRACT.md` §23, and `sdks/opaque-test-vectors.json` is the shared fixture every SDK is tested against."},{type:`note`,text:`This replaced an SRP-6a implementation that had been written eleven times, because SRP is modular arithmetic and every language has a bignum. That produced a bundled Montgomery modular exponentiation in one SDK and another that reported itself unavailable without a specific extension. OPAQUE removes the offline attack rather than repricing it, and one audited implementation removes the drift.`},{type:`h`,id:`keys`,text:`The two keys it needs`},{type:`table`,headers:[`Variable`,`What it protects`,`Cost of losing it`],rows:[[`AXIAM__AUTH__OPAQUE_SETUP_KEY`,`Encrypts each tenant's OPRF seed and AKE keypair at rest.`,`**A password reset for every user in every tenant.** Back this up.`],[`AXIAM__AUTH__OPAQUE_SESSION_KEY`,`Seals in-flight exchange state (a 120-second window).`,`In-flight logins fail and are retried. Deliberately separate from the setup key so it is cheap to rotate.`]]},{type:`p`,text:"Both are required whenever any organization or tenant has `opaque_mode` other than `disabled`. Where they come from is pluggable — environment, a mounted file, or HashiCorp Vault. See [Secrets & Vault](#/docs/secrets)."}]},{slug:`mfa`,section:`Authentication`,navLabel:`Multi-factor auth`,title:`Multi-factor authentication`,intro:`TOTP as the baseline second factor, enrolled by the user or enforced by policy, with secrets encrypted at rest.`,blocks:[{type:`h`,id:`totp`,text:`TOTP`},{type:`p`,text:`AXIAM's baseline second factor is TOTP (RFC 6238) — the six-digit rolling code produced by an authenticator app. Enrolment returns a secret and a provisioning URI for a QR code; the user confirms with one generated code, which proves their clock and the secret agree before the factor is armed.`},{type:`p`,text:"Stored TOTP secrets are encrypted at rest with AES-256-GCM under `AXIAM__AUTH__MFA_ENCRYPTION_KEY`. Losing that key makes every enrolled secret undecryptable and forces every user to re-enrol."},{type:`h`,id:`endpoints`,text:`Endpoints`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/mfa/setup/enroll`,summary:`Begin enrolment during initial account setup.`,public:!0},{method:`POST`,path:`/api/v1/auth/mfa/setup/confirm`,summary:`Confirm setup-time enrolment with a generated code.`,public:!0},{method:`POST`,path:`/api/v1/auth/mfa/enroll`,summary:`Enrol an additional factor on an authenticated account.`},{method:`POST`,path:`/api/v1/auth/mfa/confirm`,summary:`Confirm that enrolment.`},{method:`POST`,path:`/api/v1/auth/mfa/verify`,summary:`Answer an MFA challenge during login.`,public:!0},{method:`GET`,path:`/api/v1/users/{user_id}/mfa-methods`,summary:`List a user's enrolled factors.`},{method:`DELETE`,path:`/api/v1/users/{user_id}/mfa-methods/{method_id}`,summary:`Remove one factor.`},{method:`POST`,path:`/api/v1/users/{user_id}/reset-mfa`,summary:`Administratively clear every factor on an account.`}]},{type:`h`,id:`stepup`,text:`The step-up in a login`},{type:`p`,text:"When a factor is required, `POST /auth/login` does not establish a session. It returns an MFA challenge, and the SDK surfaces it as a flag rather than an exception — the session is established only once `/auth/mfa/verify` succeeds. The challenge has its own lifetime, `mfa_challenge_lifetime_secs`, set per tenant."},{type:`h`,id:`policy`,text:`Enforcing it`},{type:`p`,text:"`mfa_enforced` is a policy field on the settings baseline: set it at the organization and every tenant inherits it, or tighten it on one tenant. As with every setting, a tenant may make the posture stricter than the organization's baseline but never looser."},{type:`warn`,text:`Enforce MFA on administrative accounts before anything else. A super-admin holds every permission in the tenant, including the ones that would let an attacker remove the audit evidence of what they did with it.`},{type:`note`,text:`TOTP is the baseline, not the ceiling. **Passkeys and security keys are phishing-resistant in a way a typed code is not** — a code can be entered into a lookalike page and forwarded to the real one inside its window. See [Passkeys & WebAuthn](#/docs/passkeys).`}]},{slug:`passkeys`,section:`Authentication`,navLabel:`Passkeys & WebAuthn`,title:`Passkeys & WebAuthn`,intro:`Phishing-resistant authentication bound to your origin, with an attestation policy for deployments that need to say which authenticators are acceptable.`,blocks:[{type:`h`,id:`why`,text:`Why they are different`},{type:`p`,text:`A **passkey** is a credential held by a device the user already has — a laptop's fingerprint sensor, a phone's face unlock, a platform PIN. A **security key** is the same construction on a removable device. Both are WebAuthn credentials, and both are phishing-resistant for a structural reason: the credential is cryptographically bound to the exact origin that created it, so a lookalike page cannot use it *even if the user is completely fooled*.`},{type:`p`,text:`A one-time code cannot make that promise. It can be typed into a fake page and forwarded to the real one inside its thirty-second window, which is precisely how modern credential-phishing kits work.`},{type:`h`,id:`flows`,text:`Registration and authentication`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/webauthn/register/start`,summary:`Get creation options for a new credential.`},{method:`POST`,path:`/api/v1/auth/webauthn/register/finish`,summary:`Submit the attestation; the credential is stored.`},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/start`,summary:`Get request options for a sign-in ceremony.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/finish`,summary:`Submit the assertion; the session is established.`,public:!0}]},{type:`p`,text:"Both ceremonies are the standard WebAuthn two-step. The browser does the work — your code passes the options through to `navigator.credentials` and posts the result back."},{type:`code`,caption:`registration · browser`,code:`// 1. Ask the server for creation options.
const options = await postJson('/api/v1/auth/webauthn/register/start', {});

// 2. Let the browser and authenticator do the ceremony.
const credential = await navigator.credentials.create({ publicKey: options });

// 3. Hand the attestation back.
await postJson('/api/v1/auth/webauthn/register/finish', credential);`},{type:`note`,text:`Tell users to register **at least two**, on different devices. A passkey lives on the device that created it; if that device is the only factor and it is lost, the account is locked out. Naming them at registration matters too — "Passkey 2" is not a name anybody can act on when deciding which one to revoke.`},{type:`h`,id:`attestation`,text:`Attestation policy`},{type:`p`,text:`Most deployments should accept any authenticator the user has. Some — regulated environments, or a fleet standardised on issued hardware — need to say which models are acceptable, and prove afterwards that the rule held. That is what the per-tenant attestation policy is for.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/tenants/{tenant_id}/webauthn/attestation-policy`,summary:`Read the tenant's policy.`},{method:`PUT`,path:`/api/v1/tenants/{tenant_id}/webauthn/attestation-policy`,summary:`Set it.`},{method:`GET`,path:`/api/v1/tenants/{tenant_id}/webauthn/compliance-report`,summary:`Which registered credentials satisfy the current policy.`},{method:`GET`,path:`/api/v1/mds/status`,summary:`FIDO Metadata Service snapshot status.`},{method:`POST`,path:`/api/v1/mds/refresh`,summary:`Refresh the MDS trust anchors.`}]},{type:`p`,text:`Authenticator models are identified against the FIDO Metadata Service (MDS3). The snapshot is refreshable on demand, which is also the air-gap story: an operator with no outbound access refreshes from a fetched blob rather than from the network.`},{type:`warn`,text:"**Passkeys frequently carry no meaningful attestation.** Platform authenticators commonly attest as `none` by design, for user-privacy reasons. A strict attestation policy therefore tends to exclude exactly the credentials most users have, and turns into a security-key-only policy in practice. Decide which of those two things you actually want before setting it."},{type:`h`,id:`signin`,text:`How users sign in`},{type:`list`,items:[`**Autofill** — tapping the username field offers a saved passkey alongside saved passwords.`,`**An explicit button** — "Sign in with a passkey" below the password field, for users whose browser did not offer autofill.`,`**As a second factor** — after a password, where policy requires a step-up rather than a replacement.`]}]},{slug:`federation`,section:`Authentication`,navLabel:`Federation & SSO`,title:`Federation — SAML & OIDC`,intro:`Delegate authentication to an external identity provider, and map what it asserts onto tenant identities, groups and roles.`,blocks:[{type:`h`,id:`why`,text:`Why federate`},{type:`p`,text:`Federation lets a user from an external identity provider sign in to an AXIAM tenant without a separate local credential. AXIAM acts as a **SAML service provider** and as an **OIDC relying party**, so it slots into existing enterprise SSO rather than asking an organization to move its directory.`},{type:`p`,text:`A federated identity is an ordinary user inside the tenant. Roles, permissions, audit and MFA policy apply to it exactly as they do to a locally-credentialed one — the difference is only where the authentication happened.`},{type:`h`,id:`saml`,text:`SAML`},{type:`p`,text:`As a service provider, AXIAM consumes assertions from an external IdP, validates the signature against the configured certificate, and maps assertion attributes onto tenant identities, group membership and roles. Attribute mapping is per federation configuration.`},{type:`note`,text:"SAML support is behind a default-on `saml` build feature that links `libxml`. A build made with `--no-default-features` — which is what CI's *Build (SAML off)* job produces — has every other capability and no SAML service provider."},{type:`h`,id:`oidc`,text:`OIDC federation`},{type:`p`,text:"As a relying party, AXIAM delegates authentication upstream and then issues its own tenant-scoped session. Claim mapping is configured per tenant, and the upstream client secret is encrypted at rest with AES-256-GCM under `AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY`."},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/federation/oidc/start`,summary:`Begin an upstream authentication.`,public:!0},{method:`POST`,path:`/api/v1/auth/federation/oidc/callback`,summary:`Complete it and establish the local session.`,public:!0},{method:`GET`,path:`/api/v1/federation-configs`,summary:`List configured identity providers.`},{method:`POST`,path:`/api/v1/federation-configs`,summary:`Register one.`},{method:`PUT`,path:`/api/v1/federation-configs/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/federation-configs/{id}`,summary:`Remove it.`},{method:`GET`,path:`/api/v1/federation-links/user/{user_id}`,summary:`Which upstream identities are linked to a user.`},{method:`DELETE`,path:`/api/v1/federation-links/{id}`,summary:`Unlink one.`}]},{type:`h`,id:`links`,text:`Linking and unlinking`},{type:`p`,text:`A **federation link** records that a given upstream subject corresponds to a given local user. Links are visible and removable per user, which is what makes offboarding an IdP tractable: unlink, and the local account remains with whatever other credential it holds — rather than becoming an orphan nobody can sign in to.`},{type:`h`,id:`provisioning`,text:`Federation is not provisioning`},{type:`p`,text:`Federation answers *who is this person* at sign-in time. It does not create, update or deactivate accounts ahead of time, and it does not remove one when someone leaves. For that, use **SCIM 2.0** — Okta, Entra ID and any other SCIM-compliant IdP can drive user and group lifecycle directly. See [SCIM provisioning](#/docs/scim).`}]},{slug:`service-accounts`,section:`Authentication`,navLabel:`Service accounts`,title:`Service accounts & machine identity`,intro:`Non-human principals for service-to-service access — authenticated by a client secret or by a bound X.509 certificate, and subject to exactly the same authorization engine as a user.`,blocks:[{type:`h`,id:`what`,text:`What a service account is`},{type:`p`,text:`A service account is a tenant-scoped identity for an automated caller. It holds roles and permissions like a user, appears in the audit log like a user, and is denied by a deny grant like a user. What it does not have is a password, an email address or an interactive login.`},{type:`p`,text:`Use one wherever a human account would otherwise be shared between people or embedded in a deployment — a CI pipeline, a batch job, a backend service calling AXIAM on its own behalf. The audit trail then names the *system* that acted rather than whichever employee's credentials it was using.`},{type:`h`,id:`endpoints`,text:`Managing them`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/service-accounts`,summary:`List the tenant's service accounts.`},{method:`POST`,path:`/api/v1/service-accounts`,summary:`Create one. The secret is returned exactly once.`},{method:`GET`,path:`/api/v1/service-accounts/{sa_id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/service-accounts/{sa_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/service-accounts/{sa_id}`,summary:`Delete it.`},{method:`POST`,path:`/api/v1/service-accounts/{sa_id}/rotate-secret`,summary:`Issue a new secret. Returned once.`},{method:`POST`,path:`/api/v1/service-accounts/{sa_id}/bind-certificate`,summary:`Bind an X.509 certificate for mTLS authentication.`}]},{type:`warn`,text:`A service-account secret is shown once, at creation or rotation, and is never retrievable afterwards. If it is lost, rotate — there is no recovery path, by design.`},{type:`h`,id:`auth`,text:`Authenticating as one`},{type:`p`,text:`The ordinary path is the OAuth2 **client credentials** grant: the service account exchanges its identifier and secret for a short-lived access token, then uses that token like any other caller.`},{type:`code`,caption:`client credentials`,code:`curl -X POST 'https://iam.acme.dev/oauth2/token?tenant_id=<uuid>' \\
  -d grant_type=client_credentials \\
  -d client_id="$SA_CLIENT_ID" \\
  -d client_secret="$SA_CLIENT_SECRET" \\
  -d scope='read:orders'`},{type:`h`,id:`mtls`,text:`Certificates instead of secrets`},{type:`p`,text:"A shared secret has to be distributed, stored and rotated, and it is bearer material — whoever holds it is the service. Binding an X.509 certificate to the service account replaces that with a private key that never leaves the machine holding it, authenticated through mutual TLS terminated inside `axiam-server` rather than asserted by a proxy header."},{type:`p`,text:`This is the recommended shape for IoT fleets and for anything in a service mesh that already has a certificate. Combined with **certificate-bound access tokens**, it also means a stolen token is useless without the corresponding private key — see [FAPI 2.0 & mTLS clients](#/docs/fapi2) and [PKI & certificates](#/docs/pki).`},{type:`note`,text:"Client-certificate verification is native: `AXIAM__SERVER__TLS__CLIENT_AUTH` (`off` | `optional` | `required`) with a trust bundle at `AXIAM__SERVER__TLS__CLIENT_CA_PATH`. No proxy-asserted identity header is in the trusted path, and a misconfigured mTLS server fails fast at startup rather than serving without verification."}]}],s=[{slug:`authz`,section:`Authorization`,navLabel:`Authorization engine`,title:`The authorization engine`,intro:`Role-based, default-deny, evaluated over a cascading resource hierarchy — and reachable over REST, gRPC or AMQP with the same answer from each.`,blocks:[{type:`h`,id:`model`,text:`The evaluation model`},{type:`p`,text:`Every check asks one question: *may this principal perform this action on this resource, in this scope?* The answer is computed from the grants attached to the roles the principal holds — directly, through a group, or inherited from an ancestor resource.`},{type:`p`,text:`Precedence is short and worth memorising, because everything else follows from it:`},{type:`steps`,steps:[{title:`Default deny`,body:"Nothing is permitted unless something permits it. An unknown action on an unknown resource is denied with `no_grant`, not with an error."},{title:`An applicable allow grant permits the action`,body:`Grants apply through directly assigned roles, group-inherited roles, and roles assigned on any ancestor of the resource.`},{title:`An applicable deny grant refuses it — and beats every allow`,body:`Wherever either sits in the hierarchy. Deny wins; it is not "most specific wins". See [Deny grants & precedence](#/docs/deny).`}]},{type:`h`,id:`hierarchy`,text:`Hierarchy & cascade`},{type:`p`,text:`Resources form a tree. A role assigned on a parent cascades to every descendant, so you grant broadly at the top and refine below rather than enumerating every leaf. A resource's ancestors and children are both queryable, which is what makes an admin UI able to explain *why* a decision came out the way it did.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/resources/{resource_id}/ancestors`,summary:`The chain a grant can cascade down from.`},{method:`GET`,path:`/api/v1/resources/{resource_id}/children`,summary:`The subtree a grant here would reach.`}]},{type:`h`,id:`scopes`,text:`Scopes`},{type:`p`,text:"A **scope** narrows a permission to part of a resource — `read` on a customer record limited to the `billing` scope, say, rather than to everything the record contains. A grant carries a list of scope ids; an empty list means *all scopes*, including unscoped checks."},{type:`warn`,text:`Read that wildcard rule carefully in the deny direction: a **resource-level deny with no scopes is stronger than a scoped one**, because it matches every request for that action regardless of what scope the request names.`},{type:`h`,id:`checks`,text:`Making a check`},{type:`p`,text:"Every SDK exposes the same `can(action, resource)` call — a plain yes/no — alongside a fuller form that also returns *why* a refusal was refused (`no_grant` versus `denied_by_rule`), and a batched form for when one request needs several decisions. The transport underneath is a deployment choice, not a semantic one."},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/authz/check`,summary:`One decision.`},{method:`POST`,path:`/api/v1/authz/check/batch`,summary:`Several decisions in one round trip.`}]},{type:`codegroup`,caption:`single and batched checks`,tabs:[{label:`TypeScript`,code:`// one decision
const allowed = await client.can('read', 'doc:1');

// several in one round trip — results preserve input order
const decisions = await client.batchCheck([
  { action: 'read', resourceId: 'doc:1' },
  { action: 'write', resourceId: 'doc:1' },
]);`},{label:`Python`,code:`allowed = client.can("read", resource_id)

decisions = client.batch_check([
    AccessCheck(action="read", resource_id=resource_id),
    AccessCheck(action="write", resource_id=resource_id),
])`},{label:`Rust`,code:'// `can` answers yes/no; `check_access` returns the full decision,\n// including the reason a refusal was refused.\nlet allowed = client.can("read", resource_id, None).await?;\nlet decision = client.check_access("read", resource_id, None).await?;'},{label:`Go`,code:`allowed, err := client.Can(ctx, "read", resourceID)

allowed, reason, err := client.CheckAccess(ctx, "read", resourceID)

results, err := client.BatchCheck(ctx, []axiam.AccessCheck{
    {Action: "read", ResourceID: resourceID},
    {Action: "write", ResourceID: resourceID},
})`},{label:`Java`,code:`boolean allowed = client.can("read", "doc:1");`}]},{type:`h`,id:`guards`,text:`Declarative guards`},{type:`p`,text:"Calling `can()` by hand at the top of a handler works, and is easy to forget. Every SDK also ships the declarative form idiomatic to its language — a middleware, a dependency, an attribute macro or an annotation — so the requirement sits on the route rather than buried in its body."},{type:`codegroup`,caption:`the same guard, four ways`,tabs:[{label:`TypeScript`,code:`import { requireAccess, fromParam } from 'axiam-sdk/middleware';

app.get(
  '/documents/:id',
  requireAccess(authzSession, 'read', fromParam('id')),
  (req, res) => res.json({ documentId: req.params.id }),
);`},{label:`Python`,code:`from axiam_sdk.fastapi import AxiamUser, require_access

require_doc_read = require_access(
    verifier, "acme", authz_client, "documents:read", resource_param="doc_id"
)


@app.get("/docs/{doc_id}")
async def get_doc(doc_id: str, user: AxiamUser = Depends(require_doc_read)):
    return {"message": f"user {user.user_id} may read document {doc_id}"}`},{label:`Rust`,code:`use axiam_sdk::{require_access, require_auth};
use axiam_sdk::middleware::AxiamUser;

#[require_access(action = "read", resource_param = "id")]
async fn get_document(user: AxiamUser) -> String {
    format!("user {} may read this document", user.user_id)
}`},{label:`Java`,code:`@RestController
public class DocumentController {

    @AxiamRequireAccess(action = "read", resourceParam = "id")
    @GetMapping("/documents/{id}")
    public String read(@PathVariable("id") String id) {
        return "document " + id;
    }
}`}]},{type:`note`,text:`Each of these runs strictly **after** authentication, and issues the check for the *request's* authenticated caller — never for the application's own, usually service-account, session. That distinction is the one thing to get right when writing a guard by hand, and it is why using the shipped one is worth it.`},{type:`warn`,text:"Every SDK also offers a `requireRole`-style helper. It is a **local, no-round-trip** check against the verified identity's roles: cheaper, coarser, and **not** a substitute for the authoritative resource-level check. A role check cannot see a deny grant on the resource."},{type:`h`,id:`perf`,text:`The hot path`},{type:`p`,text:`An authorization check is the most frequently executed operation in the product, so the evaluation is a **single pass over grants that were already fetched**. Denies live on the same edge as allows and arrive in the same batched read — there is no deny-specific query, and a tenant with no deny rules pays nothing for the feature existing.`},{type:`p`,text:`Deny short-circuits and allows are only remembered, so the result does not depend on the order grants come back in: a rule set has one answer, not one answer per query plan.`},{type:`p`,text:`Where checks dominate your traffic, two levers matter. **Database CPU** is the main ceiling — measured gains of roughly 90% from a second pair of database cores. The optional **decision cache** sits on top: transformative on gRPC checks, marginal on REST ones, because REST's per-request session-cookie validation is a database read the cache does not cover. Both are covered in [Configuration](#/docs/configuration).`},{type:`note`,text:`Enabling the decision cache changes performance only, never the decision returned. Every access-narrowing mutation invalidates the affected entries immediately, so a revocation cannot leave a stale allow behind; the TTL is a bounded-staleness backstop, not the invalidation mechanism.`}]},{slug:`rbac`,section:`Authorization`,navLabel:`Roles & resources`,title:`Roles, permissions, resources & groups`,intro:`The entities the engine evaluates over, how they relate, and the API for managing each.`,blocks:[{type:`h`,id:`shape`,text:`How they fit together`},{type:`code`,caption:`the access graph`,code:`User ──────┐
           ├──> Role ──> Permission (action + effect [+ scopes])
Group ─────┘              │
  ▲                       └──> applies at a Resource
  └─ users inherit every role assigned to their groups

Resource tree:   /fleet
                   └── /fleet/decommissioned
                         └── /fleet/decommissioned/unit-7
                 a role assigned on /fleet reaches all three`},{type:`h`,id:`permissions`,text:`Permissions`},{type:`p`,text:"A permission is an action name plus an effect. AXIAM seeds **113 built-in permissions across 25 families** into every tenant at bootstrap — `users:*`, `roles:*`, `resources:*`, `oauth2_clients:*`, `certificates:*`, `ca_certificates:*`, `audit_logs:*`, `federation:*`, `webhooks:*`, `reactors:*`, `scim_tokens:*`, `gdpr:*` and the rest. These are the actions the REST API's own route guards check against, so an administrator's authority over AXIAM is expressed in the same model as an application's authority over its own resources."},{type:`p`,text:`You are not limited to them: define your own action names for your application's domain and grant them the same way.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/permissions`,summary:`List permissions in the tenant.`},{method:`POST`,path:`/api/v1/permissions`,summary:`Define one.`},{method:`GET`,path:`/api/v1/permissions/{permission_id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/permissions/{permission_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/permissions/{permission_id}`,summary:`Delete it.`}]},{type:`h`,id:`roles`,text:`Roles`},{type:`p`,text:"A role is a named collection of permissions, assignable to users and to groups, and either global or bound to a specific resource. Three are seeded at bootstrap: `super-admin` (every permission), `admin` (all entity CRUD) and `viewer` (list and get only)."},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/roles`,summary:`List roles.`},{method:`POST`,path:`/api/v1/roles`,summary:`Create one.`},{method:`PUT`,path:`/api/v1/roles/{role_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/roles/{role_id}`,summary:`Delete it.`},{method:`POST`,path:`/api/v1/roles/{role_id}/permissions`,summary:`Grant a permission to the role.`},{method:`DELETE`,path:`/api/v1/roles/{role_id}/permissions/{permission_id}`,summary:`Revoke it.`},{method:`POST`,path:`/api/v1/roles/{role_id}/users`,summary:`Assign the role to a user.`},{method:`DELETE`,path:`/api/v1/roles/{role_id}/users/{user_id}`,summary:`Unassign it.`},{method:`POST`,path:`/api/v1/roles/{role_id}/groups`,summary:`Assign the role to a group.`},{method:`DELETE`,path:`/api/v1/roles/{role_id}/groups/{group_id}`,summary:`Unassign it.`}]},{type:`h`,id:`groups`,text:`Groups`},{type:`p`,text:`A group is a named collection of users, and roles assigned to it are inherited by every member. Groups are how you avoid per-user grants — and how you make offboarding a single operation rather than an audit.`},{type:`note`,text:`Group membership inherits **denies** exactly as it inherits allows. A deny reached through a group beats an allow assigned directly to the user.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/groups`,summary:`List groups.`},{method:`POST`,path:`/api/v1/groups`,summary:`Create one.`},{method:`PUT`,path:`/api/v1/groups/{group_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/groups/{group_id}`,summary:`Delete it.`},{method:`GET`,path:`/api/v1/groups/{group_id}/members`,summary:`List members.`},{method:`POST`,path:`/api/v1/groups/{group_id}/members`,summary:`Add a member.`},{method:`DELETE`,path:`/api/v1/groups/{group_id}/members/{user_id}`,summary:`Remove one.`}]},{type:`h`,id:`resources`,text:`Resources & scopes`},{type:`p`,text:`Resources are the things being protected, arranged in a tree. Model them to match how authority actually delegates in your domain — an organizational unit, a project, a device fleet — because that shape is what a grant cascades along.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/resources`,summary:`List resources.`},{method:`POST`,path:`/api/v1/resources`,summary:`Create one, optionally under a parent.`},{method:`PUT`,path:`/api/v1/resources/{resource_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/resources/{resource_id}`,summary:`Delete it.`},{method:`GET`,path:`/api/v1/resources/{resource_id}/scopes`,summary:`List its scopes.`},{method:`POST`,path:`/api/v1/resources/{resource_id}/scopes`,summary:`Define a scope on it.`},{method:`PUT`,path:`/api/v1/resources/{resource_id}/scopes/{scope_id}`,summary:`Update a scope.`},{method:`DELETE`,path:`/api/v1/resources/{resource_id}/scopes/{scope_id}`,summary:`Delete one.`}]},{type:`h`,id:`design`,text:`Designing a role model that survives`},{type:`list`,items:[`**Grant to groups, not to users.** A per-user grant is invisible in every summary view and is the thing that outlives an employee's departure.`,`**Grant high in the tree, refine low.** Enumerating leaves is a model that has to be maintained forever as the tree grows.`,`**Use deny for carve-outs, and keep them narrow.** A deny cannot be undone by an allow, which is its value and its cost — you cannot express "deny the subtree except this leaf".`,"**Name actions after what they permit, not after who does them.** `invoices:approve` survives a reorganisation; `finance-team-action` does not.",`**Prefer scopes to resource explosion.** If you are creating one resource per field, you probably want one resource with scopes.`]}]},{slug:`deny`,section:`Authorization`,navLabel:`Deny grants`,title:`Deny grants & precedence`,intro:`An explicit deny refuses regardless of what else allows it — at any depth of the hierarchy and at equal specificity. This page is the precedence table, and the reasoning behind choosing that rule.`,blocks:[{type:`h`,id:`rule`,text:`The rule`},{type:`p`,text:"A permission grant carries an `effect` of `allow` or `deny`. Evaluation is: **default deny → an applicable allow permits → an applicable deny refuses and beats every allow.**"},{type:`p`,text:`Deny wins. Always. It is not *most specific wins*, and the difference is the whole design.`},{type:`h`,id:`table`,text:`Worked precedence table`},{type:`p`,text:"Resources: `/fleet` → `/fleet/decommissioned` → `/fleet/decommissioned/unit-7`. Every check below is on the leaf."},{type:`table`,proseFirstCol:!0,headers:[`Rules`,`Check`,`Result`,`Why`],rows:[["allow `read` on `/fleet`",`read`,`**allow**`,`An allow cascades to descendants.`],[`*(nothing)*`,`read`,"**deny** (`no_grant`)",`Default deny.`],["allow `read` on `/fleet`, deny `read` on `/fleet/decommissioned`",`read`,"**deny** (`denied_by_rule`)",`A deny cascades down and beats the ancestor allow.`],["deny `read` on `/fleet`, allow `read` on `/fleet/decommissioned`",`read`,"**deny** (`denied_by_rule`)",`**A child allow does not override an inherited deny.** This is the row to read twice.`],["allow `read` on `/fleet`, deny `write` on `/fleet`",`read`,`**allow**`,"Denies are per action; `write` says nothing about `read`."],[`allow and deny of the same action on the same node`,`read`,"**deny** (`denied_by_rule`)",`Deny wins at equal specificity too — there is no tie to break.`],[`allow via a directly assigned role, deny via a group-inherited role`,`read`,"**deny** (`denied_by_rule`)",`Denies inherit through groups exactly as allows do.`],[`allow via a global role, deny on a resource-scoped role`,`read`,"**deny** (`denied_by_rule`)",`Global versus resource-scoped changes applicability, not precedence.`]]},{type:`h`,id:`scopes`,text:`Scope interaction`},{type:`table`,proseFirstCol:!0,headers:[`Deny rule`,`Effect`],rows:[["deny `read`, **no scopes** (wildcard)","Masks `read` entirely on that node and its descendants — every scope, and unscoped checks too."],["deny `read`, scopes = [`pii`]","Masks only `read` + `pii`. `read` + `billing` and unscoped `read` are unaffected."]]},{type:`warn`,text:`A resource-level deny is *stronger* than a scope-level one, because it matches every request for that action regardless of the scope named. If you meant to carve out one scope, say so — an empty scope list is a wildcard, not a default.`},{type:`h`,id:`why`,text:`Why deny-override and not most-specific-wins`},{type:`p`,text:`Most-specific-wins reads as more expressive, and it is a security footgun. Under it the meaning of a deny depends on where every *other* rule sits, which produces three properties nobody wants:`},{type:`list`,items:[`Adding an allow deeper in the tree silently re-opens something a deny closed.`,`A reviewer cannot answer "is X denied?" by looking at the deny — they must enumerate every rule that might out-specify it.`,`Moving a resource in the hierarchy can change access without any rule changing.`]},{type:`p`,text:`Deny-override buys one property that is worth more than the expressiveness: **adding a deny can never widen access, and can never be undone by adding allows.** That is checkable, and it is asserted as a property test rather than left as a claim.`},{type:`note`,text:`The cost is real and worth stating: you cannot express "deny the subtree, except this one leaf". The answer is to narrow the deny, not to widen the allow.`},{type:`h`,id:`using`,text:`Using deny well`},{type:`list`,items:[`**Reach for deny for carve-outs and containment**, not as the everyday tool. A model where most rules are denies is a model whose allows are too broad.`,`**Keep each deny as narrow as the thing you are actually excluding** — the right action, the right scope, the lowest node that covers it.`,`**Denies are excellent for incident response.** One deny on a compromised principal's role closes access across the whole tree immediately, and no allow anywhere can reopen it.`,`**Expect surprise on row 4.** Somebody will eventually add an allow below a deny and file a bug. It is working correctly; that is the property.`]}]},{slug:`uma`,section:`Authorization`,navLabel:`UMA 2.0`,title:`UMA 2.0 — protecting resources you do not own`,intro:`A resource server describes what a request would require; AXIAM decides whether the requesting party may have it. The resource server never decides.`,blocks:[{type:`h`,id:`problem`,text:`The problem it solves`},{type:`p`,text:`A service guards resources it does not own. A caller arrives without the authority to touch them, and the service needs a way to say *what the caller would need* — precisely, and without becoming the authority itself.`},{type:`p`,text:`User-Managed Access is that mechanism. The guarding service — the **resource server** — registers its resources, asks AXIAM to mint a **permission ticket** describing what a given request requires, and the ticket is exchanged for a **Requesting Party Token** (RPT) that says the authorization engine agreed.`},{type:`note`,text:`The rule everything here serves: **the resource server never decides.** It describes what it needs; AXIAM decides, using the same RBAC check a live request would run. That is why a deny grant vetoes an RPT exactly as it vetoes anything else — it *is* the same check.`},{type:`h`,id:`mapping`,text:`What maps onto what`},{type:`table`,proseFirstCol:!0,headers:[`UMA`,`AXIAM`,`Note`],rows:[["resource set `_id`",`the resource id`,`**The same id** — there is no parallel resource store.`],["resource set `name` / `type`","`resource.name` / `resource.resource_type`",``],["`resource_scopes`","`Scope` rows on that resource",`The allow-list of names a resource server may ask for.`],[`a resource scope, when evaluated`,`the AXIAM **action**`,"`view` on the resource, not a sub-resource scope."],[`PAT`,"an access token with the `uma_protection` scope",`An ordinary client-credentials token.`],[`RPT`,"an access token with a `permissions` claim",`An ordinary token; the claim is what makes it an RPT.`]]},{type:`p`,text:`A resource registered through UMA is an ordinary AXIAM resource. It appears in the admin console, role assignments cascade through it, and the engine already understands it. The only thing marking it as UMA-registered is a read-only provenance field.`},{type:`h`,id:`endpoints`,text:`The Protection API`},{type:`api`,endpoints:[{method:`GET`,path:`/.well-known/uma2-configuration`,summary:`Discovery — token, introspection, permission and registration endpoints.`,public:!0},{method:`GET`,path:`/uma2/rreg/resource_set`,summary:`List registered resource sets.`},{method:`POST`,path:`/uma2/rreg/resource_set`,summary:`Register one.`},{method:`GET`,path:`/uma2/rreg/resource_set/{id}`,summary:`Read one.`},{method:`PUT`,path:`/uma2/rreg/resource_set/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/uma2/rreg/resource_set/{id}`,summary:`Deregister it.`},{method:`POST`,path:`/uma2/perm`,summary:`Mint a permission ticket for a request that lacked authority.`}]},{type:`h`,id:`flow`,text:`The flow`},{type:`steps`,steps:[{title:`Register the resource`,body:"Once, at deployment or on first use. The resource server holds a PAT — an ordinary client-credentials token carrying the `uma_protection` scope."},{title:`A caller arrives without authority`,body:`The resource server does not guess and does not decide. It asks AXIAM for a permission ticket naming the resource and the scopes this request would need.`,code:`POST /uma2/perm
{ "resource_id": "<id>", "resource_scopes": ["view"] }`},{title:`Return the ticket to the caller`,body:'As an RFC 7235 `WWW-Authenticate: UMA realm=..., as_uri=..., ticket=...` challenge — the standard way of saying "here is what you would need, and here is who can grant it".'},{title:`The caller exchanges the ticket for an RPT`,body:`At the token endpoint, using the UMA ticket grant. AXIAM runs the ordinary RBAC check for that requesting party. A deny grant refuses here exactly as it would refuse a live request.`},{title:`The caller retries with the RPT`,body:"The resource server validates it — introspection, or offline verification against the tenant JWKS — and reads the `permissions` claim."}]},{type:`note`,text:"A `403` from the Protection API at `/uma2/*` is an ordinary authorization refusal and maps to an authorization error in the SDKs, not to an OAuth2 protocol error. A refusal from the ticket grant at the token endpoint is an OAuth2 protocol error and carries an `error` field to dispatch on — see [Error reference](#/docs/errors)."}]}],c=[{slug:`oauth2`,section:`OAuth2 & OIDC`,navLabel:`Authorization server`,title:`OAuth2 & OpenID Connect`,intro:`AXIAM is a complete OAuth2 authorization server and OpenID Connect provider — discovery, JWKS, five grant types, introspection, revocation and userinfo.`,blocks:[{type:`h`,id:`grants`,text:`Supported grants`},{type:`table`,proseFirstCol:!0,headers:[`Grant`,`Use it for`,`Notes`],rows:[[`Authorization Code + PKCE`,`Browser and mobile applications.`,`PKCE is the expected shape for public clients; pushed authorization requests (PAR) can be required per client.`],[`Client Credentials`,`Machine-to-machine access with a service account or confidential client.`,`Authenticate with a secret, or with mutual TLS instead — see [FAPI 2.0 & mTLS clients](#/docs/fapi2).`],[`Refresh Token`,`Extending a session without re-authenticating.`,`Opaque, server-stored, single-use, rotating on every refresh.`],[`Device Authorization`,`Televisions, CLIs, headless commissioning — anything with no browser.`,`See [Device authorization grant](#/docs/device-flow).`],[`Token Exchange`,`A service calling another service on a user's behalf.`,`Narrowing only, always. See [Token exchange](#/docs/token-exchange).`]]},{type:`h`,id:`endpoints`,text:`Endpoints`},{type:`api`,endpoints:[{method:`GET`,path:`/.well-known/openid-configuration`,summary:`OIDC discovery document.`,public:!0},{method:`GET`,path:`/oauth2/jwks`,summary:`Signing keys, cached with an ETag and Cache-Control.`,public:!0},{method:`GET`,path:`/oauth2/authorize`,summary:`Authorization endpoint. Tenant is derived from client_id.`,public:!0},{method:`POST`,path:`/oauth2/par`,summary:`Pushed authorization request (RFC 9126).`,public:!0},{method:`POST`,path:`/oauth2/token`,summary:`Token endpoint — every grant.`,public:!0},{method:`POST`,path:`/oauth2/introspect`,summary:`Token introspection (RFC 7662).`,public:!0},{method:`POST`,path:`/oauth2/revoke`,summary:`Token revocation (RFC 7009).`,public:!0},{method:`GET`,path:`/oauth2/userinfo`,summary:`OIDC userinfo claims for the bearer.`,public:!0},{method:`POST`,path:`/oauth2/device_authorization`,summary:`Begin a device grant (RFC 8628).`,public:!0},{method:`GET`,path:`/oauth2/end_session`,summary:`RP-initiated logout.`,public:!0}]},{type:`h`,id:`tenant`,text:`Naming the tenant`},{type:`p`,text:`AXIAM is multi-tenant and has no default tenant, so every OAuth2 request has to say which one it means. There are two mechanisms, and which applies depends on the endpoint:`},{type:`list`,items:["**`tenant_id` as a query parameter** — required on `/oauth2/token`, `/oauth2/par` and `/oauth2/end_session`.","**Derived from `client_id`** — on `/oauth2/authorize`, because an OAuth2 client belongs to exactly one tenant."]},{type:`warn`,text:"The discovery document is **deployment-wide**, not per-tenant: one issuer, served at `/.well-known/openid-configuration`. `AXIAM__AUTH__OAUTH2_ISSUER_URL` must be an origin — a path-based issuer is rejected at startup. An off-the-shelf OIDC client that cannot add a `tenant_id` query parameter to the token endpoint therefore needs a small shim, or a per-tenant gateway route in front of AXIAM."},{type:`code`,caption:`discovery`,code:`curl -s https://iam.acme.dev/.well-known/openid-configuration | jq .

# and the keys a relying party verifies tokens with
curl -s https://iam.acme.dev/oauth2/jwks | jq .`},{type:`h`,id:`tokens`,text:`The tokens you get back`},{type:`p`,text:"Access tokens are EdDSA (Ed25519) JWTs, short-lived, and verifiable offline against the JWKS — which is the fast path for a resource server, and the one to prefer over introspecting on every request. ID tokens carry `sid`, the session identifier, which is stable across refresh. Refresh tokens are opaque and single-use."},{type:`p`,text:`Introspection exists for the cases offline verification cannot answer: whether a token has been revoked since it was issued, and what an opaque token refers to. A resource server that introspects on every request should expect to make roughly ten to twenty introspection calls per token issued — the shipped rate limits are sized for that ratio.`},{type:`h`,id:`par`,text:`Pushed authorization requests`},{type:`p`,text:"With PAR (RFC 9126) the client posts the authorization parameters to AXIAM directly and receives a `request_uri` to put in the browser redirect, instead of placing every parameter in a URL the user agent can see and modify. It can be required per client, and **is** required for any client registered under the FAPI 2.0 profile."},{type:`h`,id:`clients`,text:`Registering clients`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/oauth2-clients`,summary:`List the tenant's clients.`},{method:`POST`,path:`/api/v1/oauth2-clients`,summary:`Register one.`},{method:`GET`,path:`/api/v1/oauth2-clients/{id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/oauth2-clients/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/oauth2-clients/{id}`,summary:`Delete it.`}]},{type:`code`,caption:`a browser application`,code:`POST /api/v1/oauth2-clients
{
  "name": "acme-web",
  "redirect_uris": ["https://app.acme.dev/callback"],
  "grant_types": ["authorization_code", "refresh_token"],
  "scopes": ["openid", "profile", "email"]
}`},{type:`note`,text:`The SDKs ship OIDC relying-party helpers — the redirect, the PKCE verifier, the callback exchange and the token store — so integrating a web application is not a matter of hand-rolling the flow. See [Client SDKs](#/docs/sdks).`}]},{slug:`device-flow`,section:`OAuth2 & OIDC`,navLabel:`Device grant`,title:`Device authorization grant`,intro:`For clients that cannot show a browser or accept typed input: a television, a set-top box, a CLI on a headless machine, an IoT sensor being commissioned.`,blocks:[{type:`h`,id:`flow`,text:`The flow`},{type:`p`,text:`The device shows a short code; the user types it on a phone or laptop; the device polls until they approve. AXIAM implements the non-interactive path of RFC 8628 in full.`},{type:`code`,caption:`who talks to whom`,code:`  device                        AXIAM                    user's phone
    |                             |                            |
    |-- POST /oauth2/device_authorization -->                  |
    |<- device_code, user_code, verification_uri --            |
    |                             |                            |
  shows "go to id.acme.dev/device and enter WXYZ-1234"        |
    |                             |<-- GET /api/v1/device/verify
    |                             |--- client + scopes ------->|
    |                             |<-- POST /api/v1/device/decide
    |-- POST /oauth2/token (device_code) -->                   |
    |<- authorization_pending ----|   (repeatedly, at interval)|
    |-- POST /oauth2/token (device_code) -->                   |
    |<- access_token + refresh_token                           |`},{type:`h`,id:`start`,text:`1. Start`},{type:`code`,code:`POST /oauth2/device_authorization?tenant_id=<uuid>
Content-Type: application/x-www-form-urlencoded

client_id=oa_...&scope=openid`},{type:`code`,caption:`response`,code:`{
  "device_code": "...",
  "user_code": "WXYZ-1234",
  "verification_uri": "https://id.acme.dev/device",
  "verification_uri_complete": "https://id.acme.dev/device?user_code=WXYZ-1234",
  "expires_in": 600,
  "interval": 5
}`},{type:`note`,text:"Show `verification_uri` and `user_code` separately, and use `verification_uri_complete` for a QR code. A user who scans the code should not then have to type it."},{type:`h`,id:`approve`,text:`2. The user approves`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/device/verify`,summary:`What the verification page needs: the client and the scopes being requested.`},{method:`POST`,path:`/api/v1/device/decide`,summary:`Approve or deny the pending request.`}]},{type:`p`,text:`These two back the verification page. The user is authenticated at AXIAM in the ordinary way before deciding — the page shows *which application* is asking and *what for*, because a code typed from a screen carries no context on its own.`},{type:`h`,id:`poll`,text:`3. The device polls`},{type:`p`,text:"The device posts the device grant to the token endpoint at `interval` seconds. Every non-success answer is one of four RFC 8628 errors, and each has exactly one correct reaction:"},{type:`table`,headers:[`Answer`,`Meaning`,`What the device does`],rows:[[`authorization_pending`,`The user has not decided yet.`,`Keep polling at the same interval.`],[`slow_down`,`You are polling too fast.`,`Increase the interval, then keep polling.`],[`access_denied`,`The user refused.`,`Stop. Show that it was declined.`],[`expired_token`,`The device code expired.`,`Stop, and start a new request with a fresh code.`]]},{type:`warn`,text:"Polling faster than `interval` is what earns `slow_down`, and ignoring `slow_down` is what earns a rate-limit refusal. Treat the interval as a floor set by the server, not as a suggestion."}]},{slug:`token-exchange`,section:`OAuth2 & OIDC`,navLabel:`Token exchange`,title:`Token exchange (RFC 8693)`,intro:`A service holding a user's token needs a narrower one to call the next service — fewer scopes, a specific audience, and a record of who acted for whom.`,blocks:[{type:`h`,id:`why`,text:`Why not just forward the token`},{type:`p`,text:`A service in a mesh receives a request carrying a user's access token. To call a second service on that user's behalf it has two bad options and one good one. Forwarding the user's token verbatim is over-privileged, and the second service cannot tell the caller from the user. Using the service's own credentials has the right privileges and loses the user context entirely.`},{type:`p`,text:`Token exchange is the third option: present the token you hold, receive one that is *narrower* and that records the delegation.`},{type:`note`,text:`The rule everything below serves: **an exchange may only ever narrow.** There is no parameter, no configuration and no client grant that makes the issued token permit something the subject token did not already permit.`},{type:`h`,id:`request`,text:`The request`},{type:`code`,code:`POST /oauth2/token?tenant_id=<uuid>
Content-Type: application/x-www-form-urlencoded

grant_type=urn:ietf:params:oauth:grant-type:token-exchange
&subject_token=<jwt>
&subject_token_type=urn:ietf:params:oauth:token-type:access_token
&actor_token=<jwt>                    # optional — presence selects delegation
&actor_token_type=urn:ietf:params:oauth:token-type:access_token
&scope=read:orders write:orders       # optional — defaults to the subject's scopes
&audience=https://orders.internal     # optional
&resource=https://orders.internal/v1  # optional
&client_id=oa_...&client_secret=...`},{type:`table`,headers:[`Parameter`,`Required`,`Notes`],rows:[[`grant_type`,`yes`,"`urn:ietf:params:oauth:grant-type:token-exchange`"],[`subject_token`,`yes`,`The token being exchanged. AXIAM-issued access tokens only.`],[`subject_token_type`,`yes`,"`urn:ietf:params:oauth:token-type:access_token`"],[`actor_token`,`no`,`Present ⇒ **delegation**. Absent ⇒ **impersonation**.`],[`actor_token_type`,`with actor_token`,`Same value as above.`],[`scope`,`no`,`Must be a subset of the subject's scopes. Defaults to all of them.`],[`audience / resource`,`no`,`Narrows who the issued token is for.`]]},{type:`h`,id:`modes`,text:`Delegation vs impersonation`},{type:`p`,text:"Supplying an `actor_token` selects **delegation**: the issued token says *this actor is acting for this subject*, and both are visible to whatever receives it. Omitting it selects **impersonation**: the issued token speaks as the subject alone."},{type:`p`,text:`Prefer delegation. Impersonation is occasionally the only thing a downstream service will accept, but it erases the caller from the audit trail at exactly the moment you would most want to know who it was.`},{type:`h`,id:`narrowing`,text:`What narrowing means, concretely`},{type:`list`,items:[`**Scopes** — the requested set must be a subset of the subject token's. Asking for more is refused, not silently trimmed.`,`**Audience** — an issued token can be bound to one downstream service, so a leak at that service does not yield a token usable everywhere.`,`**Lifetime** — the issued token never outlives the subject token.`,`**Authorization** — the subject's own grants still apply. Exchange does not confer authority; it repackages it.`]},{type:`warn`,text:`Because exchange only narrows, it is not a privilege-escalation mechanism — and it is also not a way to *grant* a service access to something the user cannot reach. If a downstream call needs authority the user does not have, that is a role-model problem, not an exchange problem.`}]},{slug:`logout`,section:`OAuth2 & OIDC`,navLabel:`Logout & sessions`,title:`Logout — RP-initiated and back-channel`,intro:`When a session ends, who else needs to know? AXIAM implements both halves of the answer.`,blocks:[{type:`h`,id:`why`,text:`Two holes, two mechanisms`},{type:`p`,text:`A federation deployment is AXIAM plus N relying parties, and each logout mechanism closes a hole the other leaves open:`},{type:`list`,items:[`**Without RP-initiated logout**, a user who logs out of a relying party stays logged in at AXIAM — so the next "sign in with AXIAM" silently signs them straight back in. From the user's point of view, they did not log out.`,`**Without back-channel logout**, a user who logs out *of AXIAM* stays logged in at every relying party indefinitely, because nothing tells them. This is the one that matters in an incident: revoking a compromised account otherwise leaves N live sessions behind.`]},{type:`h`,id:`sessions`,text:`Sessions, not users`},{type:`p`,text:"Both operate on a **session**. A user with a phone and a laptop who logs out on the laptop expects the phone to stay signed in, and the specification agrees. That is why AXIAM's ID tokens carry `sid`, and why every logout token names it — an ID token identifying only the user would force both halves to be all-or-nothing."},{type:`note`,text:"`sid` is stable across refresh: a token minted by the refresh grant carries the same `sid` as the one issued at login, so a relying party that stored it can still match a logout token to its own session."},{type:`h`,id:`rp`,text:`RP-initiated logout`},{type:`api`,endpoints:[{method:`GET`,path:`/oauth2/end_session`,summary:`End the session and redirect the browser back.`,public:!0}]},{type:`table`,headers:[`Parameter`,`Required`,`Meaning`],rows:[[`tenant_id`,`yes`,`Which tenant's session is ending.`],[`id_token_hint`,`SHOULD`,`A previously-issued ID token — the only *authenticated* statement of which session and which client this is about.`],[`post_logout_redirect_uri`,`no`,`Where to send the browser afterwards. Honoured only when it is on the client's allow-list.`],[`state`,`no`,`Round-tripped back to the redirect URI.`],[`client_id`,`no`,`Identifies the requesting client.`]]},{type:`warn`,text:"Send `id_token_hint`. Without it AXIAM cannot tell *which* session the browser means, and a `post_logout_redirect_uri` cannot be validated against a specific client's allow-list — so an unhinted logout is both less precise and less able to redirect you anywhere useful."},{type:`h`,id:`backchannel`,text:`Back-channel logout`},{type:`p`,text:"When a session ends at AXIAM — through logout, administrative revocation, or an incident response — every relying party registered for back-channel logout receives a signed **logout token** naming the `sid`. The notification is server-to-server, so it works even when the user's browser is closed."},{type:`p`,text:"On the relying-party side, the handler's job is: verify the token's signature against the tenant JWKS, confirm it is a logout token, read the `sid`, and destroy the local session that matches it. Do not destroy every session for that user unless you have specifically decided that is what you want."},{type:`note`,text:`This is the mechanism that makes "revoke this account now" mean something across a federation. Register your relying parties for it before you need it — an incident is a bad time to discover that logging out of AXIAM does nothing anywhere else.`}]},{slug:`fapi2`,section:`OAuth2 & OIDC`,navLabel:`FAPI 2.0 & mTLS`,title:`FAPI 2.0 profile & mTLS clients`,intro:`Register a client under the FAPI 2.0 Security Profile, authenticate it with mutual TLS instead of a shared secret, and issue certificate-bound tokens that a stolen copy cannot use.`,blocks:[{type:`h`,id:`optin`,text:`All of it is opt-in`},{type:`p`,text:`Every field on this page defaults to what an AXIAM client already was. **A deployment that changes nothing behaves exactly as it did before** — that is the design's load-bearing property, and it is asserted by tests rather than assumed.`},{type:`h`,id:`switch`,text:`The one switch`},{type:`code`,caption:`registering a FAPI 2.0 client`,code:`POST /api/v1/oauth2-clients
{
  "name": "payments-rp",
  "redirect_uris": ["https://rp.example/callback"],
  "grant_types": ["authorization_code", "refresh_token"],
  "scopes": ["openid"],

  "profile": "fapi2",
  "require_par": true,
  "token_endpoint_auth_method": "tls_client_auth",
  "tls_client_auth_san_dns": "payments-rp.example",
  "tls_client_certificate_bound_access_tokens": true
}`},{type:`p`,text:'`profile: "fapi2"` is not a label. The server **refuses the registration** unless it also carries `require_par`, a strong `token_endpoint_auth_method`, and some form of sender-constraining — and the refusal names which one is missing:'},{type:`code`,code:`400 a fapi2 client must set require_par: FAPI 2.0 §5.3.1.2 requires pushed
    authorization requests`},{type:`note`,text:`That refusal is the point. A client satisfying eleven of twelve FAPI constraints is not "mostly FAPI" — it is a client with a hole. The bundle cannot be half-applied, so a reviewer can answer *is this client conformant?* by reading one field.`},{type:`h`,id:`mtls`,text:`mTLS client authentication`},{type:`p`,text:"`tls_client_auth` replaces the shared secret with a client certificate, matched against a configured identifier — `tls_client_auth_san_dns` and its siblings. The certificate is verified by AXIAM's own TLS listener; **no proxy-asserted identity header is in the trusted path**, so there is no `X-Client-Certificate` to forge."},{type:`table`,headers:[`Variable`,`Values`,`Default`,`Meaning`],rows:[[`AXIAM__SERVER__TLS__CLIENT_AUTH`,`off | optional | required`,`off`,`Client-certificate policy on the native listener.`],[`AXIAM__SERVER__TLS__CLIENT_CA_PATH`,`PEM bundle path`,`—`,`Trust anchors for client certificates.`]]},{type:`warn`,text:`Startup **fails fast** when client authentication is enabled but the CA path is unset, unreadable, empty or malformed. A misconfigured mTLS server never starts serving — it does not fall back to accepting anonymous clients.`},{type:`h`,id:`bound`,text:`Certificate-bound access tokens`},{type:`p`,text:"With `tls_client_certificate_bound_access_tokens`, an issued token carries a confirmation claim tying it to the thumbprint of the certificate that requested it. A resource server checks that the presenting client's certificate matches, which makes the token **sender-constrained**: copying it out of a log, a proxy or a memory dump gains an attacker nothing without the corresponding private key."},{type:`p`,text:`This is the single highest-value item on this page for an ordinary deployment, FAPI or not. Bearer tokens are bearer material; sender-constrained tokens are not.`},{type:`h`,id:`who`,text:`Who needs this`},{type:`list`,items:[`**Open banking and regulated financial APIs** — where FAPI 2.0 conformance is a requirement rather than a preference.`,`**Any high-value machine-to-machine path** — the mTLS and token-binding halves are worth having on their own, without the full profile.`,`**Nobody else, yet.** If you are starting out, register ordinary clients. The profile is here for when a compliance obligation or a threat model asks for it.`]}]}],l=[{slug:`rest`,section:`APIs & integration`,navLabel:`REST API`,title:`REST API`,intro:`The broadest surface — every entity in the system is manageable here, described by an OpenAPI 3.1 document that is generated from the server and drift-gated in CI.`,blocks:[{type:`h`,id:`spec`,text:`The specification`},{type:`p`,text:"`sdks/openapi.json` is the single source of truth for the REST surface. It is generated from the server's own route annotations, and a CI job fails any change where the committed spec diverges from a fresh export — so the document cannot quietly fall behind the code it describes."},{type:`code`,caption:`view it`,code:`# any Swagger/Redoc viewer works
npx @redocly/cli preview-docs docs/api/openapi.json

# or regenerate it after changing the API
cargo build -p axiam-server --no-default-features
./target/debug/axiam-server --dump-openapi > sdks/openapi.json`},{type:`note`,text:`AXIAM deliberately does not serve an in-app Swagger UI route. Bundling one pulls a build-time download of the Swagger UI archive, which is a supply-chain and build-fragility cost for something every developer already has a viewer for.`},{type:`h`,id:`shape`,text:`Shape of the API`},{type:`table`,proseFirstCol:!0,headers:[`Prefix`,`What lives there`],rows:[["`/api/v1/auth/*`",`Login, refresh, logout, password reset, email verification, MFA, WebAuthn, OPAQUE and federated sign-in.`],["`/api/v1/authz/*`",`Authorization checks, single and batched.`],[`Entity collections`,"`users`, `groups`, `roles`, `permissions`, `resources`, `service-accounts`, `oauth2-clients`, `organizations`, `tenants`, `certificates`, `pgp-keys`, `webhooks`, `reactors`, `notification-rules`, `federation-configs`, `scim-tokens`, `audit-logs`, `settings`."],["`/oauth2/*`",`The authorization-server endpoints. See [OAuth2 & OIDC](#/docs/oauth2).`],["`/uma2/*`",`The UMA 2.0 Protection API. See [UMA 2.0](#/docs/uma).`],["`/scim/v2/*`",`SCIM 2.0 provisioning. See [SCIM provisioning](#/docs/scim).`],["`/health`, `/ready`",`Liveness and readiness probes. See [Health & observability](#/docs/observability).`]]},{type:`h`,id:`conventions`,text:`Conventions`},{type:`list`,items:["**Two ways to authenticate.** A machine client sends `Authorization: Bearer <access_token>`, obtained from the OAuth2 token endpoint. An interactive login sets `httpOnly` cookies instead — `POST /auth/login` returns no token in its body — and state-changing requests must then echo the `axiam_csrf` cookie in an `X-CSRF-Token` header. The SDKs handle the second case for you.","**Tenancy is explicit.** Entity routes are tenant-scoped through the authenticated principal; OAuth2 endpoints take `tenant_id` as a query parameter. Nothing is inferred from a default.",`**Every route is permission-guarded.** A caller needs an explicit grant for the action behind the route — the same 113-permission registry the admin console uses.`,"**Collections paginate** with `offset` and `limit`, and return the items plus a total.",`**Mutations are audited.** Every write lands in the append-only audit log with the acting principal.`]},{type:`h`,id:`example`,text:`A worked example`},{type:`code`,caption:`create a user, put them in a group, check access`,code:`# A machine client gets a bearer token from the OAuth2 token endpoint.
# (An interactive login sets cookies instead — see the Quickstart.)
TOKEN=$(curl -sS -X POST 'https://iam.acme.dev/oauth2/token?tenant_id=<uuid>' \\
  -d grant_type=client_credentials \\
  -d client_id="$SA_CLIENT_ID" -d client_secret="$SA_CLIENT_SECRET" \\
  | jq -r .access_token)

USER=$(curl -sS -X POST https://iam.acme.dev/api/v1/users \\
  -H "authorization: Bearer $TOKEN" -H 'content-type: application/json' \\
  -d '{"email":"dana@acme.dev","username":"dana"}' | jq -r .id)

curl -sS -X POST "https://iam.acme.dev/api/v1/groups/$GROUP/members" \\
  -H "authorization: Bearer $TOKEN" -H 'content-type: application/json' \\
  -d "{\\"user_id\\":\\"$USER\\"}"

curl -sS -X POST https://iam.acme.dev/api/v1/authz/check \\
  -H "authorization: Bearer $TOKEN" -H 'content-type: application/json' \\
  -d '{"action":"read","resource_id":"doc:1"}'`},{type:`h`,id:`gdpr`,text:`GDPR endpoints`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/account/export`,summary:`Request a data export (GDPR Art. 15).`},{method:`GET`,path:`/api/v1/account/export/{token}`,summary:`Collect it, optionally PGP-encrypted.`},{method:`POST`,path:`/api/v1/account/delete`,summary:`Request erasure (Art. 17).`},{method:`POST`,path:`/api/v1/account/delete/cancel`,summary:`Cancel a pending erasure inside the grace window.`}]},{type:`note`,text:"Erasure pseudonymises the actor identity in the audit trail rather than deleting the records — an append-only log cannot have rows removed from it. The HMAC pepper that makes pseudonyms consistent is `AXIAM__GDPR_PSEUDONYM_PEPPER`; changing it breaks the linkage between old and new pseudonyms. See [Standards & compliance](#/docs/compliance)."}]},{slug:`grpc`,section:`APIs & integration`,navLabel:`gRPC API`,title:`gRPC API`,intro:`A low-latency surface for service-mesh authorization checks, token validation and user lookups — Tonic on the server, one protobuf contract shared by every SDK.`,blocks:[{type:`h`,id:`why`,text:`Why gRPC`},{type:`p`,text:"REST is the general-purpose surface; gRPC exists for the hot path. Inside a service mesh, sidecars and backends make authorization checks on nearly every request, and connection reuse plus binary framing is what keeps tail latency down. In the benchmark run, a single gRPC `CheckAccess` held a p99 of 90 ms at database saturation, and TLS 1.3 cost nothing measurable against plaintext."},{type:`h`,id:`services`,text:`Services`},{type:`p`,text:"Defined in `proto/axiam/v1/`. Every request message is tenant-scoped — `tenant_id` is a field on every RPC, because there is no default tenant."},{type:`table`,proseFirstCol:!0,headers:[`Service`,`RPCs`,`Purpose`],rows:[[`AuthorizationService`,`CheckAccess, BatchCheckAccess`,`A single access check, or several in one round trip.`],[`TokenService`,`ValidateToken, IntrospectToken`,`Signature and expiry validation, or full RFC 7662-style claims.`],[`UserService`,`GetUser, ValidateCredentials`,`Lookup by id, or a username/password check that issues no token.`]]},{type:`h`,id:`server`,text:`The server`},{type:`p`,text:"The gRPC listener starts inside `axiam-server` alongside REST and the AMQP consumer. It binds `127.0.0.1:50051` by default — loopback only — and is meant to be reached in-cluster over an internal network or mTLS, never through a public ingress. Bind address, port and the per-IP rate limit are the `AXIAM__GRPC__*` variables."},{type:`note`,text:`In the shipped Kubernetes manifests, port 50051 is intentionally *not* routed through the Ingress — it is reachable only via the in-cluster ClusterIP service.`},{type:`h`,id:`consume`,text:`Consuming it`},{type:`p`,text:`The seven SDKs implementing the full contract — Rust, TypeScript, Python, Java, C#, PHP and Go — ship pre-generated stubs, so you consume gRPC without running codegen yourself. gRPC is a **separate client** rather than a flag on the REST one — it is a different transport, with its own channel, TLS settings and connection lifetime — but it shares the session and the single-flight refresh guard, and the decisions it returns are the same ones REST returns.`},{type:`code`,caption:`building a gRPC channel · Rust`,code:`use axiam_sdk::grpc::{build_channel, GrpcChannelConfig};

// gRPC is a separate channel rather than a flag on the REST client: it is a
// different transport with its own TLS and connection settings.
let channel = build_channel(
    "https://iam.acme.dev:50051",
    &GrpcChannelConfig::default(),
)?;

// The channel then backs \`AuthzGrpcClient\`, which shares the REST client's
// single-flight refresh guard. See the SDK's \`grpc_check_access\` example.`},{type:`h`,id:`codegen`,text:`Generating your own stubs`},{type:`p`,text:"Integrating from a language with no published AXIAM SDK? Generate stubs straight from the `.proto` files with `buf generate`, or `protoc` plus your language's gRPC plugin. They are self-contained proto3 with no imports beyond the well-known types, and CI runs `buf lint` and `buf breaking` on every change — so the contract is guarded against accidental breakage."},{type:`code`,code:`buf generate   # from the vendored proto/ tree`},{type:`note`,text:"The Kotlin, Swift, C and C++ SDKs cover the REST surface today; gRPC is a planned follow-up for them. Until it lands, use the REST transport or generate stubs directly from `proto/`."}]},{slug:`amqp`,section:`APIs & integration`,navLabel:`AMQP & async`,title:`AMQP — asynchronous authorization & events`,intro:`The message bus behind deferred authorization decisions, audit ingestion, mail, webhook delivery and Reactor hooks — specified as AsyncAPI 2.6.`,blocks:[{type:`h`,id:`why`,text:`What runs over the bus`},{type:`p`,text:`Some work should not happen on a request thread. Audit ingestion must not slow down the operation being audited; webhook delivery must survive a receiver being down; a mail send must not fail a signup. AXIAM puts all of it on RabbitMQ, and exposes the same authorization engine there for callers that want a decision without holding a connection open.`},{type:`table`,proseFirstCol:!0,headers:[`Channel`,`Purpose`],rows:[[`axiam.authz.request`,`Deferred authorization requests.`],[`axiam.authz.response`,`The decisions, correlated back to the requester.`],[`axiam.audit.events`,`Audit ingestion, off the request hot path.`],[`axiam.notifications`,`Notification-rule delivery.`],[`axiam.mail.outbound`,`Outbound mail — verification, reset, alerts.`],[`axiam.webhook`,"Webhook delivery, with `axiam.webhook.retry` for backoff."]]},{type:`p`,text:"Each has a dead-letter queue (`*.dlq`) for messages that exhaust their retries, so a permanently-failing consumer produces an inspectable backlog rather than silent loss."},{type:`h`,id:`security`,text:`Transport security & authenticity`},{type:`warn`,text:"AMQP is **TLS-only**. `AXIAM__AMQP__URL` must use the `amqps://` scheme — every other scheme is refused at startup rather than downgraded."},{type:`p`,text:`Message authenticity is an HMAC over the whole message with replay protection — a nonce plus a freshness window — not merely a signature over the body. This matters because a bus consumer cannot rely on TLS peer identity the way an HTTP caller can: the broker is in between.`},{type:`h`,id:`spec`,text:`The specification`},{type:`p`,text:"`docs/api/asyncapi.yml` is a hand-authored AsyncAPI 2.6 document covering every channel, its message schema and its DLQ. The normative HMAC construction is `sdks/CONTRACT.md` §8 and §8b, which every SDK implements identically."},{type:`note`,text:`Reactors also ride this bus, but with a different contract: a Reactor can *answer* — allow, deny, or a narrowly field-allow-listed mutation — inside a timeout the server declares. See [Reactors](#/docs/reactors).`}]},{slug:`scim`,section:`APIs & integration`,navLabel:`SCIM provisioning`,title:`SCIM 2.0 provisioning`,intro:`Let Okta, Microsoft Entra ID or any SCIM-compliant IdP create, update and deactivate AXIAM users and groups directly, instead of an administrator doing it by hand.`,blocks:[{type:`h`,id:`why`,text:`Federation is not provisioning`},{type:`p`,text:`Federation answers *who is this person* at sign-in. It does not create an account before someone's first day, does not update it when they change teams, and — the one that matters — does not deactivate it when they leave. SCIM does all three, driven by the directory that already knows.`},{type:`h`,id:`support`,text:`What is implemented`},{type:`table`,proseFirstCol:!0,headers:[`Area`,`Support`],rows:[[`Users`,`Full CRUD plus PATCH.`],[`Groups`,`Full CRUD plus PATCH, including membership.`],[`Filtering`,'`userName eq "..."` on Users, `externalId eq "..."` on Users and Groups, and paging with `startIndex` / `count`.'],[`PATCH operations`,"`add` / `replace` / `remove` on the standard attribute paths Okta and Entra actually send."],[`Discovery`,"`/Schemas`, `/ServiceProviderConfig`, `/ResourceTypes`."],[`Bulk operations`,"**Not supported** — `POST /Bulk` returns `501` with a SCIM error body."],[`Complex filters`,'**Not supported** — anything other than `<attr> eq "<value>"` returns `400 invalidFilter`.'],[`ETag / conditional requests`,"**Not implemented**, and `ServiceProviderConfig` says so."]]},{type:`note`,text:`The unsupported items are the two the specification itself carves out as optional, and the two no mainstream IdP requires for user and group lifecycle. Enterprise IdP-driven provisioning is mostly CRUD.`},{type:`h`,id:`endpoints`,text:`Endpoints`},{type:`code`,code:`GET    /scim/v2/ServiceProviderConfig
GET    /scim/v2/ResourceTypes
GET    /scim/v2/Schemas

GET    /scim/v2/Users?filter=...&startIndex=1&count=50
POST   /scim/v2/Users
GET    /scim/v2/Users/{id}
PUT    /scim/v2/Users/{id}
PATCH  /scim/v2/Users/{id}
DELETE /scim/v2/Users/{id}

GET    /scim/v2/Groups?filter=...&startIndex=1&count=50
POST   /scim/v2/Groups
GET    /scim/v2/Groups/{id}
PUT    /scim/v2/Groups/{id}
PATCH  /scim/v2/Groups/{id}
DELETE /scim/v2/Groups/{id}`},{type:`h`,id:`tokens`,text:`Authentication & tenant scoping`},{type:`p`,text:`A SCIM client authenticates with a dedicated provisioning token, bound to one tenant and carrying a dedicated permission — not a super-admin session, and not an OAuth2 client secret reused for something else. An IdP integration that is compromised should be able to manage users in one tenant and nothing else.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/scim-tokens`,summary:`List provisioning tokens for the tenant.`},{method:`POST`,path:`/api/v1/scim-tokens`,summary:`Mint one. The value is returned exactly once.`},{method:`DELETE`,path:`/api/v1/scim-tokens/{id}`,summary:`Revoke one.`}]},{type:`warn`,text:"Deactivation, not deletion, is what you usually want from an IdP. A SCIM `DELETE` removes the account; setting `active: false` disables sign-in while leaving the identity intact for the audit trail. Configure your IdP's deprovisioning action deliberately."},{type:`note`,text:"Step-by-step walkthroughs for wiring up Okta and Microsoft Entra ID are in `docs/api/scim-provisioning.md` in the repository."}]},{slug:`webhooks`,section:`APIs & integration`,navLabel:`Webhooks`,title:`Webhooks`,intro:`Fire-and-forget HTTP callbacks on domain events, signed so a receiver can prove they came from AXIAM.`,blocks:[{type:`h`,id:`delivery`,text:`Signed delivery`},{type:`p`,text:`A webhook delivers an event notification to an endpoint you configure, as an outbound HTTPS POST. Every payload is signed with HMAC-SHA256 over the raw body, so a receiver can confirm the request genuinely came from AXIAM and was not altered in transit.`},{type:`p`,text:"Delivery is **at-least-once**, over the AMQP bus, with retry and exponential backoff and a dead-letter queue after `max_attempts`. Your receiver must therefore be idempotent — nothing here promises exactly-once, and a broker hiccup is a redelivery."},{type:`h`,id:`verify`,text:`Verifying a payload`},{type:`p`,text:`Compute the HMAC-SHA256 of the **raw** request body with your endpoint's shared secret and compare it in constant time against the signature header. Parse the body only after the comparison succeeds.`},{type:`codegroup`,caption:`signature verification`,tabs:[{label:`Node`,code:`import { createHmac, timingSafeEqual } from 'node:crypto';

function verify(rawBody, signature, secret) {
  const expected = createHmac('sha256', secret)
    .update(rawBody)
    .digest('hex');
  return expected.length === signature.length &&
    timingSafeEqual(Buffer.from(expected), Buffer.from(signature));
}`},{label:`Python`,code:`import hmac, hashlib

def verify(raw_body: bytes, signature: str, secret: str) -> bool:
    expected = hmac.new(
        secret.encode(), raw_body, hashlib.sha256
    ).hexdigest()
    return hmac.compare_digest(expected, signature)`},{label:`Go`,code:`func verify(rawBody []byte, signature, secret string) bool {
    mac := hmac.New(sha256.New, []byte(secret))
    mac.Write(rawBody)
    expected := hex.EncodeToString(mac.Sum(nil))
    return hmac.Equal([]byte(expected), []byte(signature))
}`}]},{type:`warn`,text:`Compare in constant time, and compare against the **raw** bytes. Re-serialising the parsed JSON and hashing that will not match — key order and whitespace are part of what was signed.`},{type:`h`,id:`managing`,text:`Managing webhooks`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/webhooks`,summary:`List configured webhooks.`},{method:`POST`,path:`/api/v1/webhooks`,summary:`Create one, subscribing to a list of event types.`},{method:`GET`,path:`/api/v1/webhooks/{id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/webhooks/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/webhooks/{id}`,summary:`Delete it.`},{method:`GET`,path:`/api/v1/reactors/events`,summary:`The event catalog — every event type that can be subscribed to.`}]},{type:`p`,text:"A webhook subscribes to a list of event type names — `user.created` and `auth.login` are typical. The catalog is a growing list rather than a fixed enum; query `/api/v1/reactors/events` for what this build actually emits rather than hard-coding a list from documentation."},{type:`note`,text:"Webhook secrets are encrypted at rest under `AXIAM__PKI__ENCRYPTION_KEY`, alongside the CA signing keys."},{type:`h`,id:`vs`,text:`Webhook or Reactor?`},{type:`p`,text:`A webhook is told what happened, after it happened, and cannot affect it. If you need to *influence* an operation — approve it, refuse it, or adjust a narrowly allow-listed field before it commits — that is a Reactor. See [Reactors](#/docs/reactors).`}]},{slug:`reactors`,section:`APIs & integration`,navLabel:`Reactors`,title:`Reactors — external hook actors`,intro:`An external process that subscribes to authorization-adjacent hook events and answers back — allow, deny, or a narrowly allow-listed mutation — inside a timeout the server declared.`,blocks:[{type:`h`,id:`what`,text:`What a Reactor is`},{type:`p`,text:`A Reactor is AXIAM's answer to Zitadel Actions and Keycloak SPIs, and the difference is the whole design: those load third-party code **into** the authorization server. A Reactor stays outside, reachable only over the AMQP bus, and answers through a signed reply schema the server validates before it believes a word of it.`},{type:`p`,text:`That boundary is what makes the feature safe to have. A crashing, hanging or malicious Reactor cannot take the authorization server with it — it can, at worst, fail its own hook inside the declared timeout, and the configured failure policy decides what that means.`},{type:`h`,id:`modes`,text:`Two modes`},{type:`table`,proseFirstCol:!0,headers:[``,`Webhook`,'Listener Reactor (`mode: "listen"`)'],rows:[[`Transport`,`Outbound HTTPS POST to a URL`,`AMQP consume from a durable, server-declared queue`],[`Authenticity`,`HMAC-SHA256 over the body`,`HMAC over the whole message, replay-protected with a nonce and freshness window`],[`Delivery`,`At-least-once with retry, backoff and a DLQ`,`At-least-once — **your consumer must be idempotent**`],[`Event catalog`,"Domain events: `user.created`, `role.assigned`, … a fixed, growing list",`Any event in the reactor registry, **including non-interceptable ones**`],[`Can it affect the operation?`,`Never — the change is already committed when it fires`,`Never, by construction — the server does not wait for a listener's reply`]]},{type:`p`,text:`The other mode intercepts: the server waits, within its declared timeout, and the Reactor's validated reply can allow, deny, or apply a mutation limited to an explicitly allow-listed set of fields.`},{type:`h`,id:`failure`,text:`Failure policy is the decision that matters`},{type:`warn`,text:`An intercepting Reactor sits in the path of a real operation. Its failure policy decides what happens when it times out or answers unintelligibly — fail open (proceed) or fail closed (refuse). Fail-closed turns a Reactor outage into an outage of whatever it hooks; fail-open turns it into a silently unenforced control. Choose deliberately, and monitor either way.`},{type:`h`,id:`registering`,text:`Registering one`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/reactors`,summary:`List registered Reactors.`},{method:`POST`,path:`/api/v1/reactors`,summary:`Register one.`},{method:`GET`,path:`/api/v1/reactors/{id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/reactors/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/reactors/{id}`,summary:`Remove it.`},{method:`GET`,path:`/api/v1/reactors/events`,summary:`The event registry — what can be hooked, and which events are interceptable.`}]},{type:`note`,text:"The wire protocol — message shape, signing, the reply schema and the timeout semantics — is normative in `sdks/CONTRACT.md` §22. The admin console's Reactors page is the same surface with a form on top."}]},{slug:`errors`,section:`APIs & integration`,navLabel:`Error reference`,title:`Error reference`,intro:`Three error types across every SDK and every language, one mapping from HTTP and gRPC status, and the handful of rules that keep a client from retrying something that cannot succeed.`,blocks:[{type:`h`,id:`types`,text:`The three types`},{type:`p`,text:`Every SDK exposes exactly three error types. Languages add idiomatic sub-types, but never replace these three — so an integration ported between languages keeps the same control flow.`},{type:`table`,proseFirstCol:!0,headers:[`Type`,`Meaning`],rows:[[`AuthError`,`Authentication failure: wrong credentials, expired session, failed MFA, a 401 on refresh.`],[`AuthzError`,`Authorization failure: the caller lacks permission for the requested operation.`],[`NetworkError`,`Transport-level failure: connection refused, timeout, TLS error, DNS failure.`],[`OAuthProtocolError`,"A **sub-type of `AuthError`**. An RFC 6749 protocol error from an `/oauth2/*` endpoint, carrying `error` and `error_description` as accessible fields."]]},{type:`h`,id:`http`,text:`HTTP status mapping`},{type:`table`,headers:[`Status`,`Type`,`Notes`],rows:[[`400`,`NetworkError`,`Malformed request — an SDK or caller programming error.`],[`any status from /oauth2/* with an OAuth2 error body`,`OAuthProtocolError`,"**Dispatch on the `error` field, not on the status.** This row wins over the status rows below."],[`401`,`AuthError`,`Unauthenticated. Triggers single-flight refresh when tokens are present.`],[`403`,`AuthzError`,`Authenticated but not authorized.`],[`408, 429`,`NetworkError`,`Timeout, or rate-limited.`],[`409`,`AuthzError`,`Conflict — resource-level access denied.`],[`5xx`,`NetworkError`,`Server error. An SDK must **not** retry authentication.`],[`connection / DNS / TLS failure`,`NetworkError`,`Carries the underlying transport error as its cause.`]]},{type:`warn`,text:"The `/oauth2/*` row is scoped to those paths only. An ordinary REST `403` — including from `/api/v1/authz/check` and from the UMA Protection API at `/uma2/*` — is an `AuthzError`, not a protocol error. The distinction matters: one is *you may not*, the other is *your client credentials or grant request were wrong*."},{type:`h`,id:`grpc`,text:`gRPC status mapping`},{type:`table`,headers:[`gRPC status`,`Type`,`Notes`],rows:[[`UNAUTHENTICATED (16)`,`AuthError`,`Triggers single-flight refresh.`],[`PERMISSION_DENIED (7)`,`AuthzError`,`Caller lacks the required permission.`],[`UNAVAILABLE (14)`,`NetworkError`,`Server unreachable.`],[`DEADLINE_EXCEEDED (4)`,`NetworkError`,`Request timed out.`],[`INTERNAL (13)`,`NetworkError`,`Server-side error.`],[`RESOURCE_EXHAUSTED (8)`,`NetworkError`,`Rate-limited.`]]},{type:`h`,id:`rules`,text:`Rules that prevent bad retries`},{type:`list`,items:[`**A 401 carrying an OAuth2 protocol error does not enter the refresh guard.** A client-authentication failure is not a session expiry, and retrying cannot fix a wrong client secret.`,`**Concurrent 401s collapse into one refresh.** The single-flight guard means N in-flight requests produce one refresh attempt, not N.`,`**Errors never contain token strings** — not in messages, not in context fields, not in stack traces.`,"**`AuthzError` carries the denied action and resource** where the response body provides them, so a log line says what was refused rather than only that something was."]},{type:`h`,id:`device`,text:`Device-grant answers`},{type:`p`,text:"The device grant's four answers are protocol errors with specific reactions rather than failures — see [Device authorization grant](#/docs/device-flow) for the table. `authorization_pending` and `slow_down` are normal, expected control flow."},{type:`note`,text:"The normative source for all of this is `sdks/CONTRACT.md` §2, which every SDK is conformance-tested against. If this page and that chapter ever disagree, the contract is authoritative."}]}],u=[{slug:`configuration`,section:`Operate`,navLabel:`Configuration`,title:`Configuration & environment variables`,intro:`Every setting on the AXIAM server image is an environment variable. This is the reference: what each variable means, its default, and an example value.`,blocks:[{type:`h`,id:`naming`,text:`The naming convention`},{type:`p`,text:"All configuration keys use a double underscore (`__`) after the `AXIAM` prefix — for example `AXIAM__DB__USERNAME`. The `__` separates both the prefix and the nested key levels (this is how the config layer distinguishes them). A single underscore (`AXIAM_DB__USERNAME`) is silently ignored and the in-code default wins, so double-check the doubling when a value doesn't take effect."},{type:`warn`,text:"Secrets (database password, JWT keys, the AES-256-GCM encryption keys, the peppers) must come from a secret manager or mounted secret — never bake real key material into an image, a compose file or git. Use a placeholder like `<set-in-secret-manager>` in any template, and never reuse a value across environments."},{type:`h`,id:`connectivity`,text:`Connectivity & bind addresses`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__DB__URL`,`SurrealDB address as a bare host:port — not a URL scheme (the Ws engine resolves a scheme as a hostname and fails).`,`surrealdb:8000`],[`AXIAM__DB__NAMESPACE`,`SurrealDB namespace.`,`axiam`],[`AXIAM__DB__DATABASE`,`SurrealDB database.`,`axiam`],[`AXIAM__AMQP__URL`,`RabbitMQ connection string, assembled from the broker credentials at the deployment layer. Must be amqps:// — AMQP is TLS-only and every other scheme is refused at startup.`,`amqps://user:pass@rabbitmq:5671`],[`AXIAM__SERVER__HOST`,`REST bind address (default 127.0.0.1). Set 0.0.0.0 in a container.`,`0.0.0.0`],[`AXIAM__SERVER__PORT`,`REST bind port (default 8090).`,`8090`],[`AXIAM__GRPC__HOST`,`gRPC bind address (default 127.0.0.1, loopback-only). Set 0.0.0.0 to serve in-cluster.`,`0.0.0.0`],[`AXIAM__GRPC__PORT`,`gRPC bind port (default 50051).`,`50051`],[`AXIAM__GRPC__GRPC_AUTHZ_PER_SEC`,`Max gRPC authz requests per second per IP (default 100).`,`100`],[`AXIAM__SERVER__CORS_ALLOWED_ORIGINS`,`Allowed CORS origins; empty disables cross-origin requests (restrictive default).`,`https://admin.acme.dev`],[`RUST_LOG`,`Log verbosity / filter. Keep it narrow in production — no internal module exposure.`,`info`]]},{type:`h`,id:`secrets`,text:`Secrets & encryption keys`},{type:`p`,text:"These are required for a real deployment. Generate the 32-byte hex keys with `openssl rand -hex 32` and the Ed25519 JWT keypair with `openssl genpkey -algorithm ed25519`."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__DB__USERNAME`,`SurrealDB username.`,`axiam`],[`AXIAM__DB__PASSWORD`,`SurrealDB password.`,`<set-in-secret-manager>`],[`AXIAM__AUTH__JWT_PRIVATE_KEY_PEM`,`Ed25519 JWT signing private key (PEM).`,`-----BEGIN PRIVATE KEY----- …`],[`AXIAM__AUTH__JWT_PUBLIC_KEY_PEM`,`Ed25519 JWT verification public key (PEM), paired with the private key.`,`-----BEGIN PUBLIC KEY----- …`],[`AXIAM__AUTH__MFA_ENCRYPTION_KEY`,`AES-256-GCM key (32-byte hex) encrypting TOTP MFA secrets at rest.`,`<64 hex chars>`],[`AXIAM__PKI__ENCRYPTION_KEY`,`AES-256-GCM key (hex) encrypting CA signing keys (and webhook secrets) at rest.`,`<64 hex chars>`],[`AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY`,`AES-256-GCM key (hex) encrypting SAML/OIDC federation client secrets at rest.`,`<64 hex chars>`],[`AXIAM__EMAIL_ENCRYPTION_KEY`,`AES-256-GCM key (hex) encrypting email/SMTP secrets; also gates the email-config admin endpoints.`,`<64 hex chars>`],[`AXIAM__GDPR_PSEUDONYM_PEPPER`,`HMAC-SHA256 pepper (hex) pseudonymizing audit-log actor identities on GDPR erasure.`,`<64 hex chars>`],[`AXIAM__AUTH__PEPPER`,`Password pepper (string) prepended before Argon2id hashing.`,`<random string>`]]},{type:`h`,id:`provider`,text:`Where secrets come from`},{type:`p`,text:"The keys above do not have to be environment variables. `AXIAM__AUTH__SECRET_PROVIDER` selects where the server fetches them at startup, and the field names it looks for are the lowercase forms — `jwt_private_key_pem`, `opaque_setup_key`, `pki_encryption_key`, `auth_pepper` and the rest. See [Secrets & HashiCorp Vault](#/docs/secrets) for the full story."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTH__SECRET_PROVIDER`,"`env` (default), `file`, or `vault`. An unknown value is **refused at startup** rather than falling back — a typo that silently reverted to `env` presents as an unexplained outage.",`vault`],[`AXIAM__AUTH__SECRET_DIR`,"Required with `file`. Directory of one file per secret, named after the field — the shape a Kubernetes or Docker secret volume already has.",`/etc/axiam/secrets`],[`AXIAM__AUTH__VAULT_ADDR`,"Required with `vault`. Base address of the Vault server. Must be TLS in production — the token is a bearer credential for the setup key.",`https://vault.internal:8200`],[`AXIAM__AUTH__VAULT_TOKEN`,"Required with `vault`. A token with read access to the mount and path below, and nothing else.",`<set-in-secret-manager>`],[`AXIAM__AUTH__VAULT_MOUNT`,"KV v2 mount point (default `secret`).",`secret`],[`AXIAM__AUTH__VAULT_PATH`,"Path within the mount holding AXIAM's keys as fields (default `axiam`).",`axiam`]]},{type:`h`,id:`opaque-keys`,text:`OPAQUE`},{type:`p`,text:"Both are required whenever any organization or tenant has `opaque_mode` other than `disabled`. They are split by what rotating them costs — see [OPAQUE](#/docs/opaque)."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTH__OPAQUE_SETUP_KEY`,`AES-256-GCM key (32-byte hex) encrypting each tenant's OPRF seed and AKE keypair at rest. **Back this up** — losing it means a password reset for every user in every tenant.`,`<64 hex chars>`],[`AXIAM__AUTH__OPAQUE_SESSION_KEY`,`AES-256-GCM key (32-byte hex) sealing in-flight exchange state (a 120-second window). Cheap to rotate; deliberately separate from the setup key.`,`<64 hex chars>`]]},{type:`h`,id:`oauth2`,text:`OAuth2 & OIDC`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTH__OAUTH2_ISSUER_URL`,`Public issuer URL for OIDC discovery. Must be an origin, not a path (path-based issuers are rejected).`,`https://iam.acme.dev`],[`AXIAM__OAUTH2__JWKS_CACHE_MAX_AGE_SECS`,`Cache-Control max-age on the JWKS endpoint, in seconds.`,`300`],[`AXIAM__AUTH__ALLOW_MISSING_AUD_AS_USER`,`Compatibility switch — treat a token with no audience claim as a user token. Leave off unless you need it.`,`false`]]},{type:`h`,id:`hashing`,text:`Argon2id hash concurrency`},{type:`p`,text:`Each in-flight Argon2id operation allocates a ~19 MiB arena, so unbounded concurrency is a memory-DoS vector. A process-wide semaphore caps peak concurrent arenas and sheds excess load with a 503 rather than queueing unboundedly. The cost parameters themselves are never weakened for throughput.`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTH__MAX_CONCURRENT_HASHES`,`Max concurrent Argon2id hash/verify ops. 0 (default) = auto → min(CPU cores, 4). Peak crypto RSS ≈ this × 19 MiB.`,`0`],[`AXIAM__AUTH__HASH_ACQUIRE_TIMEOUT_SECS`,`Seconds a request waits for a hash permit before returning a 503 backpressure error (default 5).`,`5`]]},{type:`h`,id:`authz-cache`,text:`Authorization decision cache (optional)`},{type:`p`,text:`An optional per-tenant cache that skips the SurrealDB round-trips per check. Off by default; enabling it changes performance only, never the decision returned. Every access-narrowing mutation invalidates the affected entries immediately, so no revocation can leave a stale allow — the TTL is only a bounded-staleness backstop.`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTHZ__DECISION_CACHE_ENABLED`,`Master switch (default false).`,`false`],[`AXIAM__AUTHZ__DECISION_CACHE_TTL_SECS`,`Cached-decision TTL, and the upper bound on revocation latency if an invalidation is ever missed (default 5).`,`5`],[`AXIAM__AUTHZ__DECISION_CACHE_MAX_ENTRIES`,`Max cached decisions per tenant before FIFO eviction (default 10000).`,`10000`]]},{type:`h`,id:`rate-limit`,text:`Rate limiting`},{type:`p`,text:"Every auth/OAuth2 endpoint is rate-limited per-key, per-minute. Defaults are shown; `/auth/login` always keys per-IP regardless of the key mode."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__RATE_LIMIT__PROFILE`,`Posture preset moving the machine-traffic family coherently: internet (default) | gateway | mesh. The human endpoints — login, register, password-reset, MFA — stay strict per-IP under every preset.`,`internet`],[`AXIAM__RATE_LIMIT__LOGIN_PER_MIN`,`Max /auth/login per minute per key.`,`10`],[`AXIAM__RATE_LIMIT__REGISTER_PER_MIN`,`Max register requests per minute.`,`5`],[`AXIAM__RATE_LIMIT__TOKEN_PER_MIN`,`Max /oauth2/token per minute.`,`120`],[`AXIAM__RATE_LIMIT__PASSWORD_RESET_PER_MIN`,`Max password-reset requests per minute.`,`3`],[`AXIAM__RATE_LIMIT__MFA_PER_MIN`,`Max MFA enroll/confirm/verify per minute.`,`5`],[`AXIAM__RATE_LIMIT__INTROSPECT_PER_MIN`,`Max /oauth2/introspect per minute.`,`600`],[`AXIAM__RATE_LIMIT__REVOKE_PER_MIN`,`Max /oauth2/revoke per minute.`,`60`],[`AXIAM__RATE_LIMIT__AUTHZ_CHECK_PER_MIN`,`Max authz-check requests per minute.`,`1800`],[`AXIAM__RATE_LIMIT__TRUSTED_HOPS`,`Trusted reverse-proxy hops to skip from the right of X-Forwarded-For (set 1 behind a single ingress).`,`0`],[`AXIAM__RATE_LIMIT__KEY`,`Bucket-key mode for token/introspect/revoke: ip | client_id | ip_client_id.`,`ip`],[`AXIAM__RATE_LIMIT__SHARED`,`Enables (on) or disables (off) the cross-replica write-behind shared counter. off is a single-replica escape hatch — the per-replica in-memory governor becomes the sole limiter.`,`on`],[`AXIAM__RATE_LIMIT__SHARED_SYNC_MS`,`Write-behind flush interval (ms) for the shared counter, clamped 50-60000. Scales the cross-replica overshoot bound.`,`1000`]]},{type:`note`,text:`The shared counter behind AXIAM__RATE_LIMIT__SHARED no longer performs a synchronous datastore write on the request path (write-behind design): it decides in-memory and flushes one coalesced write per bucket per AXIAM__RATE_LIMIT__SHARED_SYNC_MS. Cross-replica enforcement is therefore eventual, not synchronous — worst-case overshoot beyond the configured limit is bounded by roughly (replicas - 1) x arrival_rate_per_replica x sync_interval, and is zero on a single replica. The per-replica in-memory governor is unchanged and still caps overshoot independently. See the Deployment Guide's rate-limiting section for the full bound, the store-outage semantics (a store outage is now detected by the background flusher rather than the request, so limit=0 plus an unreachable store denies rather than allows), and the observability log lines.`},{type:`h`,id:`sizing`,text:`Suggested settings by deployment (benchmark-derived)`},{type:`p`,text:"The shipped defaults are tuned for one thing: blunting single-source abuse on a small internet-facing deployment. The benchmark's production-posture run showed exactly that — and also that those defaults are far too strict for machine-to-machine topologies, where many clients share one source IP behind a NAT or gateway. The recommendations below derive directly from the run-4 measurements (median-of-3, 2-CPU-per-container envelope) in `benchmarks/PUBLIC_BENCH_ANALYSIS.md` §7."},{type:`warn`,text:`These recommendations come from the current (still temporary) benchmark results on laptop-class hardware. They are good starting points, not guarantees — validate against your own traffic, and re-check this section as the benchmark improves.`},{type:`h`,id:`sizing-throughput`,text:`What a given envelope sustains`},{type:`p`,text:`Measured per-path ceilings, useful for sizing both hardware and limits. Post rate-limit fix, database CPU is the main ceiling: authorization checks, introspection, token issuance and userinfo all gain 42–90% from a second pair of DB cores. Logins scale with server CPU at fixed Argon2id cost, and JWKS is limited by the load generator rather than by the server.`},{type:`table`,headers:[`Envelope (server / DB)`,`Token issuance`,`Introspection`,`Authz checks REST / gRPC`,`Userinfo REST / gRPC`,`Logins`],rows:[[`2 cores / 2 cores`,`~2,700/s`,`~4,400/s`,`~750 / ~890/s`,`~4,500 / ~12,700/s`,`~69/s`],[`2 cores / 4 cores`,`~4,500/s`,`~6,200/s`,`~1,430 / ~1,680/s`,`~7,200/s (server-bound) / ~12,700/s`,`~69/s`]]},{type:`p`,text:`The optional decision cache sits on top of these: at a ~100% hit rate it lifts gRPC checks to ~11,600/s, but REST checks gain only ~5% because their per-request session-cookie validation is a database read the cache does not cover. Size for the cache-off numbers and treat the cache as headroom.`},{type:`h`,id:`sizing-knobs`,text:`Recommended values per scenario`},{type:`table`,headers:[`Variable`,`Default`,`Small internet-facing`,`M2M / microservices / IoT`,`Large multi-tenant`],rows:[[`AXIAM__RATE_LIMIT__PROFILE`,`internet`,`internet`,`gateway — one variable moves the whole machine-traffic family coherently (key mode + token/introspect/revoke/authz), leaving the human endpoints untouched`,`mesh`],[`AXIAM__RATE_LIMIT__KEY`,`ip`,`ip`,`client_id (or ip_client_id) — per-IP buckets collide behind NAT/gateways — see the security caveat below`,`ip_client_id`],[`AXIAM__RATE_LIMIT__TOKEN_PER_MIN`,`120`,`keep 120`,`per-client peak RPS × 60 × 2 (a 2-core server sustains ~163k issuances/min total)`,`budget per tenant SLA`],[`AXIAM__RATE_LIMIT__INTROSPECT_PER_MIN`,`600`,`keep 600`,`10–20× your token limit (resource servers introspect per request)`,`same rule`],[`AXIAM__RATE_LIMIT__AUTHZ_CHECK_PER_MIN`,`1800`,`keep 1800`,`6,000–60,000 per client — checks are cheap reads; the pre-revision 300/min starved any real service`,`size to the cache-ON ceiling`],[`AXIAM__RATE_LIMIT__LOGIN_PER_MIN`,`10 (always per-IP)`,`keep 10`,`60+ if users arrive through a shared NAT/proxy`,`60+, front with a WAF`],[`AXIAM__AUTHZ__DECISION_CACHE_ENABLED`,`false`,`true`,`true if your checks go over gRPC — measured 13× there at a favourable keyspace, ~+32% at a realistic one; REST checks gain only ~5%`,`true`],[`AXIAM__AUTHZ__DECISION_CACHE_TTL_SECS`,`5`,`5`,`5 (raise only if a ≤TTL revocation delay is acceptable)`,`5`],[`AXIAM__AUTHZ__BATCH_STRATEGY`,`coalesced`,`default`,"default — `coalesced` measured 744 batch ops/s ≈ 3,721 checks/s, about 5× single checks; a full-matrix re-measurement is queued",`default`],[`AXIAM__DB__POOL_SIZE`,`1`,`1`,`1 — no measured benefit; leave it alone`,`1`],[`AXIAM__AUTH__MAX_CONCURRENT_HASHES`,`0 = auto (min(cores, 4))`,`auto`,`auto`,`≈ physical cores reserved for auth; each concurrent hash holds a ~19 MiB arena`],[`Database CPU allocation`,`—`,`DB ≥ server cores`,`DB ≥ server cores if authz/userinfo-heavy (those paths scale with DB CPU; tokens don't)`,`2× server cores for read-heavy workloads`]]},{type:`note`,text:"`AXIAM__DB__POOL_SIZE` is a connection-pool sizing knob only; do not raise it expecting a throughput win. A pre-1.0 benchmark pass reported a one-off +7% on token issuance at `pool_size=4`, but that comparison was never confirmed on a settled measurement, and follow-up testing (`claude_dev/db-pool-design.md` §11) found no throughput difference between `pool_size=1` and `pool_size=8` under load. Leave it at the default `1` unless you have your own measured evidence for your deployment; `pool_size>1` still gives you independent per-connection session renewal, which is a robustness property worth having on its own, just not a speed one."},{type:`warn`,text:"Security caveat on `client_id` keying — read before changing the key mode. The `client_id` a request is bucketed by is read from the request body before the client credential is verified (that is what the OAuth2 spec puts there), so an attacker who simply varies the `client_id` string gets a fresh bucket each time and is effectively unlimited on the token, revoke and introspect endpoints. `client_id` keying is a fairness control between well-behaved clients, not an abuse control. `ip` is the only mode whose key an attacker cannot mint at will, which is why it stays the shipped default. Use `client_id`/`ip_client_id` only where something else already authenticates callers at the edge — mTLS, an API gateway, a WAF, or an IP allow-list — which is exactly the topology the M2M column assumes."},{type:`note`,text:`Two rules of thumb the data supports: if your traffic is authorization-check-heavy, spend hardware on the database first (those paths gained ~90% from a second pair of DB cores), then try the decision cache and measure its logged hit rate — it is transformative on gRPC checks and marginal on REST ones; if it's token-heavy, the limits — not the hardware — are what you'll hit first, so raise TOKEN_PER_MIN from your real per-client peak and — if and only if you have edge authentication per the caveat above — switch the key mode. The genuinely internet-exposed endpoints (login, register, password-reset, MFA) stay strict per-IP in every configuration, including under the gateway and mesh profiles.`},{type:`h`,id:`tls`,text:`Direct TLS termination (opt-in)`},{type:`p`,text:`By default the server binds plaintext and a proxy/ingress terminates TLS 1.3 in front of it. To terminate TLS inside the server process instead, set the following — the listener then binds with rustls restricted to TLS 1.3 only. When enabled, both paths are mandatory and the server fails fast at startup on a missing, unreadable or mismatched cert/key (it never falls back to plaintext).`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__SERVER__TLS__ENABLED`,`Enable in-process TLS 1.3 (default false).`,`true`],[`AXIAM__SERVER__TLS__CERT_PATH`,`Path to the PEM certificate chain (leaf first).`,`/etc/axiam/tls/tls.crt`],[`AXIAM__SERVER__TLS__KEY_PATH`,`Path to the PEM private key (PKCS#8, PKCS#1 or SEC1).`,`/etc/axiam/tls/tls.key`]]},{type:`note`,text:"The AMQP URL is assembled from the broker's own `RABBITMQ_DEFAULT_USER` / `RABBITMQ_DEFAULT_PASS` into `AXIAM__AMQP__URL` at the deployment layer. See Docker & Kubernetes for how the shipped compose file and manifests wire these together."}]}],d=[{slug:`deploy`,section:`Operate`,navLabel:`Docker & Kubernetes`,title:`Docker & Kubernetes`,intro:`Package AXIAM as a container and run it on Kubernetes with the shipped manifests.`,blocks:[{type:`h`,id:`docker`,text:`Docker`},{type:`p`,text:"`docker/` holds the Dockerfiles and the compose configurations. `docker-compose.dev.yml` runs only SurrealDB and RabbitMQ, for developing against a natively-run server. `docker-compose.prod.yml` runs the whole stack — server, admin console, database, broker and Vault — and is documented in the file itself as **not** intended for real production use."},{type:`code`,code:`just dev-up      # SurrealDB + RabbitMQ only
just prod-up     # the whole stack, including the Vault ceremony
just prod-down   # stop, keep volumes
just prod-clean  # stop and remove volumes`},{type:`p`,text:"`docker-compose.prod.yml` refuses to start without the database and broker credentials and the JWT keypair present in the environment — it uses Compose's fail-fast variable syntax rather than silently defaulting. Secret material lives in a gitignored `docker/.secrets/` directory, never in the compose file."},{type:`h`,id:`k8s`,text:`Kubernetes`},{type:`p`,text:"The manifests under `k8s/` are assembled by a kustomization and deploy `axiam-server`, the admin console, SurrealDB, RabbitMQ and Vault, together with the NetworkPolicies that keep them talking only to each other."},{type:`code`,caption:`apply`,code:`kubectl apply -k k8s/
kubectl -n axiam get pods
kubectl -n axiam logs deploy/axiam-server`},{type:`h`,id:`ports`,text:`What is exposed, and what is not`},{type:`table`,proseFirstCol:!0,headers:[`Port`,`Service`,`Exposure`],rows:[[`8090`,`REST API`,`Through the Ingress, TLS-terminated.`],[`8081`,`Admin console`,`Through the Ingress, TLS-terminated.`],[`50051`,`gRPC`,`**Deliberately not routed through the Ingress.** Reachable only via the in-cluster ClusterIP service.`],[`5671`,`AMQP`,"In-cluster only, TLS-only (`amqps://`)."],[`8200`,`Vault`,`In-cluster only, TLS.`]]},{type:`h`,id:`probes`,text:`Probes`},{type:`p`,text:"Point the liveness probe at `/health` and the readiness probe at `/ready`. The distinction matters: `/health` answers 200 whenever the process is up, while `/ready` checks database connectivity and answers 503 when it cannot reach SurrealDB — so a database blip takes replicas out of the load-balancer rotation instead of restarting them in a loop."},{type:`h`,id:`scaling`,text:`Scaling`},{type:`p`,text:"`axiam-server` is stateless — every piece of durable state is in SurrealDB — so replicas scale horizontally behind an ordinary load balancer with no session affinity required."},{type:`warn`,text:"One caveat when scaling out: the cross-replica shared rate-limit counter is **write-behind**, so enforcement across replicas is eventual rather than synchronous. Worst-case overshoot beyond a configured limit is roughly `(replicas − 1) × arrival_rate_per_replica × sync_interval`, and is zero on a single replica. The per-replica in-memory governor still caps overshoot independently."},{type:`h`,id:`tls`,text:`TLS`},{type:`p`,text:"By default the server binds plaintext and expects an ingress or proxy to terminate TLS 1.3 in front of it. To terminate inside the process instead, set `AXIAM__SERVER__TLS__ENABLED` with a certificate and key path; the listener then binds with rustls restricted to TLS 1.3 only, and fails fast at startup on a missing, unreadable or mismatched pair rather than falling back to plaintext."},{type:`warn`,text:`AXIAM is pre-1.0. Treat these manifests as a solid starting point for a staging environment, and work through [Production hardening](#/docs/hardening) before anything real depends on them.`}]},{slug:`secrets`,section:`Operate`,navLabel:`Secrets & Vault`,title:`Secrets & HashiCorp Vault`,intro:`AXIAM holds ten long-lived secrets. Two of them are the difference between "an attacker read your database" and "an attacker owns your identity provider".`,blocks:[{type:`h`,id:`what`,text:`The ten secrets`},{type:`table`,headers:[`Field`,`Shape`,`What losing it costs`],rows:[[`opaque_setup_key`,`32-byte hex`,`**A password reset for every user in every tenant.** It encrypts each tenant's OPRF seed; without it no OPAQUE record can be opened.`],[`jwt_private_key_pem`,`Ed25519 PEM`,`**Total compromise.** Whoever holds it can mint a token for any principal in any tenant.`],[`pki_encryption_key`,`32-byte hex`,`**CA compromise.** It encrypts CA private keys at rest; a leak lets an attacker issue certificates every mTLS client trusts.`],[`auth_pepper`,`text`,`Every stored password hash is invalidated if it *changes*; a leak makes offline attack on stolen hashes cheaper.`],[`opaque_session_key`,`32-byte hex`,`In-flight OPAQUE exchanges (120 s) are invalidated. Cheap to rotate — deliberately separate from the setup key.`],[`mfa_encryption_key`,`32-byte hex`,`Stored TOTP secrets become undecryptable.`],[`federation_encryption_key`,`32-byte hex`,`Stored IdP client secrets become undecryptable.`],[`email_encryption_key`,`32-byte hex`,`Stored email addresses become undecryptable.`],[`gdpr_pseudonym_pepper`,`32-byte hex`,`Existing audit pseudonyms stop linking to new ones; the audit trail breaks.`],[`jwt_public_key_pem`,`Ed25519 PEM`,`Not secret, but a mismatched pair is a confusing outage.`]]},{type:`h`,id:`providers`,text:`Three providers`},{type:`p`,text:'`AXIAM__AUTH__SECRET_PROVIDER` selects where the server fetches these at startup. An unknown value is **refused at startup** rather than falling back to `env`, because a typo that silently reverted a deployment to reading unset environment variables presents as "OPAQUE stopped working" with nothing in the logs explaining why.'},{type:`table`,proseFirstCol:!0,headers:[`Provider`,`Reads from`,`Use it for`],rows:[["`env` (default)","Environment variables — `AXIAM__AUTH__JWT_PRIVATE_KEY_PEM` and friends.","Local development. Fine for a workstation, poor for anything that has a `kubectl describe pod`."],["`file`","One file per secret in `AXIAM__AUTH__SECRET_DIR`, named after the field.",`Kubernetes and Docker secret volumes — the shape those already produce, with no Vault to run.`],["`vault`","A KV v2 secret at `<mount>/<path>` whose fields are named after the list above.",`Production. Rotation, audit, and a leak surface that is not "anything that can read the pod spec".`]]},{type:`note`,text:"AXIAM's actual requirement is small: a KV v2 secret whose fields carry those names. It does not care who runs Vault, whether it is HA, or how it is unsealed. If your organization already runs one, point `AXIAM__AUTH__VAULT_ADDR` at it, create the read-only policy, and skip the rest."},{type:`h`,id:`honesty`,text:`What Vault does and does not defend against`},{type:`warn`,text:"**Vault does not defend against a compromised application server.** It defends against the ways keys actually leak — a config file committed to git, an over-shared CI variable, a `kubectl describe pod`, a backup of an orchestrator's etcd. An attacker with code execution on the server can read whatever the server can read, and the server must be able to read these."},{type:`p`,text:"The construction that *would* defend against that is envelope encryption against a Transit-style API, where the plaintext key never leaves the KMS — at the cost of a network round trip per seal and open. The `SecretProvider` trait is the place to add it, and doing so requires no change to any authentication code."},{type:`h`,id:`local`,text:`Local development`},{type:`code`,code:`just dev-up       # SurrealDB + RabbitMQ. No Vault; provider stays \`env\`.
just vault-up     # adds a dev-mode Vault and seeds every secret into it
just vault-status # which secrets Vault holds — presence only, never values`},{type:`p`,text:`Dev-mode Vault is in-memory, starts unsealed, listens on plain HTTP and uses a fixed root token. Every one of those is wrong for production — which is exactly the point: it removes the unseal ceremony from an edit-compile-run loop.`},{type:`h`,id:`production`,text:`Production: what cannot be automated`},{type:`p`,text:"The manifests under `k8s/vault/` deploy a single-node Vault with file storage — production-*shaped*, not production-*ready*. Most teams should run HashiCorp's Helm chart with Raft and three to five replicas, or use an existing Vault. Either way these steps are what AXIAM needs, and none of them is safe to automate."},{type:`steps`,steps:[{title:`Give Vault's listener TLS`,body:`The token AXIAM presents on every fetch is a bearer credential for the setup key. Over plaintext, anything on the pod network can take it.`},{title:`Initialise — once, and never again`,body:`The output is printed once and cannot be recovered. It contains five unseal key shares and a root token. Give each share to a **different person** — three of five must cooperate to unseal, which is the whole point of the scheme. Do not put all five in one password manager, and do not store them with the root token.`,code:`kubectl exec -n axiam vault-0 -- vault operator init \\
    -key-shares=5 -key-threshold=3 -format=json`},{title:`Configure auto-unseal before you go live`,body:`Without it, every restart — a node drain, an upgrade, an OOM kill — leaves Vault sealed and AXIAM unable to start until three people are woken up. This is the single most important production step, and the one most often deferred.`},{title:`Create a read-only, single-path policy`,body:`A leaked AXIAM token must not be a key to the rest of your Vault.`,code:`vault policy write axiam - <<'EOF'
path "secret/data/axiam"     { capabilities = ["read"] }
path "secret/metadata/axiam" { capabilities = ["read"] }
EOF`},{title:`Seed the secrets`,body:`The seeding script **never regenerates a secret that already exists**, so re-running it after adding a key or after a restore is safe. That behaviour is unit-tested, precisely because getting it wrong would mean a password reset for every user.`,code:`scripts/vault-seed.sh`},{title:`Prefer Kubernetes auth over a static token`,body:`A static token works and is what the manifests ship with, because something has to bootstrap the trust chain. It does not rotate and it sits in etcd. With the Kubernetes auth method the Secret disappears and the pod authenticates with its ServiceAccount token, which Kubernetes rotates.`},{title:`Revoke the root token`,body:`Use it for the policy and seeding steps, then revoke it. Generate a new one with the root-generation ceremony on the rare occasions you need one.`}]},{type:`note`,text:`Only the *ciphertext* of the OPRF seeds lives in the database. Moving the seeds themselves to a file on the server volume would put them on the same side of the trust boundary as the key, break replicas, lose transactionality with the credential rows, and turn a lost volume into total tenant lockout.`}]},{slug:`settings`,section:`Operate`,navLabel:`Settings & policies`,title:`Settings & policies`,intro:`Password, lockout, MFA, token, certificate, email and OPAQUE policy — set as an organization baseline, and tightened per tenant.`,blocks:[{type:`h`,id:`shape`,text:`Baseline and overrides`},{type:`p`,text:"Policy is hierarchical. An **organization** holds the baseline; a **tenant** may override individual fields. An override of `null` means *inherit*, so a tenant only states what it wants to differ — and reading a tenant's effective settings returns every field fully resolved, with nothing left to work out by hand."},{type:`warn`,text:`Tenant overrides are **tighten-only**. A tenant can make its posture stricter than the organization baseline; it cannot make it looser. That is what keeps an organization-level security decision meaningful once tenants can edit their own configuration.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/organizations/{org_id}/settings`,summary:`Read the organization baseline.`},{method:`PUT`,path:`/api/v1/organizations/{org_id}/settings`,summary:`Set it.`},{method:`GET`,path:`/api/v1/settings`,summary:`Read the calling tenant's fully-resolved settings.`},{method:`PUT`,path:`/api/v1/settings`,summary:`Set the tenant's overrides.`}]},{type:`h`,id:`password`,text:`Password policy`},{type:`table`,headers:[`Field`,`Meaning`],rows:[[`min_length`,`Minimum password length.`],[`require_uppercase`,`Require at least one uppercase character.`],[`require_lowercase`,`Require at least one lowercase character.`],[`require_digits`,`Require at least one digit.`],[`require_symbols`,`Require at least one symbol.`],[`password_history_count`,`How many previous passwords a new one is checked against, so a rotation cannot cycle back.`],[`hibp_check_enabled`,`Reject passwords appearing in known-breach corpora. Worth more than any three complexity rules combined.`]]},{type:`note`,text:"If you are choosing where to spend policy strictness, spend it on `hibp_check_enabled` and length rather than on symbol classes. Composition rules push users toward predictable substitutions; breach-checking rejects the passwords attackers actually try."},{type:`h`,id:`lockout`,text:`Lockout policy`},{type:`table`,headers:[`Field`,`Meaning`],rows:[[`max_failed_login_attempts`,`Failures before the account locks.`],[`lockout_duration_secs`,`How long the first lock lasts.`],[`lockout_backoff_multiplier`,`Multiplier applied on each subsequent lock.`],[`max_lockout_duration_secs`,`Ceiling the backoff is clamped to.`]]},{type:`h`,id:`tokens`,text:`Token & MFA policy`},{type:`table`,headers:[`Field`,`Meaning`],rows:[[`access_token_lifetime_secs`,`How long an access token is valid. Shorter is better; 15 minutes is the default.`],[`refresh_token_lifetime_secs`,`How long a refresh token remains usable before re-authentication.`],[`mfa_enforced`,`Require a second factor for every user in scope.`],[`mfa_challenge_lifetime_secs`,`How long an issued MFA challenge stays answerable.`]]},{type:`h`,id:`other`,text:`Email, certificate & OPAQUE policy`},{type:`table`,headers:[`Field`,`Meaning`],rows:[[`email_verification_required`,`Whether an unverified address blocks sign-in.`],[`email_verification_grace_period_hours`,`How long an unverified account keeps working before it does.`],[`default_cert_validity_days`,`Validity applied to issued certificates when none is requested.`],[`max_cert_validity_days`,`Ceiling on requested certificate validity.`],[`opaque_mode`,"`disabled` | `optional` | `required` — see [OPAQUE](#/docs/opaque)."],[`opaque_suite`,"RFC 9807 ciphersuite. Default `ristretto255_sha512`."],[`opaque_ksf`,"Client key-stretching function. Default `argon2id`; `scrypt` is the alternative."]]},{type:`warn`,text:"`opaque_mode: required` is the one setting that can lock every user out of a tenant. Nobody can be enrolled retroactively, so it is only safe once every user already has a registration record. Run `optional` until enrolment is complete."}]},{slug:`pki`,section:`Operate`,navLabel:`PKI & certificates`,title:`PKI & certificates`,intro:`Per-tenant X.509 issuance under an organization CA, for users, services and IoT devices — plus the OpenPGP keys that sign the audit trail.`,blocks:[{type:`h`,id:`hierarchy`,text:`Certificate hierarchy`},{type:`p`,text:"An organization holds one or more **CA certificates**. Tenants issue leaf certificates under them, with RSA-4096 or Ed25519 keys. Private keys are **never stored server-side** — a leaf key is returned exactly once at issuance and cannot be recovered afterwards. CA signing keys are encrypted at rest with AES-256-GCM under `AXIAM__PKI__ENCRYPTION_KEY`."},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/organizations/{org_id}/ca-certificates`,summary:`List the organization's CAs.`},{method:`POST`,path:`/api/v1/organizations/{org_id}/ca-certificates`,summary:`Create one.`},{method:`POST`,path:`/api/v1/organizations/{org_id}/ca-certificates/{id}/revoke`,summary:`Revoke a CA.`},{method:`GET`,path:`/api/v1/certificates`,summary:`List issued leaf certificates in the tenant.`},{method:`POST`,path:`/api/v1/certificates`,summary:`Issue one. The private key is returned once.`},{method:`GET`,path:`/api/v1/certificates/{id}`,summary:`Read one.`},{method:`POST`,path:`/api/v1/certificates/{id}/revoke`,summary:`Revoke it.`}]},{type:`warn`,text:`Revoking a CA invalidates every certificate issued under it. That is the intended behaviour and it is not reversible — plan the blast radius before you do it, and prefer issuing under a fresh CA and migrating.`},{type:`h`,id:`mtls`,text:`mTLS for devices and services`},{type:`p`,text:`Certificate-based authentication gives machine identities the same tenant-scoped authorization as human users. A certificate is bound to a service account, and the TLS handshake is verified **in-process** — there is no proxy-asserted identity header in the trusted path.`},{type:`table`,headers:[`Variable`,`Values`,`Default`,`Meaning`],rows:[[`AXIAM__SERVER__TLS__CLIENT_AUTH`,`off | optional | required`,`off`,`Client-certificate policy.`],[`AXIAM__SERVER__TLS__CLIENT_CA_PATH`,`PEM bundle path`,`—`,`Trust anchors for client certificates.`]]},{type:`p`,text:"`optional` requests a certificate and verifies it if presented, while still accepting anonymous clients. `required` rejects the handshake outright without a verifying certificate. A misconfigured client-auth setup **fails fast at startup** rather than serving without verification."},{type:`h`,id:`iot`,text:`IoT commissioning`},{type:`p`,text:`This is the shape AXIAM was built for on the device side: a device is issued a certificate at commissioning, presents it for mTLS, and is authorized by the same RBAC engine as everything else. Where a device cannot show a browser to enrol, the [device authorization grant](#/docs/device-flow) covers the human-approval half.`},{type:`h`,id:`gnupg`,text:`OpenPGP keys`},{type:`p`,text:`PGP keys managed through the PKI layer sign the audit trail and encrypt data exports, so exported data and audit records are verifiable and confidential end to end.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/pgp-keys`,summary:`List the tenant's PGP keys.`},{method:`POST`,path:`/api/v1/pgp-keys`,summary:`Create one.`},{method:`POST`,path:`/api/v1/pgp-keys/{id}/encrypt`,summary:`Encrypt a payload to a key.`},{method:`POST`,path:`/api/v1/pgp-keys/{id}/revoke`,summary:`Revoke it.`},{method:`POST`,path:`/api/v1/pgp-keys/sign-audit-batch`,summary:`Sign a batch of audit records.`}]}]},{slug:`audit`,section:`Operate`,navLabel:`Audit logging`,title:`Audit logging`,intro:`An append-only, cryptographically signed record of every privileged action — tamper-evident rather than merely tamper-resistant.`,blocks:[{type:`h`,id:`appendonly`,text:`Append-only by design`},{type:`p`,text:`The audit log has no UPDATE and no DELETE path. Not "they are permission-guarded" — they do not exist. Records are chained and signed, so any attempt to alter or remove history is detectable after the fact rather than silent.`},{type:`p`,text:`Ingestion runs asynchronously over AMQP so that writing an audit record never slows down the operation being audited, and never fails it. Every authentication, authorization decision and administrative mutation across every tenant flows through the same pipeline.`},{type:`h`,id:`signing`,text:`Cryptographic signing`},{type:`p`,text:`Entries are signed with OpenPGP keys managed by the PKI layer, which gives you a trail verifiable independently of AXIAM itself. Combined with the chain between records, that is what makes the log tamper-*evident*: an attacker who can write to the database still cannot rewrite history without breaking a signature or a chain link.`},{type:`h`,id:`reading`,text:`Reading it`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/audit-logs`,summary:`Query the tenant's audit trail.`},{method:`GET`,path:`/api/v1/audit-logs/system`,summary:`System-level audit events.`}]},{type:`note`,text:"Reading audit logs is itself permission-guarded (`audit_logs:*`) and itself audited. An investigator's access to the trail is part of the trail."},{type:`h`,id:`gdpr`,text:`Audit and the right to erasure`},{type:`p`,text:"An append-only log and a legal obligation to erase data are in obvious tension. AXIAM resolves it by **pseudonymising** the actor identity rather than deleting records: on erasure, the identity is replaced by an HMAC-SHA256 pseudonym derived under `AXIAM__GDPR_PSEUDONYM_PEPPER`. The trail's integrity survives; the link to a natural person does not."},{type:`warn`,text:"Changing `AXIAM__GDPR_PSEUDONYM_PEPPER` breaks the linkage between existing pseudonyms and new ones — the same person will appear as two different actors either side of the change. Treat it as permanent."},{type:`h`,id:`retention`,text:`Retention`},{type:`p`,text:`Because the log only grows, retention is an operational decision you have to make deliberately: size the database for the volume your compliance obligation requires, and archive to cold storage rather than expecting the system to prune for you.`}]},{slug:`observability`,section:`Operate`,navLabel:`Health & observability`,title:`Health & observability`,intro:`Probes, structured logs, telemetry hooks and the audit trail — what AXIAM tells you about itself, and what it deliberately does not.`,blocks:[{type:`h`,id:`probes`,text:`Probes`},{type:`api`,endpoints:[{method:`GET`,path:`/health`,summary:`Liveness. Always 200 while the process is up.`,public:!0},{method:`GET`,path:`/ready`,summary:`Readiness. 200 only when SurrealDB answers; 503 otherwise.`,public:!0}]},{type:`p`,text:"Wire these to the right probes and the difference matters operationally: a database blip should remove a replica from the load-balancer rotation, not restart it. Pointing liveness at `/ready` converts a transient dependency failure into a restart loop that makes the outage worse."},{type:`h`,id:`logs`,text:`Logs`},{type:`p`,text:"`RUST_LOG` sets verbosity and filtering. Keep it narrow in production — `info` is the intended level, and raising it exposes internal module structure without telling you much you can act on."},{type:`warn`,text:`Two things are never in a log line, by construction: **plaintext credentials** and **token strings**. SDKs wrap secrets in redacting types, and error messages are explicitly forbidden from carrying tokens in messages, context fields or stack traces. If you find either in a log, that is a security bug worth reporting.`},{type:`p`,text:`The rate limiter emits observability lines of its own — including whether the shared cross-replica counter is reachable — which is the signal to watch when limits appear not to be applying as configured.`},{type:`h`,id:`telemetry`,text:`Client-side telemetry hooks`},{type:`p`,text:`Every SDK exposes telemetry hooks so a client can record request latency, retries, refresh attempts and authorization decisions into whatever tracing system it already uses. This is where per-request visibility lives — the hooks are in the client, where the span you want to attach to already exists.`},{type:`h`,id:`metrics`,text:`On metrics`},{type:`note`,text:"AXIAM does not currently expose a Prometheus-style `/metrics` endpoint. Today the sources of operational truth are the probes, the structured logs, the audit trail and client-side telemetry. If you need scrape-based metrics now, derive them from log ingestion rather than expecting an endpoint that is not there."},{type:`h`,id:`audit-as-obs`,text:`The audit trail is observability too`},{type:`p`,text:'For questions about *who did what*, the audit log is the authoritative source and is queryable through the API — it is not merely a compliance artefact. Authorization decisions carry their reason (`no_grant` versus `denied_by_rule`), which is usually the fastest way to answer "why was this refused?" without reconstructing the role model by hand.'},{type:`h`,id:`capacity`,text:`Capacity signals`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Usually means`],rows:[[`503s on login under load`,"The Argon2id concurrency semaphore is shedding. Raise `AXIAM__AUTH__MAX_CONCURRENT_HASHES` only if you have the memory — each permit holds a ~19 MiB arena."],[`429s from machine clients that share an egress IP`,"Per-IP buckets colliding behind NAT. This is the `gateway` rate-limit profile's exact use case."],[`Authorization checks slow before anything else does`,`Database CPU. Those paths scale with it — measured ~90% from a second pair of DB cores — while token issuance does not.`],["`/ready` flapping",`SurrealDB connectivity, not the server. Check the database before restarting replicas.`]]}]},{slug:`hardening`,section:`Operate`,navLabel:`Production hardening`,title:`Production hardening checklist`,intro:`Work through this before a deployment carries real identity traffic. Every item is something AXIAM leaves to you on purpose, because it cannot be decided from inside the process.`,blocks:[{type:`h`,id:`secrets`,text:`Secrets`},{type:`list`,items:["Set `AXIAM__AUTH__SECRET_PROVIDER` to `vault` or `file`. `env` is a development default, not a production one.","**Back up `opaque_setup_key`** if any tenant uses OPAQUE. Losing it is a password reset for every user in every tenant.","Back up `jwt_private_key_pem` and confirm the public key is its actual pair — a mismatch is a confusing outage rather than a clear error.",`Configure Vault auto-unseal *before* going live. Without it, any restart leaves AXIAM unable to start until three keyholders are woken up.`,`Distribute unseal shares to different people, and revoke the root token once the policy and seeding are done.`,`Confirm no real key material is in a compose file, a manifest, a CI variable or git history.`]},{type:`h`,id:`network`,text:`Network & TLS`},{type:`list`,items:["TLS 1.3 on every external listener — terminated at the ingress, or in-process with `AXIAM__SERVER__TLS__ENABLED`.",`**gRPC (50051) must not be publicly routed.** It is an internal service surface; the shipped manifests deliberately keep it off the Ingress.`,"AMQP must be `amqps://`. Any other scheme is refused at startup, so a failure here is a configuration error, not a silent downgrade.","Set `AXIAM__SERVER__CORS_ALLOWED_ORIGINS` to your actual admin origins. Empty disables cross-origin requests, which is the safe default.","Set `AXIAM__RATE_LIMIT__TRUSTED_HOPS` to the number of reverse proxies in front of you — otherwise every client shares one apparent IP and per-IP limits become meaningless."]},{type:`h`,id:`identity`,text:`Identity`},{type:`list`,items:[`Enforce MFA on administrative accounts. Prefer passkeys over TOTP where the browsers in use support them.`,`Create a second super-admin. Bootstrap cannot be run again to rescue you from losing the first.`,"Unset `AXIAM_BOOTSTRAP_ADMIN_EMAIL` after bootstrap completes — it is inert afterwards, and leaving it implies otherwise.","Turn on `hibp_check_enabled` and set a sensible `min_length` on the organization baseline.","Review the seeded roles. `super-admin` should have very few holders; most administrators want `admin`, and most humans want neither.",`Prefer service accounts with bound certificates over shared secrets for machine access.`]},{type:`h`,id:`limits`,text:`Rate limits & capacity`},{type:`list`,items:["Pick a rate-limit profile that matches your topology: `internet` for a public deployment, `gateway` behind a NAT or API gateway, `mesh` on a private network.","Leave the key mode at `ip` unless something already authenticates callers at the edge. `client_id` is minted by the caller, so it is a fairness control, not an abuse control.",`Size database CPU first if your traffic is authorization-check-heavy; size limits first if it is token-heavy.`,`Measure before enabling the decision cache, and check its logged hit rate afterwards — it is transformative on gRPC checks and marginal on REST ones.`]},{type:`h`,id:`ops`,text:`Operations`},{type:`list`,items:["Liveness on `/health`, readiness on `/ready`. Not the other way round.","Keep `RUST_LOG` at `info` and ship logs somewhere they survive the pod.",`Decide audit retention deliberately — the log only grows, and nothing prunes it for you.`,`Register relying parties for back-channel logout **before** you need to revoke an account in an incident.`,`If you run intercepting Reactors, decide their failure policy explicitly and monitor it. Fail-closed makes a Reactor outage your outage; fail-open makes it a silently unenforced control.`]},{type:`warn`,text:`One item this checklist cannot give you: AXIAM is pre-1.0 and its security posture is a self-assessment backed by tests and a threat model, not a certified third-party audit. Weigh that against what the deployment is protecting.`},{type:`cards`,cards:[{title:`The security model →`,body:`Threat model, cryptography choices and the shared-responsibility boundary.`,to:`security`},{title:`Configuration reference →`,body:`Every variable, its default, and a worked example value.`,to:`docs`,doc:`configuration`}]}]},{slug:`troubleshooting`,section:`Operate`,navLabel:`Troubleshooting`,title:`Troubleshooting`,intro:`The failures that come up most often, what they actually mean, and the fix.`,blocks:[{type:`h`,id:`startup`,text:`The server will not start`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Cause`,`Fix`],rows:[["`AXIAM__AUTH__SECRET_PROVIDER=... is not a known provider`","A typo in the provider name. AXIAM refuses rather than falling back to `env`.","Use exactly `env`, `file` or `vault`."],["`SECRET_PROVIDER=vault requires AXIAM__AUTH__VAULT_ADDR`",`The vault provider is selected but not fully configured.`,"Set `VAULT_ADDR` and `VAULT_TOKEN`; `VAULT_MOUNT` and `VAULT_PATH` have defaults."],[`Startup aborts on the TLS listener`,`Client authentication is enabled but the CA path is unset, unreadable, empty or malformed. A misconfigured mTLS server never starts serving.`,"Fix `AXIAM__SERVER__TLS__CLIENT_CA_PATH`, or set `CLIENT_AUTH=off`."],[`AMQP refused at startup`,"`AXIAM__AMQP__URL` does not use `amqps://`. AMQP is TLS-only.","Use the `amqps://` scheme."],[`The OIDC issuer is rejected`,"`AXIAM__AUTH__OAUTH2_ISSUER_URL` is a path, not an origin.","Use an origin — `https://iam.acme.dev`, not `https://iam.acme.dev/acme`."],[`Vault is sealed after a restart`,`No auto-unseal is configured.`,`Configure auto-unseal. Until then, unseal manually with the key threshold.`]]},{type:`h`,id:`config`,text:`A setting has no effect`},{type:`warn`,text:"The most common cause by far: a **single underscore** after the prefix. `AXIAM_DB__USERNAME` is silently ignored and the in-code default wins; `AXIAM__DB__USERNAME` is read. The one exception is `AXIAM_BOOTSTRAP_ADMIN_EMAIL`, which is genuinely single-underscore because it is read directly rather than through the layered config."},{type:`list`,items:["`AXIAM__DB__URL` takes a bare `host:port`, **not** a URL scheme. The engine resolves a scheme as a hostname and fails.",`Rate limits appearing not to apply across replicas: the shared counter is write-behind, so cross-replica enforcement is eventual. Check the limiter's own log lines.`,"Limits applying to everyone at once: `AXIAM__RATE_LIMIT__TRUSTED_HOPS` is unset behind a proxy, so every client looks like the proxy's IP."]},{type:`h`,id:`auth-issues`,text:`Authentication problems`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Cause`,`Fix`],rows:[["`403` from bootstrap",`The gate is not satisfied — email mismatch, or a missing/consumed setup token.`,`The error names which. Check the variable is set on the *server* process and matches exactly.`],["`409` from bootstrap",`Bootstrap already completed. It is one-shot, enforced by a uniqueness invariant.`,`Log in as the existing admin; create further tenants through the API.`],[`Every OPAQUE login fails after a config change`,"`opaque_setup_key` changed or is being read from a different provider, so tenant OPRF seeds cannot be decrypted.",`Restore the original key. Without it, every user needs a password reset.`],[`OPAQUE login fails for everyone in one tenant`,"`opaque_mode` was set to `required` before users enrolled. Nobody can be enrolled retroactively.","Set it back to `optional`, let enrolment complete, then flip."],[`Tokens stop verifying after a restart`,`The JWT keypair changed, or the private and public keys are not a pair.`,`Restore the original keypair from your secret store.`],[`Passkey registration buttons never appear`,`The browser does not support WebAuthn, or the page is not on a secure origin.`,`WebAuthn requires HTTPS (or localhost). Other sign-in methods are unaffected.`],[`Passkeys are rejected after setting an attestation policy`,"Platform authenticators commonly attest as `none` for privacy reasons.",`A strict attestation policy is a security-key-only policy in practice. Decide which you meant.`]]},{type:`h`,id:`authz-issues`,text:`Authorization problems`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Cause`,`Fix`],rows:[[`A user is denied despite holding an allowing role`,`A **deny** grant applies — possibly inherited from an ancestor resource or through a group. Deny beats every allow.`,`Find the deny and narrow it. Adding another allow will not help; that is the design.`],["Denied with `no_grant` rather than `denied_by_rule`",`Nothing granted the action at all — default deny, not an explicit refusal.`,`Grant the permission, or check that the resource id and action name are what you think they are.`],[`A scoped allow behaves as if it were not there`,`A resource-level deny with no scopes is a wildcard and masks every scope for that action.`,`Narrow the deny to the specific scopes you meant to exclude.`],[`A revocation seems not to take effect`,`Almost never the decision cache — access-narrowing mutations invalidate immediately. Check group membership and inherited roles first.`,`Trace the role model; the TTL is a backstop, not the mechanism.`]]},{type:`h`,id:`build`,text:`Build problems`},{type:`list`,items:["**`libxml2` headers missing** — the default `saml` feature links `libxml`. Build with `--no-default-features`, which is what CI's *Build (SAML off)* job does.","**MSRV complaints from `axiam-opaque`** — it states its own floor (1.88) rather than inheriting the workspace's, because it is vendored into SDKs with lower MSRVs. That is deliberate."]},{type:`note`,text:`Not covered here? The authorization decision reason, the audit trail and the limiter's log lines answer most "why did that happen" questions between them. For a suspected security issue, use the private advisory process rather than a public issue.`}]}],f=[{slug:`compliance`,section:`Reference`,navLabel:`Standards & compliance`,title:`Standards & compliance`,intro:`Which specifications AXIAM implements, how conformance is evidenced, and — just as importantly — what the evidence is not.`,blocks:[{type:`h`,id:`honesty`,text:`What these claims mean`},{type:`warn`,text:`Everything on this page is a **self-assessment backed by test evidence**, not a third-party certification. Each conformance matrix in the repository maps a normative MUST to the test that exercises it, so a claim can be checked rather than believed. That is stronger than a marketing bullet and weaker than an audit — treat it accordingly.`},{type:`h`,id:`protocols`,text:`Protocol conformance`},{type:`table`,proseFirstCol:!0,headers:[`Specification`,`Coverage`,`Evidence`],rows:[[`RFC 6749 — OAuth 2.0`,`Authorization code, client credentials and refresh grants, with the error semantics.`,"`docs/compliance/oauth2-rfc-compliance.md` — a MUST-by-MUST matrix naming the test for each row."],[`RFC 7636 — PKCE`,`S256 challenge on the authorization code grant.`,`Same matrix.`],[`RFC 7009 — Token revocation`,"`/oauth2/revoke`.",`Same matrix.`],[`RFC 7662 — Token introspection`,"`/oauth2/introspect`.",`Same matrix.`],[`RFC 8628 — Device authorization grant`,`The full non-interactive path.`,"`docs/api/device-flow.md`."],[`RFC 8693 — Token exchange`,`Delegation and impersonation, narrowing-only.`,"`docs/api/token-exchange.md`."],[`RFC 9126 — Pushed authorization requests`,"`/oauth2/par`, requirable per client.",`OAuth2 test suite.`],[`OpenID Connect Core 1.0 & Discovery 1.0`,"ID tokens, userinfo, discovery. `alg: none` is excluded from the discovery document and rejected at verification.","`docs/compliance/oidc-conformance.md`."],[`OIDC RP-Initiated & Back-Channel Logout 1.0`,`Session-scoped logout in both directions.`,"`docs/api/logout.md`."],[`UMA 2.0`,`Protection API and the ticket grant.`,"`docs/api/uma.md`, CONTRACT §20."],[`FAPI 2.0 Security Profile (Final)`,`An opt-in constraint bundle that a client cannot half-apply.`,"`docs/admin/fapi2-profile.md`."],[`SCIM 2.0 — RFC 7643 / 7644`,`Users and Groups CRUD plus PATCH; bulk and complex filters deliberately unsupported.`,"`docs/api/scim-provisioning.md`."],[`WebAuthn / FIDO2`,`Registration and authentication, with MDS3-backed attestation policy.`,"`docs/admin/authenticator-policies.md`."],[`RFC 9807 — OPAQUE`,`One audited implementation bound by every SDK, tested against shared vectors.`,"`sdks/opaque-test-vectors.json`, CONTRACT §23."]]},{type:`h`,id:`security`,text:`Security standards`},{type:`table`,proseFirstCol:!0,headers:[`Standard`,`Posture`],rows:[[`OWASP ASVS 4.0.3 Level 2`,`Control-by-control checklist over V2, V3, V4, V6, V7, V8, V9, V10 and V14. Every in-scope control carries an explicit status — Pass, N/A or Deferred — with the deferrals tracked by finding id rather than left blank.`],[`GDPR`,`Data export (Art. 15) and erasure (Art. 17) endpoints, with audit-actor pseudonymisation so an append-only trail can coexist with a right to erasure. Exports can be PGP-encrypted.`],[`ISO 27001`,`Targeted through access control, cryptography and audit logging — the technical controls, not the management system, which is an organizational matter AXIAM cannot supply.`],[`EU Cyber Resilience Act`,`Secure-by-default posture and a private vulnerability-disclosure process.`]]},{type:`h`,id:`crypto`,text:`Cryptography at a glance`},{type:`table`,proseFirstCol:!0,headers:[`Use`,`Algorithm`],rows:[[`Password hashing`,`Argon2id, OWASP-recommended parameters, with a server-side pepper`],[`Password-authenticated key exchange`,`OPAQUE (RFC 9807), ristretto255-SHA512, Argon2id or scrypt as the client KSF`],[`Token signing`,`EdDSA (Ed25519)`],[`Secrets at rest`,`AES-256-GCM — MFA secrets, CA keys, federation secrets, OPRF seeds, email`],[`Webhook signatures`,`HMAC-SHA256`],[`Audit signing & exports`,`OpenPGP`],[`Certificates`,`X.509 with RSA-4096 or Ed25519`],[`Transport`,`TLS 1.3 minimum; the native listener is TLS 1.3-only by policy`]]},{type:`h`,id:`supply`,text:`Supply chain & CI`},{type:`list`,items:[`The OpenAPI document is **drift-gated** — a PR fails if the committed spec diverges from a fresh export from the server.`,"The protobuf contract is guarded by `buf lint` and `buf breaking` on every change.",`Crate-layering invariants are enforced in CI: dependencies point inward, and adding a crate without placing it in the layering table is itself a failure.`,`Remediation records are verified to resolve to commits reachable from the default branch — a claimed fix on an unmerged branch does not count as shipped.`,`Every SDK is tested against the same cross-language behavioural contract and the same OPAQUE test vectors.`]},{type:`cards`,cards:[{title:`The security model →`,body:`Threat model, trust boundaries and shared responsibility.`,to:`security`},{title:`Production hardening →`,body:`What compliance leaves to you, as a checklist.`,to:`docs`,doc:`hardening`}]}]},{slug:`sdks`,section:`Reference`,navLabel:`Client SDKs`,title:`Client SDKs`,intro:`Eleven official client libraries, all conforming to one cross-language behavioral contract — so an integration ported between languages keeps the same semantics.`,blocks:[{type:`h`,id:`contract`,text:`One contract, many languages`},{type:`p`,text:"AXIAM ships SDKs for Rust, TypeScript, Python, Java, C#, PHP, Go, Kotlin, Swift, C and C++. Each lives in its own repository, and each vendors the same `CONTRACT.md`, OpenAPI document and protobuf definitions — so behaviour is identical whichever language you pick, and a difference between two SDKs is a bug in one of them rather than a matter of taste."},{type:`p`,text:`The contract is not a style guide. It specifies the error taxonomy, CSRF behaviour, the cookie-jar requirement, tenant and organization context, TLS policy, the redacting secret type, the AMQP HMAC construction, the single-flight refresh guard, route-guard interfaces, declarative authorization helpers, OIDC relying-party helpers, webhook verification, the device grant, token exchange, retry policy, deterministic shutdown, telemetry hooks, UMA, FAPI 2.0, Reactors and OPAQUE.`},{type:`table`,proseFirstCol:!0,headers:[`Tier`,`Languages`,`Coverage`],rows:[[`Full contract`,`Rust, TypeScript, Python, Java, C#, PHP, Go`,`The complete §1–§11 surface including the gRPC and AMQP transports.`],[`REST surface`,`Kotlin, Swift, C, C++`,`§1–§7 and §9–§11, including §6.1 mTLS. gRPC and AMQP are planned follow-ups.`]]},{type:`h`,id:`guarantees`,text:`What every SDK guarantees`},{type:`list`,items:[`**Tenant is an explicit constructor parameter.** There is no default tenant and no ambient context to get wrong.`,`**Secrets are wrapped in a redacting type** that cannot be printed, logged or serialised by accident.`,"**Browser code never touches a token.** Tokens live in `httpOnly` cookies; the SDK forwards the CSRF token for you.",`**Concurrent 401s collapse into one refresh**, not N — the single-flight guard is normative, not an optimisation.`,`**TLS is strict by default**, with mTLS supported everywhere including the REST-tier SDKs.`,`**One OPAQUE implementation** is bound by all of them — compiled in, through WebAssembly, or behind a C ABI. No SDK implements OPAQUE itself.`,`**Declarative route guards** in the idiom of the language — attribute, annotation, decorator, macro or middleware.`]},{type:`h`,id:`pick`,text:`Pick your language`},{type:`cards`,cards:[{title:`All SDKs →`,body:`Install snippets, quickstarts and guard examples for every language.`,to:`sdks`},{title:`Error reference →`,body:`The three error types and the status mapping every SDK implements.`,to:`docs`,doc:`errors`}]}]}],p=[...a,...o,...s,...c,...l,...u,...d,...f],m=[{label:`Getting started`,slugs:[`overview`,`quickstart`,`installation`,`bootstrap`,`concepts`]},{label:`Authentication`,slugs:[`auth`,`opaque`,`mfa`,`passkeys`,`federation`,`service-accounts`]},{label:`Authorization`,slugs:[`authz`,`rbac`,`deny`,`uma`]},{label:`OAuth2 & OIDC`,slugs:[`oauth2`,`device-flow`,`token-exchange`,`logout`,`fapi2`]},{label:`APIs & integration`,slugs:[`rest`,`grpc`,`amqp`,`scim`,`webhooks`,`reactors`,`errors`]},{label:`Operate`,slugs:[`deploy`,`configuration`,`secrets`,`settings`,`pki`,`audit`,`observability`,`hardening`,`troubleshooting`]},{label:`Reference`,slugs:[`compliance`,`sdks`]}],h=m.flatMap(e=>e.slugs);function g(e){return p.find(t=>t.slug===e)}var _=n(),v=`overview`,y=()=>{typeof window<`u`&&window.scrollTo(0,0)};function b(){if(typeof window>`u`)return null;let e=/^#\/docs\/([a-z0-9-]+)$/.exec(window.location.hash)?.[1];return e&&g(e)?e:null}function x(e){switch(e.type){case`h`:case`p`:case`note`:case`warn`:return e.text;case`list`:return e.items.join(` `);case`code`:return`${e.caption??``} ${e.code}`;case`table`:return`${e.headers.join(` `)} ${e.rows.flat().join(` `)}`;case`cards`:return e.cards.map(e=>`${e.title} ${e.body}`).join(` `);case`links`:return e.links.map(e=>`${e.label} ${e.note??``}`).join(` `);case`steps`:return e.steps.map(e=>`${e.title} ${e.body} ${e.code??``}`).join(` `);case`api`:return e.endpoints.map(e=>`${e.method} ${e.path} ${e.summary}`).join(` `);case`codegroup`:return e.tabs.map(e=>`${e.label} ${e.code}`).join(` `)}}var S=p.map(e=>({slug:e.slug,navLabel:e.navLabel,section:e.section,title:e.title,intro:e.intro,haystack:[e.title,e.navLabel,e.section,e.intro,...e.blocks.map(x)].join(` `).toLowerCase()}));function C(e){let t=e.trim().toLowerCase();if(t.length<2)return[];let n=t.split(/\s+/);return S.filter(e=>n.every(t=>e.haystack.includes(t))).sort((e,n)=>!e.title.toLowerCase().includes(t)-+!n.title.toLowerCase().includes(t)).slice(0,8)}function w({go:e}){let[n,a]=(0,i.useState)(()=>b()??v),[o,s]=(0,i.useState)(``);(0,i.useEffect)(()=>{let e=()=>{let e=b();e&&a(e)};return window.addEventListener(`hashchange`,e),()=>window.removeEventListener(`hashchange`,e)},[]),(0,i.useEffect)(()=>{},[]);let c=g(n)??p[0],l=(0,i.useMemo)(()=>c.blocks.filter(e=>e.type===`h`).map(e=>({id:e.id,text:e.text})),[c]),u=(0,i.useMemo)(()=>C(o),[o]),d=h.indexOf(c.slug),f=d>0?g(h[d-1]):void 0,x=d>=0&&d<h.length-1?g(h[d+1]):void 0,S=e=>{a(e),s(``),typeof window<`u`&&(window.location.hash=`#/docs/${e}`),y()};return(0,_.jsxs)(`div`,{className:`ax-docs`,children:[(0,_.jsxs)(`aside`,{className:`ax-docs-side`,children:[(0,_.jsxs)(`div`,{style:{margin:`0 0 18px`,padding:`0 12px`},children:[(0,_.jsx)(`input`,{type:`search`,value:o,onChange:e=>s(e.target.value),placeholder:`Search the docs…`,"aria-label":`Search the documentation`,style:{width:`100%`,boxSizing:`border-box`,padding:`8px 11px`,borderRadius:9,border:`1px solid rgba(0,212,255,.2)`,background:`rgba(0,0,0,.28)`,color:`#e2e8f0`,fontSize:13.5,outline:`none`}}),o.trim().length>=2&&(0,_.jsx)(`div`,{style:{marginTop:8},children:u.length===0?(0,_.jsxs)(`div`,{style:{fontSize:12.5,color:`#64748b`,padding:`6px 2px`},children:[`No matches for “`,o.trim(),`”.`]}):u.map(e=>(0,_.jsxs)(`button`,{className:`ax-side`,onClick:()=>S(e.slug),style:{textAlign:`left`},children:[(0,_.jsx)(`span`,{style:{display:`block`},children:e.navLabel}),(0,_.jsx)(`span`,{style:{display:`block`,fontSize:11,color:`#64748b`},children:e.section})]},e.slug))})]}),m.map((e,t)=>(0,_.jsxs)(`div`,{children:[(0,_.jsx)(`div`,{style:{fontSize:11,textTransform:`uppercase`,letterSpacing:`.14em`,color:`#64748b`,margin:t===0?`0 0 10px 14px`:`22px 0 10px 14px`},children:e.label}),e.slugs.map(e=>{let t=g(e);return t?(0,_.jsx)(`button`,{className:`ax-side${e===n?` ax-side-active`:``}`,"aria-current":e===n?`page`:void 0,onClick:()=>S(e),children:t.navLabel},e):null})]},e.label))]}),(0,_.jsxs)(`article`,{style:{minWidth:0},children:[(0,_.jsxs)(`div`,{style:{fontSize:13,color:`#64748b`,marginBottom:10},children:[c.section,` /`,` `,(0,_.jsx)(`span`,{style:{color:`#94a3b8`},children:c.navLabel})]}),(0,_.jsx)(`h1`,{style:{margin:`0 0 8px`,fontSize:`clamp(30px, 5vw, 40px)`,fontWeight:800,letterSpacing:`-.02em`},children:c.title}),(0,_.jsx)(`p`,{style:{fontSize:17,color:`#94a3b8`,margin:`0 0 30px`},children:t(c.intro)}),c.blocks.map((t,n)=>(0,_.jsx)(r,{block:t,go:e},n)),(f||x)&&(0,_.jsxs)(`nav`,{"aria-label":`Documentation pages`,style:{display:`flex`,gap:14,marginTop:44,paddingTop:24,borderTop:`1px solid rgba(255,255,255,.08)`},children:[f&&(0,_.jsxs)(`button`,{className:`glass-card ax-lift`,onClick:()=>S(f.slug),style:{flex:1,padding:16,textAlign:`left`,cursor:`pointer`,border:`1px solid rgba(0,212,255,.14)`,color:`inherit`},children:[(0,_.jsx)(`div`,{style:{fontSize:12,color:`#64748b`,marginBottom:4},children:`← Previous`}),(0,_.jsx)(`div`,{style:{fontWeight:700,color:`#e2e8f0`},children:f.navLabel})]}),x&&(0,_.jsxs)(`button`,{className:`glass-card ax-lift`,onClick:()=>S(x.slug),style:{flex:1,padding:16,textAlign:`right`,cursor:`pointer`,border:`1px solid rgba(0,212,255,.14)`,color:`inherit`},children:[(0,_.jsx)(`div`,{style:{fontSize:12,color:`#64748b`,marginBottom:4},children:`Next →`}),(0,_.jsx)(`div`,{style:{fontWeight:700,color:`#e2e8f0`},children:x.navLabel})]})]})]}),(0,_.jsxs)(`aside`,{className:`ax-docs-toc`,children:[(0,_.jsx)(`div`,{style:{textTransform:`uppercase`,letterSpacing:`.12em`,color:`#64748b`,marginBottom:12,fontSize:11},children:`On this page`}),l.map((e,t)=>(0,_.jsx)(`a`,{href:`#${e.id}`,style:{display:`block`,color:t===0?`#67e8f9`:`#94a3b8`,padding:`5px 0`,...t===0?{borderLeft:`2px solid #00d4ff`,paddingLeft:10,marginLeft:-12}:{}},children:e.text},e.id))]})]})}export{w as default};