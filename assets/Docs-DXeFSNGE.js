import{c as e,l as t,n,s as r,t as i,u as a}from"./index-BIXUafyT.js";var o=a(),s=[{slug:`overview`,section:`Getting started`,navLabel:`What is AXIAM?`,title:`What is AXIAM?`,intro:`A multi-tenant identity and access management server written in Rust — OAuth2 and OpenID Connect provider, RBAC authorization engine, PKI, and an audit trail, reachable over REST, gRPC and AMQP.`,blocks:[{type:`h`,id:`what`,text:`In one paragraph`},{type:`p`,text:`AXIAM issues and validates identities, and answers the question *may this principal perform this action on this resource?* It is a complete OAuth2 authorization server and OpenID Connect provider, a role-based authorization engine that evaluates over a hierarchy of resources, a certificate authority for machine identities, and an append-only audit log — all of it partitioned by tenant, so one deployment can serve many isolated customers without any of them being able to observe another.`},{type:`p`,text:`It is built for the two workloads that stress an IAM system in different directions: **microservices**, where an authorization check happens on nearly every request and tail latency is the constraint, and **IoT fleets**, where identities are certificates rather than passwords and devices commission themselves without a browser.`},{type:`h`,id:`capabilities`,text:`What it does`},{type:`table`,proseFirstCol:!0,headers:[`Area`,`Capability`],rows:[[`Authentication`,`Password (Argon2id), OPAQUE (RFC 9807, password never leaves the client), TOTP, WebAuthn passkeys and security keys, X.509 client certificates, and federated sign-in via SAML or OIDC.`],[`Authorization`,`Role-based, default-deny, with deny-override precedence, a cascading resource hierarchy, sub-resource scopes, groups, service accounts, and UMA 2.0 for resource servers that guard what they do not own.`],[`Standards`,`OAuth2 (authorization code + PKCE, client credentials, refresh, device grant, token exchange, PAR), OpenID Connect with discovery and RP-initiated plus back-channel logout, FAPI 2.0 Security Profile, SCIM 2.0 provisioning.`],[`Machine identity`,`Per-tenant X.509 issuance under an organization CA, mTLS authentication terminated in-process, certificate-bound access tokens, and OpenPGP keys for audit signing and encrypted exports.`],[`Operations`,`Append-only signed audit log, webhooks, Reactors (external hook actors on the AMQP bus), pluggable secret providers including HashiCorp Vault, and GDPR export/erasure endpoints.`]]},{type:`h`,id:`protocols`,text:`Three protocols, one engine`},{type:`p`,text:`The same domain logic is reachable three ways. Choosing between them is a transport decision, not a capability one — an authorization check returns the same answer whichever door it arrives through.`},{type:`table`,proseFirstCol:!0,headers:[`Protocol`,`Use it for`,`Spec`],rows:[[`REST`,`Administration, the admin console, and any HTTP integrator. The broadest surface — every entity is manageable here.`,"OpenAPI 3.1 (`sdks/openapi.json`)"],[`gRPC`,`The hot path inside a service mesh: authorization checks, token validation and user lookups where connection reuse and binary framing matter.`,"Protocol Buffers (`proto/axiam/v1/`)"],[`AMQP`,`Deferred authorization decisions, audit ingestion, mail, webhook and notification delivery, and Reactor hooks.`,"AsyncAPI 2.6 (`docs/api/asyncapi.yml`)"]]},{type:`h`,id:`shape`,text:`How a deployment is shaped`},{type:`code`,caption:`the runtime picture`,code:`  Browsers · Mobile · IoT devices · Services · Admin console
        |             |              |
     REST/HTTPS    gRPC/mTLS       AMQP
        |             |              |
        v             v              v
  +-------------------------------------------+
  |               axiam-server                |
  |   Actix-Web  |  Tonic  |  Lapin consumer  |
  +-------------------------------------------+
  |  AuthN · AuthZ · OAuth2/OIDC · Federation |
  |  PKI · SCIM · Audit · Email · Reactors    |
  +-------------------------------------------+
        |                        |
        v                        v
   SurrealDB                RabbitMQ
  (all domain state)   (async work + events)
        |
        v
   HashiCorp Vault  (long-lived secrets, production)`},{type:`p`,text:"`axiam-server` is a single binary composing a workspace of focused crates. It is stateless: every piece of durable state lives in SurrealDB, so replicas scale horizontally behind an ordinary load balancer."},{type:`h`,id:`status`,text:`Project status`},{type:`warn`,text:`AXIAM is pre-1.0 and under active development. It should not carry production identity traffic until it reaches a stable release. The security posture described throughout these docs is a self-assessment backed by tests and a threat model — not a certified third-party audit.`},{type:`h`,id:`start`,text:`Where to go next`},{type:`cards`,cards:[{title:`Quickstart →`,body:`Run the stack and make your first authorization check.`,to:`docs`,doc:`quickstart`},{title:`Browse the SDKs →`,body:`Install snippets and quickstarts for eleven languages.`,to:`sdks`},{title:`See the benchmarks →`,body:`Measured throughput, latency and the cost of each security tier.`,to:`bench`},{title:`Read the security model →`,body:`Threat model, cryptography choices and shared responsibility.`,to:`security`}]}]},{slug:`quickstart`,section:`Getting started`,navLabel:`Quickstart`,title:`Quickstart`,intro:`Get a local AXIAM instance running, create your first admin, then make an authenticated authorization check — in about ten minutes.`,blocks:[{type:`h`,id:`prereq`,text:`Prerequisites`},{type:`p`,text:"AXIAM builds with Rust 1.93+ and runs its dev infrastructure (SurrealDB + RabbitMQ) in Docker. The `just` task runner wraps the common commands. If you only want to *use* a running instance, you need none of this — skip to [your first check](#authz) and point an SDK at your server."},{type:`h`,id:`run`,text:`1. Start the stack`},{type:`code`,caption:`terminal`,code:`# clone and enter the repository
git clone https://github.com/ilpanich/axiam.git
cd axiam

# start dev infrastructure (SurrealDB + RabbitMQ)
just dev-up

# build and run the server
just build
just run`},{type:`p`,text:"The REST API comes up on `http://localhost:8090` and gRPC on `localhost:50051`. Confirm it is alive and that it can reach the database:"},{type:`code`,code:`curl -s localhost:8090/health   # liveness  — always 200 if the process is up
curl -s localhost:8090/ready    # readiness — 200 only when SurrealDB answers`},{type:`h`,id:`admin`,text:`2. Create the first admin`},{type:`p`,text:`A fresh deployment has no users at all, and the bootstrap endpoint that creates the first one is fail-closed — it refuses unless you prove you are the operator. The full procedure, both gates and the seeded role set are on [First organization, tenant and admin](#/docs/bootstrap).`},{type:`code`,caption:`the short version`,code:`# Lock bootstrap to a known address before starting the server
export AXIAM_BOOTSTRAP_ADMIN_EMAIL=admin@acme.dev

curl -X POST localhost:8090/api/v1/admin/bootstrap \\
  -H 'content-type: application/json' \\
  -d '{
        "organization_name": "Acme",
        "tenant_name": "Production",
        "email": "admin@acme.dev",
        "username": "admin",
        "password": "'"$ADMIN_PASSWORD"'"
      }'`},{type:`h`,id:`authz`,text:`3. Your first authorization check`},{type:`p`,text:"Install a client SDK, construct a client for your tenant, sign in, then call `can()`. Tenant is always explicit — AXIAM is multi-tenant and has no default tenant, so there is no ambient context to get wrong."},{type:`codegroup`,caption:`authenticate, then check`,tabs:[{label:`TypeScript`,code:`import { AxiamClient } from 'axiam-sdk';

const client = new AxiamClient({
  baseUrl: 'https://iam.acme.dev',
  tenantSlug: 'acme',
  orgSlug: 'acme',
});

const result = await client.login(email, password);
if (result.status === 'mfa_required') {
  await client.verifyMfa(result.mfaToken, code);
}

const allowed = await client.can('read', 'doc:1');`},{label:`Python`,code:`from axiam_sdk import AxiamClient

with AxiamClient(
    base_url="https://iam.acme.dev",
    tenant_slug="acme",
    org_slug="acme",
) as client:
    result = client.login(email, password)
    if result.mfa_required:
        result = client.verify_mfa(result.mfa_token, totp_code)

    allowed = client.can("read", resource_id)`},{label:`Rust`,code:`use axiam_sdk::AxiamClient;

let client = AxiamClient::builder()
    .base_url("https://iam.acme.dev")?
    .tenant_slug("acme")
    .org_slug("acme")
    .build()?;

let result = client.login("user@acme.dev", &password).await?;
if result.mfa_required {
    client.verify_mfa("123456").await?;
}

let allowed = client.can("read", resource_id, None).await?;`},{label:`Go`,code:`import axiam "github.com/ilpanich/axiam-go-sdk"

// tenantSlug is required — there is no default tenant. Login also needs
// organization context: a tenant slug is only unique within an org.
client, err := axiam.NewClient(baseURL, "acme", axiam.WithOrgSlug("acme"))

result, err := client.Login(ctx, email, password)
if result.MFARequired {
    // complete the challenge
}

allowed, err := client.Can(ctx, "read", resourceID)`},{label:`Java`,code:`try (AxiamClient client = AxiamClient.builder("https://iam.acme.dev", "acme")
        .orgSlug("acme")
        .build()) {
    LoginResult result = client.login("user@acme.dev", password);
    if (result.mfaRequired()) {
        result = client.verifyMfa(result.challengeToken(), "123456");
    }

    boolean allowed = client.can("read", "doc:1");
}`}]},{type:`note`,text:"In a browser, tokens arrive only via `httpOnly` cookies — CSRF forwarding and single-flight refresh are handled for you, and browser code imports only from `axiam-sdk/rest`. There is no code path in any SDK that puts a token in `localStorage`."},{type:`h`,id:`curl`,text:`The same thing over plain HTTP`},{type:`p`,text:"No SDK required — every capability is reachable with an HTTP client. Two things to know before you start: **login returns no token in its body**. It sets three cookies — `axiam_access`, `axiam_refresh` and a non-`httpOnly` `axiam_csrf` — so a raw HTTP client needs a cookie jar. And every state-changing request must echo the CSRF cookie back in an `X-CSRF-Token` header; `/auth/login`, the OPAQUE start endpoints and everything under `/oauth2/` are exempt, because their callers have no CSRF cookie yet."},{type:`code`,caption:`login, then check — with a cookie jar`,code:`# 1. authenticate. The tokens come back as cookies, not in the body.
curl -sS -c jar.txt -X POST https://iam.acme.dev/api/v1/auth/login \\
  -H 'content-type: application/json' \\
  -d '{"org_slug":"acme","tenant_slug":"production",
       "username_or_email":"admin@acme.dev","password":"..."}'

# 2. state-changing calls echo the CSRF cookie in a header.
CSRF=$(awk '/axiam_csrf/ {print $7}' jar.txt)

curl -sS -b jar.txt -X POST https://iam.acme.dev/api/v1/authz/check \\
  -H "x-csrf-token: $CSRF" \\
  -H 'content-type: application/json' \\
  -d '{"action":"read","resource_id":"doc:1"}'`},{type:`note`,text:"A **machine** client does not do any of this. It uses the OAuth2 client-credentials grant, which *does* return `access_token` in the response body, and then sends `Authorization: Bearer <token>` — no cookie jar and no CSRF header. See [Service accounts](#/docs/service-accounts)."},{type:`h`,id:`next`,text:`Next steps`},{type:`cards`,cards:[{title:`Browse the SDKs →`,body:`Quickstarts for all eleven languages.`,to:`sdks`},{title:`See the benchmarks →`,body:`Performance, efficiency and security posture.`,to:`bench`}]}]},{slug:`installation`,section:`Getting started`,navLabel:`Installation`,title:`Installation`,intro:`Build AXIAM from source, or run the whole stack in containers. Both paths, and what each one is good for.`,blocks:[{type:`h`,id:`which`,text:`Which path?`},{type:`table`,proseFirstCol:!0,headers:[`You want to…`,`Use`,`Notes`],rows:[[`Develop against AXIAM, or on it`,"`just dev-up` + a natively-run server",`Only SurrealDB and RabbitMQ run in Docker; the server runs on your machine so you get fast rebuilds and a debugger.`],[`Validate the whole stack end to end`,"`just prod-up`",`Server, admin console, database and broker in Compose. Documented as *not* for real production — it exists to prove the pieces fit.`],[`Run it for real`,"The Kubernetes manifests in `k8s/`",`See [Docker & Kubernetes](#/docs/deploy) and [Production hardening](#/docs/hardening).`]]},{type:`h`,id:`toolchain`,text:`Toolchain`},{type:`p`,text:"AXIAM is a Cargo workspace of focused crates — `axiam-core` holds the domain types, `axiam-db` the SurrealDB repositories, `axiam-auth`, `axiam-authz`, `axiam-oauth2`, `axiam-pki`, `axiam-scim` and the rest hold one concern each, and `axiam-server` composes them into the runnable binary. You need a Rust 1.93+ toolchain, Docker, and the `just` task runner."},{type:`code`,caption:`install the task runner`,code:`# macOS
brew install just

# or via cargo, anywhere
cargo install just`},{type:`h`,id:`services`,text:`Backing services`},{type:`p`,text:"`just dev-up` starts the development infrastructure in Docker: a SurrealDB node (the document/graph store behind every domain entity) and a RabbitMQ broker (async authorization, audit ingestion, mail and event delivery). `just dev-down` stops them again."},{type:`code`,code:`just dev-up      # start SurrealDB + RabbitMQ
just dev-down    # stop them`},{type:`h`,id:`build`,text:`Build & verify`},{type:`code`,code:`just build       # compile the workspace
just test        # run all tests
just check       # fmt + lint + test — the same gate CI enforces`},{type:`h`,id:`features`,text:`Optional build features`},{type:`p`,text:"SAML federation is behind a default-on `saml` feature because it links `libxml`, which needs system libxml2 headers. Where those are unavailable, build without default features — this is exactly what CI's *Build (SAML off)* job does, and everything except the SAML service-provider path is unaffected."},{type:`code`,code:`cargo build -p axiam-server --no-default-features`},{type:`h`,id:`compose`,text:`The full stack in Compose`},{type:`p`,text:"`just prod-up` builds and starts `axiam-server`, the admin console, SurrealDB and RabbitMQ together. It generates a local-only Ed25519 signing keypair under `docker/.secrets/` on first run and refuses to start if the database and broker credentials are not set in your shell — the Compose file uses fail-fast variable syntax rather than silently defaulting."},{type:`code`,code:`just prod-up      # build + start everything
#   admin console  http://localhost:8081
#   REST API       http://localhost:8090
#   gRPC           localhost:50051

just prod-down    # stop, keep volumes
just prod-clean   # stop and remove volumes`},{type:`warn`,text:"`docker-compose.prod.yml` is a workstation validation tool, not a production deployment. It is named for the production *image* it builds, not for where it should run. Use the Kubernetes manifests for anything real."}]},{slug:`bootstrap`,section:`Getting started`,navLabel:`First org, tenant & admin`,title:`The bootstrap procedure`,intro:`A fresh AXIAM has no organizations, no tenants and no users — and every administrative endpoint requires an authenticated caller who holds an explicit grant. Bootstrap is the one call that resolves that circularity, and it is built so that it can only ever be made by the operator, exactly once.`,blocks:[{type:`h`,id:`why`,text:`The problem bootstrap solves`},{type:`p`,text:"AXIAM is default-deny all the way down: a caller needs a token, and the principal behind that token needs a permission grant, for every administrative action. On a brand-new deployment nobody satisfies either condition, so something has to create the first administrator out of nothing. That something is `POST /api/v1/admin/bootstrap` — the only privileged endpoint reachable without an access token."},{type:`p`,text:`An endpoint like that is worth exactly as much as the gate in front of it, and as much as its guarantee that it cannot run twice. Both are enforced in the database rather than by a check in the handler, which is the part worth understanding before you run it.`},{type:`h`,id:`does`,text:`What one call does`},{type:`p`,text:`Bootstrap is a single call that provisions the entire first-run state, inside one transaction. There is no partially-bootstrapped outcome: either all of the following exists afterwards, or none of it does.`},{type:`steps`,steps:[{title:`Creates the organization`,body:"From `organization_name`, with `organization_slug` derived from it when you do not supply one. Get-or-create by slug, so retrying after a transient failure reuses the organization rather than duplicating it."},{title:`Creates the default tenant`,body:'From `tenant_name` (default `"Default"`) and `tenant_slug` (default `"default"`). Also get-or-create by slug.'},{title:`Seeds the permission registry into the tenant`,body:"All 113 built-in permissions across 25 families — `users:*`, `roles:*`, `resources:*`, `oauth2_clients:*`, `certificates:*`, `audit_logs:*`, `reactors:*`, `gdpr:*` and the rest. These are the actions the REST API's own route guards check against."},{title:`Seeds three default roles`,body:"`super-admin` (full system access — every permission), `admin` (all entity CRUD) and `viewer` (read-only: list and get). Seeding is idempotent — already-granted pairs are skipped rather than re-granted."},{title:`Creates the admin user and binds the super-admin role`,body:`The password is hashed with Argon2id server-side. If you asked for an OPAQUE baseline, the registration record is created here too.`},{title:`Takes the bootstrap lock`,body:"A uniqueness-invariant `bootstrap_lock:global` row is created in the same transaction. This is what makes the operation one-shot — see below."}]},{type:`note`,text:"Bootstrap issues **no token**. The new admin authenticates through `POST /api/v1/auth/login` like anybody else. That is deliberate: a provisioning endpoint that also hands out a session is a provisioning endpoint that is worth attacking twice."},{type:`h`,id:`gates`,text:`The gate — fail-closed, two ways to satisfy it`},{type:`p`,text:"A request is refused with `403` unless **one** of two gates is satisfied. Neither gate being configured does not mean *allow*; it means the endpoint is unusable until an operator configures one. There is no deployment in which an arbitrary caller can create the first admin."},{type:`table`,proseFirstCol:!0,headers:[`Gate`,`How it works`,`When to prefer it`],rows:[["`AXIAM_BOOTSTRAP_ADMIN_EMAIL`","Set this environment variable on the `axiam-server` process before it starts. The request's `email` field must match it **exactly**, or the call is refused. When this variable is set, `setup_token` is ignored entirely.",`Automated and declarative deployments. The operator names the first admin before the server exists, and nothing has to be recovered from a log.`],[`One-time setup token`,"Used only when the variable is unset. On first boot — and only while no admin has ever been bootstrapped — the server mints a random token, stores its SHA-256 hash, and logs the token once at `info`. Pass it as `setup_token`.",`Interactive installs and local exploration, where you are watching the server start.`]]},{type:`p`,text:"The token is single-use, and that is enforced twice. A pre-check rejects a hash that is unknown or already consumed; the authoritative guarantee is a `bootstrap_setup_token_consumed` row created inside the same transaction as the admin user, so two requests racing past the pre-check still leave exactly one winner — the loser hits a uniqueness violation and its whole transaction rolls back."},{type:`warn`,text:"The setup token is written to the server log exactly once, at `info` level. If your log pipeline drops `info`, buffers it, or you simply scroll past it, there is no way to re-read or re-mint it. Set `AXIAM_BOOTSTRAP_ADMIN_EMAIL` and restart instead — for anything unattended, prefer that gate in the first place."},{type:`note`,text:"`AXIAM_BOOTSTRAP_ADMIN_EMAIL` uses a **single** underscore after the prefix, unlike the `AXIAM__*` configuration variables. It is read straight from the environment rather than through the layered config loader, which is why it does not follow that convention — and why doubling the underscore here silently disables the gate."},{type:`h`,id:`once`,text:`Why it can only ever run once`},{type:`p`,text:"The one-shot property is not a `SELECT` followed by an `INSERT` — that pattern has a window between the two in which a second caller can also pass. Instead, creating the first super-admin includes creating `bootstrap_lock:global`, a row whose record id is a uniqueness invariant, in the *same* transaction."},{type:`list`,items:["**Two concurrent first-run requests** — both attempt the lock, one wins, the loser gets `409 Conflict` and rolls back completely. At most one super-admin can exist, with no orphaned role assignment left behind.","**Any later call** — hits the same uniqueness violation and is refused with `409`. Additional organizations, tenants and admins are created through the authenticated admin API from then on."]},{type:`h`,id:`request`,text:`Request fields`},{type:`table`,headers:[`Field`,`Required`,`Meaning`],rows:[[`organization_name`,`yes`,`Display name of the organization to create.`],[`organization_slug`,`no`,"URL-safe slug. Derived from `organization_name` when omitted or blank — ASCII alphanumerics are lower-cased and every other run of characters collapses to a single dash."],[`tenant_name`,`no`,"Display name of the default tenant. Defaults to `Default`."],[`tenant_slug`,`no`,"URL-safe slug for that tenant. Defaults to `default`."],[`email`,`yes`,"Admin email address. Must match `AXIAM_BOOTSTRAP_ADMIN_EMAIL` when that gate is in use."],[`username`,`yes`,`Admin username.`],[`password`,`yes`,`Admin password. Hashed with Argon2id before storage.`],[`setup_token`,`conditional`,"Required when `AXIAM_BOOTSTRAP_ADMIN_EMAIL` is not set; ignored when it is."],[`opaque_mode`,`no`,"Seeds the new organization's OPAQUE baseline: `disabled` (the default), `optional` or `required`."],[`opaque_suite`,`no`,"RFC 9807 ciphersuite for that baseline. Defaults to `ristretto255_sha512`."],[`opaque_ksf`,`no`,"Client key-stretching function for that baseline. Defaults to `argon2id`."]]},{type:`h`,id:`call`,text:`Making the call`},{type:`code`,caption:`gate 1 — AXIAM_BOOTSTRAP_ADMIN_EMAIL`,code:`# Set on the axiam-server process, BEFORE it starts.
export AXIAM_BOOTSTRAP_ADMIN_EMAIL=admin@acme.dev

curl -X POST https://iam.acme.dev/api/v1/admin/bootstrap \\
  -H 'content-type: application/json' \\
  -d '{
        "organization_name": "Acme Corporation",
        "organization_slug": "acme",
        "tenant_name": "Production",
        "tenant_slug": "production",
        "email": "admin@acme.dev",
        "username": "admin",
        "password": "'"$ADMIN_PASSWORD"'"
      }'`},{type:`code`,caption:`gate 2 — the one-time setup token`,code:`# Capture the token the server logged once at first boot:
#   AXIAM first-run bootstrap setup token minted. Use this token ONCE ...
export SETUP_TOKEN=...

curl -X POST https://iam.acme.dev/api/v1/admin/bootstrap \\
  -H 'content-type: application/json' \\
  -d '{
        "organization_name": "Acme Corporation",
        "email": "admin@acme.dev",
        "username": "admin",
        "password": "'"$ADMIN_PASSWORD"'",
        "setup_token": "'"$SETUP_TOKEN"'"
      }'`},{type:`code`,caption:`201 Created`,code:`{
  "message": "Bootstrap completed",
  "organization_id": "0f8c...",
  "organization_slug": "acme",
  "tenant_id": "3a91...",
  "tenant_slug": "production",
  "user_id": "c47b..."
}`},{type:`p`,text:"Keep `organization_slug` and `tenant_slug` — every SDK constructor takes them, and they are how a client names the tenant it is talking to."},{type:`h`,id:`responses`,text:`Response codes`},{type:`table`,headers:[`Status`,`Meaning`,`What to do`],rows:[[`201`,`Organization, tenant and admin created.`,`Log in and continue with the checklist below.`],[`400`,"A required field is missing or blank (`organization_name`, `email`, `username`, `password`).",`Fix the body. Nothing was created.`],[`403`,`The gate was not satisfied — email mismatch, or a missing/unknown/already-consumed setup token. The error names which.`,"Check that `AXIAM_BOOTSTRAP_ADMIN_EMAIL` is set on the *server* process and matches byte-for-byte, or that you are using a fresh token."],[`409`,`Bootstrap has already completed on this deployment, or a concurrent request won the race.`,`Not an error to retry. Log in as the existing admin; create further tenants and admins through the authenticated API.`]]},{type:`h`,id:`opaque`,text:`Seeding an OPAQUE baseline at bootstrap`},{type:`p`,text:"`opaque_mode` is settable here, and not only through the settings API afterwards, for one specific reason: OPAQUE's `required` mode cannot be turned on retroactively. A registration record has to be built from the plaintext password, and a stored Argon2id hash is not invertible — so nobody can be enrolled after the fact. If you switched a deployment to `required` while its only account had no record, you would lock the sole administrator out of their own system with no second admin to undo it."},{type:`p`,text:"Bootstrap is also the one enrolment path that does not take a client-built record, and that is not a weakening. Every other path requires the client to build the record because the server must never see the plaintext; bootstrap takes `password` in the request body by construction, so it runs the client half of the registration itself. The resulting record is indistinguishable from a client-built one."},{type:`note`,text:`If you want an OPAQUE record the server provably never held material for, change the admin password once after bootstrap through the ordinary password-change flow — that path is client-built like every other one.`},{type:`h`,id:`after`,text:`What to do immediately after`},{type:`steps`,steps:[{title:`Enrol a second factor on the admin account`,body:`A single-factor super-admin is the most valuable credential in the deployment. Register a passkey or a TOTP authenticator before anything else.`},{title:`Create a second administrator`,body:`One admin account is one lost device away from an unrecoverable deployment, and bootstrap cannot be run again to rescue you.`,code:`POST /api/v1/users            # create the user
POST /api/v1/roles/{role_id}/users   # bind them to super-admin`},{title:`Create the tenants you actually need`,body:`The bootstrap tenant is a starting point, not a container for everything. Tenants are the isolation boundary — one per customer, environment or business unit.`,code:`POST /api/v1/organizations/{org_id}/tenants
{ "name": "Acme Staging", "slug": "acme-staging" }`},{title:`Review the settings baseline`,body:`Password policy, lockout, token lifetimes, MFA enforcement and the OPAQUE mode all default at the organization and are tightened per tenant.`,code:`GET /api/v1/organizations/{org_id}/settings`},{title:`Unset the bootstrap gate`,body:"Once bootstrap has completed, `AXIAM_BOOTSTRAP_ADMIN_EMAIL` does nothing — the lock refuses every further call regardless. Removing it keeps your deployment manifest honest about what is still load-bearing."}]},{type:`cards`,cards:[{title:`Settings & policies →`,body:`The org baseline, tenant tighten-only overrides, and every policy field.`,to:`docs`,doc:`settings`},{title:`Production hardening →`,body:`The checklist to work through before this deployment carries real traffic.`,to:`docs`,doc:`hardening`}]}]},{slug:`concepts`,section:`Getting started`,navLabel:`Core concepts`,title:`Core concepts`,intro:`The domain model AXIAM is built around — organizations and tenants at the top, and the entities scoped inside them.`,blocks:[{type:`h`,id:`tenancy`,text:`Organizations & tenants`},{type:`p`,text:`**Organizations** are the top-level entity. They own the CA certificates, hold the settings baseline, and contain one or more tenants. **Tenants** are the isolation boundary: each has its own users, groups, roles, permissions, resources, certificates, OAuth2 clients, federation configuration and OPAQUE key material. Nothing crosses a tenant boundary implicitly.`},{type:`p`,text:"There is no default tenant and no ambient tenant context. Every SDK constructor takes one, every gRPC request message carries `tenant_id`, and every OAuth2 endpoint takes it as a query parameter. This is deliberate: the most common multi-tenancy bug is a query that forgot to filter, and an API that cannot express *unscoped* cannot express that bug."},{type:`code`,caption:`the containment hierarchy`,code:`Organization  (CA certificates, settings baseline)
 └── Tenant   (the isolation boundary)
      ├── Users, Groups, Service accounts
      ├── Roles → Permissions (action + resource, allow or deny)
      ├── Resources (a tree) → Scopes
      ├── OAuth2 clients, Federation configs
      ├── Certificates, PGP keys
      └── Webhooks, Reactors, Notification rules`},{type:`h`,id:`identity`,text:`Identities`},{type:`table`,proseFirstCol:!0,headers:[`Entity`,`What it is`],rows:[[`User`,`A human identity. Authenticates with a password, an OPAQUE record, a federated IdP, a passkey, or a client certificate — with optional MFA on top.`],[`Group`,`A named collection of users. Roles assigned to a group are inherited by every member, which is how you avoid per-user grants.`],[`Service account`,`A non-human identity for machine-to-machine access. Authenticates with client credentials or a bound X.509 certificate, and can have its secret rotated in place.`],[`OAuth2 client`,`A registered application. Confidential or public, with its own redirect URIs, grant types, scopes and — optionally — a FAPI 2.0 constraint bundle.`]]},{type:`h`,id:`access`,text:`Roles, permissions & resources`},{type:`p`,text:`A **permission** is an action on a resource, carrying an effect of either allow or deny. A **role** is a collection of permissions, assignable to users or to groups, and either global or bound to a specific resource. **Resources** form a tree, and a role assigned on a parent cascades to its descendants. **Scopes** add sub-resource granularity where a permission needs to apply to only part of a resource.`},{type:`p`,text:'Evaluation is **default-deny with deny-override**: nothing is permitted unless a grant allows it, and an explicit `effect: "deny"` refuses regardless of what else allows it — at any depth of the hierarchy and at equal specificity. See [the authorization engine](#/docs/authz) for the precedence table and why deny-override rather than most-specific-wins.'},{type:`note`,text:`Earlier revisions of this page described the engine as additive-only with no explicit deny. That was accurate before deny grants shipped; it is not accurate now. Deny is a first-class effect on a permission grant.`},{type:`h`,id:`sessions`,text:`Sessions, tokens & credentials`},{type:`p`,text:"Authenticating produces a **session**, identified by a `sid` that survives token refresh. Access tokens are short-lived EdDSA-signed JWTs; refresh tokens are opaque, server-stored and single-use. In a browser both live only in `httpOnly` cookies. Logging out ends the session — and, if relying parties are registered for back-channel logout, tells them so."},{type:`h`,id:`audit`,text:`Everything is audited`},{type:`p`,text:`Every authentication, authorization decision and administrative mutation is written to an append-only, cryptographically signed audit log. There are no UPDATE or DELETE paths on it. This is a property of the platform rather than a feature you enable.`},{type:`cards`,cards:[{title:`Authorization engine →`,body:`Precedence, cascade, scopes and how a decision is actually computed.`,to:`docs`,doc:`authz`},{title:`The security model →`,body:`Threat model, cryptography and shared responsibility.`,to:`security`}]}]},{slug:`tutorial`,section:`Getting started`,navLabel:`Protect an app`,title:`Tutorial — protect an app`,intro:`One small service, protected end to end: sign a user in, guard a route, and check a permission on a real resource. From an empty machine to a request that is allowed because you granted it.`,verifiedRelease:r,blocks:[{type:`h`,id:`shape`,text:`What you are building`},{type:`p`,text:"The [quickstart](#/docs/quickstart) gets a server running and makes one check with `curl`. [Core concepts](#/docs/concepts) explains what the pieces mean. This page is the bridge: a working service with the three things every protected application needs — a sign-in, a guard that rejects anonymous callers, and a per-request authorization check against a resource you created."},{type:`p`,text:"Two decisions are worth understanding before you start, because they are the ones people get wrong. **The guard and the check are different layers**: the guard establishes *who* is calling and fails with `401`; the check asks whether *that* caller may act on *this* resource and fails with `403`. And **the check is always made for the request's user**, never for the service account your application authenticated with — otherwise every user inherits your service's authority."},{type:`h`,id:`stack`,text:`1. Bring the stack up`},{type:`p`,text:`Dev infrastructure — SurrealDB and RabbitMQ — runs in Docker; the server builds with Rust. If you already have an AXIAM instance to point at, skip to step 3.`},{type:`code`,caption:`terminal`,code:`git clone https://github.com/ilpanich/axiam.git
cd axiam

just dev-up      # SurrealDB + RabbitMQ
just build
just run         # REST on :8090, gRPC on :50051`},{type:`code`,code:`curl -s localhost:8090/health   # liveness — 200 whenever the process is up
curl -s localhost:8090/ready    # readiness — 200 only once SurrealDB answers`},{type:`note`,text:"Wait for `/ready`, not `/health`. A server that is up but cannot reach its database will accept your bootstrap call and fail it, which looks like a bad request rather than a cold start."},{type:`h`,id:`bootstrap`,text:`2. Create the organization, tenant and admin`},{type:`p`,text:`A fresh deployment has no users, and the endpoint that creates the first one is fail-closed — it refuses unless you prove you are the operator. Both gates and the seeded role set are on [First organization, tenant and admin](#/docs/bootstrap); the short version:`},{type:`code`,caption:`terminal`,code:`export AXIAM_BOOTSTRAP_ADMIN_EMAIL=admin@acme.dev   # set before starting the server

curl -X POST localhost:8090/api/v1/admin/bootstrap \\
  -H 'content-type: application/json' \\
  -d '{
        "organization_name": "Acme",
        "tenant_name": "Production",
        "email": "admin@acme.dev",
        "username": "admin",
        "password": "'"$ADMIN_PASSWORD"'"
      }'`},{type:`h`,id:`grant`,text:`3. Create something to protect, and grant access to it`},{type:`p`,text:`An authorization check needs a resource to be about and a grant that reaches it. Four calls, as the tenant admin — a resource, a permission, a role holding it, and the assignment that ties a user to the role.`},{type:`steps`,steps:[{title:`Create the resource`,body:`Resources are hierarchical, and a role granted on a parent cascades to its children. Start with one node; the cascade is what you will lean on later.`,code:`POST /api/v1/resources`},{title:`Create the permission`,body:`A permission names an action on a resource type. This is the string your guard will ask about.`,code:`POST /api/v1/permissions`},{title:`Create a role and attach the permission`,body:`Roles are collections of permissions. Grant the narrow one you just made rather than reaching for the seeded admin role, which carries everything.`,code:`POST /api/v1/roles
POST /api/v1/roles/{role_id}/permissions`},{title:`Assign the role to your test user`,body:`Assign to a group instead and every member inherits it — the same grant, one indirection up.`,code:`POST /api/v1/roles/{role_id}/users`}]},{type:`note`,text:`Nothing here grants anything by default. The engine is default-deny: a subject with no matching grant is refused, and an explicit deny anywhere in the resource tree overrides every allow at any depth. See [Deny overrides](#/docs/deny).`},{type:`h`,id:`signin`,text:`4. Sign a user in`},{type:`p`,text:`The client takes a tenant **and** an organization: a tenant slug is only unique within an organization, so there is no default for either. Login has three outcomes, not two — success, MFA required, and MFA *setup* required when the tenant mandates MFA for an account that has none. Handle the third or those users cannot sign in at all.`},{type:`codegroup`,caption:`sign in`,tabs:[{label:`Rust`,code:`use axiam_sdk::client::AxiamClient;

let client = AxiamClient::builder()
    .base_url("http://localhost:8090")?
    .tenant_slug("production")
    .org_slug("acme")
    .build()?;

let login_result = client.login("user@acme.dev", &password).await?;
if login_result.mfa_required {
    client.verify_mfa("123456").await?;
}`},{label:`TypeScript`,code:`import { AxiamClient } from 'axiam-sdk';

const client = new AxiamClient({
  baseUrl: 'http://localhost:8090',
  tenantSlug: 'production',
  orgSlug: 'acme',
});

const result = await client.login(email, password);
switch (result.status) {
  case 'authenticated':
    console.log(\`signed in as \${result.user.username}\`);
    break;
  case 'mfa_required': {
    const code = await promptForMfaCode(result.availableMethods);
    await client.verifyMfa(result.mfaToken, code);
    break;
  }
}`},{label:`Python`,code:`from axiam_sdk import AxiamClient

client = AxiamClient(
    base_url="http://localhost:8090",
    tenant_slug="production",
    org_slug="acme",
)

result = client.login("user@acme.dev", password)`}]},{type:`h`,id:`guard`,text:`5. Guard the route`},{type:`p`,text:`The guard verifies the caller's token against a locally cached JWKS — no round trip per request — and injects the authenticated identity into your handler. It must be told which tenant it is guarding: the JWKS trust anchor is organization-wide, so a verifier that does not pin a tenant would accept a sibling tenant's token.`},{type:`codegroup`,caption:`authenticate every request`,tabs:[{label:`Rust`,code:`// Cargo.toml: features = ["actix"]
use axiam_sdk::middleware::AxiamUser;

// Pin the tenant — §10.1 rule 4, fails closed without it.
let verifier = JwksVerifier::new(http, &base_url)?
    .expect_tenant_id(tenant_uuid)
    .expect_audience("axiam:user");

async fn protected(user: AxiamUser) -> String {
    format!("hello {}", user.user_id)
}`},{label:`TypeScript`,code:`import express from 'express';
import { createNodeSession } from 'axiam-sdk/grpc';
import { axiamMiddleware, type AxiamRequest } from 'axiam-sdk/middleware';

const session = createNodeSession({ baseUrl: 'http://localhost:8090', tenantSlug: 'production' });
const app = express();

app.use(axiamMiddleware(session));

app.get('/protected', (req, res) => {
  const user = (req as AxiamRequest).axiamUser;
  res.json({ userId: user?.userId, roles: user?.roles });
});`},{label:`Python`,code:`from fastapi import Depends, FastAPI
from axiam_sdk.fastapi import AxiamUser, JwksVerifier, require_authenticated_user

verifier = JwksVerifier(base_url)
authenticated_user = require_authenticated_user(verifier, "production")

app = FastAPI()


@app.get("/protected")
async def protected(user: AxiamUser = Depends(authenticated_user)):
    return {"user_id": user.user_id, "roles": user.roles}`}]},{type:`h`,id:`check`,text:`6. Check the permission`},{type:`p`,text:`Authentication is not authorization. The declarative helpers add the per-endpoint check on top of the guard, resolving the resource id from the request — usually a path parameter — and asking the server whether *this* caller may perform *this* action on it.`},{type:`codegroup`,caption:`authorize the request`,tabs:[{label:`Rust`,code:`// Cargo.toml: features = ["macros"]
use axiam_sdk::require_access;
use axiam_sdk::middleware::AxiamUser;

#[require_access(action = "read", resource_param = "id")]
async fn get_document(user: AxiamUser) -> String {
    format!("user {} may read this document", user.user_id)
}`},{label:`TypeScript`,code:`import { requireAccess, fromParam } from 'axiam-sdk/middleware';

app.get(
  '/documents/:id',
  requireAccess(authzSession, 'read', fromParam('id')),
  (req, res) => res.json({ documentId: req.params.id }),
);`},{label:`Python`,code:`from axiam_sdk import AsyncAxiamClient
from axiam_sdk.fastapi import AxiamUser, require_access

authz_client = AsyncAxiamClient(base_url=base_url, tenant_slug="production")
require_doc_read = require_access(
    verifier, "production", authz_client, "documents:read", resource_param="doc_id"
)


@app.get("/docs/{doc_id}")
async def get_doc(doc_id: str, user: AxiamUser = Depends(require_doc_read)):
    return {"message": f"user {user.user_id} may read document {doc_id}"}`}]},{type:`table`,headers:[`Outcome`,`Status`,`Meaning`],rows:[[`Allowed`,`your handler runs`,`A grant matched and no deny overrode it.`],[`Unauthenticated`,"`401`",`No identity — the guard, not the check, refused.`],[`Denied`,"`403`",`Identity established, grant absent or overridden by a deny.`],[`Unresolvable resource`,"`400`",`The request named no resource the helper could resolve.`],[`Authz unavailable`,"`503`",`The check could not be made. **Fails closed** — never allowed on error.`]]},{type:`warn`,text:"The role check that ships alongside these — `require_role` and its equivalents — is a local test against the verified token's roles, with no round trip. It is cheaper and coarser, and it is **not** a substitute for the resource-level check: it cannot see a deny on a child resource, and it does not know which resource the request is about."},{type:`h`,id:`verify`,text:`7. Watch it refuse, then allow`},{type:`p`,text:"The check is worth exercising in both directions, because a guard that never refuses is indistinguishable from one that is not wired up. Call the route as a user with no grant and expect `403`; assign the role from step 3 and call again."},{type:`code`,caption:`terminal`,code:`# before the grant
curl -i -b cookies.txt localhost:3000/documents/$DOC_ID    # 403

# after POST /api/v1/roles/{role_id}/users
curl -i -b cookies.txt localhost:3000/documents/$DOC_ID    # 200`},{type:`note`,text:`No decision is cached by the helpers — every request is a fresh check — so a grant takes effect on the next call rather than on the next session. Sessions are a different matter: revoking a role does not retract a token already issued, which is what the short access-token lifetime and the revocation paths are for.`},{type:`h`,id:`next`,text:`Where to go next`},{type:`cards`,cards:[{title:`Authorization engine →`,body:`How a decision is actually computed: precedence, cascade and scopes.`,to:`docs`,doc:`authz`},{title:`Deny overrides →`,body:`Why an explicit deny beats every allow, at any depth.`,to:`docs`,doc:`deny`},{title:`Client SDKs →`,body:`The same three steps in the other eight languages.`,to:`docs`,doc:`sdks`}]}]}],c=[{slug:`auth`,section:`Authentication`,navLabel:`Passwords & sessions`,title:`Passwords, sessions & tokens`,intro:`How AXIAM verifies a credential, what it hands back, and how that session is kept alive — or ended.`,blocks:[{type:`h`,id:`passwords`,text:`Password storage`},{type:`p`,text:`Passwords are hashed with **Argon2id** using OWASP-recommended parameters, with a server-side pepper mixed in before hashing. AXIAM never stores or logs a plaintext credential, and every SDK wraps secrets in a redacting type so they cannot leak into a log line, a debug print or an error message.`},{type:`p`,text:"Each in-flight Argon2id operation allocates roughly a 19 MiB arena, which makes unbounded concurrency a memory-exhaustion vector rather than a throughput problem. A process-wide semaphore caps the number of concurrent arenas and sheds excess load with a `503` instead of queueing without limit. The cost parameters themselves are never weakened to buy throughput — see `AXIAM__AUTH__MAX_CONCURRENT_HASHES` in [Configuration](#/docs/configuration)."},{type:`note`,text:`If the password should never reach the server at all, AXIAM speaks OPAQUE (RFC 9807). It is off by default and enabled per organization or tenant — see [OPAQUE](#/docs/opaque).`},{type:`h`,id:`policy`,text:`Password policy`},{type:`p`,text:"Complexity, history and breach-checking are policy, set as an organization baseline and tightened per tenant. The fields are `min_length`, `require_uppercase`, `require_lowercase`, `require_digits`, `require_symbols`, `password_history_count` (how many previous hashes a new password is checked against) and `hibp_check_enabled` (reject passwords appearing in known-breach corpora). See [Settings & policies](#/docs/settings)."},{type:`h`,id:`lockout`,text:`Lockout`},{type:`p`,text:"Repeated failures lock an account with exponential backoff rather than a flat window: `max_failed_login_attempts` triggers a lock of `lockout_duration_secs`, multiplied by `lockout_backoff_multiplier` on each subsequent lock and clamped at `max_lockout_duration_secs`. An administrator holding `users:admin` can clear a lock early."},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/users/{user_id}/unlock`,summary:`Clear an active lockout on an account.`}]},{type:`h`,id:`tokens`,text:`Tokens`},{type:`table`,proseFirstCol:!0,headers:[`Token`,`Form`,`Lifetime & rules`],rows:[[`Access token`,`JWT signed with EdDSA (Ed25519)`,"Short-lived — 15 minutes by default, set per tenant with `access_token_lifetime_secs`. Verifiable offline against the tenant's JWKS."],[`Refresh token`,`Opaque, server-stored`,`**Single-use**: every refresh rotates it and invalidates the previous one. Reuse of a spent token is detectable and treated as compromise.`],[`ID token`,`JWT (OpenID Connect)`,"Carries `sid`, the session identifier, which is stable across refresh so a relying party can still match a logout token to its own session."]]},{type:`p`,text:"In a browser, none of these are handed to JavaScript. They live in `httpOnly` cookies, and the SDKs handle CSRF token forwarding and single-flight refresh — concurrent 401s collapse into one refresh request rather than a thundering herd of them."},{type:`h`,id:`flow`,text:`The login flow`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/login`,summary:`Authenticate with username/email and password.`,public:!0},{method:`POST`,path:`/api/v1/auth/refresh`,summary:`Exchange a refresh token for a new pair. Rotates.`,public:!0},{method:`POST`,path:`/api/v1/auth/logout`,summary:`End the session.`}]},{type:`p`,text:"`login` has **three** outcomes, not two: authenticated, MFA required, and MFA *setup* required — the last on a tenant that enforces MFA for an account that has none. It comes back as a `403` carrying a setup token, and it is recoverable: see [Multi-factor auth](#/docs/mfa) for the branch and what to do with it."},{type:`codegroup`,caption:`sign in, then handle a step-up`,tabs:[{label:`Rust`,code:`let result = client.login("user@acme.dev", &password).await?;

if result.mfa_required {
    client.verify_mfa("123456").await?;
}`},{label:`TypeScript`,code:`const result = await client.login(email, password);

switch (result.status) {
  case 'mfa_required': {
    const code = await promptForMfaCode(result.availableMethods);
    await client.verifyMfa(result.mfaToken, code);
    break;
  }
  case 'authenticated':
    console.log(\`Authenticated as \${result.user.username}\`);
    break;
}`},{label:`Python`,code:`result = client.login(email, password)

if result.mfa_required:
    result = client.verify_mfa(result.mfa_token, totp_code)

print(result.session_id, result.expires_in)`},{label:`Go`,code:`result, err := client.Login(ctx, email, password)
if err != nil {
    return err
}

if result.MFARequired {
    // complete the challenge before the session is established
}`},{label:`Java`,code:`LoginResult result = client.login("user@acme.dev", password);

if (result.mfaRequired()) {
    result = client.verifyMfa(result.challengeToken(), "123456");
}`}]},{type:`h`,id:`lifecycle`,text:`Account lifecycle`},{type:`p`,text:`Everything above assumes an account that already exists and is already verified. These five endpoints are what gets it there and back — and six of the nine account-lifecycle operations are unauthenticated on purpose, because a user who cannot log in is the entire audience for a password reset, and a user whose address is unverified may have no session at all.`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/verify-email`,summary:"Confirm an address from a mailed token. `tenant_id` is a body field.",public:!0},{method:`POST`,path:`/api/v1/auth/resend-verification`,summary:`Re-send the verification mail.`,public:!0},{method:`POST`,path:`/api/v1/auth/reset`,summary:`Begin a password reset. Accepts the workspace in slug form, like login.`,public:!0},{method:`GET`,path:`/api/v1/auth/reset/context`,summary:`The effective OPAQUE policy for the account a reset token belongs to.`,public:!0},{method:`POST`,path:`/api/v1/auth/reset/confirm`,summary:`Set the new password with the mailed token.`,public:!0}]},{type:`warn`,text:"**These endpoints are enumeration-safe, and your UI must not undo that.** `POST /auth/reset` answers `200` whether or not the address exists. `GET /auth/reset/context` answers `404` for a token that is unknown, expired *or* already consumed, without distinguishing the three, and discloses no identity — an unauthenticated endpoint that confirmed which account a token belongs to would be an oracle worth not having. A screen that says “no such user”, or that shows the account beside the form, hands back exactly what the uniform response was protecting."},{type:`p`,text:"The reset context exists because of OPAQUE. A tenant with OPAQUE enabled needs the client to build a registration record before it can send a new password, and building one needs the server's parameters — which the client cannot know before it has a token to ask with. So on any tenant that might have OPAQUE enabled, read the context first and populate the `opaque` field when it says the tenant requires it; sending a plaintext `new_password` to a tenant in `opaque_mode: required` is refused, and refused late."},{type:`codegroup`,caption:`completing a reset, OPAQUE or not`,tabs:[{label:`Rust`,code:`let context = client.password_reset_context(&token).await?;

client.confirm_password_reset(&PasswordResetConfirmation {
    token,
    new_password,
    tenant_id,
    opaque: /* build a §23 record when context.opaque is Some */ None,
})
.await?;`},{label:`TypeScript`,code:`const context = await client.passwordResetContext(token);

await client.confirmPasswordReset({
  token,
  newPassword,
  tenantId,
  ...(context.opaque ? { opaque: await client.opaqueEnrollment(newPassword) } : {}),
});`}]},{type:`note`,text:`The tokens in these mails are single-use and short-lived, which is what bounds the risk of the final mail hop being unencrypted — an inherent property of email that no server-side control closes. See [OPAQUE](#/docs/opaque) for when the password never reaches the server at all.`},{type:`h`,id:`sessions`,text:`Sessions end, not users`},{type:`p`,text:`Logout operates on a session, not on an identity. A user signed in on a phone and a laptop who logs out on the laptop expects the phone to stay signed in, and that is what happens. When relying parties are registered for back-channel logout, ending a session also notifies them — see [Logout & session management](#/docs/logout).`},{type:`h`,id:`rate`,text:`Rate limiting`},{type:`p`,text:"`/auth/login` is always keyed per source IP regardless of the deployment's rate-limit key mode, and stays strict under every posture preset. Register, password-reset, MFA and WebAuthn endpoints are likewise per-IP. The reasoning — and why `client_id` keying is a fairness control rather than an abuse control — is in [Configuration](#/docs/configuration)."}]},{slug:`opaque`,section:`Authentication`,navLabel:`OPAQUE (RFC 9807)`,title:`OPAQUE — the password never reaches the server`,intro:`An augmented PAKE in which AXIAM authenticates you without ever receiving your password, and in which a stolen credential database is not offline-crackable on its own.`,verifiedRelease:r,blocks:[{type:`h`,id:`what`,text:`What it changes`},{type:`p`,text:`In an ordinary password login the plaintext travels to the server inside the TLS session, and the server hashes it. TLS protects it in transit, but the server *has* it — which means a TLS-terminating proxy, an accidentally verbose request log, a heap dump or a memory-scraping exploit can capture it.`},{type:`p`,text:`With **OPAQUE** ([RFC 9807](https://datatracker.ietf.org/doc/rfc9807/)), the CFRG's augmented PAKE, the client blinds the password and the server evaluates an oblivious PRF over it without learning either the input or the output. The client uses the result to open a sealed envelope containing its long-term key material, and the two sides complete an authenticated key exchange. The server never holds a plaintext password at any point in the exchange.`},{type:`table`,proseFirstCol:!0,headers:[`Exposure`,`Password + Argon2id`,`OPAQUE`],rows:[[`TLS-terminating proxy sees the password`,`Yes`,`No — it sees a blinded element`],[`Verbose request log captures it`,`Yes`,`No`],[`Server heap dump contains it`,`Yes`,`No`],[`Stolen credential database is offline-crackable`,`Yes, at the cost of the KDF`,`**No** — an attacker additionally needs the tenant's OPRF seed, encrypted at rest under a key that is not in the database`],[`Compromised AXIAM server can steal it`,`Yes`,`Yes — see the limits below`]]},{type:`h`,id:`limits`,text:`What it does not do`},{type:`warn`,text:`OPAQUE does not protect against a compromised AXIAM server. A server that wants your password can serve a login page that collects it. For **browser** clients the benefit is bounded for exactly this reason — AXIAM serves the code that performs the computation. The strong case is native SDK clients, IoT devices, and deployments sitting behind infrastructure the tenant does not control.`},{type:`h`,id:`modes`,text:`Enabling it`},{type:`p`,text:"OPAQUE is **off by default** and configured as a policy: an organization sets the baseline and a tenant may only tighten it. The relevant fields are `opaque_mode`, `opaque_suite` (default `ristretto255_sha512`) and `opaque_ksf` (default `argon2id`, with `scrypt` as the alternative rung)."},{type:`table`,headers:[`Mode`,`Meaning`],rows:[[`disabled`,`The default. Only password login is offered.`],[`optional`,`Both paths work. Users enrol an OPAQUE record as they sign in or change password; those who have not yet keep using the password path.`],[`required`,`Only OPAQUE login is accepted, tenant-wide.`]]},{type:`warn`,text:"`required` cannot be turned on safely until every user in the tenant has enrolled. A registration record must be built from the plaintext password, and a stored Argon2id hash is not invertible — so **nobody can be enrolled retroactively**. Switching to `required` early locks out every user who has no record. Run `optional` until enrolment is complete, then flip."},{type:`h`,id:`flow`,text:`The exchange`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/opaque/register/start`,summary:`Begin enrolment: server evaluates the OPRF over the blinded password.`,public:!0},{method:`POST`,path:`/api/v1/auth/opaque/login/start`,summary:`Send KE1; receive KE2 plus the KSF parameters to use.`,public:!0},{method:`POST`,path:`/api/v1/auth/opaque/login/finish`,summary:`Send KE3; the session is established.`,public:!0}]},{type:`p`,text:`Login is two round trips rather than one, because the OPRF needs the server. Two rules matter to anyone integrating it directly:`},{type:`list`,items:["**Build the key-stretching function from what the server named**, never from a local default. `login/start` returns the KSF and its parameters; using anything else produces an envelope that cannot be opened.","**A failure at `finish` is indistinguishable by design.** Wrong password, unknown account, and a server holding no record all fail identically — and nothing further may be posted after one."]},{type:`code`,caption:`login · TypeScript, via the shared WASM core`,code:`import init, { OpaqueLogin, OpaqueKsf } from '@axiam/opaque-wasm';

await init();

// 1. Blind the password and start the exchange.
const login = new OpaqueLogin(password);
const started = await postJson('/api/v1/auth/opaque/login/start', {
  org_slug, tenant_slug, username_or_email, ke1: login.ke1,
});

// 2. Build the KSF from what the SERVER named.
const ksf = started.ksf === 'argon2id'
  ? OpaqueKsf.argon2id(started.memory_kib, started.iterations, started.parallelism)
  : OpaqueKsf.scrypt(started.log_n, started.r, started.p);

// 3. Open the envelope, then finish.
const finished = login.finish(password, started.ke2, ksf);

await postJson('/api/v1/auth/opaque/login/finish', {
  opaque_session: started.opaque_session,
  ke3: finished.ke3,
});`},{type:`note`,text:"`login` is **consumed** by `finish`. Calling it twice throws rather than reusing one OPRF blind across two exchanges."},{type:`h`,id:`sdk-usage`,text:`Through an SDK`},{type:`p`,text:`Integrating directly is only necessary in a browser or on an unsupported platform. Every SDK wraps the exchange in one call that returns the same result type as an ordinary login, MFA challenge included.`},{type:`codegroup`,caption:`sign in without sending the password`,tabs:[{label:`Rust`,code:`use axiam_sdk::AxiamClient;

let client = AxiamClient::builder()
    .base_url("https://axiam.example")?
    .org_slug("acme")
    .tenant_slug("default")
    .build()?;

// Same LoginResult as login(), including the MFA-challenge case.
let result = client.login_opaque("alice", "correct horse battery staple").await?;
if result.mfa_required {
    client.verify_mfa("123456").await?;
}`},{label:`Python`,code:`# Same LoginResult as login(), including the mfa_required case.
result = client.login_opaque("alice", "correct horse battery staple")`},{label:`Go`,code:`result, err := client.LoginOpaque(ctx, "alice", password)`}]},{type:`p`,text:`Enrolment is a separate call, and it exists because the server cannot build a record on its own — it never sees the plaintext. Send the result with any request that sets a password.`},{type:`codegroup`,caption:`enrol a record when setting a password`,tabs:[{label:`Rust`,code:'let enrollment = client.opaque_enrollment("new password").await?;\n// send `enrollment` as the request\'s `opaque` field'},{label:`Python`,code:`enrollment = client.opaque_enrollment("new password")
# send enrollment["registration_record"] and enrollment["opaque_session"]
# as the request's \`opaque\` object`},{label:`Go`,code:"enrollment, err := client.OpaqueEnrollment(ctx, newPassword)\n// send `enrollment` as the request body's `opaque` field"}]},{type:`note`,text:"Enrolment takes one argument where the SRP verifier it replaced took four. There is no identity, group or KDF to pass: the server names the ciphersuite and the costs in its `register/start` response, and the record binds to a credential identifier the server chooses — so enrolling against an email can no longer produce a record no login can satisfy, and a later rename no longer invalidates a credential."},{type:`warn`,text:`**Fall back to ordinary login on exactly one error.** A tenant that does not offer OPAQUE reports it as a network-class error naming OPAQUE — deliberately not an authentication error, so it cannot be mistaken for a bad password. Fall back on that one and nothing else: a failed exchange *is* a failed login, and retrying it over the password path hands the plaintext to a server that just failed to prove it holds the record.`},{type:`note`,text:`The call blocks for as long as the tenant's key-stretching function takes — Argon2id at 19 MiB by default, so tens to hundreds of milliseconds. That cost is the point, since it is what makes a stolen record expensive to attack even for someone holding the OPRF seed, but on an async runtime treat it as blocking work rather than as a quick round trip.`},{type:`h`,id:`availability`,text:`How each SDK binds it`},{type:`p`,text:`Every SDK ships OPAQUE. There is no conditional row in the sense that mattered before — the SRP implementation this replaced had four SDKs that could not compute Argon2id and one that needed a specific bignum extension. What differs now is only *how* each binds the single implementation.`},{type:`table`,proseFirstCol:!0,headers:[`SDK`,`Binding`,`What that means for you`],rows:[[`Rust`,`Native crate`,"Also compiles to `wasm32-unknown-unknown` for the WASM package."],[`TypeScript`,`WebAssembly`,`Adds a WASM module to the package; check its README for the size.`],[`Go`,`Native`,"No cgo, so a `CGO_ENABLED=0` build still works."],[`Python`,`C ABI via wheels`,`Platform wheels. A source install needs a Rust toolchain.`],[`Java`,`C ABI via JNI/FFM`,`Native artifacts ship inside the jar.`],[`Kotlin`,`As Java`,`Same JVM target, same artifacts.`],[`C#`,`C ABI via P/Invoke`,`Native assets ship in the NuGet package.`],[`PHP`,"C ABI via `ext-ffi`","**The one build-time conditional.** Without `ext-ffi` and the shared library present, the availability probe answers false."],[`Swift`,`C interop`,`Replaces the bundled modular exponentiation SRP required.`],[`C`,`Header and library`,`Direct.`],[`C++`,`Wraps the C binding`,`Direct.`]]},{type:`h`,id:`one-impl`,text:`One implementation, twelve callers`},{type:`p`,text:"AXIAM speaks OPAQUE from twelve places — the server, the admin console and eleven SDKs — and every one of them must agree on every byte. OPAQUE is not a protocol it is reasonable to hand-write once per language: it needs an OPRF, `hash_to_curve`, `expand_message_xmd`, an envelope construction and a three-message AKE."},{type:`p`,text:"So there is exactly one implementation, the `axiam-opaque` crate, and every client binds it: compiled directly (Rust, the server), through WebAssembly as `@axiam/opaque-wasm` (TypeScript, the admin console), or behind a C ABI (everything else). The cross-language contract is `sdks/CONTRACT.md` §23, and `sdks/opaque-test-vectors.json` is the shared fixture every SDK is tested against."},{type:`note`,text:`This replaced an SRP-6a implementation that had been written eleven times, because SRP is modular arithmetic and every language has a bignum. That produced a bundled Montgomery modular exponentiation in one SDK and another that reported itself unavailable without a specific extension. OPAQUE removes the offline attack rather than repricing it, and one audited implementation removes the drift.`},{type:`h`,id:`keys`,text:`The two keys it needs`},{type:`table`,headers:[`Variable`,`What it protects`,`Cost of losing it`],rows:[[`AXIAM__AUTH__OPAQUE_SETUP_KEY`,`Encrypts each tenant's OPRF seed and AKE keypair at rest.`,`**A password reset for every user in every tenant.** Back this up.`],[`AXIAM__AUTH__OPAQUE_SESSION_KEY`,`Seals in-flight exchange state (a 120-second window).`,`In-flight logins fail and are retried. Deliberately separate from the setup key so it is cheap to rotate.`]]},{type:`p`,text:"Both are required whenever any organization or tenant has `opaque_mode` other than `disabled`. Where they come from is pluggable — environment, a mounted file, or HashiCorp Vault. See [Secrets & Vault](#/docs/secrets)."}]},{slug:`mfa`,section:`Authentication`,navLabel:`Multi-factor auth`,title:`Multi-factor authentication`,intro:`TOTP as the baseline second factor, enrolled voluntarily by a signed-in user or forced during a login that cannot complete without it.`,blocks:[{type:`h`,id:`totp`,text:`TOTP`},{type:`p`,text:`AXIAM's baseline second factor is TOTP (RFC 6238) — the six-digit rolling code produced by an authenticator app. Enrolment returns a secret and a provisioning URI for a QR code; the user confirms with one generated code, which proves their clock and the secret agree before the factor is armed.`},{type:`p`,text:"Stored TOTP secrets are encrypted at rest with AES-256-GCM under `AXIAM__AUTH__MFA_ENCRYPTION_KEY`. Losing that key makes every enrolled secret undecryptable and forces every user to re-enrol."},{type:`warn`,text:"**Treat the provisioning URI as the secret, because it is one.** `otpauth://totp/…?secret=…` contains the shared secret verbatim, and it is the field that actually reaches a log — it is the one you hand to a QR renderer. Every SDK wraps both in its redacting type; a receiver that unwraps the URI to render it has unwrapped the secret."},{type:`h`,id:`paths`,text:`Two enrolment paths, and they are not interchangeable`},{type:`p`,text:`Confusing these two is the failure mode worth spelling out: they use different endpoints, different credentials and different starting states, and reaching for the wrong pair locks a user out of an account they are entitled to.`},{type:`table`,proseFirstCol:!0,headers:[``,`Voluntary`,`Forced during login`],rows:[[`Who starts it`,`A signed-in user adding a factor`,`Anyone whose tenant requires MFA on an account that has none`],[`Credential`,`The existing session`,"A `setup_token` the failed login handed back — there is no session yet"],[`Endpoints`,"`/auth/mfa/enroll` then `/auth/mfa/confirm`","`/auth/mfa/setup/enroll` then `/auth/mfa/setup/confirm`"],[`What confirming does`,`Arms the factor; the current session is untouched`,`**Completes the interrupted login** — the response is a normal login success`]]},{type:`h`,id:`voluntary`,text:`Voluntary enrolment`},{type:`steps`,steps:[{title:`Enrol`,body:"`POST /api/v1/auth/mfa/enroll` with the user's session and no body. The response carries `secret_base32` and `totp_uri`."},{title:`Show it once`,body:`Render the URI as a QR code, and offer the base32 secret for manual entry. This is the only time the secret is available.`},{title:`Confirm`,body:"`POST /api/v1/auth/mfa/confirm` with a code generated from it. **The factor is not active until this succeeds** — a client that reports MFA as enabled after the first call is reporting a factor nobody can use."}]},{type:`h`,id:`forced`,text:`Forced enrolment during login`},{type:`steps`,steps:[{title:`The login is refused, with the means to recover`,body:"`POST /api/v1/auth/login` answers `403` with `mfa_setup_required: true` and a `setup_token`. This is an outcome, not a permission failure: the caller can act on it, and an SDK that reports it as an authorization error has told them they may not log in when the server said “finish setting up, here is the token”."},{title:`Enrol with the setup token`,body:"`POST /api/v1/auth/mfa/setup/enroll`. The setup token *is* the credential — there is no session to authenticate with yet."},{title:`Confirm, and the login completes`,body:"`POST /api/v1/auth/mfa/setup/confirm` returns the normal login success, cookies and all. Nothing needs re-submitting a password."}]},{type:`codegroup`,caption:`handling all three login outcomes`,tabs:[{label:`Rust`,code:`let result = client.login(email, password).await?;

if result.mfa_required {
    client.verify_mfa(code).await?;
} else if result.mfa_setup_required {
    let setup_token = result.setup_token.as_ref().expect("populated by §25.2 rule 1");
    let enrolment = client.mfa_setup_enroll(setup_token).await?;
    render_qr(enrolment.totp_uri.expose());
    client.mfa_setup_confirm(setup_token, code).await?;   // completes the login
}`},{label:`TypeScript`,code:`switch (result.status) {
  case 'authenticated':
    break;
  case 'mfa_required':
    await client.verifyMfa(result.mfaToken, code);
    break;
  case 'mfa_setup_required': {
    const enrolment = await client.mfaSetupEnroll(result.setupToken);
    renderQr(enrolment.totpUri.expose());
    await client.mfaSetupConfirm(result.setupToken, codeTypedByUser);
    break;                                  // this completes the login
  }
}`},{label:`Python`,code:`result = client.login(email, password)

if result.mfa_required:
    result = client.verify_mfa(result.mfa_token, totp_code)
elif result.mfa_setup_required:
    enrolment = client.mfa_setup_enroll(setup_token=result.setup_token)
    render_qr(enrolment.totp_uri.get_secret_value())
    client.mfa_setup_confirm(setup_token=result.setup_token, totp_code=code)`}]},{type:`note`,text:"Contract 1.28 made the setup branch a first-class login outcome across all eleven SDKs. If you match a login result exhaustively, that is the edit: a genuine authorization refusal is still an authorization error, because the SDK matches on the body's own discriminant rather than on the `403` alone."},{type:`h`,id:`endpoints`,text:`Endpoints`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/mfa/enroll`,summary:`Begin voluntary enrolment. **Requires a session**; no body.`},{method:`POST`,path:`/api/v1/auth/mfa/confirm`,summary:`Arm the factor with a generated code. **Requires a session.**`},{method:`POST`,path:`/api/v1/auth/mfa/setup/enroll`,summary:`Begin forced enrolment, authenticated by the login's setup token.`,public:!0},{method:`POST`,path:`/api/v1/auth/mfa/setup/confirm`,summary:`Arm it and complete the interrupted login.`,public:!0},{method:`POST`,path:`/api/v1/auth/mfa/verify`,summary:`Answer an MFA challenge during login.`,public:!0},{method:`GET`,path:`/api/v1/users/{user_id}/mfa-methods`,summary:`List a user's enrolled factors.`},{method:`DELETE`,path:`/api/v1/users/{user_id}/mfa-methods/{method_id}`,summary:`Remove one factor.`},{method:`POST`,path:`/api/v1/users/{user_id}/reset-mfa`,summary:`Administratively clear every factor on an account.`}]},{type:`h`,id:`stepup`,text:`The step-up in a login`},{type:`p`,text:"When a factor is required, `POST /auth/login` does not establish a session. It returns an MFA challenge, and the SDK surfaces it as an outcome rather than an exception — the session is established only once `/auth/mfa/verify` succeeds. The challenge has its own lifetime, `mfa_challenge_lifetime_secs`, set per tenant."},{type:`h`,id:`policy`,text:`Enforcing it`},{type:`p`,text:"`mfa_enforced` is a policy field on the settings baseline: set it at the organization and every tenant inherits it, or tighten it on one tenant. As with every setting, a tenant may make the posture stricter than the organization's baseline but never looser. Turning it on is what produces the forced-enrolment branch above for accounts that have no factor yet."},{type:`warn`,text:`Enforce MFA on administrative accounts before anything else. A super-admin holds every permission in the tenant, including the ones that would let an attacker remove the audit evidence of what they did with it.`},{type:`note`,text:"TOTP is the baseline, not the ceiling. **Passkeys and security keys are phishing-resistant in a way a typed code is not** — a code can be entered into a lookalike page and forwarded to the real one inside its window. A registered passkey appears as `webauthn` among a challenge's available methods. See [Passkeys & WebAuthn](#/docs/passkeys)."}]},{slug:`passkeys`,section:`Authentication`,navLabel:`Passkeys & WebAuthn`,title:`Passkeys & WebAuthn`,intro:`Phishing-resistant authentication bound to your origin — as a second factor, or as the only factor — with an attestation policy for deployments that need to say which authenticators are acceptable.`,blocks:[{type:`h`,id:`why`,text:`Why they are different`},{type:`p`,text:`A **passkey** is a credential held by a device the user already has — a laptop's fingerprint sensor, a phone's face unlock, a platform PIN. A **security key** is the same construction on a removable device. Both are WebAuthn credentials, and both are phishing-resistant for a structural reason: the credential is cryptographically bound to the exact origin that created it, so a lookalike page cannot use it *even if the user is completely fooled*.`},{type:`p`,text:`A one-time code cannot make that promise. It can be typed into a fake page and forwarded to the real one inside its thirty-second window, which is precisely how modern credential-phishing kits work.`},{type:`h`,id:`endpoints`,text:`Six endpoints, three ceremonies`},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/webauthn/register/start`,summary:`Creation options for a new credential. **Requires a session.**`},{method:`POST`,path:`/api/v1/auth/webauthn/register/finish`,summary:`Submit the attestation; the credential is stored. **Requires a session.**`},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/start`,summary:`Request options for a second-factor ceremony, from a login's challenge token.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/finish`,summary:`Submit the assertion; the session is established.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/discoverable/start`,summary:`Request options for a usernameless sign-in — no prior step.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/discoverable/finish`,summary:`Submit the assertion; the assertion itself identifies the user.`,public:!0}]},{type:`warn`,text:"**The two authentication ceremonies are different flows, not one with a flag.** `authenticate/*` is a *second* factor: it continues a login that answered with an MFA challenge, and the challenge token names the user so the server can send an `allowCredentials` list. `discoverable/*` is a *primary* factor: nothing precedes it, `allowCredentials` is empty, and the authenticator offers whichever credential it holds for your origin. Modelling them as one call with an optional token reproduces a bug the server already fixed — `authenticate/start` decodes the token to learn who is signing in, so an empty one is rejected as malformed before anything else happens."},{type:`h`,id:`register`,text:`Enrolling a passkey`},{type:`steps`,steps:[{title:`Ask the server for creation options`,body:"`POST /api/v1/auth/webauthn/register/start`, with the user's existing session. The response carries the challenge and a state token. The server chooses `residentKey`, `userVerification`, the attestation conveyance, the exclusion list and the timeout — every one of them is a security parameter."},{title:`Run the ceremony`,body:"Hand the options to the platform: `navigator.credentials.create()` in a browser, `CreatePublicKeyCredentialRequest` on Android, `AuthenticationServices` on Apple platforms. Pass them **unchanged** — see the note below."},{title:`Post the attestation back`,body:"`POST /api/v1/auth/webauthn/register/finish` with the state token and the authenticator's response verbatim. A `201` returns the stored credential. A `403` is the tenant's attestation policy refusing *this authenticator* rather than a permission problem — surface its message, because it is how the person holding the key learns that this one will never work and a different one might."},{title:`Have the user name it, and enrol a second`,body:`A passkey lives on the device that created it. If that device is the only factor and it is lost, the account is locked out — and “Passkey 2” is not a name anyone can act on when deciding which to revoke.`}]},{type:`warn`,text:"**Pass the options through untouched, in both directions.** Do not supply a `timeout` the server omitted, do not expand an absent `authenticatorSelection` into an empty object, do not reorder or prune `pubKeyCredParams` or `excludeCredentials`, and do not re-encode the response's base64url fields. Relaxing `userVerification` to `preferred` because a CI authenticator kept prompting weakens a ceremony the server believes it configured, and the server cannot detect it: an assertion produced under weaker options is a perfectly valid assertion."},{type:`h`,id:`signin`,text:`Signing in`},{type:`steps`,steps:[{title:`As a second factor`,body:"`POST /api/v1/auth/login` answers with an MFA challenge listing `webauthn` among its methods. Call `authenticate/start` with that challenge token, run the ceremony, and post the assertion to `authenticate/finish`."},{title:`Usernameless, as the only factor`,body:"Call `discoverable/start` with your workspace — it accepts `org_id`/`org_slug` and `tenant_id`/`tenant_slug`, and unlike the OAuth2 endpoints it accepts slugs, so a slug-only client can run it. Then run the ceremony and post to `discoverable/finish`."},{title:`Either way, you are signed in`,body:"Both `finish` calls answer `200` with `access_token`, `refresh_token`, `session_id` and `expires_in`, **and** set the `axiam_access` / `axiam_refresh` / `axiam_csrf` cookie triple with an `X-CSRF-Token` response header — the same triple `POST /api/v1/auth/login` sets. Browser clients are authenticated by the cookies; non-browser clients adopt the body tokens."}]},{type:`note`,text:"The cookies arrived in **1.0.0-alpha38**. Before it these two endpoints answered with the body alone, which made a browser passkey sign-in impossible to complete: `POST /api/v1/auth/refresh` reads the refresh token from `axiam_refresh` and never from a body, so the session could not be kept alive. If you built against an older server, the body is unchanged — the cookies are additive."},{type:`note`,text:"Only the usernameless path fires the `login.post_auth` reactor hook. The username-bound ceremony continues a login that was already gated at its first step; the discoverable one has no first step to have been gated at. See [Reactors](#/docs/reactors)."},{type:`h`,id:`sdks`,text:`From an SDK`},{type:`p`,text:"A WebAuthn ceremony is two exchanges stacked: one with an *authenticator*, which needs a platform API, and one with *AXIAM*, which is four ordinary JSON round trips. Only the first needs a browser — which is why all eleven SDKs carry the AXIAM half. They expose the challenge in its wire JSON form and accept the platform's response JSON back, because that is exactly what the platform APIs take and return: `parseCreationOptionsFromJSON()` and `credential.toJSON()` in browsers, `requestJson` in and `registrationResponseJson` out on Android."},{type:`codegroup`,caption:`the JSON bridge — any runtime`,tabs:[{label:`Rust`,code:`let challenge = client.webauthn_discoverable_start(None).await?;

// The JSON form every platform authenticator API takes (§24.6a) — the exact
// string Android's CreatePublicKeyCredentialRequest and a browser's
// parseRequestOptionsFromJSON() both want.
let response_json = your_device_channel(&challenge.request_json())?;

let session = client
    .webauthn_discoverable_finish(
        &challenge.state_token,
        webauthn_response_from_json(&response_json)?,
    )
    .await?;`},{label:`TypeScript`,code:`import { webauthnRequestJson } from 'axiam-sdk/rest';

const { challenge, stateToken } = await client.webauthnDiscoverableStart();
const requestJson = webauthnRequestJson(challenge);   // → give this to the platform

// …the platform runs the ceremony and hands back a JSON string…
await client.webauthnDiscoverableFinish(stateToken, responseJson);`},{label:`Browser`,code:`// The half that needs a browser, and only this half.
const options = PublicKeyCredential.parseRequestOptionsFromJSON(requestJson);
const assertion = await navigator.credentials.get({ publicKey: options });
const responseJson = assertion.toJSON();   // → back to the SDK, unchanged`}]},{type:`p`,text:`TypeScript (browser build), the Rust WebAssembly build and Swift additionally compose the whole thing into one call, because their build can reach an authenticator API. The other SDKs deliberately do not: a server or CLI runtime has no authenticator, and emulating one in software would make the SDK the weakest link in a mechanism chosen for being the strongest.`},{type:`h`,id:`attestation`,text:`Attestation policy`},{type:`p`,text:`Most deployments should accept any authenticator the user has. Some — regulated environments, or a fleet standardised on issued hardware — need to say which models are acceptable, and prove afterwards that the rule held. That is what the per-tenant attestation policy is for. A tenant with no policy row behaves exactly as one with the defaults below.`},{type:`table`,headers:[`Field`,`Default`,`Meaning`],rows:[[`mode`,"`none`","`none`, `indirect` or `direct_required` — whether attestation is requested at all. `none` short-circuits every other field and performs no metadata lookup."],[`require_fido_certified`,"`false`","Deny unless the authenticator has any FIDO certification in its metadata history. Only enforceable when `mode` is not `none`."],[`min_certification`,"`null`","`L1` … `L3Plus` — deny unless the authenticator's highest ever certified level meets the bar."],[`allowed_aaguids`,"`null`","`null` allows every model except the blocked list. An explicitly empty array means *nothing* may register — a deliberate lockout rather than an unset field."],[`blocked_aaguids`,"`[]`",`Never registrable, regardless of the allow-list. Blocking wins.`],[`block_revoked_status`,"`true`",`Deny an authenticator whose metadata history ever reported a revocation or key compromise.`],[`unknown_aaguid`,"`null`","What to do about a model with no metadata entry. Unset resolves to `allow` under `none` and `indirect`, and to **`deny`** under `direct_required`, so the strictest mode fails closed. A read returns `effective_unknown_aaguid` alongside your stored intent."]]},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/tenants/{tenant_id}/webauthn/attestation-policy`,summary:"Read the tenant's policy, with the resolved `unknown_aaguid`."},{method:`PUT`,path:`/api/v1/tenants/{tenant_id}/webauthn/attestation-policy`,summary:"Set it. Certification fields are refused while `mode` is `none`."},{method:`GET`,path:`/api/v1/tenants/{tenant_id}/webauthn/compliance-report`,summary:`Which already-registered credentials satisfy the current policy.`},{method:`GET`,path:`/api/v1/mds/status`,summary:"The ingested FIDO metadata snapshot: serial, `next_update`, entry count, `stale`."},{method:`POST`,path:`/api/v1/mds/refresh`,summary:`Ingest a new snapshot now. Refuses to run when metadata is disabled.`}]},{type:`warn`,text:"**Passkeys frequently carry no meaningful attestation.** Platform authenticators commonly attest as `none` by design, for user-privacy reasons. A strict attestation policy therefore tends to exclude exactly the credentials most users have, and turns into a security-key-only policy in practice. Decide which of those two things you actually want before setting it."},{type:`h`,id:`mds`,text:`FIDO metadata, refresh and air gaps`},{type:`p`,text:`Authenticator models are identified against the FIDO Metadata Service (MDS3), and ingestion is **off by default** — with it disabled there are no outbound calls at all. Ingestion verifies the snapshot's signature and refuses a lower serial number than the one already stored, so an older validly-signed blob cannot be replayed to quietly reintroduce a model that has since been revoked.`},{type:`table`,headers:[`Setting`,`Default`,`Meaning`],rows:[[`AXIAM__PKI__MDS_ENABLED`,"`false`","Master switch. `false` means no background job and a refresh endpoint that refuses to run."],[`AXIAM__PKI__MDS_BLOB_URL`,`the FIDO Alliance endpoint`,`Fetch source for the network path.`],[`AXIAM__PKI__MDS_BLOB_PATH`,`unset`,`A local file for air-gapped deployments. When set it wins over the URL and **no network fetch happens at all** — you re-supply the file yourself.`],[`AXIAM__PKI__MDS_REFRESH_INTERVAL_SECS`,"`604800`","Background refresh interval — weekly. `0` disables the job; the admin endpoint still works."],[`AXIAM__PKI__MDS_LEAF_DNS`,"`mds.fidoalliance.org`",`Expected DNS name on the snapshot's signing certificate, so a legitimate hostname change is an ops action rather than a release.`],[`AXIAM__PKI__MDS_MAX_STALE_DAYS`,"`0`","How far past its `next_update` the snapshot may drift before **attested** registration is refused. `0` keeps the fail-open behaviour."]]},{type:`p`,text:`Staleness never blocks ingestion or the use of already-ingested entries: a transient outage at the FIDO Alliance must not brick registration everywhere. The cost of that choice is that a model revoked since your last refresh keeps passing policy, which is what the staleness bound is for — it is opt-in rather than defaulted because the right bound is a property of the deployment. A high-assurance tenant may want days; an air-gapped one with no automatic refresh path would be taken offline by anything short of months. It applies to attested ceremonies only, since an unattested one consults no metadata and stale metadata cannot have misled it.`},{type:`note`,text:"`register/start` answering `503` is this state, not a transient failure: the policy requires attestation and there is no usable metadata snapshot. It is a documented exception to the SDK retry policy — retrying changes nothing and only delays a message an operator needs to see. The full decision order, the known `USER_VERIFICATION_BYPASS` limitation and the air-gap procedure are in [docs/admin/authenticator-policies.md](https://github.com/ilpanich/axiam/blob/main/docs/admin/authenticator-policies.md)."},{type:`h`,id:`limits`,text:`Rate limiting`},{type:`p`,text:"All six ceremony routes are throttled per source IP by `AXIAM__RATE_LIMIT__WEBAUTHN_PER_MIN`, which defaults to **10** and is deliberately the same allowance `/auth/login` gives passwords. Each route carries that budget independently, so a ceremony — one `start`, one `finish` — spends one request from each rather than two from one bucket, and the number is the ceremonies-per-minute figure rather than half of it. It is not sized from throughput: an assertion is a single signature check, cheap next to Argon2id. What needed bounding is that every `start` allocates challenge state, and that the username-bound `authenticate/start` would otherwise be a credential-enumeration oracle."},{type:`h`,id:`users`,text:`How users experience it`},{type:`list`,items:[`**Autofill** — tapping the username field offers a saved passkey alongside saved passwords. A conditional ceremony may never settle, because the user simply may not pick one; abandon it on navigation rather than reporting an authentication failure.`,`**An explicit button** — “Sign in with a passkey” below the password field, for users whose browser did not offer autofill. This is the usernameless path.`,`**As a second factor** — after a password, where policy requires a step-up rather than a replacement.`,`End-user guidance, including what to do when a device is lost, is in [docs/user/passkeys.md](https://github.com/ilpanich/axiam/blob/main/docs/user/passkeys.md).`]}]},{slug:`federation`,section:`Authentication`,navLabel:`Federation & SSO`,title:`Federation — SAML & OIDC`,intro:`Delegate authentication to an external identity provider, and map what it asserts onto tenant identities, groups and roles.`,verifiedRelease:r,blocks:[{type:`h`,id:`why`,text:`Why federate`},{type:`p`,text:`Federation lets a user from an external identity provider sign in to an AXIAM tenant without a separate local credential. AXIAM acts as a **SAML service provider** and as an **OIDC relying party**, so it slots into existing enterprise SSO rather than asking an organization to move its directory.`},{type:`p`,text:`A federated identity is an ordinary user inside the tenant. Roles, permissions, audit and MFA policy apply to it exactly as they do to a locally-credentialed one — the difference is only where the authentication happened.`},{type:`h`,id:`saml`,text:`SAML`},{type:`p`,text:`As a service provider, AXIAM consumes assertions from an external IdP, validates the signature against the configured certificate, and maps assertion attributes onto tenant identities, group membership and roles. Attribute mapping is per federation configuration.`},{type:`note`,text:"SAML support is behind a default-on `saml` build feature that links `libxml`. A build made with `--no-default-features` — which is what CI's *Build (SAML off)* job produces — has every other capability and no SAML service provider."},{type:`h`,id:`oidc`,text:`OIDC federation`},{type:`p`,text:"As a relying party, AXIAM delegates authentication upstream and then issues its own tenant-scoped session. Claim mapping is configured per tenant, and the upstream client secret is encrypted at rest with AES-256-GCM under `AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY`."},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/auth/federation/oidc/start`,summary:`Begin an upstream authentication.`,public:!0},{method:`POST`,path:`/api/v1/auth/federation/oidc/callback`,summary:`Complete it and establish the local session.`,public:!0},{method:`GET`,path:`/api/v1/federation-configs`,summary:`List configured identity providers.`},{method:`POST`,path:`/api/v1/federation-configs`,summary:`Register one.`},{method:`PUT`,path:`/api/v1/federation-configs/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/federation-configs/{id}`,summary:`Remove it.`},{method:`GET`,path:`/api/v1/federation-links/user/{user_id}`,summary:`Which upstream identities are linked to a user.`},{method:`DELETE`,path:`/api/v1/federation-links/{id}`,summary:`Unlink one.`}]},{type:`h`,id:`setup`,text:`Registering a provider`},{type:`p`,text:`Both protocols are configured through the same endpoint and the same record — what differs is which fields matter.`},{type:`steps`,steps:[{title:`Register the configuration`,body:"`provider` is the display name. `metadata_url` is the OIDC discovery URL or the SAML metadata URL. The client secret is stored as AES-256-GCM ciphertext, never in plaintext.",code:`POST /api/v1/federation-configs
{
  "provider": "Okta",
  "protocol": "OidcConnect",
  "metadata_url": "https://acme.okta.com/.well-known/openid-configuration",
  "client_id": "<upstream-client-id>",
  "client_secret": "<upstream-client-secret>",
  "attribute_map": { "email": "email", "name": "name" },
  "enabled": true
}`},{title:`Pin the signature algorithms you accept`,body:"`allowed_algorithms` defaults to `RS256` for OIDC and is empty for SAML, where the certificate does the work instead. Narrow it to what the provider actually signs with rather than leaving it open.",code:`"allowed_algorithms": ["RS256"]`},{title:`For SAML, supply the IdP signing certificate`,body:"`idp_signing_cert_pem` is the X.509 certificate assertions are verified against. Without it there is nothing to check a signature with.",code:`"idp_signing_cert_pem": "-----BEGIN CERTIFICATE-----\\n..."`},{title:`Map the attributes onto AXIAM fields`,body:"`attribute_map` maps AXIAM field names to the provider's attribute names — not the other way round, which is the direction people write first.",code:`{ "email": "mail", "name": "displayName" }`}]},{type:`note`,text:'The mapping resolves two fields: `email`, and a display name taken from `name` with `displayName` as a fallback. A provider that calls its address attribute `mail` needs `{"email": "mail"}` — with the AXIAM name as the key.'},{type:`h`,id:`links`,text:`Linking and unlinking`},{type:`p`,text:`A **federation link** records that a given upstream subject corresponds to a given local user. Links are visible and removable per user, which is what makes offboarding an IdP tractable: unlink, and the local account remains with whatever other credential it holds — rather than becoming an orphan nobody can sign in to.`},{type:`h`,id:`provisioning`,text:`Federation is not provisioning`},{type:`p`,text:`Federation answers *who is this person* at sign-in time. It does not create, update or deactivate accounts ahead of time, and it does not remove one when someone leaves. For that, use **SCIM 2.0** — Okta, Entra ID and any other SCIM-compliant IdP can drive user and group lifecycle directly. See [SCIM provisioning](#/docs/scim).`},{type:`p`,text:`There is one adjacent place where an external identity *can* create a local user, and it is worth knowing it is a different mechanism: cross-domain token exchange, where a partner's IdP presents a subject token. That trust configuration carries a subject-mapping setting with two values.`},{type:`table`,proseFirstCol:!0,headers:[`Subject mapping`,`Behaviour`],rows:[["`linked_only`",`**The default.** Refuse an external subject with no existing link. The user must already have signed in through that provider, or been linked by an administrator, before a partner token buys anything.`],["`jit_provision`",`Provision an AXIAM user on first sight. Audited as such — that record is the only place a partner IdP's creation of a local user is visible.`]]},{type:`note`,text:`An unrecognised value is refused rather than defaulted. That matters more than it looks: the default is the stricter of the two, so a silently-tolerated typo would fail closed today and open on any future revision that flips which value is the default.`}]},{slug:`service-accounts`,section:`Authentication`,navLabel:`Service accounts`,title:`Service accounts & machine identity`,intro:`Non-human principals for service-to-service access — authenticated by a client secret or by a bound X.509 certificate, and subject to exactly the same authorization engine as a user.`,verifiedRelease:r,blocks:[{type:`h`,id:`what`,text:`What a service account is`},{type:`p`,text:`A service account is a tenant-scoped identity for an automated caller. It holds roles and permissions like a user, appears in the audit log like a user, and is denied by a deny grant like a user. What it does not have is a password, an email address or an interactive login.`},{type:`p`,text:`Use one wherever a human account would otherwise be shared between people or embedded in a deployment — a CI pipeline, a batch job, a backend service calling AXIAM on its own behalf. The audit trail then names the *system* that acted rather than whichever employee's credentials it was using.`},{type:`h`,id:`endpoints`,text:`Managing them`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/service-accounts`,summary:`List the tenant's service accounts.`},{method:`POST`,path:`/api/v1/service-accounts`,summary:`Create one. The secret is returned exactly once.`},{method:`GET`,path:`/api/v1/service-accounts/{sa_id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/service-accounts/{sa_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/service-accounts/{sa_id}`,summary:`Delete it.`},{method:`POST`,path:`/api/v1/service-accounts/{sa_id}/rotate-secret`,summary:`Issue a new secret. Returned once.`},{method:`POST`,path:`/api/v1/service-accounts/{sa_id}/bind-certificate`,summary:`Bind an X.509 certificate for mTLS authentication.`}]},{type:`warn`,text:`A service-account secret is shown once, at creation or rotation, and is never retrievable afterwards. If it is lost, rotate — there is no recovery path, by design.`},{type:`h`,id:`auth`,text:`Authenticating as one`},{type:`p`,text:`The ordinary path is the OAuth2 **client credentials** grant: the service account exchanges its identifier and secret for a short-lived access token, then uses that token like any other caller.`},{type:`code`,caption:`client credentials`,code:`curl -X POST 'https://iam.acme.dev/oauth2/token?tenant_id=<uuid>' \\
  -d grant_type=client_credentials \\
  -d client_id="$SA_CLIENT_ID" \\
  -d client_secret="$SA_CLIENT_SECRET" \\
  -d scope='read:orders'`},{type:`h`,id:`mtls`,text:`Certificates instead of secrets`},{type:`p`,text:"A shared secret has to be distributed, stored and rotated, and it is bearer material — whoever holds it is the service. Binding an X.509 certificate to the service account replaces that with a private key that never leaves the machine holding it, authenticated through mutual TLS terminated inside `axiam-server` rather than asserted by a proxy header."},{type:`p`,text:`This is the recommended shape for IoT fleets and for anything in a service mesh that already has a certificate. Combined with **certificate-bound access tokens**, it also means a stolen token is useless without the corresponding private key — see [FAPI 2.0 & mTLS clients](#/docs/fapi2) and [PKI & certificates](#/docs/pki).`},{type:`note`,text:"Client-certificate verification is native: `AXIAM__SERVER__TLS__CLIENT_AUTH` (`off` | `optional` | `required`) with a trust bundle at `AXIAM__SERVER__TLS__CLIENT_CA_PATH`. No proxy-asserted identity header is in the trusted path, and a misconfigured mTLS server fails fast at startup rather than serving without verification."},{type:`h`,id:`auth-methods`,text:`How a client proves who it is`},{type:`p`,text:`Four client-authentication methods are registrable, and the choice is recorded on the client rather than negotiated per request.`},{type:`table`,proseFirstCol:!0,headers:[`Method`,`The credential`,`Register with`],rows:[["`client_secret_post`",`A shared secret in the request body. The default, and bearer material.`,`The secret returned once at creation.`],["`tls_client_auth`",`A certificate issued by a CA the mTLS listener trusts. AXIAM matches the registered expected subject DN or SAN against the certificate rustls already verified during the handshake.`,"`tls_client_auth_subject_dn`, or the expected SAN."],["`self_signed_tls_client_auth`",`A certificate that chains to nothing, so the certificate itself is the credential — matched by SHA-256 thumbprint.`,`The thumbprint.`],["`private_key_jwt`",`A short-lived JWT the client signs with a private key whose public half AXIAM holds. Needs no transport-level cooperation at all.`,"A `jwks` inline, or a `jwks_uri` to publish at."]]},{type:`code`,caption:`registering a private_key_jwt client`,code:`POST /api/v1/oauth2-clients
{
  "client_name": "orders-batch",
  "token_endpoint_auth_method": "private_key_jwt",
  "jwks_uri": "https://orders.acme.dev/.well-known/jwks.json",
  "grant_types": ["client_credentials"]
}`},{type:`note`,text:"`private_key_jwt` is the method to reach for behind a TLS-terminating load balancer you do not control — which is exactly why FAPI 2.0 permits it alongside the mTLS methods. There is deliberately **no** `none` method: every AXIAM client is confidential, and a public-client value would let an operator register a client whose authentication is silently skipped."},{type:`h`,id:`audience`,text:`Machine tokens are a different audience`},{type:`warn`,text:"A service-account token carries `aud` of **`axiam:m2m`**, where a user token carries `axiam:user`. A route guard configured to expect the user audience will reject a perfectly valid machine token with a bare `401`, and nothing in that response says why — this is the single most confusing failure on this page. If a service account authenticates cleanly and is then refused by your own middleware, check the audience your verifier expects before checking anything else."},{type:`p`,text:`The split is deliberate rather than incidental: it lets a resource server decide that an endpoint is for humans, or for machines, without inspecting roles. Pin whichever audience the endpoint is actually for, and pin it explicitly — a verifier that checks no audience accepts both.`}]}],l=[{slug:`authz`,section:`Authorization`,navLabel:`Authorization engine`,title:`The authorization engine`,intro:`Role-based, default-deny, evaluated over a cascading resource hierarchy — and reachable over REST, gRPC or AMQP with the same answer from each.`,verifiedRelease:r,blocks:[{type:`h`,id:`model`,text:`The evaluation model`},{type:`p`,text:`Every check asks one question: *may this principal perform this action on this resource, in this scope?* The answer is computed from the grants attached to the roles the principal holds — directly, through a group, or inherited from an ancestor resource.`},{type:`p`,text:`Precedence is short and worth memorising, because everything else follows from it:`},{type:`steps`,steps:[{title:`Default deny`,body:"Nothing is permitted unless something permits it. An unknown action on an unknown resource is denied with `no_grant`, not with an error."},{title:`An applicable allow grant permits the action`,body:`Grants apply through directly assigned roles, group-inherited roles, and roles assigned on any ancestor of the resource.`},{title:`An applicable deny grant refuses it — and beats every allow`,body:`Wherever either sits in the hierarchy. Deny wins; it is not "most specific wins". See [Deny grants & precedence](#/docs/deny).`}]},{type:`h`,id:`hierarchy`,text:`Hierarchy & cascade`},{type:`p`,text:`Resources form a tree. A role assigned on a parent cascades to every descendant, so you grant broadly at the top and refine below rather than enumerating every leaf. A resource's ancestors and children are both queryable, which is what makes an admin UI able to explain *why* a decision came out the way it did.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/resources/{resource_id}/ancestors`,summary:`The chain a grant can cascade down from.`},{method:`GET`,path:`/api/v1/resources/{resource_id}/children`,summary:`The subtree a grant here would reach.`}]},{type:`h`,id:`worked`,text:`A worked resolution`},{type:`p`,text:`One tree, one allow at the top, one deny partway down — and the answer at every node. This is the whole cascade in one example.`},{type:`code`,caption:`the resource tree`,code:`/fleet                              <- allow read  (role assigned here)
├── /fleet/active
│   └── /fleet/active/unit-3
└── /fleet/decommissioned           <- deny read
    └── /fleet/decommissioned/unit-7`},{type:`table`,proseFirstCol:!0,headers:["Check `read` on",`Answer`,`Reason code`,`Why`],rows:[[`/fleet`,`allow`,"`allowed`",`The grant is here.`],[`/fleet/active`,`allow`,"`allowed`",`The allow cascades to a descendant.`],[`/fleet/active/unit-3`,`allow`,"`allowed`",`Depth does not weaken a cascade.`],[`/fleet/decommissioned`,`deny`,"`denied_by_rule`",`The deny is here, and it beats the allow it inherited from the parent.`],[`/fleet/decommissioned/unit-7`,`deny`,"`denied_by_rule`",`The deny cascades too. A deny reaches every descendant exactly as an allow does.`]]},{type:`p`,text:"Now add one more rule — an explicit allow of `read` on `/fleet/decommissioned/unit-7`, the deepest and most specific node — and ask again."},{type:`warn`,text:`The answer on that leaf is still **deny**. A child allow does not override an inherited deny, however specific the child is. This is the rule to internalise: **adding an allow can never re-open something a deny closed.** It is the property the design is built to guarantee, and it is asserted as a property test rather than left to convention.`},{type:`p`,text:`The consequence for a role model is direct: you cannot express *deny the subtree except this one leaf*. The answer is to narrow the deny rather than to widen the allow — move it to the nodes you actually mean, or split the subtree. Full precedence, including how denies behave through groups and across global versus resource-scoped roles, is on [Deny grants](#/docs/deny).`},{type:`h`,id:`scopes`,text:`Scopes`},{type:`p`,text:"A **scope** narrows a permission to part of a resource — `read` on a customer record limited to the `billing` scope, say, rather than to everything the record contains. A grant carries a list of scope ids; an empty list means *all scopes*, including unscoped checks."},{type:`warn`,text:`Read that wildcard rule carefully in the deny direction: a **resource-level deny with no scopes is stronger than a scoped one**, because it matches every request for that action regardless of what scope the request names.`},{type:`h`,id:`checks`,text:`Making a check`},{type:`p`,text:"Every SDK exposes the same `can(action, resource)` call — a plain yes/no — alongside a fuller form that also returns *why* a refusal was refused (`no_grant` versus `denied_by_rule`), and a batched form for when one request needs several decisions. The transport underneath is a deployment choice, not a semantic one."},{type:`api`,endpoints:[{method:`POST`,path:`/api/v1/authz/check`,summary:`One decision.`},{method:`POST`,path:`/api/v1/authz/check/batch`,summary:`Several decisions in one round trip.`}]},{type:`codegroup`,caption:`single and batched checks`,tabs:[{label:`TypeScript`,code:`// one decision
const allowed = await client.can('read', 'doc:1');

// several in one round trip — results preserve input order
const decisions = await client.batchCheck([
  { action: 'read', resourceId: 'doc:1' },
  { action: 'write', resourceId: 'doc:1' },
]);`},{label:`Python`,code:`allowed = client.can("read", resource_id)

decisions = client.batch_check([
    AccessCheck(action="read", resource_id=resource_id),
    AccessCheck(action="write", resource_id=resource_id),
])`},{label:`Rust`,code:'// `can` answers yes/no; `check_access` returns the full decision,\n// including the reason a refusal was refused.\nlet allowed = client.can("read", resource_id, None).await?;\nlet decision = client.check_access("read", resource_id, None).await?;'},{label:`Go`,code:`allowed, err := client.Can(ctx, "read", resourceID)

allowed, reason, err := client.CheckAccess(ctx, "read", resourceID)

results, err := client.BatchCheck(ctx, []axiam.AccessCheck{
    {Action: "read", ResourceID: resourceID},
    {Action: "write", ResourceID: resourceID},
})`},{label:`Java`,code:`boolean allowed = client.can("read", "doc:1");`}]},{type:`h`,id:`guards`,text:`Declarative guards`},{type:`p`,text:"Calling `can()` by hand at the top of a handler works, and is easy to forget. Every SDK also ships the declarative form idiomatic to its language — a middleware, a dependency, an attribute macro or an annotation — so the requirement sits on the route rather than buried in its body."},{type:`codegroup`,caption:`the same guard, four ways`,tabs:[{label:`TypeScript`,code:`import { requireAccess, fromParam } from 'axiam-sdk/middleware';

app.get(
  '/documents/:id',
  requireAccess(authzSession, 'read', fromParam('id')),
  (req, res) => res.json({ documentId: req.params.id }),
);`},{label:`Python`,code:`from axiam_sdk.fastapi import AxiamUser, require_access

require_doc_read = require_access(
    verifier, "acme", authz_client, "documents:read", resource_param="doc_id"
)


@app.get("/docs/{doc_id}")
async def get_doc(doc_id: str, user: AxiamUser = Depends(require_doc_read)):
    return {"message": f"user {user.user_id} may read document {doc_id}"}`},{label:`Rust`,code:`use axiam_sdk::{require_access, require_auth};
use axiam_sdk::middleware::AxiamUser;

#[require_access(action = "read", resource_param = "id")]
async fn get_document(user: AxiamUser) -> String {
    format!("user {} may read this document", user.user_id)
}`},{label:`Java`,code:`@RestController
public class DocumentController {

    @AxiamRequireAccess(action = "read", resourceParam = "id")
    @GetMapping("/documents/{id}")
    public String read(@PathVariable("id") String id) {
        return "document " + id;
    }
}`}]},{type:`note`,text:`Each of these runs strictly **after** authentication, and issues the check for the *request's* authenticated caller — never for the application's own, usually service-account, session. That distinction is the one thing to get right when writing a guard by hand, and it is why using the shipped one is worth it.`},{type:`warn`,text:"Every SDK also offers a `requireRole`-style helper. It is a **local, no-round-trip** check against the verified identity's roles: cheaper, coarser, and **not** a substitute for the authoritative resource-level check. A role check cannot see a deny grant on the resource."},{type:`h`,id:`perf`,text:`The hot path`},{type:`p`,text:`An authorization check is the most frequently executed operation in the product, so the evaluation is a **single pass over grants that were already fetched**. Denies live on the same edge as allows and arrive in the same batched read — there is no deny-specific query, and a tenant with no deny rules pays nothing for the feature existing.`},{type:`p`,text:`Deny short-circuits and allows are only remembered, so the result does not depend on the order grants come back in: a rule set has one answer, not one answer per query plan.`},{type:`p`,text:`Where checks dominate your traffic, two levers matter. **Database CPU** is the main ceiling — measured gains of roughly 90% from a second pair of database cores. The optional **decision cache** sits on top: transformative on gRPC checks, marginal on REST ones, because REST's per-request session-cookie validation is a database read the cache does not cover. Both are covered in [Configuration](#/docs/configuration).`},{type:`note`,text:`Enabling the decision cache changes performance only, never the decision returned. Every access-narrowing mutation invalidates the affected entries immediately, so a revocation cannot leave a stale allow behind; the TTL is a bounded-staleness backstop, not the invalidation mechanism.`}]},{slug:`rbac`,section:`Authorization`,navLabel:`Roles & resources`,title:`Roles, permissions, resources & groups`,intro:`The entities the engine evaluates over, how they relate, and the API for managing each.`,blocks:[{type:`h`,id:`shape`,text:`How they fit together`},{type:`code`,caption:`the access graph`,code:`User ──────┐
           ├──> Role ──> Permission (action + effect [+ scopes])
Group ─────┘              │
  ▲                       └──> applies at a Resource
  └─ users inherit every role assigned to their groups

Resource tree:   /fleet
                   └── /fleet/decommissioned
                         └── /fleet/decommissioned/unit-7
                 a role assigned on /fleet reaches all three`},{type:`h`,id:`permissions`,text:`Permissions`},{type:`p`,text:"A permission is an action name plus an effect. AXIAM seeds **113 built-in permissions across 25 families** into every tenant at bootstrap — `users:*`, `roles:*`, `resources:*`, `oauth2_clients:*`, `certificates:*`, `ca_certificates:*`, `audit_logs:*`, `federation:*`, `webhooks:*`, `reactors:*`, `scim_tokens:*`, `gdpr:*` and the rest. These are the actions the REST API's own route guards check against, so an administrator's authority over AXIAM is expressed in the same model as an application's authority over its own resources."},{type:`p`,text:`You are not limited to them: define your own action names for your application's domain and grant them the same way.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/permissions`,summary:`List permissions in the tenant.`},{method:`POST`,path:`/api/v1/permissions`,summary:`Define one.`},{method:`GET`,path:`/api/v1/permissions/{permission_id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/permissions/{permission_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/permissions/{permission_id}`,summary:`Delete it.`}]},{type:`h`,id:`roles`,text:`Roles`},{type:`p`,text:"A role is a named collection of permissions, assignable to users and to groups, and either global or bound to a specific resource. Three are seeded at bootstrap: `super-admin` (every permission), `admin` (all entity CRUD) and `viewer` (list and get only)."},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/roles`,summary:`List roles.`},{method:`POST`,path:`/api/v1/roles`,summary:`Create one.`},{method:`PUT`,path:`/api/v1/roles/{role_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/roles/{role_id}`,summary:`Delete it.`},{method:`POST`,path:`/api/v1/roles/{role_id}/permissions`,summary:`Grant a permission to the role.`},{method:`DELETE`,path:`/api/v1/roles/{role_id}/permissions/{permission_id}`,summary:`Revoke it.`},{method:`POST`,path:`/api/v1/roles/{role_id}/users`,summary:`Assign the role to a user.`},{method:`DELETE`,path:`/api/v1/roles/{role_id}/users/{user_id}`,summary:`Unassign it.`},{method:`POST`,path:`/api/v1/roles/{role_id}/groups`,summary:`Assign the role to a group.`},{method:`DELETE`,path:`/api/v1/roles/{role_id}/groups/{group_id}`,summary:`Unassign it.`}]},{type:`h`,id:`groups`,text:`Groups`},{type:`p`,text:`A group is a named collection of users, and roles assigned to it are inherited by every member. Groups are how you avoid per-user grants — and how you make offboarding a single operation rather than an audit.`},{type:`note`,text:`Group membership inherits **denies** exactly as it inherits allows. A deny reached through a group beats an allow assigned directly to the user.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/groups`,summary:`List groups.`},{method:`POST`,path:`/api/v1/groups`,summary:`Create one.`},{method:`PUT`,path:`/api/v1/groups/{group_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/groups/{group_id}`,summary:`Delete it.`},{method:`GET`,path:`/api/v1/groups/{group_id}/members`,summary:`List members.`},{method:`POST`,path:`/api/v1/groups/{group_id}/members`,summary:`Add a member.`},{method:`DELETE`,path:`/api/v1/groups/{group_id}/members/{user_id}`,summary:`Remove one.`}]},{type:`h`,id:`resources`,text:`Resources & scopes`},{type:`p`,text:`Resources are the things being protected, arranged in a tree. Model them to match how authority actually delegates in your domain — an organizational unit, a project, a device fleet — because that shape is what a grant cascades along.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/resources`,summary:`List resources.`},{method:`POST`,path:`/api/v1/resources`,summary:`Create one, optionally under a parent.`},{method:`PUT`,path:`/api/v1/resources/{resource_id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/resources/{resource_id}`,summary:`Delete it.`},{method:`GET`,path:`/api/v1/resources/{resource_id}/scopes`,summary:`List its scopes.`},{method:`POST`,path:`/api/v1/resources/{resource_id}/scopes`,summary:`Define a scope on it.`},{method:`PUT`,path:`/api/v1/resources/{resource_id}/scopes/{scope_id}`,summary:`Update a scope.`},{method:`DELETE`,path:`/api/v1/resources/{resource_id}/scopes/{scope_id}`,summary:`Delete one.`}]},{type:`h`,id:`design`,text:`Designing a role model that survives`},{type:`list`,items:[`**Grant to groups, not to users.** A per-user grant is invisible in every summary view and is the thing that outlives an employee's departure.`,`**Grant high in the tree, refine low.** Enumerating leaves is a model that has to be maintained forever as the tree grows.`,`**Use deny for carve-outs, and keep them narrow.** A deny cannot be undone by an allow, which is its value and its cost — you cannot express "deny the subtree except this leaf".`,"**Name actions after what they permit, not after who does them.** `invoices:approve` survives a reorganisation; `finance-team-action` does not.",`**Prefer scopes to resource explosion.** If you are creating one resource per field, you probably want one resource with scopes.`]}]},{slug:`deny`,section:`Authorization`,navLabel:`Deny grants`,title:`Deny grants & precedence`,intro:`An explicit deny refuses regardless of what else allows it — at any depth of the hierarchy and at equal specificity. This page is the precedence table, and the reasoning behind choosing that rule.`,verifiedRelease:r,blocks:[{type:`h`,id:`rule`,text:`The rule`},{type:`p`,text:"A permission grant carries an `effect` of `allow` or `deny`. Evaluation is: **default deny → an applicable allow permits → an applicable deny refuses and beats every allow.**"},{type:`p`,text:`Deny wins. Always. It is not *most specific wins*, and the difference is the whole design.`},{type:`h`,id:`table`,text:`Worked precedence table`},{type:`p`,text:"Resources: `/fleet` → `/fleet/decommissioned` → `/fleet/decommissioned/unit-7`. Every check below is on the leaf."},{type:`table`,proseFirstCol:!0,headers:[`Rules`,`Check`,`Result`,`Why`],rows:[["allow `read` on `/fleet`",`read`,`**allow**`,`An allow cascades to descendants.`],[`*(nothing)*`,`read`,"**deny** (`no_grant`)",`Default deny.`],["allow `read` on `/fleet`, deny `read` on `/fleet/decommissioned`",`read`,"**deny** (`denied_by_rule`)",`A deny cascades down and beats the ancestor allow.`],["deny `read` on `/fleet`, allow `read` on `/fleet/decommissioned`",`read`,"**deny** (`denied_by_rule`)",`**A child allow does not override an inherited deny.** This is the row to read twice.`],["allow `read` on `/fleet`, deny `write` on `/fleet`",`read`,`**allow**`,"Denies are per action; `write` says nothing about `read`."],[`allow and deny of the same action on the same node`,`read`,"**deny** (`denied_by_rule`)",`Deny wins at equal specificity too — there is no tie to break.`],[`allow via a directly assigned role, deny via a group-inherited role`,`read`,"**deny** (`denied_by_rule`)",`Denies inherit through groups exactly as allows do.`],[`allow via a global role, deny on a resource-scoped role`,`read`,"**deny** (`denied_by_rule`)",`Global versus resource-scoped changes applicability, not precedence.`]]},{type:`h`,id:`scopes`,text:`Scope interaction`},{type:`table`,proseFirstCol:!0,headers:[`Deny rule`,`Effect`],rows:[["deny `read`, **no scopes** (wildcard)","Masks `read` entirely on that node and its descendants — every scope, and unscoped checks too."],["deny `read`, scopes = [`pii`]","Masks only `read` + `pii`. `read` + `billing` and unscoped `read` are unaffected."]]},{type:`warn`,text:`A resource-level deny is *stronger* than a scope-level one, because it matches every request for that action regardless of the scope named. If you meant to carve out one scope, say so — an empty scope list is a wildcard, not a default.`},{type:`h`,id:`why`,text:`Why deny-override and not most-specific-wins`},{type:`p`,text:`Most-specific-wins reads as more expressive, and it is a security footgun. Under it the meaning of a deny depends on where every *other* rule sits, which produces three properties nobody wants:`},{type:`list`,items:[`Adding an allow deeper in the tree silently re-opens something a deny closed.`,`A reviewer cannot answer "is X denied?" by looking at the deny — they must enumerate every rule that might out-specify it.`,`Moving a resource in the hierarchy can change access without any rule changing.`]},{type:`p`,text:`Deny-override buys one property that is worth more than the expressiveness: **adding a deny can never widen access, and can never be undone by adding allows.** That is checkable, and it is asserted as a property test rather than left as a claim.`},{type:`note`,text:`The cost is real and worth stating: you cannot express "deny the subtree, except this one leaf". The answer is to narrow the deny, not to widen the allow.`},{type:`h`,id:`using`,text:`Using deny well`},{type:`list`,items:[`**Reach for deny for carve-outs and containment**, not as the everyday tool. A model where most rules are denies is a model whose allows are too broad.`,`**Keep each deny as narrow as the thing you are actually excluding** — the right action, the right scope, the lowest node that covers it.`,`**Denies are excellent for incident response.** One deny on a compromised principal's role closes access across the whole tree immediately, and no allow anywhere can reopen it.`,`**Expect surprise on row 4.** Somebody will eventually add an allow below a deny and file a bug. It is working correctly; that is the property.`]},{type:`links`,links:[{label:`Deny-override design note`,href:`https://github.com/ilpanich/axiam/blob/main/claude_dev/deny-override-design.md`,note:`The full precedence table, the scope interaction, and why most-specific-wins was rejected.`}]}]},{slug:`uma`,section:`Authorization`,navLabel:`UMA 2.0`,title:`UMA 2.0 — protecting resources you do not own`,intro:`A resource server describes what a request would require; AXIAM decides whether the requesting party may have it. The resource server never decides.`,blocks:[{type:`h`,id:`problem`,text:`The problem it solves`},{type:`p`,text:`A service guards resources it does not own. A caller arrives without the authority to touch them, and the service needs a way to say *what the caller would need* — precisely, and without becoming the authority itself.`},{type:`p`,text:`User-Managed Access is that mechanism. The guarding service — the **resource server** — registers its resources, asks AXIAM to mint a **permission ticket** describing what a given request requires, and the ticket is exchanged for a **Requesting Party Token** (RPT) that says the authorization engine agreed.`},{type:`note`,text:`The rule everything here serves: **the resource server never decides.** It describes what it needs; AXIAM decides, using the same RBAC check a live request would run. That is why a deny grant vetoes an RPT exactly as it vetoes anything else — it *is* the same check.`},{type:`h`,id:`mapping`,text:`What maps onto what`},{type:`table`,proseFirstCol:!0,headers:[`UMA`,`AXIAM`,`Note`],rows:[["resource set `_id`",`the resource id`,`**The same id** — there is no parallel resource store.`],["resource set `name` / `type`","`resource.name` / `resource.resource_type`",``],["`resource_scopes`","`Scope` rows on that resource",`The allow-list of names a resource server may ask for.`],[`a resource scope, when evaluated`,`the AXIAM **action**`,"`view` on the resource, not a sub-resource scope."],[`PAT`,"an access token with the `uma_protection` scope",`An ordinary client-credentials token.`],[`RPT`,"an access token with a `permissions` claim",`An ordinary token; the claim is what makes it an RPT.`]]},{type:`p`,text:`A resource registered through UMA is an ordinary AXIAM resource. It appears in the admin console, role assignments cascade through it, and the engine already understands it. The only thing marking it as UMA-registered is a read-only provenance field.`},{type:`h`,id:`endpoints`,text:`The Protection API`},{type:`api`,endpoints:[{method:`GET`,path:`/.well-known/uma2-configuration`,summary:`Discovery — token, introspection, permission and registration endpoints.`,public:!0},{method:`GET`,path:`/uma2/rreg/resource_set`,summary:`List registered resource sets.`},{method:`POST`,path:`/uma2/rreg/resource_set`,summary:`Register one.`},{method:`GET`,path:`/uma2/rreg/resource_set/{id}`,summary:`Read one.`},{method:`PUT`,path:`/uma2/rreg/resource_set/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/uma2/rreg/resource_set/{id}`,summary:`Deregister it.`},{method:`POST`,path:`/uma2/perm`,summary:`Mint a permission ticket for a request that lacked authority.`}]},{type:`h`,id:`flow`,text:`The flow`},{type:`steps`,steps:[{title:`Register the resource`,body:"Once, at deployment or on first use. The resource server holds a PAT — an ordinary client-credentials token carrying the `uma_protection` scope."},{title:`A caller arrives without authority`,body:`The resource server does not guess and does not decide. It asks AXIAM for a permission ticket naming the resource and the scopes this request would need.`,code:`POST /uma2/perm
{ "resource_id": "<id>", "resource_scopes": ["view"] }`},{title:`Return the ticket to the caller`,body:'As an RFC 7235 `WWW-Authenticate: UMA realm=..., as_uri=..., ticket=...` challenge — the standard way of saying "here is what you would need, and here is who can grant it".'},{title:`The caller exchanges the ticket for an RPT`,body:`At the token endpoint, using the UMA ticket grant. AXIAM runs the ordinary RBAC check for that requesting party. A deny grant refuses here exactly as it would refuse a live request.`},{title:`The caller retries with the RPT`,body:"The resource server validates it — introspection, or offline verification against the tenant JWKS — and reads the `permissions` claim."}]},{type:`note`,text:"A `403` from the Protection API at `/uma2/*` is an ordinary authorization refusal and maps to an authorization error in the SDKs, not to an OAuth2 protocol error. A refusal from the ticket grant at the token endpoint is an OAuth2 protocol error and carries an `error` field to dispatch on — see [Error reference](#/docs/errors)."}]}],u=[{slug:`oauth2`,section:`OAuth2 & OIDC`,navLabel:`Authorization server`,title:`OAuth2 & OpenID Connect`,intro:`AXIAM is a complete OAuth2 authorization server and OpenID Connect provider — discovery, JWKS, five grant types, introspection, revocation and userinfo.`,verifiedRelease:r,blocks:[{type:`h`,id:`grants`,text:`Supported grants`},{type:`table`,proseFirstCol:!0,headers:[`Grant`,`Use it for`,`Notes`],rows:[[`Authorization Code + PKCE`,`Browser and mobile applications.`,`PKCE is the expected shape for public clients; pushed authorization requests (PAR) can be required per client.`],[`Client Credentials`,`Machine-to-machine access with a service account or confidential client.`,`Authenticate with a secret, or with mutual TLS instead — see [FAPI 2.0 & mTLS clients](#/docs/fapi2).`],[`Refresh Token`,`Extending a session without re-authenticating.`,`Opaque, server-stored, single-use, rotating on every refresh.`],[`Device Authorization`,`Televisions, CLIs, headless commissioning — anything with no browser.`,`See [Device authorization grant](#/docs/device-flow).`],[`Token Exchange`,`A service calling another service on a user's behalf.`,`Narrowing only, always. See [Token exchange](#/docs/token-exchange).`]]},{type:`h`,id:`endpoints`,text:`Endpoints`},{type:`api`,endpoints:[{method:`GET`,path:`/.well-known/openid-configuration`,summary:`OIDC discovery document.`,public:!0},{method:`GET`,path:`/oauth2/jwks`,summary:`Signing keys, cached with an ETag and Cache-Control.`,public:!0},{method:`GET`,path:`/oauth2/authorize`,summary:`Authorization endpoint. Tenant is derived from client_id.`,public:!0},{method:`POST`,path:`/oauth2/par`,summary:`Pushed authorization request (RFC 9126).`,public:!0},{method:`POST`,path:`/oauth2/token`,summary:`Token endpoint — every grant.`,public:!0},{method:`POST`,path:`/oauth2/introspect`,summary:`Token introspection (RFC 7662).`,public:!0},{method:`POST`,path:`/oauth2/revoke`,summary:`Token revocation (RFC 7009).`,public:!0},{method:`GET`,path:`/oauth2/userinfo`,summary:`OIDC userinfo claims for the bearer.`,public:!0},{method:`POST`,path:`/oauth2/device_authorization`,summary:`Begin a device grant (RFC 8628).`,public:!0},{method:`GET`,path:`/oauth2/end_session`,summary:`RP-initiated logout.`,public:!0}]},{type:`h`,id:`tenant`,text:`Naming the tenant`},{type:`p`,text:`AXIAM is multi-tenant and has no default tenant, so every OAuth2 request has to say which one it means. There are two mechanisms, and which applies depends on the endpoint:`},{type:`list`,items:["**`tenant_id` as a query parameter** — required on `/oauth2/token`, `/oauth2/par` and `/oauth2/end_session`.","**Derived from `client_id`** — on `/oauth2/authorize`, because an OAuth2 client belongs to exactly one tenant."]},{type:`warn`,text:"The discovery document is **deployment-wide**, not per-tenant: one issuer, served at `/.well-known/openid-configuration`. `AXIAM__AUTH__OAUTH2_ISSUER_URL` must be an origin — a path-based issuer is rejected at startup. An off-the-shelf OIDC client that cannot add a `tenant_id` query parameter to the token endpoint therefore needs a small shim, or a per-tenant gateway route in front of AXIAM."},{type:`code`,caption:`discovery`,code:`curl -s https://iam.acme.dev/.well-known/openid-configuration | jq .

# and the keys a relying party verifies tokens with
curl -s https://iam.acme.dev/oauth2/jwks | jq .`},{type:`h`,id:`code-flow`,text:`Authorization Code + PKCE, end to end`},{type:`p`,text:`The grant to use for anything with a browser or a mobile front end. PKCE binds the eventual token request to the party that started the flow, so an authorization code intercepted on the redirect is worthless without the verifier that never left the client.`},{type:`steps`,steps:[{title:`Generate a verifier and its challenge`,body:"The verifier is a high-entropy random string the client keeps. The challenge is its SHA-256, base64url-encoded. Only `S256` is accepted — `plain` would defeat the purpose, since the value on the wire would be the secret itself.",code:`code_verifier  = <43-128 chars, base64url, random>
code_challenge = BASE64URL(SHA256(code_verifier))`},{title:`Send the user to the authorization endpoint`,body:"The tenant is derived from `client_id` here — an OAuth2 client belongs to exactly one tenant — so no `tenant_id` parameter is needed on this call. `state` is yours to check on the way back.",code:`GET /oauth2/authorize
  ?response_type=code
  &client_id=<client-id>
  &redirect_uri=https://app.acme.dev/callback
  &scope=openid%20profile%20email
  &state=<opaque-to-the-server>
  &code_challenge=<challenge>
  &code_challenge_method=S256`},{title:`The user authenticates and consents`,body:`AXIAM runs whatever the tenant requires — password or OPAQUE, then MFA or a passkey. None of that is the client's concern; the client sees only the redirect that follows.`},{title:`Handle the redirect`,body:"Check `state` against what you sent, and check `iss` names the server you started with. The code is single-use and short-lived.",code:`GET https://app.acme.dev/callback
  ?code=<authorization-code>
  &state=<what-you-sent>
  &iss=https://iam.acme.dev`},{title:`Redeem the code for tokens`,body:"`tenant_id` **is** required here, as a query parameter, because the token endpoint does not derive it. Send the verifier, not the challenge — the server recomputes the hash and compares.",code:`POST /oauth2/token?tenant_id=<uuid>
Content-Type: application/x-www-form-urlencoded

grant_type=authorization_code
&code=<authorization-code>
&redirect_uri=https://app.acme.dev/callback
&client_id=<client-id>
&code_verifier=<verifier>`}]},{type:`warn`,text:"The `tenant_id` asymmetry between the two endpoints is the single most common integration mistake here. `/oauth2/authorize` derives the tenant from `client_id`; `/oauth2/token`, `/oauth2/par` and `/oauth2/end_session` all require it as a query parameter. An off-the-shelf OIDC client that cannot add one needs a shim or a per-tenant gateway route."},{type:`h`,id:`iss`,text:`Checking who answered — RFC 9207`},{type:`p`,text:"Every authorization response carries an `iss` parameter naming the server that sent it, and AXIAM emits it for **every** client rather than only FAPI ones. It defends against the mix-up attack: a client that talks to more than one authorization server on a shared redirect URI cannot otherwise tell which one answered, so an attacker controlling one of them can have a code minted by an honest server delivered to their own token endpoint."},{type:`note`,text:"It is on the **error** redirect too, and validating it there is not a formality — one variant of the mix-up attack works by injecting an error response, so a client that checks `iss` on success and skips it on failure has left the door it just closed ajar. Making emission conditional was rejected for the matching reason: mix-up is precisely the attack a client does not know it is under, so protection that depends on somebody remembering to switch it on is not protection."},{type:`h`,id:`dpop`,text:`DPoP — binding a token to a key`},{type:`p`,text:"A bearer token is usable by whoever holds it. DPoP (RFC 9449) binds one to a key pair the client generates: the token carries a `cnf` claim naming the key's thumbprint, and each request carries a proof signed with it. A stolen token alone is then not enough."},{type:`warn`,text:"A token carrying `cnf` is **not** a bearer token, and a resource server that accepts one without verifying possession has silently converted it back into one. The SDK contract makes the check mandatory rather than optional, and phrases it as *reject when you cannot verify* rather than *verify when you can*: middleware that does not understand `cnf` must refuse the token, not ignore the claim. See [FAPI 2.0 & mTLS clients](#/docs/fapi2)."},{type:`h`,id:`tokens`,text:`The tokens you get back`},{type:`p`,text:"Access tokens are EdDSA (Ed25519) JWTs, short-lived, and verifiable offline against the JWKS — which is the fast path for a resource server, and the one to prefer over introspecting on every request. ID tokens carry `sid`, the session identifier, which is stable across refresh. Refresh tokens are opaque and single-use."},{type:`p`,text:`Introspection exists for the cases offline verification cannot answer: whether a token has been revoked since it was issued, and what an opaque token refers to. A resource server that introspects on every request should expect to make roughly ten to twenty introspection calls per token issued — the shipped rate limits are sized for that ratio.`},{type:`h`,id:`anatomy`,text:`What is inside an access token`},{type:`table`,proseFirstCol:!0,headers:[`Claim`,`Meaning`,`When present`],rows:[["`sub`",`The subject's id.`,`Always.`],["`tenant_id`, `org_id`",`The tenant and organization the token is scoped to.`,`Always — there is no default tenant.`],["`iss`",`The issuing origin.`,`Always.`],["`iat`, `exp`",`Issued-at and expiry, as Unix timestamps.`,`Always.`],["`jti`",`Token id. For a user flow this is the issuing session's id, which is what makes session revocation able to find it; for machine-to-machine it is a random UUID.`,`Always.`],["`aud`","`axiam:user` or `axiam:m2m`.",`Always on a current token.`],["`scope`",`Space-separated OAuth2 scopes.`,`When non-empty scopes were granted.`],["`sub_kind`",`Whether the subject is a user, a service account or an OAuth2 client. Informational — it does not affect validation or authorization.`,`Always.`],["`cnf`",`The confirmation key this token is bound to, by certificate thumbprint or JWK thumbprint.`,`Only on a sender-constrained token.`],["`act`",`RFC 8693 actor claim — who is acting for the subject. Nested for chained delegation and depth-capped.`,`Only on a **delegation** exchange. Deliberately absent on impersonation.`],["`permissions`",`The UMA resource and scope pairs allowed at issue time. Its presence is what makes a token an RPT; there is no separate token type.`,`Only on an RPT.`]]},{type:`table`,headers:[`Token`,`Lifetime`,`Shape`],rows:[[`Access token`,`15 minutes`,`EdDSA (Ed25519) JWT, verifiable offline against the JWKS.`],[`Authorization code`,`10 minutes`,`Single-use.`],[`Refresh token`,`30 days`,`Opaque, server-stored, single-use, rotating on every refresh.`],[`MFA challenge`,`5 minutes`,`Single-use.`]]},{type:`note`,text:"Two claims are worth reading twice. `act` is absent on an impersonation exchange **by design** — impersonation's whole definition is that the result is indistinguishable from a token the subject obtained directly, which is why it is off by default, gated per client, and visible only in the audit record. And `permissions` is a record of a decision already made, never an input to a future one: nothing in the authorization path reads it to grant anything, and a live check re-evaluates against the engine."},{type:`h`,id:`par`,text:`Pushed authorization requests`},{type:`p`,text:"With PAR (RFC 9126) the client posts the authorization parameters to AXIAM directly and receives a `request_uri` to put in the browser redirect, instead of placing every parameter in a URL the user agent can see and modify. It can be required per client, and **is** required for any client registered under the FAPI 2.0 profile. The flow, the four rules that trip implementations up and the SDK helpers are on [Pushed authorization](#/docs/par)."},{type:`h`,id:`clients`,text:`Registering clients`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/oauth2-clients`,summary:`List the tenant's clients.`},{method:`POST`,path:`/api/v1/oauth2-clients`,summary:`Register one.`},{method:`GET`,path:`/api/v1/oauth2-clients/{id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/oauth2-clients/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/oauth2-clients/{id}`,summary:`Delete it.`}]},{type:`code`,caption:`a browser application`,code:`POST /api/v1/oauth2-clients
{
  "name": "acme-web",
  "redirect_uris": ["https://app.acme.dev/callback"],
  "grant_types": ["authorization_code", "refresh_token"],
  "scopes": ["openid", "profile", "email"]
}`},{type:`note`,text:`The SDKs ship OIDC relying-party helpers — the redirect, the PKCE verifier, the callback exchange and the token store — so integrating a web application is not a matter of hand-rolling the flow. See [Client SDKs](#/docs/sdks).`}]},{slug:`device-flow`,section:`OAuth2 & OIDC`,navLabel:`Device grant`,title:`Device authorization grant`,intro:`For clients that cannot show a browser or accept typed input: a television, a set-top box, a CLI on a headless machine, an IoT sensor being commissioned.`,blocks:[{type:`h`,id:`flow`,text:`The flow`},{type:`p`,text:`The device shows a short code; the user types it on a phone or laptop; the device polls until they approve. AXIAM implements the non-interactive path of RFC 8628 in full.`},{type:`code`,caption:`who talks to whom`,code:`  device                        AXIAM                    user's phone
    |                             |                            |
    |-- POST /oauth2/device_authorization -->                  |
    |<- device_code, user_code, verification_uri --            |
    |                             |                            |
  shows "go to id.acme.dev/device and enter WXYZ-1234"        |
    |                             |<-- GET /api/v1/device/verify
    |                             |--- client + scopes ------->|
    |                             |<-- POST /api/v1/device/decide
    |-- POST /oauth2/token (device_code) -->                   |
    |<- authorization_pending ----|   (repeatedly, at interval)|
    |-- POST /oauth2/token (device_code) -->                   |
    |<- access_token + refresh_token                           |`},{type:`h`,id:`start`,text:`1. Start`},{type:`code`,code:`POST /oauth2/device_authorization?tenant_id=<uuid>
Content-Type: application/x-www-form-urlencoded

client_id=oa_...&scope=openid`},{type:`code`,caption:`response`,code:`{
  "device_code": "...",
  "user_code": "WXYZ-1234",
  "verification_uri": "https://id.acme.dev/device",
  "verification_uri_complete": "https://id.acme.dev/device?user_code=WXYZ-1234",
  "expires_in": 600,
  "interval": 5
}`},{type:`note`,text:"Show `verification_uri` and `user_code` separately, and use `verification_uri_complete` for a QR code. A user who scans the code should not then have to type it."},{type:`h`,id:`approve`,text:`2. The user approves`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/device/verify`,summary:`What the verification page needs: the client and the scopes being requested.`},{method:`POST`,path:`/api/v1/device/decide`,summary:`Approve or deny the pending request.`}]},{type:`p`,text:`These two back the verification page. The user is authenticated at AXIAM in the ordinary way before deciding — the page shows *which application* is asking and *what for*, because a code typed from a screen carries no context on its own.`},{type:`h`,id:`poll`,text:`3. The device polls`},{type:`p`,text:"The device posts the device grant to the token endpoint at `interval` seconds. Every non-success answer is one of four RFC 8628 errors, and each has exactly one correct reaction:"},{type:`table`,headers:[`Answer`,`Meaning`,`What the device does`],rows:[[`authorization_pending`,`The user has not decided yet.`,`Keep polling at the same interval.`],[`slow_down`,`You are polling too fast.`,`Increase the interval, then keep polling.`],[`access_denied`,`The user refused.`,`Stop. Show that it was declined.`],[`expired_token`,`The device code expired.`,`Stop, and start a new request with a fresh code.`]]},{type:`warn`,text:"Polling faster than `interval` is what earns `slow_down`, and ignoring `slow_down` is what earns a rate-limit refusal. Treat the interval as a floor set by the server, not as a suggestion."}]},{slug:`token-exchange`,section:`OAuth2 & OIDC`,navLabel:`Token exchange`,title:`Token exchange (RFC 8693)`,intro:`A service holding a user's token needs a narrower one to call the next service — fewer scopes, a specific audience, and a record of who acted for whom.`,blocks:[{type:`h`,id:`why`,text:`Why not just forward the token`},{type:`p`,text:`A service in a mesh receives a request carrying a user's access token. To call a second service on that user's behalf it has two bad options and one good one. Forwarding the user's token verbatim is over-privileged, and the second service cannot tell the caller from the user. Using the service's own credentials has the right privileges and loses the user context entirely.`},{type:`p`,text:`Token exchange is the third option: present the token you hold, receive one that is *narrower* and that records the delegation.`},{type:`note`,text:`The rule everything below serves: **an exchange may only ever narrow.** There is no parameter, no configuration and no client grant that makes the issued token permit something the subject token did not already permit.`},{type:`h`,id:`request`,text:`The request`},{type:`code`,code:`POST /oauth2/token?tenant_id=<uuid>
Content-Type: application/x-www-form-urlencoded

grant_type=urn:ietf:params:oauth:grant-type:token-exchange
&subject_token=<jwt>
&subject_token_type=urn:ietf:params:oauth:token-type:access_token
&actor_token=<jwt>                    # optional — presence selects delegation
&actor_token_type=urn:ietf:params:oauth:token-type:access_token
&scope=read:orders write:orders       # optional — defaults to the subject's scopes
&audience=https://orders.internal     # optional
&resource=https://orders.internal/v1  # optional
&client_id=oa_...&client_secret=...`},{type:`table`,headers:[`Parameter`,`Required`,`Notes`],rows:[[`grant_type`,`yes`,"`urn:ietf:params:oauth:grant-type:token-exchange`"],[`subject_token`,`yes`,`The token being exchanged. AXIAM-issued access tokens only.`],[`subject_token_type`,`yes`,"`urn:ietf:params:oauth:token-type:access_token`"],[`actor_token`,`no`,`Present ⇒ **delegation**. Absent ⇒ **impersonation**.`],[`actor_token_type`,`with actor_token`,`Same value as above.`],[`scope`,`no`,`Must be a subset of the subject's scopes. Defaults to all of them.`],[`audience / resource`,`no`,`Narrows who the issued token is for.`]]},{type:`h`,id:`modes`,text:`Delegation vs impersonation`},{type:`p`,text:"Supplying an `actor_token` selects **delegation**: the issued token says *this actor is acting for this subject*, and both are visible to whatever receives it. Omitting it selects **impersonation**: the issued token speaks as the subject alone."},{type:`p`,text:`Prefer delegation. Impersonation is occasionally the only thing a downstream service will accept, but it erases the caller from the audit trail at exactly the moment you would most want to know who it was.`},{type:`h`,id:`narrowing`,text:`What narrowing means, concretely`},{type:`list`,items:[`**Scopes** — the requested set must be a subset of the subject token's. Asking for more is refused, not silently trimmed.`,`**Audience** — an issued token can be bound to one downstream service, so a leak at that service does not yield a token usable everywhere.`,`**Lifetime** — the issued token never outlives the subject token.`,`**Authorization** — the subject's own grants still apply. Exchange does not confer authority; it repackages it.`]},{type:`warn`,text:`Because exchange only narrows, it is not a privilege-escalation mechanism — and it is also not a way to *grant* a service access to something the user cannot reach. If a downstream call needs authority the user does not have, that is a role-model problem, not an exchange problem.`}]},{slug:`logout`,section:`OAuth2 & OIDC`,navLabel:`Logout & sessions`,title:`Logout — RP-initiated and back-channel`,intro:`When a session ends, who else needs to know? AXIAM implements both halves of the answer.`,blocks:[{type:`h`,id:`why`,text:`Two holes, two mechanisms`},{type:`p`,text:`A federation deployment is AXIAM plus N relying parties, and each logout mechanism closes a hole the other leaves open:`},{type:`list`,items:[`**Without RP-initiated logout**, a user who logs out of a relying party stays logged in at AXIAM — so the next "sign in with AXIAM" silently signs them straight back in. From the user's point of view, they did not log out.`,`**Without back-channel logout**, a user who logs out *of AXIAM* stays logged in at every relying party indefinitely, because nothing tells them. This is the one that matters in an incident: revoking a compromised account otherwise leaves N live sessions behind.`]},{type:`h`,id:`sessions`,text:`Sessions, not users`},{type:`p`,text:"Both operate on a **session**. A user with a phone and a laptop who logs out on the laptop expects the phone to stay signed in, and the specification agrees. That is why AXIAM's ID tokens carry `sid`, and why every logout token names it — an ID token identifying only the user would force both halves to be all-or-nothing."},{type:`note`,text:"`sid` is stable across refresh: a token minted by the refresh grant carries the same `sid` as the one issued at login, so a relying party that stored it can still match a logout token to its own session."},{type:`h`,id:`rp`,text:`RP-initiated logout`},{type:`api`,endpoints:[{method:`GET`,path:`/oauth2/end_session`,summary:`End the session and redirect the browser back.`,public:!0}]},{type:`table`,headers:[`Parameter`,`Required`,`Meaning`],rows:[[`tenant_id`,`yes`,`Which tenant's session is ending.`],[`id_token_hint`,`SHOULD`,`A previously-issued ID token — the only *authenticated* statement of which session and which client this is about.`],[`post_logout_redirect_uri`,`no`,`Where to send the browser afterwards. Honoured only when it is on the client's allow-list.`],[`state`,`no`,`Round-tripped back to the redirect URI.`],[`client_id`,`no`,`Identifies the requesting client.`]]},{type:`warn`,text:"Send `id_token_hint`. Without it AXIAM cannot tell *which* session the browser means, and a `post_logout_redirect_uri` cannot be validated against a specific client's allow-list — so an unhinted logout is both less precise and less able to redirect you anywhere useful."},{type:`h`,id:`backchannel`,text:`Back-channel logout`},{type:`p`,text:"When a session ends at AXIAM — through logout, administrative revocation, or an incident response — every relying party registered for back-channel logout receives a signed **logout token** naming the `sid`. The notification is server-to-server, so it works even when the user's browser is closed."},{type:`p`,text:"On the relying-party side, the handler's job is: verify the token's signature against the tenant JWKS, confirm it is a logout token, read the `sid`, and destroy the local session that matches it. Do not destroy every session for that user unless you have specifically decided that is what you want."},{type:`note`,text:`This is the mechanism that makes "revoke this account now" mean something across a federation. Register your relying parties for it before you need it — an incident is a bad time to discover that logging out of AXIAM does nothing anywhere else.`}]},{slug:`fapi2`,section:`OAuth2 & OIDC`,navLabel:`FAPI 2.0 & mTLS`,title:`FAPI 2.0 profile & mTLS clients`,intro:`Register a client under the FAPI 2.0 Security Profile, authenticate it with mutual TLS instead of a shared secret, and issue certificate-bound tokens that a stolen copy cannot use.`,blocks:[{type:`h`,id:`optin`,text:`All of it is opt-in`},{type:`p`,text:`Every field on this page defaults to what an AXIAM client already was. **A deployment that changes nothing behaves exactly as it did before** — that is the design's load-bearing property, and it is asserted by tests rather than assumed.`},{type:`h`,id:`switch`,text:`The one switch`},{type:`code`,caption:`registering a FAPI 2.0 client`,code:`POST /api/v1/oauth2-clients
{
  "name": "payments-rp",
  "redirect_uris": ["https://rp.example/callback"],
  "grant_types": ["authorization_code", "refresh_token"],
  "scopes": ["openid"],

  "profile": "fapi2",
  "require_par": true,
  "token_endpoint_auth_method": "tls_client_auth",
  "tls_client_auth_san_dns": "payments-rp.example",
  "tls_client_certificate_bound_access_tokens": true
}`},{type:`p`,text:'`profile: "fapi2"` is not a label. The server **refuses the registration** unless it also carries `require_par`, a strong `token_endpoint_auth_method`, and some form of sender-constraining — and the refusal names which one is missing:'},{type:`code`,code:`400 a fapi2 client must set require_par: FAPI 2.0 §5.3.1.2 requires pushed
    authorization requests`},{type:`p`,text:"A client carrying `require_par` is then refused at `/oauth2/authorize` if it sends its parameters inline, so the constraint holds at use as well as at registration — see [Pushed authorization](#/docs/par)."},{type:`note`,text:`That refusal is the point. A client satisfying eleven of twelve FAPI constraints is not "mostly FAPI" — it is a client with a hole. The bundle cannot be half-applied, so a reviewer can answer *is this client conformant?* by reading one field.`},{type:`h`,id:`mtls`,text:`mTLS client authentication`},{type:`p`,text:"`tls_client_auth` replaces the shared secret with a client certificate, matched against a configured identifier — `tls_client_auth_san_dns` and its siblings. The certificate is verified by AXIAM's own TLS listener; **no proxy-asserted identity header is in the trusted path**, so there is no `X-Client-Certificate` to forge."},{type:`table`,headers:[`Variable`,`Values`,`Default`,`Meaning`],rows:[[`AXIAM__SERVER__TLS__CLIENT_AUTH`,`off | optional | required`,`off`,`Client-certificate policy on the native listener.`],[`AXIAM__SERVER__TLS__CLIENT_CA_PATH`,`PEM bundle path`,`—`,`Trust anchors for client certificates.`]]},{type:`warn`,text:`Startup **fails fast** when client authentication is enabled but the CA path is unset, unreadable, empty or malformed. A misconfigured mTLS server never starts serving — it does not fall back to accepting anonymous clients.`},{type:`h`,id:`bound`,text:`Certificate-bound access tokens`},{type:`p`,text:"With `tls_client_certificate_bound_access_tokens`, an issued token carries a confirmation claim tying it to the thumbprint of the certificate that requested it. A resource server checks that the presenting client's certificate matches, which makes the token **sender-constrained**: copying it out of a log, a proxy or a memory dump gains an attacker nothing without the corresponding private key."},{type:`p`,text:`This is the single highest-value item on this page for an ordinary deployment, FAPI or not. Bearer tokens are bearer material; sender-constrained tokens are not.`},{type:`h`,id:`who`,text:`Who needs this`},{type:`list`,items:[`**Open banking and regulated financial APIs** — where FAPI 2.0 conformance is a requirement rather than a preference.`,`**Any high-value machine-to-machine path** — the mTLS and token-binding halves are worth having on their own, without the full profile.`,`**Nobody else, yet.** If you are starting out, register ordinary clients. The profile is here for when a compliance obligation or a threat model asks for it.`]}]},{slug:`par`,section:`OAuth2 & OIDC`,navLabel:`Pushed authorization (PAR)`,title:`Pushed authorization requests`,intro:`RFC 9126 — send the authorization request over an authenticated back channel and put an opaque handle in the browser, so what travels through the user agent is a random string that cannot be edited into meaning something else.`,blocks:[{type:`h`,id:`what`,text:`What PAR changes`},{type:`p`,text:"In a plain authorization code flow, `scope`, `redirect_uri`, `state` and the PKCE challenge all ride through the user agent in a URL. Anything that can see or rewrite that URL — a malicious extension, a referrer leak, a tampered deep link — is party to the request. PAR moves the whole thing: the client `POST`s the parameters straight to AXIAM over an authenticated connection, gets back an opaque `request_uri`, and redirects with that instead."},{type:`p`,text:`This is an **extension of the normal flow, not a replacement**. Discovery, the token exchange, refresh and the whole ID-token validation checklist are unchanged, and a client that never pushes behaves exactly as it did.`},{type:`api`,endpoints:[{method:`POST`,path:`/oauth2/par?tenant_id={uuid}`,summary:"Push an authorization request. Form-encoded, client-authenticated, answers **`201`**."},{method:`GET`,path:`/oauth2/authorize?client_id={id}&request_uri={uri}`,summary:`Redeem it — exactly those two parameters and no others. Needs the user's session, as it always did.`}]},{type:`h`,id:`flow`,text:`The flow, end to end`},{type:`steps`,steps:[{title:`Build the request as you always did`,body:"Your SDK's `oidc_begin` produces `state`, `nonce`, the PKCE verifier and its `S256` challenge. PAR does not compute anything of its own — there is no second generator, and the `code_verifier` you keep for the exchange is the one this step already gave you."},{title:`Push it`,body:"`POST /oauth2/par` with the parameters form-encoded, `tenant_id` as a **query** parameter, and client authentication. A `201` returns `request_uri` and `expires_in`.",code:`HTTP/1.1 201 Created
Content-Type: application/json

{
  "request_uri": "urn:ietf:params:oauth:request_uri:…",
  "expires_in": 60
}`},{title:`Redirect with the handle`,body:"Send the user to `/oauth2/authorize` with **only** `client_id` and `request_uri`. Everything else was pushed, and the server reads it from there."},{title:`Exchange the code unchanged`,body:"The callback and the token exchange are exactly what they were: the same `authorization_code` grant, the same `code_verifier`, and the `redirect_uri` that was pushed. Storing the pushed parameters and the exchange parameters separately just creates two places for them to disagree."}]},{type:`h`,id:`rules`,text:`Four rules worth reading before you implement it`},{type:`warn`,text:"**It answers `201`, not `200`.** RFC 9126 §2.2 specifies Created, and the response names a resource that did not exist before the call. A success predicate written as `status == 200` treats every successful push as a failure — this is the single most likely defect in a PAR implementation, which is why it leads the list."},{type:`list`,items:["**The authorization URL carries exactly two parameters.** Not `response_type`, not `redirect_uri`, not `scope`, not `state`, not the PKCE pair. The server **refuses** a request that mixes a `request_uri` with any inline authorization parameter rather than merging them — and re-adding them “for compatibility” restores the parameter-confusion attack the refusal prevents, where an attacker supplies the inline value they want and lets the pushed copy satisfy whichever check reads the other one.","**The `request_uri` is single-use and short-lived** — 60 seconds, consumed the moment `/oauth2/authorize` reads it. There is deliberately no configuration knob: the window only has to cover one browser redirect, and a tunable that only trends longer is a tunable that only widens a replay window. A second use is `invalid_request`, not a duplicate-suppressed success.","**A push is never retried.** It is a `POST` that creates server state, so it sits outside the SDKs' read-only retry eligibility. A transport failure after the request left the client is surfaced rather than retried — the safe recovery is a fresh push, which costs one round trip and cannot double-consume anything.","**Treat the `request_uri` as opaque.** Do not parse it, do not validate its `urn:` prefix as a precondition, do not reconstruct one. Checking the prefix buys nothing and breaks the moment the format is versioned."]},{type:`h`,id:`auth`,text:`It is authenticated, and that is the point`},{type:`p`,text:"Unlike the device authorization endpoint, `/oauth2/par` requires client authentication — the parameters stop travelling through the browser, and the ones that arrive are attributable to a client that proved it holds a credential. Which credential follows the client's registered method: `client_secret` for `client_secret_post`, a `client_assertion` for `private_key_jwt`, and **nothing at all** for the two mTLS methods, whose credential is the TLS connection itself."},{type:`note`,text:"`invalid_client` on a push has three usual causes, and the second is the one that wastes an afternoon: a wrong secret; a secret sent by a client registered for `tls_client_auth`, `private_key_jwt` or `self_signed_tls_client_auth`, which is **refused rather than ignored**; or a client certificate the transport never presented. See [FAPI 2.0 & mTLS](#/docs/fapi2)."},{type:`h`,id:`fapi`,text:`Required for FAPI 2.0`},{type:`p`,text:'Registering a client with `profile: "fapi2"` forces `require_par`, and a client with `require_par` set is **refused** at `/oauth2/authorize` when it sends its parameters inline. A FAPI 2.0 client therefore cannot authorize any other way — which is the intent: the profile is a constraint bundle a client cannot half-apply.'},{type:`h`,id:`sdks`,text:`From an SDK`},{type:`p`,text:"All eleven SDKs ship the push as a single operation that extends the existing OIDC helpers rather than introducing a parallel vocabulary. It returns the authorization URL already built, plus the `nonce` and `code_verifier` the begin step produced."},{type:`codegroup`,caption:`push, redirect, exchange`,tabs:[{label:`Rust`,code:`let configuration = client.oidc_discover().await?;
let request = client.oidc_begin(&configuration, OidcBeginParams {
    redirect_uri: redirect_uri.clone(),
    scope: Some("openid profile".into()),
    ..Default::default()
})?;

let pushed = client.oidc_par(OidcParParams {
    request,
    redirect_uri: redirect_uri.clone(),
    scope: Some("openid profile".into()),
    tenant_id: None,
    configuration: Some(configuration),
}).await?;

redirect(&pushed.url);`},{label:`TypeScript`,code:`const configuration = await oidc.oidcDiscover();
const request = oidc.oidcBegin({ configuration, redirectUri, scope: 'openid profile' });

const pushed = await oidc.oidcPar({ request, redirectUri, scope: 'openid profile', configuration });

redirect(pushed.authorizationUrl);

// …on the callback, unchanged by PAR:
const tokens = await oidc.oidcExchange({
  code, redirectUri, nonce: pushed.nonce, codeVerifier: pushed.codeVerifier,
});`},{label:`Python`,code:`configuration = client.oidc_discover()
request = client.oidc_begin(configuration=configuration, redirect_uri=uri, scope="openid profile")

pushed = client.oidc_par(
    request=request,
    redirect_uri=uri,
    scope="openid profile",
    configuration=configuration,
    tenant_id=tenant_id,
)
redirect(pushed.authorization_url)

# …on the callback, unchanged by PAR:
tokens = client.oidc_exchange(
    code=code,
    redirect_uri=uri,
    nonce=pushed.nonce,
    code_verifier=pushed.code_verifier,
)`}]},{type:`note`,text:"`request_uri` is wrapped in each SDK's redacting secret type. It is short-lived and single-use, and both of those are reasons it gets treated as harmless — but between the push and the redirect it is a bearer handle to a fully-formed authorization request, and a log line is the wrong place for it to sit for the length of that window. The normative rules are [CONTRACT §26](https://github.com/ilpanich/axiam/blob/main/sdks/CONTRACT.md); the server side is `crates/axiam-oauth2/src/par.rs`."}]}],d=`1.0.0-alpha38`,f=[{id:`api-authentication-sessions`,label:`Authentication & sessions`,blurb:`Signing in, keeping a session alive, the account lifecycle, and the WebAuthn ceremonies.`,operations:[{method:`POST`,path:`/api/v1/auth/device`,summary:`Authenticate a device via its client certificate (mTLS).`,public:!0},{method:`POST`,path:`/api/v1/auth/login`,summary:``,public:!0},{method:`POST`,path:`/api/v1/auth/logout`,summary:``},{method:`POST`,path:`/api/v1/auth/mfa/confirm`,summary:``},{method:`POST`,path:`/api/v1/auth/mfa/enroll`,summary:``},{method:`POST`,path:`/api/v1/auth/mfa/setup/confirm`,summary:`Confirm MFA enrollment and complete login using a setup token.`,public:!0},{method:`POST`,path:`/api/v1/auth/mfa/setup/enroll`,summary:`Start MFA enrollment using a setup token (issued during login when MFA is enforced but not yet configured).`,public:!0},{method:`POST`,path:`/api/v1/auth/mfa/verify`,summary:``,public:!0},{method:`POST`,path:`/api/v1/auth/opaque/login/finish`,summary:"Returns the same three outcomes as `POST /api/v1/auth/login` — `200` with session cookies, `202` for an MFA challenge, `403` for MFA setup — so a client can share one result handler across both login paths.",public:!0},{method:`POST`,path:`/api/v1/auth/opaque/login/start`,summary:``,public:!0},{method:`POST`,path:`/api/v1/auth/opaque/register/start`,summary:``,public:!0},{method:`POST`,path:`/api/v1/auth/refresh`,summary:``,public:!0},{method:`POST`,path:`/api/v1/auth/resend-verification`,summary:`Creates a new email verification token and enqueues it for async delivery via the mail queue (D-14).`,public:!0},{method:`POST`,path:`/api/v1/auth/reset`,summary:"Initiates a password reset by enqueuing an `OutboundMailMessage` to the async mail queue.",public:!0},{method:`POST`,path:`/api/v1/auth/reset/confirm`,summary:`Confirms a password reset using a one-time token and a new password.`,public:!0},{method:`GET`,path:`/api/v1/auth/reset/context`,summary:`Password reset would otherwise be a permanent hole in OPAQUE coverage.`,public:!0},{method:`POST`,path:`/api/v1/auth/verify-email`,summary:`Verifies a user's email using a one-time token.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/discoverable/finish`,summary:`Complete a usernameless ceremony.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/discoverable/start`,summary:`Begin a **usernameless** WebAuthn ceremony for a workspace.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/finish`,summary:`Complete a WebAuthn passkey authentication ceremony.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/authenticate/start`,summary:`Begin a WebAuthn passkey authentication ceremony.`,public:!0},{method:`POST`,path:`/api/v1/auth/webauthn/register/finish`,summary:`Complete a WebAuthn passkey registration ceremony.`},{method:`POST`,path:`/api/v1/auth/webauthn/register/start`,summary:`Begin a WebAuthn passkey registration ceremony for the authenticated user.`},{method:`POST`,path:`/api/v1/device/decide`,summary:`Record the user's approval or refusal.`},{method:`GET`,path:`/api/v1/device/verify`,summary:`Look up a pending grant by the code the user typed.`},{method:`POST`,path:`/api/v1/mds/refresh`,summary:"Fetches (or, when `AXIAM__PKI__MDS_BLOB_PATH` is set, loads locally), verifies, and ingests the FIDO MDS3 BLOB."},{method:`GET`,path:`/api/v1/mds/status`,summary:``},{method:`GET`,path:`/api/v1/tenants/{tenant_id}/webauthn/attestation-policy`,summary:``},{method:`PUT`,path:`/api/v1/tenants/{tenant_id}/webauthn/attestation-policy`,summary:``},{method:`GET`,path:`/api/v1/tenants/{tenant_id}/webauthn/compliance-report`,summary:``}]},{id:`api-oauth2-openid-connect`,label:`OAuth2 & OpenID Connect`,blurb:`The authorization server, its clients, and the discovery documents.`,operations:[{method:`GET`,path:`/.well-known/openid-configuration`,summary:`Returns the OpenID Provider metadata per OpenID Connect Discovery 1.0.`,public:!0},{method:`GET`,path:`/api/v1/oauth2-clients`,summary:``},{method:`POST`,path:`/api/v1/oauth2-clients`,summary:``},{method:`GET`,path:`/api/v1/oauth2-clients/{id}`,summary:``},{method:`PUT`,path:`/api/v1/oauth2-clients/{id}`,summary:``},{method:`DELETE`,path:`/api/v1/oauth2-clients/{id}`,summary:``},{method:`GET`,path:`/oauth2/authorize`,summary:`The user must be authenticated (redirected to login first if not).`},{method:`POST`,path:`/oauth2/device_authorization`,summary:`Build an OAuth2 JSON error response with the appropriate HTTP status.`,public:!0},{method:`GET`,path:`/oauth2/end_session`,summary:"/`POST /oauth2/end_session` — OIDC RP-Initiated Logout 1.0 (B5).",public:!0},{method:`POST`,path:`/oauth2/introspect`,summary:`Accepts form-encoded body.`,public:!0},{method:`GET`,path:`/oauth2/jwks`,summary:`Returns the public signing keys used by the authorization server so that relying parties can verify JWTs without sharing a secret.`,public:!0},{method:`POST`,path:`/oauth2/par`,summary:"Accepts an authorization request over a direct, client-authenticated POST and returns an opaque `request_uri` to put in the browser redirect instead of the parameters.",public:!0},{method:`POST`,path:`/oauth2/revoke`,summary:`Accepts form-encoded body.`,public:!0},{method:`POST`,path:`/oauth2/token`,summary:`Accepts form-encoded body per RFC 6749.`,public:!0},{method:`GET`,path:`/oauth2/userinfo`,summary:`Returns claims about the authenticated user.`}]},{id:`api-federation`,label:`Federation`,blurb:`SAML service provider and OIDC relying-party configuration, and the SSO entry points.`,operations:[{method:`POST`,path:`/api/v1/auth/federation/oidc/callback`,summary:`Consumes the state row (single-use), runs the verified OIDC flow from plan 04-02 (nonce from DB — not caller-supplied), provisions or links the user, and returns Set-Cookie response (no token in body).`,public:!0},{method:`POST`,path:`/api/v1/auth/federation/oidc/start`,summary:"Generates a server-side state+nonce pair, persists it in `federation_login_state` (10-min TTL), and returns the IdP authorization URL.",public:!0},{method:`GET`,path:`/api/v1/federation-configs`,summary:``},{method:`POST`,path:`/api/v1/federation-configs`,summary:``},{method:`GET`,path:`/api/v1/federation-configs/{id}`,summary:``},{method:`PUT`,path:`/api/v1/federation-configs/{id}`,summary:``},{method:`DELETE`,path:`/api/v1/federation-configs/{id}`,summary:``},{method:`DELETE`,path:`/api/v1/federation-links/{id}`,summary:``},{method:`GET`,path:`/api/v1/federation-links/user/{user_id}`,summary:``},{method:`POST`,path:`/api/v1/federation/oidc/authorize`,summary:`Builds the authorization URL for the external OIDC provider.`},{method:`POST`,path:`/api/v1/federation/oidc/callback`,summary:`Handles the callback from the external OIDC provider after user authentication.`}]},{id:`api-identity`,label:`Identity`,blurb:`Users, the groups they belong to, and the service accounts that act without one.`,operations:[{method:`GET`,path:`/api/v1/groups`,summary:``},{method:`POST`,path:`/api/v1/groups`,summary:``},{method:`GET`,path:`/api/v1/groups/{group_id}`,summary:``},{method:`PUT`,path:`/api/v1/groups/{group_id}`,summary:``},{method:`DELETE`,path:`/api/v1/groups/{group_id}`,summary:``},{method:`GET`,path:`/api/v1/groups/{group_id}/members`,summary:``},{method:`POST`,path:`/api/v1/groups/{group_id}/members`,summary:``},{method:`DELETE`,path:`/api/v1/groups/{group_id}/members/{user_id}`,summary:``},{method:`GET`,path:`/api/v1/service-accounts`,summary:``},{method:`POST`,path:`/api/v1/service-accounts`,summary:``},{method:`GET`,path:`/api/v1/service-accounts/{sa_id}`,summary:``},{method:`PUT`,path:`/api/v1/service-accounts/{sa_id}`,summary:``},{method:`DELETE`,path:`/api/v1/service-accounts/{sa_id}`,summary:``},{method:`POST`,path:`/api/v1/service-accounts/{sa_id}/rotate-secret`,summary:``},{method:`GET`,path:`/api/v1/users`,summary:``},{method:`POST`,path:`/api/v1/users`,summary:"Creates a user and atomically records a `terms_of_service` consent row (REQ-8 / Art. 7 proof of consent)."},{method:`GET`,path:`/api/v1/users/{user_id}`,summary:``},{method:`PUT`,path:`/api/v1/users/{user_id}`,summary:``},{method:`DELETE`,path:`/api/v1/users/{user_id}`,summary:``},{method:`GET`,path:`/api/v1/users/{user_id}/mfa-methods`,summary:`List all MFA methods registered for a user.`},{method:`DELETE`,path:`/api/v1/users/{user_id}/mfa-methods/{method_id}`,summary:`Remove a specific MFA method.`},{method:`POST`,path:`/api/v1/users/{user_id}/reset-mfa`,summary:`Reset MFA for a user — disables MFA, clears the secret, and revokes all existing sessions.`},{method:`POST`,path:`/api/v1/users/{user_id}/unlock`,summary:"Resets a locked user account: clears `locked_until`, resets `failed_login_attempts` to 0, and sets status back to `Active`."}]},{id:`api-data-subject-rights`,label:`Data-subject rights`,blurb:`GDPR export (Art. 15) and erasure (Art. 17), acting on the caller's own account.`,operations:[{method:`POST`,path:`/api/v1/account/delete`,summary:`Initiates Art. 17 erasure: immediately disables the account, revokes all sessions, emails a single-use cancel link, and schedules purge at +30 d (D-07/D-08/D-09).`},{method:`POST`,path:`/api/v1/account/export`,summary:`Enqueues an async GDPR Art. 15 data-export job.`},{method:`GET`,path:`/api/v1/account/export/{token}`,summary:`Single-use download of the GDPR export blob (D-13).`},{method:`GET`,path:`/api/v1/auth/account/delete/cancel`,summary:"(token-authenticated; listed in `PUBLIC_PATHS`).",public:!0}]},{id:`api-authorization`,label:`Authorization`,blurb:`The graph a decision is read from — roles, permissions, resources and scopes — the check endpoints, and UMA's protection API.`,operations:[{method:`GET`,path:`/.well-known/uma2-configuration`,summary:`Advertises only what v1 actually serves.`,public:!0},{method:`POST`,path:`/api/v1/authz/check`,summary:"Evaluate a single authorization check for the authenticated caller (or, if the caller holds `authz:check_as`, for an arbitrary subject)."},{method:`POST`,path:`/api/v1/authz/check/batch`,summary:`Evaluate an ordered list of authorization checks.`},{method:`GET`,path:`/api/v1/permissions`,summary:``},{method:`POST`,path:`/api/v1/permissions`,summary:``},{method:`GET`,path:`/api/v1/permissions/{permission_id}`,summary:``},{method:`PUT`,path:`/api/v1/permissions/{permission_id}`,summary:``},{method:`DELETE`,path:`/api/v1/permissions/{permission_id}`,summary:``},{method:`GET`,path:`/api/v1/resources`,summary:``},{method:`POST`,path:`/api/v1/resources`,summary:``},{method:`GET`,path:`/api/v1/resources/{resource_id}`,summary:``},{method:`PUT`,path:`/api/v1/resources/{resource_id}`,summary:``},{method:`DELETE`,path:`/api/v1/resources/{resource_id}`,summary:``},{method:`GET`,path:`/api/v1/resources/{resource_id}/ancestors`,summary:``},{method:`GET`,path:`/api/v1/resources/{resource_id}/children`,summary:``},{method:`GET`,path:`/api/v1/resources/{resource_id}/scopes`,summary:``},{method:`POST`,path:`/api/v1/resources/{resource_id}/scopes`,summary:``},{method:`GET`,path:`/api/v1/resources/{resource_id}/scopes/{scope_id}`,summary:``},{method:`PUT`,path:`/api/v1/resources/{resource_id}/scopes/{scope_id}`,summary:``},{method:`DELETE`,path:`/api/v1/resources/{resource_id}/scopes/{scope_id}`,summary:``},{method:`GET`,path:`/api/v1/roles`,summary:``},{method:`POST`,path:`/api/v1/roles`,summary:``},{method:`GET`,path:`/api/v1/roles/{role_id}`,summary:``},{method:`PUT`,path:`/api/v1/roles/{role_id}`,summary:``},{method:`DELETE`,path:`/api/v1/roles/{role_id}`,summary:``},{method:`GET`,path:`/api/v1/roles/{role_id}/groups`,summary:`Lists the groups directly assigned this role.`},{method:`POST`,path:`/api/v1/roles/{role_id}/groups`,summary:``},{method:`DELETE`,path:`/api/v1/roles/{role_id}/groups/{group_id}`,summary:``},{method:`GET`,path:`/api/v1/roles/{role_id}/permissions`,summary:``},{method:`POST`,path:`/api/v1/roles/{role_id}/permissions`,summary:``},{method:`DELETE`,path:`/api/v1/roles/{role_id}/permissions/{permission_id}`,summary:``},{method:`GET`,path:`/api/v1/roles/{role_id}/users`,summary:"Lists the users directly assigned this role (the inverse of `GET /users/{id}/roles`)."},{method:`POST`,path:`/api/v1/roles/{role_id}/users`,summary:``},{method:`DELETE`,path:`/api/v1/roles/{role_id}/users/{user_id}`,summary:``},{method:`POST`,path:`/uma2/perm`,summary:"# Who may call this A **Protection API Token**: an ordinary AXIAM access token carrying the `uma_protection` scope."},{method:`GET`,path:`/uma2/rreg/resource_set`,summary:`Lists only the resources **this client** registered.`},{method:`POST`,path:`/uma2/rreg/resource_set`,summary:`Creates the resource **and** its declared scopes.`},{method:`GET`,path:`/uma2/rreg/resource_set/{id}`,summary:``},{method:`PUT`,path:`/uma2/rreg/resource_set/{id}`,summary:`The scope list is **replaced**, not merged: FedAuthz defines the update as putting the resource set's new state, and a merge would make it impossible to remove a scope.`},{method:`DELETE`,path:`/uma2/rreg/resource_set/{id}`,summary:``}]},{id:`api-organizations-tenants`,label:`Organizations & tenants`,blurb:`The tenancy boundary itself, and the settings and mail configuration that hang off it.`,operations:[{method:`GET`,path:`/api/v1/organizations`,summary:``},{method:`POST`,path:`/api/v1/organizations`,summary:``},{method:`GET`,path:`/api/v1/organizations/{org_id}`,summary:``},{method:`PUT`,path:`/api/v1/organizations/{org_id}`,summary:``},{method:`DELETE`,path:`/api/v1/organizations/{org_id}`,summary:``},{method:`GET`,path:`/api/v1/organizations/{org_id}/email-config`,summary:``},{method:`PUT`,path:`/api/v1/organizations/{org_id}/email-config`,summary:``},{method:`DELETE`,path:`/api/v1/organizations/{org_id}/email-config`,summary:``},{method:`GET`,path:`/api/v1/organizations/{org_id}/settings`,summary:``},{method:`PUT`,path:`/api/v1/organizations/{org_id}/settings`,summary:``},{method:`GET`,path:`/api/v1/organizations/{org_id}/tenants`,summary:``},{method:`POST`,path:`/api/v1/organizations/{org_id}/tenants`,summary:``},{method:`GET`,path:`/api/v1/organizations/{org_id}/tenants/{tenant_id}`,summary:``},{method:`PUT`,path:`/api/v1/organizations/{org_id}/tenants/{tenant_id}`,summary:``},{method:`DELETE`,path:`/api/v1/organizations/{org_id}/tenants/{tenant_id}`,summary:``},{method:`GET`,path:`/api/v1/settings`,summary:`Returns the effective (merged) security settings for the authenticated user's tenant.`},{method:`PUT`,path:`/api/v1/settings`,summary:`Set tenant-level overrides.`},{method:`GET`,path:`/api/v1/tenants/{tenant_id}/email-config`,summary:``},{method:`PUT`,path:`/api/v1/tenants/{tenant_id}/email-config`,summary:``},{method:`DELETE`,path:`/api/v1/tenants/{tenant_id}/email-config`,summary:``}]},{id:`api-pki-certificates`,label:`PKI & certificates`,blurb:`Certificate authorities, issued certificates, and the OpenPGP keys audit exports are signed with.`,operations:[{method:`GET`,path:`/api/v1/certificates`,summary:``},{method:`POST`,path:`/api/v1/certificates`,summary:``},{method:`GET`,path:`/api/v1/certificates/{id}`,summary:``},{method:`POST`,path:`/api/v1/certificates/{id}/revoke`,summary:``},{method:`GET`,path:`/api/v1/organizations/{org_id}/ca-certificates`,summary:``},{method:`POST`,path:`/api/v1/organizations/{org_id}/ca-certificates`,summary:``},{method:`GET`,path:`/api/v1/organizations/{org_id}/ca-certificates/{id}`,summary:``},{method:`POST`,path:`/api/v1/organizations/{org_id}/ca-certificates/{id}/revoke`,summary:``},{method:`GET`,path:`/api/v1/pgp-keys`,summary:``},{method:`POST`,path:`/api/v1/pgp-keys`,summary:``},{method:`GET`,path:`/api/v1/pgp-keys/{id}`,summary:``},{method:`POST`,path:`/api/v1/pgp-keys/{id}/encrypt`,summary:``},{method:`POST`,path:`/api/v1/pgp-keys/{id}/revoke`,summary:``},{method:`POST`,path:`/api/v1/pgp-keys/sign-audit-batch`,summary:``},{method:`POST`,path:`/api/v1/service-accounts/{sa_id}/bind-certificate`,summary:``}]},{id:`api-eventing`,label:`Eventing`,blurb:`Webhooks, reactors and notification rules — everything that tells something else what happened.`,operations:[{method:`GET`,path:`/api/v1/notification-rules`,summary:``},{method:`POST`,path:`/api/v1/notification-rules`,summary:``},{method:`GET`,path:`/api/v1/notification-rules/{id}`,summary:``},{method:`PUT`,path:`/api/v1/notification-rules/{id}`,summary:``},{method:`DELETE`,path:`/api/v1/notification-rules/{id}`,summary:``},{method:`GET`,path:`/api/v1/reactors`,summary:``},{method:`POST`,path:`/api/v1/reactors`,summary:``},{method:`GET`,path:`/api/v1/reactors/{id}`,summary:``},{method:`PUT`,path:`/api/v1/reactors/{id}`,summary:``},{method:`DELETE`,path:`/api/v1/reactors/{id}`,summary:``},{method:`GET`,path:`/api/v1/reactors/events`,summary:`Served rather than documented so an admin UI, an SDK generator and an operator all read the same list the dispatcher enforces.`},{method:`GET`,path:`/api/v1/webhooks`,summary:``},{method:`POST`,path:`/api/v1/webhooks`,summary:``},{method:`GET`,path:`/api/v1/webhooks/{id}`,summary:``},{method:`PUT`,path:`/api/v1/webhooks/{id}`,summary:``},{method:`DELETE`,path:`/api/v1/webhooks/{id}`,summary:``}]},{id:`api-provisioning`,label:`Provisioning`,blurb:"SCIM provisioning tokens. The SCIM 2.0 endpoints themselves are served under `/scim/v2`.",operations:[{method:`GET`,path:`/api/v1/scim-tokens`,summary:``},{method:`POST`,path:`/api/v1/scim-tokens`,summary:``},{method:`DELETE`,path:`/api/v1/scim-tokens/{id}`,summary:`Revokes rather than deletes: the row is what lets an operator later answer "was this credential still live on the day of the incident?", and a deleted row answers nothing.`}]},{id:`api-audit-operations`,label:`Audit & operations`,blurb:`The append-only trail, and the endpoints a monitor reads.`,operations:[{method:`GET`,path:`/api/v1/audit-logs`,summary:``},{method:`GET`,path:`/api/v1/audit-logs/system`,summary:`Returns audit entries for unauthenticated/system requests (nil tenant_id).`},{method:`GET`,path:`/health`,summary:``,public:!0},{method:`GET`,path:`/ready`,summary:``,public:!0}]}].flatMap(e=>[{type:`h`,id:e.id,text:e.label},{type:`p`,text:e.blurb},{type:`api`,endpoints:e.operations}]),p=[{slug:`rest`,section:`APIs & integration`,navLabel:`REST API`,title:`REST API`,intro:`The broadest surface — every entity in the system is manageable here, described by an OpenAPI 3.1 document that is generated from the server and drift-gated in CI.`,blocks:[{type:`h`,id:`spec`,text:`The specification`},{type:`p`,text:"`sdks/openapi.json` is the single source of truth for the REST surface. It is generated from the server's own route annotations, and a CI job fails any change where the committed spec diverges from a fresh export — so the document cannot quietly fall behind the code it describes."},{type:`code`,caption:`view it`,code:`# any Swagger/Redoc viewer works
npx @redocly/cli preview-docs docs/api/openapi.json

# or regenerate it after changing the API
cargo build -p axiam-server --no-default-features
./target/debug/axiam-server --dump-openapi > sdks/openapi.json`},{type:`p`,text:"The running server serves the document and a browsable Swagger UI itself, at `/api/docs/openapi.json` and `/api/docs/`. Treat that as the authoritative copy for the build you are talking to — the committed spec is the same document, exported."},{type:`h`,id:`shape`,text:`Shape of the API`},{type:`table`,proseFirstCol:!0,headers:[`Prefix`,`What lives there`],rows:[["`/api/v1/auth/*`",`Login, refresh, logout, password reset, email verification, MFA, WebAuthn, OPAQUE and federated sign-in.`],["`/api/v1/authz/*`",`Authorization checks, single and batched.`],[`Entity collections`,"`users`, `groups`, `roles`, `permissions`, `resources`, `service-accounts`, `oauth2-clients`, `organizations`, `tenants`, `certificates`, `pgp-keys`, `webhooks`, `reactors`, `notification-rules`, `federation-configs`, `scim-tokens`, `audit-logs`, `settings`."],["`/oauth2/*`",`The authorization-server endpoints. See [OAuth2 & OIDC](#/docs/oauth2).`],["`/uma2/*`",`The UMA 2.0 Protection API. See [UMA 2.0](#/docs/uma).`],["`/scim/v2/*`",`SCIM 2.0 provisioning. See [SCIM provisioning](#/docs/scim).`],["`/health`, `/ready`",`Liveness and readiness probes. See [Health & observability](#/docs/observability).`]]},{type:`h`,id:`conventions`,text:`Conventions`},{type:`list`,items:["**Two ways to authenticate.** A machine client sends `Authorization: Bearer <access_token>`, obtained from the OAuth2 token endpoint. An interactive login sets `httpOnly` cookies instead — `POST /auth/login` returns no token in its body — and state-changing requests must then echo the `axiam_csrf` cookie in an `X-CSRF-Token` header. The SDKs handle the second case for you.","**Tenancy is explicit.** Entity routes are tenant-scoped through the authenticated principal; OAuth2 endpoints take `tenant_id` as a query parameter. Nothing is inferred from a default.",`**Every route is permission-guarded.** A caller needs an explicit grant for the action behind the route — the same 113-permission registry the admin console uses.`,"**Collections paginate** with `offset` and `limit`, and return the items plus a total.",`**Mutations are audited.** Every write lands in the append-only audit log with the acting principal.`]},{type:`h`,id:`example`,text:`A worked example`},{type:`code`,caption:`create a user, put them in a group, check access`,code:`# A machine client gets a bearer token from the OAuth2 token endpoint.
# (An interactive login sets cookies instead — see the Quickstart.)
TOKEN=$(curl -sS -X POST 'https://iam.acme.dev/oauth2/token?tenant_id=<uuid>' \\
  -d grant_type=client_credentials \\
  -d client_id="$SA_CLIENT_ID" -d client_secret="$SA_CLIENT_SECRET" \\
  | jq -r .access_token)

USER=$(curl -sS -X POST https://iam.acme.dev/api/v1/users \\
  -H "authorization: Bearer $TOKEN" -H 'content-type: application/json' \\
  -d '{"email":"dana@acme.dev","username":"dana"}' | jq -r .id)

curl -sS -X POST "https://iam.acme.dev/api/v1/groups/$GROUP/members" \\
  -H "authorization: Bearer $TOKEN" -H 'content-type: application/json' \\
  -d "{\\"user_id\\":\\"$USER\\"}"

curl -sS -X POST https://iam.acme.dev/api/v1/authz/check \\
  -H "authorization: Bearer $TOKEN" -H 'content-type: application/json' \\
  -d '{"action":"read","resource_id":"doc:1"}'`},{type:`h`,id:`index`,text:`Every endpoint`},{type:`p`,text:`**181 operations across 121 paths**, grouped by domain and in path order. This index is generated from \`sdks/openapi.json\` at \`${d}\` — it is not a curated selection, so a route that exists appears here, and one that appears here exists. Endpoints marked \`PUBLIC\` are reachable **without an access token**; everything else needs one, and a permission behind it. Read that marker precisely on the OAuth2 endpoints: \`/oauth2/token\`, \`/oauth2/par\`, \`/oauth2/introspect\` and \`/oauth2/revoke\` take no bearer token, but they do authenticate the *client* — by secret, assertion or the TLS connection — so “no access token” is not “open”.`},{type:`note`,text:`Where a row carries no description, the handler has none in the specification beyond its route — the OpenAPI document is the place to fix that, not this page. For request and response schemas, read the document itself or point a viewer at the running server.`},...f,{type:`h`,id:`gdpr`,text:`GDPR endpoints`},{type:`p`,text:"The four data-subject endpoints are in the index above, under *Data-subject rights*. Export and erasure act on the caller's own account, or on another's with `users:erase`. The cancel link is the odd one: it arrives by email as a single-use token, so it is a `GET` with the token in the query and is reachable without a session — the person cancelling an erasure may already have lost access to the account they are rescuing."},{type:`note`,text:"Erasure pseudonymises the actor identity in the audit trail rather than deleting the records — an append-only log cannot have rows removed from it. The HMAC pepper that makes pseudonyms consistent is `AXIAM__GDPR_PSEUDONYM_PEPPER`; changing it breaks the linkage between old and new pseudonyms. See [Standards & compliance](#/docs/compliance)."}]},{slug:`grpc`,section:`APIs & integration`,navLabel:`gRPC API`,title:`gRPC API`,intro:`A low-latency surface for service-mesh authorization checks, token validation and user lookups — Tonic on the server, one protobuf contract shared by every SDK.`,verifiedRelease:r,blocks:[{type:`h`,id:`why`,text:`Why gRPC`},{type:`p`,text:"REST is the general-purpose surface; gRPC exists for the hot path. Inside a service mesh, sidecars and backends make authorization checks on nearly every request, and connection reuse plus binary framing is what keeps tail latency down. In the benchmark run, a single gRPC `CheckAccess` held a p99 of 90 ms at database saturation, and TLS 1.3 cost nothing measurable against plaintext."},{type:`h`,id:`services`,text:`Services and RPCs`},{type:`p`,text:"Five services, defined in `proto/axiam/v1/` and all registered by the same listener. Every request message is tenant-scoped — `tenant_id` is a field on the request, not a header, because there is no default tenant."},{type:`table`,proseFirstCol:!0,headers:[`Service`,`RPC`,`What it does`],rows:[[`AuthorizationService`,"`CheckAccess`",`One access check. The hot-path RPC this service exists for.`],[`AuthorizationService`,"`BatchCheckAccess`",`Several checks in one round trip; results preserve input order.`],[`TokenService`,"`ValidateToken`","Signature, expiry and tenant. Returns `cnf` and `token_type`."],[`TokenService`,"`IntrospectToken`","Full RFC 7662 claims, plus `scope`, `client_id`, UMA `permissions` and `ext_exchange_iss`."],[`UserInfoService`,"`GetUserInfo`",`The OIDC identity read — the gRPC-only counterpart of REST's userinfo endpoint (§1.1).`],[`UserService`,"`GetUser`",`Lookup by id.`],[`UserService`,"`ValidateCredentials`",`A username/password check that issues no token. An Argon2id verification, and rate-limited as a CPU guard rather than a read ceiling.`],[`ReactorAdminService`,"`ListReactorEvents`, `CreateReactor`, `ListReactors`, `GetReactor`, `UpdateReactor`, `DeleteReactor`",`Reactor registration and lifecycle — the administrative surface behind the [reactors page](#/docs/reactors).`]]},{type:`h`,id:`checkaccess`,text:`One CheckAccess call`},{type:`p`,text:"The request names the tenant, the subject, an action and a resource. `subject_id` may be left empty to mean *the subject carried by the verified token* — over gRPC it can only ever restate the caller, since there is no cross-subject form of the check on this transport."},{type:`codegroup`,caption:`CheckAccess`,tabs:[{label:`Rust`,code:`use axiam_sdk::grpc::{AuthzGrpcClient, CheckAccessRequest, GrpcChannelConfig, build_channel};

// \`connect_lazy\` performs no network I/O — the TCP and TLS handshake
// happens on the first RPC.
let channel = build_channel("https://iam.acme.dev:50051", &GrpcChannelConfig::default())?;
let client = AuthzGrpcClient::new(channel, token_manager, refresh_fn);

let decision = client
    .check_access(CheckAccessRequest {
        tenant_id,
        subject_id,
        action: "resource:read".to_string(),
        resource_id,
        scope: None,
    })
    .await?;

println!("allowed: {}, reason: {:?}", decision.allowed, decision.reason);`},{label:`Go`,code:`// arg 1 is an optional custom CA PEM for a dev server (§6).
creds, err := axiamgrpc.NewTLSCredentials(nil, nil, nil)
conn, err := axiamgrpc.NewGRPCClient(target, creds, interceptor)
authzClient := axiamgrpc.NewAuthzClient(conn, refreshFn)

allowed, denyReason, err := authzClient.CheckAccess(ctx, axiamgrpc.CheckAccessRequest{
	TenantID:   tenantID,
	SubjectID:  subjectID,
	Action:     "resource:read",
	ResourceID: resourceID,
})`},{label:`Python`,code:`from axiam_sdk.grpc import AuthzGrpcClient

client = AuthzGrpcClient(
    "iam.acme.dev:50051",
    token_fn=lambda: current_access_token,  # non-blocking cache read
    tenant_id=tenant_id,
    refresh_fn=refresh_fn,  # invoked once on UNAUTHENTICATED, then one retry
)

decision = client.check_access(subject_id, "resource:read", resource_id)`},{label:`grpcurl`,code:`# The server registers no reflection service, so point grpcurl at the
# protos directly.
grpcurl \\
  -import-path proto -proto axiam/v1/authorization.proto \\
  -H "authorization: Bearer $ACCESS_TOKEN" \\
  -d '{"tenant_id":"<uuid>","action":"resource:read","resource_id":"<uuid>"}' \\
  iam.acme.dev:50051 axiam.v1.AuthorizationService/CheckAccess`}]},{type:`p`,text:"The response carries `allowed` plus a machine-readable `reason_code`: `allowed`, `no_grant` when nothing matched, or `denied_by_rule` when an explicit deny overrode an allow. The distinction is the one worth surfacing to a user — `no_grant` means ask an administrator, `denied_by_rule` means one has already decided."},{type:`warn`,text:"`deny_reason` is deprecated. It carries the same string as `reason` until AXIAM 2.0 removes it; new code reads `reason` and must not depend on the older field surviving."},{type:`h`,id:`deadlines`,text:`Deadlines and retries`},{type:`p`,text:`The retry policy is contract-level and identical across transports, so a gRPC check retries exactly as a REST one does. Every value below is binding on an SDK claiming conformance.`},{type:`table`,headers:[`Parameter`,`Value`],rows:[[`Attempt cap`,`3 total — one initial call and two retries`],[`Base delay`,`200 ms`],[`Delay cap`,`5 s on any single wait`],[`Backoff`,"`min(cap, base × 2^(attempt−1))` — 200 ms, then 400 ms"],["`Retry-After`",`A floor on the computed backoff, never a ceiling`]]},{type:`p`,text:"Only side-effect-free operations are eligible, and that is not the same as *reads a GET*: `CheckAccess` and `BatchCheckAccess` both qualify, and they are the reason the policy exists. Token minting, credential validation and every mutation are excluded — a transient failure after the server committed is indistinguishable at the client from one before it committed."},{type:`note`,text:`A caller who needs more than three attempts should retry at their own layer, where the deadline is known. An SDK may lower the cap or switch retry off; it may never raise it, because a caller who can raise it turns one client into the herd a backoff exists to prevent.`},{type:`h`,id:`sender-constrained`,text:`Sender-constrained tokens`},{type:`note`,text:"`ValidateToken` and `IntrospectToken` return a `cnf` claim, and a token that carries one is **not** a bearer token whichever wire it arrived on. `valid: true` means the signature, expiry and tenant check out — not that the token is usable as presented. When `cnf` is present the caller must verify possession against its **own** connection, because AXIAM cannot: the proof is bound to the caller's connection, not to the one carrying the introspection call. A `cnf` whose members are all empty must be refused rather than read as unbound — proto3 cannot tell an absent string from an empty one."},{type:`p`,text:"AXIAM's own gRPC interceptor refuses `jkt`-bound tokens, because a Tonic interceptor sees neither the HTTP method nor the URI a DPoP proof is bound to. That is the server's limitation and should not be copied: an SDK guarding a real endpoint knows both, so it can and should verify the proof."},{type:`h`,id:`codegen`,text:`Generating your own stubs`},{type:`p`,text:"Integrating from a language with no published AXIAM SDK? Generate stubs straight from the `.proto` files with `buf generate`, or `protoc` plus your language's gRPC plugin. They are self-contained proto3 with no imports beyond the well-known types, and CI runs `buf lint` and `buf breaking` on every change — so the contract is guarded against accidental breakage."},{type:`code`,code:`buf generate   # from the vendored proto/ tree`},{type:`note`,text:"The Kotlin, Swift, C and C++ SDKs cover the REST surface. gRPC is deferred rather than scheduled for them — the contract sets no §-level gRPC requirement for those four — so use the REST transport, or generate stubs straight from `proto/` if you need this surface. The one thing REST cannot substitute for is `GetUserInfo`, which has no REST form in the SDK vocabulary."}]},{slug:`amqp`,section:`APIs & integration`,navLabel:`AMQP & async`,title:`AMQP — asynchronous authorization & events`,intro:`The message bus behind deferred authorization decisions, audit ingestion, mail, webhook delivery and Reactor hooks — specified as AsyncAPI 2.6.`,verifiedRelease:r,blocks:[{type:`h`,id:`why`,text:`What runs over the bus`},{type:`p`,text:`Some work should not happen on a request thread. Audit ingestion must not slow down the operation being audited; webhook delivery must survive a receiver being down; a mail send must not fail a signup. AXIAM puts all of it on RabbitMQ, and exposes the same authorization engine there for callers that want a decision without holding a connection open.`},{type:`table`,proseFirstCol:!0,headers:[`Queue`,`Purpose`,`Dead-letters to`],rows:[[`axiam.authz.request`,`Deferred authorization requests.`,"`axiam.authz.request.dlq`"],[`axiam.authz.response`,`The decisions, correlated back to the requester.`,`—`],[`axiam.audit.events`,`Audit ingestion, off the request hot path.`,"`axiam.audit.events.dlq`"],[`axiam.notifications`,`Notification-rule delivery.`,`—`],[`axiam.mail.outbound`,`Outbound mail — verification, reset, alerts.`,"`axiam.mail.outbound.dlq`"],[`axiam.webhook`,`Webhook delivery.`,"`axiam.webhook.dlq`"],[`axiam.webhook.retry`,"Delay queue for webhook backoff. Nothing consumes it — a message published here with a per-message TTL dead-letters back to `axiam.webhook` when the TTL expires, which is how the retry delay happens without a consumer sleeping.","`axiam.webhook` (by design)"]]},{type:`note`,text:"Dead-lettering is per queue, not universal. Four queues have a DLQ; `axiam.authz.response` and `axiam.notifications` do not, and `axiam.webhook.retry` dead-letters *forward* into the primary queue as its delay mechanism rather than as a failure path. Messages that reach a `.dlq` are real and replayable — they are not dropped."},{type:`h`,id:`exchanges`,text:`Exchanges`},{type:`table`,proseFirstCol:!0,headers:[`Exchange`,`Kind`,`Purpose`],rows:[[`axiam.authz.cache.invalidate`,`fanout`,`Cross-replica authorization decision-cache invalidations. Fanout rather than a work queue for a load-bearing reason: every replica binds its own exclusive auto-delete queue, so every replica sees every invalidation. A shared queue would hand each message to exactly one consumer and leave the rest serving stale allows.`],[`axiam.reactor.events`,`reactor hook events`,`The Reactor bus — see [Reactors](#/docs/reactors) for the request/reply contract.`]]},{type:`h`,id:`envelope`,text:`One signed message`},{type:`p`,text:"Every message carries an HMAC-SHA256 over its own body, and since `key_version` 2 that body must also carry a `nonce` and an `issued_at`. Both are always emitted — never omitted — so they fall inside the signed bytes."},{type:`code`,caption:`an authz request as it travels · the reference vector`,code:`{
  "correlation_id": "22222222-2222-2222-2222-222222222222",
  "tenant_id":      "11111111-1111-1111-1111-111111111111",
  "subject_id":     "33333333-3333-3333-3333-333333333333",
  "action":         "documents:read",
  "resource_id":    "44444444-4444-4444-4444-444444444444",
  "scope":          "confidential",
  "key_version":    2,
  "nonce":          "55555555-5555-5555-5555-555555555555",
  "issued_at":      "2026-07-10T12:00:00Z",
  "hmac_signature": "13d73b3aa8a400fc3f64dbc20b36952d8584142feb822b5f77495b0f587049ed"
}`},{type:`p`,text:"The signature is computed over the same object with `hmac_signature` **absent**, serialized in exactly the field order above — `correlation_id`, `tenant_id`, `subject_id`, `action`, `resource_id`, `scope` (omitted when null), `key_version`, `nonce`, `issued_at`. Order is part of the construction, not a formatting detail: a verifier that re-serializes in a different order computes a different digest and rejects a valid message."},{type:`p`,text:"The key is not the master secret. It is a per-tenant subkey derived with HKDF-SHA256, domain-separated and versioned by `key_version`, then scoped to the tenant — so a signature made with one tenant's subkey never verifies under another's, and rotating the master key yields entirely different subkeys without breaking messages already in flight under the previous version."},{type:`table`,headers:[`Check`,`Rule`],rows:[[`Signature`,`Recompute and compare in constant time. A mismatch is nacked **without** requeue and logged as a security event — never the digest itself.`],[`Version`,"`key_version` below 2 is rejected outright. The v2 cutover is hard; there is no grace path."],[`Freshness`,"`issued_at` must lie within ±5 minutes of the consumer's clock (`AXIAM__AMQP__REPLAY_SKEW_SECS`)."],[`Replay`,"`(tenant_id, nonce)` is recorded durably; a repeat inside the freshness window is a replay and is rejected."]]},{type:`note`,text:"Vectors every SDK must reproduce byte-for-byte live in `crates/axiam-amqp/tests/fixtures/v2_reference_vectors.json` — the sample above is one of them. Reproducing the canonical JSON and recomputing the digest is the conformance test, which is why the fixture is shared rather than each SDK inventing its own."},{type:`h`,id:`security`,text:`Transport security`},{type:`warn`,text:"AMQP is **TLS-only**. `AXIAM__AMQP__URL` must be `amqps://`; every other scheme is refused before a socket opens, in a debug build exactly as in a release one. There is no environment variable, build profile or flag that changes the answer — the `ALLOW_PLAINTEXT` escape hatch that once permitted `amqp://` has been removed."},{type:`p`,text:`That removal has a history worth repeating, because it is the usual shape of this failure. The flag existed for a year, and four of the project's own stacks reached for it — dev compose, the e2e stack, the benchmark target and CI — each with a locally sound argument. None was wrong on its own. The aggregate was that "AMQP is TLS-only" described the production compose file and the Kubernetes manifests, and nothing else the repository actually ran.`},{type:`p`,text:`An in-cluster broker's certificate is usually privately issued, so **supplying a custom CA bundle is the common case, not the exception** — every SDK that speaks AMQP must support it. Client certificates toward the broker are supported where an SDK offers them, and the certificate and key are required together: half a client identity fails closed rather than connecting without the mutual half.`},{type:`note`,text:`TLS and the HMAC are not alternatives. TLS gives confidentiality but terminates at the broker, which then re-sends; the HMAC gives authenticity and replay protection end-to-end **across** that hop. Production needs both, and an SDK offering either as a substitute for the other is not conformant.`},{type:`h`,id:`spec`,text:`The specification`},{type:`p`,text:"`docs/api/asyncapi.yml` is a hand-authored AsyncAPI 2.6 document covering every channel, its message schema and its DLQ. The normative HMAC construction is `sdks/CONTRACT.md` §8 and §8b, which every SDK implements identically."},{type:`note`,text:`Reactors also ride this bus, but with a different contract: a Reactor can *answer* — allow, deny, or a narrowly field-allow-listed mutation — inside a timeout the server declares. See [Reactors](#/docs/reactors).`}]},{slug:`scim`,section:`APIs & integration`,navLabel:`SCIM provisioning`,title:`SCIM 2.0 provisioning`,intro:`Let Okta, Microsoft Entra ID or any SCIM-compliant IdP create, update and deactivate AXIAM users and groups directly, instead of an administrator doing it by hand.`,verifiedRelease:r,blocks:[{type:`h`,id:`why`,text:`Federation is not provisioning`},{type:`p`,text:`Federation answers *who is this person* at sign-in. It does not create an account before someone's first day, does not update it when they change teams, and — the one that matters — does not deactivate it when they leave. SCIM does all three, driven by the directory that already knows.`},{type:`h`,id:`support`,text:`What is implemented`},{type:`table`,proseFirstCol:!0,headers:[`Area`,`Support`],rows:[[`Users`,`Full CRUD plus PATCH.`],[`Groups`,`Full CRUD plus PATCH, including membership.`],[`Filtering`,'`userName eq "..."` on Users, `externalId eq "..."` on Users and Groups, and paging with `startIndex` / `count`.'],[`PATCH operations`,"`add` / `replace` / `remove` on the standard attribute paths Okta and Entra actually send."],[`Discovery`,"`/Schemas`, `/ServiceProviderConfig`, `/ResourceTypes`."],[`Bulk operations`,"**Not supported** — `POST /Bulk` returns `501` with a SCIM error body."],[`Complex filters`,'**Not supported** — anything other than `<attr> eq "<value>"` returns `400 invalidFilter`.'],[`ETag / conditional requests`,"**Not implemented**, and `ServiceProviderConfig` says so."]]},{type:`note`,text:`The unsupported items are the two the specification itself carves out as optional, and the two no mainstream IdP requires for user and group lifecycle. Enterprise IdP-driven provisioning is mostly CRUD.`},{type:`h`,id:`endpoints`,text:`Endpoints`},{type:`code`,code:`GET    /scim/v2/ServiceProviderConfig
GET    /scim/v2/ResourceTypes
GET    /scim/v2/Schemas

GET    /scim/v2/Users?filter=...&startIndex=1&count=50
POST   /scim/v2/Users
GET    /scim/v2/Users/{id}
PUT    /scim/v2/Users/{id}
PATCH  /scim/v2/Users/{id}
DELETE /scim/v2/Users/{id}

GET    /scim/v2/Groups?filter=...&startIndex=1&count=50
POST   /scim/v2/Groups
GET    /scim/v2/Groups/{id}
PUT    /scim/v2/Groups/{id}
PATCH  /scim/v2/Groups/{id}
DELETE /scim/v2/Groups/{id}`},{type:`h`,id:`tokens`,text:`Minting a provisioning token`},{type:`p`,text:"A SCIM client authenticates with a **provisioning token** — a long-lived, revocable bearer handle meant to be pasted into an IdP once and forgotten. It is not an access token, and it is not a separate token *type* on the SCIM endpoints: `/scim/v2/*` uses the same bearer authentication as the rest of the REST API."},{type:`steps`,steps:[{title:`Create a dedicated provisioning user`,body:`Non-interactive, used for nothing else. A shared administrator account here makes the audit trail useless and the blast radius unnecessary.`,code:`POST /api/v1/users
{ "username": "scim-provisioner", "...": "..." }`},{title:`Create a role holding only scim:provision`,body:"Least privilege, and specifically not the `admin` role — the default-role seeder grants admin every permission except `admin:bootstrap`, so it carries `scim:provision` along with everything else.",code:`POST /api/v1/roles
POST /api/v1/roles/{role_id}/permissions   { "permission": "scim:provision" }`},{title:`Assign the role to that user`,body:`The token you mint next inherits its authority from here, so this is the step that decides what the IdP can do.`,code:`POST /api/v1/roles/{role_id}/users`},{title:`Mint the provisioning token`,body:"Requires `scim_tokens:create`. The value is returned exactly once and stored only as a SHA-256 hash — there is no way to read it back.",code:`POST /api/v1/scim-tokens`},{title:`Paste it into the IdP`,body:`Okta calls this HTTP Header authentication; Entra calls it the Secret Token. Nothing else needs to be configured on the AXIAM side.`}]},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/scim-tokens`,summary:`List provisioning tokens for the tenant.`},{method:`POST`,path:`/api/v1/scim-tokens`,summary:`Mint one. The value is returned exactly once.`},{method:`DELETE`,path:`/api/v1/scim-tokens/{id}`,summary:`Revoke one.`}]},{type:`h`,id:`token-limits`,text:`What the token can and cannot do`},{type:`table`,proseFirstCol:!0,headers:[`Property`,`Behaviour`],rows:[[`Scope`,"Accepted on `/scim/v2/*` and **nowhere else** — not `/api/v1/*`, not `/oauth2/*`. That containment is what makes a year-long credential defensible in a system whose access tokens live 15 minutes."],[`Permissions`,"**None of its own.** It authenticates *as* the tenant user it was minted for, and that user's `scim:provision` grant decides everything. Unassign the role or deactivate the user and every token bound to them stops working — no separate revocation needed."],[`Storage`,`Only a SHA-256 hash is kept. The value is shown once, at creation.`],[`Recognisability`,"Fixed prefix `axiam_scim_`, so a secret scanner or a grep finds it if it is ever pasted somewhere it should not be."],[`Lifetime`,"Always expires — there is no never-expires option. The ceiling is `AXIAM__SCIM_TOKEN_MAX_LIFETIME_DAYS`, default 365."],[`Tenant reach`,`One tenant, enforced by construction rather than by a check: every repository call takes its tenant id from the token's own claim, never from the request path or body. A token for tenant A cannot even name tenant B's users in a query.`]]},{type:`warn`,text:"**Treat this as an administrator credential, not an integration credential.** RFC 7643 makes `password` a writable User attribute and `PATCH /scim/v2/Users/{id}` honours it — so a holder of `scim:provision` can set any user's password in the tenant, including a tenant administrator's, and then sign in as them. That makes the one permission strictly more powerful than `users:create` and `users:update` combined; the native admin API has no equivalent, because `PUT /api/v1/users/{id}` never writes a password hash. Rotate and store it the way you would an admin password."},{type:`note`,text:"Most Okta and Entra deployments federate and never push a password, so nothing is lost by the IdP never exercising that capability. AXIAM cannot yet take it away, though — `password` writes are not behind a second permission. Worth knowing before you grant it rather than after."},{type:`h`,id:`deprovision`,text:`What deprovisioning actually does`},{type:`p`,text:"Deactivation is immediate and complete. A SCIM `password` write, an `active: false` (by `PUT` or `PATCH`), and `DELETE /scim/v2/Users/{id}` each revoke every live session **and** every OAuth2 refresh token the target holds, on top of flushing the authorization decision cache."},{type:`note`,text:`That last part is the fix for a real gap: before it, only the decision cache was flushed, so an account an offboarding job had just deactivated still held a spendable refresh token. If you are reasoning about offboarding latency, this is the behaviour to rely on.`},{type:`warn`,text:"Deactivation, not deletion, is usually what you want from an IdP. A SCIM `DELETE` removes the account; `active: false` disables sign-in while leaving the identity intact for the audit trail. Configure your IdP's deprovisioning action deliberately — both are wired, and they are not the same decision."},{type:`h`,id:`okta`,text:`Okta`},{type:`steps`,steps:[{title:`Create or edit an app integration with SCIM provisioning`,body:`In the Okta Admin Console, under the app's Provisioning tab.`},{title:`Set the SCIM connector base URL`,body:`The whole SCIM surface hangs off this prefix.`,code:`https://<your-axiam-host>/scim/v2`},{title:`Set the unique identifier field to userName`,body:"This is the attribute AXIAM filters on, and the only User filter supported besides `externalId`."},{title:`Enable the provisioning actions you want`,body:`Push New Users, Push Profile Updates and Push Groups are all supported — standard CRUD plus PATCH.`},{title:`Set authentication mode to HTTP Header and paste the token`,body:`The provisioning token from the steps above. Okta's own Test API Credentials button then exercises the real request shapes: a filtered user lookup, a create, and a deactivating PATCH.`}]},{type:`p`,text:'Okta deactivates with `{"op": "replace", "path": "active", "value": false}`. Group push sends a `displayName` and, once members are assigned, a `members` array, then patches membership with a `members[value eq "<uuid>"]` remove when one user leaves the group.'},{type:`h`,id:`entra`,text:`Microsoft Entra ID`},{type:`steps`,steps:[{title:`Set provisioning mode to Automatic`,body:`Entra admin center → Enterprise applications → your app → Provisioning.`},{title:`Set the Tenant URL`,body:`Entra's name for the same SCIM base URL.`,code:`https://<your-axiam-host>/scim/v2`},{title:`Paste the provisioning token as the Secret Token`,body:`Same credential as Okta's HTTP Header value.`},{title:`Test the connection`,body:`Entra probes a single-user page as its validity check, so a green result means auth and paging both work.`,code:`GET /scim/v2/Users?startIndex=1&count=1`},{title:`Leave the default attribute mappings alone`,body:"Entra's defaults already match the supported subset: `userPrincipalName` to `userName`, `mail` and `otherMails` to `emails`, `givenName` and `surname` to the `name` sub-attributes, and `accountEnabled` to `active`."}]},{type:`note`,text:'Entra deactivates with a path-less operation — `{"op": "replace", "value": {"active": false}}` — where Okta names the path. Both shapes are handled, which is the kind of divergence that otherwise shows up as an IdP that can create users but never disable them.'},{type:`h`,id:`limits`,text:`Rate limiting`},{type:`p`,text:"The whole `/scim/v2` scope shares **one** bucket, `AXIAM__RATE_LIMIT__SCIM_PER_MIN`, defaulting to 600 per minute per IP — Users, Groups and discovery, reads and writes alike. Past it, requests get the standard `429` with `Retry-After: 60`, which a well-behaved SCIM client honours."},{type:`note`,text:"That ceiling is a CPU guard rather than a throughput number: creating a SCIM user generates and Argon2id-hashes an initial password, and a `password` patch re-hashes one. No rate-limit profile preset moves it — a service-mesh capacity decision must not silently widen an administrative surface. For scale, at a typical IdP page size of 200 a full import of a 100,000-user directory is roughly 500 list calls, well inside one minute's budget."},{type:`links`,links:[{label:`SCIM provisioning reference`,href:`https://github.com/ilpanich/axiam/blob/main/docs/api/scim-provisioning.md`,note:`Field mappings, the PATCH shapes each IdP sends, and the contract fixtures.`}]},{type:`note`,text:`The Okta and Entra contract fixtures are hand-constructed from each vendor's published SCIM notes and RFC 7644's examples, not captured from live traffic. Read them as the request shapes those vendors are documented to send, rather than as a captured-traffic compatibility guarantee.`}]},{slug:`webhooks`,section:`APIs & integration`,navLabel:`Webhooks`,title:`Webhooks`,intro:`Fire-and-forget HTTP callbacks on domain events, signed with a timestamped HMAC so a receiver can prove they came from AXIAM and reject a replay.`,blocks:[{type:`h`,id:`delivery`,text:`Signed delivery`},{type:`p`,text:`A webhook delivers an event notification to an endpoint you configure, as an outbound HTTPS POST. Delivery is **at-least-once**: the server queues each delivery on a durable AMQP topology, retries with exponential backoff, and dead-letters what never succeeds. Your receiver must be idempotent — nothing here promises exactly-once, and a retry replays a validly signed request.`},{type:`p`,text:"Every attempt goes through the same SSRF guard the federation client uses: the host is resolved fresh, a private, loopback or link-local address is refused, the validated IP is pinned into the connection so nothing can re-resolve between the check and the send, and a non-HTTPS target is treated as blocked. The shared secret is stored AES-256-GCM encrypted under `AXIAM__PKI__ENCRYPTION_KEY`, is never returned by any endpoint, and is decrypted in memory only to compute a signature — with no key configured the subsystem fails closed with a `503` rather than delivering unsigned."},{type:`h`,id:`headers`,text:`What arrives`},{type:`table`,headers:[`Header`,`Value`],rows:[[`X-Axiam-Signature`,"`t=<unix_seconds>,v1=<hex_lowercase>`"],[`X-Axiam-Timestamp`,"unix seconds, decimal — the same value as `t=`"],[`X-Axiam-Event`,"the event type, e.g. `user.created`"],[`X-Axiam-Delivery`,`delivery UUID — the at-least-once dedup key`]]},{type:`p`,text:'`v1` is `HMAC-SHA256(secret, "<timestamp>.<raw_body>")`, hex-encoded lowercase, where `<timestamp>` is byte-identical to the `t=` field. Binding the timestamp into the signed string is what lets a receiver enforce a replay window: a captured delivery replayed an hour later carries a valid MAC over a timestamp that is now stale.'},{type:`code`,caption:`a delivery, on the wire`,code:`POST /webhooks/axiam HTTP/1.1
Content-Type: application/json
X-Axiam-Timestamp: 1785700000
X-Axiam-Signature: t=1785700000,v1=3f2b…c91d
X-Axiam-Event: user.created
X-Axiam-Delivery: 018f3c2a-8f11-7b0e-9a54-2c1f7d3e5b90

{"id":"018f3c2a-…","username":"alice"}`},{type:`note`,text:`The body is the **event payload itself** — there is no envelope around it. The event type and the delivery id travel in headers, so read them there rather than expecting them in the JSON.`},{type:`h`,id:`verify`,text:`Verifying a delivery`},{type:`p`,text:"Every SDK ships the verifier, so this is not a thing to hand-roll: `verify_webhook` in Rust and Python, `verifyWebhook` in TypeScript, `AxiamWebhooks.verify` in Java, Kotlin and Swift, `AxiamWebhooks.Verify` in C#, `AxiamWebhooks::verify` in PHP, `webhook.Verify` in Go, `axiam_webhook_verify` in C and `axiam::webhook::verify` in C++. Each takes the secret, the raw `X-Axiam-Signature` value, the raw body bytes and an optional tolerance defaulting to **300 s**, compares in constant time, and fails closed and quiet — the error never carries the expected signature."},{type:`codegroup`,caption:`verifying a delivery`,tabs:[{label:`Rust`,code:`use axiam_sdk::webhook::{WebhookVerifyOptions, verify_webhook};

async fn receive(req: HttpRequest, body: web::Bytes) -> HttpResponse {
    let header = |n: &str| req.headers().get(n).and_then(|v| v.to_str().ok()).unwrap_or("");

    let opts = WebhookVerifyOptions::new()
        .event_type(header("X-Axiam-Event"))
        .delivery_id(header("X-Axiam-Delivery"))
        .timestamp_header(header("X-Axiam-Timestamp"));

    // \`body\` is the UNPARSED request body — verify first, parse after.
    match verify_webhook(&secret, header("X-Axiam-Signature"), &body, &opts) {
        Ok(event) => {
            if already_seen(event.delivery_id) {
                return HttpResponse::Ok().finish();   // at-least-once retry
            }
            handle(serde_json::from_slice(event.body).unwrap());
            HttpResponse::Ok().finish()
        }
        // Never echo the reason back to the sender.
        Err(_) => HttpResponse::Unauthorized().finish(),
    }
}`},{label:`TypeScript`,code:`import { verifyWebhook, WebhookVerifyError } from 'axiam-sdk';

app.post('/webhooks/axiam', (req, res) => {
  try {
    // req.rawBody is the exact bytes off the wire — express.json() alone
    // discards them; capture them with its verify callback.
    verifyWebhook(secret, req.header('X-Axiam-Signature'), req.rawBody);
  } catch (err) {
    if (err instanceof WebhookVerifyError) return res.status(400).end();
    throw err;
  }

  const type = req.header('X-Axiam-Event');
  const deliveryId = req.header('X-Axiam-Delivery');   // dedup on this
  res.status(200).end();
});`},{label:`Python`,code:`from axiam_sdk.webhook import WebhookVerifyError, verify_webhook

@app.post("/webhooks/axiam")
def axiam_webhook():
    try:
        event = verify_webhook(
            secret=WEBHOOK_SECRET,
            signature_header=request.headers["X-Axiam-Signature"],
            body=request.get_data(),          # raw bytes, NOT re-serialized JSON
            event_type=request.headers.get("X-Axiam-Event"),
            delivery_id=request.headers.get("X-Axiam-Delivery"),
        )
    except WebhookVerifyError:
        return "invalid signature", 400

    # event.delivery_id is the at-least-once dedup key.
    return "", 200`},{label:`Go`,code:`body, err := io.ReadAll(r.Body)
if err != nil {
    http.Error(w, "failed to read body", http.StatusBadRequest)
    return
}

if _, err := webhook.Verify(
    axiam.Sensitive(webhookSecret),
    r.Header.Get("X-Axiam-Signature"),
    body,
); err != nil {
    http.Error(w, "invalid webhook signature", http.StatusUnauthorized)
    return
}

deliveryID := r.Header.Get("X-Axiam-Delivery")   // dedup on this
w.WriteHeader(http.StatusOK)`}]},{type:`warn`,text:"**Verify the raw bytes.** Re-serialising the parsed JSON and hashing that will not match — key order and whitespace are part of what was signed, and most JSON body parsers discard the original bytes by default. Parse only after the comparison succeeds, take `t=` from the signature header rather than from `X-Axiam-Timestamp` (only the former is covered by the MAC), and treat a header with no `v1` as a failure rather than as nothing to check."},{type:`h`,id:`events`,text:`The event catalog`},{type:`p`,text:`A webhook subscribes to a list of event-type names. Three are emitted today, from the user-management endpoints and from SCIM provisioning alike, so an IdP-driven provisioning run raises the same events as an API call:`},{type:`table`,headers:[`Event`,`Raised when`,`Payload`],rows:[[`user.created`,"A user is created through `POST /api/v1/users` or SCIM provisioning","`id`, `username`"],[`user.updated`,"A user is updated through the API, SCIM `PUT` or SCIM `PATCH`","`id`, `username`"],[`user.deleted`,`A user is deleted through the API or deprovisioned through SCIM`,"`id`"]]},{type:`note`,text:`The subscription list is not validated against a catalog — it must be non-empty, and that is all. Subscribing to a name the server does not raise is accepted and simply never fires, so treat a silent webhook as a possible typo before treating it as a delivery failure. The catalog is expected to grow; a delivery you do not recognise should be ignored rather than rejected.`},{type:`h`,id:`retry`,text:`Retry, backoff and the dead-letter queue`},{type:`p`,text:"Retry scheduling belongs to the broker, not to a sleeping task. A delivery is published to `axiam.webhook`; a failed attempt is republished to `axiam.webhook.retry` with a per-message TTL and no consumer attached, so when the TTL expires RabbitMQ dead-letters it back onto `axiam.webhook` for the next attempt. Once the attempts are exhausted the delivery lands on `axiam.webhook.dlq`, where it is real and replayable rather than silently dropped. Every attempt and every terminal outcome is written to the audit log."},{type:`table`,headers:[`Config key`,`Default`,`Meaning`],rows:[[`AXIAM__WEBHOOK__MAX_ATTEMPTS`,"`5`",`Total delivery attempts before the message is dead-lettered; the first attempt counts as one.`],[`AXIAM__WEBHOOK__BACKOFF_BASE_MS`,"`5000`",`Delay before the first retry.`],[`AXIAM__WEBHOOK__BACKOFF_CEILING_MS`,"`3600000`",`Upper bound on any single retry delay — one hour.`]]},{type:`p`,text:"The delay is `base × 2^(attempt − 1)`, clamped to the ceiling — 5 s, 10 s, 20 s, 40 s on the defaults. The multiplier is fixed at 2 and is not configurable."},{type:`warn`,text:"A webhook also carries a per-endpoint `retry_policy` (`max_retries`, `initial_delay_secs`, `backoff_multiplier`), which is validated and stored — `max_retries` at most 10, `initial_delay_secs` between 1 and 3600, `backoff_multiplier` between 0 and 10. The delivery consumer does **not** read it: the schedule that runs is the deployment-wide one above. Treat the field as recorded intent, not as a per-endpoint control."},{type:`h`,id:`managing`,text:`Managing webhooks`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/webhooks`,summary:`List configured webhooks. The secret is never included.`},{method:`POST`,path:`/api/v1/webhooks`,summary:`Create one, subscribing to a list of event types.`},{method:`GET`,path:`/api/v1/webhooks/{id}`,summary:`Read one.`},{method:`PUT`,path:`/api/v1/webhooks/{id}`,summary:`Update the URL, the subscription, the enabled flag or the secret.`},{method:`DELETE`,path:`/api/v1/webhooks/{id}`,summary:`Delete it.`}]},{type:`code`,caption:`POST /api/v1/webhooks`,code:`{
  "url": "https://hooks.example.com/axiam",
  "events": ["user.created", "user.updated", "user.deleted"],
  "secret": "whsec_…",
  "retry_policy": { "max_retries": 5, "initial_delay_secs": 10, "backoff_multiplier": 2.0 }
}`},{type:`p`,text:"The URL must be HTTPS and must resolve to a globally routable address — a private, loopback or link-local target is refused at creation as well as at delivery. Every endpoint is permission-gated (`webhooks:create`, `webhooks:list`, `webhooks:get`, `webhooks:update`, `webhooks:delete`)."},{type:`h`,id:`rotation`,text:`Rotating a secret`},{type:`p`,text:"AXIAM signs each delivery with exactly one secret and sends exactly one `v1` value, so there is no overlap window on the server side. The overlap has to live in your receiver, which is why the order below matters: the secret is read fresh on every attempt, so a delivery still being retried when you rotate is re-signed with the new secret."},{type:`steps`,steps:[{title:`Generate the new secret`,body:`Use a high-entropy random value. It is a MAC key, not a password — length beats memorability, and nothing ever needs to type it.`},{title:`Teach the receiver to accept either`,body:`Verify against the new secret and fall back to the old one on failure, using the same SDK helper twice. Deploy this **before** rotating, so no delivery arrives with a secret your receiver has never heard of.`},{title:`Rotate on the server`,body:"`PUT` the webhook with the new `secret`. It is encrypted with AES-256-GCM before storage and never returned in a response, so this is also the only way to change it — there is no read-back.",code:`PUT /api/v1/webhooks/{id}
{ "secret": "whsec_new_…" }`},{title:`Wait out the retry window, then drop the old secret`,body:"Anything still in flight is re-signed with the new secret on its next attempt, so the fallback is only needed for deliveries already accepted by your receiver. Give it the worst-case retry span — `MAX_ATTEMPTS` attempts of backoff, up to the ceiling — before removing the old key, then redeploy with the single new secret."}]},{type:`h`,id:`vs`,text:`Webhook or Reactor?`},{type:`p`,text:`A webhook is told what happened, after it happened, and cannot affect it. If you need to *influence* an operation — approve it, refuse it, or adjust a narrowly allow-listed field before it commits — that is a Reactor. See [Reactors](#/docs/reactors).`}]},{slug:`reactors`,section:`APIs & integration`,navLabel:`Reactors`,title:`Reactors — external hook actors`,intro:`An external process that subscribes to authorization-adjacent hook events and answers back — allow, deny, or a narrowly allow-listed mutation — inside a timeout the server declared.`,blocks:[{type:`h`,id:`what`,text:`What a Reactor is`},{type:`p`,text:`A Reactor is AXIAM's answer to Zitadel Actions and Keycloak SPIs, and the difference is the whole design: those load third-party code **into** the authorization server. A Reactor stays outside, reachable only over the AMQP bus, and answers through a signed reply schema the server validates before it believes a word of it.`},{type:`p`,text:`That boundary is what makes the feature safe to have. A crashing, hanging or malicious Reactor cannot take the authorization server with it — it can, at worst, fail its own hook inside the declared timeout, and the configured failure policy decides what that means.`},{type:`h`,id:`registry`,text:`The five hookable events`},{type:`p`,text:"The registry is data, not prose: it lives in [EVENT_REGISTRY](https://github.com/ilpanich/axiam/blob/main/crates/axiam-core/src/models/reactor.rs), in `crates/axiam-core/src/models/reactor.rs`, and is served live at `GET /api/v1/reactors/events`, which is the copy a tool should read — the REST layer validates a registration against it and the dispatcher validates a reply against it, so there is one source and no second list to drift. The table below is that data as of this release."},{type:`table`,headers:[`Event`,`Interceptable`,`A patch may set`,`Default failure policy`,`Purpose`],rows:[[`token.pre_issue`,`yes`,"the `ext.` namespace only","`fail_open`","Enrich or veto token issuance. May add claims under `ext.` only."],[`login.post_auth`,`yes`,"nothing — veto, or `require_mfa`","`fail_closed`",`After credentials verify, before session issuance: veto or require step-up MFA.`],[`user.pre_create`,`yes`,"`username`, `email`, the `metadata.` namespace","`fail_closed`",`Validate or normalize a new user's profile fields.`],[`user.pre_update`,`yes`,"`username`, `email`, the `metadata.` namespace","`fail_closed`",`Validate or normalize a profile update.`],[`grant.pre_assign`,`yes`,`nothing — veto only`,"`fail_closed`",`Veto a role or permission assignment (four-eyes workflows). Veto-only.`]]},{type:`p`,text:"An allow-list entry ending in a dot is a **namespace prefix**, and it matches a field that starts with the entry and has at least one character after the dot. So `ext.` admits `ext.department` and `ext.a.b.c`, and refuses `ext.` itself, `ext`, `extra`, `external_id` and `evil.ext.department`. Everything else follows from that one rule: `token.pre_issue` cannot reach `iss`, `sub`, `aud`, `exp`, `scope` or any other standard claim, because none of them begins with `ext.` — a hook that can rewrite `sub` is a hook that can mint a token for anyone, and a correctly signed reply setting it is refused exactly as a forged one is."},{type:`p`,text:"**The asymmetry in the last column is the most instructive fact on this page.** `token.pre_issue` defaults to fail-open because its mutation is optional enrichment — an unreachable reactor costs you a claim, and degrading a feature is the right answer. The other four default to fail-closed because they can veto: a fraud check that times out has not passed, and an unreachable approver is not an approval. A registration that names several events inherits the **strictest** default among them, in either array order."},{type:`note`,text:"`login.post_auth` covers every interactive sign-in, not only password login: it fires on password authentication, on SAML ACS, on the OIDC callback and on usernameless passkey sign-in — in each case after the credentials verify and before any session or token is issued. MFA completion and the username-bound WebAuthn ceremony are not separate firings; both continue a login already gated at its first step. The federated and usernameless paths have no step-up branch, so a `require_mfa` answer there fails the sign-in rather than being silently dropped — see [CONTRACT §22.5](https://github.com/ilpanich/axiam/blob/main/sdks/CONTRACT.md)."},{type:`h`,id:`modes`,text:`Intercept and listen`},{type:`p`,text:"A registration is `intercept` or `listen`. An interceptor sits in the operation: the server publishes the event to that reactor's queue, waits up to the declared timeout, and applies the validated reply. A listener is fire-and-forget observation — the server never waits and never reads a reply, so it cannot affect any outcome."},{type:`warn`,text:'**Listen registrations are refused today.** `POST` and `PUT` answer `503` for `mode: "listen"`, because no hook site fans out to listeners yet: the registration would receive nothing, and — being a listener — would produce no outcome in which you could notice. Register with `mode: "intercept"`, or create it with `enabled: false` until the fan-out ships.'},{type:`p`,text:"All five events above are interceptable, so the registry's `interceptable` flag has no effect today. It is carried because a sixth event may be listen-only, and the rule is already fixed: a listener may subscribe to **every** registered event, including one the registry marks non-interceptable — precisely because it cannot influence it."},{type:`h`,id:`vs`,text:`Reactor or webhook?`},{type:`table`,proseFirstCol:!0,headers:[``,`Webhook`,`Intercepting Reactor`],rows:[[`Transport`,`Outbound HTTPS POST to a URL you configure`,`AMQP consume from a durable, server-declared queue`],[`Authenticity`,"HMAC-SHA256 over `timestamp.body`, in a signed-timestamp header",`HMAC over the whole message, in **both** directions, replay-protected with a nonce and a freshness window`],[`When it runs`,`After the change is committed`,`Inside the operation, before it commits`],[`Can it affect the operation?`,`Never`,`Allow, deny, or a mutation limited to the event's allow-list`],[`What it hears`,`The domain events a webhook can subscribe to — see [Webhooks](#/docs/webhooks)`,`The five registry events above, and nothing else`],[`If it is unreachable`,`Retried, then dead-lettered; the operation already happened`,"The registration's `failure_policy` decides — and for four of the five events the default is to refuse"]]},{type:`h`,id:`wire`,text:`On the wire`},{type:`p`,text:"Both directions carry the same v2 signature: `HMAC-SHA256` with the tenant's HKDF-derived AMQP subkey over the canonical serialization, with `nonce` and `issued_at` **inside** the signed bytes and a ±300 s two-sided freshness window. A reply is an instruction to change a token or refuse a login, so an unsigned reply is not a weak reply — it is not a reply at all. The two shapes below are illustrative; [CONTRACT §22.3–§22.4](https://github.com/ilpanich/axiam/blob/main/sdks/CONTRACT.md) is normative."},{type:`code`,caption:`event — server → reactor`,code:`{
  "tenant_id": "11111111-1111-1111-1111-111111111111",
  "event": "token.pre_issue",
  "correlation_id": "22222222-2222-2222-2222-222222222222",
  "payload": { "sub": "alice", "client_id": "portal" },
  "timeout_ms": 500,
  "key_version": 2,
  "nonce": "aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa",
  "issued_at": "2026-07-10T12:00:00Z",
  "hmac_signature": "…"
}`},{type:`code`,caption:`reply — reactor → server`,code:`{
  "correlation_id": "22222222-2222-2222-2222-222222222222",
  "tenant_id": "11111111-1111-1111-1111-111111111111",
  "event": "token.pre_issue",
  "decision": "mutate",
  "patch": { "ext.cost_center": "42", "ext.department": "eng" },
  "key_version": 2,
  "nonce": "bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb",
  "issued_at": "2026-07-10T12:00:00Z",
  "hmac_signature": "…"
}`},{type:`p`,text:"`payload` never carries a credential, a token or a signing key: a reactor is told what is being decided, not handed the means to act on it elsewhere. `correlation_id` is the single-use handle for one dispatch and must be echoed **in the reply body** — copying it only into the AMQP property produces a reply the server discards. `timeout_ms` is inside the signed body so it cannot be widened in transit; it is sent so an actor can shed load rather than answer into a closed window."},{type:`list`,items:[`The server validates in a fixed order — identity, freshness, signature, then semantics — so allow-list logic is never spent on bytes nobody authenticated.`,`**One forbidden patch key rejects the whole patch**, including the fields that would have been fine. An SDK must not quietly filter a handler's patch down to the allowed subset: that leaves the author believing a field was set when it was dropped.`,'**`allow` and `patch` are mutually exclusive** — a mutation must be `decision: "mutate"`. `require_mfa` rides on `allow`, on `login.post_auth` only.',`Every rejection is audited and resolves to the registration's failure policy. A rejected reply is not a softer failure than no reply at all.`]},{type:`h`,id:`budget`,text:`Timeouts and the budget`},{type:`table`,proseFirstCol:!0,headers:[`Setting`,`Value`],rows:[["`timeout_ms` default",`500 ms`],["`timeout_ms` accepted range","1 … 5 000 ms — `0` and anything larger is refused at registration"],[`Chain wall-clock ceiling`,`5 000 ms`],[`Effective per-reactor budget`,"`min(timeout_ms, 5000 − elapsed)`"],[`Per-tenant in-flight interceptions`,`64 by default`]]},{type:`p`,text:"Interceptors for one event run sequentially in ascending `priority`, and a deny short-circuits the rest. The budget is wall-clock rather than a sum, and running out of it is **not** a way to skip a check: when the ceiling is exhausted the remaining reactors are not contacted and each of their failure policies is applied anyway, so an unreached fail-closed veto still denies. Back-pressure is immediate rather than queued — breaching the in-flight cap fails the interception at once and applies the policy, because queueing behind a concurrency bound just turns it into an unbounded latency bound."},{type:`note`,text:"A fail-open timeout produces `allow` **and** an audit record naming the reactor. That pair is the whole difference between “no reactor was configured” and “the reactor never answered”, so do not infer reactor health from the outcome alone — `GET /api/v1/reactors/{id}` reports `last_seen_at`, `recent_timeout_count` and `recent_veto_count` for exactly this reason."},{type:`h`,id:`hotpath`,text:`Never on the check path`},{type:`warn`,text:"`authz.check`, `authz.check_batch` and `token.introspect` are **not hookable**, and no SDK may present them as such: they are absent from the registry, a registration naming one is refused as an unknown event, and the dispatcher resolves an unregistered event to `allow` without contacting anything. The reason is arithmetic, not policy — a reactor round trip is milliseconds and the check path's budget is microseconds. An application that needs external input on an authorization decision writes a **deny grant**, which the engine evaluates in the hot path at hot-path cost."},{type:`h`,id:`handlers`,text:`Binding handlers`},{type:`p`,text:`A reactor registered for three events opens with a dispatch on the event name, and that dispatch is where the expensive defect lives: the catch-all arm that returns *allow* answers on behalf of code that never ran, defeating an operator's fail-closed setting from a file they never read. Every SDK that ships the runtime therefore also ships a declarative binder — one handler per event, composed into the single handler the runtime takes. A name outside the registry is refused **at bind time**, and an event with no handler **abstains**: no reply, and the failure policy decides.`},{type:`codegroup`,caption:`declarative handler binding`,tabs:[{label:`Rust`,code:`use axiam_sdk::amqp::reactor::{ReactorDecision, ReactorRouter, events, reactor_serve};

let handler = ReactorRouter::new()
    .bind(events::TOKEN_PRE_ISSUE, |event| async move {
        ReactorDecision::mutate([("ext.cost_center", "42")])
    })
    .bind(events::LOGIN_POST_AUTH, |event| async move {
        ReactorDecision::deny("embargoed region")
    })
    .build()?;   // every rejected binding at once, not one per run

reactor_serve(config, handler).await`},{label:`TypeScript`,code:`import { REACTOR_EVENTS, reactorHandlers, reactorServe } from 'axiam-sdk/amqp';

await reactorServe(
  options,
  reactorHandlers({
    [REACTOR_EVENTS.TOKEN_PRE_ISSUE]: (event) => mutate({ 'ext.cost_center': '42' }),
    [REACTOR_EVENTS.LOGIN_POST_AUTH]: async (event) => deny('embargoed region'),
  }),
);`},{label:`Python`,code:`from axiam_sdk.amqp import LOGIN_POST_AUTH, TOKEN_PRE_ISSUE, ReactorRouter, reactor_serve

router = ReactorRouter()

@router.on(TOKEN_PRE_ISSUE)
def enrich_token(event):            # sync or async, both work
    return mutate({"ext.cost_center": "42"})

@router.on(LOGIN_POST_AUTH)
async def screen_login(event):
    return deny("embargoed region") if await embargoed(event) else allow()

await reactor_serve(dialer, config, router.handler())`},{label:`Go`,code:`handler, err := amqp.NewReactorMux().
    On(amqp.ReactorEventTokenPreIssue, enrichToken).
    On(amqp.ReactorEventLoginPostAuth, screenLogin).
    Handler()
if err != nil {
    return err // every rejected binding at once, not one per run
}
err = amqp.ReactorServe(ctx, dialer, cfg, handler)`},{label:`Swift`,code:`// The §8b guard is a public, tested function — call it before
// anything opens a socket.
let endpoint = try amqpsEndpoint(brokerURL, caPEM: caPEM)

var router = ReactorRouter()
try router.on(.loginPostAuth) { event in
    let payload = try event.decodePayload(LoginPayload.self)
    return suspicious(payload) ? .allowWithStepUp : .allow
}

let config = ReactorConfig(tenantID: tenantID, reactorID: reactorID, signingKey: subkey)
try await reactorServe(config: config, transport: yourTransport, handler: router.handler())`}]},{type:`p`,text:`The eight managed runtimes — Rust, TypeScript, Python, Java, Kotlin, C#, PHP and Go — bundle the AMQP client and connect for you. **Swift, C and C++ ship the protocol core over a transport you supply**: the same verification, canonical signing, allow-lists and binder, with no vendored broker client, because there is no maintained AMQP client for those targets this project is willing to put onto embedded and mobile deployments. Their transport interface has exactly two capabilities — take the next delivery, publish a reply to a named destination — and deliberately no declare, bind or queue-name derivation, since a reactor that can bind can bind itself to another tenant's issuance events.`},{type:`note`,text:"Because that runtime never sees a broker URL, each of the three exposes the transport guard — `amqps://` only, no loopback exception, no plaintext fallback, no verification-skip switch — as a public, tested function (`amqpsEndpoint`, `axiam_amqps_endpoint`, `axiam::amqps_endpoint`) and calls it in its own example transport. A requirement that reads as enforced and is not is the failure mode that rule exists to stop."},{type:`h`,id:`registering`,text:`Registering one`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/reactors`,summary:`List registered Reactors, with health counters.`},{method:`POST`,path:`/api/v1/reactors`,summary:"Register one. `400` on an unknown event or an out-of-range timeout."},{method:`GET`,path:`/api/v1/reactors/{id}`,summary:"Read one, including `last_seen_at` and the 24-hour timeout and veto counts."},{method:`PUT`,path:`/api/v1/reactors/{id}`,summary:`Update it.`},{method:`DELETE`,path:`/api/v1/reactors/{id}`,summary:`Remove it — never refused, so a bad registration can always be undone.`},{method:`GET`,path:`/api/v1/reactors/events`,summary:`The event registry, verbatim — the live copy of the table above.`}]},{type:`code`,caption:`POST /api/v1/reactors`,code:`{
  "name": "fraud-screen",
  "description": "Denies logins from embargoed regions",
  "events": ["login.post_auth"],
  "mode": "intercept",
  "priority": 10,
  "timeout_ms": 500,
  "enabled": true
}`},{type:`p`,text:"Omit `timeout_ms` to take the 500 ms default, and omit `failure_policy` to take the strictest default among the events named. Every endpoint is permission-gated (`reactors:list`, `reactors:create`, `reactors:get`, `reactors:update`, `reactors:delete`), and the server declares the exchange, the queue and the bindings — an actor consumes, and never declares topology of its own."},{type:`note`,text:`The wire protocol — message shape, signing, the reply schema and the timeout semantics — is normative in [CONTRACT.md §22](https://github.com/ilpanich/axiam/blob/main/sdks/CONTRACT.md). The admin console's Reactors page is the same surface with a form on top.`}]},{slug:`errors`,section:`APIs & integration`,navLabel:`Error reference`,title:`Error reference`,intro:`Three error types across every SDK and every language, one mapping from HTTP and gRPC status, and the handful of rules that keep a client from retrying something that cannot succeed.`,blocks:[{type:`h`,id:`types`,text:`The three types`},{type:`p`,text:`Every SDK exposes exactly three error types. Languages add idiomatic sub-types, but never replace these three — so an integration ported between languages keeps the same control flow.`},{type:`table`,proseFirstCol:!0,headers:[`Type`,`Meaning`],rows:[[`AuthError`,`Authentication failure: wrong credentials, expired session, failed MFA, a 401 on refresh.`],[`AuthzError`,`Authorization failure: the caller lacks permission for the requested operation.`],[`NetworkError`,`Transport-level failure: connection refused, timeout, TLS error, DNS failure.`],[`OAuthProtocolError`,"A **sub-type of `AuthError`**. An RFC 6749 protocol error from an `/oauth2/*` endpoint, carrying `error` and `error_description` as accessible fields."]]},{type:`h`,id:`http`,text:`HTTP status mapping`},{type:`table`,headers:[`Status`,`Type`,`Notes`],rows:[[`400`,`NetworkError`,`Malformed request — an SDK or caller programming error.`],[`any status from /oauth2/* with an OAuth2 error body`,`OAuthProtocolError`,"**Dispatch on the `error` field, not on the status.** This row wins over the status rows below."],[`401`,`AuthError`,`Unauthenticated. Triggers single-flight refresh when tokens are present.`],[`403`,`AuthzError`,`Authenticated but not authorized.`],[`408, 429`,`NetworkError`,`Timeout, or rate-limited.`],[`409`,`AuthzError`,`Conflict — resource-level access denied.`],[`5xx`,`NetworkError`,`Server error. An SDK must **not** retry authentication.`],[`connection / DNS / TLS failure`,`NetworkError`,`Carries the underlying transport error as its cause.`]]},{type:`warn`,text:"The `/oauth2/*` row is scoped to those paths only. An ordinary REST `403` — including from `/api/v1/authz/check` and from the UMA Protection API at `/uma2/*` — is an `AuthzError`, not a protocol error. The distinction matters: one is *you may not*, the other is *your client credentials or grant request were wrong*."},{type:`h`,id:`grpc`,text:`gRPC status mapping`},{type:`table`,headers:[`gRPC status`,`Type`,`Notes`],rows:[[`UNAUTHENTICATED (16)`,`AuthError`,`Triggers single-flight refresh.`],[`PERMISSION_DENIED (7)`,`AuthzError`,`Caller lacks the required permission.`],[`UNAVAILABLE (14)`,`NetworkError`,`Server unreachable.`],[`DEADLINE_EXCEEDED (4)`,`NetworkError`,`Request timed out.`],[`INTERNAL (13)`,`NetworkError`,`Server-side error.`],[`RESOURCE_EXHAUSTED (8)`,`NetworkError`,`Rate-limited.`]]},{type:`h`,id:`rules`,text:`Rules that prevent bad retries`},{type:`list`,items:[`**A 401 carrying an OAuth2 protocol error does not enter the refresh guard.** A client-authentication failure is not a session expiry, and retrying cannot fix a wrong client secret.`,`**Concurrent 401s collapse into one refresh.** The single-flight guard means N in-flight requests produce one refresh attempt, not N.`,`**Errors never contain token strings** — not in messages, not in context fields, not in stack traces.`,"**`AuthzError` carries the denied action and resource** where the response body provides them, so a log line says what was refused rather than only that something was."]},{type:`h`,id:`device`,text:`Device-grant answers`},{type:`p`,text:"The device grant's four answers are protocol errors with specific reactions rather than failures — see [Device authorization grant](#/docs/device-flow) for the table. `authorization_pending` and `slow_down` are normal, expected control flow."},{type:`note`,text:"The normative source for all of this is `sdks/CONTRACT.md` §2, which every SDK is conformance-tested against. If this page and that chapter ever disagree, the contract is authoritative."}]}],m=[{slug:`configuration`,section:`Operate`,navLabel:`Configuration`,title:`Configuration & environment variables`,intro:`Every setting on the AXIAM server image is an environment variable. This is the reference: what each variable means, its default, and an example value.`,verifiedRelease:r,blocks:[{type:`h`,id:`naming`,text:`The naming convention`},{type:`p`,text:"All configuration keys use a double underscore (`__`) after the `AXIAM` prefix — for example `AXIAM__DB__USERNAME`. The `__` separates both the prefix and the nested key levels (this is how the config layer distinguishes them). A single underscore (`AXIAM_DB__USERNAME`) is silently ignored and the in-code default wins, so double-check the doubling when a value doesn't take effect."},{type:`warn`,text:"Secrets (database password, JWT keys, the AES-256-GCM encryption keys, the peppers) must come from a secret manager or mounted secret — never bake real key material into an image, a compose file or git. Use a placeholder like `<set-in-secret-manager>` in any template, and never reuse a value across environments."},{type:`h`,id:`scope`,text:`What this page covers`},{type:`p`,text:`This is a **curated reference, not an exhaustive one.** It documents the settings a deployment normally has to make a decision about — connectivity, secrets, token and hashing policy, rate limits, caches and TLS. The keys it leaves out are transport and pool tuning, debug-build fallbacks, and switches for features that are not shipped yet: things with working defaults and no deployment-specific right answer.`},{type:`note`,text:"The complete list is the configuration structs themselves, and it is worth knowing that they are the authority in two ways. Many keys never appear as a literal string in the source: the config layer derives `AXIAM__SERVER__HOST` from the `host` field of the server config struct, so grepping for the variable name finds nothing while the variable works perfectly. If a setting you need is not on this page, look for the field rather than for the key."},{type:`note`,text:"That scope is enforced rather than asserted. `scripts/check-config-key-coverage.py` compares every key literally present in the server against this page, and fails on one that is neither documented here nor carrying a written exemption saying why a deployment never needs to set it — so the gap between the two can be argued with, but it cannot quietly grow."},{type:`h`,id:`connectivity`,text:`Connectivity & bind addresses`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__DB__URL`,`SurrealDB address as a bare host:port — not a URL scheme (the Ws engine resolves a scheme as a hostname and fails).`,`surrealdb:8000`],[`AXIAM__DB__NAMESPACE`,`SurrealDB namespace.`,`axiam`],[`AXIAM__DB__DATABASE`,`SurrealDB database.`,`axiam`],[`AXIAM__AMQP__URL`,`RabbitMQ connection string, assembled from the broker credentials at the deployment layer. Must be amqps:// — AMQP is TLS-only and every other scheme is refused at startup.`,`amqps://user:pass@rabbitmq:5671`],[`AXIAM__AMQP__TLS__CA_CERT_PATH`,`CA bundle for the broker's certificate. rustls compiles its roots in, so an in-cluster broker issued by a private CA is unverifiable without this — and that is the common case rather than the exception.`,`/etc/axiam/amqp/ca.pem`],[`AXIAM__AMQP__TLS__CLIENT_CERT_PATH`,`Client certificate chain for mutual TLS toward the broker. Optional.`,`/etc/axiam/amqp/client.pem`],[`AXIAM__AMQP__TLS__CLIENT_KEY_PATH`,`Its private key. Required together with the certificate — setting one without the other is refused at startup rather than connecting without the mutual half.`,`/etc/axiam/amqp/client.key`],[`AXIAM__SERVER__HOST`,`REST bind address (default 127.0.0.1). Set 0.0.0.0 in a container.`,`0.0.0.0`],[`AXIAM__SERVER__PORT`,`REST bind port (default 8090).`,`8090`],[`AXIAM__GRPC__HOST`,`gRPC bind address (default 127.0.0.1, loopback-only). Set 0.0.0.0 to serve in-cluster.`,`0.0.0.0`],[`AXIAM__GRPC__PORT`,`gRPC bind port (default 50051).`,`50051`],[`AXIAM__GRPC_TLS_CERT_PATH`,`PEM certificate for the gRPC listener. **Both this and the key must be set or gRPC serves plaintext** — acceptable in a mesh that provides its own transport security, and not otherwise. Note the single underscore after the prefix: these two are read directly rather than through the layered config.`,`/etc/axiam/grpc/server.pem`],[`AXIAM__GRPC_TLS_KEY_PATH`,`Its private key. Set together with the certificate; the server logs a warning at startup whenever TLS is off.`,`/etc/axiam/grpc/server.key`],[`AXIAM__GRPC__GRPC_AUTHZ_PER_SEC`,`Max gRPC authz requests per second per IP (default 100).`,`100`],[`AXIAM__SERVER__CORS_ALLOWED_ORIGINS`,`Allowed CORS origins; empty disables cross-origin requests (restrictive default).`,`https://admin.acme.dev`],[`RUST_LOG`,`Log verbosity / filter. Keep it narrow in production — no internal module exposure.`,`info`]]},{type:`h`,id:`secrets`,text:`Secrets & encryption keys`},{type:`p`,text:"These are required for a real deployment. Generate the 32-byte hex keys with `openssl rand -hex 32` and the Ed25519 JWT keypair with `openssl genpkey -algorithm ed25519`."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__DB__USERNAME`,`SurrealDB username.`,`axiam`],[`AXIAM__DB__PASSWORD`,`SurrealDB password.`,`<set-in-secret-manager>`],[`AXIAM__AUTH__JWT_PRIVATE_KEY_PEM`,`Ed25519 JWT signing private key (PEM).`,`-----BEGIN PRIVATE KEY----- …`],[`AXIAM__AUTH__JWT_PUBLIC_KEY_PEM`,`Ed25519 JWT verification public key (PEM), paired with the private key.`,`-----BEGIN PUBLIC KEY----- …`],[`AXIAM__AUTH__MFA_ENCRYPTION_KEY`,`AES-256-GCM key (32-byte hex) encrypting TOTP MFA secrets at rest.`,`<64 hex chars>`],[`AXIAM__PKI__ENCRYPTION_KEY`,`AES-256-GCM key (hex) encrypting CA signing keys (and webhook secrets) at rest.`,`<64 hex chars>`],[`AXIAM__AUTH__FEDERATION_ENCRYPTION_KEY`,`AES-256-GCM key (hex) encrypting SAML/OIDC federation client secrets at rest.`,`<64 hex chars>`],[`AXIAM__EMAIL_ENCRYPTION_KEY`,`AES-256-GCM key (hex) encrypting email/SMTP secrets; also gates the email-config admin endpoints.`,`<64 hex chars>`],[`AXIAM__GDPR_PSEUDONYM_PEPPER`,`HMAC-SHA256 pepper (hex) pseudonymizing audit-log actor identities on GDPR erasure.`,`<64 hex chars>`],[`AXIAM__AUTH__PEPPER`,`Password pepper (string) prepended before Argon2id hashing.`,`<random string>`]]},{type:`h`,id:`provider`,text:`Where secrets come from`},{type:`p`,text:"The keys above do not have to be environment variables. `AXIAM__AUTH__SECRET_PROVIDER` selects where the server fetches them at startup, and the field names it looks for are the lowercase forms — `jwt_private_key_pem`, `opaque_setup_key`, `pki_encryption_key`, `auth_pepper` and the rest. See [Secrets & HashiCorp Vault](#/docs/secrets) for the full story."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTH__SECRET_PROVIDER`,"`env` (default), `file`, or `vault`. An unknown value is **refused at startup** rather than falling back — a typo that silently reverted to `env` presents as an unexplained outage.",`vault`],[`AXIAM__AUTH__SECRET_DIR`,"Required with `file`. Directory of one file per secret, named after the field — the shape a Kubernetes or Docker secret volume already has.",`/etc/axiam/secrets`],[`AXIAM__AUTH__VAULT_ADDR`,"Required with `vault`. Base address of the Vault server. Must be TLS in production — the token is a bearer credential for the setup key.",`https://vault.internal:8200`],[`AXIAM__AUTH__VAULT_TOKEN`,"Required with `vault`. A token with read access to the mount and path below, and nothing else.",`<set-in-secret-manager>`],[`AXIAM__AUTH__VAULT_MOUNT`,"KV v2 mount point (default `secret`).",`secret`],[`AXIAM__AUTH__VAULT_PATH`,"Path within the mount holding AXIAM's keys as fields (default `axiam`).",`axiam`],[`AXIAM__AUTH__VAULT_CA_CERT_PATH`,`Trust anchor for a Vault fronted by a private CA. rustls compiles its roots in, so a Vault certificate issued by an internal PKI — cert-manager, or the repository's own dev certificates — is otherwise unverifiable and the server fails at startup with a bare transport error.`,`/etc/axiam/vault/ca.pem`]]},{type:`h`,id:`opaque-keys`,text:`OPAQUE`},{type:`p`,text:"Both are required whenever any organization or tenant has `opaque_mode` other than `disabled`. They are split by what rotating them costs — see [OPAQUE](#/docs/opaque)."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTH__OPAQUE_SETUP_KEY`,`AES-256-GCM key (32-byte hex) encrypting each tenant's OPRF seed and AKE keypair at rest. **Back this up** — losing it means a password reset for every user in every tenant.`,`<64 hex chars>`],[`AXIAM__AUTH__OPAQUE_SESSION_KEY`,`AES-256-GCM key (32-byte hex) sealing in-flight exchange state (a 120-second window). Cheap to rotate; deliberately separate from the setup key.`,`<64 hex chars>`]]},{type:`h`,id:`oauth2`,text:`OAuth2 & OIDC`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTH__OAUTH2_ISSUER_URL`,`Public issuer URL for OIDC discovery. Must be an origin, not a path (path-based issuers are rejected).`,`https://iam.acme.dev`],[`AXIAM__OAUTH2__JWKS_CACHE_MAX_AGE_SECS`,`Cache-Control max-age on the JWKS endpoint, in seconds.`,`300`],[`AXIAM__AUTH__ALLOW_MISSING_AUD_AS_USER`,`Compatibility switch — treat a token with no audience claim as a user token. Leave off unless you need it.`,`false`]]},{type:`h`,id:`hashing`,text:`Argon2id hash concurrency`},{type:`p`,text:`Each in-flight Argon2id operation allocates a ~19 MiB arena, so unbounded concurrency is a memory-DoS vector. A process-wide semaphore caps peak concurrent arenas and sheds excess load with a 503 rather than queueing unboundedly. The cost parameters themselves are never weakened for throughput.`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTH__MAX_CONCURRENT_HASHES`,`Max concurrent Argon2id hash/verify ops. 0 (default) = auto → min(CPU cores, 4). Peak crypto RSS ≈ this × 19 MiB.`,`0`],[`AXIAM__AUTH__HASH_ACQUIRE_TIMEOUT_SECS`,`Seconds a request waits for a hash permit before returning a 503 backpressure error (default 5).`,`5`]]},{type:`h`,id:`authz-cache`,text:`Authorization decision cache (optional)`},{type:`p`,text:`An optional per-tenant cache that skips the SurrealDB round-trips per check. Off by default; enabling it changes performance only, never the decision returned. Every access-narrowing mutation invalidates the affected entries immediately, so no revocation can leave a stale allow — the TTL is only a bounded-staleness backstop.`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__AUTHZ__DECISION_CACHE_ENABLED`,`Master switch (default false).`,`false`],[`AXIAM__AUTHZ__DECISION_CACHE_TTL_SECS`,`Cached-decision TTL, and the upper bound on revocation latency if an invalidation is ever missed (default 5).`,`5`],[`AXIAM__AUTHZ__DECISION_CACHE_MAX_ENTRIES`,`Max cached decisions per tenant before FIFO eviction (default 10000).`,`10000`]]},{type:`h`,id:`rate-limit`,text:`Rate limiting`},{type:`p`,text:"Every auth/OAuth2 endpoint is rate-limited per-key, per-minute. Defaults are shown; `/auth/login` always keys per-IP regardless of the key mode."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__RATE_LIMIT__PROFILE`,`Posture preset moving the machine-traffic family coherently: internet (default) | gateway | mesh. The human endpoints — login, register, password-reset, MFA — stay strict per-IP under every preset.`,`internet`],[`AXIAM__RATE_LIMIT__LOGIN_PER_MIN`,`Max /auth/login per minute per key.`,`10`],[`AXIAM__RATE_LIMIT__REGISTER_PER_MIN`,`Max register requests per minute.`,`5`],[`AXIAM__RATE_LIMIT__TOKEN_PER_MIN`,`Max /oauth2/token per minute.`,`120`],[`AXIAM__RATE_LIMIT__PASSWORD_RESET_PER_MIN`,`Max password-reset requests per minute.`,`3`],[`AXIAM__RATE_LIMIT__MFA_PER_MIN`,`Max MFA enroll/confirm/verify per minute.`,`5`],[`AXIAM__RATE_LIMIT__WEBAUTHN_PER_MIN`,"Max WebAuthn ceremony requests per minute, applied to each of the six `/auth/webauthn/*` routes independently.",`10`],[`AXIAM__RATE_LIMIT__INTROSPECT_PER_MIN`,`Max /oauth2/introspect per minute.`,`600`],[`AXIAM__RATE_LIMIT__REVOKE_PER_MIN`,`Max /oauth2/revoke per minute.`,`60`],[`AXIAM__RATE_LIMIT__TOKEN_EXCHANGE_PER_MIN`,`Max RFC 8693 token exchanges per minute.`,`120`],[`AXIAM__RATE_LIMIT__PAR_PER_MIN`,`Max /oauth2/par per minute.`,`120`],[`AXIAM__RATE_LIMIT__END_SESSION_PER_MIN`,`Max /oauth2/end_session per minute. Never moved by a profile preset.`,`30`],[`AXIAM__RATE_LIMIT__DEVICE_AUTHORIZATION_PER_MIN`,`Max device-grant starts per minute. Sized as a state-allocation guard rather than from capacity — each call reserves a user code — and never moved by a preset.`,`12`],[`AXIAM__RATE_LIMIT__DEVICE_VERIFY_PER_MIN`,`Max device-code verifications per minute. Human-driven, so sized for a person at a keyboard; never moved by a preset.`,`10`],[`AXIAM__RATE_LIMIT__UMA_PERM_PER_MIN`,`Max UMA permission-ticket requests per minute.`,`120`],[`AXIAM__RATE_LIMIT__UMA_TICKET_PER_MIN`,`Max UMA ticket redemptions per minute.`,`120`],[`AXIAM__RATE_LIMIT__AUTHZ_CHECK_PER_MIN`,`Max authz-check requests per minute.`,`1800`],[`AXIAM__RATE_LIMIT__TRUSTED_HOPS`,`Trusted reverse-proxy hops to skip from the right of X-Forwarded-For (set 1 behind a single ingress).`,`0`],[`AXIAM__RATE_LIMIT__KEY`,`Bucket-key mode for token/introspect/revoke: ip | client_id | ip_client_id.`,`ip`],[`AXIAM__RATE_LIMIT__SHARED`,`Enables (on) or disables (off) the cross-replica write-behind shared counter. off is a single-replica escape hatch — the per-replica in-memory governor becomes the sole limiter.`,`on`],[`AXIAM__RATE_LIMIT__SHARED_SYNC_MS`,`Write-behind flush interval (ms) for the shared counter, clamped 50-60000. Scales the cross-replica overshoot bound.`,`1000`]]},{type:`note`,text:`The shared counter behind AXIAM__RATE_LIMIT__SHARED no longer performs a synchronous datastore write on the request path (write-behind design): it decides in-memory and flushes one coalesced write per bucket per AXIAM__RATE_LIMIT__SHARED_SYNC_MS. Cross-replica enforcement is therefore eventual, not synchronous — worst-case overshoot beyond the configured limit is bounded by roughly (replicas - 1) x arrival_rate_per_replica x sync_interval, and is zero on a single replica. The per-replica in-memory governor is unchanged and still caps overshoot independently. See the Deployment Guide's rate-limiting section for the full bound, the store-outage semantics (a store outage is now detected by the background flusher rather than the request, so limit=0 plus an unreachable store denies rather than allows), and the observability log lines.`},{type:`h`,id:`sizing`,text:`Suggested settings by deployment (benchmark-derived)`},{type:`p`,text:"The shipped defaults are tuned for one thing: blunting single-source abuse on a small internet-facing deployment. The benchmark's production-posture run showed exactly that — and also that those defaults are far too strict for machine-to-machine topologies, where many clients share one source IP behind a NAT or gateway. The recommendations below derive directly from the run-4 measurements (median-of-3, 2-CPU-per-container envelope) in `benchmarks/PUBLIC_BENCH_ANALYSIS.md` §7."},{type:`warn`,text:`These recommendations come from the current (still temporary) benchmark results on laptop-class hardware. They are good starting points, not guarantees — validate against your own traffic, and re-check this section as the benchmark improves.`},{type:`h`,id:`sizing-throughput`,text:`What a given envelope sustains`},{type:`p`,text:`Measured per-path ceilings, useful for sizing both hardware and limits. Post rate-limit fix, database CPU is the main ceiling: authorization checks, introspection, token issuance and userinfo all gain 42–90% from a second pair of DB cores. Logins scale with server CPU at fixed Argon2id cost, and JWKS is limited by the load generator rather than by the server.`},{type:`table`,headers:[`Envelope (server / DB)`,`Token issuance`,`Introspection`,`Authz checks REST / gRPC`,`Userinfo REST / gRPC`,`Logins`],rows:[[`2 cores / 2 cores`,`~2,700/s`,`~4,400/s`,`~750 / ~890/s`,`~4,500 / ~12,700/s`,`~69/s`],[`2 cores / 4 cores`,`~4,500/s`,`~6,200/s`,`~1,430 / ~1,680/s`,`~7,200/s (server-bound) / ~12,700/s`,`~69/s`]]},{type:`p`,text:`The optional decision cache sits on top of these: at a ~100% hit rate it lifts gRPC checks to ~11,600/s, but REST checks gain only ~5% because their per-request session-cookie validation is a database read the cache does not cover. Size for the cache-off numbers and treat the cache as headroom.`},{type:`h`,id:`sizing-knobs`,text:`Recommended values per scenario`},{type:`table`,headers:[`Variable`,`Default`,`Small internet-facing`,`M2M / microservices / IoT`,`Large multi-tenant`],rows:[[`AXIAM__RATE_LIMIT__PROFILE`,`internet`,`internet`,`gateway — one variable moves the whole machine-traffic family coherently (key mode + token/introspect/revoke/authz), leaving the human endpoints untouched`,`mesh`],[`AXIAM__RATE_LIMIT__KEY`,`ip`,`ip`,`client_id (or ip_client_id) — per-IP buckets collide behind NAT/gateways — see the security caveat below`,`ip_client_id`],[`AXIAM__RATE_LIMIT__TOKEN_PER_MIN`,`120`,`keep 120`,`per-client peak RPS × 60 × 2 (a 2-core server sustains ~163k issuances/min total)`,`budget per tenant SLA`],[`AXIAM__RATE_LIMIT__INTROSPECT_PER_MIN`,`600`,`keep 600`,`10–20× your token limit (resource servers introspect per request)`,`same rule`],[`AXIAM__RATE_LIMIT__AUTHZ_CHECK_PER_MIN`,`1800`,`keep 1800`,`6,000–60,000 per client — checks are cheap reads; the pre-revision 300/min starved any real service`,`size to the cache-ON ceiling`],[`AXIAM__RATE_LIMIT__LOGIN_PER_MIN`,`10 (always per-IP)`,`keep 10`,`60+ if users arrive through a shared NAT/proxy`,`60+, front with a WAF`],[`AXIAM__AUTHZ__DECISION_CACHE_ENABLED`,`false`,`true`,`true if your checks go over gRPC — measured 13× there at a favourable keyspace, ~+32% at a realistic one; REST checks gain only ~5%`,`true`],[`AXIAM__AUTHZ__DECISION_CACHE_TTL_SECS`,`5`,`5`,`5 (raise only if a ≤TTL revocation delay is acceptable)`,`5`],[`AXIAM__AUTHZ__BATCH_STRATEGY`,`coalesced`,`default`,"default — `coalesced` measured 744 batch ops/s ≈ 3,721 checks/s, about 5× single checks; a full-matrix re-measurement is queued",`default`],[`AXIAM__DB__POOL_SIZE`,`1`,`1`,`1 — no measured benefit; leave it alone`,`1`],[`AXIAM__AUTH__MAX_CONCURRENT_HASHES`,`0 = auto (min(cores, 4))`,`auto`,`auto`,`≈ physical cores reserved for auth; each concurrent hash holds a ~19 MiB arena`],[`Database CPU allocation`,`—`,`DB ≥ server cores`,`DB ≥ server cores if authz/userinfo-heavy (those paths scale with DB CPU; tokens don't)`,`2× server cores for read-heavy workloads`]]},{type:`note`,text:"`AXIAM__DB__POOL_SIZE` is a connection-pool sizing knob only; do not raise it expecting a throughput win. A pre-1.0 benchmark pass reported a one-off +7% on token issuance at `pool_size=4`, but that comparison was never confirmed on a settled measurement, and follow-up testing (`claude_dev/db-pool-design.md` §11) found no throughput difference between `pool_size=1` and `pool_size=8` under load. Leave it at the default `1` unless you have your own measured evidence for your deployment; `pool_size>1` still gives you independent per-connection session renewal, which is a robustness property worth having on its own, just not a speed one."},{type:`warn`,text:"Security caveat on `client_id` keying — read before changing the key mode. The `client_id` a request is bucketed by is read from the request body before the client credential is verified (that is what the OAuth2 spec puts there), so an attacker who simply varies the `client_id` string gets a fresh bucket each time and is effectively unlimited on the token, revoke and introspect endpoints. `client_id` keying is a fairness control between well-behaved clients, not an abuse control. `ip` is the only mode whose key an attacker cannot mint at will, which is why it stays the shipped default. Use `client_id`/`ip_client_id` only where something else already authenticates callers at the edge — mTLS, an API gateway, a WAF, or an IP allow-list — which is exactly the topology the M2M column assumes."},{type:`note`,text:`Two rules of thumb the data supports: if your traffic is authorization-check-heavy, spend hardware on the database first (those paths gained ~90% from a second pair of DB cores), then try the decision cache and measure its logged hit rate — it is transformative on gRPC checks and marginal on REST ones; if it's token-heavy, the limits — not the hardware — are what you'll hit first, so raise TOKEN_PER_MIN from your real per-client peak and — if and only if you have edge authentication per the caveat above — switch the key mode. The genuinely internet-exposed endpoints (login, register, password-reset, MFA) stay strict per-IP in every configuration, including under the gateway and mesh profiles.`},{type:`h`,id:`mds`,text:`WebAuthn attestation metadata (FIDO MDS3)`},{type:`p`,text:"Attestation policy checks an authenticator model against the FIDO Alliance's metadata service. Ingestion is **off by default** — `false` means zero outbound calls, no background job, and an admin-triggered refresh that refuses to run — so a deployment that does not verify attestation carries no dependency on the FIDO Alliance at all."},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__PKI__MDS_ENABLED`,"Master switch. Default `false`.",`true`],[`AXIAM__PKI__MDS_BLOB_URL`,"Fetch source (default `https://mds3.fidoalliance.org/`).",`https://mds3.fidoalliance.org/`],[`AXIAM__PKI__MDS_BLOB_PATH`,`Local BLOB file for an air-gapped deployment. When set it wins over the URL and no network fetch happens at all.`,`/etc/axiam/mds/blob.jwt`],[`AXIAM__PKI__MDS_REFRESH_INTERVAL_SECS`,"Background refresh interval; default 604800 (weekly). `0` disables the background job while leaving the admin-triggered refresh working.",`604800`],[`AXIAM__PKI__MDS_LEAF_DNS`,"Expected DNS SAN on the BLOB's leaf signing certificate (default `mds.fidoalliance.org`). Configurable so a legitimate FIDO hostname change is an operations action rather than a code release.",`mds.fidoalliance.org`],[`AXIAM__PKI__MDS_MAX_STALE_DAYS`,"Refuse **attested** registration once the ingested BLOB is this many days past its own `nextUpdate`. Default `0`, which disables the check.",`30`]]},{type:`note`,text:`The staleness bound is opt-in rather than defaulted, and that is a deliberate trade rather than an oversight. Ingestion never hard-fails on staleness — a transient outage at the FIDO Alliance must not brick WebAuthn registration for everyone — and the cost of that choice is a window in which an authenticator model revoked since the last successful refresh still passes policy, because the revocation is in a BLOB this deployment has not seen. The right bound is a property of the deployment: a high-assurance tenant may want days, while an air-gapped one with no automatic refresh path would be taken offline by anything short of months. Silently inventing a number would pick for you.`},{type:`p`,text:`The bound applies to attestation only. A ceremony that requests no attestation consults no metadata, so stale metadata cannot have misled it.`},{type:`h`,id:`tls`,text:`Direct TLS termination (opt-in)`},{type:`p`,text:`By default the server binds plaintext and a proxy/ingress terminates TLS 1.3 in front of it. To terminate TLS inside the server process instead, set the following — the listener then binds with rustls restricted to TLS 1.3 only. When enabled, both paths are mandatory and the server fails fast at startup on a missing, unreadable or mismatched cert/key (it never falls back to plaintext).`},{type:`table`,headers:[`Variable`,`Meaning`,`Example`],rows:[[`AXIAM__SERVER__TLS__ENABLED`,`Enable in-process TLS 1.3 (default false).`,`true`],[`AXIAM__SERVER__TLS__CERT_PATH`,`Path to the PEM certificate chain (leaf first).`,`/etc/axiam/tls/tls.crt`],[`AXIAM__SERVER__TLS__KEY_PATH`,`Path to the PEM private key (PKCS#8, PKCS#1 or SEC1).`,`/etc/axiam/tls/tls.key`]]},{type:`note`,text:"The AMQP URL is assembled from the broker's own `RABBITMQ_DEFAULT_USER` / `RABBITMQ_DEFAULT_PASS` into `AXIAM__AMQP__URL` at the deployment layer. See Docker & Kubernetes for how the shipped compose file and manifests wire these together."}]}],h=[{slug:`deploy`,section:`Operate`,navLabel:`Docker & Kubernetes`,title:`Docker & Kubernetes`,intro:`Package AXIAM as a container and run it on Kubernetes with the shipped manifests.`,verifiedRelease:r,blocks:[{type:`h`,id:`docker`,text:`Docker`},{type:`p`,text:"`docker/` holds the Dockerfiles and the compose configurations. `docker-compose.dev.yml` runs only SurrealDB and RabbitMQ, for developing against a natively-run server. `docker-compose.prod.yml` runs the whole stack — server, admin console, database, broker and Vault — and is documented in the file itself as **not** intended for real production use."},{type:`code`,code:`just dev-up      # SurrealDB + RabbitMQ only
just prod-up     # the whole stack, including the Vault ceremony
just prod-down   # stop, keep volumes
just prod-clean  # stop and remove volumes`},{type:`p`,text:"`docker-compose.prod.yml` refuses to start without the database and broker credentials and the JWT keypair present in the environment — it uses Compose's fail-fast variable syntax rather than silently defaulting. Secret material lives in a gitignored `docker/.secrets/` directory, never in the compose file."},{type:`h`,id:`k8s`,text:`Kubernetes`},{type:`p`,text:"The manifests under `k8s/` are assembled by a kustomization and deploy `axiam-server`, the admin console, SurrealDB, RabbitMQ and Vault, together with the NetworkPolicies that keep them talking only to each other."},{type:`code`,caption:`apply`,code:`kubectl apply -k k8s/
kubectl -n axiam get pods
kubectl -n axiam logs deploy/axiam-server`},{type:`h`,id:`ports`,text:`What is exposed, and what is not`},{type:`table`,proseFirstCol:!0,headers:[`Port`,`Service`,`Exposure`],rows:[[`8090`,`REST API`,`Through the Ingress, TLS-terminated.`],[`8081`,`Admin console`,`Through the Ingress, TLS-terminated.`],[`50051`,`gRPC`,`**Deliberately not routed through the Ingress.** Reachable only via the in-cluster ClusterIP service.`],[`5671`,`AMQP`,"In-cluster only, TLS-only (`amqps://`)."],[`8200`,`Vault`,`In-cluster only, TLS.`]]},{type:`h`,id:`probes`,text:`Probes`},{type:`p`,text:"Point the liveness probe at `/health` and the readiness probe at `/ready`. The distinction matters: `/health` answers 200 whenever the process is up, while `/ready` checks database connectivity and answers 503 when it cannot reach SurrealDB — so a database blip takes replicas out of the load-balancer rotation instead of restarting them in a loop."},{type:`h`,id:`scaling`,text:`Scaling`},{type:`p`,text:"`axiam-server` is stateless — every piece of durable state is in SurrealDB — so replicas scale horizontally behind an ordinary load balancer with no session affinity required."},{type:`warn`,text:"One caveat when scaling out: the cross-replica shared rate-limit counter is **write-behind**, so enforcement across replicas is eventual rather than synchronous. Worst-case overshoot beyond a configured limit is roughly `(replicas − 1) × arrival_rate_per_replica × sync_interval`, and is zero on a single replica. The per-replica in-memory governor still caps overshoot independently."},{type:`h`,id:`tls`,text:`TLS`},{type:`p`,text:"By default the server binds plaintext and expects an ingress or proxy to terminate TLS 1.3 in front of it. To terminate inside the process instead, set `AXIAM__SERVER__TLS__ENABLED` with a certificate and key path; the listener then binds with rustls restricted to TLS 1.3 only, and fails fast at startup on a missing, unreadable or mismatched pair rather than falling back to plaintext."},{type:`h`,id:`sizing-guides`,text:`Sizing, measured`},{type:`p`,text:`Two deployment guides in the repository answer the questions this page raises and does not settle — both are derived from benchmark runs and query plans rather than from estimates.`},{type:`links`,links:[{label:`Sizing your rate limits`,href:`https://github.com/ilpanich/axiam/blob/main/docs/deployment/rate-limit-sizing.md`,note:`Which of the three postures fits your topology, what the presets are worth, and the measured envelope behind them.`},{label:`The authorization read path`,href:`https://github.com/ilpanich/axiam/blob/main/docs/deployment/authz-read-path.md`,note:`What one uncached check costs in database round-trips, and which cache removes which one.`}]},{type:`table`,proseFirstCol:!0,headers:[`You are`,`Rate-limit posture`,`Why`],rows:[[`A small internet-facing deployment where humans log in from many addresses`,`the shipped defaults`,`Sized to stop single-source credential stuffing and token probing.`],[`A machine-to-machine, microservice or IoT fleet arriving through a shared NAT or egress gateway`,"`gateway`",`Many distinct OAuth2 clients share one source address, so per-IP buckets collide and the fleet throttles itself.`],[`Reachable only on a private network or service mesh`,"`mesh`",`Ceilings become runaway-loop guards; abuse control belongs at the network edge.`]]},{type:`note`,text:`Presets are opt-in — setting no profile keeps the shipped defaults byte for byte — and **no profile ever changes the human endpoints**. Login, registration, password reset and MFA stay strict and per-IP in every posture, because a service-mesh capacity decision must not silently widen the surface an attacker actually attacks.`},{type:`p`,text:`On the read path, the thing worth knowing before tuning: an uncached authorization check is a short sequence of *sequential* database round-trips, and the REST surface carries one the gRPC surface does not — a session-revocation lookup, because the gRPC interceptor validates the token signature and stops. Enabling only the decision cache therefore helps gRPC considerably more than REST; the session-validation cache is what closes the gap.`},{type:`warn`,text:`Both caches are process-local by default, and both carry the same multi-replica caveat: a revocation handled by one replica is not seen by the others until their entries expire. Enable them at the same TTL — a deployment that has accepted a five-second decision-staleness window gains nothing from a zero-second session-staleness window, and the reverse leaves the looser of the two setting your actual revocation latency.`},{type:`warn`,text:`AXIAM is pre-1.0. Treat these manifests as a solid starting point for a staging environment, and work through [Production hardening](#/docs/hardening) before anything real depends on them.`}]},{slug:`secrets`,section:`Operate`,navLabel:`Secrets & Vault`,title:`Secrets & HashiCorp Vault`,intro:`AXIAM holds ten long-lived secrets. Two of them are the difference between "an attacker read your database" and "an attacker owns your identity provider".`,blocks:[{type:`h`,id:`what`,text:`The ten secrets`},{type:`table`,headers:[`Field`,`Shape`,`What losing it costs`],rows:[[`opaque_setup_key`,`32-byte hex`,`**A password reset for every user in every tenant.** It encrypts each tenant's OPRF seed; without it no OPAQUE record can be opened.`],[`jwt_private_key_pem`,`Ed25519 PEM`,`**Total compromise.** Whoever holds it can mint a token for any principal in any tenant.`],[`pki_encryption_key`,`32-byte hex`,`**CA compromise.** It encrypts CA private keys at rest; a leak lets an attacker issue certificates every mTLS client trusts.`],[`auth_pepper`,`text`,`Every stored password hash is invalidated if it *changes*; a leak makes offline attack on stolen hashes cheaper.`],[`opaque_session_key`,`32-byte hex`,`In-flight OPAQUE exchanges (120 s) are invalidated. Cheap to rotate — deliberately separate from the setup key.`],[`mfa_encryption_key`,`32-byte hex`,`Stored TOTP secrets become undecryptable.`],[`federation_encryption_key`,`32-byte hex`,`Stored IdP client secrets become undecryptable.`],[`email_encryption_key`,`32-byte hex`,`Stored email addresses become undecryptable.`],[`gdpr_pseudonym_pepper`,`32-byte hex`,`Existing audit pseudonyms stop linking to new ones; the audit trail breaks.`],[`jwt_public_key_pem`,`Ed25519 PEM`,`Not secret, but a mismatched pair is a confusing outage.`]]},{type:`h`,id:`providers`,text:`Three providers`},{type:`p`,text:'`AXIAM__AUTH__SECRET_PROVIDER` selects where the server fetches these at startup. An unknown value is **refused at startup** rather than falling back to `env`, because a typo that silently reverted a deployment to reading unset environment variables presents as "OPAQUE stopped working" with nothing in the logs explaining why.'},{type:`table`,proseFirstCol:!0,headers:[`Provider`,`Reads from`,`Use it for`],rows:[["`env` (default)","Environment variables — `AXIAM__AUTH__JWT_PRIVATE_KEY_PEM` and friends.","Local development. Fine for a workstation, poor for anything that has a `kubectl describe pod`."],["`file`","One file per secret in `AXIAM__AUTH__SECRET_DIR`, named after the field.",`Kubernetes and Docker secret volumes — the shape those already produce, with no Vault to run.`],["`vault`","A KV v2 secret at `<mount>/<path>` whose fields are named after the list above.",`Production. Rotation, audit, and a leak surface that is not "anything that can read the pod spec".`]]},{type:`note`,text:"AXIAM's actual requirement is small: a KV v2 secret whose fields carry those names. It does not care who runs Vault, whether it is HA, or how it is unsealed. If your organization already runs one, point `AXIAM__AUTH__VAULT_ADDR` at it, create the read-only policy, and skip the rest."},{type:`h`,id:`honesty`,text:`What Vault does and does not defend against`},{type:`warn`,text:"**Vault does not defend against a compromised application server.** It defends against the ways keys actually leak — a config file committed to git, an over-shared CI variable, a `kubectl describe pod`, a backup of an orchestrator's etcd. An attacker with code execution on the server can read whatever the server can read, and the server must be able to read these."},{type:`p`,text:"The construction that *would* defend against that is envelope encryption against a Transit-style API, where the plaintext key never leaves the KMS — at the cost of a network round trip per seal and open. The `SecretProvider` trait is the place to add it, and doing so requires no change to any authentication code."},{type:`h`,id:`local`,text:`Local development`},{type:`code`,code:`just dev-up       # SurrealDB + RabbitMQ. No Vault; provider stays \`env\`.
just vault-up     # adds a dev-mode Vault and seeds every secret into it
just vault-status # which secrets Vault holds — presence only, never values`},{type:`p`,text:`Dev-mode Vault is in-memory, starts unsealed, listens on plain HTTP and uses a fixed root token. Every one of those is wrong for production — which is exactly the point: it removes the unseal ceremony from an edit-compile-run loop.`},{type:`h`,id:`production`,text:`Production: what cannot be automated`},{type:`p`,text:"The manifests under `k8s/vault/` deploy a single-node Vault with file storage — production-*shaped*, not production-*ready*. Most teams should run HashiCorp's Helm chart with Raft and three to five replicas, or use an existing Vault. Either way these steps are what AXIAM needs, and none of them is safe to automate."},{type:`steps`,steps:[{title:`Give Vault's listener TLS`,body:`The token AXIAM presents on every fetch is a bearer credential for the setup key. Over plaintext, anything on the pod network can take it.`},{title:`Initialise — once, and never again`,body:`The output is printed once and cannot be recovered. It contains five unseal key shares and a root token. Give each share to a **different person** — three of five must cooperate to unseal, which is the whole point of the scheme. Do not put all five in one password manager, and do not store them with the root token.`,code:`kubectl exec -n axiam vault-0 -- vault operator init \\
    -key-shares=5 -key-threshold=3 -format=json`},{title:`Configure auto-unseal before you go live`,body:`Without it, every restart — a node drain, an upgrade, an OOM kill — leaves Vault sealed and AXIAM unable to start until three people are woken up. This is the single most important production step, and the one most often deferred.`},{title:`Create a read-only, single-path policy`,body:`A leaked AXIAM token must not be a key to the rest of your Vault.`,code:`vault policy write axiam - <<'EOF'
path "secret/data/axiam"     { capabilities = ["read"] }
path "secret/metadata/axiam" { capabilities = ["read"] }
EOF`},{title:`Seed the secrets`,body:`The seeding script **never regenerates a secret that already exists**, so re-running it after adding a key or after a restore is safe. That behaviour is unit-tested, precisely because getting it wrong would mean a password reset for every user.`,code:`scripts/vault-seed.sh`},{title:`Prefer Kubernetes auth over a static token`,body:`A static token works and is what the manifests ship with, because something has to bootstrap the trust chain. It does not rotate and it sits in etcd. With the Kubernetes auth method the Secret disappears and the pod authenticates with its ServiceAccount token, which Kubernetes rotates.`},{title:`Revoke the root token`,body:`Use it for the policy and seeding steps, then revoke it. Generate a new one with the root-generation ceremony on the rare occasions you need one.`}]},{type:`note`,text:`Only the *ciphertext* of the OPRF seeds lives in the database. Moving the seeds themselves to a file on the server volume would put them on the same side of the trust boundary as the key, break replicas, lose transactionality with the credential rows, and turn a lost volume into total tenant lockout.`}]},{slug:`settings`,section:`Operate`,navLabel:`Settings & policies`,title:`Settings & policies`,intro:`Password, lockout, MFA, token, certificate, email and OPAQUE policy — set as an organization baseline, and tightened per tenant.`,blocks:[{type:`h`,id:`shape`,text:`Baseline and overrides`},{type:`p`,text:"Policy is hierarchical. An **organization** holds the baseline; a **tenant** may override individual fields. An override of `null` means *inherit*, so a tenant only states what it wants to differ — and reading a tenant's effective settings returns every field fully resolved, with nothing left to work out by hand."},{type:`warn`,text:`Tenant overrides are **tighten-only**. A tenant can make its posture stricter than the organization baseline; it cannot make it looser. That is what keeps an organization-level security decision meaningful once tenants can edit their own configuration.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/organizations/{org_id}/settings`,summary:`Read the organization baseline.`},{method:`PUT`,path:`/api/v1/organizations/{org_id}/settings`,summary:`Set it.`},{method:`GET`,path:`/api/v1/settings`,summary:`Read the calling tenant's fully-resolved settings.`},{method:`PUT`,path:`/api/v1/settings`,summary:`Set the tenant's overrides.`}]},{type:`h`,id:`password`,text:`Password policy`},{type:`table`,headers:[`Field`,`Meaning`],rows:[[`min_length`,`Minimum password length.`],[`require_uppercase`,`Require at least one uppercase character.`],[`require_lowercase`,`Require at least one lowercase character.`],[`require_digits`,`Require at least one digit.`],[`require_symbols`,`Require at least one symbol.`],[`password_history_count`,`How many previous passwords a new one is checked against, so a rotation cannot cycle back.`],[`hibp_check_enabled`,`Reject passwords appearing in known-breach corpora. Worth more than any three complexity rules combined.`]]},{type:`note`,text:"If you are choosing where to spend policy strictness, spend it on `hibp_check_enabled` and length rather than on symbol classes. Composition rules push users toward predictable substitutions; breach-checking rejects the passwords attackers actually try."},{type:`h`,id:`lockout`,text:`Lockout policy`},{type:`table`,headers:[`Field`,`Meaning`],rows:[[`max_failed_login_attempts`,`Failures before the account locks.`],[`lockout_duration_secs`,`How long the first lock lasts.`],[`lockout_backoff_multiplier`,`Multiplier applied on each subsequent lock.`],[`max_lockout_duration_secs`,`Ceiling the backoff is clamped to.`]]},{type:`h`,id:`tokens`,text:`Token & MFA policy`},{type:`table`,headers:[`Field`,`Meaning`],rows:[[`access_token_lifetime_secs`,`How long an access token is valid. Shorter is better; 15 minutes is the default.`],[`refresh_token_lifetime_secs`,`How long a refresh token remains usable before re-authentication.`],[`mfa_enforced`,`Require a second factor for every user in scope.`],[`mfa_challenge_lifetime_secs`,`How long an issued MFA challenge stays answerable.`]]},{type:`h`,id:`other`,text:`Email, certificate & OPAQUE policy`},{type:`table`,headers:[`Field`,`Meaning`],rows:[[`email_verification_required`,`Whether an unverified address blocks sign-in.`],[`email_verification_grace_period_hours`,`How long an unverified account keeps working before it does.`],[`default_cert_validity_days`,`Validity applied to issued certificates when none is requested.`],[`max_cert_validity_days`,`Ceiling on requested certificate validity.`],[`opaque_mode`,"`disabled` | `optional` | `required` — see [OPAQUE](#/docs/opaque)."],[`opaque_suite`,"RFC 9807 ciphersuite. Default `ristretto255_sha512`."],[`opaque_ksf`,"Client key-stretching function. Default `argon2id`; `scrypt` is the alternative."]]},{type:`warn`,text:"`opaque_mode: required` is the one setting that can lock every user out of a tenant. Nobody can be enrolled retroactively, so it is only safe once every user already has a registration record. Run `optional` until enrolment is complete."}]},{slug:`pki`,section:`Operate`,navLabel:`PKI & certificates`,title:`PKI & certificates`,intro:`Per-tenant X.509 issuance under an organization CA, for users, services and IoT devices — plus the OpenPGP keys that sign the audit trail.`,verifiedRelease:r,blocks:[{type:`h`,id:`hierarchy`,text:`Certificate hierarchy`},{type:`p`,text:"An organization holds one or more **CA certificates**. Tenants issue leaf certificates under them, with RSA-4096 or Ed25519 keys. Private keys are **never stored server-side** — a leaf key is returned exactly once at issuance and cannot be recovered afterwards. CA signing keys are encrypted at rest with AES-256-GCM under `AXIAM__PKI__ENCRYPTION_KEY`."},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/organizations/{org_id}/ca-certificates`,summary:`List the organization's CAs.`},{method:`POST`,path:`/api/v1/organizations/{org_id}/ca-certificates`,summary:`Create one.`},{method:`POST`,path:`/api/v1/organizations/{org_id}/ca-certificates/{id}/revoke`,summary:`Revoke a CA.`},{method:`GET`,path:`/api/v1/certificates`,summary:`List issued leaf certificates in the tenant.`},{method:`POST`,path:`/api/v1/certificates`,summary:`Issue one. The private key is returned once.`},{method:`GET`,path:`/api/v1/certificates/{id}`,summary:`Read one.`},{method:`POST`,path:`/api/v1/certificates/{id}/revoke`,summary:`Revoke it.`}]},{type:`warn`,text:`Revoking a CA invalidates every certificate issued under it. That is the intended behaviour and it is not reversible — plan the blast radius before you do it, and prefer issuing under a fresh CA and migrating.`},{type:`h`,id:`mtls`,text:`mTLS for devices and services`},{type:`p`,text:`Certificate-based authentication gives machine identities the same tenant-scoped authorization as human users. A certificate is bound to a service account, and the TLS handshake is verified **in-process** — there is no proxy-asserted identity header in the trusted path.`},{type:`table`,headers:[`Variable`,`Values`,`Default`,`Meaning`],rows:[[`AXIAM__SERVER__TLS__CLIENT_AUTH`,`off | optional | required`,`off`,`Client-certificate policy.`],[`AXIAM__SERVER__TLS__CLIENT_CA_PATH`,`PEM bundle path`,`—`,`Trust anchors for client certificates.`]]},{type:`p`,text:"`optional` requests a certificate and verifies it if presented, while still accepting anonymous clients. `required` rejects the handshake outright without a verifying certificate. A misconfigured client-auth setup **fails fast at startup** rather than serving without verification."},{type:`h`,id:`iot`,text:`Enrolling an IoT device, end to end`},{type:`p`,text:`This is the shape AXIAM was built for on the device side: a device is issued a certificate at commissioning, presents it for mTLS, and is then authorized by the same RBAC engine as everything else. Four steps, and one of them is the one people expect to need and do not.`},{type:`steps`,steps:[{title:`Have an organization CA`,body:`CA certificates are organization-scoped and are the trust root every leaf in that organization chains to. The response carries the CA's signing private key **once** — AXIAM never persists the plaintext — so store it in your secret manager before you do anything else.`,code:`POST /api/v1/organizations/{org_id}/ca-certificates
{
  "subject": "CN=Acme Corp Root CA",
  "key_algorithm": "Ed25519",
  "validity_days": 3650
}`},{title:`Issue the device certificate`,body:"Leaf certificates are tenant-scoped. Set `cert_type` to `Device` — that is what makes the certificate addressable by fingerprint at authentication time. The private key comes back once and is never stored.",code:`POST /api/v1/certificates
{
  "issuer_ca_id": "<ca-certificate-uuid>",
  "subject": "CN=sensor-0421.acme.dev",
  "cert_type": "Device",
  "key_algorithm": "Ed25519",
  "validity_days": 365
}`},{title:`Commission the device with the key pair`,body:`Write the certificate and its private key into the device at manufacture or first provisioning. This is the only moment the private key exists outside the device, which is why the issuing response is the one call whose output you must capture.`},{title:`Let it connect over mTLS`,body:`The device presents its client certificate on the TLS handshake. Nothing further needs registering — the binding step that service accounts require does not apply here.`}]},{type:`warn`,text:"**Device certificates are not bound to anything.** A `Service` certificate must be attached to a service account explicitly, and it is natural to assume a device needs the same. It does not: on connection AXIAM computes the certificate's SHA-256 fingerprint, looks a `Device` certificate up directly by it, checks the certificate is active and unexpired, and **verifies the full chain** to the issuing organization's CA. Looking for a bind endpoint for a device is looking for something that does not exist."},{type:`note`,text:`A fingerprint match alone never authenticates a device. If the organization has no active CA certificate, the chain check has nothing to verify against and the attempt fails closed — which is the behaviour you want, and also the first thing to check when a correctly-commissioned device is refused.`},{type:`p`,text:"Two limits worth knowing before you commission a fleet. A tenant can cap certificate lifetime through its `max_certificate_validity_days` setting, and a request exceeding that cap is rejected rather than silently shortened. And where a device cannot show a browser to enrol a *user*, the [device authorization grant](#/docs/device-flow) covers the human-approval half — that is a different mechanism from this one, and the two are often confused."},{type:`h`,id:`gnupg`,text:`OpenPGP keys`},{type:`p`,text:`PGP keys managed through the PKI layer sign the audit trail and encrypt data exports, so exported data and audit records are verifiable and confidential end to end.`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/pgp-keys`,summary:`List the tenant's PGP keys.`},{method:`POST`,path:`/api/v1/pgp-keys`,summary:`Create one.`},{method:`POST`,path:`/api/v1/pgp-keys/{id}/encrypt`,summary:`Encrypt a payload to a key.`},{method:`POST`,path:`/api/v1/pgp-keys/{id}/revoke`,summary:`Revoke it.`},{method:`POST`,path:`/api/v1/pgp-keys/sign-audit-batch`,summary:`Sign a batch of audit records.`}]}]},{slug:`audit`,section:`Operate`,navLabel:`Audit logging`,title:`Audit logging`,intro:`An append-only, cryptographically signed record of every privileged action — tamper-evident rather than merely tamper-resistant.`,blocks:[{type:`h`,id:`appendonly`,text:`Append-only by design`},{type:`p`,text:`The audit log has no UPDATE and no DELETE path. Not "they are permission-guarded" — they do not exist. Records are chained and signed, so any attempt to alter or remove history is detectable after the fact rather than silent.`},{type:`p`,text:`Ingestion runs asynchronously over AMQP so that writing an audit record never slows down the operation being audited, and never fails it. Every authentication, authorization decision and administrative mutation across every tenant flows through the same pipeline.`},{type:`h`,id:`signing`,text:`Cryptographic signing`},{type:`p`,text:`Entries are signed with OpenPGP keys managed by the PKI layer, which gives you a trail verifiable independently of AXIAM itself. Combined with the chain between records, that is what makes the log tamper-*evident*: an attacker who can write to the database still cannot rewrite history without breaking a signature or a chain link.`},{type:`h`,id:`reading`,text:`Reading it`},{type:`api`,endpoints:[{method:`GET`,path:`/api/v1/audit-logs`,summary:`Query the tenant's audit trail.`},{method:`GET`,path:`/api/v1/audit-logs/system`,summary:`System-level audit events.`}]},{type:`note`,text:"Reading audit logs is itself permission-guarded (`audit_logs:*`) and itself audited. An investigator's access to the trail is part of the trail."},{type:`h`,id:`gdpr`,text:`Audit and the right to erasure`},{type:`p`,text:"An append-only log and a legal obligation to erase data are in obvious tension. AXIAM resolves it by **pseudonymising** the actor identity rather than deleting records: on erasure, the identity is replaced by an HMAC-SHA256 pseudonym derived under `AXIAM__GDPR_PSEUDONYM_PEPPER`. The trail's integrity survives; the link to a natural person does not."},{type:`warn`,text:"Changing `AXIAM__GDPR_PSEUDONYM_PEPPER` breaks the linkage between existing pseudonyms and new ones — the same person will appear as two different actors either side of the change. Treat it as permanent."},{type:`h`,id:`retention`,text:`Retention`},{type:`p`,text:`Because the log only grows, retention is an operational decision you have to make deliberately: size the database for the volume your compliance obligation requires, and archive to cold storage rather than expecting the system to prune for you.`}]},{slug:`observability`,section:`Operate`,navLabel:`Health & observability`,title:`Health & observability`,intro:`Probes, structured logs, telemetry hooks and the audit trail — what AXIAM tells you about itself, and what it deliberately does not.`,verifiedRelease:r,blocks:[{type:`h`,id:`probes`,text:`Probes`},{type:`api`,endpoints:[{method:`GET`,path:`/health`,summary:`Liveness. Always 200 while the process is up.`,public:!0},{method:`GET`,path:`/ready`,summary:`Readiness. 200 only when SurrealDB answers; 503 otherwise.`,public:!0}]},{type:`p`,text:"Wire these to the right probes and the difference matters operationally: a database blip should remove a replica from the load-balancer rotation, not restart it. Pointing liveness at `/ready` converts a transient dependency failure into a restart loop that makes the outage worse."},{type:`h`,id:`jobs`,text:`Scheduled-job liveness`},{type:`p`,text:`A background sweep that stops running is invisible. Nothing errors and nothing returns a 500 — the work simply stops happening, and for the two jobs where that matters most, GDPR erasure and certificate expiry, the first symptom can be a regulator's question months later. A failure was always logged, but a log line nobody greps for is not a control.`},{type:`api`,endpoints:[{method:`GET`,path:`/health/jobs`,summary:`Last-known outcome of every registered sweep. Always 200.`}]},{type:`code`,caption:`a degraded response`,code:`{
  "status": "degraded",
  "jobs": [
    {
      "name": "gdpr_purge",
      "last_success_at": "2026-08-21T02:00:04Z",
      "last_failure_at": "2026-08-23T02:00:01Z",
      "last_error": "database unreachable",
      "consecutive_failures": 3,
      "stalled": true
    },
    {
      "name": "audit_retention",
      "last_success_at": "2026-08-23T02:00:07Z",
      "last_failure_at": null,
      "last_error": null,
      "consecutive_failures": 0,
      "stalled": false
    }
  ]
}`},{type:`p`,text:"Six sweeps are registered: `saml_assertion_replay`, `federation_login_state`, `amqp_nonce_replay`, `gdpr_purge`, `gdpr_export` and `audit_retention`. Each appears in the snapshot from startup, before its first run — so a job that has never once succeeded is visible as such rather than simply absent."},{type:`table`,headers:[`Field`,`What it tells you`],rows:[["`stalled`",`The job has missed three consecutive expected runs. **This is the field to alert on.**`],["`consecutive_failures`",`Failures since the last success; reset to zero on success.`],["`last_error`",`The last error text, for whoever is now looking at this wondering what broke.`],["`last_success_at`","When it last completed cleanly. `null` means never."]]},{type:`note`,text:'`stalled` is computed server-side rather than left to the caller, because the sweep interval is configuration a dashboard does not have — and "how long is too long" is not a judgement worth re-deriving from timestamps in three places. The threshold is three missed intervals rather than one: a sweep that overruns, or a tick skipped under load, is normal and must not page anyone.'},{type:`warn`,text:"The endpoint returns **200 even when degraded**, and that is deliberate. It is not a readiness gate: a stuck cleanup sweep is an operational problem, not a reason to pull a healthy server out of the load balancer and send its traffic to replicas running the same stuck code. Alert on `status` being `degraded`, or on a specific job's `stalled` — never wire this to a liveness probe."},{type:`h`,id:`logs`,text:`Logs`},{type:`p`,text:"`RUST_LOG` sets verbosity and filtering. Keep it narrow in production — `info` is the intended level, and raising it exposes internal module structure without telling you much you can act on."},{type:`warn`,text:`Two things are never in a log line, by construction: **plaintext credentials** and **token strings**. SDKs wrap secrets in redacting types, and error messages are explicitly forbidden from carrying tokens in messages, context fields or stack traces. If you find either in a log, that is a security bug worth reporting.`},{type:`p`,text:`The rate limiter emits observability lines of its own — including whether the shared cross-replica counter is reachable — which is the signal to watch when limits appear not to be applying as configured.`},{type:`h`,id:`telemetry`,text:`Client-side telemetry hooks`},{type:`p`,text:`Every SDK exposes telemetry hooks so a client can record request latency, retries, refresh attempts and authorization decisions into whatever tracing system it already uses. This is where per-request visibility lives — the hooks are in the client, where the span you want to attach to already exists.`},{type:`h`,id:`metrics`,text:`On metrics`},{type:`note`,text:"AXIAM does not currently expose a Prometheus-style `/metrics` endpoint. Today the sources of operational truth are the probes, the structured logs, the audit trail and client-side telemetry. If you need scrape-based metrics now, derive them from log ingestion rather than expecting an endpoint that is not there."},{type:`h`,id:`audit-as-obs`,text:`The audit trail is observability too`},{type:`p`,text:'For questions about *who did what*, the audit log is the authoritative source and is queryable through the API — it is not merely a compliance artefact. Authorization decisions carry their reason (`no_grant` versus `denied_by_rule`), which is usually the fastest way to answer "why was this refused?" without reconstructing the role model by hand.'},{type:`h`,id:`capacity`,text:`Capacity signals`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Usually means`],rows:[[`503s on login under load`,"The Argon2id concurrency semaphore is shedding. Raise `AXIAM__AUTH__MAX_CONCURRENT_HASHES` only if you have the memory — each permit holds a ~19 MiB arena."],[`429s from machine clients that share an egress IP`,"Per-IP buckets colliding behind NAT. This is the `gateway` rate-limit profile's exact use case."],[`Authorization checks slow before anything else does`,`Database CPU. Those paths scale with it — measured ~90% from a second pair of DB cores — while token issuance does not.`],["`/ready` flapping",`SurrealDB connectivity, not the server. Check the database before restarting replicas.`]]}]},{slug:`hardening`,section:`Operate`,navLabel:`Production hardening`,title:`Production hardening checklist`,intro:`Work through this before a deployment carries real identity traffic. Every item is something AXIAM leaves to you on purpose, because it cannot be decided from inside the process.`,verifiedRelease:r,blocks:[{type:`p`,text:`Each row is one check, the setting or action it turns on, and what goes wrong if you skip it. The last column is the point: an item whose consequence you cannot state is an item nobody will prioritise.`},{type:`h`,id:`secrets`,text:`Secrets`},{type:`table`,proseFirstCol:!0,headers:[`Check`,`Setting or action`,`If you skip it`],rows:[[`Secrets come from a manager, not the environment`,"`AXIAM__AUTH__SECRET_PROVIDER` = `vault` or `file`","`env` is a development default. Key material ends up in a process listing, a compose file and a CI log."],[`OPAQUE setup key is backed up`,"Back up `opaque_setup_key`",`Losing it is a forced password reset for every user in every tenant. There is no recovery path.`],[`JWT key pair is backed up and actually a pair`,"Back up `jwt_private_key_pem`; verify the public key matches",`A mismatch presents as a confusing outage — tokens are minted and then rejected — rather than as a clear error.`],[`Vault auto-unseal is configured before go-live`,`Vault auto-unseal`,`Any restart leaves AXIAM unable to start until enough keyholders are woken up.`],[`Vault trust anchor is set if it uses a private CA`,"`AXIAM__AUTH__VAULT_CA_CERT_PATH`",`rustls does not trust an internal PKI, and the server dies at startup with a bare transport error that names nothing.`],[`Unseal shares are distributed and the root token revoked`,`Split shares across people; revoke root after seeding`,`One person holding every share is the failure mode the threshold exists to prevent.`],[`No key material in git, images, manifests or CI variables`,`Audit history, not just the working tree`,`A rotated secret that is still in git history is still a leaked secret.`]]},{type:`h`,id:`network`,text:`Network & TLS`},{type:`table`,proseFirstCol:!0,headers:[`Check`,`Setting or action`,`If you skip it`],rows:[[`TLS 1.3 on every external listener`,"Terminate at the ingress, or `AXIAM__SERVER__TLS__ENABLED` in-process",`The default binds plaintext and assumes something in front of it terminates TLS. If nothing does, nothing warns you.`],[`gRPC is not publicly routed`,`Keep 50051 off the Ingress`,`It is an internal service surface. The shipped manifests deliberately keep it on the ClusterIP service only.`],[`AMQP uses amqps://`,"`AXIAM__AMQP__URL`",`Nothing — this one cannot be skipped. Any other scheme is refused at startup, so a mistake here is a visible configuration error rather than a silent downgrade.`],[`CORS names your actual admin origins`,"`AXIAM__SERVER__CORS_ALLOWED_ORIGINS`",`Empty disables cross-origin requests, which is the safe default — so the risk is over-broadening it, not forgetting it.`],[`Proxy hop count is accurate`,"`AXIAM__RATE_LIMIT__TRUSTED_HOPS`",`Every client shares one apparent address, per-IP limits become meaningless, and one abusive caller throttles everybody.`]]},{type:`h`,id:`identity`,text:`Identity`},{type:`table`,proseFirstCol:!0,headers:[`Check`,`Setting or action`,`If you skip it`],rows:[[`MFA enforced on administrative accounts`,`Tenant MFA policy; prefer passkeys over TOTP where browsers allow`,`The accounts that can grant every other permission are protected by a password alone.`],[`A second super-admin exists`,`Create one deliberately`,`Bootstrap cannot be run again to rescue you from losing the first.`],[`Bootstrap variable unset after bootstrap`,"Unset `AXIAM_BOOTSTRAP_ADMIN_EMAIL`",`It is inert afterwards, so this is hygiene rather than exposure — leaving it implies a live gate that is not there.`],[`Password policy is set on the organization baseline`,"`hibp_check_enabled`, plus a sensible `min_length`",`Known-breached passwords are accepted, and the strongest hash in the world does not help against a password already in a wordlist.`],[`Seeded roles reviewed`,"Audit who holds `super-admin`","`super-admin` should have very few holders. Most administrators want `admin`; most humans want neither."],[`Machine access uses bound certificates`,`Service accounts with certificates over shared secrets`,`A shared secret is copyable, does not expire on its own, and leaves no evidence of which copy was used.`],[`SCIM provisioning tokens treated as admin credentials`,`Rotate and store like an admin password`,"`scim:provision` can set any user's password in the tenant, including an administrator's. See [SCIM provisioning](#/docs/scim)."]]},{type:`h`,id:`limits`,text:`Rate limits & capacity`},{type:`table`,proseFirstCol:!0,headers:[`Check`,`Setting or action`,`If you skip it`],rows:[[`Rate-limit posture matches the topology`,"`AXIAM__RATE_LIMIT__PROFILE` — `internet`, `gateway` or `mesh`",`A NAT-fronted fleet throttles itself on per-IP buckets; a public deployment on mesh ceilings has effectively none.`],[`Bucket key left at ip unless the edge authenticates`,"`AXIAM__RATE_LIMIT__KEY`","`client_id` is minted by the caller, which makes it a fairness control and not an abuse control."],[`Capacity sized against the right bottleneck`,`Database CPU for check-heavy traffic; limits for token-heavy traffic`,`Tuning the wrong one moves no numbers and costs a maintenance window.`],[`Caches measured, not assumed`,`Enable, then read the logged hit rate`,`The decision cache is transformative on gRPC checks and marginal on REST ones, because REST carries a session lookup gRPC does not.`]]},{type:`h`,id:`ops`,text:`Operations`},{type:`table`,proseFirstCol:!0,headers:[`Check`,`Setting or action`,`If you skip it`],rows:[[`Probes wired the right way round`,"Liveness on `/health`, readiness on `/ready`","Pointing liveness at `/ready` turns a database blip into a restart loop that makes the outage worse."],[`Scheduled jobs are alerted on`,"Alert on `stalled` from `/health/jobs`",`A sweep that stops running raises nothing. For GDPR erasure the first symptom can be a regulator's question.`],[`Logs survive the pod`,"`RUST_LOG=info`, shipped off-host",`The audit trail persists; your diagnostic context does not.`],[`Audit retention decided deliberately`,`Set a retention policy`,`The log only grows. Nothing prunes it for you, and it is append-only by design.`],[`Back-channel logout relying parties registered in advance`,`Register before you need them`,`An incident is the wrong time to discover that revoking an account does not sign it out anywhere else.`],[`Reactor failure policy chosen explicitly`,`Decide fail-open versus fail-closed per hook, and monitor it`,`Fail-closed makes a reactor outage your outage; fail-open makes it a silently unenforced control. Not choosing picks one anyway.`]]},{type:`warn`,text:`One item this checklist cannot give you: AXIAM is pre-1.0 and its security posture is a self-assessment backed by tests and a threat model, not a certified third-party audit. Weigh that against what the deployment is protecting.`},{type:`cards`,cards:[{title:`The security model →`,body:`Threat model, cryptography choices and the shared-responsibility boundary.`,to:`security`},{title:`Configuration reference →`,body:`Every variable, its default, and a worked example value.`,to:`docs`,doc:`configuration`}]}]},{slug:`troubleshooting`,section:`Operate`,navLabel:`Troubleshooting`,title:`Troubleshooting`,intro:`The failures that come up most often, what they actually mean, and the fix.`,verifiedRelease:r,blocks:[{type:`h`,id:`startup`,text:`The server will not start`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Cause`,`Fix`],rows:[["`AXIAM__AUTH__SECRET_PROVIDER=... is not a known provider`","A typo in the provider name. AXIAM refuses rather than falling back to `env`.","Use exactly `env`, `file` or `vault`."],["`SECRET_PROVIDER=vault requires AXIAM__AUTH__VAULT_ADDR`",`The vault provider is selected but not fully configured.`,"Set `VAULT_ADDR` and `VAULT_TOKEN`; `VAULT_MOUNT` and `VAULT_PATH` have defaults."],[`Startup aborts on the TLS listener`,`Client authentication is enabled but the CA path is unset, unreadable, empty or malformed. A misconfigured mTLS server never starts serving.`,"Fix `AXIAM__SERVER__TLS__CLIENT_CA_PATH`, or set `CLIENT_AUTH=off`."],[`AMQP refused at startup`,"`AXIAM__AMQP__URL` does not use `amqps://`. AMQP is TLS-only.","Use the `amqps://` scheme."],[`The OIDC issuer is rejected`,"`AXIAM__AUTH__OAUTH2_ISSUER_URL` is a path, not an origin.","Use an origin — `https://iam.acme.dev`, not `https://iam.acme.dev/acme`."],[`Vault is sealed after a restart`,`No auto-unseal is configured.`,`Configure auto-unseal. Until then, unseal manually with the key threshold.`]]},{type:`h`,id:`vault`,text:`Vault will not come up`},{type:`p`,text:`Vault is the production secret provider, and the failures below are the ones actually hit while getting the shipped production stack running — each one presents as something other than what it is.`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Cause`,`Fix`],rows:[[`The server panics at startup with a bare transport error to Vault`,`Vault's certificate is issued by an internal PKI. rustls compiles its roots in, so a private CA is not trusted and the failure surfaces as an unhelpful transport error rather than as a certificate problem.`,"Point `AXIAM__AUTH__VAULT_CA_CERT_PATH` at the issuing CA."],["Vault restart-loops on `error loading TLS cert`",`The listener key file is mode 0600 and unreadable to the uid the container runs as.`,`Make the key readable by the container's user, not only by the host user that generated it.`],[`Every run after a failed initialisation wedges`,`An initialisation that failed mid-write left an empty state file behind, which later runs then treated as real.`,"Drive initialisation from Vault's own `sys/init` status rather than from the presence of a state file, and validate before writing it."],[`Vault cannot be reached from the host to initialise or unseal it`,`Its port is published on loopback only.`,`Run the initialise, unseal and seed steps from the host, or publish the port where the tooling can reach it.`]]},{type:`note`,text:"A compose stack that will not start at all, with no service logs to read, is worth checking for shell-style interpolation that Compose does not implement: `${VAR:default}` is not valid Compose syntax, and a file using it fails before anything runs."},{type:`h`,id:`database`,text:`Database`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Cause`,`Fix`],rows:[[`Data vanishes on restart`,`SurrealDB is running its in-memory datastore. AXIAM requires a persistent engine.`,`Use a persistent engine; the shipped compose files and the Kubernetes StatefulSet already do.`],[`A startup WARN saying the storage engine could not be attested`,`Expected, and not a misconfiguration. SurrealDB publishes no datastore identity over the wire, so the attestation cannot confirm the engine either way.`,`Nothing. The hard guard exists and refuses a memory datastore as soon as a SurrealDB release exposes the engine name.`]]},{type:`h`,id:`jobs-stopped`,text:`Cleanup work has silently stopped`},{type:`p`,text:`Erasure requests that never complete, certificates that never expire, replay tables that grow without bound — these share a cause and a check. A scheduled sweep that stops running raises nothing, so the symptom is always downstream and always late.`},{type:`code`,code:`curl -s localhost:8090/health/jobs | jq '.status, .jobs[] | select(.stalled)'`},{type:`p`,text:"See [scheduled-job liveness](#/docs/observability) for the response shape and what `stalled` means. If a job shows `consecutive_failures` climbing, `last_error` is the reason."},{type:`h`,id:`config`,text:`A setting has no effect`},{type:`warn`,text:"The most common cause by far: a **single underscore** after the prefix. `AXIAM_DB__USERNAME` is silently ignored and the in-code default wins; `AXIAM__DB__USERNAME` is read. The one exception is `AXIAM_BOOTSTRAP_ADMIN_EMAIL`, which is genuinely single-underscore because it is read directly rather than through the layered config."},{type:`list`,items:["`AXIAM__DB__URL` takes a bare `host:port`, **not** a URL scheme. The engine resolves a scheme as a hostname and fails.",`Rate limits appearing not to apply across replicas: the shared counter is write-behind, so cross-replica enforcement is eventual. Check the limiter's own log lines.`,"Limits applying to everyone at once: `AXIAM__RATE_LIMIT__TRUSTED_HOPS` is unset behind a proxy, so every client looks like the proxy's IP."]},{type:`h`,id:`auth-issues`,text:`Authentication problems`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Cause`,`Fix`],rows:[["`403` from bootstrap",`The gate is not satisfied — email mismatch, or a missing/consumed setup token.`,`The error names which. Check the variable is set on the *server* process and matches exactly.`],["`409` from bootstrap",`Bootstrap already completed. It is one-shot, enforced by a uniqueness invariant.`,`Log in as the existing admin; create further tenants through the API.`],[`Every OPAQUE login fails after a config change`,"`opaque_setup_key` changed or is being read from a different provider, so tenant OPRF seeds cannot be decrypted.",`Restore the original key. Without it, every user needs a password reset.`],[`OPAQUE login fails for everyone in one tenant`,"`opaque_mode` was set to `required` before users enrolled. Nobody can be enrolled retroactively.","Set it back to `optional`, let enrolment complete, then flip."],[`Tokens stop verifying after a restart`,`The JWT keypair changed, or the private and public keys are not a pair.`,`Restore the original keypair from your secret store.`],[`Passkey registration buttons never appear`,`The browser does not support WebAuthn, or the page is not on a secure origin.`,`WebAuthn requires HTTPS (or localhost). Other sign-in methods are unaffected.`],[`Passkeys are rejected after setting an attestation policy`,"Platform authenticators commonly attest as `none` for privacy reasons.",`A strict attestation policy is a security-key-only policy in practice. Decide which you meant.`]]},{type:`h`,id:`authz-issues`,text:`Authorization problems`},{type:`table`,proseFirstCol:!0,headers:[`Symptom`,`Cause`,`Fix`],rows:[[`A user is denied despite holding an allowing role`,`A **deny** grant applies — possibly inherited from an ancestor resource or through a group. Deny beats every allow.`,`Find the deny and narrow it. Adding another allow will not help; that is the design.`],["Denied with `no_grant` rather than `denied_by_rule`",`Nothing granted the action at all — default deny, not an explicit refusal.`,`Grant the permission, or check that the resource id and action name are what you think they are.`],[`A scoped allow behaves as if it were not there`,`A resource-level deny with no scopes is a wildcard and masks every scope for that action.`,`Narrow the deny to the specific scopes you meant to exclude.`],[`A revocation seems not to take effect`,`Almost never the decision cache — access-narrowing mutations invalidate immediately. Check group membership and inherited roles first.`,`Trace the role model; the TTL is a backstop, not the mechanism.`]]},{type:`h`,id:`build`,text:`Build problems`},{type:`list`,items:["**`libxml2` headers missing** — the default `saml` feature links `libxml`. Build with `--no-default-features`, which is what CI's *Build (SAML off)* job does.","**MSRV complaints from `axiam-opaque`** — it states its own floor (1.88) rather than inheriting the workspace's, because it is vendored into SDKs with lower MSRVs. That is deliberate."]},{type:`note`,text:`Not covered here? The authorization decision reason, the audit trail and the limiter's log lines answer most "why did that happen" questions between them. For a suspected security issue, use the private advisory process rather than a public issue.`}]}],g=`https://github.com/ilpanich/axiam/blob/main/sdks/CONTRACT.md`,_={1:`#§1-method-naming-map`,2:`#§2-error-taxonomy`,3:`#§3-csrf-behavior`,4:`#§4-cookie-jar-requirement`,5:`#§5-tenant--organization-context-contract`,6:`#§6-tls-policy`,7:`#§7-sensitivet-requirement`,8:`#§8-amqp-hmac-contract`,9:`#§9-single-flight-refresh-guard`,10:`#§10-middleware--route-guard-interface`,11:`#§11-declarative-authorization-helpers`,12:`#§12-oidc--sso-relying-party-helpers`,13:`#§13-webhook-signature-verification`,14:`#§14-device-authorization-grant-rfc-8628`,15:`#§15-token-exchange-rfc-8693`,16:`#§16-retry-policy-d5`,17:`#§17-client-side-decision-memo-d5`,18:`#§18-deterministic-shutdown-d5`,19:`#§19-telemetry-hooks-d5`,20:`#§20-uma-20--protection-api-and-ticket-grant-x2`,21:`#§21-fapi-20-profile-and-mtls-client-credentials-x5`,22:`#§22-reactors--amqp-extension-actors-x1`,23:`#§23-opaque-rfc-9807`,24:`#§24-webauthn-and-passkeys-w1`,25:`#§25-account-lifecycle-and-mfa-enrolment-w2`,26:`#§26-pushed-authorization-requests-rfc-9126`,"1.1":`#§11-grpc-only-operations`,"3a":`#§3a-resource-server-middleware-csrf-inbound`,"5.1":`#§51-organization-context-required-for-login-and-refresh`,"6.1":`#§61-client-certificate-authentication-mtls`,"8b":`#§8b-amqp-transport-security-a6`,"10.1":`#§101-minimum-local-verification-set-normative`,"10.2":`#§102-revocation-posture-differs-per-transport-informative-must-be-documented`,"10.3":`#§103-sender-constrained-tokens-over-grpc-contract-117-normative`,"11.1":`#§111-canonical-helper-vocabulary`,"11.2":`#§112-semantics-normative-identical-in-all-sdks`,"12.1":`#§121-canonical-operation-set-and-endpoint-map`,"12.2":`#§122-per-language-naming-map`,"12.3":`#§123-cross-cutting-rules-normative-identical-in-all-sdks`,"12.4":`#§124-id-token-validation-checklist-normative-for-oidc_exchange`,"12.5":`#§125-sensitivet-applicability`,"12.6":`#§126-swift-c-and-c-ported--contract-111`,"12.7":`#§127-logout-helpers-b5`,"12.7.1":`#§1271-canonical-operation-set`,"12.7.2":`#§1272-logout_url-rules-normative`,"12.7.3":`#§1273-verify_logout_token-rules-normative`,"12.7.4":`#§1274-per-language-naming-map`,"12.7.5":`#§1275-sensitivet-applicability`,"12.7.6":`#§1276-required-tests`,"14.1":`#§141-canonical-operation-set-and-endpoint-map`,"14.2":`#§142-polling-normative--the-part-implementations-get-wrong`,"14.3":`#§143-device_login--the-composed-helper`,"14.4":`#§144-per-language-naming-map`,"14.5":`#§145-sensitivet-applicability`,"14.6":`#§146-required-tests`,"15.1":`#§151-canonical-operation-and-endpoint-map`,"15.2":`#§152-semantics-normative`,"15.3":`#§153-error-mapping`,"15.4":`#§154-per-language-naming-map`,"15.5":`#§155-sensitivet-applicability`,"15.6":`#§156-required-tests`,"15.7":`#§157-external-idp-subject-tokens-x4`,"16.1":`#§161-the-policy-normative--every-value-here-is-binding`,"16.2":`#§162-what-is-eligible-normative`,"16.3":`#§163-which-failures-retry-normative`,"16.4":`#§164-interaction-with-the-fail-closed-rule`,"16.5":`#§165-observability`,"16.6":`#§166-per-language-naming-map`,"16.7":`#§167-required-tests`,"17.1":`#§171-semantics-normative`,"17.2":`#§172-per-language-naming-map`,"17.3":`#§173-required-tests`,"18.1":`#§181-semantics-normative`,"18.2":`#§182-per-language-naming-map`,"18.3":`#§183-required-tests`,"19.1":`#§191-events-normative`,"19.2":`#§192-rules-normative`,"19.3":`#§193-per-language-naming-map`,"19.4":`#§194-required-tests`,"20.1":`#§201-canonical-operations-and-endpoint-map`,"20.2":`#§202-semantics-normative`,"20.3":`#§203-the-www-authenticate-uma-challenge`,"20.4":`#§204-error-mapping`,"20.5":`#§205-per-language-naming-map`,"20.6":`#§206-sensitivet-applicability`,"20.7":`#§207-required-tests`,"21.1":`#§211-what-an-sdk-must-do-normative`,"21.2":`#§212-client-registration-fields-informative`,"21.3":`#§213-authenticating-as-an-mtls-client-informative`,"21.4":`#§214-rfc-9207-iss-on-authorization-responses-informative-act-on-it`,"21.5":`#§215-discovery-additions-informative`,"21.6":`#§216-what-is-not-in-this-contract-version`,"21.7":`#§217-dpop-sender-constrained-tokens-contract-116-rfc-9449`,"21.7.1":`#§2171-what-a-dpop-bound-request-looks-like`,"21.7.2":`#§2172-verifying-a-proof-normative-for-any-sdk-that-claims-to`,"21.7.3":`#§2173-declining-is-a-supported-answer`,"21.8":`#§218-private_key_jwt-client-authentication-contract-116-rfc-7523-§22`,"21.9":`#§219-per-sdk-dpop-posture`,"22.1":`#§221-topology`,"22.2":`#§222-message-security--§8-in-both-directions`,"22.3":`#§223-the-event-server--reactor`,"22.4":`#§224-the-reply-reactor--server`,"22.5":`#§225-the-event-registry-and-the-mutable-field-allow-lists`,"22.6":`#§226-ordering-mutation-merge-and-the-chain`,"22.7":`#§227-hot-path-exclusion-normative-must-not`,"22.8":`#§228-timeouts-failure-policy-and-the-budget`,"22.9":`#§229-registration-informative--the-admin-surface`,"22.10":`#§2210-the-sdk-runtime-helper`,"22.11":`#§2211-swift-c-and-c--the-protocol-core-and-the-transport-that-is-not-bundled`,"22.12":`#§2212-sensitivet-applicability`,"22.13":`#§2213-required-tests`,"22.14":`#§2214-declarative-handler-binding-contract-122`,"22.14.1":`#§22141-required-tests`,"23.0":`#§230-what-this-is-for-and-what-it-is-not-for`,"23.0.1":`#§2301-why-srp-was-replaced-in-one-paragraph-an-sdk-readme-can-quote`,"23.1":`#§231-one-implementation-not-eleven--and-what-that-means-for-an-sdk`,"23.2":`#§232-method-naming-map`,"23.3":`#§233-the-exchanges`,"23.4":`#§234-normative-rules`,"23.5":`#§235-wire-shapes`,"23.6":`#§236-server-policy-an-sdk-must-understand`,"23.7":`#§237-required-tests`,"23.8":`#§238-per-sdk-posture`,"24.0":`#§240-the-division-of-labour-normative--everything-below-follows-from-it`,"24.1":`#§241-canonical-operation-set-and-endpoint-map`,"24.2":`#§242-the-two-authentication-ceremonies-are-different-flows-not-one-with-a-flag`,"24.3":`#§243-credential-adoption-normative`,"24.4":`#§244-error-taxonomy-normative`,"24.5":`#§245-sensitivet-applicability`,"24.6":`#§246-running-the-ceremony-the-json-bridge-and-the-linked-api-helpers`,"24.7":`#§247-per-language-naming-map-and-per-sdk-posture`,"24.8":`#§248-required-tests`,"25.1":`#§251-canonical-operation-set-and-endpoint-map`,"25.2":`#§252-the-two-mfa-enrolment-paths-are-different-and-confusing-them-locks-users-out`,"25.3":`#§253-sensitivet-applicability-and-the-one-field-that-will-be-got-wrong`,"25.4":`#§254-password-reset-and-what-password_reset_context-is-actually-for`,"25.5":`#§255-per-language-naming-map`,"25.6":`#§256-required-tests`,"26.1":`#§261-canonical-operation-set-and-endpoint-map`,"26.2":`#§262-semantics-normative`,"26.3":`#§263-error-mapping`,"26.4":`#§264-per-language-naming-map`,"26.5":`#§265-sensitivet-applicability`,"26.6":`#§266-required-tests`};function v(e){let t=_[e];return t?`${g}${t}`:g}var y=`✓`,b=`—`,x=`https://github.com/ilpanich/axiam/blob/main`,S=`${x}/docs/compliance`,C=`${x}/docs/api`,w=`${x}/docs/admin`,T=[{id:`rust`,cells:[y,y,y,y,y,y,y,y,y]},{id:`typescript`,cells:[y,y,y,y,y,y,y,y,y]},{id:`python`,cells:[y,y,y,y,y,y,y,y,y]},{id:`java`,cells:[y,y,y,y,y,y,y,y,y]},{id:`csharp`,cells:[y,y,y,y,y,y,y,y,y]},{id:`php`,cells:[y,y,y,y,y,y,y,y,y]},{id:`go`,cells:[y,y,y,y,y,y,y,y,y]},{id:`kotlin`,cells:[y,b,`reactor exchange only`,y,y,y,y,y,y]},{id:`swift`,cells:[y,b,b,y,y,y,`transport yours`,y,y]},{id:`c`,cells:[y,b,b,y,y,y,`transport yours`,y,y]},{id:`cpp`,cells:[y,b,b,y,y,y,`transport yours`,y,y]}],E=e=>t.find(t=>t.id===e),D=[`rust`,`typescript`,`python`,`go`,`java`].map(E).filter(e=>!!e),O=e=>D.map(t=>({label:t.name,code:e(t)})),k=[{slug:`compliance`,section:`Reference`,navLabel:`Standards & compliance`,title:`Standards & compliance`,intro:`Which specifications AXIAM implements, how conformance is evidenced, and — just as importantly — what the evidence is not.`,verifiedRelease:r,blocks:[{type:`h`,id:`honesty`,text:`What these claims mean`},{type:`warn`,text:`Everything on this page is a **self-assessment backed by test evidence**, not a third-party certification. Each conformance matrix in the repository maps a normative MUST to the test that exercises it, so a claim can be checked rather than believed. That is stronger than a marketing bullet and weaker than an audit — treat it accordingly.`},{type:`h`,id:`protocols`,text:`Protocol conformance`},{type:`table`,proseFirstCol:!0,headers:[`Specification`,`Coverage`,`Evidence`],rows:[[`RFC 6749 — OAuth 2.0`,`Authorization code, client credentials and refresh grants, with the error semantics.`,`[OAuth2 RFC compliance matrix](${S}/oauth2-rfc-compliance.md) — MUST by MUST, naming the test for each row.`],[`RFC 7636 — PKCE`,`S256 challenge on the authorization code grant.`,`[Same matrix](${S}/oauth2-rfc-compliance.md).`],[`RFC 7009 — Token revocation`,"`/oauth2/revoke`.",`[Same matrix](${S}/oauth2-rfc-compliance.md).`],[`RFC 7662 — Token introspection`,"`/oauth2/introspect`.",`[Same matrix](${S}/oauth2-rfc-compliance.md).`],[`RFC 8628 — Device authorization grant`,`The full non-interactive path.`,`[Device flow reference](${C}/device-flow.md).`],[`RFC 8693 — Token exchange`,`Delegation and impersonation, narrowing-only.`,`[Token exchange reference](${C}/token-exchange.md).`],[`RFC 9126 — Pushed authorization requests`,"`/oauth2/par`, requirable per client.",`[Integration test suite](${x}/crates/axiam-api-rest/tests/par_test.rs), over [the PAR implementation](${x}/crates/axiam-oauth2/src/par.rs).`],[`OpenID Connect Core 1.0 & Discovery 1.0`,"ID tokens, userinfo, discovery. `alg: none` is excluded from the discovery document and rejected at verification.",`[OIDC Core conformance matrix](${S}/oidc-conformance.md).`],[`OIDC RP-Initiated & Back-Channel Logout 1.0`,`Session-scoped logout in both directions.`,`[Logout reference](${C}/logout.md).`],[`UMA 2.0`,`Protection API and the ticket grant.`,`[UMA reference](${C}/uma.md), and [CONTRACT §20](${v(`20`)}).`],[`FAPI 2.0 Security Profile (Final)`,`An opt-in constraint bundle that a client cannot half-apply.`,`[FAPI 2.0 profile guide](${w}/fapi2-profile.md).`],[`SCIM 2.0 — RFC 7643 / 7644`,`Users and Groups CRUD plus PATCH; bulk and complex filters deliberately unsupported.`,`[SCIM provisioning reference](${C}/scim-provisioning.md).`],[`WebAuthn / FIDO2`,`Registration and authentication, with MDS3-backed attestation policy.`,`[Authenticator policy guide](${w}/authenticator-policies.md).`],[`RFC 9807 — OPAQUE`,`One audited implementation bound by every SDK, tested against shared vectors.`,`[Shared test vectors](${x}/sdks/opaque-test-vectors.json), and [CONTRACT §23](${v(`23`)}).`]]},{type:`h`,id:`security`,text:`Security standards`},{type:`table`,proseFirstCol:!0,headers:[`Standard`,`Posture`,`Evidence`],rows:[[`OWASP ASVS 4.0.3 Level 2`,`Control-by-control checklist over V2, V3, V4, V6, V7, V8, V9, V10 and V14. Every in-scope control carries an explicit status — Pass, N/A or Deferred — with the deferrals tracked by finding id rather than left blank.`,`[ASVS L2 checklist](${S}/asvs-l2-checklist.md), with the deferrals in the [findings register](${S}/FINDINGS.md).`],[`GDPR`,`Data export (Art. 15) and erasure (Art. 17) endpoints, with audit-actor pseudonymisation so an append-only trail can coexist with a right to erasure. Exports can be PGP-encrypted.`,`[GDPR compliance record](${S}/gdpr-compliance.md) — export completeness, erasure durability and consent, article by article.`],[`ISO 27001`,`Targeted through access control, cryptography and audit logging — the technical controls, not the management system, which is an organizational matter AXIAM cannot supply.`,`[Security audit](${x}/claude_dev/security-audit.md) — which also states the ISMS certification scope it deliberately excludes.`],[`EU Cyber Resilience Act`,`Secure-by-default posture and a private vulnerability-disclosure process.`,`[Security audit](${x}/claude_dev/security-audit.md) for the essential-requirements mapping; [SECURITY.md](${x}/SECURITY.md) for the disclosure process.`]]},{type:`h`,id:`crypto`,text:`Cryptography at a glance`},{type:`table`,proseFirstCol:!0,headers:[`Use`,`Algorithm`],rows:[[`Password hashing`,`Argon2id, OWASP-recommended parameters, with a server-side pepper`],[`Password-authenticated key exchange`,`OPAQUE (RFC 9807), ristretto255-SHA512, Argon2id or scrypt as the client KSF`],[`Token signing`,`EdDSA (Ed25519)`],[`Secrets at rest`,`AES-256-GCM — MFA secrets, CA keys, federation secrets, OPRF seeds, email`],[`Webhook signatures`,`HMAC-SHA256`],[`Audit signing & exports`,`OpenPGP`],[`Certificates`,`X.509 with RSA-4096 or Ed25519`],[`Transport`,`TLS 1.3 minimum; the native listener is TLS 1.3-only by policy`]]},{type:`h`,id:`supply`,text:`Supply chain & CI`},{type:`list`,items:[`The OpenAPI document is **drift-gated** — a PR fails if the committed spec diverges from a fresh export from the server.`,"The protobuf contract is guarded by `buf lint` and `buf breaking` on every change.",`Crate-layering invariants are enforced in CI: dependencies point inward, and adding a crate without placing it in the layering table is itself a failure.`,`Remediation records are verified to resolve to commits reachable from the default branch — a claimed fix on an unmerged branch does not count as shipped.`,`Every SDK is tested against the same cross-language behavioural contract and the same OPAQUE test vectors.`]},{type:`cards`,cards:[{title:`The security model →`,body:`Threat model, trust boundaries and shared responsibility.`,to:`security`},{title:`Production hardening →`,body:`What compliance leaves to you, as a checklist.`,to:`docs`,doc:`hardening`}]}]},{slug:`sdks`,section:`Reference`,navLabel:`Client SDKs`,title:`Client SDKs`,intro:`Eleven official client libraries, all conforming to one cross-language behavioral contract — so an integration ported between languages keeps the same semantics.`,verifiedRelease:r,blocks:[{type:`h`,id:`contract`,text:`One contract, many languages`},{type:`p`,text:`AXIAM ships SDKs for Rust, TypeScript, Python, Java, C#, PHP, Go, Kotlin, Swift, C and C++. Each lives in its own repository, and each vendors the same [CONTRACT.md](https://github.com/ilpanich/axiam/blob/main/sdks/CONTRACT.md), OpenAPI document and protobuf definitions — so behaviour is identical whichever language you pick, and a difference between two SDKs is a bug in one of them rather than a matter of taste.`},{type:`p`,text:`The contract is not a style guide. It specifies the error taxonomy, CSRF behaviour, the cookie-jar requirement, tenant and organization context, TLS policy, the redacting secret type, the AMQP HMAC construction, the single-flight refresh guard, route-guard interfaces, declarative authorization helpers, OIDC relying-party helpers, webhook verification, the device grant, token exchange, retry policy, deterministic shutdown, telemetry hooks, UMA, FAPI 2.0, Reactors, OPAQUE, WebAuthn, the account-lifecycle and MFA-enrolment operations and pushed authorization requests.`},{type:`h`,id:`matrix`,text:`What each SDK ships`},{type:`table`,proseFirstCol:!0,headers:[`SDK`,`REST core §1–§7, §9–§13`,`gRPC`,`AMQP §8`,`Device & exchange §14–§15`,`Retry, memo, shutdown, telemetry §16–§19`,`UMA §20`,`Reactors §22`,`OPAQUE §23`,`WebAuthn, lifecycle, PAR §24–§26`],rows:T.map(e=>[E(e.id)?.name??e.id,...e.cells])},{type:`p`,text:`Read the columns as *what the library gives you*, not as tiers. The REST core is universal — every SDK carries the method-naming map, the three error types, CSRF and cookie handling, required tenant context, strict TLS with mTLS, the redacting secret type, single-flight refresh, route guards, declarative authorization, the OIDC relying-party helpers and the webhook verifier. Reactors reach all eleven: the eight managed runtimes bundle an AMQP client, and Swift, C and C++ ship the same protocol core over a transport you supply.`},{type:`note`,text:"Two nuances the table cannot hold. **Kotlin's AMQP is reactor-only** — it implements the v2 HMAC and transport rules for the reactor exchange, but not the async-authz and audit-ingestion message types. And **§21's DPoP posture differs per SDK**: PHP and Swift verify proofs but do not generate them, and C and C++ decline proof verification and reject `jkt`-bound tokens outright, which is what the route-guard rules require of them. CONTRACT §21.9 is the per-SDK index."},{type:`warn`,text:"This table is transcribed from the eleven SDK READMEs at `1.0.0-alpha38` and is a snapshot for orientation. Each README's conformance statement is the SDK's own claim and the thing to check before you depend on a section."},{type:`h`,id:`packages`,text:`Canonical package names`},{type:`p`,text:`Install under exactly these names. Eleven public registries are eleven chances for a typosquat, so pin versions, commit lockfiles and keep dependency scanning on — the threat model carries this as an open, shared-responsibility item.`},{type:`table`,proseFirstCol:!0,headers:[`SDK`,`Registry`,`Package`,`Install`],rows:t.map(e=>[e.name,`[${e.registry}](${e.registryUrl})`,`\`${e.pkg}\``,`\`${e.install}\``])},{type:`h`,id:`login`,text:`Connect and check`},{type:`p`,text:"The shape is the same everywhere: construct a client with an explicit tenant and organization, sign in, ask a question about a resource. Tokens land in `HttpOnly` cookies and the SDK forwards the CSRF token — browser code never touches a token."},{type:`codegroup`,caption:`login and an authorization check`,tabs:O(e=>e.quickstart)},{type:`h`,id:`guards`,text:`Guarding a route`},{type:`p`,text:`Each SDK expresses the same guard in the idiom of its framework — decorator, dependency, middleware, annotation, attribute or macro. The check runs **before** the handler body, so a denied request never reaches your code, and a guard with no tenant configured fails closed rather than admitting everything.`},{type:`codegroup`,caption:`declarative route guards`,tabs:O(e=>e.guardExample)},{type:`h`,id:`guarantees`,text:`What every SDK guarantees`},{type:`list`,items:[`**Tenant is an explicit constructor parameter.** There is no default tenant and no ambient context to get wrong.`,`**Secrets are wrapped in a redacting type** that cannot be printed, logged or serialised by accident.`,"**Browser code never touches a token.** Tokens live in `httpOnly` cookies; the SDK forwards the CSRF token for you.",`**Concurrent 401s collapse into one refresh**, not N — the single-flight guard is normative, not an optimisation.`,`**TLS is strict by default**, with mTLS supported everywhere including the SDKs that ship REST only.`,`**One OPAQUE implementation** is bound by all of them — compiled in, through WebAssembly, or behind a C ABI. No SDK implements OPAQUE itself.`,`**Declarative route guards** in the idiom of the language — attribute, annotation, decorator, macro or middleware.`,`**A webhook verifier** that takes the raw bytes, compares in constant time and fails closed. See [Webhooks](#/docs/webhooks).`]},{type:`h`,id:`pick`,text:`Pick your language`},{type:`cards`,cards:[{title:`All SDKs →`,body:`Install snippets, quickstarts and guard examples for every language.`,to:`sdks`},{title:`Error reference →`,body:`The three error types and the status mapping every SDK implements.`,to:`docs`,doc:`errors`}]}]}],A=[...s,...c,...l,...u,...p,...m,...h,...k],j=[{label:`Getting started`,slugs:[`overview`,`quickstart`,`installation`,`bootstrap`,`concepts`,`tutorial`]},{label:`Authentication`,slugs:[`auth`,`opaque`,`mfa`,`passkeys`,`federation`,`service-accounts`]},{label:`Authorization`,slugs:[`authz`,`rbac`,`deny`,`uma`]},{label:`OAuth2 & OIDC`,slugs:[`oauth2`,`device-flow`,`token-exchange`,`logout`,`fapi2`,`par`]},{label:`APIs & integration`,slugs:[`rest`,`grpc`,`amqp`,`scim`,`webhooks`,`reactors`,`errors`]},{label:`Operate`,slugs:[`deploy`,`configuration`,`secrets`,`settings`,`pki`,`audit`,`observability`,`hardening`,`troubleshooting`]},{label:`Reference`,slugs:[`compliance`,`sdks`]}],M=j.flatMap(e=>e.slugs);function N(e){return A.find(t=>t.slug===e)}var P=e(),F=`overview`,I=()=>{typeof window<`u`&&window.scrollTo(0,0)};function L(){if(typeof window>`u`)return null;let e=/^#\/docs\/([a-z0-9-]+)$/.exec(window.location.hash)?.[1];return e&&N(e)?e:null}function R(e){switch(e.type){case`h`:case`p`:case`note`:case`warn`:return e.text;case`list`:return e.items.join(` `);case`code`:return`${e.caption??``} ${e.code}`;case`table`:return`${e.headers.join(` `)} ${e.rows.flat().join(` `)}`;case`cards`:return e.cards.map(e=>`${e.title} ${e.body}`).join(` `);case`links`:return e.links.map(e=>`${e.label} ${e.note??``}`).join(` `);case`steps`:return e.steps.map(e=>`${e.title} ${e.body} ${e.code??``}`).join(` `);case`api`:return e.endpoints.map(e=>`${e.method} ${e.path} ${e.summary}`).join(` `);case`codegroup`:return e.tabs.map(e=>`${e.label} ${e.code}`).join(` `)}}var z=A.map(e=>({slug:e.slug,navLabel:e.navLabel,section:e.section,title:e.title,intro:e.intro,haystack:[e.title,e.navLabel,e.section,e.intro,...e.blocks.map(R)].join(` `).toLowerCase()}));function B(e){let t=e.trim().toLowerCase();if(t.length<2)return[];let n=t.split(/\s+/);return z.filter(e=>n.every(t=>e.haystack.includes(t))).sort((e,n)=>!e.title.toLowerCase().includes(t)-+!n.title.toLowerCase().includes(t)).slice(0,8)}function V({go:e}){let[t,r]=(0,o.useState)(()=>L()??F),[a,s]=(0,o.useState)(``);(0,o.useEffect)(()=>{let e=()=>{let e=L();e&&r(e)};return window.addEventListener(`hashchange`,e),()=>window.removeEventListener(`hashchange`,e)},[]),(0,o.useEffect)(()=>{},[]);let c=N(t)??A[0],l=(0,o.useMemo)(()=>c.blocks.filter(e=>e.type===`h`).map(e=>({id:e.id,text:e.text})),[c]),u=(0,o.useMemo)(()=>B(a),[a]),d=M.indexOf(c.slug),f=d>0?N(M[d-1]):void 0,p=d>=0&&d<M.length-1?N(M[d+1]):void 0,m=e=>{r(e),s(``),typeof window<`u`&&(window.location.hash=`#/docs/${e}`),I()};return(0,P.jsxs)(`div`,{className:`ax-docs`,children:[(0,P.jsxs)(`aside`,{className:`ax-docs-side`,children:[(0,P.jsxs)(`div`,{style:{margin:`0 0 18px`,padding:`0 12px`},children:[(0,P.jsx)(`input`,{type:`search`,value:a,onChange:e=>s(e.target.value),placeholder:`Search the docs…`,"aria-label":`Search the documentation`,style:{width:`100%`,boxSizing:`border-box`,padding:`8px 11px`,borderRadius:9,border:`1px solid rgba(0,212,255,.2)`,background:`rgba(0,0,0,.28)`,color:`#e2e8f0`,fontSize:13.5,outline:`none`}}),a.trim().length>=2&&(0,P.jsx)(`div`,{style:{marginTop:8},children:u.length===0?(0,P.jsxs)(`div`,{style:{fontSize:12.5,color:`#64748b`,padding:`6px 2px`},children:[`No matches for “`,a.trim(),`”.`]}):u.map(e=>(0,P.jsxs)(`button`,{className:`ax-side`,onClick:()=>m(e.slug),style:{textAlign:`left`},children:[(0,P.jsx)(`span`,{style:{display:`block`},children:e.navLabel}),(0,P.jsx)(`span`,{style:{display:`block`,fontSize:11,color:`#64748b`},children:e.section})]},e.slug))})]}),j.map((e,n)=>(0,P.jsxs)(`div`,{children:[(0,P.jsx)(`div`,{style:{fontSize:11,textTransform:`uppercase`,letterSpacing:`.14em`,color:`#64748b`,margin:n===0?`0 0 10px 14px`:`22px 0 10px 14px`},children:e.label}),e.slugs.map(e=>{let n=N(e);return n?(0,P.jsx)(`button`,{className:`ax-side${e===t?` ax-side-active`:``}`,"aria-current":e===t?`page`:void 0,onClick:()=>m(e),children:n.navLabel},e):null})]},e.label))]}),(0,P.jsxs)(`article`,{style:{minWidth:0},children:[(0,P.jsxs)(`div`,{style:{fontSize:13,color:`#64748b`,marginBottom:10},children:[c.section,` /`,` `,(0,P.jsx)(`span`,{style:{color:`#94a3b8`},children:c.navLabel})]}),(0,P.jsx)(`h1`,{style:{margin:`0 0 8px`,fontSize:`clamp(30px, 5vw, 40px)`,fontWeight:800,letterSpacing:`-.02em`},children:c.title}),(0,P.jsx)(`p`,{style:{fontSize:17,color:`#94a3b8`,margin:`0 0 30px`},children:n(c.intro)}),c.blocks.map((t,n)=>(0,P.jsx)(i,{block:t,go:e},n)),c.verifiedRelease&&(0,P.jsxs)(`p`,{style:{fontSize:12,color:`#64748b`,marginTop:32,paddingTop:16,borderTop:`1px solid rgba(0,212,255,.10)`},children:[`Verified against AXIAM `,c.verifiedRelease,`.`]}),(f||p)&&(0,P.jsxs)(`nav`,{"aria-label":`Documentation pages`,style:{display:`flex`,gap:14,marginTop:44,paddingTop:24,borderTop:`1px solid rgba(255,255,255,.08)`},children:[f&&(0,P.jsxs)(`button`,{className:`glass-card ax-lift`,onClick:()=>m(f.slug),style:{flex:1,padding:16,textAlign:`left`,cursor:`pointer`,border:`1px solid rgba(0,212,255,.14)`,color:`inherit`},children:[(0,P.jsx)(`div`,{style:{fontSize:12,color:`#64748b`,marginBottom:4},children:`← Previous`}),(0,P.jsx)(`div`,{style:{fontWeight:700,color:`#e2e8f0`},children:f.navLabel})]}),p&&(0,P.jsxs)(`button`,{className:`glass-card ax-lift`,onClick:()=>m(p.slug),style:{flex:1,padding:16,textAlign:`right`,cursor:`pointer`,border:`1px solid rgba(0,212,255,.14)`,color:`inherit`},children:[(0,P.jsx)(`div`,{style:{fontSize:12,color:`#64748b`,marginBottom:4},children:`Next →`}),(0,P.jsx)(`div`,{style:{fontWeight:700,color:`#e2e8f0`},children:p.navLabel})]})]})]}),(0,P.jsxs)(`aside`,{className:`ax-docs-toc`,children:[(0,P.jsx)(`div`,{style:{textTransform:`uppercase`,letterSpacing:`.12em`,color:`#64748b`,marginBottom:12,fontSize:11},children:`On this page`}),l.map((e,t)=>(0,P.jsx)(`a`,{href:`#${e.id}`,style:{display:`block`,color:t===0?`#67e8f9`:`#94a3b8`,padding:`5px 0`,...t===0?{borderLeft:`2px solid #00d4ff`,paddingLeft:10,marginLeft:-12}:{}},children:e.text},e.id))]})]})}export{V as default};