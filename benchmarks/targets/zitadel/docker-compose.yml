# Benchmark target: Zitadel (resource-capped reference setup).
#
# Same CPU/memory envelope as the other targets. Zitadel disables the
# resource-owner-password grant by default, so the comparative "login" scenario
# falls back to client_credentials (handled in scenarios/lib/targets.js). TLS mode
# is controlled by ZITADEL_TLS_* set by the runner from the security profile.
#
# Pinned to Zitadel v4 (GA). The OIDC endpoints the adapter uses
# (/oauth/v2/token, /oauth/v2/introspect, /oauth/v2/keys, /oidc/v1/userinfo) are
# standard discovery paths unchanged from v2; `start-from-init`, the masterkey
# flag, and the ZITADEL_DATABASE_*/EXTERNAL* env are all still valid in v4. v4
# does default to the new (v2) login UI, which does not affect these API flows —
# but validate a live v4 container before publishing any head-to-head numbers.
name: bench-zitadel

services:
  zitadel:
    image: ghcr.io/zitadel/zitadel:v4.16.2
    container_name: bench-zitadel
    cpus: ${BENCH_CPUS:-2}
    mem_limit: ${BENCH_MEM:-2048m}
    command: 'start-from-init --masterkey "MasterkeyNeedsToHave32Characters" --tlsMode ${BENCH_ZITADEL_TLS_MODE:-disabled}'
    environment:
      ZITADEL_DATABASE_POSTGRES_HOST: postgres
      ZITADEL_DATABASE_POSTGRES_PORT: "5432"
      ZITADEL_DATABASE_POSTGRES_DATABASE: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_USERNAME: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_PASSWORD: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_SSL_MODE: disable
      ZITADEL_DATABASE_POSTGRES_ADMIN_USERNAME: postgres
      ZITADEL_DATABASE_POSTGRES_ADMIN_PASSWORD: postgres
      ZITADEL_DATABASE_POSTGRES_ADMIN_SSL_MODE: disable
      ZITADEL_EXTERNALSECURE: "${BENCH_ZITADEL_EXTERNAL_SECURE:-false}"
      # External port + published port follow the profile: 8090 plaintext, the TLS
      # port (8443) for https profiles. BENCH_PORT is set by the TLS profile env;
      # empty for p0 so it falls back to BENCH_APP_PORT.
      ZITADEL_EXTERNALPORT: "${BENCH_PORT:-${BENCH_APP_PORT:-8090}}"
      ZITADEL_EXTERNALDOMAIN: localhost
      # Built-in TLS for the https security profiles (tlsMode=enabled). Zitadel
      # then serves HTTPS+h2 directly on its port using the shared throwaway cert
      # (runner/gen-certs.sh, mounted at /certs). Empty/false for p0-plaintext.
      # Unlike AXIAM/Keycloak, Zitadel serves TLS-only when enabled (no plaintext
      # side port), so seeding also goes over https — handled by `just bench-seed`.
      ZITADEL_TLS_ENABLED: "${BENCH_ZITADEL_TLS_ENABLED:-false}"
      ZITADEL_TLS_CERTPATH: "${BENCH_ZITADEL_TLS_CERTPATH:-}"
      ZITADEL_TLS_KEYPATH: "${BENCH_ZITADEL_TLS_KEYPATH:-}"
      # First-init bootstrap of an admin service account + PAT so runner/seed.sh
      # can provision the benchmark client_credentials machine user unattended
      # (Zitadel has no scriptable password grant). These FIRSTINSTANCE_* vars
      # ONLY take effect on the very first init against an empty DB — `bench-down
      # -v` (volume wipe) is required for a re-provision. The PAT is written into
      # the `machinekey` volume; seed.sh reads it via `docker cp` (the distroless
      # image has no shell for `docker exec cat`).
      ZITADEL_FIRSTINSTANCE_PATPATH: /machinekey/pat.txt
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_MACHINE_USERNAME: bench-admin-sa
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_MACHINE_NAME: Bench Admin SA
      ZITADEL_FIRSTINSTANCE_ORG_MACHINE_PAT_EXPIRATIONDATE: "9999-12-31T23:59:59Z"
      # I17(b) (improvement-after-run4-benchmark.md §D): oauth2_password_login
      # is bcrypt-dominated on this target — Zitadel's default bcrypt cost 14
      # (2^14 rounds) makes `login` p50 ~22s, far outside any latency-
      # comparable window against AXIAM's Argon2id/Keycloak's PBKDF2 at their
      # own tuned costs. `HASHER_COST` is Zitadel's documented, runtime-
      # configurable password-hasher setting (self-hosting/manage/configure)
      # — this line WIRES it through as an opt-in override, defaulting to
      # Zitadel's OWN built-in default (14) so an unset BENCH_ZITADEL_HASHER_COST
      # changes nothing about the default matrix (byte-identical to this
      # line being absent). Only takes effect for users created AFTER the
      # value changes (bcrypt cost is baked into the hash at creation time,
      # not a login-time setting) — so a labelled sensitivity cell needs a
      # FRESH instance + fresh seed at the lower cost, not just a live
      # container restart with the var flipped:
      #   BENCH_ZITADEL_HASHER_COST=4 just target=zitadel bench-down
      #   BENCH_ZITADEL_HASHER_COST=4 just target=zitadel bench-up
      #   BENCH_ZITADEL_HASHER_COST=4 just target=zitadel bench-seed
      #   just target=zitadel scenario=oauth2_password_login.js bench-run
      # Label the resulting cell "zitadel-lowcost-bcrypt(cost=4), NON-DEFAULT"
      # in any report that includes it — never compare it head-to-head
      # against the default-cost matrix cell. Prepared but NOT exercised
      # against a live instance from this sandbox (no Zitadel reachable
      # here) — verify the cost actually lands in newly-created users'
      # stored hashes before trusting the resulting latency number.
      ZITADEL_SYSTEMDEFAULTS_PASSWORDHASHER_HASHER_COST: "${BENCH_ZITADEL_HASHER_COST:-14}"
    volumes:
      - machinekey:/machinekey
      # Throwaway TLS certs (shared with the other targets). Read-only; ignored
      # by Zitadel unless ZITADEL_TLS_ENABLED=true (https profiles).
      - ../../profiles/certs:/certs:ro
    ports:
      # 8090 plaintext (p0) or the TLS port 8443 (https profiles) — same container
      # port 8080, whose scheme is decided by tlsMode.
      - "${BENCH_PORT:-${BENCH_APP_PORT:-8090}}:8080"
    depends_on:
      postgres: { condition: service_healthy }
      # A freshly-created named volume is root:root 0755, but the Zitadel image
      # runs non-root and must write the PAT into it — so open the perms first.
      machinekey-init: { condition: service_completed_successfully }
    restart: "no"
    networks: [bench]

  # One-shot: make the machinekey volume writable by Zitadel's non-root user
  # (UID-agnostic 0777) before it runs `start-from-init`. Exits 0 immediately;
  # `up -d --wait` gates on its successful completion.
  machinekey-init:
    image: busybox:1.37
    container_name: bench-zitadel-machinekey-init
    command: ["sh", "-c", "chmod 0777 /machinekey"]
    volumes:
      - machinekey:/machinekey
    restart: "no"
    networks: [bench]

  postgres:
    # A9: postgres:16-alpine is a floating tag (breaks provenance — see the
    # matching note on surrealdb in targets/axiam/docker-compose.yml, and on
    # keycloak's own postgres in targets/keycloak/docker-compose.yml). No
    # registry access here to resolve+pin a real digest, so pin it at run
    # time instead by exporting BENCH_PG_IMAGE, e.g.:
    #   docker pull postgres:16-alpine
    #   export BENCH_PG_IMAGE="postgres@$(docker inspect --format '{{index .RepoDigests 0}}' postgres:16-alpine | cut -d@ -f2)"
    image: ${BENCH_PG_IMAGE:-postgres:16-alpine}
    container_name: bench-zitadel-postgres
    cpus: ${BENCH_DB_CPUS:-2}
    mem_limit: ${BENCH_DB_MEM:-1024m}
    # Minimal, uniform tuning applied identically to Keycloak's postgres (C2) —
    # sized for the standard 1 GiB cap, not a durability change (see the
    # durability-parity note in docs/methodology.md §7). Defaults left
    # otherwise stock so the comparison stays "same DB, sane settings", not
    # "tuned for AXIAM's benefit".
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "effective_cache_size=512MB"
      - "-c"
      - "max_connections=200"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: "no"
    networks: [bench]

volumes:
  machinekey:

networks:
  bench:
    driver: bridge
