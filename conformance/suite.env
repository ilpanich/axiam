# OIDF conformance-suite pin (X5.2).
#
# EVERY value here is pinned deliberately. A conformance result is evidence
# submitted to the OpenID Foundation, and evidence produced by "whatever the
# suite was that afternoon" is not reproducible — the whole point of publishing
# the receipts is that somebody else can regenerate them.
#
# To move the pin: change SUITE_VERSION, run `just conformance-up` (it will pull
# the new image), re-run the plans, and commit the new reports under
# docs/conformance/ ALONGSIDE the old ones rather than over them. A version bump
# that changes a verdict is itself a finding worth keeping.

# ---------------------------------------------------------------------------
# The suite
# ---------------------------------------------------------------------------

# gitlab.com/openid/conformance-suite release tag. The published images are
# tagged with the release version; `latest` is deliberately NOT used.
SUITE_VERSION=release-v5.1.34

# The image the compose file runs. Overridable so a deployment behind an egress
# policy can mirror it into a local registry without editing the compose file.
SUITE_IMAGE=ghcr.io/openid/conformance-suite:${SUITE_VERSION}

# Where the suite's web UI and API land on the host. 8443 collides with the
# benchmark harness's TLS port, so the suite gets 8442 and the mismatch is
# deliberate: the two harnesses must be able to run at once, because iterating
# on a conformance failure while watching its performance cost is a thing you
# actually want to do.
SUITE_PORT=8442
SUITE_BASE_URL=https://localhost.emobix.co.uk:${SUITE_PORT}

# ---------------------------------------------------------------------------
# The system under test
# ---------------------------------------------------------------------------

# AXIAM's issuer as the suite will see it. This MUST be reachable from inside
# the suite's container — `localhost` there is the suite, not AXIAM — which is
# why the default names the docker host gateway rather than loopback.
AXIAM_ISSUER=https://host.docker.internal:8443
AXIAM_TENANT_ID=

# The two clients the FAPI 2.0 OP test plan needs. Both must be registered with
# profile=fapi2; `conformance-register` provisions them and rewrites this file's
# CLIENT_* values in place.
#
# Variant 1 — mutual-TLS client authentication (RFC 8705 §2.1).
CLIENT_MTLS_ID=
CLIENT_MTLS_CERT=certs/client-mtls.crt
CLIENT_MTLS_KEY=certs/client-mtls.key

# Variant 2 — the second client the plan uses for its "client authentication
# type" pair. AXIAM implements the mTLS half of FAPI 2.0's client-auth
# requirement; `private_key_jwt` is not implemented yet (see
# claude_dev/fapi-conformance-runbook.md "Known gaps"), so this is registered as
# `self_signed_tls_client_auth` and the plan is run with the mTLS variant twice
# — once PKI, once self-signed. That is NOT a full FAPI 2.0 submission and the
# runbook says so in as many words.
CLIENT_SELF_SIGNED_ID=
CLIENT_SELF_SIGNED_CERT=certs/client-self-signed.crt
CLIENT_SELF_SIGNED_KEY=certs/client-self-signed.key

# The org/tenant admin credential `conformance-register` uses to provision the
# two clients. Never committed with a real value; `conformance-register` reads
# it from the environment when set and prompts otherwise.
AXIAM_ADMIN_TOKEN=
