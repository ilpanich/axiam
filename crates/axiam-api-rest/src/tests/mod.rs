//! Unit and integration tests for `axiam-api-rest` that live in dedicated
//! test modules under `src/tests/` (as opposed to the inline `#[cfg(test)]`
//! blocks in individual source files).

pub mod route_openapi_parity_test;
