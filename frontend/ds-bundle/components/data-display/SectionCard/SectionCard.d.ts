import * as React from 'react';

/**
 * SectionCard — from frontend@0.0.0.
 */
export interface SectionCardProps {
  [key: string]: unknown;
}

export declare const SectionCard: React.ComponentType<SectionCardProps>;
