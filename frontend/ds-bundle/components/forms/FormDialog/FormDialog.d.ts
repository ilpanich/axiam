import * as React from 'react';

/**
 * FormDialog — from frontend@0.0.0.
 */
export interface FormDialogProps {
  [key: string]: unknown;
}

export declare const FormDialog: React.ComponentType<FormDialogProps>;
