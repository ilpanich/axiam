# K8S-F8: see secrets-owned-by-tofu-surrealdb.yml for why all three of these
# leave the render. Split one file per Secret because kustomize v5.4.2 crashes
# on a multi-document strategic-merge patch listed under `patches:`.
---
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-credentials
  namespace: axiam
$patch: delete
