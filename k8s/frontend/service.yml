apiVersion: v1
kind: Service
metadata:
  name: axiam-frontend
  namespace: axiam
  labels:
    app: axiam
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: axiam
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
