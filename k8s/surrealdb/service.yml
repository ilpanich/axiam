apiVersion: v1
kind: Service
metadata:
  name: surrealdb
  namespace: axiam
  labels:
    app: axiam
    component: surrealdb
spec:
  clusterIP: None
  selector:
    app: axiam
    component: surrealdb
  ports:
    - port: 8000
      targetPort: 8000
      name: http
      protocol: TCP
