syntax = "proto3";

package axiam.v1;

// User service for lookups and credential validation.
service UserService {
  // Retrieve a user by ID.
  rpc GetUser(GetUserRequest) returns (UserResponse);

  // Validate username/email + password without issuing tokens.
  rpc ValidateCredentials(ValidateCredentialsRequest) returns (ValidateCredentialsResponse);
}

message GetUserRequest {
  string tenant_id = 1;
  string user_id = 2;
}

message UserResponse {
  string id = 1;
  string tenant_id = 2;
  string username = 3;
  string email = 4;
  // User status: "active", "inactive", "locked", "pending_verification".
  string status = 5;
  // RFC 3339 timestamps.
  string created_at = 6;
  string updated_at = 7;
}

message ValidateCredentialsRequest {
  string tenant_id = 1;
  string username_or_email = 2;
  string password = 3;
}

message ValidateCredentialsResponse {
  bool valid = 1;
  // User UUID. Empty when credentials are invalid.
  string user_id = 2;
}
