/**
 * Public assets live in `public/` and are served from the site base
 * (`/axiam/` on GitHub Pages). Reference them through `BASE_URL` so the path is
 * correct in every environment — dev server, preview and the deployed site.
 */
export const logoMark = `${import.meta.env.BASE_URL}axiam_logo_mark.png`;
